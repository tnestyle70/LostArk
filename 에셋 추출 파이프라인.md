# Lost Ark UPK 맵 복원에서 이펙트·애니메이션·타임라인 복원까지

> 작성일: 2026-07-30
> 목적: HeartRB 발탄 맵 복원에서 실제로 사용한 UPK 해석 절차를 정본으로 고정하고, 같은 원리를 캐릭터 스킬 이펙트 복원에 적용하는 방법과 현재 확인된 한계를 팀에 전달한다.
> 상태: 맵 절차는 완료 결과를 기록한다. 이펙트 절차는 실제 UPK 샘플로 성립 여부를 검증했지만, 전체 스킬 ID를 시각 이펙트 호출 이벤트에 연결하는 데이터 lane과 모든 Cascade 분포 구조의 decoder는 아직 구현 전이다.

## 0. 결론부터

어제 발탄 맵을 살린 핵심은 `Valtan`이라는 이름이 붙은 파일을 많이 찾은 것이 아니다.

```text
게임 DB로 실제 레벨 family 식별
  -> UModel -nameresolve로 논리 패키지명을 난독화 UPK에 연결
  -> UPK 복호화·LZ4 해제
  -> NameTable / ImportTable / ExportTable / export property 복구
  -> Object reference의 PackageIndex를 끝까지 따라 실제 에셋 경로 확정
  -> Component reference와 Actor/Component Transform 결합
  -> 원본 에셋 추출·재질 의존성 폐쇄·WModel 조리
  -> MapTool placement 생성
```

처음 찾은 `BG_RAD_VALTAN_A`의 StaticMesh 17개는 그 패키지가 **소유한 export**였다. HeartRB 레벨이 실제로 사용하는 메시 목록은 `LV_LUT_HEARTRB_ED_PS`, `SL00`~`SL05`가 가진 **ImportTable과 Component ObjectProperty**에서 나왔다. 이 방법으로 고유 exact 경로 260개와 exact placement 13,091개를 복구했다.

이펙트도 같은 원리로 구할 수 있다. 다만 맵의 한 개 placement가 이펙트에서는 여러 데이터 lane으로 갈라진다.

```text
스킬 ID/분기
  -> 어느 animation을 어떤 play rate로 재생하는가
  -> 어느 시점에 어떤 effect/action notify를 발생시키는가
  -> 어느 ParticleSystem을 생성하는가
  -> emitter / LOD / module / curve / burst / lifetime
  -> material instance / parent material / texture / mesh
  -> bone/socket 또는 world target에 적용할 상대 Transform
  -> trail sample, sound, camera, hit/gameplay timing
```

따라서 `ParticleSystem` 패키지 하나만 풀었다고 “스킬 이펙트 완전 복원”이 되는 것은 아니다. 그러나 이 문서 작성 중 실제 샘플을 읽어 다음 항목까지 복구되는 것을 확인했다.

- ParticleSystem → 5개 emitter → 각 LODLevel → Required/Spawn/Lifetime/Location/Velocity/Size/Color module 연결
- RequiredModule → 정확한 MaterialInstanceConstant 전체 경로
- MIC → parent Material → Texture2D와 scalar/texture parameter
- 캐릭터 AnimSequence의 정확한 clip 이름, 길이, frame 수
- 창술사 skill-key별 무기 trail ParticleSystem, 시작·종료 시각, 1/60초 sample 위치
- Action notify의 ParticleSystem, local rotation, relative location, relative scale
- SkillCam/Matinee의 sequence 길이, animation set, animation 이름, event/move/FOV track 존재

즉, 기존 SourceCatalog의 “이름 목록만 가능하고 Cascade graph는 복구 불가”라는 결론은 **UModel CLI 단독 기준으로는 맞지만 전체 UPK 직접 parser 기준으로는 더 이상 최종 결론이 아니다.** 그래프와 참조는 복구 가능하다. 남은 일은 커스텀 배열 구조 decoder와 스킬 데이터 조인, 런타임 표현 확장이다.

---

## 1. 증거 등급

앞으로 맵과 이펙트 모두 다음 등급을 사용한다.

| 등급 | 의미 | 예시 |
|---|---|---|
| `GAME_ORIGINAL / EXACT` | 원본 package/table의 index, property, byte payload에서 직접 읽은 값 | PackageIndex, Transform, emitterDuration, AnimSequenceLength |
| `DERIVED_CONVERSION` | exact 원본을 명시적 공식으로 변환한 값 | UE3 cm → Client m, Rotator → quaternion |
| `RECONSTRUCTED` | 원본 관계가 없거나 아직 decoder가 없어 사람이 추정한 값 | 영상 눈대중 delay, 임의 texture, 수동 위치 |
| `AUTHORED_VARIANT` | 원작 복원이 아니라 의도적으로 새로 만든 변형 | 팀이 새로 만든 색상/타이밍 variant |

`이름이 비슷하다`, `영상에서 그렇게 보인다`, `같은 직업 폴더에 있다`는 exact 증거가 아니다. 저장 문서에는 원본과 추정을 섞지 않는다.

---

## 2. 발탄 맵 복원의 실제 타임라인

### 2.1 1단계: `Valtan` 이름으로 시작했고 17개가 나왔다

Lost Ark 전용 UModel로 논리 패키지 `BG_RAD_VALTAN_A`를 열면 난독화된 물리 파일 `542N9YJ2R1Y3NYH2RYFTMPE9.upk`가 연결되고 StaticMesh export가 17개 나온다.

이 결과 자체는 정확하다. 잘못될 수 있는 해석은 다음 한 줄이다.

```text
BG_RAD_VALTAN_A가 export하는 17개
  != HeartRB 레벨이 실제로 참조하는 모든 StaticMesh
```

나중에 확인한 결과 이 17개 이름은 HeartRB `PS/SL00~SL05` exact StaticMesh ImportTable과 교집합이 0이었다. 비교·overlay 후보가 될 수는 있어도 exact level placement라고 부르면 안 된다.

### 2.2 2단계: 실제 발탄 레벨 family를 DB에서 찾았다

이름 추측 대신 다음 데이터 연결을 사용했다.

```text
EFTable_Npc
  Valtan model = MN_RPBF_01
  ClassifyIndex = 3705100
    -> IntegrateDungeon/content = 37051
      -> ZoneBase PK 37051
        -> LevelFileName = LV_LUT_HEARTRB_ED_PS
          -> LV_LUT_HEARTRB_ED_PS + SL00~SL05
```

이 체인이 `LV_LUT_HEARTRB_ED`가 조사 대상 발탄 HeartRB family라는 근거다.

#### 2.2.1 팀원이 문서만 받고 `.db`를 찾지 못한 이유

게임 설치 폴더에 `EFTable_*.db`가 낱개 파일로 보이지 않는 것은 누락이 아니다. 현재 KR 설치본에서는 데이터베이스가 다음 컨테이너 안에 들어 있다.

```text
C:/ProgramData/Smilegate/Games/LOSTARK/EFGame/
  data1.lpk
  data2.lpk
  data3.lpk
  data4.lpk
  leveldata1.lpk
  leveldata2.lpk
```

확장자는 `.ipk`가 아니라 소문자 L인 `.lpk`다. 폰트에 따라 `l`과 `I`가 비슷해 보이므로 파일명을 그대로 복사해 확인한다.

문서 작성 시점의 로컬 설치본 실측은 다음과 같다.

| 파일 | 물리 크기 | GiB | 첫 4바이트 little-endian 값 |
|---|---:|---:|---:|
| `data2.lpk` | 852,015,496 bytes | 0.794 | `0x00000318` = 792 |
| `leveldata2.lpk` | 1,079,396,400 bytes | 1.005 | `0x00000554` = 1,364 |

두 파일을 합치면 약 1.799 GiB다. 첫 `u32` 뒤에는 평문 SQLite가 아니라 암호화된 LPK directory/header entry 영역이 온다. 따라서 `data2.lpk`를 SQLite 도구나 UPK parser에 직접 넣으면 열리지 않는 것이 정상이다.

#### 2.2.2 UPK 복호화와 LPK/DB 복호화는 전혀 다른 경로다

이 문서의 맵·이펙트 package 절차와 DB 절차를 섞으면 안 된다.

```text
UPK lane
  난독화된 *.upk
    -> UModel -game=lostark -kr -nameresolve
    -> UE3 PackageSummary
    -> Lost Ark AES-256 ECB + LZ4 chunk 복원
    -> Name/Import/Export/property

DB lane
  data*.lpk / leveldata*.lpk
    -> LPK directory/header 복호화
    -> entry name + offset + stored/compressed size 복구
    -> 대상 EFTable_*.db entry 추출
    -> DB payload 전용 key derivation과 block decrypt
    -> SQLite format 3 검증
```

`Tools/LevelPlacementExtractor/extract_ue3_placements.py`의 `LOSTARK_KR_AES_KEY`는 **UPK compressed chunk용**이다. 이 키를 `data2.lpk`나 추출된 `EFTable_*.db`에 적용하면 안 된다. UModel도 `.lpk` 내부 SQLite table을 대신 풀어 주는 도구가 아니다.

#### 2.2.3 “헤더가 4로 시작한다”는 말의 정확한 판정

정상 복호화된 SQLite 3 파일은 첫 16바이트가 반드시 다음 값이어야 한다.

```text
hex:
53 51 4C 69 74 65 20 66 6F 72 6D 61 74 20 33 00

ASCII:
SQLite format 3\0
```

다음 중 하나라면 아직 SQLite parser 단계가 아니다.

- 첫 바이트가 ASCII `4`인 `0x34`다.
- hex viewer 문자열이 `4...`로 시작하지만 위 16바이트 magic과 다르다.
- 파일 전체가 고엔트로피처럼 보이고 table name, `sqlite_master`, `CREATE TABLE`이 전혀 없다.
- SQLite 도구가 `file is not a database`를 반환한다.

이 상태를 “새 DB schema라서 parser가 없다”라고 결론내리면 안 된다. 대부분 다음 중 하나다.

1. `.lpk` 자체를 `.db`로 이름만 바꿨다.
2. LPK directory는 읽었지만 DB entry body가 아직 암호문이다.
3. 다른 지역 또는 이전 빌드의 key/base를 사용했다.
4. DB 전체를 한 번의 CBC stream으로 풀어야 한다고 잘못 가정했다.
5. 1,024-byte 경계마다 IV를 재설정해야 하는데 연속 상태로 복호화했다.
6. 압축된 entry와 암호화된 entry의 처리 순서를 바꿨다.
7. 마지막 block의 padding 또는 원래 크기 trim을 잘못 처리했다.

현재 로컬 원본 `data2.lpk`의 시작은 `18 03 00 00`, `leveldata2.lpk`는 `54 05 00 00`이다. 즉 팀원이 본 `4...`가 이 값들과 다르면, 그 파일은 원본 LPK가 아니라 중간 추출물일 가능성이 높다. 파일명, 전체 크기, 첫 64바이트 hex, SHA-256을 먼저 받아 어느 단계의 산출물인지 확정한다.

#### 2.2.4 공개 구현에서 확인되는 DB payload 복호화 계약

2023년 공개 구현은 LPK 안의 `EFTable_<name>.db`에 다음 파생 절차를 사용했다. 이 절차는 **구조를 이해하기 위한 기준 구현**이며 현재 KR key/base 값이 그대로 유효하다는 뜻은 아니다.

```python
# file_name 예: EFTable_SkillEffect.db
table_stem = regex_group(r"EFTable_(.*)\.db", file_name)

# 공개 구현의 문자열 입력은 UTF-16LE에 해당한다.
digest = MD5(table_stem.encode("utf-16le")).digest()

# base16은 해당 region/build에서 loader가 쓰는 16-byte 값이다.
mixed = bytes(base16[i] ^ digest[15 - i] for i in range(16))

# mixed의 lowercase hex ASCII 32자를 SHA-256한다.
aes_key = SHA256(mixed.hex().encode("ascii")).digest()

plain = bytearray()
for offset in range(0, encrypted_entry_size, 1024):
    block = ciphertext[offset:offset + 1024]
    plain += AES_256_CBC_DECRYPT(
        block,
        key=aes_key,
        iv=b"\x00" * 16,       # 매 1,024-byte block마다 새로 시작
        padding=CURRENT_LOADER_RULE,
    )

plain = plain[:original_entry_size]
assert plain[:16] == b"SQLite format 3\x00"
```

중요한 점은 다음 세 가지다.

- 파생 입력은 전체 `EFTable_SkillEffect.db`가 아니라 정규식으로 잘라낸 `SkillEffect` 부분이다.
- IV는 파일 시작에서 한 번만 설정하는 것이 아니라 **각 1,024-byte chunk마다 zero IV로 다시 시작**한다.
- AES mode, padding, 마지막 block trim은 예전 라이브러리의 default를 추측해 복사하지 말고 현재 loader의 호출 인자와 entry metadata를 그대로 따른다.

공개 코드 기준 출처:

- `https://pastebin.com/BZkk8DFD`
- `https://www.gildor.org/smf/index.php?topic=3055.195`

#### 2.2.5 현재 KR 빌드에서는 이전 key를 붙여 넣지 않는다

문서 작성 시점 로컬 `data2.lpk`의 첫 528-byte header entry를 예전 공개 KR/EU/RU Blowfish key 가정으로 시험했다. ASCII key/hex key와 32-bit endian swap 조합 모두에서 다음 header invariant를 만족하지 못했다.

```text
0 < nameLength <= 512
name이 출력 가능한 상대 경로
storedSize/originalSize가 파일 경계 안
모든 entry 누적 offset이 LPK 물리 크기 이하
```

따라서 다른 AI가 인터넷의 오래된 key를 복사해 실패한 것은 예상 가능한 결과다. 현재 KR 빌드에서는 “AES/Blowfish 같다”는 알고리즘 이름만으로 끝내지 말고 **현재 설치본과 동일한 `EFEngine.dll`에서 key source와 호출 계약을 다시 확인**해야 한다.

현재 실측한 분석 기준 바이너리:

```text
file:
C:/ProgramData/Smilegate/Games/LOSTARK/Binaries/Win64/EFEngine.dll

FileVersion: 1.0.8767.0
Size:        19,929,968 bytes
SHA-256:    CC09E083C21BDE705E4B8BC7A07D7014C618EF3F3A3ECF533E5B1FA326AA10E7
```

이 바이너리는 다음 함수를 이름과 RVA까지 export한다.

| 현재 RVA | export anchor | 조사 목적 |
|---:|---|---|
| `0x002192E0` | `FPackingPackageFileCache::Decrypt` | LPK entry decrypt 호출 계약 |
| `0x00219310` | `FArchiveFileReaderWindowsEncrypted::DecryptBuffer` | encrypted file buffer 변환 |
| `0x0023EE60` | `FPackingPackageFileCache::GetBufferOffset` | entry offset 산출 |
| `0x0023F1A0` | `FPackingPackageFileCache::GetBufferSize` | entry stored size 산출 |
| `0x0024ACB0` | `FPackingPackageFileCache::GetPackFileArchive` | 대상 LPK archive 선택 |
| `0x002631B0` | `FFileManagerWindows::InitializeAltAESKeyMap` | build/파일별 대체 key 등록 |
| `0x00276D20` | `FFileManagerWindows::IsUsingAlternateAESKey` | 어떤 파일이 어느 key lane을 타는지 확인 |
| `0x00279AF0` | `FPackingPackageFileCache::Load` | fileCount/header/entry 전체 load 흐름 |
| `0x002F10D0` | `FPackingPackageFileCache::Uncompress` | decrypt와 decompress 순서 확인 |

RVA는 위 SHA-256의 빌드에서만 유효하다. 패치 후에는 export name으로 다시 찾고 새 hash/RVA를 기록한다. 이 DLL은 디스크의 code region이 보호되어 있어 `dumpbin /disasm` 결과가 정상 함수처럼 보이지 않을 수 있다. 그 경우 랜덤한 명령열을 의사코드로 믿지 말고, 승인된 오프라인 분석 환경에서 loader가 초기화된 뒤의 module code와 실제 함수 입출력을 관찰한다. 라이브 서버 접속이나 안티치트 우회를 작업 절차에 넣지 않는다.

#### 2.2.6 리버싱 시 고정해야 할 관찰값

목표는 암호 알고리즘 이름을 맞히는 것이 아니라 아래 계약을 한 행씩 확정하는 것이다.

```text
[LPK directory]
fileCount 위치와 endian
header entry stride
header cipher/mode/key source
name length/name encoding
original size/stored size/compressed size
payload start와 alignment

[EFTable DB payload]
table stem 산출 규칙
base/key 선택 규칙
MD5/SHA-256 입력 byte encoding
AES mode
IV 값과 reset interval
padding/trim 규칙
decrypt와 decompress의 순서
```

권장 관찰 순서:

1. `FPackingPackageFileCache::Load` 진입에서 LPK path와 첫 `u32 fileCount`를 기록한다.
2. header decrypt 직전/직후 buffer를 각각 저장하고, 직후 buffer에서 name length·경로·size invariant를 검사한다.
3. `GetBufferOffset`/`GetBufferSize` 반환값이 파일 경계 안인지 전 entry에 대해 검증한다.
4. `EFTable_SkillEffect.db`처럼 이름이 확정된 한 entry만 선택한다.
5. DB decrypt 함수에 들어가는 table stem, base, derived key, IV, input length를 기록한다.
6. offset `0`, `1024`, `2048`에서 IV 또는 cipher context가 새로 초기화되는지 확인한다.
7. decrypt 직후 첫 16바이트가 SQLite magic인지 확인한다.
8. magic이 맞아도 끝내지 말고 page size, file length, `PRAGMA quick_check`까지 통과시킨다.

key를 무작위 대입해 SQLite magic만 우연히 맞추는 방식은 사용하지 않는다. directory의 모든 offset/size invariant와 SQLite 전체 무결성 검사를 동시에 만족해야 exact 복호화다.

#### 2.2.7 복호화 완료 판정과 SQLite 파싱

현재 workspace에서 실제 사용한 평문 DB는 다음 검증을 통과한다.

| DB | bytes | SQLite page size | `PRAGMA quick_check` | table count |
|---|---:|---:|---|---:|
| `EFTable_GameAction.db` | 516,096 | 1,024 | `ok` | 1 |
| `EFTable_SkillEffect.db` | 46,563,328 | 1,024 | `ok` | 1 |
| `EFTable_Prop.db` | 8,779,776 | 1,024 | `ok` | 1 |

Python 표준 라이브러리만으로 read-only 검증할 수 있다.

```python
from pathlib import Path
import sqlite3

p = Path(r"C:\path\to\EFTable_SkillEffect.db")
with p.open("rb") as f:
    assert f.read(16) == b"SQLite format 3\x00"

con = sqlite3.connect(f"file:{p}?mode=ro", uri=True)
assert con.execute("PRAGMA quick_check").fetchone()[0] == "ok"
print("page_size", con.execute("PRAGMA page_size").fetchone()[0])
print(con.execute(
    "SELECT name, sql FROM sqlite_master WHERE type='table'"
).fetchall())
con.close()
```

다음 조건을 모두 만족하기 전에는 `EFTable_*.db 추출 완료`라고 기록하지 않는다.

- 첫 16바이트가 정확한 SQLite magic이다.
- 파일 크기가 header의 page size 배수다.
- `PRAGMA quick_check`가 `ok`다.
- `sqlite_master`에서 실제 table schema가 읽힌다.
- 기대 PK row를 query할 수 있다.
- source LPK path, entry name, source offset/size, tool version, SHA-256 receipt를 남겼다.

#### 2.2.8 다른 AI 또는 팀원에게 반드시 같이 넘길 입력

암호문 `.db` 하나만 보내고 “열어 달라”고 하면 current key와 container metadata를 복구할 근거가 부족하다. 최소 다음 묶음을 전달한다.

```text
1. 원본 data2.lpk 또는 필요한 byte range에 대한 합법적인 로컬 접근
2. 원본 LPK 전체 크기, 첫 64바이트 hex, SHA-256
3. 같은 패치의 EFEngine.dll FileVersion과 SHA-256
4. 실패한 중간 .db의 파일명, 크기, 첫 64바이트 hex, SHA-256
5. 사용한 region/build와 기존 extractor/script 버전
6. 성공 조건: SQLite magic + quick_check ok + target row query
```

AI에게는 다음 경계를 명시한다.

```text
이 입력은 UE3 UPK가 아니라 Lost Ark LPK 안의 EFTable SQLite entry다.
UPK용 UModel AES/LZ4 key를 재사용하지 마라.
"file is not a database"는 schema parser 문제가 아니라 복호화 미완료로 분류하라.
current EFEngine.dll의 FPackingPackageFileCache/EncryptedReader export를 anchor로 사용하라.
1,024-byte IV reset, table-stem key derivation, padding/trim을 각각 검증하라.
SQLite format 3 magic과 PRAGMA quick_check가 모두 통과해야 완료다.
```

### 2.3 3단계: 논리 이름을 실제 난독화 UPK로 연결했다

| logical package | physical UPK | Imports | exact StaticMesh imports |
|---|---|---:|---:|
| `LV_LUT_HEARTRB_ED_PS` | `312N3UN2RBQY9N952CQJ2CVG.upk` | 138 | 6 |
| `LV_LUT_HEARTRB_ED_SL00` | `534P5WP4TDS0BPB74ESL4EI522.upk` | 628 | 86 |
| `LV_LUT_HEARTRB_ED_SL01` | `534P5WP4TDS0BPB74ESL4EI529.upk` | 773 | 142 |
| `LV_LUT_HEARTRB_ED_SL02` | `534P5WP4TDS0BPB74ESL4EI52G.upk` | 716 | 122 |
| `LV_LUT_HEARTRB_ED_SL03` | `534P5WP4TDS0BPB74ESL4EI52N.upk` | 803 | 130 |
| `LV_LUT_HEARTRB_ED_SL04` | `534P5WP4TDS0BPB74ESL4EI52U.upk` | 487 | 81 |
| `LV_LUT_HEARTRB_ED_SL05` | `534P5WP4TDS0BPB74ESL4EI521.upk` | 731 | 142 |
| 합계 | 7 packages | 4,276 | 709 |

사용한 UModel 옵션의 핵심은 항상 다음 세 개다.

```text
-game=lostark -kr -nameresolve
```

여기서 중요한 교정 사항이 있다.

- 우리가 물리 파일명을 임의의 hash 함수로 다시 계산한 것이 아니다.
- Lost Ark용 UModel의 `-nameresolve`가 게임의 논리 패키지명과 난독화된 물리 파일명 연결을 해석했다.
- `-kr`은 KR 패키지 형식·키 경로를 선택한다.
- 따라서 “Valtan 문자열을 hash해서 파일을 찾았다”보다 “UModel name resolver로 논리 이름을 실제 컨테이너에 연결했다”가 정확한 설명이다.

### 2.4 4단계: 처음에는 UModel 메모리, 최종본은 원본 UPK 직접 parser를 사용했다

UModel CLI의 `-list`는 package header의 Names/Exports/Imports 개수와 지원 object 목록을 보여 줬지만, level의 모든 ImportTable row와 Actor payload를 완전하게 출력하지 않았다.

초기 조사에서는 다음 방식으로 exact ImportTable을 증명했다.

1. UModel이 target logical package를 정상 해독해 메모리에 올린다.
2. 별도 read-only scanner가 `OpenProcess + ReadProcessMemory`로 UModel 프로세스를 읽는다.
3. header `ImportCount`와 동일한 수의 ImportTable row를 전부 복구한다.
4. 프로세스 주입, 메모리 쓰기, patch는 하지 않는다.

이 방식으로 4,276 import row와 709 StaticMesh import를 확인했고, object name 기준 중복 제거 결과는 254개였다.

최종 production placement extractor는 메모리 scanner를 입력으로 쓰지 않는다. UModel은 논리→물리 이름 해석에만 사용하고, 물리 UPK를 직접 읽기 전용으로 복원한다.

정식 구현:

```text
Tools/LevelPlacementExtractor/extract_ue3_placements.py
```

### 2.5 5단계: Lost Ark UPK의 복호화·압축 해제 규칙을 확정했다

정식 parser의 byte pipeline은 다음과 같다.

```text
physical UPK
  -> UE3 PackageSummary, tag 0x9E2A83C1
  -> Lost Ark 20-byte compressed chunk descriptor
  -> chunk 내부 compressed block table
  -> 각 compressed block 앞쪽 최대 4,096 bytes를 16-byte 경계까지 AES-256 ECB decrypt
  -> LZ4 block decompress
  -> logical package byte stream 재조립
  -> NameTable / ImportTable / ExportTable / serial payload
```

KR key의 정본은 코드에 한 번만 둔다.

```text
Tools/LevelPlacementExtractor/extract_ue3_placements.py
  LOSTARK_KR_AES_KEY
```

다른 문서나 새 도구에 키 문자열을 복제하지 말고 공통 module로 분리해 재사용하는 것이 다음 단계다.

실측 규칙:

- package compression flags: `0x44`
- compressed chunk descriptor: 표준 16 bytes + `encrypted_lz4` 4 bytes = 20 bytes
- uncompressed block 최대 크기: `0x20000`
- ImportTable row: 28 bytes
- Lost Ark `IntProperty`: 선언 payload 앞에 별도 8-byte numeric descriptor
- Actor export에는 UnrealScript stack frame이 있을 수 있어 property 시작 offset이 고정되지 않음
- 유효한 첫 property tag를 탐색하고 `None` terminator까지 완전 파싱되는 후보만 채택

### 2.6 6단계: PackageIndex를 따라 실제 asset path를 만들었다

UE3 ObjectProperty와 table의 object reference는 정수 `PackageIndex`다.

```text
index < 0  -> ImportTable[-index - 1]
index > 0  -> ExportTable[index - 1]
index = 0  -> null/root
```

각 row의 `Outer`를 0까지 따라가면 다음처럼 전체 경로가 만들어진다.

```text
pvp_retown_a.mesh.bg_pvp_retown_floor01_sm
```

이 전체 경로가 exact identity다. object leaf name만 저장하면 서로 다른 package/group의 동명 에셋을 충돌시킨다.

ImportTable 단계의 254는 **leaf object name 기준** 집합이었다. 최종 Component ObjectProperty를 full path로 읽은 production placement 단계에서는 exact 고유 경로가 260개다. 두 수치는 서로 다른 identity 기준이므로 모순이 아니다.

### 2.7 7단계: asset reference와 Transform을 결합했다

맵의 “무엇을 어디에 놓는가”는 한 테이블에 같이 있지 않았다.

```text
StaticMeshComponent.StaticMesh ObjectProperty
  -> 어떤 exact mesh인가

Actor/Component tagged property
  -> Location / Rotation / DrawScale / DrawScale3D
```

적용 규칙:

- 일반 `StaticMeshActor`, `InterpActor`: Actor의 Transform
- `StaticMeshCollectionActor`: Component의 `Translation`, `Rotation`, `Scale`, `Scale3D`
- UE3 Rotator: 65,536 units = 360 degrees
- placement stable ID: source logical package + component export index/path

결과:

```text
PS placement                 142
SL00~SL05 placement       12,949
합계                      13,091
exact 고유 full path          260
property parse error             0
독립 재실행 hash 불일치          0
```

### 2.8 8단계: Client 좌표계로 명시 변환했다

```text
UE3 position (X,Y,Z) cm
  -> Client position (X,Z,-Y) m
  -> 축 변환 뒤 × 0.01

UE3 scale (SX,SY,SZ)
  -> Client signed scale (SX,SZ,SY)

UE3 Rotator
  -> quaternion
  -> 동일 basis 변환
  -> normalize
```

골든 placement:

```text
source: SL00:export:1234
asset:  pvp_retown_a.mesh.bg_pvp_retown_floor01_sm

UE3 location: (849.491821, 2140.945313, 975.819458)
UE3 yaw:      -90 degrees
UE3 scale:    (0.490689, 0.513935, 0.225717)

Client pos:   (8.494918, 9.758195, -21.409453)
Client scale: (0.490689, 0.225717, 0.513935)
```

### 2.9 9단계: exact reference를 실제 원본 리소스에 역매핑했다

맵 level UPK는 다른 package의 StaticMesh를 참조할 뿐 실제 mesh payload를 모두 소유하지 않는다. 그래서 exact full path/name을 전체 raw archive와 조인했다.

현재 근거:

- 전체 raw StaticMesh glTF: 29,519개
- BG archive: 19,259개
- HeartRB exact mesh unresolved: 0
- 최종 HeartRB exact WModel/runtime asset: 260/260

개별 object 추출은 다음 형태다.

```powershell
& C:\Users\user\Desktop\Resource_LostArk\06_Tools\UEViewerLostArk_runtime\umodel_lostark_v7.exe `
  -export -game=lostark -kr -nameresolve -gltf -dds `
  -path=C:\ProgramData\Smilegate\Games\LOSTARK\EFGame\ReleasePC\Packages `
  -out=<fresh-output> `
  -obj=<exact-object-name> <logical-package>
```

그 다음 MIC props와 texture dependency를 추적하고 `ModelAssetConverter --pretransform --no-auto-textures --scale 100` 경로로 조리했다. 런타임은 legacy `CCookedModel/CBinaryAssetObject`가 아니라 `CModel -> CMaterial` 통합 경로를 사용한다.

### 2.10 10단계: Destroy는 정적 맵과 별도 lane으로 복구했다

Destroy는 PS/SL placement만으로 나오지 않는다.

```text
DeployData_37051.loa
  -> EFTable_Prop.db
  -> EFDLProp LookInfo
     -> intact / fractured / skeletal visual
  -> StateOffActionId
     -> EFTable_GameAction.db
     -> EFTable_SkillEffect.db
  -> TriggerMapData_37051.loa
```

현재 정확한 상태:

| 항목 | 수치/상태 |
|---|---|
| 원본 DeployData record | 169 |
| arena 반경 subset | 112 |
| arena 시각 Prop | 85, 9 family |
| static intact/fractured | 77 placement, 8 family |
| skeletal `ITR_02326` | 8 placement |
| Deploy WModel | 17, static 16 + skeletal 1 |
| 현재 상태 전환 | 85개 전체 일괄 `Intact / Fractured / Despawned` |

현재 Destroy는 exact 모델과 original DeployData Transform을 확인하는 기반까지다.

- static 77개는 intact/fractured model을 즉시 교체한다.
- `ITR_02326` 8개는 exact skeleton/model은 있으나 animation clip이 0이라 bind pose다.
- placement별 TriggerMap 연결, 낙하 시간축, 물리, particle, audio, navigation 갱신, restore는 아직 없다.

이 경계는 이펙트 복원에서도 그대로 중요하다. “재료와 위치가 있다”와 “원작 gameplay event가 정확한 시각에 실행된다”는 다른 완료 조건이다.

---

## 3. 현재 맵 정본 수치

```text
정적 exact assets                 260
정적 exact placements         13,091
현재 Map assets                  269 = exact 260 + overlay 9
현재 Map placements           13,103 = exact 13,091 + overlay 12
Destroy arena placements          85
Destroy families                    9
Destroy WModels                    17
```

정본 문서:

```text
.md/GB/07-30/2026-07-30_LOSTARK_VALTAN_MAP_RECONSTRUCTION_HANDOFF.md
.md/GB/07-30/맵추출파이프라인.md
.md/GB/07-29/2026-07-29_LOSTARK_MAP_ASSET_EXTRACTION_RUNTIME_RESULT.md
.md/GB/07-29/gotchas.md
```

---

## 4. 이펙트 복원에 그대로 적용되는 원리

### 4.1 맵과 같은 부분

| 맵 | 이펙트 |
|---|---|
| Level Component가 StaticMesh ObjectProperty를 가짐 | ParticleModuleRequired가 Material ObjectProperty를 가짐 |
| PackageIndex/Outer를 따라 exact mesh path 구성 | PackageIndex/Outer를 따라 exact material/texture/mesh path 구성 |
| Actor/Component property가 Transform 소유 | Action notify/particle data가 socket·relative Transform 소유 |
| PS/SL family를 모두 열어야 완전 목록이 나옴 | Skill/action/animation/particle/material package family를 모두 열어야 완전 graph가 나옴 |
| 이름 검색은 후보 발견용 | `par_*`, skill 이름 검색은 후보 발견용 |
| ImportTable + Export property가 exact 증거 | ImportTable + module/export property가 exact 증거 |

### 4.2 맵과 다른 부분

맵에서는 한 Actor/Component가 asset과 Transform을 비교적 직접 연결한다. 이펙트의 “배치”는 최소 세 종류다.

1. **스킬 로컬 부착**: 캐릭터 bone/socket + relative location/rotation/scale
2. **월드 생성**: caster 위치, target 위치, impact point, 투사체 위치, 지면 normal
3. **애니메이션 추적**: 두 socket/bone의 시간별 위치 또는 baked trail edge sample

또한 ParticleSystem은 “어떻게 보이는가”를 소유하지만 “스킬 몇 초에 생성되는가”를 항상 소유하지 않는다. 호출 시각은 skill/action notify, animation notify, Matinee event track 같은 외부 데이터에 있을 수 있다.

따라서 다음 둘을 분리해야 한다.

```text
Particle recipe
  emitter/module/material/curve/lifetime

Invocation recipe
  skill variant/event time/attachment/relative transform/stop policy
```

---

## 5. 기존 Effect SourceCatalog의 현재 상태

현재 catalog:

```text
Client/Bin/Resources/LostArk/Effect_Tool/SourceCatalog/source_manifest.json
Client/Bin/Resources/LostArk/Effect_Tool/SourceCatalog/particle_systems.csv
```

수치:

| 그룹 | package 범위 | ParticleSystem 이름 수 |
|---|---|---:|
| Bard | `FX_PC_MBD_00`~`08` | 689 |
| Glaivier | `FX_PC_FLM_00`~`10` | 754 |
| Common | `FX_BS_03/04/06` | 790 |
| 합계 | 23 scanned particle packages | 2,233 |

이 CSV는 discovery catalog다. `ParticleSystem 이름 수 = 완성 스킬 이펙트 수`가 아니다. 여러 ParticleSystem이 한 스킬을 구성할 수 있고, 하나가 여러 variant에서 재사용될 수 있다.

기존 `Tools/EffectResourceIntake/README.md`의 제한은 UModel CLI만으로 조사한 시점의 결론이다. 이번 직접 parser 검증으로 다음을 갱신한다.

```text
이름 목록만 추출 가능                 -> 과거 결론
UPK 직접 parser로 Cascade object graph -> 실제 가능 확인
모든 custom distribution/curve decoder -> 아직 미구현
완전한 skill invocation graph          -> skill/action data join 필요
```

---

## 6. 이펙트 샘플 UPK에서 실제로 복구한 것

### 6.1 ParticleSystem graph

검증 package:

```text
logical:  fx_wr_f_av_087d_upper
physical: 0IK5UBC5U05Q145Q3NGM5FXYYTC.upk
object:   par_l_wr_f_av_087d_upper_01
```

UModel CLI `-dump`는 custom ParticleSystem/ParticleModule class를 지원 object로 deserialise하지 못했다. 그러나 맵 production parser의 UPK 복호화·table/property parser를 그대로 적용한 결과는 다음과 같다.

```text
logical bytes       21,945
NameTable              145
ImportTable             24
ExportTable             42
property parse error     0
```

Export 구성:

| class | count |
|---|---:|
| ParticleSystem | 1 |
| ParticleSpriteEmitter | 5 |
| ParticleLODLevel | 5 |
| ParticleModuleRequired | 4 |
| ParticleModuleSpawn | 2 |
| ParticleModuleLifetime | 2 |
| ParticleModuleSize | 4 |
| ParticleModuleLocation 계열 | 4 |
| ParticleModuleVelocity | 2 |
| Color/ColorOverLife/ColorScale 계열 | 5 |
| CameraOffset/Rotation/ParameterDynamic/SizeMultiplyLife | 나머지 |

ParticleSystem property:

```text
Emitters             -> 5개 ParticleSpriteEmitter export reference
UpdateTime_Delta     -> 0.0166666675, 약 1/60초
UseFixedRelativeBox  -> true
LODSettings/Distance -> custom payload 존재
```

각 emitter는 `LODLevels` export를 가리키고, 각 LODLevel은 다음 reference를 가진다.

```text
RequiredModule
SpawnModule
Modules[]
PeakActiveParticles
```

RequiredModule에서 실제로 읽힌 값:

```text
bUseLocalSpace       true
bKillOnDeactivate    true
bUseLegacyEmitterTime false
EmitterDuration      0.5 seconds
RandomImageTime      1.0
Material             exact ObjectProperty
```

즉 emitter/module 연결, local/world space, duration, material까지 exact graph로 복구 가능하다.

### 6.2 Material과 texture 재료

RequiredModule의 exact material reference 세 개:

```text
fx_m_mi_01.fx_mi.fx_e_pa_fd_07_1_ad
fx_m_mi_01.fx_mi.fx_f_pa_ri_01_3_ad
fx_m_mi_01.fx_mi.fx_j_pa_flamepillar_01_3_tr
```

논리 package `FX_M_MI_01`은 물리 package `YGI3SB3OBJ3O18GUMP6QMP8L5.upk`로 resolve됐다.

단일 MIC 추출 명령:

```powershell
& C:\Users\user\Desktop\Resource_LostArk\06_Tools\UEViewerLostArk_runtime\umodel_lostark_v7.exe `
  -export -game=lostark -kr -nameresolve -dds -groups `
  -path=C:\ProgramData\Smilegate\Games\LOSTARK\EFGame\ReleasePC\Packages `
  -out=<fresh-output> `
  -obj=fx_e_pa_fd_07_1_ad FX_M_MI_01
```

이 명령 하나가 MIC만 저장한 것이 아니라 dependency를 따라 다음을 함께 export했다.

```text
MIC:             fx_e_pa_fd_07_1_ad.mat + props
Parent Material: fx_e_pa_twinkle_01_ad.mat + props
Texture2D:       fx_e_fluid_006.dds
Texture2D:       fx_d_noise_003.dds
Texture2D:       fx_e_atypical_005_cl.dds
```

MIC exact parameter 예:

```text
add_emissive_intensity = 0.1
emissive_paning        = 0
emissive_tiling        = 1
twinkle_intensity      = 30
twinkle_paning         = 0.1
twinkle_tiling         = 1
mask_tex               = fx_e_fluid_006
twinkle_tex            = fx_d_noise_003
```

Parent Material:

```text
BlendMode = Additive
ReferencedTextures includes fx_e_atypical_005_cl, fx_d_noise_003
```

따라서 “레시피는 있는데 재료가 없다”는 경우의 정답은 수동 파일명 검색이 아니다.

```text
ParticleSystem module ObjectProperty
  -> MIC exact path
  -> MIC parent + parameter override
  -> parent material referenced textures/default parameters
  -> UModel single-object dependency export
```

StaticMesh 전체 archive는 mesh ingredient 찾기에 유용하지만, 그것만으로 material/texture ingredient가 완전하다고 판정하면 안 된다. 위 dependency closure를 별도로 수행해야 한다.

---

## 7. 애니메이션 clip 데이터에서 실제로 복구한 것

### 7.1 창술사

```text
logical:       PC_FLM_00
physical:      UB1MW291QZZEZ7DQ61SSPSC.upk
logical bytes: 18,877,255
NameTable:     652
ImportTable:   24
ExportTable:   517
AnimSequence:  429
AnimSet:       2
parse error:   0
Notify property: 0
```

예:

| SequenceName | length seconds | frames |
|---|---:|---:|
| `sk_riseup_02` | 1.400000 | 43 |
| `sk_riseup_01` | 2.666667 | 81 |
| `sk_talonstrike_custom_4_03-2` | 1.166667 | 34 |
| `sk_talonstrike_custom_4_03-1` | 1.100000 | 34 |
| `sk_talonstrike_02` | 1.133333 | 35 |

### 7.2 바드

```text
logical:       PC_MBD_00
physical:      UB1M94I1QZZEZQRH61SSPS1.upk
logical bytes: 10,339,774
NameTable:     550
ImportTable:   24
ExportTable:   287
AnimSequence:  202
AnimSet:       2
parse error:   0
Notify property: 0
```

예:

| SequenceName | length seconds | frames |
|---|---:|---:|
| `sk_march_end` | 1.133333 | 35 |
| `sk_march_loop` | 1.600000 | 49 |
| `sk_stigma_end` | 1.100000 | 34 |
| `sk_oratorio` | 4.833333 | 146 |
| `sk_preludetodeath` | 1.500000 | 46 |
| `sk_ferbenasaria` | 7.600000 | 229 |

### 7.3 중요한 경계

두 package의 AnimSequence/AnimSet property를 전수 파싱했지만 `Notifies` property와 particle/effect/trail import는 없었다.

따라서 다음 결론이 맞다.

```text
PC_FLM_00 / PC_MBD_00
  -> animation pose, clip name, length, frame count의 정본
  -> 스킬 이펙트 호출 시각의 정본은 아님
```

스킬 이펙트 타이밍을 AnimSequence package 하나에서 계속 찾으면 안 된다. 별도 action/notify/trail/skill data를 조인해야 한다.

---

## 8. 창술사 무기 트레일 타이밍과 배치 샘플 복구

검증 package:

```text
logical:  LANCEMASTER_ANIMNOTIFY_TRAILS
physical: A5OJXH5NUXG9Y5OPHOVUP4T9YUG5PAN.upk
```

구성:

```text
logical bytes             250,272
NameTable                      59
ImportTable                     9
ExportTable                    92
animnotify_trails              43
efdata_animnotify_trails       43
objectredirector                6
```

`efdata_animnotify_trails` object 이름 자체가 skill/variant key다.

```text
lancemaster_<skillId>_<variant fields...>
```

각 `efdata_animnotify_trails`는 `trail_default`로 실제 `animnotify_trails` export를 가리킨다. 실제 trail object는 다음을 소유한다.

```text
PSTemplate       ParticleSystem exact reference
LastStartTime    seconds
EndTime          seconds
TrailSampledData array
```

실측 예:

```text
key:       lancemaster_25010_0_1_0
particle:  fx_bs_01.trail.par_a_warrior_01
start:     0.404013991 seconds
end:       0.767305970 seconds
samples:   26
```

`TrailSampledData`도 opaque blob으로 끝나지 않았다. sample 한 개는 정확히 다음 property를 가진다.

```text
RelativeTime       float
FirstEdgeSample    Vector
ControlPointSample Vector
SecondEdgeSample   Vector
```

앞 다섯 sample:

| relative time | first edge | control point | second edge |
|---:|---|---|---|
| 0.000000 | `(-38.6252,-4.4414,84.0368)` | `(-74.7008,12.6722,86.4197)` | `(-110.7764,29.7858,88.8026)` |
| 0.016667 | `(-37.5560,-9.5149,82.1588)` | `(-76.0251,1.3428,83.6564)` | `(-114.4941,12.2005,85.1541)` |
| 0.033333 | `(-34.5192,-15.5798,80.2546)` | `(-74.4322,-13.0004,80.8003)` | `(-114.3452,-10.4210,81.3460)` |
| 0.050000 | `(-26.0471,-23.9302,78.7717)` | `(-64.5243,-34.8590,78.5018)` | `(-103.0014,-45.7877,78.2319)` |
| 0.066667 | `(-13.2171,-30.1312,77.2391)` | `(-45.1000,-54.2727,76.4188)` | `(-76.9829,-78.4141,75.5984)` |

간격은 약 1/60초다. 즉 무기 trail은 단순한 start/end notify뿐 아니라 원본 edge/control point 궤적까지 복구할 수 있다.

같은 catalog 전수 조사에서 `ANIMNOTIFY`를 포함한 logical package는 345개였고 모두 `ANIMNOTIFY_TRAILS` 계열이었다. class 합계에는 `efdata_animnotify_trails`, `efanimnotify_trails`, `animnotify_trails`가 존재한다. 직업/몬스터에 따라 raw sample을 가진 class와 runtime notify형 class가 다를 수 있으므로 세 class를 모두 지원해야 한다.

---

## 9. Action notify의 부착 Transform 복구

검증 package:

```text
logical:  EFDATASTORE_ACTIONTEMPLATE
physical: T0M1Q1JQRCT5U1FQLRKQTDY61QT.upk
```

연결:

```text
EFActionNotify_PlayParticleEffect
  -> EFActionParticleData
    -> EFParticleData
      -> ParticleSystem
      -> attachment/relative transform options
```

실측 object:

```text
NotifyName = GroggyEffect
GroupName  = Paralyzation_Test
ParticleSystem = fx_bs_00.hit.par_e_counter_hit_01
bApplyLocalRotation = true
RelativeLocation = (0, 0, 150)
RelativeScale    = (3, 3, 3)
```

이것은 “특정 스킬의 전체 실제 row”가 아니라 action notify archetype/template 샘플이다. 하지만 이펙트 배치 정보가 어느 class/property 형태로 존재하는지는 확정해 준다.

따라서 이펙트 importer는 최소 다음 필드를 저장해야 한다.

```text
spawn space: local/world
attach target: actor/bone/socket/projectile/impact/ground
socket or bone name
relative location
relative rotation
relative scale
apply local rotation
inherit owner scale/rotation 여부
deactivate/kill policy
```

---

## 10. SkillCam/Matinee 타임라인도 같은 parser로 열린다

별도 package 예:

```text
STANDARD_SKILLCAM_LANCEMASTER
STANDARD_SKILLCAM_BARD
STANDARD_FX_LANCEMASTER
```

창술사 SkillCam에서 확인한 항목:

```text
RemoteEvent:
  skillcam_lancemaster_01
  skillcam_lancemaster_02
  skillcam_lancemaster_03

InterpData lengths:
  5.354692459 sec
  3.840925932 sec
  3.840925932 sec

AnimNodeSequence examples:
  sk_super_gabolg_02
  sk_superflashblink_01
  sk_super_squalllance_02

GroupAnimSet:
  pc_flm_00.ani.pc_flm_00_ani

Tracks:
  AnimControl / Event / Move / Director / FOV / UltimateSkillCinematicControl
```

바드 SkillCam에서 확인한 항목:

```text
RemoteEvent:
  skillcam_bard_01
  skillcam_bard_02

InterpData lengths:
  4.884663105 sec
  5.164574146 sec

AnimNodeSequence:
  sk_super_symphonia

GroupAnimSet examples:
  pc_mbd_00.ani.pc_mbd_00_ani
  sk_mbd_ane_00.ani.sk_mbd_ane_00_ani
  sk_mbd_grp_00.ani.sk_mbd_grp_00_ani
```

현재 generic parser는 track array의 count와 payload를 보존하지만 `AnimSeqs`, `EventTrack`, `MoveTrack`, `FloatTrack`, `CutTrack` 내부 key를 모두 구조화하지는 않는다. 완전한 궁극기 카메라/Matinee 복원에는 이 배열들의 UE3 struct decoder가 추가로 필요하다.

---

## 11. 완전한 캐릭터 스킬 이펙트 복원 절차

### 11.1 입력을 스킬 ID 하나로 고정한다

처음부터 직업 package 20개를 전부 runtime으로 넣지 않는다.

```text
class
skillId
tripod/variant/stance/stage
target animation key
```

를 한 행으로 고정하고 끝까지 dependency closure를 만든 다음 batch 확장한다.

### 11.2 논리 package 후보를 찾는다

후보군:

```text
FX_PC_<class>_xx                particle source
PC_<class>_xx                   character animation
SK_<class>_*                    skill-specific mesh/animation
WP_<class>_* / FX_WP_<class>_* weapon and weapon effect
<CLASS>_ANIMNOTIFY_TRAILS       trail timing/sample
STANDARD_SKILLCAM_<CLASS>        ultimate camera/matinee
PC_<CLASS>_<gender>             AkEvent/sound bank references
```

후보 탐색은 source catalog와 UModel `-list/-pkginfo`를 사용하되, 채택은 exact reference로만 한다.

### 11.3 Skill/action invocation lane을 복구한다

목표 join:

```text
Skill row / skill variant
  -> action or animation key
  -> animation sequence name + play rate + blend/loop
  -> timed action/notify event
  -> particle/sound/camera/gameplay payload
```

현재 확보한 `EFTable_GameAction.db`와 `EFTable_SkillEffect.db`는 Destroy action과 hit/gameplay effect를 연결하는 데 사용했다. `SkillEffect`의 Area/Hit/Push/MultiHit 값은 시각 ParticleSystem 호출 graph와 동일한 것이 아니다.

게임 설치 폴더에서 이 DB가 보이지 않거나 추출본이 `file is not a database`로 열리지 않으면 2.2.1~2.2.8의 LPK/DB 전용 복호화 절차를 먼저 완료한다. UPK용 AES/LZ4 parser로 대체하지 않는다.

현재 workspace에는 player skill 전체를 animation/effect action에 잇는 `EFTable_Skill.db` 또는 동등한 원본 데이터 lane이 아직 확보되지 않았다. 따라서 다음을 exact로 찾기 전에는 “스킬 타이밍 완전 복원”이라고 닫지 않는다.

```text
skillId
skill variant/tripod/stage
animation sequence
play rate/start offset
visual action event time
particle action payload
```

창술사 trail key는 skill ID를 포함하므로 부분 join의 강한 anchor가 된다.

### 11.4 ParticleSystem graph를 추출한다

각 selected ParticleSystem에 대해 다음 graph를 DFS/BFS한다.

```text
ParticleSystem
  -> Emitters[]
    -> LODLevels[]
      -> RequiredModule
      -> SpawnModule
      -> Modules[]
        -> RawDistributionFloat/Vector
        -> Material/Mesh/Object reference
        -> Event/Beam/Trail/SubUV data
```

각 node의 stable ID는 `logicalPackage + fullObjectPath + exportIndex`로 만든다. vector index는 저장 ID가 아니다.

### 11.5 분포·curve·burst custom payload를 decode한다

현재 generic parser는 tagged property와 ObjectProperty 연결을 읽지만 다음 payload 일부를 raw hex/count로 남긴다.

```text
RawDistributionFloat/Vector
Distribution*Constant/Uniform/Curve
BurstList
LODSettings
SubUV
Beam/Trail module custom structs
Event generator/receiver payload
```

decoder 작성 규칙:

1. NameTable의 struct/property name을 먼저 사용한다.
2. payload size, array count, `None` terminator를 validation한다.
3. class별 golden sample과 hash를 둔다.
4. 이해하지 못한 필드는 버리지 말고 `unsupportedRaw`와 source offset을 보존한다.
5. fallback default로 조용히 바꾸지 않는다.

### 11.6 material dependency closure를 만든다

```text
RequiredModule.Material
  -> MIC
    -> parent material
    -> scalar/vector/texture/static switch override
      -> parent referenced textures/defaults
        -> Texture2D export
```

Mesh emitter라면 추가로:

```text
ParticleModuleTypeDataMesh / mesh module
  -> StaticMesh/SkeletalMesh
    -> material slots
      -> MIC/Material/Texture closure
```

UModel single-object export receipt와 SHA-256 manifest를 남긴다.

### 11.7 animation lane을 결합한다

```text
skill action animation name
  -> PC_<class> / SK_<class> AnimSet
  -> exact AnimSequence
  -> SequenceLength / NumFrames
  -> animation key payload export/cook
```

이때 action의 play rate가 1이 아니면 event의 실제 wall-clock time을 변환해야 한다.

```text
wallClockTime = animationLocalTime / playRate
```

start offset, section, loop, charge stage가 있으면 각각 반영한다. clip 길이만 보고 event 시간을 비례 추정하지 않는다.

### 11.8 trail lane을 결합한다

```text
skillId/variant key
  -> efdata_animnotify_trails
  -> animnotify_trails or efanimnotify_trails
  -> PSTemplate
  -> LastStartTime / EndTime
  -> RelativeTime + edge/control samples
```

두 가지 runtime 모드를 구분한다.

- exact baked sample replay: 원본 edge/control sample을 좌표계 변환 후 재생
- bone/socket live trail: source/target bone을 매 frame sample

원본 package가 baked sample을 제공하면 첫 번째가 가장 직접적인 복원이다. skeleton/weapon variation 대응 때문에 live mode가 필요하면 별도 derived provenance로 둔다.

### 11.9 placement/attachment lane을 결합한다

각 invocation event에 다음을 저장한다.

```text
timeSeconds
particleAssetId
spawnSpace
attachTarget
boneOrSocket
relativePosition
relativeRotation
relativeScale
inherit flags
stop/deactivate policy
```

월드 impact effect는 static Transform이 아니라 runtime hit result를 입력으로 받는다.

```text
caster transform
target transform
aim direction
impact point
surface normal/material
projectile transform
```

이 값은 package에서 “고정 배치”로 나오지 않을 수 있다. package는 origin rule/offset을, runtime gameplay는 실제 target point를 제공한다.

### 11.10 sound/camera/gameplay lane을 별도로 결합한다

```text
AkEvent package / sound bank
STANDARD_SKILLCAM / Matinee
SkillEffect hit/area/multi-hit
camera shake / FOV / time dilation
```

시각 particle이 맞아도 타격 시각, sound, camera가 빠지면 완전한 스킬 복원이 아니다. 반대로 gameplay hit timing을 particle lifetime으로 추정해서도 안 된다.

### 11.11 Client format으로 stage한 뒤 한 번에 commit한다

권장 output 분리:

```text
effect.catalog.json       생성 가능한 원본 asset 정의
effect.graph.json         emitter/module/dependency graph
effect.timeline.json      skill/variant별 timed invocation
effect.trails.json        trail window와 samples
effect.materials.json     MIC/material/texture closure
effect.audit.json         source package/export/property/hash/provenance
```

로드 순서:

```text
parse -> validate -> stage -> commit
```

중간 하나라도 실패하면 새 graph/timeline을 전부 rollback하고 기존 runtime 상태를 보존한다.

---

## 12. 현재 Client Effect schema와의 간극

현재 `Client/Public/Effect_Types.h`의 schema version은 3이며 다음을 이미 표현한다.

```text
Emitter:
  SPRITE / MESH / BEAM / RIBBON / ANIM_TRAIL

Module:
  REQUIRED / SPAWN / LIFETIME
  INITIAL_LOCATION / VELOCITY / SIZE / COLOR
  SIZE/ALPHA/VELOCITY/COLOR_OVER_LIFE
  COLLISION / EVENT / LOD / ROTATION
  SUB_UV / MESH_ANIMATION / BEAM / TRAIL

Asset:
  duration / warmup / provenance
```

그러나 원본 Cascade를 손실 없이 담기에는 아직 좁다.

- distribution이 `CONSTANT`, `UNIFORM_RANGE` 중심이고 임의 curve/parameter 분포가 부족하다.
- blend mode가 `ALPHA`, `ADDITIVE` 중심이다.
- 원본 module class와 알 수 없는 property payload 보존 필드가 없다.
- LOD별 module graph와 module sharing identity가 충분히 표현되지 않을 수 있다.
- action timeline, attachment target, socket, relative Transform은 asset schema 밖에 있다.
- Material3의 모든 shader expression을 현재 `.wmat`가 동일하게 표현하는지는 별도 검증이 필요하다.

따라서 importer가 원본을 현재 schema의 비슷한 default로 강제 축약하면 안 된다.

권장 순서:

1. 원본 graph를 lossless audit JSON으로 먼저 저장한다.
2. schema v3가 표현 가능한 node만 `DERIVED_CONVERSION`으로 변환한다.
3. 표현 불가능한 node는 explicit unsupported로 보고한다.
4. 실제 필요 module부터 schema/runtime/shader를 확장한다.
5. exact와 authored approximation을 UI에서 구분한다.

---

## 13. 권장 공용 도구 구조

현재 `extract_ue3_placements.py`에 들어 있는 package core를 중복 복사하지 않는다.

```text
Tools/LostArkPackage/lostark_upk.py
  PackageSummary
  AES/LZ4 logical stream
  Name/Import/Export parser
  PackageIndex/full path resolver
  tagged property parser
  raw payload/source offset preservation

Tools/EffectExtractor/extract_particle_graph.py
  ParticleSystem/emitter/LOD/module graph

Tools/EffectExtractor/extract_material_closure.py
  MIC/parent/material/texture/mesh dependency closure

Tools/EffectExtractor/extract_anim_sequences.py
  AnimSet/AnimSequence metadata and payload receipt

Tools/EffectExtractor/extract_animnotify_trails.py
  skill-key/start/end/edge/control samples

Tools/EffectExtractor/extract_action_timeline.py
  skill/action/notify/attachment/timing join

Tools/EffectExtractor/build_effect_runtime.py
  exact graph -> supported Client format stage/commit
```

새 Python 도구는 `.vcxproj` 등록 대상이 아니다. 이후 새 C++ runtime class를 추가하면 `Client.vcxproj`와 `Client.vcxproj.filters`를 모두 등록하고 Debug/Release build를 검증해야 한다.

---

## 14. 팀원이 한 스킬을 복원할 때의 실행 체크리스트

### 조사

- [ ] class, skill ID, variant/tripod/stage를 고정했다.
- [ ] logical package 이름을 UModel `-nameresolve`로 physical UPK에 연결했다.
- [ ] candidate 이름이 아니라 exact Import/ObjectProperty를 확보했다.
- [ ] animation clip 이름·길이·frame 수를 exact AnimSequence에서 읽었다.
- [ ] visual invocation time과 action payload의 원본 source를 찾았다.
- [ ] trail package가 있는지 class/skill ID로 조사했다.
- [ ] sound/camera/gameplay event lane을 별도로 확인했다.

### 재료

- [ ] ParticleSystem → emitter → LOD → module graph가 닫혔다.
- [ ] Required/mesh module의 material/mesh reference가 full path다.
- [ ] MIC parent chain과 모든 texture override/default가 닫혔다.
- [ ] UModel export receipt와 SHA-256 manifest가 있다.
- [ ] unresolved dependency가 0이거나 명시적 unsupported list가 있다.

### 시간·배치

- [ ] event time은 영상 추정이 아니라 action/notify/track property다.
- [ ] animation play rate/start offset을 반영했다.
- [ ] local/world space가 확정됐다.
- [ ] attach bone/socket 또는 runtime world origin rule이 확정됐다.
- [ ] relative position/rotation/scale와 inherit flag가 있다.
- [ ] deactivate/kill/loop policy가 있다.
- [ ] trail start/end/sample 또는 live bone rule이 있다.

### 런타임

- [ ] 원본 graph와 변환 graph의 provenance가 분리됐다.
- [ ] parse/validate/stage/commit과 rollback이 동작한다.
- [ ] reload 후 stable ID가 유지된다.
- [ ] 정지 frame 3개 이상과 동영상으로 원작 timing을 비교했다.
- [ ] Debug/Release에서 shader/material/texture 누락이 없다.
- [ ] particle count, draw call, memory, lifetime leak을 검증했다.

---

## 15. “완벽 복원” 완료 판정

다음 항목이 모두 닫혀야 특정 스킬 하나를 `GAME_ORIGINAL` 완전 복원이라고 부른다.

```text
[A] Skill identity
    skill ID + 모든 variant/stage/tripod key

[B] Animation
    exact sequence + play rate + blend/start/loop + key payload

[C] Invocation timeline
    모든 visual/audio/camera/gameplay event의 exact time/order

[D] Particle graph
    emitter/LOD/module/distribution/curve/burst/lifetime/event

[E] Resource closure
    material/MIC/texture/static/skeletal mesh/subUV

[F] Placement
    local/world + socket/bone/target + relative Transform

[G] Trail
    window + edge/control sample 또는 exact bone sampling rule

[H] Runtime semantics
    spawn/loop/deactivate/kill/collision/sort/blend/LOD

[I] Cross-lane sync
    animation, particle, hit, sound, camera가 같은 clock에서 일치

[J] Verification
    deterministic reload + source audit + visual/timing comparison
```

현재는 `[D]`의 graph/reference와 `[E]`의 material/texture closure, `[B]`의 animation metadata, 창술사 `[G]`의 trail data가 실제 샘플에서 열렸다. `[C]`의 전체 player skill invocation join과 모든 custom distribution/Matinee key decoder, runtime shader parity는 아직 완료 전이다.

---

## 16. 다른 세션·팀 작업 시 금지 사항

- `Valtan`, `Bard`, `FLM`, `skill name` 문자열 검색 결과를 완전 목록이라고 결론내리지 않는다.
- UModel `-list`에 지원 object가 없다고 package에 데이터가 없다고 결론내리지 않는다.
- ParticleSystem leaf name만 저장하지 않는다. logical package와 full object path를 함께 저장한다.
- `ParticleSystem 1개 = 스킬 1개`로 세지 않는다.
- static mesh archive가 있다고 material/texture/animation/timing까지 있다고 간주하지 않는다.
- AnimSequence length만 보고 effect spawn time을 비례 추정하지 않는다.
- `EFTable_SkillEffect`를 시각 ParticleSystem recipe로 오해하지 않는다.
- unsupported curve/module을 1초 lifetime·흰색·additive 같은 default로 조용히 바꾸지 않는다.
- exact와 reconstruction을 같은 catalog group/provenance로 저장하지 않는다.
- 다른 세션의 Engine/Client 변경을 되돌리거나 정리하지 않는다.
- `.codex_tmp`, build 산출물, 원본 UPK, 대형 Resources를 소스 커밋에 섞지 않는다.

---

## 17. 재현에 필요한 현재 경로

```text
Lost Ark physical packages
C:/ProgramData/Smilegate/Games/LOSTARK/EFGame/ReleasePC/Packages

Lost Ark UModel
C:/Users/user/Desktop/Resource_LostArk/06_Tools/UEViewerLostArk_runtime/umodel_lostark_v7.exe

UPK production decoder / map placement extractor
C:/Users/user/Desktop/LostArk/Tools/LevelPlacementExtractor/extract_ue3_placements.py

Map reports
C:/Users/user/Desktop/Resource_LostArk/05_Reports/MapExtraction/LV_LUT_HEARTRB_ED

Effect source catalog
C:/Users/user/Desktop/LostArk/Client/Bin/Resources/LostArk/Effect_Tool/SourceCatalog

Effect probe source package
C:/Users/user/Desktop/Resource_LostArk/00_SourcePackages/EffectSmoke_FX_WR_F_AV_087D_UPPER

Effect UModel logs
C:/Users/user/Desktop/Resource_LostArk/05_Reports/EffectPilot

Full logical/physical/class catalog
C:/Users/user/Desktop/Final_LostArk/_work/full_pkginfo_scan_canonical_v2_20260728/catalog.sqlite3

Current Client effect schema
C:/Users/user/Desktop/LostArk/Client/Public/Effect_Types.h
```

---

## 18. 최종 전달 문장

> 발탄 맵은 `Valtan` 이름의 17개 export로 만든 것이 아니다. DB로 `LV_LUT_HEARTRB_ED` level family를 확정하고, Lost Ark UModel의 name resolver로 난독화 UPK를 찾은 다음, UPK를 AES/LZ4 복원해 ImportTable·Export property·PackageIndex를 직접 읽었다. 그 결과 exact StaticMesh 260개와 original Transform placement 13,091개를 복구했다. Destroy는 별도 DeployData/Prop/LookInfo lane에서 arena 85개를 배치했으며 현재는 수동 상태 전환 기반까지다. 이펙트도 같은 참조 그래프 방식으로 ParticleSystem→Emitter→Module→MIC→Material→Texture, AnimSequence, skill-key trail timing/sample, Action notify 상대 Transform까지 복구 가능함을 실제 UPK로 확인했다. 다만 완전한 스킬 복원은 player skill/action 데이터의 호출 시각 조인, 모든 Cascade/Matinee custom array decoder, socket/world spawn 규칙, sound/camera/gameplay sync와 Client shader/runtime parity까지 닫아야 한다.
