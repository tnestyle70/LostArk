# 2026-09-05 캐릭터 정보창(P) PLAN

원작 `EFUI_CHARACTERINFO` `characterinfo.gfx`의 장비 탭을 제품 UI(CUI_Sprite + LOA 폰트 텍스트)로 복원한다.
창 가운데 캐릭터는 별도 카메라 렌더 타깃(원작 `CharInfoTexture` RTT 구조)으로 그린다.
아바타 도감 창은 다음 작업이며, 이번에는 `장비/아바타` 토글과 좌측 하단 버튼 전환(무기 이펙트 ↔ 아바타 도감)까지만 포함한다.

## 원작 자료 (모두 확보, `D:\ClaudeWork\Extracted\`)

- `CharacterInfoGfx_Extracted/`: `characterinfo.gfx`(hash `OVSG0AA9W7WALO7GFVMDY2.upk`), `characterinfo.xml`(swf2xml), AS3 988개
  (`ark.ui.charInfo.CharInfoWndContent`, `CharInfoEquipPanel`, `CharInfoEquipItemPage`, `CharInfoEquipInfoPage`,
  `CharInfoEquipAbilityPage`, `CharInfoEquipAvatarPage`), 아틀라스 32장(`png/`), 배치 추적 `trace_*.txt`.
- 공용 스킨: `ShareImageGfx_Extracted/shareimagev2_structure.xml`(창 배경 `V2subWndBG`, 상단 장식 `V2windowTopDeco_center`,
  닫기 `V2closeBtn_*`, 탭 `V2tabBtn_*`, 버튼 `V2btn_*`, 등급 배경 `V2gradeIconBG_03/04/05/06`, 장비 슬롯 실루엣
  `Shared_CharacterInfo_EquipSlot_*`, 클래스 엠블럼 `ClassSymbol_CharInfo_N`, 프리셋 탭 `Shared_PresetBtn_*`),
  `Components_Extracted/componentsv2.xml`(`NewUIWindow_renew_V2`, `EFToggleButton_Tab_V2`, `GradeIcon_00V2`).
- 아이템 아이콘: `EFUI_ICONATLAS_G` `gl_item_0`(워로드 세트, 64px 셀 16열, `EFTable_Item.IconIndex`),
  `EFUI_ICONATLAS_A` `acc_0`/`ability_1`(악세·어빌리티 스톤). 원본 테이블 `Desktop/SourceData/LPK/data2/.../EFTable_Item.db`,
  `EFTable_ItemSet.db`. 아이템/세트 한글 이름 문자열 테이블은 덤프에 없어 세트 식별은 아이콘 시트로 사용자가 고른다.
- 추적/크롭 스크립트(job tmp): `gfx_trace.py`, `charinfo_crops.py`, `charinfo_items.py`, `charinfo_layout.py`.

## 원작 배치 (1920 → 1280 ×2/3, 창 원점 (286.67,131), 창 1060×687)

| 요소 | 원작(창 기준 px) | 슬롯 |
|---|---|---|
| 창 배경/상단 장식/닫기 | (0,0) 1060×687 / (368,30) 326×18 / (1024,16) 19×15 | CI_WinBg, CI_TopDeco, CI_Close |
| 탭 5개 | tabGroupManager (16,39), 172 간격, 94×36 ×1.79 | CI_Tab_0..4 |
| 패널 배경 | CharInfo_Equip_BGMc 12×12 타일 (14,77) 1036×603 | CI_PanelBg |
| 프리뷰 | mouseAreaMc (436,120) 357×490 | CI_Preview (RTT) |
| 좌우 그라데이션 | (356,120)/(791,120) 69×520 | CI_LeftGrad/RightGrad |
| 아이템 레벨 박스 | abilityPage (843,106) 217×182, 전투력 (854,216), 기본 특성 타이틀 (855,271) | CI_ItemLevelBg 등 |
| 장비 슬롯 좌 | SlotManager (380,163) + {0,59,118,177,236,295}, 잠금 354 | CI_Slot*_L0..5, _Lock |
| 장비 슬롯 우 | (798,163) + {0,59,118,177,236}, 스톤 (801,461), 팔찌 (798,517) | CI_Slot*_R0..6 |
| 프리셋 줄 | (351,83) 57 간격 10칸, 이름 배경 (354,127) | CI_PresetTab_k, CI_PresetNameBg |
| 장비/아바타 토글 | 프레임 (482,604), 버튼 (484,606)/(614,606) 131×29 | CI_Toggle* |
| 좌하단 버튼/상세정보 | (13,648) 133×30 / (855,642) 194×36 | CI_BottomLeftBtn, CI_DetailBtn |
| 정보 페이지 | 기본정보 bg (14,78) 340×236, 엠블럼 (140,100) 83×83, 정보 행 (13,314+35r), 특수장비 (14,454), 아크패시브 (14,543) | CI_BasicInfo*, CI_ClassSym, CI_InfoRow_r, CI_Gear*, CI_Ark* |
| 아바타 페이지 | 프레임 8개 (383/791, 197..493), 눈 체크 8개 | CI_AvatarFrame_k, CI_AvatarCheck_k |

텍스트(YoonGasiIIM/YG760, 원작 px → ×2/3)는 `CCharacterInfoWindowView::Render_Text`에 창 기준 px로 적는다.

## 구현

- 리소스(`Client/Bin/Resources/UI/CharacterInfo/`, 92장 + `Items/`): 창/탭/버튼 스킨, 패널 조각, 슬롯 테두리·실루엣·등급 배경,
  클래스 엠블럼 6장(워로드 #9, 창술사 #16, 건슬링어 #31, 슬레이어 #34 추정, 도화가 #23, 디멘션마스터 #41),
  워로드 세트 아이콘 8묶음(`Items/Warlord_<SetIndex>/`), 공용 악세 아이콘(`Items/Common/`).
- 데이터: `Data/UI/CharacterInfo/CharacterInfo_Layout.json`(슬롯 161개, HUDLayoutTool "Character Info" 문서),
  `CharacterInfoDisplay.json`(서버 정본이 없는 표시값: 아이템 레벨, 전투력, 전투 특성 6종, 원정대 레벨, 칭호/길드/영지,
  아크 패시브, 클래스별 한글 이름·엠블럼·장비 세트 폴더·등급, 프리뷰 카메라).
- 코드
  - `CCharacterInfoWindowView`(신규): 슬롯 가시성·hover·클릭·타이틀 드래그·프리뷰 드래그 회전, 텍스트, RTT.
  - `CCharacter::Render_PreviewParts`, `CPart_Body::Render_Pass`, `CPart_Equipment::Render_Pass`: 파츠를 지정 pass로 다시 그림.
  - `Shader_VtxMeshBinary.hlsl`: `pass ScreenCutin`(index 19, 맨 뒤 추가) — 소켓 무기 파츠용 전방 조명.
    스킨 파츠는 기존 `Shader_VtxAnimMeshBinary` `ScreenCutin`(index 5).
  - `CUILayoutRuntime::Set_SlotTextureSRV / Get_SlotIds`, Level(Bern/Valtan/CharacterSelect) `Get_LocalCharacter`,
    `CCombatHUDViewModel::Try_Get_ProfileAttackPower`.
  - `CMainApp`: P 키 토글(아이템 재련 프리뷰는 U로 이동), `Update_CombatHUD`에서 Update/Hide,
    `Render()`에서 world 렌더 전에 `Render_Portrait`, 텍스트 pass.
- 실제 데이터: 닉네임(복제 캐릭터), 클래스·최대 생명력(HUD 스냅샷), 공격력(PlayerProfiles.json). 나머지는 표시 JSON.

## 검증

- `cl /Zs` 변경·신규 TU 8개, `fxc /T fx_5_0` `Shader_VtxMeshBinary.hlsl`, JSON parse 2개, `git diff --check`,
  `test_release_client_surface_contract.py` PASS.
- 사용자 실행 확인 필요: 베른/발탄/캐릭터 선택에서 P → 창 표시, 캐릭터 프리뷰(카메라 값은 `portrait`에서 조정),
  드래그 회전, 탭/토글/닫기, 워로드 장비 아이콘, 타이틀 드래그.

## 남은 경계

- 아바타 도감 창 본체, 각인/보석/카드/아크 그리드 탭 내용, 원정대 레벨 배지 아트, PlayerDeco 배경(외부 패키지 미추적).
- 워로드 외 클래스 장비 세트 아이콘(각 클래스 아틀라스에서 같은 방식으로 추가 가능), 세트 이름(문자열 테이블 없음).

## 아바타 도감 창 원본 추출 (2026-09-05 밤, 구현 전)

- 패키지 `EFUI_AVATARBOOK`(`OVSG0AWZWLW73MMUD62YW6.upk`) → `avatarbook.gfx`, 루트 심볼 `AvatarBookWindowContent`(char 618),
  AS3 `ark.ui.avatarBook.AvatarBookWndContent`. 창 1532×800. 추출물 `D:\ClaudeWork\Extracted\AvatarBookGfx_Extracted\`
  (`avatarbook.xml`, `as3/`, `png/`, `trace_root_d1/d3.txt`, `trace_groups.txt`, `trace_tabs.txt`).
- 카메라 1/2/3·무기/투구/상의 토글·숨김 체크는 공용 `EFUI_PREVIEW` `preview.gfx`(`PreviewControl` 베이스)에서 옴 →
  `D:\ClaudeWork\Extracted\PreviewGfx_Extracted\`.
- 리소스 94장 `Client/Bin/Resources/UI/AvatarBook/`(job tmp `avatarbook_crops.py`).

| 요소 | 원작(창 기준 px) |
|---|---|
| 프리뷰 그룹 | (7,48); previewControl (8,91); characterScreen 604×652 배경 + `avatarStashPreviewTexture` RTT; 마우스 영역 605×650 |
| 프리뷰 슬롯 | previewPageEquipSlotManager (24,143) + slot 0..14 (491/537, -1..399 50 간격, 0.6배 42px); slotBG_n 44×44 (506/552, 50..450) |
| 눈 토글 | showItem_0..6 (471,319)(471,54)(517,104)(517,154)(471,204)(471,254)(517,404) 38×37, effectShow_0 (522,293) |
| 카메라 탭 | cameraTabGroup (6,580) → tab_k (13, 36k) 42×33, 숫자 YG760 14 |
| 회전 안내 | rotationMouseDescMc (24,693) 아이콘+`[$]ingameshop.rotat` 12px |
| 배경 전환 탭 | bgChangeTabGroup (493,91) tab_k (37/61/85?, 0) 0.7배 |
| 프리셋 탭 줄 | previewPageTabList (7,48) 10칸 (배경 4.0625×0.289×128=520×37), edit_btn (529,49) 41×35, presetLoad_btn (573,49) 40×35 |
| 하단 버튼 | autoEquip (76,753) 노랑 V2yellowBtn 103×36 ×1.5145, revert (234,753), save (392,753) |
| 카테고리 탭 | avatarBookTabGroup (631,49): tab_0..9 x=0,38,78,118,157,194,232,270.55,310,344.2, 36×36 |
| 탭 이름 bg | tabNameBG (633,91) 881×214 (상단 그라데이션), tabName_lb (870,99) 중앙 |
| 즐겨찾기 카운트 | 별 (626,101) + favoriteCount_lb (651,102) |
| 클래스 콤보 | (1356,100) 0.936×; 카테고리 콤보 (626,140) 0.773×; 검색창 (761,140) 3.64×; 검색 버튼 (1153,140) 59×32; 상세 옵션 체크 (1214,140) 1.437×; 정렬 콤보 (1365,141) |
| 그리드 | defaultTileList (625,183) 864×432, 셀 72 간격 12열, ARKBookMarkSlot 70×70 + 별 (42,42); 스크롤바 (1499,182) ×2.17 |
| 구분선 | V2Line_HDivision (624,622) ×6.116 = 893 |
| 선택 이름 | selectedAvatarName_lb (637,635) |
| 액션 버튼 | shop/craft/tint/auction (1232/1280/1328/1376, 633) 47×33.5; disassembly/movebag (1424/1472, 633) 0.5배 |
| 보유 현황 | hasitems bar (638,674) 869×74, stepper 화살표 (737,689)/(1407,689), 셀 10개 (804,686)+54k ×0.714 |
| 하단 텍스트 | otherEquipCharacterName_lb (637,760) |

구현은 캐릭터 정보창과 같은 구성(레이아웃 JSON + `CUILayoutRuntime` + 전용 View + 같은 RTT 프리뷰)으로 진행한다.

## 2026-09-05 피드백 반영 (2차, 미빌드)

- 누락 아이콘 추가 (layout JSON에 슬롯 9개 append, 재생성 아님): `CI_ItemLevelInfo`/`CI_HonorInfo`
  (녹색 i, `V2InfoLocalBtn` = `EFUI_LOCALRESOURCE` `localresource_loc_int_i7` (999,951)-(1024,977)),
  `CI_BasicHelp`/`CI_CombatHelp` (`TooltipButton_V2` = `V2HelpBtn` 25x26, 능력치 페이지 (182,180)/(1012,319)+1),
  `CI_HonorIcon` (`Shared_Honor_Icons_V2` = `_01_normal` 100x62 @0.5, honorGroup+(58,-1)),
  `CI_ExpeditionBadge` (`ARKIcon` `GradeIcon_00V2` 64px @2.6667*0.3438, info page (13,103); 등급 프레임은
  런타임 데이터라 legend 플레이트로 가정), `CI_ArkPillIcon_0..2` (`emoticon_ark_evolution/realization/leaf`,
  pill+(4,1)/(4,2)/(4,1)). 원본 추출: `D:/ClaudeWork/Extracted/LocalResourceGfx_Extracted/`.
- 장신구 7종 아이콘: 사용자 스크린샷 크롭(46px)으로 `Resources/UI/Items/Common/*.png` 교체 (catalog 경로 동일).
- 닉네임 색 `#fff0b0`, 라벨 `TEXT_BOOST=1.15` + 화면 픽셀 정수 스냅.
- 텍스트 뚫림: `CUIInputRouter::Set_TopWindowRect/Is_UnderTopWindow` (화면 px, End_Frame에서 해제).
  정보창이 매 프레임 등록, Inventory/Party/Chat 텍스트 패스는 그 안의 글자를 건너뜀.
  `m_pCharacterInfoView`를 Party 뒤에 생성해 스프라이트도 최상단.
- UI 위 좌클릭 평타: `CMainApp::Update`의 `mouseCaptured`에 `Is_MouseClaimedThisFrame || Was_MouseClaimedLastFrame`
  OR. Inventory는 패널 전체 hover claim, Party는 roster 전체, Chat은 입력창 열린 동안 로그/입력바 hover claim.
- 임시 누수 트레이서(`Client.cpp` LeakTrace, 커밋 금지) v2: 해제 시 항목 삭제(backward-shift), 슬롯 free-list,
  `{N} ` + `... bytes long` 분리 report 조합. 사용자 실행 후 `Client/Bin/Debug/ClientLeak.user.log` 확인.

## 2026-09-06 아바타 도감 구현 (미빌드)

- 새 파일: `Client/Public,Private/AvatarBookWindowView.{h,cpp}`, `CharacterPortraitRenderer.{h,cpp}`(정보창 RTT를
  공용 클래스로 추출), `Data/UI/AvatarBook/AvatarBook_Layout.json`(504 슬롯, 생성기 scratch `avatarbook_layout.py`),
  `AvatarBookDisplay.json`(탭 이름·카메라). vcxproj/filters `02.GameObjects\04. UI`, `96.DataFiles\UI`에 등록,
  HUDLayoutTool "Avatar Book" 문서 추가.
- 진입: 정보창 아바타 페이지 좌하단 버튼 → `CCharacterInfoWindowView::Take_AvatarBookRequest` → `CMainApp`이 토글.
  정보창이 닫히면 도감도 닫힘. 도감이 열리면 `Set_Covered(true)`로 정보창은 top-window 등록을 멈추고 라벨을 가림.
- 입어보기 모델: 그리드 클릭 = 마네킹에 입기(프리뷰 마스크), 눈 토글 = 벗기기, 되돌리기 = 실제 상태, 자동 장착 =
  전부, 저장 = `CCharacter::Set_AvatarPartVisible`(실제 캐릭터가 바뀌는 유일한 경로).
  `Render_PreviewParts(skinned, socketed, overrideKinds, hiddenKinds)`로 프리뷰가 실제와 독립.
- 그리드 내용 = `ItemCatalog.json` grade `avatar` + 클래스 일치 + 캐릭터 spec에 해당 AVATAR 파츠가 있는 항목. 카테고리
  탭 2(머리)/5(상의)만 필터, 나머지 탭은 빈 그리드. 카메라 1/2/3 = 전신/상반신/얼굴 프리셋.
- 데이터 없어 표시만인 것: 프리셋 탭, 배경 전환, 검색·정렬·클래스 콤보, 상세 옵션, 즐겨찾기, 상점/제작/염색/경매/분해/
  가방 버튼, 보유 현황 stepper, 아바타 포인트.
- 검증: `cl /Zs` 6개 TU 통과, JSON parse, vcxproj XML parse, `git diff --check` 통과. 화면 확인은 사용자.
