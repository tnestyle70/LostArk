# 파티 레이드 입장 투표 결과 — 2026-09-19 보정

Bern 군단장 레이드 UI의 쿠크 선택과 전원 수락은 구현돼 있었으나, 실제 Server 파티 이동의
목적지 검증이 발탄만 허용해 쿠크 2~4인 입장이 실패했다. 실패 통지의 Shared Writer/Reader도
발탄만 허용해 쿠크 거절 사유를 전송하지 못했다. 두 검증에 `KAKULSAYDON_ARENA`를 추가했다.
기존 전원 admission·송신 준비 이후 원자적 이동, 실패 시 Bern 상태 보존을 그대로 사용한다.

입장 UI의 typed command와 ServerApp binding은 기존 경로다. wire 형식·protocol 번호와
Data는 바꾸지 않았다. 맵 입장과 전투 시작은 구분하며, 기존 쿠크 entry collider와
전원 revision/resource ACK가 gate intro를 시작한다.

검증은 실제 CGameRoom/ServerApp·Shared 구현을 사용한 Git 제외 focused 실행으로 완료했다.

- 쿠크 1~4인 수락 후 실제 ServerApp 전송과 각 `S2C_ENTER_ACCEPTED`의 쿠크 목적지 확인.
- 쿠크 1~4인 거절 시 Bern player·party·binding 보존.
- 발탄 2/4인 이동 회귀와 발탄·쿠크 각 6개 admission/송신 실패의 원상태 보존.
- 지연된 실패 통지의 실제 encode/decode와 선택 목적지/사유 보존.
- Shared 발탄·쿠크 round-trip, 모든 잘린 입력, 알 수 없는 사유 및 비레이드 목적지 거절 20 checks PASS.
- 변경 C++의 Release 컴파일 및 `git diff --check` PASS. 제품 Release 빌드/ZIP 증거는 배포 RESULT가 소유한다.

실행 로그: `out/KoukuRaidEntry20260919/focused.run.log`, `protocol.run.log` 모두 failures 0.
Client/UI 및 실제 Server listener는 실행하지 않았다. 여러 PC의 입장 화면은 사용자 확인 대상이다.
