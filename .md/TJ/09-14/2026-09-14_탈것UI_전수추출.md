# 탈것 UI 전수 추출 (2026-09-14)

만들기 전에 소스 종류를 전부 쓸고, 빠진 것을 이름으로 남긴다. 구현은 하지 않았다.
브랜치 `feature/vehicle-ui`. 추출물은 `D:/ClaudeWork/Extracted/Vehicle/`.

## 1. 지금 프로젝트에 있는 것 (JS 팀원 작업, main 반영됨)

| 항목 | 내용 |
|---|---|
| 탈것 4종 | `Data/Actors/VehicleCatalog.json` — 6705 황금 테르페이온, 7104 은색 전투 랩터, 9370 고요한 별빛의 가호, 7209 레인보우 모코보드 |
| Server | `GameRoom_VehicleRiding.cpp`, `VehicleCatalog`, `VehicleProfiles.json`(MoveSpeed 5.0 = 표 500/100), protocol 82 `C2S_SET_VEHICLE_RIDING` / `S2C_SET_VEHICLE_RIDING_RESULT` |
| Client | `CPart_Vehicle`, `VehiclePresentationAssetService`, `PlayerController::Update_VehicleRiding` — **H 키**로 탑승/하차 토글, `CCombatHUDViewModel::Get_Player().iVehicleId`(0 = 도보) |
| Debug | F1 `Vehicle Riding (Debug)`: 기본 탈것 라디오 선택 |
| 거절 사유 | `VEHICLE_RIDING_RESULT`: 지역 불가 / 지금 불가 / 모르는 탈것 / 세션 / 일반 거절 — 현재는 `OutputDebugString`만, 화면 표시 없음 |
| 미구현 (팀원 RESULT 명시) | 제품 탈것 선택 UI, 탈것 스킬, 탑승·하차 클립, 소환 파티클, 발굽 사운드 |

UI가 읽을 계약은 이미 있다: `iVehicleId`, `Request_SetVehicleRiding(seq, vehicleId)`, `Consume_VehicleRidingResult`.

## 2. 원작 UI가 어디에 있나 (gfx 661개 전수 검색)

`SweepAllNames` 심볼 덤프에서 `vehicle|riding|탈것` 문자열이 있는 무비 11개:

| 무비 | 역할 | 이번 범위 |
|---|---|---|
| **`vehicle`** (`EFUI_VEHICLE`, `OVSG0AZO9GA1ODE2YWRMP2.upk`) | 탈것 보관함 창 | **핵심** |
| **`quickslot`** | 탑승 중 HUD — `vehicleSlotList`(64×64 슬롯 1개), `QuickSlot_VehicleSlot`, `INTERACTION_TYPE_VEHICLE` 모드 전환, `ST_Hud_VehicleHudShow` | **핵심** |
| `townhud` | 영지 우측 `modeVehicleButton`(50×50, `TownHud_I4.tga`), 툴팁 `[$]town.btn_vehicle` | 영지 전용 |
| `systemquickmenu`, `exitmenu` | `MENU_TYPE_VEHICLE` — 메뉴에서 보관함 열기 | 메뉴 항목 |
| `commonobject` | `vehicleWnd` 창 껍데기 등록 | 창 프레임 |
| `masterkey` | `riding` 문자열 1건 | 무관 |
| `townplacement`, `cashshop`, `cashshopbuy`, `yozpot` | 영지 배치·상점 탭 | 무관 |

## 3. `vehicle.gfx` 구조 (감사 도구 결과)

sprites 33 · named instance 41 · EditText 33 · 프레임 라벨 150 · 이미지 8 · **내장 사운드 0**.

루트: `vehicleWnd` = `DefaultUIWindow_V2`(componentsv2) 하나. 창 배치 (1376, 186), 스케일 (1.455, 2.790) — 창 프레임 기본 크기에 곱해 실제 크기가 나온다.

### 3.1 `VehicleWndContent` (sprite 41) 자식

| depth | 인스턴스 | 클래스/출처 | 위치 | 비고 |
|---|---|---|---|---|
| 1 | `listBG` | 로컬 sprite 1 (`V2Line_HDivision_mc`) | (12,165) | 리스트 배경 구분선 |
| 74 | `vehicleTabList` | `Vehicle_TabList` | (14,40) | 탭: 일반 / PC방 (`sys.vehicle.ui_btn_tab_general/pcroom`) |
| 33 | `vehicleSearchInputTF` | `DefaultTextInput_AutoComplete_V2` | (13,85) s(3.654,1) | 이름 검색, placeholder `honortitle.ui_text_enter_honortitle_name` |
| 34 | `vehicleSearchBtn` | 로컬 25 (`V2FindBtn_*`) | (410,85) | |
| 31 | `bookmarkCheckBox` | `DefaultCheckBox_V2` | (10,122) | `vehicle.window_bookmarktoggle` 즐겨찾기만 보기 |
| 18 | `vehicleSortingCheckBox` | 로컬 22 | (315,125) s(1.126,1) | 정렬 토글 |
| 44 | `alignComboBox` | `Shared_ComboBoxHtmlExAliign_V2` | (314.9,125) | 기본 정렬 / 이름 순 |
| 14 | `vehicleList` | `VehicleList`(sprite 4) | (14,165) | RollingList, 아이템 = `VehicleListItem` |
| 16 | `vehicleScrollBar` | `DefaultScrollBar_V2` | (450,164.9) s(1,2.5) | |
| 11/13 | `pcRoomVehicleList` / ScrollBar | 〃 | 〃 | PC방 탭 |
| 17 | `emptyCaution` | `Shared_BaseCaution_V2` | (13,163) | `vehicle.window_bookmarknotice` |
| 32 | `desc_lb` | `SimpleLabel` | (17,675) s(6.892,2) | `vehicle.window_quickslot_desc` |
| 38 | `summonActionCheckBox` | 로컬 33 | (13,721) s(1.282,1) | `social.ui_btn_summon_action` 소환 모션 |
| 29 | `confirmBtn` | `AnimatedButton_renew_V2` | (259,721) | `vehicle.window_accept` **탑승** / `window_release` 탑승 해제 |
| 30 | `closeBtnDummy` | `AnimatedButton_renew_V2` | (365,721) | `vehicle.window_exit` 닫기 |
| 48 | `extendGroup` | `VehicleExtendGroup`(sprite 37) | (−538,345) | 왼쪽으로 펼쳐지는 소환 모션 패널 |
| 46 | — | `V2WindowDeco` | (77,30) | 창 상단 장식 |
| 47 | `tutorial0` | `UiTutorialTarget` | (14,165) | 튜토리얼 타깃 |

### 3.2 `VehicleListItem` (sprite 71, 90프레임)

라벨 `up@1 over@11 down@21 disabled@31 out@41 selected_up@51 … selected_disabled@81`.

| depth | 인스턴스 | 출처 | 위치 |
|---|---|---|---|
| 1 | 배경 도형 59 | `Vehicle_I5` sub77 (402×60, 아틀라스 0,0–352,60) | (0,0) |
| 2 | `bookmarkBG_mc` | `Vehicle_I5` sub60 (276×58, 519,0–795,58) | (1,2) |
| 4 | `over_mc` | `FC2stepList_overMC` (shareimage) | s(2.15,1.71) |
| 5 | `selected_mc` | `FC2stepList_select` (shareimage) | s(2.16,1.77) |
| 6 | `equipMc` | `Vehicle_I5` sub63 (163×60, 354,0–517,60) — **(탑승중)** 리본 | (0,1) |
| 8 | `textField` (이름) | `$YG760` 14pt, 321×24 | (99,10) |
| 9 | `descriptionTF` | `$YG760` 14pt, 328×24 | (99,32) |
| 10 | `additionalStatTF` | `$YG760` 14pt 우측정렬 160×24 | (267,10) |
| 11 | `slot` | `arkSlot_renew_basic_V2` s(0.71) → 아이콘 자리 | (36,6) |
| 12 | `bookmarkCheckBox` | `Vehicle_bookMarkBtn` (`shared_favoritesBtnM_*`) | (4,16) |
| 15 | `avatarSetTF` | `VehicleListItem_avatarSet_SimpleLabel` (sub55 93×19) | (335,4) |

`setData` 필드: `iconData, vehicleName, description, bindKey, additionalStat, equip, bookmark, newIcon, usableBookmark, id`. 이름 색 `#d5ac66`(선택) / `#afafaf`(비활성).

### 3.3 `VehicleExtendGroup` (소환 모션 패널)

`WindowBG_V2` s(1.624,1.54), `V2WindowDeco`, 제목 `$YoonGasiIIM` 18pt `#fff7e2` `[$]social.ui_title_summon_action`,
`summonActionList`(2열 × 4행, 아이템 244×68) + 스크롤바, `descTitle`, `applyBtn`(`social.ui_btn_summon_action_apply`).
`Vehicle_SummonActionListItem`: 슬롯 + 이름(`$YG760` 14pt) + `origin_applying_mc`(`[$]social.ui_lbl_summon_action_selected` 적용 중 `#ffd200`).

### 3.4 폰트 전수

`$YG760` 12pt ×2, 14pt ×15 · `$YoonGasiIIM` 16pt ×15, 18pt ×1. (1920 스테이지 기준.)

### 3.5 공용 컴포넌트 → 아틀라스 좌표 (shareImageV2 / shareImage, 이미 추출됨)

| 클래스 | 페이지 | 영역 |
|---|---|---|
| `V2step2List_normal / over / selected` | IE / IE / I2 | (0,250 206×35) / (790,212 206×35) / (771,986 208×37) |
| `V2tabBtn_normal/over/down/disabled/selected` | IE | 94×36: (288,212)/(847,173)/(96,212)/(192,212)/(0,212) |
| `V2btn_normal/over/down/disabled/selected` | I46·IE | 103×36: (643,988)/(328,988)/IE(532,173)/(853,988)/(433,988) |
| `V2FindBtn_normal/over/down/disable` | IE | 59×32: (380,358)/(258,358)/(502,358)/(441,358) |
| `V2closeBtn_normal/over/down/disabled` | IB | 19×15: (785,1005)/(827,1005)/(848,1005)/(806,1005) |
| `V2windowTopDeco_center` | IB | (293,1005 326×18) |
| `shared_favoritesBtnM_List_normal/over/down` | IE | (614,517 24×22)/(640,517 22×22)/(784,517 22×22) |
| `shared_favoritesBtnM_seleted/selectedOver/selectedDown` | IE | (585,488 26×26)/(668,458 28×28)/(518,458 28×28) |
| `V2contentLockIcon_01` | IE | (690,0 43×43) |
| `newRedDotIcon` | shareImage I5 | (570,1006 16×16) |

좌표를 못 푼 것: `FC2stepList_overMC`, `FC2stepList_select`(shareimage export는 있음, 비트맵 fill 없음 → 벡터 하이라이트로 추정), `V2Line_HDivision_mc`(shareimagev2, 벡터 선), `V2contentLockDeco`(`localresource_loc_int` 별도 패키지), `V2windowBackground`(shareimagev2 export, 9-slice 창 배경 — `trace_defaultwnd_d3.txt`에 이전 추적 있음).

## 4. 테이블

### `EFTable_Vehicle` (593행, 97열) — 구현된 4종

| PK | Comment1 | Icon/Index | RidingMode | Skill | MovingSkill | ParticleSoundPackage | HideWeapon |
|---|---|---|---|---|---|---|---|
| 6705 | 말 | Vehicle/245 | 1 | 96000·96010·96020 | 96030 | Default_Vehicle | 0 |
| 7104 | 랩터 | Vehicle/107 | 3 | 95750 | — | Default_Vehicle | 0 |
| 7209 | 호버보드 | Vehicle/393 | 5 | — | 95722 | Vehicle_Fly | 0 |
| 9370 | 별빛의 가호 | NewVehicle/47 | 46 | 98381·98382·98383 | 98380 | Vehicle_Fly | **1** |

공통: MoveSpeed 500, CollisionX/Z 80/120(모코보드 60/120), InteractionRange 100, Fov 50, RidingCamera 180000,
CommonActionId0 **53000**(내리기), UseCommonActionIndex **53001**(휘파람=타기), IsSummonVehicleSlot 1, Grade 4.
9370은 `HideWeapon=1` — 탑승 시 무기를 숨긴다. 나머지 셋은 0.

### 탈것 스킬 (`EFTable_Skill`, 이름은 GameMsg)

| ID | 이름 | 쿨 | Icon | 설명 |
|---|---|---|---|---|
| 96000 | 승리포즈 | 5s | Vehicle/63 | 테르페이온이 날개를 펼치며 상체를 들어올린다 |
| 96010 | 휴식 | 8s | Vehicle/64 | 날개를 감싸며 휴식한다 |
| 96020 | 뒷발차기 | 5s | Vehicle/65 | 제자리에서 뒷발차기 |
| 96030 | 대쉬 | 10s | Vehicle/60 | 날개짓을 하며 도약 |
| 95750 | 달리기 | 8s | Vehicle/61 | 랩터가 일정 시간 빠르게 달린다 |
| 95722 | 기분 좋은 질주 | 8s | Vehicle/60 | 모코보드 질주 |
| 98380 | 별빛 질주 | 10s | Vehicle/60 | 전방 고속 이동 |
| 98381 | 밤하늘 | 8s | Vehicle/63 | 밤구름 위로 떠오름 |
| 98382 | 별빛 그네 | 8s | Vehicle/64 | 그네 |
| 98383 | 조용한 밤 | 8s | Vehicle/65 | 별의 기억 |

### `EFTable_CommonAction`

53000 `내리기`(type 7, Icon `vehicle_01`/7 → `Vehicle_0`(512,256)) · 53001 `휘파람`(type 8, Icon `sAction_01`/159 → `Action_1`(320,448)). 53400/53401 `소환의 맹세`/`도약`(type 8, 다른 탈것 타기 모션).
소환 모션 목록은 `OnVehicleAction` 컬럼이 있는 행이지만 1204행이 값 2로 뭉쳐 있어 **어떤 행이 "소환 모션" 후보인지 이 컬럼만으로는 못 가른다.** 실제 목록은 서버가 준다.

### `EFTable_HotKey` / `EFTable_Key`

218행. `Function`이 정수 enum이고 GameMsg의 `tip.name.enum_efkey_function_action_summon_vehicle`은 이름만 있어 **정수↔이름 매핑이 데이터에 없다.** 원작 기본키는 여기서 못 읽는다. 프로젝트는 팀원이 정한 **H**를 쓴다.

### `IconInfo.loa` — 아이콘 좌표 (모두 확인, 크롭 완료 `Vehicle/icons/crops/`)

| 아이콘 | 페이지 | 좌표 | 패키지 |
|---|---|---|---|
| Vehicle_245 (테르페이온) | `vehicle_1` | (192,832 64×64) | `EFUI_ICONATLAS_V` |
| Vehicle_107 (랩터) | `vehicle_1` | (256,384) | 〃 |
| Vehicle_393 (모코보드) | `vehicle_2` | (704,256) | 〃 |
| NewVehicle_47 (별빛의 가호) | `newvehicle_0` | (768,640 **128×128**) | `EFUI_ICONATLAS_N` |
| Vehicle_60/61 | `vehicle_1` | (768,192)/(832,192) | 〃 |
| Vehicle_63/64/65 | `vehicle_0` | (0,0)/(64,0)/(128,0) | 〃 |
| vehicle_01_7 (내리기) | `vehicle_0` | (512,256) | 〃 |
| sAction_01_159 (휘파람) | `action_1` | (320,448) | `EFUI_ICONATLAS_A` |

`vehicle_1/2`는 umodel이 `.tga`로 내보낸다(`.dds`가 아님). 눈으로 확인: 별빛의 가호는 파란 원형 그네, 내리기는 초록 화살표+말, 휘파람은 입술+음표, 스킬 63/64/65는 초록/파랑/빨강 문양.

## 5. 문자열 (GameMsg, `vehicle` 키 1,461개 중 UI 관련)

무비가 쓰는 `[$]` 키 18개는 전부 있다: `vehicle.window_title` **탈것**, `window_accept` **탑승**, `window_release` **탑승 해제**, `window_exit` 닫기, `window_equip` `<FONT COLOR='#23C9FF'> (탑승중)</FONT>`, `window_movespeed` 이동속도 {0}, `window_expiretime` 기한 만료, `window_unlimitied` 무제한, `window_bookmarktoggle/notice`, `window_quickslot_desc`, `ui_btn_sort_name_asc/defalut`, `ui_btn_tab_general/pcroom`, `ui_lbl_set_effect` 세트 효과, `tps_mouse_activation` ALT / `tps_mouse_tooltip`, `social.ui_*_summon_action_*` 5개, `timer.eventtimer_day/hour/min/sec`.

서버 거절 문구 — `VEHICLE_RIDING_RESULT`에 대응: `sys.common.action_result_failure_cannot_spawn_vehicle_by_this_place` 탈것을 탑승할 수 없는 지역입니다 / `_by_zone` 일반 탈것을 탑승할 수 없는 지역입니다 / `_by_indoor` 소환할 수 없는 공간 / `_by_self` 소환할 수 없는 상태 / `action_error_onvehicle` 탑승 중에는 불가능한 행동입니다 / `not_slot_vehicle` 탈것 슬롯에 장착되지 않아 소환할 수 없습니다.

## 6. 사운드

| 소스 | 결과 |
|---|---|
| `vehicle.gfx` | 내장 사운드 0. 컴포넌트 `soundTheme`은 기본(Window=`ST_Common`) |
| `UISoundTheme.loa` | 창 자체 테마 없음. `quickSlotFrame`/`ST_Hud`에 **`VehicleHudShow`** 이벤트 있음 (`INTERACTION_TYPE_VEHICLE` 전환 시 `eventAndSound("ST_Hud_VehicleHudShow")`) |
| 팀장 Sound dump | `VehicleHudShow`에 정확히 대응하는 wav **없음**. 형제 이벤트는 있다: `ui_window_hud_life_show1`, `ui_window_hud_pickup_show1`, `ui_window_hud_transform_show1`, `ui_window_hud_hide1`(Select/). vehicle 것은 `SOUND_VEHICLE*` 패키지가 **미추출**이라 없는 것 |
| 탈것 아이템 사용음 | `item_vehicle_{horse,raptor,hoverboard,turtle,wolf,ladybug,chamkri}1` 각 3종 (UI/Equipment) |
| Wwise 패키지 | `SOUND_VEHICLE`, `SOUND_VEHICLE_2026`, `_NONSTREAM` — 이름 복원됨, 내용 미추출 |
| `EFDataPS_VehicleSet.Default_Vehicle / Vehicle_Fly` | 발자국 = `EFDataPS_FootStep.Vehicle_Default`(`FX_BS_06.FootDown.Par_L_VFootStepDefault_01` 등) / `Vehicle_Fly`(`Par_S_FootStepDefault_001`) — 파티클·발굽 사운드 세트, UI 밖 |

## 7. 아직 없는 것 (명시)

- `vehicle_1/2` 아틀라스에서 나머지 탈것 아이콘 전체(593종) — 4종만 크롭했다.
- `V2windowBackground` 9-slice 창 배경의 실제 크롭 — `Components_Extracted/trace_defaultwnd_d3.txt`의 추적을 따라가면 된다.
- `SOUND_VEHICLE*` Wwise 패키지 추출 — 팀장 파이프라인 재실행 필요.
- 원작 기본 단축키 정수 매핑.
- 레퍼런스 캡처: 탈것 창·탑승 HUD 영상 프레임이 **없다**. 배치 검증은 gfx 좌표로만 가능하다.

## 8. 추출물 위치

```
D:/ClaudeWork/Extracted/Vehicle/
  xml/{vehicle,quickslot,townhud,commonobject,systemquickmenu,exitmenu,masterkey,shareimagev2,shareimage}.xml
  script/{vehicle,quickslot,townhud}/   AS3
  tex/vehicle_i5.{dds,png}             EFUI_VEHICLE 아틀라스 1024×128
  icons/EFUI_ICONATLAS_{V,N,A}/         아틀라스 페이지, icons/crops/ 11장
  tables/…/EFTable_{Vehicle,ItemSeasonVehicle,Skill,HotKey,Key}.db
  particlesound/…/EFDataPS_{VehicleSet,FootStep}.*.loa
  vehicle_audit.json                    감사 도구 출력
```
