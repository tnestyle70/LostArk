# 2026-09-04 에스더 컷인 무비 교체 RESULT

PLAN: `2026-09-04_에스더컷인_무비교체_PLAN.md`

## 구현 완료

- 무비 파이프라인(`Tools/MoviePipeline`): 파일명 난독 해제(`deobfuscate_names.py`), 제약 기반 정확
  키 solver(`bink_key_solver.py`, `ipk_to_bk2.py`가 사용), 게임 `bink2w64.dll` 디코더(`bink_to_png.py`),
  contact sheet(`movie_contact_sheet.py`), 이름 기반 일괄 복호(`dump_named_movies.py`), 컷인 DDS
  쿠킹(`make_esther_cutin_dds.py`). 무비 363개 논리명 목록 `movies_names.txt`.
- 컷인 자산: `Client/Bin/Resources/UI/EstherCutin/<Name>/<Name>_NNN.dds` 1280×720 DXT5 30fps,
  Silian 130 / Waye 111 / Bahunturr 107 / Inanna 111 / Ninave 116 (505.6MB, Git 비추적 —
  팀장 Drive 전달 대상). 슬롯 placeholder `UI/EstherCutin/cutin_empty.png`.
- `NpcCatalog.json` `cutinWindow` → `cutinMovie { framePrefix, frameCount, fps, delayMs }`
  (실리안 0 / 웨이 2600 / 바훈투르 500 ms, 스폰 기준). 파서 `ActorCatalog.*` 갱신.
- `CEstherCutinPresentationService`: 모델 카메라 렌더 전부 제거, `CMainApp` 소유 인스턴스가
  `Data/UI/Esther/EstherCutin.json` 풀스크린 슬롯에 flipbook 1회 재생. `HUD_ESTHER_CUTIN_REQUEST.Clips`
  제거. F1 Debug 패널은 미리보기 버튼만.
- Server `EstherSkillSystem`: `CHARACTER_SELECT_ARENA`에서도 게이지 활성(테스트 편의, UI는 계속 숨김).
  Character Select 로더에 에스더 NPC 3종 prewarm 추가(첫 발동 멈춤 제거).

## 자동 검증

- Client/Server Debug x64 MSBuild 성공(에러 0, 신규 파일 경고 0).
- `Server.exe --contract-test`: 에스더 계약 10개 PASS. 발탄 패턴 계약 12개 FAILURE는 이 변경과 무관한
  브랜치 기존 상태(`Boss-relative combat object leaves navigable arena` 등 발탄 데이터 판정).
- JSON parse(`NpcCatalog.json`, `EstherCutin.json`), `git diff --check` 통과.
- SMELT 기준 복호 SHA `4ad79637…` 불변, ESTHER_*/COMBINEDSKILL_* 65개 복호 실패 0.

## 사용자 확인(2026-09-04)

- Character Select에서 Ctrl+Z/X/C → 풀스크린 컷인 재생 확인. 담당자 확인 결과 에스더 월드 이펙트 미표시는
  기존 상태(이 변경과 무관).
- 타이밍: 실리안(스폰 즉시) 일치. 웨이 1800→2300→2600, 바훈투르 1000→500으로 조정 후 채택.

## 남은 경계

- 실리안 원작 타이밍(키 입력 즉시)은 스폰 트리거로는 1초 늦다. 필요 시 캐스터 `ESTHER_CAST` 스냅샷에
  에스더 슬롯을 실어 트리거를 옮기는 프로토콜 확장.
- 프레임은 최초 재생 시 스트리밍 로드 후 `CUITextureCache`에 상주(발탄 3종 ≈306MB). 필요 시 prewarm/evict.
- 이난나/니나브는 DDS만 준비, NPC 엔트리 없음.
