# 2026-09-04 서버 선택창(로비) 원작 재현 PLAN

시작 시퀀스 Phase 2. 로비(`LEVEL::LOBBY`)를 원작 로그인/서버 선택 화면으로 바꾼다.
Phase 1(캐릭터 선택창)은 `09-03/2026-09-03_캐릭터선택창_PLAN.md`.

## 사용자 결정

- 서버 이름·상태(원활/혼잡)는 사용자가 정한다 → `Data/UI/Lobby/LobbyServers.json`으로 분리.
- 기존 로비 버튼 4개(테스트/게임시작/발탄/베른) 제거. 서버 선택(접속) 버튼 하나만 남기고
  그것이 캐릭터 선택창을 연다. 발탄/베른/테스트는 Debug F1 로비 패널에서만 진입.
- 로고 등장 애니(37프레임) 포함.
- 로비 배경 무비 = `LOGIN_CINEMA_1END.bk2` 오른쪽 정렬 16:9 크롭(앞서 적용).
- 캐릭터 선택창 배경 = 월페이퍼 프리뷰 09번 Real-ESRGAN x4 업스케일(앞서 적용).

## 원작 추출

- `EFUI_LOBBY.upk`(`OVSG0A1M33KDY2YW58JJUH.upk`) `login` efswfmovie →
  `Desktop/LobbyGfx_Extracted/OVSG0A1M33KDY2YW58JJUH/login.gfx`,
  `login_structure.xml`(swf2xml), `login_as3/`(347 클래스).
- 심볼 추적 도구: job tmp `gfx_trace.py <xml> <symbolId> [depth]` (PlaceObject 행렬·shape
  bounds·DefineSubImage→아틀라스 영역·EditText 폰트/색까지 재귀 덤프).
- 아틀라스: `Login_I27`=login_i27.dds(아이콘), `I96`=login_i96.dds(패널/리스트 프레임),
  `I115`=login_i115.dds(하단 그라데이션 1920×1080 중 y 917~1080만 불투명), `IDB`(제목 밑줄),
  `Login_I1..I25.nopack`(960×540 로고 등장 37프레임, 심볼 37→1 순서 = I25→I1),
  `Login_I26.nopack`(660×216 정지 로고), `ICB/ICC.nopack`(트라이포드/스마일게이트 로고).
- 공용: `AnimatedButton_renew18_V2`(componentsv2, V2btn 103×36 9-slice, 접속 버튼은
  scale 1.534×1.278 = 158×46), 행 스킨 `V2step2List_over`(shareImageV2_IE 790,212 206×35),
  `V2step2List_selected`(shareImageV2_I2 771,986 208×37).
- 문자열 테이블(`[$]serverselect.*`, `[$]login.fla_*`)은 LPK/SourceData에서 찾지 못함.
  서버 선택/서버/상태/캐릭터/뒤로/종료/환경설정/생성 불가는 원작 표기 기억, 접속 버튼 라벨은
  사용자 지시대로 "서버 선택". 상태 색(원활 초록/보통 노랑/혼잡 빨강)은 추정.

## 원작 배치(1920×1080) → 1280×720 (×2/3)

루트(MainTimeline): bg_mc(전체) / loginBackLogoImage_mc(무비+I115 오버레이) / LoginLogo
(U0.5,V0, pivot topMiddle → 로고 (630,0,660,216)) / loginServerSelectGroup(Server_Login,
fixedHeight 428, V0.97 → 프레임 원점 y=1047.6−428=619.6) / loginCopyright_mc(V1 하단).

| 요소 | 1920 좌표 | 1280 슬롯 |
|---|---|---|
| 하단 그라데이션 | (0,917,1920,163) | Lobby_BottomGradient (0,611.33,1280,108.67) |
| 로고 등장 애니 | 로고 −(159,135) 오프셋, 960×540 | Lobby_LogoIntro (314,−90,640,360) 20fps 1회 |
| 정지 로고 | (630,0,660,216) | Lobby_Logo (420,0,440,144) |
| 패널 | serverSelector(613,−202)+shape(1,−50) → (614,417.6,705,631) | Lobby_ServerPanel (409.33,278.4,470,420.67) |
| 제목 밑줄 | selector-local (26,92) 649×20 | Lobby_ServerTitleLine (426,339.73,432.67,13.33) |
| 제목 텍스트 | (90,68) 533×30 YoonGasiIIM 22 #fff7e2 | Lobby_ServerTitleText |
| 리스트 프레임 | severListMc(−47,115)+shape(63,35) → (629,567.6,680,296) | Lobby_ServerListFrame (419.33,378.4,453.33,197.33) |
| 헤더 서버/상태/캐릭터 | x 150/402/559 w 238/137/137, y 2, YoonGasiIIM 18 #d5ac66 | Lobby_ServerHeader* |
| 행 | (654,567.6+37i) 626×37, 8행 | Lobby_ServerRow_i (436,378.4+24.67i,417.33,24.67) |
| 행 인원 아이콘 | (522,8) 21×19 | Lobby_ServerRowCount_i |
| 접속 버튼 | (888,889.6) 158×46 | Lobby_JoinButton (592,593.07,105.33,30.67) |
| 종료/뒤로/환경설정 | LayoutEx left 23/139, right −23, bottom −15 → y 331 | Lobby_ExitIcon/PrevIcon/OptionIcon |
| 스마일게이트/트라이포드 | left 30 / right −30, bottom −20 | Lobby_SmilegateLogo / Lobby_TripodLogo |

행 내부(626 기준): 태그 x10 YoonGasiIIM 16(신규 #ffd43e/추천 #00aeff/그 외 #22facf),
서버명 x62 w238 YG760 14 중앙, 상태 x314 w137, 캐릭터 상태 x476 w124("생성 불가"),
인원 아이콘 x522, 인원 수 x548 w50 좌측.

## 구현

- 리소스(Git 비추적, Drive 전달): `Client/Bin/Resources/UI/Lobby/ServerSelect/*.png` 24장,
  `UI/Lobby/LogoIntro/logo_intro_000..036.png`(7.4MB).
- `Data/UI/Lobby/Lobby_Layout.json`: 버튼 슬롯 4개 제거, 위 슬롯 33개 추가(텍스트 전용
  슬롯은 layers 비움), `Lobby_StatusText`를 (240,160,800,40)으로 이동. TitleBackground 유지.
- `Data/UI/Lobby/LobbyServers.json`(신규, vcxproj/filters `96.DataFiles\UI` 등록):
  `servers[] { name, state, tag, characterCount, creatable }` 최대 8행. 사용자가 편집.
- `CMainApp`: `LOBBY_PRODUCT_BUTTONS`/`Resolve_LobbyProductButtonRects` 제거,
  `Load_LobbyServers()`, `Update_LobbyButtons`(행 hover/선택, 접속 → `CCharacterSelectWindowView::Open`,
  종료 → `WM_CLOSE`, 뒤로/환경설정은 hover만, 로비 진입마다 로고 애니 1회),
  `RenderLobbyButtonText`(제목/헤더/행/버튼 캡션/저작권, Release 상태줄 유지).

## 검증

- JSON parse, `git diff --check` 통과. 빌드·실행 확인은 사용자(F5).
- 확인 경로: 실행 → 로비에서 로고 빛 연출 후 정지 로고, 서버 패널·행 hover/선택, 서버 선택
  버튼 → 캐릭터 선택창, 창의 서버 선택(뒤로) → 로비 복귀, 종료 버튼 → 프로그램 종료.

## 남은 경계

- 뒤로/환경설정 버튼은 hover만(원작 뒤로=로그인 페이지, 환경설정 창 없음).
- 원작 waitProcessingMc(접속 대기 스피너), 등급 표시(rating), 프리미엄/PC방 심볼, 이벤트 토큰 미포함.
- 문자열·상태 색은 추정(위 참조). 행 텍스트는 1280 기준 9.3px라 작게 보일 수 있음 → 폰트 px 상수만 조정.
