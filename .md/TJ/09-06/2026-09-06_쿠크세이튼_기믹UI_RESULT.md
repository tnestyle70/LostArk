# 2026-09-06 쿠크세이튼 기믹 UI RESULT

> PR325 통합 갱신: 아래 A단계는 이 UI 브랜치의 당시 구현 기록이다. 통합 코드에서는 v59의 실제
> 광기 수치를 기본 공급자로 연결했고, F1 preview는 별도 Debug override다. 원래 class 입력을 유지하는
> 광대 몸체 교체를 POLYMORPH 스킬 모드로 자동 연결하지 않는다. 현재 검증·남은 범위는
> [PR325 통합 결과](../../GB/09-06/2026-09-06_KOUKU_SAYDON_GATE_FIX_HAMMER_MADNESS_CLOWN_AVATAR_RESULT.md)를 따른다.

PLAN: `2026-09-06_쿠크세이튼_기믹UI_PLAN.md`. 브랜치 `feature/kouku-gimmick-ui` (origin/main c67f8476 기준, 병합 시점까지 main 변동 없음).

## 구현 완료 (A단계)

- 발밑 광기 게이지 `CKoukuMadnessGaugeView` (`Data/UI/KoukuSaydon/MadnessGauge_Layout.json`): 원작 `indicator.gfx`
  madnessGauge 오프셋, 상태 아이콘 3장, 노랑(0~48)/빨강(49~100) 채움, 49% 흰 눈금, 상태 상승 플래시.
- 전투 HUD 인터랙션 모드 `CMainApp::Update_KoukuHudMode` (`Data/UI/KoukuSaydon/KoukuHudModes.json`, `HUD_Layout.json`에
  엠블럼 2슬롯 append): 피에로 Q/W/E, 마리오 Q/W, 댄스 Q/W/E/R 랜덤, 카드 미로 Q. 빈 키는 기존 검은 슬롯, 자물쇠 없음.
  엠블럼은 HUD 중심(x 673.5)에 정렬.
- 쿠크 아레나에서 전투 HUD·보스 바·키 라벨·쿨다운 텍스트·데미지 숫자가 뜨도록 레벨 게이트 8곳 확장.
- 데이터 공급은 `HUD_KOUKU_GIMMICK_STATE` + `_DEBUG` F1 `Kouku UI Preview` 패널(Enable, 광기 슬라이더, 모드 콤보,
  Reroll, 5초 쿨다운). Release에는 공급자가 없어 항상 숨김.
- 리소스 `Client/Bin/Resources/UI/KoukuSaydon/{Madness 7장, Hud 13장, Dance 38장(부록 A 미사용 후보)}` — UI 폴더 전체를
  Drive로 전달.

## 검증

- 자동: `cl /Zs` MainApp/KoukuMadnessGaugeView/Level_KakulSaydonArena/CombatHUDViewModel/HUDLayoutTool 오류 0, JSON parse
  3개, vcxproj/filters XML parse, `git diff --check`, `Tools/Build/test_release_client_surface_contract.py` 4/4 PASS.
  `test_build_domain_pipeline_receipts.py`는 18개 중 2개가 PowerShell 출력 CP949 디코딩 오류로 실패(변경 전과 동일한 환경
  문제).
- 사용자 실행(2026-09-06 새벽, F5 Debug): 쿠크 아레나 F1 프리뷰로 광기 0/49/100 상태·위치, 100 채움, 눈금, 모드별 엠블럼
  중앙 정렬과 고정 슬롯, 댄스 랜덤을 확인 완료.

## 수정 이력

- 자물쇠 슬롯 제거(레퍼런스에는 검은 빈 슬롯만 있음).
- 엠블럼 x 원작 화면 좌표 → HUD 중심 정렬.
- 프리뷰 슬롯 배정 람다가 이전 모드 포인터를 잡던 버그 수정.
- 상태 2에서 빨간 채움 유지, 49% 흰 눈금 추가.

## 남은 경계 (B단계)

- Server 계약 없음: `PLAYER_SNAPSHOT`에 광기/변신/모드 필드가 없어 실제 전투에서는 프리뷰 패널 외 공급자가 없다.
  protocol v58→v59, Server 소유 광기 누적·변신·모드 전환, `CClientReplication::Apply_LocalPlayer` 연결은 보스 담당과 합의 후.
- 변신 시 캐릭터 모델 `MN_RPCZ_00` 교체, 변신 스킬 입력·판정은 이 범위 밖.
