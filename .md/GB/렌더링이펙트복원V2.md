# 렌더링·이펙트 복원 V2

### 보스 연출 WModel과 Character 본체의 연결

쿠크 연출 보스는 기존 Character 본체에 원본 WANM payload를 추가한다. 같은 골격의 Map 파생
모델이라도 전체 WModel을 덮어쓰면 본체의 geometry layout·재질·기존 전투 클립을 잃을 수 있다.
골격/rest basis의 byte 동일성을 확인한 뒤 animation section만 병합하고, 기존 section과 원본
clip bytes·이름을 보존한다. 서로 다른 내용의 같은 클립 이름은 거부하며 변환을 임의로 중복하지 않는다.

WorldSequence의 modelAssetId와 BossCatalog의 animationSetId는 Character 정본으로 연결한다.
소품의 Map 경로, stable object/instance ID, occurrence timing, modelPreScale, material source와
bone/world transform은 별도 계약이다. 전체 모델 교체 후 `animations[0]`으로 본 부착을 샘플하면
다른 전투 클립을 읽으므로 명시한 clip name으로 resolve한다. 재생성 도구에도 같은 처리를 적용한다.

현재 쿠크·세이튼·큰 세이튼 3개 본체에 연출 11개가 병합됐고 원래 1관문 패턴·쇼타임 클립을
보존했다. 실제 CModel 본 행렬의 원본 일치 확인과 최종 GPU 화면 확인은 구분한다.
구체적인 11개 연결·Resources 배포 파일·수치 검증은
[이관 결과](09-21/2026-09-21_KOUKU_CHARACTER_CINEMATICS_RESULT.md)와
[리소스 목록](09-21/2026-09-21_KOUKU_CHARACTER_CINEMATICS_RESOURCES.md)에 둔다.


### 본 이동 단위와 최종 설치 후보의 동일성

Character 골격의 armature scale과 원본 SkelControl translation 단위를 함께 확인한다.
scale100 armature 아래의 metre 단위 로컬 translation에 원본 cm 이동값을 그대로 넣으면
얼굴·가방이100배 늘어난다. BASIS 변환과 단위 변환을 구분하고, 실제 골격 basis를 검증한 뒤
translation에0.01을 한 번 적용한다. 회전·scale이나 다른 클립으로 보정을 전파하지 않는다.
현재 쿠크 본체의 기존 연출11개에 최종엔딩4클립이 연결돼 있으며, 엔딩의 원본 translation
control만 교정했다. geometry·재질·골격과 다른 클립은 byte 단위로 보존했다.

검증 후에도 최종 staging과 설치 파일의 SHA가 검증 후보와 같은지 확인한다. 이전 후보를
복사해 둔 staging은 후속 후보 수정으로 자동 갱신되지 않는다. authored effect의 version을
숫자만 올리지 않으며 해당 형식의 필수 root와 실제 Product reader를 함께 검증한다.
원본 실패 재현·수정 후보 검사·설치 후 검사와 GPU 화면 판정을 각각 기록한다.
구체 수치와 설치 근거는 [빙고 재생 복구 결과](09-21/2026-09-21_KOUKU_COMPLETE_PLAY_WORLD_AND_BINGO_RESULT.md)를 따른다.

### 물리 해상도와 카메라 영역의 복원 경계

DPI 미선언1280×720 렌더가150% 모니터에서1920×1080 크기로 보이는 경우와 카메라 배율을
분리한다. EXE manifest·창 DPI·실제 client/swapchain/RT를 함께 조사한다. 같은16:9와 동일
카메라의 균일 확대는 화면 대비 맵 크기를 바꾸지 않는다. native pixel 렌더링은 내부 해상도와
선명도를 바로잡는 경로이며 원본 조명·재질·개별 Effect의 동등성 증거가 아니다.

현재 경로는 PerMonitorV2 physical client를 Engine Resize_Viewport로 제출하고, full-size
MRT/HDR/scene post/source-light-mask depth 및 half-size Bloom/SSAO와 depth/texel을 먼저
준비한다. swapchain 성공 뒤 기존 RenderTarget wrapper의 GPU 자원과 viewport를 commit해
MRT alias를 보존한다. 보관된 backbuffer output 참조와 context binding을 해제하고 같은 크기의
exclusive fullscreen 전환에도 ResizeBuffers를 수행한다. 품질·환경·fixed shadow·Level을
재초기화하지 않는다. 자원 준비 실패와 복구 불가능한 device/driver 실패는 구분해 전달한다.
UI reference resolution과 물리 pixel atlas 선택도 별도 계약으로 유지한다.

쿠크 원본 PS export43의 ViewDistance1900cm는 입구 camera volume의 값이다. 변환한
Z[-86.428,1.409]m는 세이튼 전장 Z737.53m를 포함하지 않으므로09-14의1관문 전체19m 해석은
현재 전장에 적용할 근거가 아니다. 원본 convex 경계 안에서만19m를 적용하고 바깥은 공통
CDO16m를 기준으로 삼는다. 아직 해독하지 못한 native camera update·줌 상태까지 복구했다는
뜻은 아니며 원작 최종 framing은 사용자 화면 대조가 남는다. manual profile은 source region
사용 여부로 분리하고 기존 사용자 pose를 보존한다. 공 먹기 화염은 실제 source occurrence와
TypeData/StartSize/notify/owner basis를 별도로 비교하며 카메라 수정에 전역 배율을 섞지 않는다.

구체 근거·자동 검사·제품 빌드와 사용자 화면 미확인 상태는
[물리 해상도·카메라 결과](09-20/2026-09-20_NATIVE_RESOLUTION_DPI_AND_KOUKU_CAMERA_IMPLEMENTATION_RESULT.md)에 기록한다.

### V1/V2 이펙트 상한과 Release 실패 진단

같은 패턴의 이펙트여도 실제 occurrence가 V1_EFFECT인지 GROUP/V2인지 먼저 구분한다.
V1 PresentationService의 scene/owner budget 거절은 새 spawn admission이며, V2 Mesh/Decal을
그 budget으로 판정하지 않는다. 현재 B2 P11은 V2 Mesh/Decal, P29 내려치기는 V1 경로다.
asset 실재·예약량·동시 실행량과 Engine provider/draw 실패를 서로 구분한다.

Release에서도 EffectFailure.user.log와 RendererExit.user.log에 준비/admission/draw/프레임
실패를 bounded 기록한다. 일반 활성 이펙트를 매 프레임 기록하지 않으며 느린 준비만 별도
기록한다. V1 로컬 격리와 V2 draw 실패의 frame 전파를 같은 증상으로 취급하지 않는다.
Server entry failed는 공통 복구 문구이므로 실제 Client session reason/queue와 같은 시각의
Server session/RoomPerf를 비교한다. 클라이언트 GPU 부하를 Server 부하로 곧바로 단정하지
않으며 상한/timeout을 일괄 확대하지 않는다. 컷씬 배경만 검게 되는 경우 actor/effect가
남는지 확인하고 경계의 map visibility·scene·light/camera owner를 먼저 대조한다.
검증 수치와 사용자 화면 판정은 [쿠크 통합 RESULT](09-18/2026-09-18_KOUKU_RAID_COLLIDER_SOUND_INTEGRATION_RESULT.md)에 구분한다.


### 탑승자 animation donor의 armature 배율

AnimationSet의 skeleton hash와 bone 순서 일치는 animation import 배율의 일치를 보장하지 않는다. `Attach_AnimationSet`은 donor rest/mesh/preScale을 복사하지 않으므로 실제 target body의 rest 및 기존 clip scale key와 donor의 같은 armature key를 따로 비교한다. 잘못된 고정 import 배율은 해당 donor key만 근거에 맞춰 교정하고, 전체 Character/vehicle/socket에 역배율을 강제하지 않는다. 위치·회전·시간·다른 bone을 보존한 뒤 실제 target mesh와 inverse bind를 사용한 skin 결과를 확인한다. 차원술사 탑승자 6개 donor의 source 100 → 현재 body 1 교정과 별도 Resources 배포 근거는 [탈것 확장 결과 G09](../JS/09-14/2026-09-14_VEHICLE_ADDITIONS_RESULT.md#g09-2026-09-19-차원술사-탑승-시-100배-확대-교정)에 기록했다.

### Source sprite의 회전축 편집과 Billboard

카드 출력의 원본 EPAL_Rotate_Z는 좌표계 변환 뒤 Client ROTATE_Y로 소비된다. sourceRecipe와 기존 followEmitterAxisRotation을 켠 ROTATE_X/Y/Z는 emitter가 정한 축을 유지하며 카메라를 향한다. Billboard를 꺼 source facing을 우회하지 않고 이 옵션으로 방향을 편집한다. local space는 현재 root, world space는 출생 root를 사용하고 opt-off의 기존 facing 동작은 보존한다. 실제 quad 앞면·pivot·180도 반전·평행 camera를 수치로 검사하며 화면 판정과 구분한다.

source 이동 곡선은 nested distribution의 CDO 기본값을 먼저 확인한다. 손 본과 root snapshot 사이 배율·방향 차이는 해당 occurrence에서 측정하고 전역 shader 보정으로 전파하지 않는다. 원본 곡선 기반 연속 궤적의 저작 보정과 미해독 native 방향 설정을 구분한다. 후속 연결과 검증은 [세이튼 구현 결과](09-17/2026-09-17_SAYDON_CARD_TRUMPET_CHARGE_IMPLEMENTATION_RESULT.md)의 G18 이후를 따른다.

### 공중 패턴의 source Stage와 착지 앵커

기존 `ALBION_AIRBORNE` 경로는 설치 모델의 하강량을 현재 높이에서 지면까지 정규화한다. JUMP의 0 ms는 첫 sample부터 초기 높이를 설정하며 양수 시간은 기존 상승 보간이다. 타깃 착지의 native root lateral 성분은 앵커 위치를 바꾸지 않고, SLAM이 소유한 Stage 이후에는 다음 원본 root 상승·이동을 재개한다. stage 번호나 clip 이름만으로 창을 판단하지 않고 startOffsetMs와 실제 native curve를 함께 확인한다. optional SELECT 정책은 시작 시 navigation ground XYZ를 고정해 착지와 선택 Effect group에 공유하며 기본 APPEAR 정책은 등장 시점 재조회를 유지한다. 검증 수치와 실제 등록·Publish 상태는 [세이튼 구현 결과](09-17/2026-09-17_SAYDON_CARD_TRUMPET_CHARGE_IMPLEMENTATION_RESULT.md)의 G16~G17에 기록한다.

### Orbit 종료 폭발과 sector 메시의 형상 경계

Orbit 입자의 최종 위치를 사용하는 death event는 화면 frame과 같은 Orbit offset 평가 함수를 소비한다. event 위치에 회전된 offset을 두 번 적용하거나 원점에서 폭발시키지 않으며 Effect root 회전 뒤 frame/event 위치를 함께 대조한다. 비둘기 네 마리 묶음은 이 경로에 연결됐다.

피자의 angular geometry와 재질 opacity/flow mask를 구분한다. 원본 sector mesh를 회전 조합한 빈 각도는 해당 mesh layer의 측정값이며 별도 halfcylinder·sprite까지 공통 각도로 잘린 결과가 아니다. 현재 피자 묶음은 별도로 추가했던 exp02 원형8개를 제외하고 원본 exp03의16개 요소를 유지한다. 범용90도 angular mask나 빈 구간을 채우는 임의 복제는 추가하지 않았다. 실제 삼각형 coverage와 원본·설치 native shader의 alpha 비교는 각각의 입력 범위에서만 유효하며 사용자 화면의 모든 틈이 원본 의도임을 증명하지 않는다. 개별 수치와 남은 화면 경계는 같은 결과의 G20 이후를 따른다.

### V1 Beam 직접 소비와 앵커 그룹 편집

CASCADE_BEAM_V1은 직접 Playback::Sample_Trail에서 TypeData source/target을 소비한다. document-owned projection 필요 여부는 carrier 종류별로 구분하고 Beam+Ribbon/baked 혼합 문서에서도 source Beam을 보존한다. 실제 실행 admission은 codec뿐 아니라 Catalog/projection/Playback 경로로 검사한다.

부착 그룹의 전체 회전은 SocketLocalTransform의 위치를 고정하고 회전을 바꾸는 방식으로 소켓 원점을 유지한다. 평균 Element 중심에서 개별 transform을 다시 계산하는 기존 기능과 UI에서 구분한다. source renderer shape·velocity·local/world 정책을 변경하지 않고, 동일 runtime anchor slot의 conflicting definition은 변경 전에 거절한다. 후속 수치·사용자 관찰 경계는 [세이튼 구현 결과](09-17/2026-09-17_SAYDON_CARD_TRUMPET_CHARGE_IMPLEMENTATION_RESULT.md)의 G07에 기록했다.

### native static 배경의 ambient 입력과 dragon local space

Lance ALT V의 바위 배경은 설치 메시가 빠진 상태가 아니라 Mesh carrier가 native PS의 ambient/sky
입력을 채우지 않는 결함이 확인됐다. native782~800 Mesh만 현재 committed scene directional ambient를
`g_LanceVAStaticAmbient`로 전달한다. 원본 shader 수식은 유지하며 이 연결을 원작 연출의 전체
환경조명 복원 완료로 취급하지 않는다. UE skylight hemisphere는 현재 scene owner가 없어 0을 유지한다.
실제 native786 product shader의 통제된 texture/ambient 수치 readback으로 0→색→0 전환을 검증했다.
Warlord17250의 static 배경도 같은 Lance native family를 사용하므로 이 입력 연결을 함께 소비한다.
클래스 이름만 보고 Warlord family의 ambient bind가 해당 배경을 덮는다고 판단하지 않는다.

사용자가 V/ALT V dragon 모델만 local space를 켜도록 범위를 확정하여 실제 세 모델의 8문서 23행을
원본 Required true로 복구했다. 본·socket·Transform과 다른 particle의 world-space 설정은 보존했다.
SourceRecipe가 true여도 DIRECT_AUTHORED_DOCUMENT의 저작 false를 자동 덮지 않는 점을 함께 점검한다.
세부 범위와 빌드/사용자 확인 경계는 [창술사 G13/G14 결과](09-11/2026-09-11_TIGER_HORSE_ANIMATION_AND_LANCEMASTER_ALTV_RESULT.md)다.

### 부착 이펙트의 원본 localSpace와 선택적 조명 제외

`DIRECT_AUTHORED_DOCUMENT`는 원본 Required 값을 reconstructed 경로에서 다시 적용하지 않는다.
따라서 본·카메라 부착 metadata가 정상이어도 저작 `particle.localSpace=false`이면 기존 입자가
생성 시점의 root를 계속 사용한다. 원본 `buselocalspace`, 실제 cue와 설치 모델, 앵커 갱신 순서,
살아 있는 입자의 변위를 함께 검사한다. 차원술사 V는 해당 camera_view 17개의 원본 true를 복구했고
실제 Catalog→Playback에서 카메라 +1m에 입자 +1m 이동을 확인했다. 초기 바닥·유리 재질의
시각 문제 전체가 이 수정으로 해결됐다는 뜻은 아니다. 사용자가 world-space를 요청한 다른
이펙트까지 전역으로 되돌리지 않는다.

차원술사 T의 소환 모델은 사용자 요청에 따라 exact cue만 기존 mask/depth와 NONBLEND를 유지하고
diffuse/specular 조명 항 대신 emissive RGB를 출력한다. scene post와 fog는 기존대로 소비한다.
이는 해당 모델의 조명 제외 정책이며 원작 전체 shader 복원 완료 판정이 아니다.
세부 근거는 [차원술사 T/V 결과](09-15/2026-09-15_DIMENSIONMASTER_T_UNLIT_V_RECTANGLE_IMPLEMENTATION_RESULT.md)다.

### 연출의 플레이어 도착과 카메라 종료

쥐 불꽃에 맞춘 플레이어 도착은 Effect 좌표를 Character Transform에 직접 복사하지 않는다.
원본 발생기의 XZ와 현재 occurrence 변환을 합성한 뒤 Server navigation의 지면 Y를 사용하고,
Sequence Logic이 기존 typed command로 요청하여 Server가 목적지를 검증한다. 현재 연결은
Sequence 저작의 `ROOM_PLAYER_ARRIVAL`이며 같은 방의 최대 네 명을 고정 슬롯으로 배정한다.
Effect 발생기 자체의 공중 Y와 플레이어의 발 위치를 혼동하지 않는다.
일반 Play는 종료에서 follow 카메라로 돌아오며, 명시적 Complete 재생만 승인된 관문과
저장 Pattern Flow를 이어간다. 이동 요청의 성공과 실제 이펙트·카메라 화면 판정은 구분한다.
세부 근거는 [Sequence 결과 G26](09-14/2026-09-14_KOUKU_SEQUENCE_PLAYBACK_EDITOR_IMPLEMENTATION_RESULT.md)이다.

이 문서는 Character Select·캐릭터·무기·스킬 이펙트를 복원하면서 확인한 원리, 반복 결함,
실제 연결 범위와 다음 확장 순서를 유지하는 공통 노트다. 이름의 V2는 이 기록의 두 번째
복원 단계이며 **Effect Tool V2 전용 문서나 새 런타임의 이름이 아니다.**

복원·손튜닝 문서는 F1 `Effect Tool V1`에서 계속 편집한다. `Effect Tool V2`는 별도 창으로
leaf/group·target attachment를 편집하며 V1을 열 때 함께 생성하지 않는다. 각 도구의 draft와
Sequencer를 분리하고, 기존 V1의 선택 항목 지연 로드·저장 성능 개선을 유지한다.

발탄 검증용 action Full Restore 35개는 원본 action/stage/clip/time metadata를 통해
실제 애니메이션과 함께 재생한다. All Effects 패턴의 PRODUCT 바로 아래 `[FULL RESTORE]`는
SourceActionIds와 현재 ClipOccurrences를 모두 비교하며, 라이브러리 직접 열기는 해당 원본
clip을 사용한다. editor preview와 Product cue 승격은 별개이며 제품 full.restore cue는 0개다.
`.restore`를 player recovery로 오분류하지 않고 기존 World Preview와 Valtan animation clock을
사용한다. notify delay·root basis·scale을 preview에서 중복 적용하지 않는다.

맵의 source BG 검증은 catalog/shader와 CModel이 같은 supported branch를 받아야 한다.
subspecular-only와 grass emissive flicker는 기존 shader 지원을 따른다. embedded material의
누락 PNG는 실제 source DDS 대응을 확인해 교정하며 geometry/scope를 바꾸지 않는다.
빌드·실행 경계와 비UI 검증은 [맵·발탄 결과 G24-V](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md)에 기록한다.

새 작업은 먼저 해당 표면/스킬의 현재 코드와 이 노트를 확인한다. 확인된 원인과 해결 방법은
이 문서의 해당 항목을 갱신하고, 날짜별 변경 파일·빌드 로그·실패 과정은 대응 RESULT에 둔다.
오래된 현재 상태를 남긴 채 아래에 반대되는 내용을 계속 붙이지 않는다. 조사만 한 후보,
제품 소스에 연결한 항목, 빌드한 항목, 사용자가 화면에서 확인한 항목을 구분한다.

현재 작업의 상세 근거: [구현 PLAN](09-08/2026-09-08_CHARACTER_MATERIAL_AND_EFFECT_ROUND2_ESTIMATE_PLAN.md),
[구현 RESULT](09-08/2026-09-08_CHARACTER_MATERIAL_AND_EFFECT_ROUND2_ESTIMATE_RESULT.md).

## 화염·masked LocalVF·distortion 동반 합성의 현재 연결

워로드 Alt+V 시작 공간의 활성 cylinder는 최초 full 생성부터 native 재질 미연결로 빠진
항목이었다. 기존 family의 native673으로 원본 PS/VS와 세 texture를 연결하고 원본 발생기를
추가했다. 이 재질은 mesh UV 대신 화면 좌표·viewport 비율·SceneDepth를 읽는다. 원본의
실제 sampler와 varying 소비를 기준으로 복구하며 legacy의 미해결 texture를 사용하지 않는다.
원본 PS 수치 대조, 실제 전체 Product Stage와 발생 재생을 검증했다. 제품 빌드·화면 확인의
상태는 [워로드 결과 G17](09-09/2026-09-09_WARLORD_ASVF_FULL_RESTORE_IMPLEMENTATION_RESULT.md)에 둔다.

워로드 후속 배치에서는 원본 notify 여섯 개의 FRotator yaw가 저장 변환에서 모두0으로
사라져 마지막 균열이 겹쳤다. source 회전은 발생 중심과 axis-locked quad의 방향을 각각
소비하는 기존 입력에 연결한다. 위치 행렬만 맞춘 것을 최종 띠 방향 복구로 대신하지 않는다.
별도 root에 저작한 큰 방패와 본 부착 낙뢰는 설치 b_effectroot의 축을 실측해 해당 발생의
socket에서만 맞춘다. source 기준 배치 반경과 사용자 FOV·높이 튜닝, 실제 재생 검증과
화면 확인 경계는 [워로드 결과 G18](09-09/2026-09-09_WARLORD_ASVF_FULL_RESTORE_IMPLEMENTATION_RESULT.md)에 기록한다.

화염링 `backstep.ring`의 hoop2875는 원본 masked PS가 particle alpha를 읽는 `CB0[0].w`가
0으로 남아 전부 discard됐고, twirl2876은 이미 opacity가 포함된 RGB에 최종 합성이 alpha0을
다시 곱했다. 같은 화면에서 서로 다른 두 재질 층이 빠지는 결함이다. 기존 창술사 masked
LocalVF와 같은 방식으로 정확한 PS/VS·native float4 wire가 확인된13개 masked 프로그램의
row0을 `input.color`에 연결하고, 원본 additive/PS A0가 확인된130개 early-return의 출력
coverage를 일반 dispatch와 같게 연결한다. 원본 mask·RGB·fade·왜곡, 정상 translucent를 보존한다.

도화가 Alt+V 31930의 나비 native536도 같은 masked prefix 결함을 고쳤다. 정확한 원본
PS `390b1fe8a7081c45bf96c8afc4bf11e9` / VS `d17daa101dec2b4493fce2f510407f32` 조합에서만
CB0[0]을 particle RGBA에 연결한다. Group512와 생성기 수정, 원본 packed PS 수치 대조,
현재 전체 문서의 나비9개 발생, 생성 동치와 Mesh512 FXC를 확인했다. 설치 CSO와 실행 중
Client 반영 및 최종 화면은 [도화가 결과 G12](09-09/2026-09-09_ARTIST_CORE_FULL_RESTORE_IMPLEMENTATION_RESULT.md)의 별도 상태를 따른다.

나비 후속 실측에서는 같은 문서 안에서도 카메라 anchor와 실제 골격 anchor의 배율이 달랐다.
설치 메시의 cm→m 변환과 별개로 pelvis combined basis의0.01이 다시 곱해졌다. 기존 본 배율
제거 판단은 document와 runtime anchor ID를 함께 받아 확인된 `GRABBED_SOCKET_BODY`만
처리하며, Tool과 제품이 같은 인자를 전달한다. 하나의 slot을 공유하는 발생은 같은 정책을
사용해야 한다. 문서 전체를 등록해 다른 발·카메라 발생까지 확대하지 않는다. 원본 배율,
설치 CSO 실행 코드 일치와 미확인 화면 범위는 [도화가 결과 G13](09-09/2026-09-09_ARTIST_CORE_FULL_RESTORE_IMPLEMENTATION_RESULT.md#g13-09-15-나비-실제-골격-배율과-solo-검토)에 둔다.

화염 world-offset2874/3316은 원본 TEXCOORD5 world position과 기존 emitter WorldToLocal
uniform을 함께 소비한다. world-position firecross3604에는 world position과 neutral
pre-view translation/opacity만 필요하다. 이름이 같은 화염이어도 이 engine prefix를
mesh particle color와 혼합하지 않는다. 생성기와 설치 함수, 실제 renderer binding을 함께 유지한다.

이 변경은 기존 Native family와 Resources를 사용한다. 입자 발생량·회전·크기·저작 파일은 바꾸지
않았다. 과거2580 불뿜기의 SpawnPerUnit birth0 복구와 이번 shader 결함은 별개다.
현재 검증 범위와 제품 반영, 사용자 화면 대기는 [화염 재질 결과](09-14/2026-09-14_FIRE_MATERIAL_CLIPPING_RESULT.md)에 둔다.

## 관문 조명과 저작 Enabled의 소유권

쿠크 3관문의 어두운 Scene Profile과 기존 배경 조명 제외는 관문 상태가 소유한다.
청색 스포트라이트와 새 저작 광원의 enabled·색·밝기는 MapLight source가 소유한다.
현재 `Prepare_GateMapLights`는 원본 `source_baked_character`/`source_direct`와 기존
1·2관문 광원만 제외하고 나머지 광원의 enabled를 보존한다. 단일 청색 ID 강제 활성화는
새 백색광과 off 입력을 모두 버리므로 재사용하지 않는다. 청색광 삭제도 유효한 문서 변경이다.

`Same_MapLightSource`의 enabled·색상·개수 비교 뒤 관문 문서가 다시 만들어지고,
`CMapLightPresentationRuntime`은 활성 광원만 기존 transient 경로에 제출한다.
Engine은 transient 목록과 조명 RT를 매 프레임 비우므로 이전 프레임의 청색 조도를
재사용하는 구조가 아니다. 구운 RNM·정적 그림자와 shader uniform 재설정 최적화는 별개다.
변경 전후 회귀·컴파일·배포와 사용자 화면 확인의 범위는
[3관문 조명 결과](09-14/2026-09-14_KOUKU_GATE3_LIGHT_ENABLED_RESULT.md)를 따른다.

## 먼저 이해할 원리와 확장 순서

최종 그림에는 네 종류의 입력이 함께 필요하다.

| 입력 | 쉬운 뜻 | 누락됐을 때 생기는 실제 문제 |
|---|---|---|
| 모델·텍스처·UV·색 채널 | 그릴 모양과 재료 | 추가 UV 누락, 잘못된 material 선택, 거리에 따른 깜빡이는 무늬 |
| 재질식·shader 분기·uniform | 그 재료를 빛과 섞는 방법 | 금속이 갈색 고무처럼 보임, 사각형 sprite, 반사색 손실 |
| 발생 시간·위치·회전·기본값 | 어디서 언제 어떻게 움직이는지 | 재질은 맞는데 100배 크거나 90도 돌아가고 파편이 한쪽에 모임 |
| 장면 입력·조명·합성·카메라 | 장면 속에서 최종 표시하는 방법 | 유리 배경 왜곡 부재, lightmap 중복, 노출 손실, 전체 재생 누락 |

따라서 텍스처를 찾았거나 유리 하나가 보였다는 사실은 큰 진전이지만 전체 스킬 완성과
같지는 않다. 반대로 새 스킬마다 모든 코드를 처음부터 만들 필요도 없다. **같은 원본
프로그램과 입력 계약이면 재사용하고, 다른 계산·geometry·장면 입력만 추가한다.**
색만 다른 MIC는 값 변경으로 끝날 수 있으나 static switch·opacity·굴절·화면 합성이 다르면
같은 유리라는 이름만으로 같은 프로그램이라고 판단하지 않는다.

현재 흐름은 full restore에서 원본 재질·발생을 복구한 뒤 사용자가 개별 재생으로 핵심을
남기는 것이다. W/E full의 사용자 선별 결과는 보존한다. Q 튜닝은 성공2개+실제 crack1개이며
파편 크기는 full Q mesh03의 원본 size4모듈/SourceScale1과 같다. R/A 검격 교정은 현재
사용자가 선별 중인 full 문서를 대상으로 한다. W/F/V의 파편은 cube가 아닌 crack이다.
Alt V 튜닝은 원본 box25행+crack1행+원본 CModel cue1개와448 camera key를 사용한다.
초기2개 mesh와 후반 skinned model cue에 occurrence의 연출 직전 HDR/Bloom 캡처와 ALT178
native material을 연결했다. 현재 full311행/tuning26행과 retimed cube WModel의 설치를
확인했으며 원작 capture camera의 동일성과 사용자 화면 판정은 별도다.

작업량은 파일 개수보다 빠진 계약에 좌우된다. 이미 연결한 재질·모델·atlas 조합의 배치 추가는
작고, 새 MIC/UV/분기 연결은 그보다 크며, 카메라 이펙트·자식 모델·화면 후처리가 얽힌 Alt+V는
별도 재생 구성 작업이다. 전체 원본 목록의 admission 조사 없이 기간이나 완성률을 제시하지 않는다.

## 이펙트의 실제 연결 범위

### Logic 생성과 Timeline Effect의 수명 소유권

알비온 파란 원은 기존 합본 `effect.kouku.albion.bluecircle.warning.impact.runtime`이
2초 예고 뒤 폭발을 소유한다. Server의 typed `ALBION_BLUE_CIRCLE`는 기존 CombatObject
spawn/snapshot/despawn과 Client visual join을 재사용하며 7초 수명으로 tail을 보존한다.
Logic의 이름만 있는 랜덤 탐색·Summon은 이 소비자를 대신하지 못한다. Timeline에 별도로
합본을 짧게 배치하면 Server 생성이 생기는 것이 아니라 해당 시각에 Effect만 재생하고 정리한다.
nav 랜덤 보충과 선택 플레이어 위치는 Server가 한 번에 확정하며, 현행 발탄 도끼처럼 생성 후
고정된다. 무피해 효과의 간격을 damage hit으로 위장하지 않는다. 기존 shader/particle/재질과
Resources는 그대로 사용한다. 소스 검증과 아직 설치하지 않은 P39 연결안의 경계는
[알비온 결과](09-14/2026-09-14_KOUKU_ALBION_RANDOM_VOLLEY_RESULT.md)를 따른다.

### 오브젝트 추출에서 재질·환경 입력을 보존하는 공통 절차

새 오브젝트와 기존 모델의 정적·애니메이션·반전 변형 모두 같은 절차를 따른다. geometry와
D/N/S 텍스처만 추출한 결과를 원본 재질까지 적용한 결과로 취급하지 않는다.

공통 도구와 입력 형식은 [LevelPlacementExtractor README](../../Tools/LevelPlacementExtractor/README.md#팀-재추출-순서-가시성기하재질을-함께-보존)를 따른다.
schema3는 actor/component·archetype/CDO의 source visibility를 보존하며 LV_MODULE/nav/water/FX
이름으로 숨기지 않는다. 기존 Bern/variant cook은 실제 glTF의 추가 UV·COLOR0·tangent.w를
WModel에 보존한다. 공용 source parameter 추출기는 명시적0/false/null과 부모 상속을
보존하고 미기록 기본값을 별도 보고한다. 원본 mip 회수는 mip0 압축 byte 일치 후 수행한다.
source surface compiler와 scene consumer는 정확한 slot/MIC/필드 coverage 및 입력·texture
hash를 대조한다. 알 수 없는 scalar나 render flag를 family 전체 지원으로 대신 승인하지 않는다.
MIC switch OFF만으로 vertex/pixel의 다른 경로까지 해당 값을 쓰지 않는다고 단정하지 않는다.

1. 원본 mesh의 실제 material array와 actor/component override를 찾고, 각 slot의 MIC 상속·
   static switch·shader permutation·uniform 기본값·texture/sampler를 해석한다. 이름이 같은 slot도
   variant MIC가 다를 수 있으므로 model ID와 원본 object 경로를 함께 확인한다.
2. geometry는 필요한 UV0/UV1/UV2, vertex color, normal/tangent와 단위를 보존한다. WMA2의 일반
   텍스처 경로 외에 native family/program과 named parameter, mask 채널, roughness/specular,
   IBL texture 또는 cube, BRDF LUT, 색 공간과 sampler를 해당 material descriptor에 연결한다.
   WModel 변환 자체가 이 shader 계약을 자동으로 추론하지는 않는다.
3. actor/weapon의 기존 exact model 재질은 `CActorCatalog::Build_ModelLoadDescription`으로
   `CModel → CMaterial`에 전달한다. 파생 World Object는 원본과 slot이 같을 때
   `materialSourceModelAssetId`로 기존 catalog 재질을 참조한다. 배경 source는 승인된 Area
   `mapMaterialBindings`를 사용한다. 한 모델의 family나 program 번호를 모든 오브젝트에 복사하지 않는다.
4. 맵의 직접광·ambient·환경 영역·fog·노출은 장면 입력으로 별도 확인한다. 원본 IBL 반사와 장면
   조명은 서로 대체하지 않는다. 배치에 종속된 RNM/lightmap/static shadow를 움직이는 파생 모델에
   복사하거나 빠진 반사를 맵 ambient 증가로 가리지 않는다. geometry의 음수 축 반전은 광학 반사가 아니다.
5. 실제 target slot과 descriptor 입력, Resources 파일·추가 UV, publisher 결과와 runtime 소비를
   확인한다. 모델·재질·입력·게시·빌드·사용자 화면 확인 상태를 구분한다. 미지원 family 또는
   명시 source의 누락은 오류를 보존하며 이전 승인 객체/저장을 유지한다.

변환 명령은 [ModelAssetConverter 사용법](../../Tools/ModelAssetConverter/README.md), 저장·게시와
재질 우선순위는 [Area 데이터 계약](../TEAM/AREA_DATA_LAYER_GUIDE.md), pixel 입력 계약은
[Effect family/runtime ABI](../TEAM/EFFECT_FAMILY_RUNTIME_ABI_RESTORATION_GUIDE.md)를 따른다.
Resources는 기존 일곱 폴더의 Drive 관리 입력이며 새 manifest나 binary Git 추적을 요구하지 않는다.

맵·보스 복원도 같은 원본 계산/실제 소비 입력 원칙을 사용한다. 쿠크 보스·소품 33모델/50재질 슬롯과 맵의 원본 재질·환경 영역·안개를 연결했고, 베른은 원본 geometry·배치·RNM·정적 그림자 및 물·반투명·하늘을 같은 CModel/CMaterial로 통합 중이다. 현재 코드 연결, 마지막 빌드, 사용자 확인은 [쿠크 재질 결과](09-11/2026-09-11_KOUKU_BOSS_PROP_NATIVE_MATERIAL_IMPLEMENTATION_RESULT.md), [베른 직접광 결과](09-11/2026-09-11_BERN_NATIVE_FORWARD_IMPLEMENTATION_RESULT.md), [베른 조명 결과](09-11/2026-09-11_BERN_LIGHTING_GEOMETRY_IMPLEMENTATION_RESULT.md)에서 구분한다. 이 문구를 전체 원본 frame 복원 완료로 해석하지 않는다.

| 범위 | 연결/확인된 내용 | 아직 끝나지 않은 내용 |
|---|---|---|
| Kouku 독립 Resource의 원본 본 부착 | sourceModelPreview의 actor·clip·Source In을 기존 CNpc/CModel Preview로 준비. 총구발사 등 FOLLOW bone 11문서/138요소의 metadata와 실제 모델·본 샘플 4,632회 검사 통과 | Product의 기록되지 않은 animation/camera history를 대체하지 않는다. 새 Client의 실제 재생·시각 확인은 사용자 대기. [결과](09-12/2026-09-12_KOUKU_RESOURCE_SOURCE_PREVIEW_RESULT.md) |
| Kouku Composition Effect/공포 | V1 Effect/Element와 V2 GROUP의 occurrence 수명·anchor 기록, BODY/WEAPON bone 선택을 기존 renderer에 연결. FEAR Result가 Scene/캐릭터 Light/지연 ScreenPost를 함께 소유. 머리 레이저·망치 anchor·얼굴 Result를 원본과 제품 데이터에 연결하고 Product 빌드 완료 | 사용자 실제 재생과 시각 확인 필요 |
| Kouku 공 World Object | 원본 mn_rhcn_00_mi의 monster PBR program21, S mask·hdr07_1 IBL·BRDF LUT를 포함한8개 입력을 기존 CModel material override에 연결. 원본 shader와73,728픽셀 수치 대조 통과 | worldsequences426 materialProfile 통합·게시와 정규 Debug Product 빌드 완료. 09-10 사용자 요청범위 인게임 검증 완료 보고. BG 바닥 재질 복사나 map ambient 증폭은 하지 않음 |
| Kouku 잡기 손 socket | CNpc의 실제 BODY `bip001-l-hand` combined matrix와 현재 actor root를 기존 `IPlayerHandGripSocketSource`에 연결. 게시된 패턴별 grip의 미터 단위 보정을 사용하며 발탄의 보정값을 복사하지 않음. 짧은 판정과 Hold 종료를 분리해 게시·빌드하고 Server 부착/해제 검증 완료 | 캐릭터 손 위치와 해제 장면의 사용자 시각 검증은 미완료. 실행 증거는 대응 RESULT 참조 |
| Artist F | 사용자가 손으로 위치 등을 조립해 만족한 선례 | 이것만으로 모든 source family/자동 배치가 완성됐다는 근거는 아님 |
| Q 비교본 `이펙트_차원술사Q` | 기존 2 element, 사용자가 유리 표현 확인. 손 조정 비교본 보존 | 전체 Q 원본 발생 항목을 대표하지 않음 |
| Q 전체 `이펙트_차원술사Q_전체` | 활성 원본 cue의 10 element, 선택 native 재질 42~51, 원본 Resource 연결. 사용자가 새 Q의 수평 검격·빛 화면을 제공 | 사용자는 Q2의 큰 사각 유리를 손튜닝 출발점으로 선호. 전체 원본 유사도/Play All·Seek·저장 승인은 별도 |
| Q 공통 입력 교정 | cm 단위, Dynamic 기본값, source 배율, class-default 분포, snapshot basis, radial velocity 소비 | 원본 난수열·CPU 모듈 구현 전체의 수학적 동일성은 미확정 |
| Q/W/E/R/A 손튜닝 | Q tuning은 기존 cube/slice4+금색 crack1회. R tuning dust3행과 A full dust4행은 검은 중심을 없앤 은은한 보라 경계 alias를 소비하고 사용자의 voronoi 비교본은 보존 | purple-rim은 사용자 요청의 저작 보강이며 원본 PS 복원 판정이 아님. 최종 크기·색·조작감은 사용자 확인 전 |
| All Effects/Sequencer | 206 JSON 전체 parse 대신4KiB root metadata, Save 해당 row/occurrence 갱신과 clock/camera 보존. CPU 목록58.94초→45.29ms 실측 | 전체 UI Open/Save 시간과 실제 playback continuity는 사용자 확인 필요 |
| F/V | F full43행, V full42행. F mesh15행에 기존 경로와 SD325~332 원본 재질을 연결 | F SD329의 위치는 독립 저작 곡선이며 원본 emitter 간 상속과 동등하지 않음. 사용자 전체 재생 확인 필요 |
| Alt+V | 현재 full311행/tuning26행과 CModel cue. 초기 mesh2개와 후반 animated cube에 ALT178 frozen HDR/Bloom 입력, 원본448 camera key, 30Hz cube WModel 설치 확인 | 원본 capture view/CB0[0..5]는 미해석. 화면 채움은 project camera-fit adapter이며 사용자 재생 확인 전. [현재 결과](09-13/2026-09-13_KOUKU_CINEMATIC_WORKBENCH_IMPLEMENTATION_RESULT.md#g05-차원술사-장면-이미지-큐브의-현재-연결) |
| W/E/R/A full | 현재 W19/E19/R33/A66행. A 기존62행과 보라색 crack 두 종류×4회를 보존하고 사용자 요청의 R 보라색 cube4행을 추가. cube는 A crack의 생성 위치·초기 운동과 네 타격 시간을 사용 | 추가 cube는 사용자 요청의 저작 조합. 원본 A cube emitter를 특정한 완전 복원이나 사용자 화면 PASS는 아님 |
| S2050220 | 실제 S full30행(sprite24+mesh2+light1+post3). V63 주검격4개만 사용자 가독성 보정으로size2. 원본8mip·색·시간·움직임과 unified 비교본 보존 | RGBNoise2/ZoomBlur1은기존native재질연결. 광원radius전체원본일치·최종화면은미확인. 수치근거는Round2 RESULT G37 |
| R2050180 | 실제 R full33행. 기존WR208/259/260핵심9행을보존하고 누락보라보조6+RGBNoise2+ZoomBlur1+light1 연결 | Q51 R tuning과별개. 누락층색기여/14개R·S추가행출력은수치확인했으며 원본전체합성과사용자시각승인은미완료 |
| T/D | F1 D full25행/visible24. 비베이크UniformRange3개와본체alpha곡선을회수하고dust2의중복UV이동제거. op4는원본범위선택→XYZ4난수소비 | 기존hidden줌블러1과실제D gameplay unified/binding은보존. 새EXE필요. 전체외형확인및T복원은미완료 |
| BA0/1/2/3 | `_01/_02/_03/_04` manual4단 연결. full은15/12/7/25행이며 튜닝 비교본을 보존 | 사용자 실제4단 입력과 외형 확인 필요 |
| Artist/Warlord/Lance 전체 full | Artist19/Warlord27/Lance46의92문서·2,402 elements와 CModel cue15개. native program은267/269/331개이며 기존 EffectCatalog와 실제 animevents91개 clip에 연결. 워로드 V 통합 검토본은 clip 실행본과 별도로 유지 | Solo 미지원 발생은 사유를 보존해 제외. 용·호랑이·말의 시간 단위와 Lance SubUV Movie를 교정했다. 컴파일·저장 검증과 사용자 화면 확인은 [현재 결과](09-09/2026-09-09_BERN_LOCAL_LIGHT_AND_THREE_CLASS_FULL_RESTORE_IMPLEMENTATION_RESULT.md)에서 구분 |
| full 광원 |4class106문서에 typed point light119행. 선택 first-LOD source 후보142행과 직접 대조했고 component/CDO 상속 확인 | source 후보23행 누락(DimensionMaster22/WarlordT1), 초기 색·alpha·particle 수명 미소비가 반복됨. source-character pass 전 transient queue 조기 clear는 수정·빌드 완료. directional control25건은 의미·활성 미해독으로 별도. [G14/G15](09-09/2026-09-09_BERN_LOCAL_LIGHT_AND_THREE_CLASS_FULL_RESTORE_IMPLEMENTATION_RESULT.md) |
| Artist Alt V 꽃밭 | 단일 full254행: 기존 clip1 52+clip2 202. Field01 위치 provider2+표시30행을 포함하고 실제 Drawable/Stage·provider closure 검사 통과. 하늘 구체2개와 원본 카메라4행 연결 | native892 하늘은 opaque_back_depth_write로 일반 깊이 검사·쓰기 사용. 원본 HidePawn Parts9와 directional control 해제 수명은 미확정이며 전체 맵 차폐 복원으로 판정하지 않음. GPU 발생 수 오류 수정·Debug 배포 및09-10 사용자 요청범위 인게임 검증 완료 보고 |

마리오 공처럼 기존 `Effect/.../Meshes` 아래 파일이 있는 경우에도 본체가 particle-only라는 뜻은 아니다.
MN_PPCC_00의 노란/파란 LookInfo는 같은 skeletal mesh와 서로 다른 명시 MIC/DDS를 사용한다.
정적 Map Assets 배치에는 원본 bind geometry를 NONANIM으로 쿠킹하고 glTF의 실제 material 이름에
variant diffuse/normal/specular를 연결한다. 원본 리그/클립, shader 전체 복원, 피격/폭발 gameplay는 별도다.
현재 공 2종과 MN_RHCN_01 폭탄, 추가 빨간 별/줄무늬/광대 얼굴 공 및 MN_REUP_04+나팔 정적 소품의 Palette 연결·Drive 위치·수동 확인 경계는
[마리오 Palette 결과](09-09/2026-09-09_MARIO_PALETTE_PROPS_RESULT.md)에 기록한다.
추가 네 소품은 WORLD Object Resource와 HOLD 표시 모션도 등록했다. 이는 기존 Play Sequence trigger의 presentation이며 불꽃, 공격 AI, 영구 server entity를 추가한 것은 아니다. 실제 위치·트리거와 광대 기본 자세의 화면 확인은 사용자 설정/판정으로 남긴다.

마리오 원본 몬스터 REUP/RHKP/CDMD의 PSA→glTF 결합에서는 root 회전을 유지하고
자식 뼈 quaternion을 conjugate해야 한다. PSA track 순서/flattened parent가 아니라
mesh의 실제 hierarchy를 사용한다. 이를 생략하면 팔·손이 뒤틀리고 같은 뼈에 붙은 망치도
어긋난다. 또한 UModel의 normalized UINT8 skin weights는 현재 bundled Assimp에 넣기 전에
FLOAT accessor로 풀어야 한다. 그대로 넣으면 일부 가중치가 garbage/NaN/Inf로 쿠킹된다.
`build_umodel_gltf_psa.py`는 이 두 변환을 수행한다. 기존 재질·geometry·socket을 임의로
바꿔 가리는 문제가 아니다. 교정 재쿠킹의 확인 범위는 MarioOriginal 3종과 Artist DRA,
Artist TIG, LanceMaster HOR다. 다른 자산을 일괄 갱신한 것으로 간주하지 않는다.
뼈대/가중치 수치 확인과 사용자의 실제 모션 승인은 별개다.

Sprite/Mesh는 **그리는 geometry 종류**다. 각각 하나의 shader family 이름이 아니다.
한 sprite 안에도 texture mask·절차적 원형 빛·굴절·깊이 fade 등 서로 다른 프로그램이 있다.
현재 registry는 복원한 원본 식, 제한된 adapter, 프로젝트 튜닝이 함께 존재하므로
“공통 family가 전부 복원됐다”는 표현을 사용하지 않는다.

full은 검토 문서의 이름이다. 모든 원본 action/광원/발생량과 실제 시각 결과의 완료 표식이 아니다.
현재 Alt V318은 수치 fixture에서308행이 발생하며8행은 .1초 fallback 창보다 뒤의 burst까지
도달하지 않는다. importer의 요소당64 BUDGET_CLAMP는 엔진의 입자 수 한도가 아니다.
원본 burst/rate/loop와 현재 cap을 대조하고 실제 Spawn 요청·수용·손실을 분리해 기록한다.

같은 모델의 입자를 묶을 때는 primitive 순서와 effective shader/texture 상태를 함께 보존한다.
여러 submesh를 각각 instance로 바꾸면 alpha 순서가 바뀔 수 있다. 동일 상태인 submesh의
기하를 CModel 내부에서 원래 순서로 준비한 뒤 입자 전체를 instance로 반복하면 순서를 유지할
수 있다. source material slot 범위는 보존하고 override/geometry/입력 변경 때 재검증한다.
현재 native mesh occurrence에 이 구조를 적용했다. crack032/037의 실제 CModel WARP 검사182건과
일반/instance shader의 GPU 수치 대조를 수행했다. 원본 vertex68-byte/index32 ABI는 유지한다.
[성능 G08](09-09/2026-09-09_EFFECT_SHADER_BUILD_AND_PLAY_ALL_PERFORMANCE_RESULT.md)의 검증 범위를 따르며 G07의 제출량 계산을 FPS 개선율로 쓰지 않는다.

광원은 element의 emissive/alpha와 실제 light RGB 에너지를 구분한다. typed light.color.w=0만으로
무광원 판정하지 않는다. 원본 초기 색·alpha·particle lifetime, spawn/motion/radius 갱신과
실제 PointLightComponent 상속을 함께 연결해야 한다. transient light 목록은 ordinary와
source-character 모든 pass가 같은 frame 입력으로 읽고 기존 Renderer::Draw 성공/실패의
Clear_Frame에서 정리해야 한다. pass 안의 조기 clear가 일부 표면의 조명 수신을 누락시킬 수 있다.

### 원본 프로그램과 실제 소비 입력을 함께 연결한다

정적 맵도 Base PS가 같다는 이유만으로 같은 재질 구현으로 묶지 않는다. Baked PS의 RNM 입력과 별도 Light PS의 diffuse/specular 계산이 다를 수 있다. 한 모델에 opaque·translucent·background material slot이 함께 있으면 실제 slot의 render mode로 제출한다. forward 재질은 deferred marker 행을 쓰지 않으므로 해당 frame 목록/256행 예산에 등록하지 않는다. 원본 투명 재질의 직접광 출력은 현재 alpha/fog 합성 adapter와 구분해서 검증한다.

RNM/ShadowMap2D는 원본 material slot과 component/instance UV를 함께 연결한다. instanced foliage는 component의 공통 UV bias 대신 native per-instance bias를 사용하는 경우가 있다. 같은 physical geometry의 RNM·색·재질 variant는 CModel geometry와 CMaterial texture payload를 공유해야 한다. atlas variant마다 CPU/GPU geometry와 같은 texture를 중복 생성하면 전체 맵 복구가 메모리 폭증으로 바뀐다.

원본 G8 ShadowMap2D가 signed-distance 형식이면 sample을 그대로 조도에 곱하지 않는다. 원본 PS transfer와 light GUID/channel을 적용해 해당 direct diffuse/specular만 감쇠한다. RNM·환경광·emissive에 재곱하지 않는다. 현재 penumbraWidth 0.05는 원본 CPU 값을 확보하지 못한 PROJECT_ADAPTER다. static mesh UV atlas의 복구와 동적 캐릭터의 원본 환경 그림자는 별개다.

추가 UV를 다른 UV로 대신 채우지 않는다. 베른 native sky는 별도 TEXCOORD2를 쓰므로 optional UV2 WMSH와 requiredExtraUVMask 검증을 소비한다. 원본 native 크기·배치가 일치해도 화면상 크기는 카메라와 캐릭터 presentation에 달려 있다. 비교 이미지의 구도 차이만으로 맵 전체 scale을 변경하지 않는다.

Character source PS의 varying은 프로그램마다 다르다. Warlord legacy head program12의
Light는 UV2/light3/view5/position6을 소비하는데, 일반 layout의 UV4/light5/view7/position8을
넣으면 UV가 고정되고 광원 normalize가 NaN이 된다. 원본 VS 출력과 PS 입력을 함께 맞춘다.
정확한 텍스처를 복사하는 것만으로 이 결함은 해결되지 않는다. native opaque와 hair의 opacity
의미도 구분하며, hair의 draw-owned coverage/color identity를 0으로 두면 전체가 사라질 수 있다.

현재 Artist 기본 본체·장비·머리·붓 8모델의 21재질은 원본 program15~20과 기존1/4/5/6을
사용한다. TJ 눈 가중치/UV와 현재 헤어 UV1을 함께 유지하며 mask hair19는 원본 TwoSided를
사용한다. 추가 선택 장비의 소스 추적과 기본 장비의 runtime 연결 수는 구분한다.
DimensionMaster의 236본·헤어 UV1을 TJ의 이전 225본/UV 없는 파일로 덮지 않는다.
Customizing clip을 통합할 때 이름·채널·시간과 skeleton hash를 같은 데이터 단위로 검사한다.

환경 cube를 연결했더라도 native BRDF/SH가 비어 있으면 최종 반사 기여가 없을 수 있다.
차원술사 무기의 전체 Base 출력은 cube ON/OFF에 동일했고 원본 BRDF 입력이 미확정이어서
추가 복원을 보류했다. 중간 Inf만으로 최종 RGB NaN을 단정하지 않으며 실제 최종 출력까지
검사한다. 프로젝트 GGX 근사 LUT를 native 복구로 표현하지 않는다.
구현·빌드·사용자 시각 확인의 구분은 [캐릭터 재질 G11 결과](09-09/2026-09-09_CHARACTER_SCALE_AND_MATERIAL_PARITY_RESULT.md)를 따른다.

새 family는 HLSLI뿐 아니라 texture lane·scene depth/color·vertex color·음수 scale의 cull·후처리 submit까지 확인한다. Source SceneColor만 샘플하는 프로그램도 화면 UV 계산에서 depth SRV 크기를 사용하면 depth binding이 필요하다. native mesh의 반전 scale은 기존 one-sided 반대 cull pass를 소비해야 한다.

원본 Required에 SubUV가 있다는 사실만으로 mesh UV에 atlas 변환을 적용하지 않는다. Artist536/825/826의 실제 LocalMesh VS는 geometry UV를 그대로 전달하고, 대응 sprite SubUV VF는 CPU가 만든 current/next atlas UV를 전달한다. 원본 VF와 PS가 소비하는 입력을 기준으로 나눈다. Random은 Changes0이면 출생 이미지를 유지하며, 변경하는 경우 원본 serialized RandomImageTime의 상대나이 간격을 소비한다.

SubUV Movie는 출생 때 startingFrame0의 무작위 선택을 한 번만 수행하고, 명시 프레임은1기준으로 변환한다. update에서는 원본 FrameRate 분포와 bUseEmitterTime에 맞는 시간 입력을 소비하고 fixed delta만큼 atlas 프레임을 누적한다. RealTime 요청은 별도 시계 계약이 없어 거부한다. 기존 random SubUV와 Movie의 시간·난수 규칙을 혼용하지 않는다.

소환 WModel의 WANM이1000tick이고 기존 CAnimation이 고정30tick을 사용하는 경우 파일의 duration과 모든 key time을 함께30/1000로 정규화한다. playable character의24tick metadata를 이유로 Engine 전역 시계를 변경하지 않는다. 실제 CModel의 시간별 bone palette 변화를 확인해야 모델 로드와 정지 포즈를 구분할 수 있다. 말의 material section 여러 개는 같은 skeleton/clip을 사용하고 ModelCue ID로 owner anchor를 구분한다.

TIG/HOR는 기존 WMSH/WSKL/재질·position/scale/time keys가 정상이어도 PSA child quaternion 변환 누락으로 자세가 뒤틀렸다. root 회전은 유지하고 실제 mesh hierarchy로 child를 판별한다. PSA BONENAMES와 glTF joint 순서가 다르면 명시적인 `--allow-bone-order-remap`에서만 unique name의 전단사 대응으로 animation target을 연결하고 skin JOINTS/IB 순서는 바꾸지 않는다. 서로 다른 본의 회전 오류를 모델 전체 rotation으로 가리지 않는다.

ModelCue의 optional `loop`는 생략 시 false이며 holdLastFrame과 동시에 켤 수 없다. loop는 cue window 안에서 animation clock만 clip duration으로 감싸고 local velocity 이동은 되감지 않는다. 단순 holdLastFrame=false는 loop가 아니다. Artist D의 두 TIG는 교정된 source Y-up/+X-forward를 yaw -90으로 owner +Z에 맞추며 각자 stable cue ID와 같은 phase를 사용한다. HOR의 네 section은 말 네 마리가 아니라 한 모델의 재질 조각이므로 같은 pose와 local transform을 유지한다.

원본 clip root의 수평 locomotion과 ModelCue velocity가 함께 적용되면 animation loop 경계에서 원본 displacement가 초기화되어 뒤로 튈 수 있다. optional `suppressHorizontalRootMotionBone`은 생략·빈 값에서 기존 동작을 유지하고, 명시한 본만 새 unposed prototype의 기존 `CModel::Enable_RootMotionSuppression(name, 1)`로 설정한다. source asset X/Z는 rest translation을 유지하며 source Y의 점프는 보존한다. 이 축은 asset/cue transform 적용 전 기준이다. suppression 이름은 shared prototype key와 prepared resource signature에 포함하며 누락 본은 stage를 거부해 기존 자원을 보존한다. 원본 binary나 quaternion을 다시 고치지 않고 Codec 저장·재로드, 옵션만 바꾼 재-stage, 실제 CModel의 반복·seek와 원본 Y 일치를 확인한다. Artist D의 두 TIG가 `b_root`를 선택하며 다른 ModelCue는 명시하지 않으면 영향을 받지 않는다.

LanceMaster ALT V full은 실제 character clip의 시작점으로 기존 clip1~4 문서를 합친 하나의 effect다. source emitter 내부 상대 시각에 외부 clip offset을 중복 적용하지 않는다. 첫 clip의 animevent 하나가 자연 종료를 소유하고 뒤 clip에서 같은 full을 다시 spawn하지 않는다. 원본 MeshParticle의 source socket 편집은 stable ElementId로 runtime anchor slot을 분리해 같은 Midcontrol의 다른 occurrence를 바꾸지 않는다. 현재 연결과 수치·빌드·사용자 화면 경계는 [호랑이·말과 ALT V 결과](09-11/2026-09-11_TIGER_HORSE_ANIMATION_AND_LANCEMASTER_ALTV_RESULT.md)를 따른다.

SizeMultiplyVelocity는 현재 Size에 cm/sec 속력과 배율을 곱하며 BaseSize를 덮지 않는다. source spawn footer와 authoring trim이 live Size를 되돌리면 수식을 추가해도 결과가 사라진다. Orbit offset Update는 매 tick BaseOffset으로 복원한 뒤 곡선을 더해야 수렴 곡선이 누적 발산하지 않는다. 복수 LINK와 변동 rotation/rate는 별도 payload/위상 계약이 닫히기 전까지 거부한다.

### S의 검은 공간과 푸른 검격: 새 식과 누락된 입력을 구분한다

현재 S는2050220 `MomentaryRift / 일점 관통`이다. 예전2050550 S 문서는 이전 슬롯 배정의
찰나를 다루므로 현재 원본 근거로 혼용하지 않는다. source skill→clip→enabled notify→
first LOD→MIC/선택 프로그램으로 식별한다. 활성30개는 sprite24·LocalCrack mesh2·
screenPost3·light1이다. 활성 목록에 cube/swing/helix mesh는 없다.

블랙홀의 검은 바탕은 SD320 MaskControl의 texture 없는 원형 alpha와 검은 RGB다.
유효값에서는 `m=saturate(1-2*length(UV-.5))`, `alpha=saturate(m^4*30)*particleAlpha`,
`RGB=0`이며 alpha 합성으로 기존 바닥을 어둡게 한다. 그 위에 polar ring321·문양322·
Twirl323이 각기 다른 식으로 놓인다.320~323은 source SceneColor를 직접 샘플하지 않는다.
검은 texture나 별도 SceneCapture가 필요하다고 추측하지 않는다.

긴 검격의 Shine V63은 particle RGB `(2,4,20)` 위에 noise 두 장의 곱과 UV 형태 mask가 만든
흰빛을 더한다. 유효값의 RGB는 `ParticleColor.rgb + light*(7.5,7.5,7.5)`이고 alpha는
body mask·끝 fade·depth fade·particle alpha의 별도 계산이다. 진한 청색과 옅은 빛의
관계는 이 공간별 발광·투명도와 장면 합성에서 생기므로 단색 tint/Bloom만으로 대체하지 않는다.
최종 파스텔 색감 전체를 한 식으로 확정한 것은 아니며 노출·합성·다른 발생의 기여도 남는다.

Shine e5/e13은 동일 PS를 사용하되 StartSize의 ±Y, 저장된 pivotY .9/.1, 중첩된5개
SizeMultiplyLife와 속도 정렬을 함께 유지한다. 원본 VS는 Q에서 복구한 offset-center dynamic
VS와 같지만 native velocity basis 적용 목록에 V63이 없어 다른 camera-plane 회전을 선택하던
차이를 확인했다. G31에서 profile63을 기존 basis 경로에 추가하고 실제 함수의 CPU 수치 대조와
renderer translation unit 컴파일을 통과했다. 새 Shine PS는 없다. 원본 CPU live selector·
signed-size packing과 최종 화면 기여량까지 확인한 것은 아니다. 원본 pivot .9/.1은 그대로 유지한다.

같은 누락을 특정 스킬 이름만으로 찾지 않는다. 실제 선택 VS의 geometry 계산을 비교해
V58/65/67/69와 ALT80/81/82/127/129/169/185/191/194/195/196/200/201/202도
기존 native velocity basis로 연결했다. regular/dynamic/SubUV와 offset-center VS는
payload·pivot uniform 위치가 다르므로 이를 지운 shader 재사용을 하지 않는다. 원본
pivot과 UV 입력은 보존하고 CAMERA_VELOCITY의 방향 계산만 공유한다.

S의 Orbit4행은 원본 CDO의 83-byte Options를 다시 읽어 spawn=true, update=false,
emitterTime=false로 해석했다. 이전 chain이 없는 단일 LINK만 현재 가산 초기 상태로
실행한다. raw hex나 live 옵션, 복수 LINK는 여전히 거부한다. visible만 켜도 남아 있는
`material.execution`의 hold가 Solo를 차단하므로 실제 단독 Drawable까지 확인한다.

고유 원본 PS 기준 particle14개는 기존 V/ALT10개와 이미 소스 연결된 SD4개다. 후처리 PS2개도
현재 함수가 있어 '새 shader10개'를 현재 남은 작업량으로 세지 않는다. 함수 존재와 모든 입력의
정확성·실제 실행은 별개다. 사용자가 원하는 핵심본은 블랙홀·검격·주변 스크류의 세 시각 묶음이며
정확히3 element를 뜻하지 않는다. full에는 원본 particle26개를 남기며 손튜닝 cone/helix는
기존 unified에서 비교한다. 흰 스크류를 독립 helix나 특정 emitter로 아직 확정하지 않았다.

S e49의 V69 splitline은 실제 입자39개 입력에서 Dynamic이 모두0이었다. 선택 dynamic VF/VS와
모듈 부재 시 Null Dynamic 기본값(1,1,1,1)을 복구해야 원본 PS의
`floor(textureR+Dynamic.z)`가 alpha를 계속0으로 만들지 않는다. 기존 Q45/49 기본값 분기에
V69만 포함하며 활성 Dynamic 모듈의 저작값과 원본 PS는 바꾸지 않는다. 재질이 실행 가능하고
입자가 생성되어도 shader 입력 누락으로 안 보일 수 있다는 사례다. V63 Shine은 기존 입력으로도
양의 alpha를 출력했으므로 이번 교정을 새 Shine 수식 복원으로 표현하지 않는다.

원본 값→element→descriptor→codec→renderer→HLSL의 실례와 재감사 근거는
[S 원본 재질 조사 결과](09-09/2026-09-09_DIMENSIONMASTER_S_MATERIAL_FORENSIC_RESULT.md)에 둔다.

### Q의 유리는 무엇을 복원했나

큐브는 기존 모델 위에서 원본 MIC가 선택한 CubeSample PS 계산을 실행한다. 현재 장면의
HDR color copy를 읽어 시선에 따른 UV 왜곡, 밝은 caustic 무늬, 모서리 강조, particle 색과
투명도를 합성한다. 유리 texture 한 장을 불투명하게 붙인 결과가 아니다.

기존 `CEffectObject → CEffectDocumentRenderer → mesh/sprite shader` 경로를 사용하며,
`Shader_EffectCubeSampleScene.hlsli`와 `Shader_EffectDimensionMasterQNative.hlsli`가
각 선택 프로그램을 소유한다. material descriptor는 원본 texture 이름·sampler·색 공간과
named uniform을 기존 constant buffer에 연결한다. SceneColor copy는 Engine Renderer가
blend 전에 준비하며, 이를 실제로 읽는 occurrence를 그리기 직전에 현재 HDR로 갱신한다.
같은 occurrence의 particle과 material slot은 복사본 하나를 공유한다. 원본 RT0 식과 프로젝트 장면 adapter의 연결이며 원본 엔진의 fog·
보조 MRT·색 인코딩 전체까지 동일함을 입증한 것은 아니다.

Q의 검격은 별도 Slice 식이다. flow texture, UV 변형, 반경 mask, 칼날 무늬, depth fade가
함께 작동한다. 다른 스킬의 검격도 이 식과 동일한 원본 프로그램이면 재사용할 수 있다.
이름이 검격이라는 이유로 모든 distortion/보조 합성 분기가 같다고 보지는 않는다.

근거: [CubeSample shader](../../Client/Bin/ShaderFiles/Shader_EffectCubeSampleScene.hlsli),
[Q native shader](../../Client/Bin/ShaderFiles/Shader_EffectDimensionMasterQNative.hlsli),
[Q material descriptor](../../Client/Public/Effect_DimensionMasterQMaterial.h),
[실제 renderer](../../Client/Private/Effect_DocumentRenderer.cpp).

### Alt+V 큐브 안의 SceneCapture는 일반 유리 굴절과 다른 입력이다

사용자 첨부에서 카메라는 캐릭터 상체를 비추지만 큐브 면에는 맵 바닥과 캐릭터를 위에서
본 장면이 담겨 있다. 실제 원본에도 enabled `CEFActionNotify_SceneCapture`가0~.1초에 있다.
초기 camera notify013의 `par_m_swp_tw_s1_camera_01` emitter18/31은 `fm_h_box_01_1`을
카메라 앞.5m에 두고 `fx_m_me_swp_box_01`을 요구한다.2초의 notify036 애니메이션 큐브도
같은 override를 요구하지만 별도 발생이다. 첫 화면의 큐브를 후반 model cue 하나로 단정하지 않는다.

이 재질의 원본 PS는 `screencapture` texture를 world position과 engine-owned CB0[0..5]
행렬로 계산한 좌표에서 읽는다. `00_alpha01_usecamuv=true`, `99_use_meshtype=true`,
`capture_centeruvtile=1.15`를 확인했다. Parent는 masked이고 실제 lighting model은
`mlm_unlit`이다. 과거 out의 PS pass 이름에서 추정한 lit/non-unlit 분류는 부정확하다.

현재 Q42의 `Target_EffectSceneColor`는 해당 occurrence 직전 HDR 복사본이다.
Alt V 초기 emitter18/31은 별도 ALT178의 원본116 RT0 식을 사용하며 occurrence 시작에
완료된 `Target_SceneHDR`를 GPU에서 한 번 복사해 frozen texture로 보관한다. Save refresh는
그 occurrence의 기존 캡처를 유지한다. 화면 이미지 파일을 저장하거나 UI를 캡처하지 않는다.

원본 capture-view CB0[0..5]는 미해석이다. 현재는 프로젝트 cube-face UV→원본 /360 식의
명시 adapter이며 원본 masked RT0 alpha0도 기존 alpha carrier의 particle alpha로 연결했다.
따라서 native RGB/왜곡 식 연결과 원작 capture camera/투영의 동일성은 구분한다. 후반
animated CModel의 skeleton/clip/child bone과 30Hz retime을 유지하며 exact cube ModelCue도
ALT178 capture override를 사용한다. 기존 사용자 opacity/tint는 유지한다. 이는 현재 view의
2D texture를 mesh에 넣는 구조이며 6면 cubemap rendering이 아니다. 사용자 구도·투명도 판정 전이다.

### 셰이더 패스의 상태 차이와 중복 컴파일을 구분한다

같은 VS_MAIN/PS_MAIN을 pass마다 compile하면 Effect bytecode에 실행 코드가 반복될 수 있다.
기존 결과의 실제 SHEX가 mesh7벌/particle5벌 동일했다. 두 preview entry는 이제 global
VertexShader/PixelShader 각1개를 compile하고 기존7/5개 pass에서 공유한다. pass 순서·이름과
blend/depth/cull은 그대로다. 새 복원 식을 포함한 Debug /Od /Zi 검사는72초/93초이며
결과는98.34MB→14.16MB,74.24MB→15.29MB다. 전체 Product 빌드 시간의 보장은 아니다.
새 HLSLI 계산 변경은 해당 CSO를 재생성하지만 JSON 색·크기·위치·수명 손튜닝은 FXC 대상이 아니다.
패스의 VS/PS 공유만으로 큰 native switch의 컴파일 비용이 해결되지는 않는다. 새 프로그램을
기존 cohort에 추가할 때 64 ID 구간별 물리 include와 실제 carrier 선택을 함께 확장한다.
쿠크 installer처럼 한 구간의 상한만 계속 늘리면 수백 재질을 하나의 PS로 컴파일하게 된다.
생성기·include·dispatch·실행 표·project/filter를 같은 변경에서 맞추고 함수 본문과 native ID를
보존한다. write-if-changed와 무변경 Build, 한 leaf의 실제 재컴파일 범위도 확인한다.
Artist Mesh/Particle의 선택 include는 가변 전체 설치 목록과 분리한다. 실제 case의 carrier guard를
그대로 사용해 지원 ID를 판별하며, 새 쿠크 ID 때문에 공통 carrier의 상한을 계속 갱신하지 않는다.
ModelCue와 SourceCharacter 표면 program은 서로 다른 selector다. 기본 FX가 소유하는 native
pass는 annotation으로 CShader에 전달하고, SourceGroup에는 같은 입력 ABI만 남긴다. 이전 draw의
surface selector가 남아 있어도 native pass는 기본 FX의 원래 계산을 사용한다. 그룹의 직접 native
pass 호출은 실패하며, 기존 pass 이름·순서·input signature와 변수 타입 검사를 완화하지 않는다.
일반 FX와 V2의 동일 compile 식도 공유하되 함수 본문·렌더 상태·조건부 guard를 보존한다.
적용 및 검증 범위는 [컴파일 분리 결과](09-14/2026-09-14_SHADER_PROGRAM_COMPILE_ISOLATION_IMPLEMENTATION_RESULT.md)에 기록한다.
새 분할의 시간·제품 검증은 [빌드 성능 결과 G09](09-09/2026-09-09_EFFECT_SHADER_BUILD_AND_PLAY_ALL_PERFORMANCE_RESULT.md)를 따른다.
FXC의 float4 배열 초기화에서는 각 원소를 float4로 명시한다. 배열 안의 scalar 0을 혼합하면
X3017 initializer 불일치가 발생할 수 있으므로 값은 유지하며 float4(0,0,0,0)으로 작성한다.

### E01. RGBA를 찾는 것과 실제 소비까지 연결하는 것은 다르다

원본 package → material instance → parent/static switch → 선택 shader → texture/sampler와
uniform → mesh/particle varying → 장면 입력 → blend/depth의 순서로 따라간다. ABI는 이
입력을 어느 위치와 형식으로 전달하는지의 계약이고, shader 식은 전달된 입력을 계산하는
코드다. 둘 다 필요하다. raw shader를 찾았다는 사실만으로 DynamicParameter나 VertexColor,
source UV, SceneDepth가 올바르게 공급된다고 판단하지 않는다.

색 변화만 필요하면 descriptor/parameter를 재사용한다. 수식이 다르면 같은 파일의 명시적
프로그램이나 별도 HLSLI로 분리할 수 있다. 스킬마다 복사본을 늘리는 것보다 같은 원본
계산을 한 곳에서 고치는 편이 확대에 유리하다. shader 수천 개를 수동 제작하는 것이 목표가 아니다.

### E02. 노란 사각형은 반드시 texture 누락을 뜻하지 않는다

Q45/49는 원본 Dynamic 모듈이 없는 경우 (1,1,1,1) 입력이 필요했다. 0을 공급한 상태에서
Q45는 UV가 중앙 한 점으로 모이고 opacity 지수가 0이 돼 큰 평면이 드러났고,
Q49는 alpha/fade 식을 제대로 소비하지 못했다. 해당 두 프로그램의 모듈 부재에만 기본값을
공급한다. 실제 Dynamic 모듈/곡선은 그대로 평가하며 다른 프로그램에 1을 전역 강제하지 않는다.

skeletal ModelCue는 particle DynamicParameter를 소유하지 않는다. 창술사 T 용의 native1360처럼
원본 재질의 `MeshEmitterDynamicParameter`를 사용하는 경우에는 이미 bind한 material parameter를
명시적으로 전달한다. zero-init particle 입력을 그대로 넘기면 source noise가 정상이어도 opacity가
항상0이 된다.1360은 `g_LanceVASourceMaterialParameters[8u]`를 소비하며 다른 profile은 기존 입력을 유지한다.

사각형이 남으면 material 선택·alpha 식·blend·입자 UV·정렬을 분리한다. 사각형이라고 모두
같은 결함은 아니다. 검은 조각 역시 additive/alpha 합성 후보를 먼저 구분하고 이미지 한 장으로
발생 element를 확정하지 않는다.

### E03. `unbound`와 원형 빛

이전 All Effects의 `unbound`는 표시할 Resource 파일명을 찾지 못했다는 라벨이었다.
Q47은 원본 PS의 texture sample이 0개인 절차적 glow이므로 texture가 없는 것이 정상이다.
UV 중심까지 거리 `d = length(UV - 0.5)`로 감쇠를 만들고 power·색·alpha를 계산한다.
원형 입자도 사각 평면 위에서 바깥을 투명하게 만들어 보인다.

정확한 Q47 material 계약이 확인된 경우만 `Procedural Glow (no texture)`로 표시한다.
다른 진짜 누락 Resource의 unbound를 일괄 정상 표시로 바꾸지 않는다.

Q47의 원형은 UV 안의 감쇠 형태다. 원본 Size는 200×600cm이고 속도 정렬·pivotY=0.9,
크기 수명 곡선을 함께 사용한다. 따라서 최종 화면에 항상 완전한 원으로 나타나는 것은 아니다.
중앙의 장식 무늬나 검격까지 Q47 하나가 그린다고 이미지 한 장으로 단정하지 않는다.

### E04. 원본에서 생략된 값은 0이 아니다

원본은 instance에서 변경된 값만 저장할 수 있다. class-default(CDO) → archetype → instance를
필드별로 합쳐야 한다. Q는 8개 분포에서 실제 행동을 바꾸는 기본값이 빠졌다.

| 발견 | 실제 영향 | 교정 방식 |
|---|---|---|
| Mesh/Sprite StartRotation 범위 누락 | 파편/검격이 같은 방향에 모임 | 원본 uniform 범위와 LUT metadata 복원 |
| MeshRotationRate의 LUT는 있지만 op/chunk 누락 | 범위에서 고르지 않고 첫 값만 읽음 | 명시 LUT 유지, 상속 metadata만 복원 |
| LocationDirect ScaleFactor 1이 0으로 투영 | 원본 -130→110cm 이동 곡선이 소거됨 | source (1,1,1) 기본 분포 연결 |
| Cylinder VelocityScale 1 누락 | 원기둥 반경 방향 이동이 없음 | source 1 복원과 radialvelocity=true 소비를 함께 연결 |

Cylinder radial velocity는 높이축을 뺀 원형 평면의 속도다. 원본 CDO의 true를 확인했고
기존 소비자에 명시된 true만 적용한다. 속도값을 먼저 복원하고 이 분기를 빼먹으면 높이축으로도
파편이 흩어진다. 의미 대조는 [Epic Cylinder 설명](https://dev.epicgames.com/documentation/en-us/unreal-engine/location-modules?application_version=4.27)을 참고했으며,
이 문서가 LostArk CPU 구현 전체를 증명하는 것은 아니다.

명시적 0/false/LUT는 덮어쓰지 않는다. 전역 default=1 패치는 금지한다. 이번 Q 원샷 수량은
원본 burst 합계 15개이며, 기존 손 조정 count 배율을 전체 복구본에 복사하지 않는다.
원본과 난수열·모든 CPU 옵션까지 동일한지는 별도 경계다. bAdjustForWorldSpace,
bInheritParent와 생략된 Required duration 등은 확인된 소비 범위 이상으로 완료 처리하지 않는다.

자동 확대의 다음 수정 지점은 기존
[build_imported_effect_documents.py](../../Tools/LevelPlacementExtractor/build_imported_effect_documents.py)의
`SourceIndex`다. 현재 raw properties만 복사하고 class/archetype 상속을 소비하지 않으며,
`build_distribution_recipe`는 op가 없으면 target class로 1/2를 추정한다. 따라서 Q JSON을
고친 것만으로 다음 스킬의 자동 변환이 교정되지는 않는다. 명시적인 native CDO/archetype
입력을 같은 SourceIndex에서 합친 뒤 기존 분포·duration/detail 생성기를 재사용해야 한다.
struct의 필드 병합과 배열 전체 교체, 명시 0/false/null 보존, 상속 순환/누락 시 오류 보존을
검증하고 우선 Q10만 재생성해 대조한다. 이 공통 importer 확장은 현재 미구현이며 전체
authored 문서를 일괄 재생성하는 작업과 분리한다.

### E05. 100배 크기와 90도 방향은 서로 다른 단계다

쿠크처럼 ActorX/FBX로 쿠킹한 MN_RPCT_05의 골격은 root basis100을 이미 포함한다.
설치된 세 공격 clip의 본을 읽으면 CModel preScale .017 뒤의 basis는1.7이며 socket local
입력은 미터다. 이 본에 cm 역보정100을 또 곱하면170이 되어 크기뿐 아니라 소켓 오프셋도
100배로 벌어진다. `Build_SourceAnchorWorlds`는 이미 포함된 단위를 다시 곱하지 않는다.
정점 단위, skeleton local/root basis, model preScale, owner root와 notify offset을 따로 측정한다.
raw 행렬이 유한하다는 검사나 synthetic1.7 anchor를 넣은 성공은 실제 모델 부착 검증이 아니다.

설치된 Q cube/swing/crack WModel 정점은 원본 cm를 `(x,z,-y)`로 변환한 것이다.
modelPreScale=.01은 단위 변환이고 원본 StartSize는 입자 크기다. 둘을 중복 적용하지 않는다.
Character CModel은 추가 Y -90도 import basis를 쓰지만 root Transform에는 그 회전이 없다.
Q의 root snapshot에 source basis -90도를 한 번 넣어 source +X 전방을 actor +Z에 맞춘다.

원본 cue 위치·속도·메시를 각각 눈대중으로 90도 돌리지 않는다. 기존
`actionCueAttachment.snapshotRootSourceBasisYawDegrees`가 local 공간 입력 전체에 같은
기준축을 적용한다. bone-follow나 다른 캐릭터에 전역 강제할 값이 아니다.
회전은 거리 자체를 줄이지 않으므로 남은 거리 차이는 anchor·location·원본 cue로 다시 분리한다.

일부 mesh만 90도 틀리면 먼저 실제 정점의 면·긴 축과 원본 TypeData pre-rotation을
확인한다. sourceRecipe literal의 존재와 `detail.mesh.sourceTypeDataRotationDegrees`의
runtime 소비는 다르며, 특정 asset 전용 codec 보정이 다른 asset까지 처리한다고 가정하지 않는다.
FRotator 정수·StartRotation turn·TypeData degree를 구분하고 기존 UE3 Euler 변환으로
pre-rotation과 particle rotation을 각각 한 번 적용한다. 최종 재생 행렬의 중심·전방·법선과
geometry preScale을 포함한 크기를 대조한 뒤 사용자 화면 확인을 받는다. 원본 notify 배율과
사용자 확대값을 분리하며, 정상 carrier나 다른 bone-follow occurrence에 일괄 보정을 넣지 않는다.

### E06. Play All이 2초 멈춘 뒤 끝으로 가면 시계와 준비를 구분한다

원본 Q 수명보다 긴 동기 resource 준비 시간이 첫 dt에 포함되면 재생 시작부터 끝으로
이동한다. Preview row를 만들었는데 Sequencer Play가 그 임시 row를 삭제하거나 입력
owner를 요청하지 않는 경우도 있었다. 리소스가 보이는 Family 재생 성공이 전체 재생의
성공을 보장하지 않는 이유다.

현재 Play/Preview는 transient row를 유지하고 성공 시 interaction을 요청한다. 준비 직후
한 번의 advance를 제외하며 정상 dt는 그대로 사용한다. full recovery는 버릴 WorldPreview를
먼저 준비하지 않고 Sequencer occurrence를 한 번 준비한다. 실패 시 기존 유효 row를
보존하고 Loop는 선택 anchor를 유지한다. GPU 초기 준비 자체가 무조건 사라지는 것은 아니다.

실제 멤버 함수 CPU 대조에서 이전 transient 삭제/큰 dt skip/owner 누락을 재현했고 수정
분기를 검증했다. GPU·모델 경계는 대역이므로 UI 재생과 화면 PASS를 대신하지 않는다.

### E07. 원본 Product, 복구본, 손 조정본을 구분한다

복구본은 `Data/Effects/Authored/*.restore.effect.json`이고 All Effects의 해당 skill Product
아래 Recovery Effect로 연결한다. 제품 `.unified`와 성공한 Q2 비교본을 자동 덮어쓰지 않는다.
현재 full Q는 `effect.dimensionmaster.skill.2050100.full.restore`다. 별도 툴 대신 기존 V1의
All Effects·Model View·Sequencer·Play/Family를 사용한다. Save/Load는 같은 stable ID와
원본 식 입력을 보존하며 editor draft도 실제 occurrence snapshot에 전달해야 한다.

먼저 full Q를 Model View root 기준으로 Play All하고 같은 시각을 Seek/Family로 비교한다.
원본 위치·회전·기본값을 정리한 뒤에도 차이가 남으면 hand authoring 단계로 넘어갈 수 있다.
그때 바꾼 위치/색/수량은 PROJECT_TUNED로 기록하고 원본 바이너리에서 얻은 값으로 포장하지 않는다.

손튜닝본의 optional `sourceRecipe.authoredModuleOverrides=true`는 기존 module/distribution
편집 UI를 연다. sourceRecipe 실행·재질·geometry 소비는 유지하며 원본 full과 native 증거는
읽기 전용으로 남긴다. Q42 몸체는 material `color`, Q51 윤곽은 `aura_color`가 소유한다.
일반 RGBA만 바꾸고 실제 색 입력을 수정했다고 판단하지 않는다.

같은 재질의 반복은 emitter burst로 합칠 수 있지만 birth 시각과 particle age를 구분한다.
Q42 유리는 출생 시각별 위치·크기·수명·속도 곡선으로 묶을 수 있다. Q51 검격은 material
UV가 emitter 시간에 의존하므로 여러 타격을 한 emitter로 합치면 후속 검격의 무늬가 바뀐다.
element 수 감소를 목표로 이 차이를 버리지 않는다. 셰이더 계산·컴파일 변형 수와 실제
particle 수·화면 중첩 비용도 따로 확인한다.

### E08. 속도 정렬의 U/V와 모델 기준축은 별개다

Q의 원본 offset-center VS 두 개는 앞 181개 명령이 동일하다. velocity 선택 분기에서
U=cross(toCamera,motion), V=-motion을 사용한다. Required의 명시 PSA_Velocity를 읽어도
renderer가 local X를 속도에 맞추면 세로로 긴 검격·glow가 90도 틀어진다.
우리 ParticleRect는 local=(u-0.5,0.5-v)이므로 이 분기를 표현하는 world matrix는
row0=U, row1=+motion이다. row1을 -motion으로 복사하면 UV가 뒤집힌다.

Make_ParticleSpriteWorld는 확인된 Q native profile과 명시 CAMERA_VELOCITY에만 이
3D motion plane을 사용한다. authored pivot/크기를 유지하고 이 정렬에서는 roll을 적용하지
않는다. Q49의 명시 rectangle, 다른 profile, source-disabled 경로는 이전 동작을 유지한다.
속도 0 또는 시선과 평행하여 평면을 정의할 수 없는 경우는 기존 finite fallback을 유지한다.
원본 LostArk CPU의 실제 selector CB를 실행 중 회수한 것은 아니며, 원본 VS 분기와
serialized alignment·별도 Epic source 구현을 대조한 명시적 대응이다.

원본 UV 네 모서리와 실제 함수·DirectXMath의 197개 수치 확인은 통과했다.
카메라 평면 안에서 각도만 90도 바꾸면 입자가 깊이 방향으로 움직일 때 오차가 남으므로
단순 전체 yaw 보정으로 이 문제를 대신하지 않는다. 실제 실행 화면의 최종 승인은 별도다.

### E09. 한 클래스의 모든 스킬이 안 나오면 이벤트 문서 로드를 먼저 확인한다

한 슬롯만 다른 경우에는 `PlayerSkills → skillbindings → animevents → 실제 effect asset ID`를
먼저 대조한다. 워로드의 F17140 native446 번개 보강이 V17170에만 있었고 Alt+V17250에는
다른1166 sprite만 복제돼 있던 사례가 있다. Alt+V 두 clip에는 F의 native446 mesh/WPO,
DynamicParameter, root snapshot과 수명을 그대로 재사용하고 실제 startcolor만 황금 HDR로
저작한다. 새 발생 수와 색은 사용자 요청 튜닝이며 원본 Alt+V 발생 수로 기록하지 않는다.

`.animevents`는 원본 참고 행과 제품 `effectref=asset` 행을 함께 세어 헤더의 총 이벤트 수와 비교한다. 제품 EFFECT를 합치거나 삭제하면서 헤더 수를 그대로 두면 `CAnimationEffectCueDocument::Load_FromText`가 문서 전체를 거부하여 정상 스킬까지 등록되지 않는다. Artist Alt V에 이어 LanceMaster Alt V 네 행→한 행 통합에서도 같은 문제가 재발했다(선언3139/실제3136). Effect 파일 존재·catalog ID 연결만으로 완료하지 않고 실제 `Load_ForProductPrewarm`과 설치 모델의 clip catalog를 받는 `Load_WithCatalogSnapshot`을 확인한다. Lance의 헤더 교정 후 두 경로 모두43cue/171HIT, clip·ID·anchor 누락0을 확인했으며 Client 재시작 후 화면은 사용자 검증 대상이다. 통합 도구가 최종 event 산출물의 header count도 함께 소유하게 하고, runtime parser 검사를 끄지 않는다. 상세는 [호랑이·말과 ALT V 결과 G07](09-11/2026-09-11_TIGER_HORSE_ANIMATION_AND_LANCEMASTER_ALTV_RESULT.md#g07-창술사-전체-이펙트-미출력의-실제-로더-회귀)을 따른다.

개별 full 문서의 Codec/Stage 성공만으로 캐릭터 입력 연결을 확인했다고 기록하지 않는다. 기존 로더의 문서 검증을 유지하고, 헤더 수·Catalog 대상과 실제 모델 clip을 넣은 Load를 함께 확인한다. 이미 생성된 캐릭터는 정본 수정 후 class 재선택이나 재입장으로 다시 생성해야 한다. 상세 근거는 [Artist 결과 G09](09-09/2026-09-09_ARTIST_CORE_FULL_RESTORE_IMPLEMENTATION_RESULT.md)에 둔다.

통합 문서는 실제 Product Catalog의 파일 읽기 계약까지 검사한다. Lance ALT V의 20,049,144-byte 문서는 Codec의 64MiB 상한에는 들어왔지만 별도의 Product 16MiB 상한에서 거부됐다. 현재 `CEffectDocumentCodec::MAXIMUM_DOCUMENT_BYTES`와 bounded Load를 저작·제품이 공유하고 catalog index의 한도는 별도로 유지한다. actual request→Loader Stage→document/prepared renderer commit과 초과 입력의 기존 상태 보존을 확인한다. 파일 압축만으로 현재 입력을 통과시키는 방식은 F1 재저장 때 재발할 수 있다. [Lance 결과 G08](09-11/2026-09-11_TIGER_HORSE_ANIMATION_AND_LANCEMASTER_ALTV_RESULT.md#g08-alt-v-제품-로더의-16mib--64mib-불일치)에 실패와 수정 후 검사를 구분한다.

본에 붙은 MeshParticle의 방향은 실제 설치 메시의 긴 축, 손본 basis, Source MeshRotation과 TypeData pre-rotation을 함께 확인한다. Lance V `fm_n_flm_ydr_00_sm`과 ALT V `fm_x_flm_gdr_01`/dragon의 긴 축은 local +Y이며 장착 무기는 +X다. 원본 TypeData pitch=-90이 typed detail에 누락되면 MeshRotation quarter-turn만 적용돼 손본 -Z로 향한다. 원본 `[roll,pitch,yaw]=[0,-90,0]`를 `sourceTypeDataRotationDegrees`에 한 번 투영해 기존 Euler 변환으로 +X에 맞춘다. degree 입력을 MeshRotation distribution처럼 다시 360배 하지 않는다. import scale 정상화나 손 offset을 되돌려 빠진 회전을 숨기지 않으며, source와 사용자 local/socket rotation은 별도로 보존한다. 실제 vertex·pose 검사는 사용자 최종 화면 판정을 대신하지 않는다.

같은 창 mesh를 사용하는 T34650의 두 full 문서에도 typed pitch 누락/0이 남아 있었다. 네 창 행을 같은 원본 회전으로 맞추고 dragon 두 행에는 사용자가 ALT V clip1에서 확정한 Transform position `[1,0,0]`을 적용했다. donor의 실제 `Midcontrol -> b_weapon_rhand` 부착은 유지했다. 수정 후 T 화면 확인은 별도이며 [후속 결과 G11](09-11/2026-09-11_TIGER_HORSE_ANIMATION_AND_LANCEMASTER_ALTV_RESULT.md#g11-t-창의-방향과-사용자-alt-v-dragon-위치-적용--2026-09-12)에 기록한다.

T 돌진 용처럼 section 경계가 끊겨 보이면 실제 변형된 공통 정점의 일치와 alpha 감쇠를 따로 검사한다. 현재 용은 13시점의 목 경계 위치가 일치하지만 native1360의 시선각 5승 감쇠가 비스듬한 면을 거의 투명하게 만든다. 두 ModelCue의 opacity Fresnel power를2로 완화한 사용자 튜닝과 자동·화면 검증 범위는 같은 [결과 G12](09-11/2026-09-11_TIGER_HORSE_ANIMATION_AND_LANCEMASTER_ALTV_RESULT.md#g12-t-돌진-용의-목-경계와-투명도-조정--2026-09-12)에 구분한다.

### E10. simulation-only 위치 제공 요소는 GPU 발생 수에 포함하지 않는다

위치 제공 요소는 CPU에서 다른 입자의 위치를 계산하지만 그 자체를 그리지 않는다. Playback이 해당 요소를 GpuOccurrences에서 제외하면 Renderer의 GPU family·개수·순서 집계도 같은 조건으로 제외해야 한다. Artist Alt V는254행 중 provider2개가 이 기준과 달라 Render admission에서 실패했다. provider의 visible 값을 끄거나 전체 frame 개수 검사를 없애지 않고 family를 END로 분류해 두 소비자를 맞춘다.

static WorldObject의 CModel material override가 SOURCE_CHARACTER이면 일반 map shader 초기화 뒤에도 Bind_SourceCharacter를 호출해야 해당 재질의 frame/light 입력이 채워진다. 공의 원본 PBR처럼 텍스처가 존재해도 범용 D/N/S/E만 연결하면 mask 의미와 환경반사 계산을 잃을 수 있다.

## 렌더링 복원은 실제 표면에서 소비되는 입력부터 연결한다

렌더링 툴은 이미 구현한 계산의 빛·노출·반사 강도를 비교하고 조정하는 도구다. 빠진 texture 채널, 잘못 선택된 material, 끊긴 lightmap UV나 원본 shader 연산을 slider가 대신 만들어 주지는 않는다. 새 표면은 **보이는 배치 식별 → 실제 material 선택 → 원본 texture/parameter/분기 → CMaterial 입력 → geometry 채널 → shader·조명·후처리** 순서로 확인한다.

원작도 DirectX로 그린다는 사실은 같은 그림을 보장하지 않는다. 같은 GPU 명령을 사용하더라도 어떤 재질식·입력·광원·간접광·색 변환을 조합하느냐가 다르다. 특히 밝게 하거나 채도를 올리는 조작은 원인 분리 이후에 수행한다. 조작감·이펙트 발생 시점·발생 위치는 별도 gameplay/presentation 입력이다.

### 현재 재사용할 수 있는 구현과 보류 범위

아래 상태는 누적 조사 개수와 실제 연결 개수를 구분한다. 날짜별 빌드 경과는 대응 RESULT를 정본으로 삼고 여기에 반복하지 않는다.

| 범위 | 현재 실제 연결 | 확대해서 해석하지 않을 범위 |
|---|---|---|
| 쿠크 선택 바닥 2재질 | 원본 배치 MIC의 specular/reflection 식, RGB specular 전달, tangent 부호 보완, 일반/인스턴스 shader, publisher와 Workbench A/B | 쿠크 모든 표면·원본 lightmap·원본 전체 조명 복원은 아님. 나머지 `diffuse-sampler` 3행은 별도 샘플링 수정 |
| Character Select FLOOR12·BRIDGE01E | 기존 선택 6배치와 동일 입력의 BRIDGE 4배치 추가, 4 variant, 5 material 행에 PBR 표면·UV1·배치별 RNM lightmap·선택 환경반사 연결 | 모든 같은 이름의 배치나 전체 803배치에 원본 재질을 연결한 것은 아님 |
| Character Select MAGICFLOOR03D | 얇은 띠 15배치에 source specular program5, 두 atlas 그룹, UV1 연결 | 안쪽 넓은 링과 중앙 원판을 같은 mesh로 취급하지 않음 |
| Character Select FLOOR13H | 넓은 링 439/482/486/507의 4배치에 기존 PBR program4와 선택 발광, UV1·atlas, 근거가 있는 482의 환경반사 연결 | 같은 mesh 이름만으로 다른 배치의 조명·환경 입력까지 같다고 판단하지 않음 |
| 캐릭터 축소 품질 | 실제 45모델의 247 TGA에 CPU full mip, 차원술사 무기 DDS12개에 하위 mip 설치, static/skinned Aniso16, 창술사 잘못된 LUT 경로3개 제거 | 모든 피부·눈·머리·아바타의 원본 재질식 구현은 아님 |
| 원본 캐릭터 재질 | CharacterCatalog에 LANCE_MASTER 5행, DIMENSIONMASTER 18행의 modelMaterialOverrides 활성. 별도 헤어를 포함해10개 모델·24개 material slot에 기존 CModel/CMaterial 경로로 연결. 차원술사 body/head/외부 AnimSet의236bone과 eye/hair UV 설치 | 헤어 실제 decoder·154+2clip Attach/Clone·program7/pass6, 새 Product DLL/CSO의24slot 바인딩과 Debug build·환경profile25 게시 통과. 사용자 외형 확인은 대기. 다른 클래스·새 아바타의 자동 복원 완료를 뜻하지 않음 |
| 발탄 환경 | 중앙 바닥4·주변 바위3에 원본 overlay/specular 분기, 배치별 COLOR0·UV1·RNM 연결. 저작·Resources 설치와 runtime Publish 완료 | 사용자 요청으로 Product 빌드 중단. 다른 세션의 전체 빌드 후 실행 필요. 사용자 화면 상태는 석재 RESULT 확인. 나머지 맵·Deploy·파편·환경 cube는 이번 적용 범위 아님 |
| 황금 테르페이온 탈것 | VehicleCatalog 3행: 몸체 `mn_pmstg_01_mi` 새 SourceCharacter program 85(RefShaderCache GPU-skin BasePass/directional light PS에서 생성, 원본 program 24 재생성 byte-exact로 생성기 검증), 장식 `-1_mi`는 기존 24, 갈기 `-3_mi`는 기존 18. 파라미터 115·텍스처 18, 원본 TGA 11개를 `Character/Vehicle/Terpeion/SourceMaterials`에 설치 | 탈것 LookInfo 파티클·발광 이펙트는 아님. Product 빌드 후 사용자 외형 확인 전 |
| 탈것 3종 추가 | 은색 전투 랩터 기존 program 23/25, 고요한 별빛의 가호 realpbr 몸체 신규 86(환경 cube)·`pbr_base_trn_reflection` 외피 신규 88(탈것 파츠 BLEND forward, 단면), 레인보우 모코보드 `pbr_base_msk_vfx` 신규 87. 원본 TGA는 `Character/Vehicle/<Name>/SourceMaterials`. 09-14 보정: 별빛 WModel 1.3 원본 UV1 join, 88 base의 엔진 행(cb0[0].w=1, cb0[21].x=1 추론, sky light 행에 scene ambient 근사)과 fog/view/up varying 배치, 모코보드 self-rigged 재쿠킹 | LookInfo 파티클·스폰 이펙트는 아님. 사용자 확인: 모코보드 좌석·무지개 흐름, 별빛 색·불투명도 "잘 나와" |
| 탈것 2종 추가 | 아우프슈텐-R `mn_pmshe_00-2_mi` 기존 program 85, 바다 유니콘 튜브 `mn_pmsut_00-6_mi` 기존 program 24(RefShaderCache Base/Light PS ID 일치). 원본 TGA는 `Character/Vehicle/{Aufstehen,SeaUnicornTube}/SourceMaterials` | LookInfo 파티클은 아님. 사용자 확인 "다 잘 나와" |
| 고대의 신화 탈것 | 슬롯 5개 `monster_base_msk_high_realpbr` 신규 program 89(realpbr, 환경 cube는 게임 IBL). CSO 그룹 84~89로 확장. 원본 TGA는 `Character/Vehicle/AncientMyth/SourceMaterials` | LookInfo 파티클은 아님. 사용자 확인 "잘 나와" |

현재 CS 저작/런타임 `mapmaterials`는 모두 **9 material 행 / 29 placementLighting 행**이다. family는 `bg_base_pbr_opa` 6행, `bg_base_pbr_seamless_opa` 1행, `bg_seamless-specular_opa` 2행이다. 803배치·55mesh·75재질·14 terminal family·179 render-input 조합을 조사한 결과는 확대 준비 자료이며 연결 완료 수가 아니다.

BRIDGE 442/444/458/474와 FLOOR13H 507은 기존 복구 배치와 mesh·ordered MIC·atlas pair·환경 입력이 같아 기존 variant를 재사용했다. 각각의 atlas 좌표·decode scale은 원본 배치별 값으로 연결했다. 이처럼 **같은 원본 입력을 가진 표면 확대에는 새 shader나 Resource가 필요하지 않을 수 있다.** 기존 가시성·위치·크기는 유지하며 source/runtime publisher까지 연결했다. 새 5배치의 실제 화면은 사용자 확인 전이다.

원본 중앙 원판과 현재 십자형 구조의 표시 조합은 아직 완전히 설명되지 않았다. 같은 이름의 mesh, 밑면의 존재, 겹친 삼각형만으로 bridge 삭제·투명화·높이 변경을 수행하지 않는다. 사용자는 중앙 링의 금색 변화를 확인했지만 이것이 원작 전체 일치나 모든 표면의 visual PASS는 아니다.

근거: [바닥 복구 RESULT](C:/Users/user/Desktop/LostArk/.md/GB/09-07/2026-09-07_FLOOR_MATERIAL_RECOVERY_RESULT.md), [링·축소 품질 RESULT](C:/Users/user/Desktop/LostArk/.md/GB/09-08/2026-09-08_CHARACTER_MATERIAL_MINIFICATION_RESULT.md), [캐릭터 후보의 활성 보류 RESULT](C:/Users/user/Desktop/LostArk/.md/GB/09-08/2026-09-08_CHARACTER_MATERIAL_AND_EFFECT_ROUND2_ESTIMATE_RESULT.md), [CS 저작 재질 정본](C:/Users/user/Desktop/LostArk/Data/Maps/Authoring/LV_LOBBY_CLASSSELECT_SL00/LV_LOBBY_CLASSSELECT_SL00.mapmaterials.json).

### 실제 배치와 material identity를 먼저 닫는다

MapCatalog는 생성 정의, placement는 위치·회전·크기와 stable placement ID, material 문서는 named material override를 소유한다. 원본 StaticMesh의 기본 material과 원본 actor/component가 선택한 material은 다를 수 있다. 예를 들어 같은 BRIDGE01E의 410/490과 411/495는 다른 원본 재질을 사용한다. override가 하나만 지정된 slot은 남은 기본 material 상속을 보존한다.

기존 `build_map_material_variants.py`로 ordered material 조합을 만들고, 필요한 경우 baked texture pair/environment가 다른 입력도 variant로 분리한다. immutable CMaterial을 공유하는 clone을 배치마다 뒤늦게 덮어쓰지 않는다. 같은 variant를 쓰는 배치의 atlas scale/bias와 decode scale은 계속 배치별이다.

```text
mapmaterials + MapCatalog + mapplacements
→ Publish-MapAuthoring
→ runtime mapassets/material 참조
→ CMapAssetCatalog / Loader / MapTool MODEL_ASSET_LOAD_DESC
→ CModel named material override 검증
→ immutable CMaterial texture·수치
→ CMapAssetRenderUtils
→ 일반/인스턴스 shader
→ G-buffer → deferred 조명 → 후처리
```

실패 시 기존 catalog/material을 유지하는 `parse → validate → stage → commit`을 재사용한다. 필수 texture·UV1이 없는데 성공처럼 진행하거나, source 파일명만 비교해서 다른 package의 입력을 대체하지 않는다. MapTool authoring만 수정했으면 runtime publish가 별도로 필요하다. EXE 빌드와 material prototype 재생성도 같은 작업이 아니다.

근거: [MapAssetCatalog](C:/Users/user/Desktop/LostArk/Client/Private/MapAssetCatalog.cpp), [Model의 override 검증](C:/Users/user/Desktop/LostArk/Engine/Private/Model.cpp), [MapAssetRenderUtils](C:/Users/user/Desktop/LostArk/Client/Private/MapAssetRenderUtils.cpp), [기존 publisher](C:/Users/user/Desktop/LostArk/Tools/MapPipeline/Publish-MapAuthoring.ps1).

### 채널 이름보다 실제 식과 분기를 따른다

| 입력 | 이 프로젝트에서 확인된 의미 | 복원할 때 피할 오류 |
|---|---|---|
| ORM | 선택 CS PBR family에서는 R=AO, G=roughness, B=metallic. 각 intensity/power를 적용한 뒤 제한하고 F0를 계산 | 모든 ORM 파일에 같은 식 적용, 원본 intensity가 1을 넘는다는 이유로 저장 때 [0,1] 제한 |
| Specular RGB | 쿠크 아래 바닥은 S.rgb, 위 바닥은 S texture 비활성 분기에서 가공 전 D.rgb를 사용 | `_s` 파일을 전역 RGB specular로 해석하거나 이미 비활성인 원본 texture 분기 활성화 |
| Normal RG/A | 선택 바닥은 RG로 XY를 얻고 Z를 먼저 복원한 다음 강도를 적용. A는 2D 반사 mask | A를 roughness로 대입, 강도 적용 전에/후에 Z를 복원하는 순서 임의 교체 |
| 2D reflection | 해당 바닥 family는 고유색을 밝히고 어둡게 하는 원본 합성식 | reflection을 무조건 emissive에 더해 색·빛 반응을 바꿈 |
| Emissive | FLOOR13H는 E texture·색·강도와 원본 nested cosine/sine 시간식. 기존 elapsedTime 사용 | 정적색만 연결하고 원본 시간식을 복원했다고 표현하거나, 매 draw 활성 flag를 초기화하지 않음 |
| BRDF LUT | 시선·반사 조건으로 조회하는 계산표 | 일반 mesh UV로 읽는 specular texture처럼 연결. 창술사 eye/hair/eyelash의 잘못된 경로3개가 이 사례 |

일반 legacy skinned 경로는 지금도 S.rgb를 밝기 한 값으로 줄여 사용한다. 선택 바닥의 RGB specular 경로가 추가됐다고 모든 캐릭터 반사색 손실이 해결된 것은 아니다. 반대로 프로젝트 전체에 PBR/roughness/lightmap 연결이 없다고 설명하는 것도 현재 상태와 다르다.

### 모코코와 창술사 기본 몸, 차원술사 의상을 구분한다

`LanceMaster.wmodel`은 창술사 기본 몸·애니메이션 모델이다. 화면에서 본체처럼 보이는
모코코는 `LanceMaster_Helmet_Mokoko.wmodel`과 `LanceMaster_Upper_Mokoko.wmodel`의
avatar head/armor 두 부품이며, 부모 몸의 skeleton을 공유하고 기본 장비를 가린다.
이번 source material 활성은 모코코 2모델의 3MIC와 창술사 장·단창에 적용됐다.
`LanceMaster.wmodel` 자체에는 이번 source override가 없으며 이전 LUT 경로3개 정정과 구분한다.

모코코 3재질은 program1 classic-skin으로 material-owned `hdr07_1.tga`의 2D 반사와
관련 IBL 수치를 실제 소비한다. 차원술사 몸10행은 의상5(program3 realPBR), 노출 피부1
(program1), 얼굴/눈/속눈썹/머리 각1(program4/5/6/7)로 나뉜다. 의상 program3과 무기
program8/9는 Character Select의 검증된 RGBM6 cube를 scene RenderingProfile에서 공급한다.
기존0 대체 sample을 실제 cube sample로 연결했으며 원본 SH와 다른 scene의 probe 선택은 미완료다.
모코코와 차원술사 의상 대표 normal 강도는 모두1이고 양쪽 normal sample·계산은 연결돼 있다.
따라서 사용자가 보고한 의상의 어둡고 생기 부족한 느낌은 normal 누락으로 단정하지 않는다.
구체적 미완료 항목인 장면 반사·간접광과 실제 basecolor/roughness/mask 기여를 먼저 분리한다.
이 차이의 화면 기여량은 아직 측정하지 않았다. 맵의 baked lightmap/선택 환경 입력이
캐릭터에 자동 적용된다고 설명하거나 모코코 shader/CS 전용 cube를 모든 캐릭터에 복사하지 않는다.

차원술사의 실제 별도 hair55는 기존225bone 뒤 원본11bone을 연결한236bone rig와 UV1,
source program7을 사용한다. 숨긴 bodyhair override만으로 표시되는 헤어 복원을 판정하지 않는다.
의상·무기의 scene cube는 material-owned2D IBL과 다른 입력이며 Renderer가 SRV를 stage하고
CMaterial의 base pass가 소비한다. profile의 environment 생략은 이전 cube 해제, 실패는 전체
profile 보존으로 처리한다. 동일 cube의 품질 편집은 SRV를 재사용하고 명시 Reload에서 다시 읽는다.

워로드14슬롯은 후속 복원에서 원본 source program과 material-owned2D IBL을 연결했다.
차원술사는 이미 선택한 원본 계산의 scene 입력을 보완하는 작업이며, 워로드의2D texture를
차원술사 환경 큐브로 복사하지 않는다. 현재 구현·검증과 사용자 화면 확인 상태는
[차원술사 재질 RESULT](09-09/2026-09-09_CHARACTER_SCALE_AND_MATERIAL_PARITY_RESULT.md)의 G10과
[워로드 재질 기록](09-09/2026-09-09_BERN_LOCAL_LIGHT_AND_THREE_CLASS_FULL_RESTORE_IMPLEMENTATION_RESULT.md)의 G17을 따른다.
근거: [발탄·세 캐릭터 조사 RESULT](09-08/2026-09-08_VALTAN_MATERIAL_LIGHTING_AUDIT_RESULT.md).

현재 `MODEL_SURFACE_FAMILY`의 0~5는 legacy와 선택 map 표면, 6은 SOURCE_CHARACTER,
7은 발탄에서 선택한 SOURCE_OVERLAY_OPAQUE다. 예전 out의 map family6/7/8 초안 번호를
현재 enum 의미로 복사하면 안 된다. 숫자는 저장 계약·현재 consumer를 함께 확인한다.

근거: [현재 surface 계약](C:/Users/user/Desktop/LostArk/Engine/Public/BinaryAsset/ModelAssetData.h:25), [선택 map 원본 식](C:/Users/user/Desktop/LostArk/Client/Bin/ShaderFiles/Shader_MapMaterialSurface.hlsli:194), [일반 캐릭터 scalar specular](C:/Users/user/Desktop/LostArk/Client/Bin/ShaderFiles/Shader_VtxAnimMeshBinary.hlsl:183).

### 색 공간·geometry·축소 품질을 서로 구분한다

색상 입력은 sRGB decode 후 선형 공간에서 조명하고, 마지막 표시 단계에서 encode한다. 그러나 원본의 특정 diffuse가 SRGB=false인 사례도 있으므로 이름만으로 일괄 바꾸지 않는다. 원본 tagged 값이 없으면 native class-default까지 확인하고, 미확정이면 프로젝트 해석으로 남긴다. 선택 CS의 ORM/lightmap linear와 일부 engine 상수는 그런 경계다.

normal/mask/alpha의 데이터 의미를 보존한다. 현재 TGA mip은 diffuse/emissive RGB를 linear로 풀어 면적 평균하고 다시 encode하며, alpha는 색 공간 변환 없이 독립 평균한다. normal/mask 채널은 linear 평균이다. 이를 원본의 normal 분산 보정이나 특수 alpha-coverage mip 알고리즘까지 복구한 것으로 표현하지 않는다.

DDS 로더는 파일에 있는 mip을 읽는다. 모든 DDS가 자동으로 full mip이 생기는 구조가 아니다. 차원술사 무기 DDS12개는 mip0 압축 bytes와 decoded pixels를 보존하며 하위 mip을 offline 생성해 mip11로 교체했다. TGA full mip은 CMaterial CPU 경로에서 생성한다. Loader worker가 rendering immediate context의 GenerateMips를 호출하는 우회는 사용하지 않는다. Aniso16과 FXAA는 서로 다른 역할이며, FXAA만 켜서 texture minification 복구를 끝내지 않는다.

Tangent handedness, UV1, COLOR0는 mesh에 실제로 있던 채널과 프로젝트 재구성을 구분한다. 반복 UModel 추출에서 달라지는 COLOR0/tangent.w를 원본으로 인증하지 않는다. BRIDGE/MAGICFLOOR의 일부 tangent 부호는 UV 미분 재구성이었고, 근거가 있는 FLOOR12/FLOOR13H는 보존 경로를 사용했다. Normal G나 binormal을 화면 느낌만으로 한 번 더 반전하지 않는다. UV1은 UV0 복제로 대신할 수 없으며 추가 UV가 필수인 source program은 실제 채널 부재를 거부한다.

근거: [CMaterial CPU mip·색 공간·DDS 로더](C:/Users/user/Desktop/LostArk/Engine/Private/Material.cpp:43), [추가 UV와 baked 입력 거부](C:/Users/user/Desktop/LostArk/Engine/Private/Model.cpp:1070), [geometry cooker](C:/Users/user/Desktop/LostArk/Tools/ModelAssetConverter/cook_wmodel_geometry_contract.py), [축소 품질 실측](C:/Users/user/Desktop/LostArk/out/CharacterMinification20260908/character_sampling_findings.md).

### 직접광·lightmap·환경반사를 별도로 연결한다

lightmap은 밝은 그림 한 장을 diffuse에 붙이는 입력이 아니다. 선택 CS 표면은 `atlasUV = UV1 * scale + bias`, 평균/방향 texture 각각의 RGB decode scale, tangent normal과 RNM 기저로 구운 조명을 계산한다. RNM 기저는 tangent space이므로 world 축 변환을 다시 적용하지 않는다. 같은 bridge material을 쓰는 410/490도 atlas 좌표·계수를 공유하면 안 된다.

원본 Point91의 LightmapGuid와 선택 component의 baked GUID가 연결됐다. 이미 구워진 조명을 같은 dynamic point로 다시 더하지 않는다. 선택 maplights에 넣은 Point86/Spot99는 별도이며, 원작의 단위·감쇠까지 동일하다고 확정하지 않은 PROJECT_AUTHORED 연결이다. 원본 Point/Spot 정보 존재가 원작의 모든 빛을 재현했다는 뜻은 아니다.

원본 hdr01 환경은 실제 TextureCube이며 RGBM range6을 decode한다. 선택 환경은 원본 cube와 **프로젝트 수치 적분 BRDF LUT**를 함께 사용한다. LUT는 원작에서 추출한 파일이 아니다. 원본 engine hemisphere/SH·reflection origin·일부 색공간이 미확정이라 완전한 원작 간접광/IBL 복구로 표시하지 않는다. CS의 per-material cube를 캐릭터나 다른 맵 전역 환경으로 하드코딩하지 않는다.

직접광 결과에 이미 albedo가 들어간 source program은 합성 때 또 곱하지 않는다. 반대로 G-buffer diffuse를 흰색으로 둔 채 legacy ambient를 더하면 흰 조명이 재질색과 무관하게 붙어 탈색감을 만들 수 있다. 활성화한 캐릭터 재질은 원본 RT3 표면색을 ambient 입력으로 쓰고 native 직접광은 별도 누적하는 구조다. 실제 조명 조건의 최종 외형은 사용자 확인이 남아 있다.

source character 직접광은 `Target_Depth`의 marker 5와 material row ID로 자기 픽셀만 소비한다. row마다 전체 화면 PS를 반복하는 비용은 `CRenderer` 소유의 별도 D24S8 stencil mask로 줄인다. marker 5인 픽셀을 한 번 표시하고 source light만 stencil == 1에서 실행하며, 기존 row discard·직접광 계산·additive 순서·FP16 누적은 유지한다. mask는 scene depth를 읽거나 쓰지 않고 source light가 끝나거나 실패하면 기존 RTV/DSV를 복원한다. EffectV2 outline과 Esther가 scene stencil의 0xff를 사용하므로 해당 DSV나 bit를 mask와 공유하지 않는다. mask 생성 실패 시 한 번 진단한 뒤 같은 크기에서는 기존 source row 제출을 사용한다.

화면 전체가 marker 5라면 mask 생성 비용이 추가되므로 PS 호출 수 감소를 모든 장면의 FPS 개선으로 해석하지 않는다. 실제 light pass PS/VS 호출 수, CPU 제출과 GPU 시간을 분리해서 확인한다. 소스 반영·수치 동등성·빌드·사용자 화면 검증은 [Character Select 200fps RESULT](09-12/2026-09-12_CHARACTER_SELECT_200FPS_RESULT.md)에서 구분한다.
근거: [lightmap·cube 원본 연결](C:/Users/user/Desktop/LostArk/out/CharacterSelectRestore20260908/lighting_binding_findings.md), [실제 RNM·RGBM 소비](C:/Users/user/Desktop/LostArk/Client/Bin/ShaderFiles/Shader_MapMaterialSurface.hlsli:267), [deferred source 캐릭터 소비](C:/Users/user/Desktop/LostArk/Engine/Bin/ShaderFiles/Shader_Deferred.hlsl:402).

### 발탄 석재는 원본 overlay·specular 계열과 배치별 구운 조명을 복원한다

발탄 원본 PS/SL00~SL05는 LightMapTexture2D420개와 배치별 LightMap2D 입력을 보유한다.
sourceId가 같은12,744배치의 입력을 읽었고, 중앙 석재7배치부터 WModel의 UV1과 material/placement
조명 연결을 추가한다. 남은 native tail을 무광원/조명 없음으로 추정하지 않는다. shadowmap
참조 배열을 건너뛴 뒤 lightmap subrecord를 읽어야 하며, 미해석 후속 색/vertex stream이
있으면 component 전체 ABI를 복원했다고 표시하지 않는다.

대표 `lv_lut_heartrb.mat.bg_pap_stone_rock04_mi_ksr`는 `bg_base_opa`의 D/N+overlay D/N,
정점색, saturation/brightness, specular_color/intensity/power를 사용하는 원본 분기다.
이 선택식에는 ORM이 없고 D alpha는 opacity clip이 아니다. CS PBR 또는 쿠크 바닥의
alpha clip 분기에 텍스처만 바꾸는 것으로 대체하지 않는다. 원본 식의 정점색 의존과 실제
모델/배치가 비백색 정점색을 가진다는 증거도 구분한다. 원본 mesh 자체 color buffer가
비어 있어도 component override에 색이 있을 수 있다. 이번7배치에는 실제 색이 있으며
floor4는 모두 다르고 rock3는2그룹이다. A는전부255이고 R은실제분포가 있다. source VS의
BGRA→RGBA swizzle을 한 번만 적용해 CModel COLOR0으로 운반한다.

CPU 정점색 배열이 존재해도 GPU 입력이 연결됐다는 뜻은 아니다. 이번에 WModel reader는
색을 보존했지만 VTXMESH/CMesh 업로드에서 빠진 경로를 발견했다. 실제 vertex stride68,
RGBA8 COLOR0 offset64, 일반·인스턴스 layout과 업로드를 함께 연결했다. 같은 데이터에
swizzle을 다시 적용하지 않으며 public 구조 변경 뒤에는 Engine/Client를 함께 빌드한다.

원본 asm은 레지스터를 덮어쓴 뒤의 값을 추적한다. 이 식의 Base/Direct/Baked는 모두
`overlayD.a² * (1-saturate(baseNormal.z²*D.a))`를 coverage 입력으로 사용한다. 이전
raw XY 길이 해석은 레지스터 덮어쓰기를 놓친 오류였다. 다만 mixed normal은 Base/Baked만
정규화하며 Direct diffuse는 미정규화 mixed normal, Direct specular는 base normal을 쓴다.
기존 G-buffer의 두 여유 재질 입력을 marker7에 한해 재사용해 이 차이를 보존한다.

원본 floor UV1과 현재 WModel/source glTF의 정점 수가 다르므로 같은 인덱스로
UV를 덮어쓰지 않는다. native corner 대응으로 UV1·색·tangent W를 보존해 배치별 variant를
쿠킹했다. UModel 반복 추출의 rock COLOR0/W가 변하거나 UV1의 half-zero 값을 잘못 내보내는 경우도
확인했으므로 원본 native 채널을 대조한다. 같은 atlas도 배치의 좌표·계수는
다를 수 있으며 이동·복제한 EDITOR 배치에 원본 baked를 무조건 재사용하지 않는다.

원본 Point GUID가 lightmap의 참조 목록에 들어 있으면 직접광과 구운 diffuse의 중복 기여를
확인한다. 현재 발탄 Point22개는 다른 배치의 baked에 포함되지만 이번7배치의 GUID와는
교집합0이다. 전체맵의 교집합을 개별 바닥의 중복으로 오해하지 않는다. GUID 일치만으로 즉시 광원을 삭제하거나
모든 원본 광원을 동적으로 추가하지 않는다. 발탄에서 찾은256×16 LUT는 ColorLookupTable
이며 BRDF LUT가 아니다. static map, 파괴 가능한 Deploy, 파편과 원본 LAND/컷신 입력도
실제 소비자와 활성 범위를 나눠 확장한다.

첫 비교 배치는 `LV_LUT_HEARTRB_ED_SL00:export:1271`, runtime ID
`11789713951910399058`이다. 조사 분모는
[발탄 조사 RESULT](09-08/2026-09-08_VALTAN_MATERIAL_LIGHTING_AUDIT_RESULT.md), 실제 연결과
검증·실행 상태는 [석재 RESULT](09-08/2026-09-08_VALTAN_ARENA_STONE_RESTORATION_RESULT.md)를 따른다.

### Rendering Workbench에서 확인하고 마지막에 튜닝한다

같은 카메라·선택 배치·광원·Exposure/Gamma/Bloom 상태를 유지하고 Floor Materials의 A/B와 Base color → Normal → Direct specular 또는 Roughness/Metallic/Material AO → Reflection delta → Final 순서로 기여를 분리한다. 이 진단은 지원되는 선택 재질 범위의 출력이다. 화면의 모든 표면을 대상으로 하는 검사는 아니다.

쿠크 선택 바닥 family1/2의 marker1 직접 반사는 재질 RGB에 MapLight diffuse radiance를 곱한다. MapLight가 0으로 두는 legacy specular를 곱해 직접 반사가 끊기는 소비자를 교정했다. marker0/2의 legacy specular와 Phong lobe는 유지한다. 기존 하네스36 GPU조건은 통과했고 수정 전에는 직접 반사만 실패했다. 바닥 전체 검정과 최종 화면 판정은 대응 RESULT로 구분한다.

직접 반사가 검다면 재질 입력뿐 아니라 실제 광원도 확인한다. legacy specular와 선택 PBR 직접 반사는 같은 slider 계약이 아니므로, 어느 shader가 해당 광원의 diffuse/specular를 소비하는지 먼저 읽는다. global 밝기 증가로 이 차이를 가리지 않는다. tone mapping의 clipping·White Point·Gamma는 고유색·빛 입력이 맞은 뒤 맞춘다.

가까운 화면과 먼 화면을 비교해 texture mip/filter, geometry 경계 AA, 재질/빛 결함을 분리한다. 새 MRT와 추가 source material × light draw에는 비용이 있다. 합성 WARP 검사나 빌드 성공으로 실제 장면 GPU 병목 또는 FPS 개선을 주장하지 않는다. 실제 원본 유사도는 사용자가 제출한 관찰과 비교로 종료한다.

검증은 변경 범위의 JSON/XML·기존 publisher·실제 CModel/Clone binding·필수 입력 실패 보존·해당 shader 수치·필요한 최소 컴파일을 재사용한다. 기존 쿠크23/CS57 native 검사와 shader 검사는 그 당시 입력 범위의 증거다. 새 표면을 추가하면서 예전 PASS를 자동 승계하지 않는다. WARP는 실제 shader에 대한 수치 실행이며 원본 Client 화면 비교가 아니다.

### Resources와 재개 시 함께 전달할 것

`Client/Bin/Resources` binary는 Git 대상이 아니다. PR에는 Resources-relative ID와 필요한 물리 경로만 기록하며 팀장이 Drive로 공유한다. 새 입력을 설치할 때 추가/교체 여부, 정확한 상대 경로·용도·크기와 Drive 전달 여부를 즉시 알린다. out 분석 자료나 미설치 후보를 설치 완료본에 섞지 않는다. 새 Resource manifest/잠금팩/hash publish 체계를 완료 조건으로 추가하지 않는다.

현재 렌더링 변경의 공유 위치는 다음과 같다.

| 위치(Resources 상대) | 전달 의미 |
|---|---|
| `Map/LV_LUT_MIDNIGHTC_ED/`의 선택 FLOOR08/FLOOR08A override 폴더 | 현재 두 WModel은 원본 geometry 계약 형식이며 새 원본 추출과 vertex/index payload가 일치한다. 과거 before hash 기록을 현재 상태로 승계하지 않는다. 이번에는 교체하지 않음 |
| `Map/CHARACTERSELECTMAP/`의 선택 FLOOR12/BRIDGE01E/MAGICFLOOR03D/FLOOR13H variant 폴더 | 실제 연결된 모델·재질 texture. 같은 맵의 모든 out 추출물을 전달하는 뜻 아님 |
| `Map/Lighting/CharacterSelect/` | 선택 baked pair·원본 cube·프로젝트 BRDF LUT |
| `Map/LV_LUT_HEARTRB_ED/SourceStoneRestore/` | 원본 바닥·바위443배치에 연결한 공유 geometry와 기본/overlay D/N. 배치별 원본 색·UV1 및 두 좌표계 변환을 반영한 접선 부호 포함 |
| `Map/LV_LUT_HEARTRB_ED/SourceDeployRestore/` | 중앙 Deploy A/B의 원본 geometry와 표면 texture. 원본 UV1/UV2·정점 색·재질 슬롯을 기존 CModel/CMaterial로 소비 |
| `Map/Lighting/Valtan/` | 연결된 배치별 RNM 및 원본234 shadow 입력과 lv_lut_heartrb_lut.dds. 미참조 추출물 전체 공유를 뜻하지 않음 |
| `Character/LanceMaster/LanceMaster.wmodel` | 잘못된 LUT 경로3개만 정정한 기존 모델 교체 |
| `Character/WP_WSWP_M_06/textures/` | 12개 DDS의 mip chain 교체 |
| `Character/SourceMaterials/` | 원본 캐릭터 재질용 TGA29개, 84,149,596 bytes. 앞 단계에서 설치한 입력이며 이번 22행 활성에서 소비. Q 필수 입력으로 확대하지 않음 |
| `Character/DimensionMaster/DimensionMaster_Character.wmodel` | 이번 교체본 74,781,640 bytes. eye UV1/2와 hair UV1을 추가하며 기존 형상·뼈·인덱스 보존. 추가 UV reader가 포함된 EXE와 함께 공유 |

캐릭터 원본식은 22행과 WModel 추가 UV를 함께 설치·활성화했다. 새 source eye/hair profile만 먼저 켜면 CModel의 UV 검증에서 로딩을 막을 수 있으므로 설치·새 reader·활성화를 함께 확인해야 한다. 실제 설치 경로의 CModel 생성·clone·재질 입력 검사 40개와 공통 load description helper의 CPU 검사 18개가 통과했다. 정상 playable Model View는 기존 prototype 경로에서 이 재질을 이미 소비하며, generic/V2 모델 fallback에도 같은 catalog helper를 연결했다. Drive ZIP/업로드와 사용자 화면 확인은 아직 수행하지 않았다.

## 매 복원 단위의 기록과 종료 기준

native surface가 legacy 발광 overlay를 사용하지 않으면 생성 단계도 동일한 surface family로
검증한다. 원본 crack에 없는 emissive texture를 Initialize에서 요구하면 재질 준비 뒤의 Deploy
staging이 실패한다. Map Effect의 파괴 바닥 owner flag는 유지하고 legacy 재질의 필수 검사는
그대로 둔다. [발탄 석재 결과 G06](09-15/2026-09-15_VALTAN_STONE_EXPANSION_IMPLEMENTATION_RESULT.md#g06-원본-바닥-재질의-deploy-staging-실패-교정).

원본 tone/LUT 수치 검증과 활성 맵 전체의 밝기 승인은 별개다. 조명·재질 복원이 진행 중인
맵에 원본 후처리·region을 기본값으로 일괄 활성화하지 않는다. 사용자 밝기 회귀가 있으면
기존 활성 profile을 복구하고 source/before 비교 profile을 보존해 독립적으로 비교한다.

### 정적 메시 접선과 actor의 blend·기본 오라

UE→glTF `(x,z,y)`와 glTF→runtime `(x,y,-z)`는 각각 반사다. native packed TangentZ.W를
glTF에 넣을 때 한 번 반전하고 공통 cooker에서 다시 반전해야 최종 runtime W가 원본과 같다.
원본 T/N/W·UV 미분 B와 최종 geometry를 함께 대조한다. 신규 변환에서 발견한 부호 결함을
이미 정상인 모델에 전파하지 않는다. 현재 쿠크 FLOOR08/FLOOR08A와 기존 발탄7개는 원본 대조 후 유지했다.

유령 발탄 program84는 원본 master의 translucent 계약을 기존 native character forward pass로
소비한다. 격자 coverage를 원본 alpha 합성과 혼동하지 않는다. post-render depth/sort는 별도 경계다.
LookInfo 기본 오라는 action notify와 구분해 실제 본·TRS·owner 수명을 연결한다. source loops0은
유한1회가 아니며, owner-sustained 재생은 기존 고정 step Update를 유지한다. 주기 reset이나 매프레임
전체 Seek로 입자 이력을 대체하지 않는다. 세부 근거·검증은 [맵·발탄 결과](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md)에 둔다.

### 원본 장면 후처리와 밝기 비교

Bern/Character Select의 원본 WorldInfo·chain·환경 영역은 같은 bloom이나 ambient 숫자로
대체할 수 없다. World override와 effect의 bUseWorldSettings, inherited flag를 함께 resolve한다.
Bern Lightmass의 환경색은 baked 입력이고, 실제 runtime SH packing은 별도 경계다.
현재 local light315개 중285 SOURCE_CHARACTER 분리는 유지하며, 원본 monster carrier를 쓰는
배경의 material baked enabled와 픽셀 RNM flag가 모두 있을 때는 이 광원을 다시 더하지 않는다.

Rendering Benchmark의 Before/Restored source profile/Return은 네 맵의 별도 profile을 사용하는
session 비교다. 원본 bloom threshold/scale/tint와 맵·region의 활성 후처리를 기존 service로 연결한다.
optional sourcePostProcess는 native UE3 customizable curve와 LUT packing을 사용한다.
원본 LUTBlend는256×16 A8R8G8B8이며 기존 Deferred pass에 추가한 GPU bake로 생성한다.
원본 DXBC의1e-8 pow floor와 GPU 계산 순서를 보존한다. LUT 원본 bytes·활성 override·neutral
입력을 구분하고 source desaturation과 이전 display desaturation을 중복 적용하지 않는다.
블록이 없는 profile은 기존 Hable을 유지한다. 이것은 SSAO, DOF, vignette, 원본 sun exclusion과
환경 particle의 재현을 대신하지 않는다.
기존 RNM/반사/발광이 함께 담긴 emissive의 안개 처리는 버퍼 의미를 분리하기 전에 통째로 바꾸지 않는다.
앞 단계 입력 출처는 [원본 렌더링 결과](09-14/2026-09-14_BERN_RENDERING_RESTORATION_RESULT.md),
현재 native 계산·연결·검증은 [맵·발탄 결과](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md)에 둔다.

확인된 문제는 아래 항목으로 기존 증상 항목에 합쳐 갱신한다. 실패 시도 전체를 쌓는 일지는
RESULT에 남기고 이 노트에는 다음 사람이 반복하지 않을 원리와 남은 경계를 남긴다.

| 기록 | 필요한 내용 |
|---|---|
| 대상/증상 | stable skill/element 또는 placement/material, 카메라·재생 모드·시각 |
| 원본 근거 | package/object/선택 프로그램/채널/CDO·archetype·instance 구분 |
| 실제 원인/소비자 | 빠진 입력·틀린 계산과 이를 읽는 코드·Data 정본 |
| 수정 범위 | 공통 식 재사용인지 새 분기인지, 적용 대상과 보존 대상 |
| 검증 | 해당 JSON/XML/publisher/최소 컴파일, 수치 확인, 사용자 화면 관찰을 각각 기록 |
| 공유 | Resources 추가/교체 정확 경로와 설치/Drive 전달 상태 |
| 다음 경계 | 실제 미연결 기능과 원본 근거가 없는 입력. 추측 완료율을 쓰지 않음 |

Q 이번 위치·시계 교정에는 새 Resources가 없다. 전체 Q 단계에서 추가한
`Effect/DimensionMaster/Textures/FX_MASTERMATERIAL/fx_e_normal.dds`는 기존 Drive 공유 대상이다.
맵·캐릭터는 위 표의 이전 공유 범위와 이후 실제 설치를 구분한다. out 후보는 runtime 입력이 아니다.

다음 사람이 이어갈 때는 이 문서 → 해당 PLAN/RESULT → 현재 git diff → 실제 resource/data
소비자 순서로 확인한다. 코드/JSON 수정과 빌드/EXE 배포는 별개다. 현재 실행 파일에 포함됐는지는
마지막 링크/배포 기록으로 확인하고, 사용자 확인 전 visual PASS나 전체 복원 완료를 쓰지 않는다.


### 복구본 목록 표시와 실제 Open의 작업 분리

스킬의 형제 restore/full/tuning 행 표시만으로 전체 문서를 decode/Drawable 검증하지 않는다.
Play/Open Editor는 선택 파일의 기존 validation을 수행하고 저장본 요소 열람은 Show Elements,
재열람은 Refresh Elements가 소유한다. Current Effect는 현재 편집 문서를 표시한다.
긴 Family 목록은 ImGuiListClipper로 화면에 보이는 행만 만들되 전체 Family 순서 ordinal과
stable Element ID를 유지한다. UI 표시 생략이 runtime element 제거를 의미하지 않는다.

두 native Effect shader는 Product CSO에도 공유 VS/PS가 반영됐다. C++ 도구만 수정한
후속 증분 Product에서는 CSO를 재생성하지 않는 것을 확인했다. 제품 빌드 실측과 실행
상태는 대응 ROUND2 RESULT의 G30을 따르며 Open/Save 체감과 최종 시각 판정은 사용자 몫이다.

### Solo의 의존 모델·준비 시간과 종료 자원 수명

Element/Group/Family Solo는 선택 요소의 modelCueId가 요구하는 모델을 숨긴 pose 공급자로
남긴다. Alt V full의 자식27개는 부모 cue를 삭제하면 codec 단계에서 실패한다. 실제로
stage한 필터·draft 문서를 Update/Seek의 anchor 수집에도 사용하고, 제외한 형제의 bone을
다시 요구하지 않는다. CPU-only Open Editor 다음 Solo는 기존 shared character/skill target을
먼저 준비한다. Play All은 기존 sequencer가 소유한다.

리소스 준비와 같은 프레임의 긴 delta는 첫 preview 시각에 더하지 않는다. 다음 정상 tick부터
진행해 짧은 검격이 첫 표시 전에 끝나는 것을 막는다. 이 변경은 저작 수명·delay를 늘리는
보정이 아니다. 실제 CPU 시계 검사는 화면 재생의 증거와 구분한다.

원본 MIC의 `overridedtwosided`는 parent의 `twosided`보다 우선한다. V57/58/59/60/70/71/73/74/75는
parent만 읽은 descriptor를 교정했고, 사용하는 A/V full14행과 추가 S58도 같은 two-sided
profile을 사용한다. 특히 V70의 `fm_h_sphere_01_1` 반구는 단면 culling으로 안쪽 면이
사라질 수 있었다. blend나 투명도를 전역 변경해 감추지 않는다.

WVF1은 원본 XYZ volume/sample을 저장한다. 입자만 Client `(x,z,-y)`로 변환하고 field를
그대로 두면 같은 중심이 필드 밖으로 판정된다. domain·translation·rotation·sample velocity를
함께 변환한다. CylinderSpin의 radialvelocity는 높이축 속도를 제외하며 원형 평면 성분을
보존한다. source 모듈이 존재하는 것과 그 옵션을 실제로 소비하는 것을 구분한다.

V2 shutdown은 loader join과 Level/tool 객체 소멸 뒤 target/group callback·snapshot을 먼저
비우고 GPU cache·particle pool을 마지막에 비운다. 종료 누락으로 남은 strong/COM reference와
반복 플레이의 지속 증가 누수는 별개다. Engine shutdown 중 D3D live report는 뒤에서 해제할
cache도 포함하므로 그 보고 한 번이나 작은 수명 probe만으로 전체 누수 유무를 판정하지 않는다.

### 한 mesh particle이 원본 재질 슬롯 여러 개를 사용할 때

D `7508b2684fd272292d7b`의 `fm_d_crack_037`은 source materialIndex0의 LocalCrack
286 triangles와 index1의 Ice565 triangles로 구성된다. 단일 Required.Material은
EngineMaterials.DefaultMaterial이지만 실제 ParticleModuleMeshMaterial은 두 MIC를 지정한다.
`detail.mesh.sourceMaterialSlots`가 있으면 그 배열이 실행 재질을 소유하고 기존 primary
metadata는 보존한다. 원본 슬롯 번호를 현재 mesh vector 위치로 바꾸지 않는다.

각 material은 기존 native descriptor·codec·resource stage를 사용한다. 같은 CModel과 한 번
평가한 particle의 위치·수명·dynamic parameter를 공유하고 해당 source materialIndex만 그린다.
중복/누락 슬롯, 원본 MeshMaterial 경로 불일치와 리소스 실패는 전체 stage를 거부하며 기존
preview를 유지한다. 슬롯0은 V66, 슬롯1은 새 SD324의 정확한 Ice RT0 식이다. Ice는 기존
일반 texture3장과 tangentView를 쓰며 SceneColor/Depth sample을 추가하지 않는다.
Tool의 Play/Solo와 detail 저장도 effective slots를 사용하고 사용하지 않는 primary를 활성화하지
않는다. 새 Resources는 없으며 두 표면의 최종 모습은 사용자 확인 전이다.


### DimensionMaster 저작 preview의 시간·재질·운동 경계

V1의 문서 validation/ready 문구는 각 element의 Solo, 입자 생성 또는 화면 품질을 보증하지
않는다. hidden, material hold, source module 지원, bone/owner 이력, 실제 첫 생성 tick과
GPU 재질 출력을 순서대로 구분한다. 특히 mesh material slots를 쓰는 element는 사용하지 않는
primary hold가 아니라 실제 slot 계약으로 판단한다. Data Files의 Add Element도 같은 경계를
사용하되 FOLLOW/Trail/source history를 임의로 분리하지 않는다.

수명 배율 Life16은16초가 아니라 원본 particle life의16배다. emitter 생성 구간과 실제 입자
생존 구간은 다르며,1ms짜리 수동 burst는 첫 eligible tick에서 한 번 생성해야 한다.
시간만 다른 동일 검격은 stable element ID별로 독립 발생한다. CPU birth가 확인됐는데 화면에
보이지 않는 경우에는 shader mask/alpha, transform/방향, attachment를 별도로 조사한다.

Q tuning은 금색 crack 한 항목을 한 번 생성한다. 네 타격은 A full의 WR279/280 검격과 보라색 crack에 해당하며 A의 기존0.25/0.60/0.90/1.30초 시간과 검격 shape는 보존한다.
glass-hole sprite의 현재 R tuning3행과 A full4행은 사용자 요청의
`effect.project-tuned.dimensionmaster-glasshole-purple-rim.v1`을 사용한다. Q51 계산 뒤 원본 RGB
mask의 최대 성분에 rimIntensity0.5를 곱하고, 검은 부분은 alpha0으로 만든다. 경계 색은
(0.48,0.12,0.90), rimOpacity0.35이며 기존 native alpha에 곱해 깊이·입자 fade를 유지한다.
packet row11.z=2가 이 저작 alias를 선택한다. 원본 Q51과 이전 core alias의 계산은 보존한다.
같은 dustparticle texture 이름은 같은 shader나 일괄 보강을 뜻하지 않는다. 사용자가 만든
voronoi 검격과 Q/W/E/D의 다른 native 항목은 보존한다.
S의 cone과 helix2개는 사용자가 unified에 만든 저작 mesh이며 full 원본과 분리했다.
두 source velocity의 부호가 달라도 추가 yaw가 뒤집으면 같은 방향으로
나갈 수 있으므로 R 파편처럼 transform과 반구 위치 분산까지 함께 확인한다.

F의 SD325–332는 기존 source-native 경로를 확장하고 원본 texture/parameter/PS를 소비한다.
SD329 하나의 LocationEmitter는 다른 emitter의 입자 state를 요구한다. 현재 독립 저작 항목은
ppp Orbit에서 만든 startlocation curve를 사용하는 authored tuning이며 원본 cross-emitter
운동/RNG와 동등하다고 기록하지 않는다. FOLLOW element의 수치 fixture 성공도 실제 Animation
Target과 뼈대 이력이 준비된 Client Solo 성공으로 승격하지 않는다.

선택·재생·저장 비용은 서로 나누어 측정한다. S sprite500개는 이미 하나의 instanced draw를
사용한다. 같은 vector-field/SubUV 설정은 element/tick 또는 draw batch에서 재사용하되 입자별
값과 순서는 유지한다. Save는 candidate 검증과 atomic temp 왕복을 유지하면서 중복 canonical
파싱/preview 준비를 줄인다. MSVC에서 map 멤버를 가진 JSON node의 vector 재할당은 깊은 복사를
유발할 수 있으므로 실제 parser CPU와 누적 allocation을 확인한다. 누적 allocation 감소를
누수 수정이나 retained 메모리 감소로 표현하지 않는다.

Play All의 source update module 분류는 실제 staged document에서 원본 순서·enabled 상태로
준비한다. `Stage_PrevalidatedDocument`는 prepared 리소스를 재사용한 value-only 편집도 허용하므로
이전 문서의 분류를 그대로 쓰지 않는다. prepared owner를 교체할 때 그 owner 내부의 shared_ptr를
reference로 붙잡지 말고 필요한 immutable document 수명을 값으로 보존한다. 입자별 분포·RNG·시간
계산은 그대로 두고 불변 표시값과 모듈 분류만 재사용한다. `Update_WithTransformHistory`가 실제
Sequencer 경로이므로 일반 Update만 측정해 재생 비용을 대표하지 않는다.

`CShader`의 variable/pass lookup과 마지막 성공 입력은 해당 Effect 수명의 공유 owner에 속한다.
객체별 uniform cache는 Clone 사이 갱신을 놓치므로 금지한다. 모든 Clone의 setter가 같은 owner를
갱신할 때만 작은 raw/matrix와 단일 SRV의 동일 쓰기를 생략하며, 종류 변경·array·실패는 기록을
무효화한다. pass Apply는 항상 수행한다. Debug 비용과 clone 교차·실제 GPU 출력을 함께 검증한다.
입자 instance buffer는 현재 slice의 offset과 append cursor를 구분한다. 미사용 tail에만
WRITE_NO_OVERWRITE를 적용하고 tail 부족 때 WRITE_DISCARD한다. frame 경계에서 cursor를
임의 초기화하지 않으며 draw 순서·payload·startInstance를 유지한다.

Native 셰이더 분리는 물리 파일과 실제 실행 프로그램을 함께 나눈다. 기존 Find/Has 계약을
통과한 profile을 `Effect_ShaderFamily.h`의 carrier·family·구간으로 준비하며 숫자 범위만으로
새 profile을 허용하지 않는다. Native와 범용 backend의 배타성을 실제 staging/override에서
확인한 뒤 Native FX의 쓰이지 않는 범용 PS front-end와 packet bind를 제외했다. Generic FX의
기존 backend 우선순위는 보존하고, mixed native/backend 상태는 선택 경계에서 실패한다.
원본 함수 본문·VS 입력·WPO·sampler·scene/depth와 pass는 유지하며, 대표 실제 FX 출력 비교와
전체 family의 입력/pass 검증을 구분한다. 정적 명령 수 감소는 실제 FPS 개선율이 아니다.

Saved Skill Effects 목록은 고정190px 대신 창의 남은 높이를 사용한다. 상시 장문 안내와 중복
진단 요약을 없애고 실패 상태는 한 줄과 전체 툴팁으로 보존한다. Add Element와 검색은 유지한다.
미지원 Solo 정리는 사용자 범위 승인 뒤 현재 effective material·enabled 상태·의존성 근거로만
수행한다. 전체14개 full 문서에서 확정 차단149행을 제거했으며 원본이 아닌 S 손튜닝3행은 별도로
분리했다. 정상 owner/model cue와 사용자 숨김만 있는 D1행은 보존한다. 첫 프레임의 size/alpha0,
지연 발생 또는 bone 준비 필요 자체를 삭제 근거로 사용하지 않는다.


### 원본 occurrence와 다른 스킬에서 복사한 저작 검격의 구분

R/A Q51 dust의 normal `fx_j_normal_bc5_09`는 원본10mip, S V63의 noise009/014/021은 원본8mip을
회수해 Resources에 연결했다. 이전 UModel 기본 DDS는 mip0만 보존했다. 원본 PS가 SampleBias를
쓰는 texture는 하위 mip 없이 같은 수식만 복구해도 축소 입력이 달라진다. 이번 회수는 원본 mip을
기존 UModel decode 경로로 각각 가져왔고 mip0 바이트를 보존했다. 범용 Crunch decoder는 CRC가
맞아도 UModel mip0와 결과가 달라 채택하지 않았다. 원본 BC1/BC5 데이터의 동일성을 먼저 확인한다.

원본 R WR208/259/260의 SampleBias texture8개도 같은 방식으로 하위66mip을 회수했다.
caustic05/electric008/noise03/environment02/cloud021/noise030/auratile02/atypical03_1이다.
현재 mip 수는 각각10/8/10/10/8/9/9/10이며 실제 D3D11 resource/SRV와 원본 mip0 보존을
확인했다. WR260 main trail007과 Q51 mask처럼 원본이 명시적 LOD -1을 쓰는 항목은 이번
축소 입력 결함과 구분한다. Resources 상대 ID를 공유하는 다른 저작 요소에도 같은 파일이 적용된다.

WR260은 원본 CPU uniform의 sin/cos를 GPU intrinsic으로 바꾼 prefix에서도 작은 UV 차이가
발생했다. 원본과 같은 CPU 평가를 기존 parameter builder의 미사용 row13에 연결하고 실제
rotator 편집을 유지했다. source constant buffer를 양쪽에 동일 공급한 검사와 native prefix를
포함한 검사를 구분해야 한다. raw PS 일치만으로 uniform 생성까지 같은 것으로 판단하지 않는다.

Source sprite Required의 `psortmode_viewprojdepth`는 alpha sprite의 element 내부 입자를
camera clip-Z 큰 순서로 그린다. 현재 renderer는 이 명시적 값을 기존 instance 업로드에서
소비하며, 원본 Location에 대응하는 World.translation을 사용한다. pivot/cameraOffset은 sort
key에 넣지 않고 같은 깊이는 입력 순서로 유지한다. 이 지원을 PSC 간 전역 투명 정렬이나
다른 sort mode의 복원으로 확대하지 않는다.

Q51의 원본 immediate32와 다르던10개 instruction 상수를 복구했고, sprite의 texture V와
normal에 대응하는 tangent-view yz 부호를 교정했다. 원본 PS와 같은 통제 입력의120조건은
수치 허용 오차를 통과했지만 실제 원작 장면/Live VF 전체 또는 사용자 화면 일치의 증거는 아니다.
Q51 alpha를 만드는 normal/mask와 dust RGB는 다른 입력이다. 정상 구간과 특수 분열 구간을
구분하기 위해 원본 Dynamic 시간 곡선은 보존하며, 검은 면 보강으로 원본 계산을 대체하지 않는다.

R tuning의 `purple-group-1/2/3-slash`는 R foldcut 원본이 아니라 Q nailstrike
`particlespriteemitter_13`(Q51 glasshole)에서 복사한 행이다. 원본 module의 PSA_Velocity,
center offset 및 size를 직사각형 정렬·무오프셋·가로 크기로 바꾼 입력이다. 따라서 R tuning의
모양 문제를 원본 R PS의 결함으로 동일시하거나, clean-core 합성을 원본 복원으로 기록하지 않는다.
R full의 WR259 sprite와 WR260 swing mesh는 원본 foldcut의 별도 material/VF 경로다.

원본 selected shader와 같은 코드가 있다는 사실만으로 모든 입력의 복원이 입증되지는 않는다.
material constants·texture sampler·vertex factory 출력·particle module 및 scene/fog/depth를
동일 입력으로 대조하고, native RT0와 engine adapter의 검증 범위를 분리한다. project-authored
보정은 별도 profile로 식별하며 원본 계산 오류를 확인하기 전에 유일한 해결책으로 선택하지 않는다.

### Effect Sequencer의 공통 Composition authoring

Full Restore 전체의 번짐은 V1 root `bloomIntensity`로 저장한다. 1·2·3·4 등 번호별 문서와 통합
문서는 각자의 stable effectAssetId를 유지하고 별도 값을 갖는다. 생략값1.3, 유한한0~16 계약이며
Effect Detail의 `Skill Bloom Intensity`가 현재 문서 전체 preview에 즉시 적용된다. Save 전에
재생을 멈췄다 시작해도 저작값을 유지하며 Reload/Revert는 다시 읽은 문서 값을 사용한다.
원본 color/emissive를 줄여 bloom을 흉내 내지 않는다. 공용 RT2에 문서별 밝기 추출 기여를 모으고
기존 blur/final을 사용한다. 저장·UI·draw 전달과 실제 검증 범위는
[문서별 bloom 계약](../TEAM/EFFECT_FAMILY_RUNTIME_ABI_RESTORATION_GUIDE.md#문서별-bloom-기여)과
[스킬별 Bloom 결과](09-12/2026-09-12_EFFECT_PER_SKILL_BLOOM_RESULT.md)를 따른다.

`CEffectAuthoringSequencer`는 Action Workbench와 같은 `CompositionTimeline` 및
`CompositionResourceTree`를 사용한다. Animation/whole Effect/geometry Collider/Sound/
Camera/Screen Post occurrence가 하나의 clock을 소비한다. 전체 문서의 element를 자동으로 펼치지 않는다.
복구본 `.restore`의 개별 Solo 또는 Effect Detail의 `Timeline Solo`는 선택 stable element ID만
임시 행 하나로 표시한다. 원본문서 0초를 유지하고 원래 등장 시각으로 이동해 정지하며,
필요한 model cue는 비표시 anchor로 보존한다. Detail 수정/Refresh는 선택 scope와 cursor를 유지하고
실제 element 종료 시각으로 행 길이를 갱신한다. 이 임시 행은 Append/Save로 승격하지 않는다.
Stop 뒤 기존 저장행으로 돌아오며 전체 Preview는 기존 문서 경로를 사용한다.
`.effectsequence.json` v4의 추가 트랙은 별도 authoring 문서이며 skillbindings와 Server 권위
combat timing을 변경하지 않는다. Collider는 기존 geometry definition과 `CHitAreaWire`를 사용한다.
선택은 캐시된 metadata를 읽고 실제 선택 resource의 검증은 Append/Play/Load에서 수행한다.
구현·검증·사용법은 09-07 Composition Workbench PLAN/RESULT G28·G29와 팀 사용서17.8이 정본이다.


원본 shader literal의 정확도를 검사할 때는 DXBC immediate32 bits를 기준으로 삼는다.
D3DDisassemble의6자리 출력만 번역하면 작은 threshold가0으로 잘릴 수 있다. Q51에서
10개 instruction/11개 lane 차이를 확인했고5e-7→0 사례가 있다. 이 exact-bit 교정은 현재 제품에
반영됐으며 통제 입력120조건의 출력 대조와 G36 Product 빌드가 완료됐다. 출처가 원본이라는 사실과 번역 후 literal이
동일하다는 사실을 구분하고, 식의 일치 판정에 원본 bits·binding·입력 레지스터를 함께 확인한다.

사용자의 A 원본 관찰에서 검격 경계의 가로 파쇄는 정상 표현이다. 검은 중심 면과 보라색
경계의 시간별 형성·소멸, 주변 cube/crack의 발생을 구분한다. 모든 가로 틈을 없애는 고정 중심
mask를 원본 복원 기준으로 사용하지 않는다. 정지 이미지의 모양만으로 실제 element 개수나
시간 순서를 확정하지 않으며, 이 관찰 자체는 현재 Client 출력의 visual PASS가 아니다.

Rendering Benchmark의 맵 A/B는 기존 `bUseSourceMaterials`만 달리하고 level·camera·viewport·quality·fog·shadow·light의 실제 필드를 비교한다.
구조체 padding을 조건 키에 넣지 않는다. 측정 중 조건 변화는 비교 제외, GPU 표본 부족은 비교 불가로 표시한다.
이 비교는 이미 연결된 source material에만 적용되며 전체 맵 복원 여부를 대신 증명하지 않는다.

UModel glTF의 normalized UBYTE skin weight는 converter 출력과 원본 bone 이름별 influence를 대조해야 한다.
MN_CDMD_00에서 입력은 정상이지만 출력이 손상됐으며, 원본 정규화 값을 FLOAT accessor로 바꾸어 재cook한 뒤
모든 정점의 일치와 실제 CModel clone/clip/texture 로드가 통과했다. runtime validator를 완화하거나 NaN만 제거하지 않는다.

## 후속 복원에서 확인한 비베이크 분포와 준비 비용

source distribution의 LUT가 비었다는 사실만으로 원본값0이나 damaged asset으로 판단하지 않는다.
bCanBeBaked=false인 UniformRange/ConstantCurve는 원본 객체의4범위 벡터와 keys를 회수해야 한다.
분리된 양수·음수 범위를 하나의 min/max로 합치면 원본에 없는0폭 검격을 만든다. 현재 D의 일반
authored distribution operation4는 strict14값 payload와 selector→XYZ 난수 순서를 사용한다.
기존 reconstructed source projection의 operation 범위와 혼용하지 않는다.

V의 MomentaryRift와 F/BA의 일부 StartSize도 같은 범위 축소 결함이 있었다.
source 이름이 UniformRange인데 operation1/2인 행은 원본 네 벡터와 실제 runtime RNG
소비를 대조한다. 이미 사용자가 편집한 일반 size multiplier까지 일괄 초기화하지 않는다.

원본 PS에 MIC의 emissive scalar가 들어 있는데 importer가 그 값을 generic
`emissiveIntensity`에도 넣으면 발광이 두 번 증폭된다. exact native 연결 시 이 외부
배율과 UV panning이 원본 uniform을 중복 적용하는지 확인한다. 단순히 원본 PS 계산이
일치한다는 결과만으로 최종 합성까지 정상이라고 판정하지 않는다.

SceneColor를 포함한 sprite를 오래된 pre-BLEND 화면과 합성하면 앞서 그린 효과가
사각 carrier 범위에서 사라질 수 있다. V의 해당 occurrence는 native RT0 연산의
오류와 달리 화면 복사 시점의 결함이었다. 갱신은 현재 MRT0가 SceneHDR인지 검증하고
RTV/SRV 충돌을 해제한 뒤 복사하며 MRT1과 DSV도 복원한다.

복원용 disassembly의 소수6자리 출력은 원본 shader 상수 정본이 아니다. 원래
`2.5e-7` 또는 `5e-7`인 비교 임계값이0으로 출력되면 log/pow 뒤0-mask를 선택하는
분기가 없어질 수 있다. 원본 DXBC의 IEEE immediate bits와 번역식을 대조하고,
임의 epsilon을 더하는 대신 그 비트를 보존한다. 원본 PS와의 GPU 비교에는 UV의
정확한 중심·끝과 경계 분기를 포함한다. 호스트에서 binding한 행렬도 각 native
함수의 실제 constant 대입까지 이어지는지 확인한다.

scene exposure를 받지 않아야 하는 화면 이미지에는 V2 ScreenPost TexturedOverlay의
optional `displaySpace`를 사용한다. 기본false는 기존 HDR 합성이고 true는 최종 tone
mapping 이후/UI 이전 합성이다. 원본 이미지·투명도·타이밍·크기는 유지한다. 쿠크
공포 얼굴처럼 표시 공간 이미지의 밝기 문제를 전체 Scene Profile 노출 변경으로
해결하지 않는다.

native panning이 있는 재질에 generic UV speed를 중복 적용하면 clamp alpha mask가 잘릴 수 있다.
교정한 발생과 원본 옵션의 근거는 Round2 RESULT G37이다. S 검격의 size2는 사용자 가독성 보정이며
원본 크기 복원과 구분한다. 추가 light와 post의 source identity, color와 lifetime은 복구했지만
원본 light radius·attenuation 전체 일치는 입증하지 않았다.

큰 full 문서의 Load, immutable admission hash, 일반 저작 준비, 고정 tick, 입자 draw와 투명 픽셀
비용은 서로 다르다. 일반 저작 준비는 이미 검증된 값 편집 문서에 쓰지 않는 canonical 재직렬화를
생략하며 기본·immutable·visual/reconstructed identity 검사는 유지한다. AltV318의 준비6.3초→1.3ms와
2,060 checkpoint 동일성을 확인했다. 이후 공통 module 분포/literal 준비와 native mesh instancing을
구현했고 full106문서의30,822 checkpoint도 byte-exact였다. Debug headless HistoryUpdate 합계는
38.4초→18.0초이며 화면 렌더링 시간/FPS가 아니다. JSON Load와 큰 투명 면의 중첩 비용은 별도다.
native frontend는 registry뿐 아니라 실제 shader의 family 범위도 함께 확인해야 한다. 새 high-profile
971저장행이 기존 guard에서 clip되던 결함을 별도로 교정했다. 이 변화는 실제 표시량을 늘릴 수 있어
성능 수정의 동일 출력 검증과 구분한다. 상세는09-09 Effect Shader Build and Play All Performance
RESULT G08이며 사용자 실제 재생과 visual fidelity는 아직 승인되지 않았다.

## 소환 모델·카메라·숨긴 위치 제공 요소의 연결

원본 emitter 내부 시간과 실제 action occurrence 수명은 다르다. Artist31930의 500초
emitter를 그대로 자연 종료시키면 다음 사용의 mesh 예약이 owner 한도를 초과한다.
내부 분포 시계는 보존하고 6초 action cue의 `cue_end`로 종료한다. 다음 action의 동일
camera cue가 도착하면 이전 예약을 해제하되 Layer update 중 container를 지우지 않고
service update로 제거를 미룬다. 한도를 높여 증상을 가리지 않는다.

원본 static 배경이 이미 있는 각성기는 새 skybox를 만들 필요가 없다. 선택적인
`compositionLayer: sceneBackdrop`은 MESH/opaque depth-write 배경에만 허용한다.
로컬의 실제 활성 배경이 제출되는 프레임에는 맵·level sky·map shadow를 생략하고,
배경은 기존 NONLIGHT 단계에서 일반 투명 FX보다 먼저 그린다. 캐릭터 depth는 유지한다.
원래 alpha인 장식은 normal 합성을 유지한다. 전체 맵 교체 방식이며 부분 pixel mask는
아니다. remote occurrence와 Solo 제외 요소는 이 요청을 만들지 않고 frame 끝에 해제한다.

Lance V/T 용의 확인된 geometry는 변환 전후 크기가 같았다. playable model의
pre-scale과 source bone basis가 합쳐져 attachment에 0.01 축척이 다시 들어간 경우였다.
해당 source-bone attachment에서 basis만 정규화하고 translation과 원본 particle size는
유지한다. 변환 오류와 runtime 중복 축척을 geometry/행렬 실측으로 구분한다.

Warlord17250에는 원본 camera particle 10개와 static notify 10개(48개 mesh section)가
누락돼 있었다. camera track만 복구해도 배경이 채워지지 않는 이유다. 재사용 가능한
기존 CModel geometry/MIC section을 먼저 대조하고 원본 bone·offset·회전·duration을
각 occurrence에 연결한다. 구현·후보 적용·제품 빌드·사용자 화면 상태는
[후속 RESULT](09-10/2026-09-10_AWAKENING_KOUKU_PRESENTATION_IMPLEMENTATION_RESULT.md)에서 구분한다.

UModel PSA를 glTF로 옮길 때 root와 non-root quaternion의 좌표 변환 계약을 구분한다.
child W 부호 누락은 가중치·UV가 정상인 평면 소환 모델도 뒤집힌 가느다란 선으로 보이게
할 수 있다. 크기를 임의로 키우기 전에 원본 pose와 삼각형 방향을 전체 clip에서 비교한다.
Artist DRA의 quaternion 교정은 원본 폭을 유지한다. 사용자의 몸통 두께 요청으로 현재 설치본은
그 교정 위에 bind X 폭을5배(약19cm→95cm)로 저작했다. 애니메이션 길이·clip bytes와
가중치는 유지하고 normal은 역전치로 보정했다. 이 폭은 PROJECT_AUTHORED이며
원본 좌표 복원의 결과와 구분한다. 주변 helix 먹선은 별도 요소다.

CAMERA_VIEW FOLLOW 중 bone과 model-cue 의존성이 없는 일반 요소는 원본 애니메이션
이력이 필요하지 않다. Saved Element 복사는 이 좁은 부착을 그대로 보존할 수 있으며,
bone FOLLOW·trail·source history까지 일반화하지 않는다. Sprite의 billboard가 최종
면 방향을 재구성하면 Transform rotation만 바꿔서는 기울기가 바뀌지 않는다. 사용자가
회전할 일반 평면은 기존 billboard=false와 명시 rotation 계약으로 저작한다.

Artist31930 원본 연출은 player에 부착한 카메라·안쪽 하늘 구체·꽃밭을 조합한다. 맵 이동이나
두 번째 캐릭터/모델 런타임을 추가하지 않는다. CEffectRecoveryCamera의 sidecar 해석과
샘플링을 F1 및 제품이 공유하며, 제품은 사전 준비 시 캐시한 문서를 현재 local action에만
적용한다. 원본 local-only 요소는 remote 렌더 제출에서 제외한다. 원본 engine directional
light의 release 수명과 일부 hide-pawn 의미가 미확정인 상태는 카메라 복구와 구분한다.

원본 ERM_None emitter는 화면에 안 보이더라도 다른 emitter의 위치를 제공할 수 있다.
일반 SourceRecipe의 simulationOnly/particleSystemOccurrenceId/emitterName과 안정 ID
provider 참조는 기존 Playback 안에서 이 역할만 표현한다. 같은 발생·유일한 이름·선행 순서·
충분한 생존 시간의 provider를 검사하고 draw/resource staging에서 제외한다. Solo에는
선택한 요소와 필요한 provider만 포함한다. 현재 지원은 생성 시 position 상속이며 velocity/
rotation 상속과 provider chain은 거절한다. validator는 source 전용 검사가 아니라 일반
authored Save/Load 및 Stage의 실제 소비 경로에서 실행되어야 한다.
`Validate_Drawable`도 simulation-only provider를 resource binding 검사에서 제외해야 한다.
일반 Load/Save가 성공해도 이 별도 검사에서 텍스처를 요구하면 Play가 invalid가 된다.
현재 Artist31930은254행·4카메라를 단일 full.restore로 연결하며, 후속 clip의 시작 시간과
provider·카메라 clock을 함께 이동했다. 실제 표시30행과 provider closure Solo30개가 통과했다.

워로드 본체9개 모델14개 slot은 원본 material 소유 texture/parameter와 source program1~14
범위로 연결됐다. 눈 UV1/UV2와 머리 UV1도 실제 모델 admission에 필요하다. 반면 소환 방패
PBR01/02의 engine 소유 cubemap/BRDF와 light prefix 일부는 미확정이다. 원본 PS 명령과
현재 엔진의 directional/point/spot 입력 연결을 원작 engine 전역 상태 전체의 복원이라고
표현하지 않는다. material 소유2D IBL과 engine cubemap도 서로 구분한다.


### 정적 World Object의 명시 diffuse와 공유 character material 상태

`CWorldSequenceObject`의 static draw는 `CMapAssetRenderUtils::Bind_Material`에 명시 diffuse SRV를
전달할 수 있다. 이 분기는 `CMaterial::Bind_ShaderResource(DIFFUSE)`의 character program reset을
거치지 않으므로 공유 `Shader_VtxMeshBinary`에 직전 장비의 source program/row·염색·피격 발광 값이
남으면 올바른 diffuse가 로드돼 있어도 다른 재질 계산을 사용한다. 정적 draw에서 해당 선택 상태를
초기화해야 하며 optional character 변수가 없는 instanced shader는 기존 방식으로 유지한다.

쿠크 등장 공은 `fm_d_rhcn_00.wmodel`과 `mn_rhcn_00_d.dds` 연결을 유지한다. 실제 DDS는 원본과
동일하고 유색 texel이 존재한다. texture 파일 존재·4slot 로드 성공만으로 공유 shader의 잔류 상태를
배제할 수 없다. 이번 수정은 Client의 기존 material binder이며 Resources payload를 수정하지 않는다.
source mapping/수치·컴파일과 사용자 화면에서의 색 확인은 별도 증거로 기록한다.

골격을 확장한 WModel은 mesh/WSKL뿐 아니라 각 WANM trailer의 skeletonHash도 동기화한다.
애니메이션 key·event를 그대로 보존하더라도 이전 골격 hash가 남으면 실제 WAnimationReader가
전체 모델을 거절한다. Python section parse만으로 actual decoder 성공을 대신하지 않는다.


### 같은 cooked materialName을 쓰는 카드 variant의 native texture

조커 `MN_RHOC_00-1.wmodel`의 cooked materialName은 일반 카드와 같은 `mn_rhoc_00_mi`다.
이 이름은 CModel slot 검색 계약이고 원본 MIC identity와는 다르다. 실제 조커 MIC는
`mn_rhoc_00-1.mat.mn_rhoc_00-1_mi`이며 설치 WModel의 D/N/S도 `mn_rhoc_00-1_{d,n,s}.dds`다.
BossCatalog에 일반 카드의 sourceMaterial·texture를 복제하면 native override가 올바른 내장
variant 입력을 덮어쓴다. `modelAssetId → cooked slot → 원본 MIC → expression texture`를 함께
대조하며 cooked slot 이름을 원본 MIC 이름으로 바꾸지 않는다.

`source.character.monster-6ff78ae19259.v1`의 program 26은 native expression 1의 diffuse RGBA와
약 1/3 alpha mask를 직접 소비한다. 일반 diffuse override나 보라색 tint 추정으로 이 오접속을
보정하지 않는다. 조커와 일반 카드는 shader·36개 parameter가 같고 D/N/S 입력이 다르다.
현재 BossCatalog 조커 행은 기존 Character variant texture를 expression 0=N(linear),
1=D(srgb), 2=S(srgb)에 연결한다. Resources payload와 공통 shader는 수정하지 않았다.
JSON·설치 입력 검증과 사용자 화면 미확인 경계는
[조커 교정 결과](09-12/2026-09-12_KOUKU_JOKER_NATIVE_TEXTURE_RESULT.md)에 구분한다.

### 맵 그림자와 캐릭터 재제출의 비용 경계

쿠크의 정적 이동 마커는 Level이 metadata와 7초 phase를 소유하고 MainApp이 최종 카메라 이후
가시성을 확정한다. 기존 MapAssetRenderUtils의 sphere predicate에 입자 tail·mesh·camera offset을
포함한 bounds와 진입/이탈 여유를 공급하며 invalid 입력은 기존 표시를 유지한다. 처음 필요한
마커만 prepared target으로 생성하고, 화면 밖에서는 handle을 보존한 채 external sample과 제출을
생략한다. 재진입은 현재 phase의 실제 history를 복구한 뒤 표시한다. 임의의 플레이어 거리만으로
자유 카메라·연출 카메라에 보이는 표시를 숨기지 않는다.

Level-owned external marker만 EffectPresentationService의 명시 sample/제출을 사용한다.
EffectObject의 일반 Late_Update와 이 owner는 같은 Submit_RenderGroups 본문을 공유하며, 명시
owner의 자동 제출은 비활성화한다. Shader·입자 수·Server 트리거를 변경하는 별도 runtime은 없다.
검증한 marker 정의가 바뀌면 bounds를 다시 확인한다. 현재 정의·sphere 실측·재진입 검사는
[쿠크 마커 성능 결과](09-14/2026-09-14_KOUKU_MARKER_VISIBILITY_PERFORMANCE_RESULT.md)에 둔다.

맵 shadow culling은 camera frustum과 분리한다. `CShadow`가 실제 shader에 바인딩하는
view/projection의 보수적인 world sphere 검사만 사용하며, 화면 밖 caster를 camera 기준으로
제거하지 않는다. frame provider가 light를 바꿀 수 있으므로 shadow instance 준비는 실제
Render_Shadow에서 한다. light 행렬·placement·visibility 변경은 cache를 갱신하고, invalid
입력은 제출을 유지한다. GPU upload 성공 전에는 이전 payload/revision을 교체하지 않는다.

정적 depth 재사용은 기본 거절인 GameObject 계약을 통해 검증된 불투명 및 시간·카메라에
독립적인 alpha-tested 맵 batch/개별 객체를 선택한다. 기존 shadow pass와 alpha 판정은
그대로 실행해 깊이를 저장한다. 최종 light view/projection, source-material 모드, weak owner와 instance
revision이 같을 때 정적 depth를 복사하고 동적 caster를 매 프레임 합성한다. transform·visibility·
bounds·객체 구성·light·scene replacement 변경과 실패는 재사용을 중단한다. fade,
morph와 미확인 변형은 기존 경로를 유지한다.
BG masked의 parallax/panning, legacy/foliage의 UV 이동과 외부 texture override는 캐시에서
제외한다. opaque는 기존 null-PS, alpha-tested는 원래 PS를 유지하며 두 조건을 혼동하지 않는다.
개별 객체는 실제 Transform, bounds/cull 상태와 mesh별 cast/pass를 exact snapshot으로 비교한다.
fallback 객체의 light-volume 검사도 실제 vertex
변형을 보수적으로 제외한다. Character Select/Bern/Valtan/Kouku는 같은 소비자를 사용한다.
local light 감쇠가 정확히0이면 뒤의 재질 계산을 생략할 수 있으나 광원 범위·순서·개수는 유지한다.
실제 depth/조명 출력 비교와 제품 FPS는 [후속 성능 결과](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md)의 G27/G29/G31에서 구분한다.

Level Initialize 중 ambient admission은 아직 current level이 LOADING일 수 있다. 임시 clone
probe만 현재 LOADING layer에서 생성·검증·전부 Stop하고 실제 activation 이후 spawn의 target
level은 유지한다. target 또는 LOADING 이외의 소유자 불일치는 거절한다. owner-sustained
SOURCE_LOOP는 queued/즉시 spawn에서 같은 level-owned/no-boss 또는 boss-owned 계약을
검사해야 한다. active-level 검사 자체를 삭제하거나 probe를 실제 맵의 살아 있는 effect로 남기지 않는다.

LEVEL_ACTIVE + SOURCE_LOOP의 장식용 static sprite는 source module·distribution·수명·이동·크기·
pivot를 감싼 bound를 CEffectPlayback이 계산할 수 있을 때만 final-camera 단계에서 visual clock을
멈출 수 있다. 숨은 particle/RNG state는 보존하되 hidden wall time을 따라잡지 않는다. root가 바뀌거나
camera가 non-rigid이면 fail-open하고 light/screen/control/mesh/trail/미지원 module에는 적용하지
않는다. 기존 거리별 Stop/respawn, Server trigger·combat·외부 sample 시간은 유지한다. actor 중심점
검사로 전체 effect를 제거하거나 모든 Level에 asset-ID 예외 표를 만들지 않는다. deferred local light는
영향 sphere 전체가 밖일 때만 pack 이전에 생략하며 원본 light 목록·shadow caster는 유지한다.
[범위·검증·재진입 정책](09-22/2026-09-22_BERN_RELEASE_PROFILER_OPTIMIZATION_RESULT.md)의 G07을 따른다.

MapInstance의 family 0~5는 tangent/baked-lighting 계산 없는 shadow pass 18~20을 사용한다.
source material 활성 family 3/4/5는 alpha와 무관한 opaque 계약이므로 position VS와 null PS의
21~23을 사용한다. source 비활성 시 legacy alpha 경로를 유지하고 복잡 family의 12~14,
기존 map/water pass 번호는 보존한다. animated character shadow는 기존 diffuse alpha만
준비하며 static socket shader의 surface 경로를 임의로 간소화하지 않는다. 장비 포즈 재사용은
source owner/revision과 destination 변이 revision이 모두 일치할 때만 허용한다.

GPU timestamp 구간은 CPU 명령 제출 공백도 포함할 수 있다. shadow elapsed가 크다는 사실만으로
순수 GPU shader 병목을 확정하지 않고 CPU self, PS/VS 호출과 동일 장면의 사용자 캡처를 함께 본다.
구현과 수치 검증 범위는 [맵·캐릭터 성능 결과](09-12/2026-09-12_MAP_CHARACTER_RENDER_PERFORMANCE_RESULT.md)에 둔다.

공통 static mesh LOD는 재질 복원 경계를 유지한다. CMesh 공유와 material variant를 구분하여 현재 draw의 불투명·비변형 조건을 다시 검사하고, geometry 단순화의 기존 0.25px 한계를 유지한다. CPU에 있는 scalar 선택을 compute/indirect로 왕복하지 않으며 visible instance의 보수적 view-space envelope를 사용한다. draw 수 감소와 정점 수 감소는 별개이므로 pass CPU/GPU, 실제 draw·선택 indices를 함께 비교한다. 수치 동일성과 사용자 화면 판정은 [베른 Release 최적화 결과](09-22/2026-09-22_BERN_RELEASE_PROFILER_OPTIMIZATION_RESULT.md)에서 분리한다.

### 원본 재질 증가 이후 실제 로딩과 배포 확인

FXC 성공은 CShader admission 성공과 다르다. native material 분기가 늘면 Debug 비최적화 Effect가 로더의 기존 크기 제한을 넘을 수 있다. 해당 shader의 실제 CSO 크기와 CShader 생성·material bind를 확인하며, 검증된 shader 최적화로 해결한 경우 그 설정을 프로젝트에 보존한다.

같은 원본 재질을 사용하는 Binary와 MapInstance의 프로젝트 최적화 설정도 함께 확인한다. 한쪽만 비최적화이면 재질 복구 뒤 실제 draw 경로의 비용이 달라질 수 있다. 계산 보존은 기존/후보 CSO의 실제 MRT 값과 discard를 비교하고, 성능은 사용자의 같은 장면 profiler로 별도 확인한다. 카메라 revision 변경과 실제 인스턴스 GPU payload 변경도 구분한다. culling·hysteresis를 유지해도 동일 payload의 WRITE_DISCARD는 생략할 수 있다. [베른 FPS 결과](09-11/2026-09-11_BERN_FRAME_TIME_RECOVERY_IMPLEMENTATION_RESULT.md)에 구현과 미측정 경계를 기록한다.

RNM variant를 새 catalog shard로 분할하면 Imported 기준 배치도 같은 stable ID·transform·visibility를 유지한 채 catalog 소유 shard와 맞춰야 한다. publisher의 shard 내부 asset 참조 검사를 완화해서 숨기지 않는다. placement 하나에 opaque·water slot이 섞이면 asset 단위 RNM 개수만으로 모든 slot의 조명 소비를 판단할 수 없다. material slot과 source placement를 함께 join해 미소비 분모를 구분한다.

### 발탄 재질과 presentation admission의 연결

발탄 본체·갑옷·도끼·유령의 5개 모델 11개 slot은 `BossCatalog.modelMaterialOverrides`를
`CValtanPresentationAssetService`의 실제 `MODEL_LOAD_DESC`에 전달해야 한다. 원본 재질 입력을
catalog에 작성하는 것만으로 plain-path 모델 로더에 적용되지는 않는다. 본체와 장비는 기존
source program21, 유령은 cloud/rim/opacity를 처리하는 program84를 사용한다. 유령 opacity는
현재 deferred의 ordered coverage에 연결되며 원작의 sorted translucency와 scene hemisphere
입력 전체를 복원한 것으로 표현하지 않는다.

program 번호는 character 함수 목록뿐 아니라 CModel admission과 CMaterial의 map-forward·water
예약 범위까지 대조한다. 유령의 초기38은 map 계열과 충돌해 실제 bind에서 실패했고 비충돌84로
옮겼다. CModel create/clone과 prototype 해제 뒤의 실제 shader bind를 확인해야 이 차이를 잡을 수 있다.

같은 BossCatalog를 읽는 발탄 presentation generation reader도 optional material override를
인정해야 한다. Python publisher가 성공해도 별도 C++ root whitelist가 이를 거부하면
`CClientReplication`이 화면 발탄의 `Apply_NetworkState`를 건너뛴다. Server 전투가 진행하면서
중앙 발탄이 idle에 남는 경우, 보스가 두 개라고 가정하기 전에 admission 실패를 확인한다.
optional array 승인과 unknown/type 거부를 함께 검증하고 변경 뒤 Server generation을
정본 publisher로 동기화한다. 실행 증거와 사용자 시각 확인은 해당 복구 RESULT에서 구분한다.

local Composition Preview clone은 Server 발탄과 다른 presentation 객체다. Server Play를 제출해도
기존 clone을 해제하지 않으면 idle 복제본이 화면에 남는다. submit 성공 뒤 기존 PreviewPanel을
해제하고 Animation Tool의 자동 재생성도 함께 억제한다. 명시적 local staging 또는 Level 변경에서
다시 허용하며 submit 실패와 다른 asset preview는 보존한다. live boss의 visibility를 임의 전환해
이 문제를 가리지 않는다.

V2 binding의 typed `LocalTransform`을 편집에 노출하면 runtime도 full scale·rotation·translation을
소비해야 한다. 이전 convenience yaw/offset만 사용하면 X/Z 회전과 크기가 저장돼도 보이지 않는다.
일반 mesh는 binding S*R*T를 pivot에 한 번 적용하고, pivot basis를 다시 만드는 billboard/particle은
기존 `Set_OccurrenceScale`에도 binding 크기를 전달한다. Group child 자체의 크기는 기존 Params가
소유하므로 parent·binding·child의 각 축척을 중복하지 않는다.

발탄 중앙 circlefloor의 두 slot은 mesh default의 rain MIC 대신 해당 level component의 MIC를
소비한다. 기존 stone과 engine-equivalent static parameter set이 같아 기존 overlay family7을
재사용하며, 원본 배치의 UV1·component COLOR0와 RNM을 같은 variant에 연결했다. 두 MIC의
brightness·normal·overlay scalar는 각각 유지한다. 이것을 generic 직접 bind를 사용하는 Deploy
A/B 석판·난간과 다른 shader를 쓰는 하층 바닥의 복원 완료로 확대하지 않는다.


### 쿠크 1관문 V1 패턴과 원본 본·재질 연결

새 폭죽/무지개 native 22개는 기존 Kouku carrier의 2304~2495 dispatch에 연결했다.
2360 MacroUV는 실제 occurrence 중심과 원본 world radius 2m를 View/Projection에 전달한다.
최신 CSO의 독립 WARP 69회는 오류·비유한 값이 없고 21개 프로그램에서 RGB를 확인했다.
2409의 RGB 0은 원본 시선각/mask와 Dynamic 입력을 추가로 구분해야 하며 사용자 화면 판정은 남아 있다.

무지개 격자는 Action4219916 stage002 → SkillEffect421991412~421991421 → fixed-area
Projectile421991401~421991405 → FireDecal_T/Fire_BigEarthWave의 원본 연결을 사용한다.
342개 element는 45도·135도 배치, 양방향 source Rotator, 줄마다 다른 Timer, 6×6 연기 atlas,
squarecross 불꽃 mesh와 원본 파라미터를 포함한다. 개별 warning/wave/light와 전체 grid를
동일 V1 경로로 저장한다. SCENE03A의 festival과 fireworks는 별도 Matinee0/7의 원본 Toggle과
상대 배치를 사용하고 이동은 Cascade 모듈에서 가져온다. 신규 재질22개 VF 프로그램은 기존
Kouku native carrier의2360..2371/2400..2409에 연결한다. 기존47개 프로그램은 유지한다.

Effect Tool 독립 재생의 임시 플레이어 root와 Action Workbench/Sequencer Benchmark에
Append한 occurrence의 MAP/BOSS/WORLD는 서로 다른 소유다. MAP은 identity basis와 저장된
미터 위치를 쓰며 follow·bone·world 참조를 거부한다. 데이터·빌드·수치 검사와 사용자 화면
판정은 [무지개·폭죽 결과](09-11/2026-09-11_KOUKU_PLAYER_ANCHOR_RAINBOW_FIREWORKS_IMPLEMENTATION_RESULT.md)에서 구분한다.

독립 Play All의 scene player는 실제 복제 local player의 commit과 수명을 따라야 한다.
`CClientReplication`의 spawn/class 교체 성공에서 AnimationTargetService를 연결하고, exact local
despawn/reset/destructor에서 해제한다. 실패한 교체와 remote player는 기존 target을 보존하고
종료 시 이미 제거된 Layer를 다시 조작하지 않는다. 카메라 target 연결만으로 Effect anchor가 준비됐다고 판단하지 않는다.
`Enter an arena with a scene player before Play All`은 V1 factory 이전 거절이며 shader 표시 실패와 별개다.
festival은 독립 0초 시작 문서와 시퀀스용 33.40897초 시작 문서를 구분하고, 저장된 MAP 위치와 root attachment를 보존한다.
천막·관람차 주변의 다색 방사형 폭죽은 별도 `intro.fireworks.full.restore`의 Matinee7 계열이다.
현재 저장 시퀀스의 P4는 authored festival을 소비하고 fireworks occurrence는 없다. 원본 Matinee7의
무대와 P1 팝업북의 위치·소품이 달라 원본 MAP 좌표를 그대로 P1에 추가할 수 있다고 간주하지 않는다.
원본 정렬값과 이미지 대조 근거는 위 결과 문서 G06에 기록하며 사용자 화면 확인과 구분한다.

무대 앞 곡선형 금빛 축포는 SCENE03A Matinee0의 `Par_Q_Trail_01`과 actor Move를 함께 소비한다.
그룹 FName의 대소문자 비구분 연결과 원본 key/time/tangent를 보존하며, 누락된 Move를 정지 node로 통과시키지 않는다.
선택 trail의 Required는 원본 CDO loop 0과 Matinee Toggle 수명을 사용한다. 이동 거리 기반
mesh/sprite 방출·world-space ribbon 이력·VelocityInheritParent를 같이 대조한다. source track의
속도는 해당 emitter world origin에서 계산해 birth basis로 옮기며 기존 root-only 경로는 유지한다.
독립 `intro.gold-trails.full.restore`의 4경로/24행과 기존 source/authored portal-arrival 문서가
이 복구를 소비하고, 현재 P4는 authored portal-arrival의 저장된 시각·MAP 위치를 유지한다.
원본 camera cut과 같은 시각의 위치 도약은 평활하지 않는다. 원본 바닥 기준과 수치 검사·제품 빌드·사용자 화면
판정의 구분은 위 결과 문서 G07을 따른다. 새 shader·Resources는 없다.

Composition Resource의 단독 Preview는 선택 Pattern의 actor/gate/boss placement를 기준으로
기존 CNpc/CModel Preview를 준비한다. 오망성처럼 SourceModelPreview가 없는 그룹도 이 선택을
전달하며 actor를 플레이어 위치로 덮지 않는다. Timeline의 MAP 좌표는 별도 절대 월드 위치다.
Anchor 변경은 TRS와 함께 stage하여 Dirty·Save·Preview·Play 모두 같은 값을 소비한다.
MAP 원점0을 각 관문 중앙으로 해석하지 않는다. 기존 root-history의 과거 누락 오류를 숨기지
않으며 편집본과 실제 provider, 저장 좌표를 분리해 진단한다. 범위는 [패턴 그룹 결과 G10–G11](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md)에 기록한다.

쿠크 1관문 `4219877` 내려치기 C와 `4219801` 불뿜기는 Effect Tool V1 All Effects의
KoukuSaydon authored 문서와 Action Workbench Saydon/1관문 Composition resource로 연결한다.
`.restore` 접미사만으로 player skill 복원을 선택하지 않는다. Open Editor는 CPU 문서를 열고,
Play/Solo는 현재 draft의 source attachment를 검사하고, 실제 source bone이 필요한 경우에만
effect ID에 대응하는 유일 Composition pattern과 CNpc/CModel preview를 typed 경계로 선택한다.
root/camera-only 문서는 Composition 모델 없이 Play 시점의 실제 플레이어 yaw·위치를 기준으로
재생한다. 본은 기존 inverse(ownerRoot) × previewRoot 경로로 옮긴다. Valtan 전용 standalone load
intent를 쿠크 모델 준비에 재사용하지 않는다. All Effects 목록의 Play All도 같은 경계를 사용한다. 저장하지 않은 문서 전환은 기존
guard를 따르며, 문서는 로드됐지만 preview 준비만 실패했다면 로드 성공과 재생 실패를 구분해
이유를 표시하고 전환 창을 닫는다.
같은 문서의 Play All도 모델과 FX의 시계를 함께0으로 초기화한다. 첫 Solo에서 모델을 아직 만들지
않았다면 typed 모델 준비 뒤 root를 조회하며, 선택 검증 실패는 기존 항목을 보존한다.

원본 RPCT05 Action notify와 embedded animation은 별도 입력이다. 원본 first LOD emitter
53개는 복수 소켓의 독립 발생을 펼치면 runtime73개가 된다. 파생 `sourceAnchorAnimations`는
실제 source clip offset/play window/loop를 보존하며, 과거 본은 현재 pose를 복제하지 않고 기존
CModel pose sampler로 평가한다. 설치된 FBX 골격의100배 root basis와 body preScale0.017을
함께 반영한 본 배율1.7을 그대로 사용한다. 추가100배 역보정을 하지 않는다. 제자리 수동
패턴의 frozen actor root와 움직이는 source bone timeline은 구분한다. notify가 bone-follow여도
Required의 bUseLocalSpace가 false인 입자는 출생 뒤 월드에 남아야 한다. CDO까지 해석한 뒤
생략된 false를 legacy importer의 미확인 true fallback으로 바꾸지 않는다.

RPCT05 몸체에 붙는 WP05 지팡이는 원본 socket이 identity여도 설치 geometry basis가 다르다.
G1/G3 catalog의 weapon preScale0.01과 X축 preRotation -90도를 함께 소비한다. 원점·반경만
맞는지 확인하면 지팡이 머리의90도 roll 차이를 놓치므로 원본 PSK·설치 WModel의 전체 정점과
세 축을 같은 body bone에 합성해 비교한다. 해당 모델의 실제 수치와 연결 범위는
[패턴 그룹 결과 G11](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md)에 둔다.

3관3방향 브레스 생성기는 같은 RPCT05의4개 사용 socket에 UE→PSK mirror와 설치 bind 차이를
명시적으로 투영한다. source bone 이름이 같다는 이유로 UE local TRS를 그대로 복사하지 않는다.
원본 bone·FRotator·scale을 검증하고 실제 설치 geometry/bind/애니메이션 witness로 확인한 범위만
사용한다. FX03는 원본의 spine2 proxy를 유지하며 입 정점에 맞추려는 임의 offset을 추가하지 않는다.
전역 extractor나 기존 다른 Effect에 일괄 적용한 변경이 아니므로 다른 occurrence는 별도로 확인한다.

재질은 기존 native carrier의 2304..2341 범위에37원본재질·38VF프로그램을 연결한다. 텍스처0인
analytic PS도 원본 DXBC 선언과 native uniform binding으로 검증한다. source expression에 남은
미사용 texture 수와 shader가 실제 sample하는 수를 혼동하지 않는다. Disto05는 색상 BasePass와
별도 distortion accumulation PS를 함께 소비해야 한다. signed positive/negative 채널과 프로젝트
RT1의 signed UV offset 사이 변환은 명시적인 렌더 adapter다.

차원술사 V localcrack native66에도 별도 원본 distortion PS를 연결했다. 기본 색 패스가
맞아도 distortion MRT가0이면 유리의 굴절은 빠진다. serialized uniform 배열의 후보가
겹치면 같은 shader class의 검증된 object와 배열 시작 위치를 대조하고, particle dynamic
입력·view 방향·scene-depth 단위를 함께 확인한다. distortion discard를 기본 색의 clip으로
확장하지 않는다. 원본 PS와 WARP 108조건 수치 대조 및 소스 FXC는 통과했으며, 제품 재빌드와
사용자 화면 확인은 [T/V 결과 G06](09-15/2026-09-15_DIMENSIONMASTER_T_UNLIT_V_RECTANGLE_IMPLEMENTATION_RESULT.md#g06-유리-재질의-별도-원본-distortion-pass-연결)의 별도 상태를 따른다.

원본 궤적은 baked edge 좌표만으로 닫히지 않는다. point lifetime, source color/alpha 곡선과
dynamic parameter를 기존 trail vertex에 전달한다. AccelerationOverLifetime은 원본
`acceloverlife` 분포를 normalized particle age에서 평가한다. 데칼은 이미 World에 size/depth가
구워진 경우 shader localY가 정규화 좌표이므로, 원본 near/far/span/center로 source cm를 복구한다.
near/far와 원본 roll/yaw-only 상태도 기존 ground projector로 전달한다. LOD의 Lifetime·Spawn·Size·
Velocity가 외부 UPK object를 참조할 수 있으므로 선택 PS package 내부만 읽어서는 모듈 목록이
닫히지 않는다. 생성0 또는 C++ cardinality 거절은 원본의 모듈 부재로 단정하지 않고 import된
전체 object reference를 먼저 해석한다. 누락을 임의 burst로 채우거나 parser 검증을 완화하지 않는다.

authored v15의 baked AnimationTrail은 `runtimeCarrier`와 history뿐 아니라 활성 SourcePresentation의
profile·source identity를 요구한다. Catalog의 document-owned projection 생성과 immutable 준비,
Playback의 projection stage까지 연결해야 한다. 일반 `Stage_Document`의 성공이나 centerline Points
생성만으로 원본 잔상 기하가 연결됐다고 판정하지 않고, 실제 `EdgePairs` 생성과 point tail을 확인한다.
Effect Tool의 authoring occurrence도 현재 draft의 immutable 사본으로 같은 projection을 준비하고
`Stage_PrevalidatedVisualProgramDocument`를 사용해야 한다. Element Solo의 선택 사본에서는
제외된 sibling의 baked history만 제거하고, 선택 Element가 참조하는 history의 필수 검증은 유지한다.

복원 문서 Solo와 Shift/Ctrl 선택의 Play Group은 이 authoring occurrence와 Sequencer 시계를
같이 사용한다. Solo 뒤 Sequencer가 paused=true로 남아 Play를 다시 요구하던 경로를 제거한다.
Play Group은 임시 row의 선택ID와 반복 여부를 소유하고 선택 element의 min start~max end를
반복한다. provider·model cue의 긴 수명은 반복 구간에 포함하지 않으며 원본 element의 시각과
anchor 의존성은 유지한다. draft 수정 후 Refresh에서도 같은 함수로 선택 구간을 다시 계산한다.
저장된 sequence나 그 Loop 설정에 임시 group 상태를 기록하지 않는다.

선택 class cue와 Deploy prototype은 Kouku Loader에서 준비한다. Level activation에서 같은
prototype을 재생성하거나 입장 뒤 프레임별 cue 준비로 밀어내지 않는다. 코드의 소유 스레드 이동과
실제 FPS 개선 측정은 구분한다. Source 관계·실제 자원 경로·자동 검증·사용자 화면 확인은
[두 패턴 결과](09-11/2026-09-11_KOUKU_GATE1_TWO_PATTERN_FULL_RESTORE_IMPLEMENTATION_RESULT.md)와
[원본 복구 경계](09-11/2026-09-11_KOUKU_GATE1_FULL_RESTORE_SOURCE_RESULT.md)에 기록한다.

### 거미카운터의 원본 바닥·리본 연결

현재 Composition의 `KAKULSAYDON_G1_PATTERN_15`는 저장ID에 G1이 있지만 실제 gate는 GATE2,
actor는 MN_RPCZ_00이며 원본 action4219776을 소비한다. gate나 actor를 ID 문자열로 추정하지
않는다. 기존 `rpcz00_att_battle_6_02` 준비 반복과 `6_03` 세 돌진에 원본 notify를 연결한다.
stage1은 눈빛·먼지5요소, stage2는 검정/빨강 바닥·돌진9요소이며 기존 V1 authoring 경로를 쓴다.

새 native2342~2350 중2349의 원본 PS는 월드 위치에서 emitter-local 좌표를 복구하고 원본
alpha를 소비한다. generator가 이 입력을0으로 채우면 검정/빨강 바닥이 모두 투명해진다.
기존 차원술사341/361의 SourceEmitterWorld 역행렬 계산을 재사용하고, 원본 VS/PS의 대응 lane을
함께 연결한다. 셰이더가 컴파일된다는 사실만으로 world-position/alpha ABI를 닫은 것으로
기록하지 않는다. 기존 다른 native 함수의 동작은 바꾸지 않는다.

2346은 기존 v15 bounded Cascade Ribbon을 사용한다. native material admission도 TRAIL과
CASCADE_RIBBON_V1을 정확히 허용해야 하며, Playback과 renderer가 원본 width/RGBA/dynamic을
모두 전달한다. authored white/선형 fade로 대체하면 source 재질식만 복구해도 색을 잃는다.
SpawnPerUnit emitter는 저속·정지 입력에서0개가 정상일 수 있으므로 Rate/Burst0만으로 비활성으로
분류하거나 burst를 보충하지 않는다. 같은 입력을 실제 이동 단위 이상 움직여 생성 여부를 확인한다.

원본12 emitter/두 눈 소켓 전개14요소, 재사용 Resources18개, CPU 및 shader 검증과 수동 경계는
[거미카운터 결과](09-11/2026-09-11_KOUKU_SPIDER_COUNTER_SOURCE_EFFECT_IMPLEMENTATION_RESULT.md)에 둔다.
Solo/Play Group의 실제 구현·검증은
[선택 재생 결과](09-11/2026-09-11_EFFECT_TOOL_SOLO_AND_SELECTED_GROUP_PLAYBACK_RESULT.md)를 따른다.

### 쿠크 팝업북 연출 배치와 전투 아레나

움직이는 팝업북 세트와 고정 전투 아레나는 별도 placement 집합이다. 같은 모델 부품을 공유해도
asset variant와 배치 조명이 다를 수 있으므로, 연출용이라는 이유만으로 재질 미복구로 분류하지 않는다.
맵 펼침 136개 배치의 37개 모델과 실제 mesh가 사용하는 40개 재질 slot은 현재 복구 재질을 소비한다.
전투 아레나의 구운 RNM 조명을 움직이는 부품에 그대로 복사하지 않는다. 정상 펼침의 MAP 모션은
4,507ms 키를 유지하면서 연출 occurrence 수명인 37,800ms까지 마지막 배치를 유지하고, 책은
동일 `evt2_book02` template의 HOLD instance를 사용한다. standing arena는 MAP occurrence가
끝난 뒤 복귀한다. Preview 중단과 실패 때는 차용 전 표시 상태를 복구한다.

F1과 mesh가 같은 6개 placement는 기존 F1 material asset을 재사용하고 Transform은 보존한다.
책과 F1의 Z 차이 204.8m는 효과 위치에 일괄 더하지 않는다. 기존 F1 local light 32개를 책 공간에
옮긴 문서 사본과 전용 scene profile을 해당 HOLD cue 수명 동안 사용한다. source provider는
수정하지 않으며 WORLD 샘플 이후 한 provider만 제출한다. Stop에서 이미 등록한 provider를
Clear하면 같은 프레임의 렌더가 실패할 수 있으므로 소유 포인터만 해제한다. 이 연결은 재질·조명
입력의 재사용이며 F1의 baked 간접광까지 픽셀 단위로 같다는 뜻이 아니다. 적용과 수치 검증은
[팝업북·피날레 결과 G08](09-12/2026-09-12_KOUKU_SOURCE_SEQUENCE_RESTORE_RESULT.md)에 기록한다.

팝업북의 stable Deploy ID `DEPLOY_BOSS_MN_RPCT_00`은 저장 참조를 유지하면서 실제 1관문
`Character/KoukuSaton/MN_RPCT_05/MN_RPCT_05.wmodel`을 연결한다. 기존 `CActorCatalog`의
모델 준비와 `CModel`의 source character 재질 경로가 같은 BossCatalog override를 사용한다.
두 RPCT 모델의 clip 이름 호환은 skeleton·animation bytes의 동일성을 뜻하지 않는다. 교체 모델의
자체 clip으로 기존 track을 재생하며, 원본 컷신 배우 캐스팅을 그대로 재현했다는 의미로 확대하지 않는다.

공용 native material dispatcher는 같은 prefix를 공유하는 family의 실제 소유자를 구분한다.
`source.map.translucent-tiled.v1`, `source.map.translucent-reflection.v1`,
`source.map.translucent-bump.v1`은 Forward program 34/35/36이며 숫자형 44~63 Translucent
handler보다 먼저 명시 분기한다. handler를 직접 실행한 shader 검증만으로 공용 catalog admission을
보증하지 않는다. 실제 `CMapAssetCatalog::Load_Area`도 확인하고, 실패 시 기존 문서를 보존한 채
Loader의 session diagnostic에 하위 parser 오류를 전달한다.


### 쿠크 컷신의 생성형 BG8 맵과 비구운 광원

2관문 진입의 배경 소품130개는 부모 Matinee의 Move를 포함한14개 WORLD로 연결한다. 부모의 저장 Transform만 사용하면 카드80장과 받침·다리 판50장이 정지 자세로 남는다. 현재 우리 시퀀스에 배경14묶음만 더해 P3 WORLD31개로 구성하고 카메라5박스와 기존 연출은 보존했다. 손에 붙는 미니 세트35개는 미반영이다. 기존 BG8 모델·재질을 사용하며 새 shader나 Resources를 만들지 않았다. 데이터 검증과 사용자 화면 판단은 [G16 결과](09-13/2026-09-13_KOUKU_G12_CUTSCENES_CAMERA_MAP_RESULT.md)에서 구분한다.

새로 구운 컷신 actor는 World Object의 materialSourceModelAssetId로 기존 ActorCatalog의 승인 재질을 가져온다. 움직이는 맵은 mapMaterialBindings의 대상 materialName을 동일 Area의 sourceAssetId/sourceMaterialName에 연결하고 정적 RNM/static shadow 입력을 제거한다. CModel의 기존 실제 slot 검증을 통과한 재질만 생성된다. Shader_VtxAnimMeshBinary의 BG8은 공유 Shader_MapMaterialSurface 평가와 기존 Deferred GBuffer를 사용하며 표면 발광 시간은 Sequence localMs다.

쿠크 광원119개 중 원래 SOURCE_CHARACTER였던84개는 UNBAKED 수광을 사용한다. 기존 baked bit가 있는 맵에는 다시 비추지 않고 비구운 맵과 actor를 비춘다. Shader 수광6조합 WARP 수치검사와 parser 실패보존 검사를 통과했다. SCENE04A 이동광원3개는 기존 V1 typed point-light와 cubic distribution으로 연결됐으며 원본645시점 대조의 최대 위치오차0.000435114m, radiance오차0.00000190735다. 채널별 UE3 receiver와 dynamic shadow map, 최종 화면 fidelity는 이 수치검사의 승인범위가 아니다.

원본의 부착 소품과 투명 material override, 카메라 급격한 이동을 별도로 회수해야 한다. 전체98 InterpGroup 개수나 생성 모델 수를 전체 pixel 복원 증거로 쓰지 않는다. 설치·검증·미연결 항목은 [진입 컷신 결과](09-11/2026-09-11_KOUKU_GATE_INTRO_COMPOSITION_RESTORE_RESULT.md)에 구분한다.


### v15 supplemental-only carrier와 실제 renderer 준비

`ADAPTER_PACKET_V1` projection은 LocalDecal material adapter 없이 document-owned baked AnimationTrail/Cascade Ribbon supplemental만 포함할 수 있다. renderer 준비는 유효한 supplemental의 존재와 typed adapter 수 일치를 구분해야 하며, LocalDecal 수가 0이라는 이유로 전체 occurrence를 거부하지 않는다. P29 내려찍기와 P15 거미카운터 stage2가 이 잘못된 조건으로 clone 이전에 차단됐다. 수정 후 두 문서의 실제 Product WARP resource prepare·renderer attach·EffectObject clone이 성공했다. CPU frame count나 단일 native shader draw는 이 통합 준비 경계의 검증을 대신하지 않는다. 아레나 화면은 사용자 판정이며 상세 증거는 [내려찍기 결과](09-11/2026-09-11_KOUKU_GATE1_TWO_PATTERN_FULL_RESTORE_IMPLEMENTATION_RESULT.md)와 [거미카운터 결과](09-11/2026-09-11_KOUKU_SPIDER_COUNTER_SOURCE_EFFECT_IMPLEMENTATION_RESULT.md)를 따른다.

### 독립 World 클릭·이동 표시

`effect.world.mouse_click`은 `FX_BS_03.mark.par_b_picking_01`의 mesh1/decal2/sprite1,
`effect.world.move_destination`은 `par_i_movetrack_01`의 아래 화살표mesh1/ring·glow·point sprite11을
기존 V1 문서와 carrier로 연결한다. Resources는 `Effect/World`에 필요한 WModel2/DDS11만 둔다.
F1 Effect Tool V1의 World 목록과 Saved Effects의 World category가 실제 소비자다.

피킹의 수축처럼 보이는 동작은 정점 WPO가 아니라 signed particle alpha(-1~+1)를 사용하는
원본 UV 마스크 이동이다. 이동 arrow의 bobbing은 VelocityOverLife이며, 원본 TypeData 회전과
locked-axis camera-facing은 native instanced/scalar 양쪽에서 유지한다. 모든 alpha lane을 opacity로
취급하거나, PS만 복구하고 TypeData·camera-facing을 생략하지 않는다.

World preview는 Play 시 scene-player root를 빌리고 저장 sequence의 모델·Loop를 바꾸지 않는다.
클릭1.2초/이동7초 재생창을 기존 occurrence에서 반복한다. source의9개 반복 emitter와3개 초기pulse,
bounded particle tail과 Tool preview window를 구분한다. 실제 gameplay 우클릭은 최초 물리 press의
이동 송신이 성공할 때만 mouse_click을 한 번 생성한다. hold의 목적지 재전송은 표식을 재시작하지
않으며 raw edge는 UI·잡힘·타기팅 분기의 early return 전에도 관찰한다. NPC 접근의 명시 클릭은
기존 표시를 유지한다. 쿠크 move_destination은 Level이 입구 jump.1~3/paper.1~2의 실제 trigger
중심을 읽어 별도 handle로 반복하고, 일회용 paper 실행과 Level 종료 때 정리한다.

mouse_click LocalDecal의 native alpha는 투명도가 아니라 환형 마스크의 반경 입력이다.
Life 배율은 수명과 over-life 진행만 바꾸므로 별도 burst 지연이나 최초 마스크 공백을 없애지 않는다.
현재 두 decal의 burst는0이고 원본 curve/shader를 보존한 요소별 color.multiply.w(.4/.5)로
최초 링 위치를 조정한다. CPU birth와 texture/mask 교차 검증은 사용자 화면 확인과 구분한다.
원본 회수는 [World 표시 결과](09-11/2026-09-11_WORLD_MARKERS_AND_PROFILER_ANALYSIS_RESULT.md),
현재 입력·트리거 연결과 검증은 [재생과 표식 수정](09-12/2026-09-12_KOUKU_PLAYBACK_AND_WORLD_MARKER_IMPLEMENTATION_RESULT.md)을 따른다.

### Kouku native cohort의 모델 include 및 pass 상수 경계

원본 이펙트 색상 함수와 왜곡 companion은 모두 `ARTIST_NATIVE_MODEL_ONLY`에서 제외한다.
`Shader_VtxAnimMeshBinary`는 동일 native 헤더의 실제 skeletal 함수만 사용하므로 일반
effect 함수의 scene-depth 선언을 가져오기 위해 모델의 include 순서를 바꾸지 않는다.

원본 CB2 pass 상수는 shader별 선언 크기를 유지한다. 확인된 5행 lit 프로그램의
diffuse/specular override는 material 값을 보존하는 중립값을, 7행 pixel checker/aspect
프로그램은 현재 render target 크기를 공급한다. material CB0 closure나 PS 생성 성공만으로
carrier 컴파일 성공을 대신하지 않는다. [전체 이펙트 작업 결과](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md)에 빌드 증거와 미완 범위를 구분한다.

쿠크 왜곡 PS `2d8c822c88934149bfa9587fd772c1e8`의 CB1[4]는 원본 VS 세 종류가 TEXCOORD5에 전달한 world position을 투영한다. 대응 왜곡 12함수에 source projection을 선언하고 clip position의 재투영을 제거했다. mesh/particle의 기존 world·projection 입력을 사용하며 ribbon native2697은 Trail 보간 및 renderer 바인딩까지 연결한다. 원본 native3338의 texture index9는 실제 열 번째 SRV이므로 renderer·screen-post 준비/바인딩, 공통·모델 선언과 생성 helper를 10개로 맞췄다. 실제 FXC·최소 CPP 검증과 제품 반영 여부는 [후속 결과](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md)에 구분한다.


## Object 그룹과 인형·외곽불의 후속 연결

World Object의 count·spacing·Delay는 Object/Motion authoring이 소유하며 Effect의 별도 생성 runtime으로 복제하지 않는다. Box Detail은 정확한 object ID와 motion instance ID를 Object Tool로 보낸다. Save는 WORLD Collider/Logic 참조와 함께 처리하고 모호한 공유 참조는 기존 draft를 유지한다. 카드 6장과 조커 1장의 준비 풀은 동일 document/resource 범위에 한해 재사용한다. [그룹·사전 준비 결과](09-12/2026-09-12_WORLD_OBJECT_GROUP_EDIT_AND_PREWARM_RESULT.md).

Object의 불뿜기 유지 시간을 늘릴 때 전체 Lifetime, Required의 배출 반복, 입자 잔여 수명과 모델
source 시간을 구분한다. 원본 CDO에서 loop 0을 확인한 sustained 요소만 유한 emission window 안에서
반복시키고 준비·종료 notify는 원래 속도로 유지한다. animationTracks의 optional sourceStartMs는
timeline start와 독립된 원본 시작 시각이며 Object 자세·Effect 본 이력·Deploy가 함께 소비한다.
기본 0의 기존 loop 산술은 유지하고 source 범위 초과는 기존 preview 교체 전에 거부한다.
인형 Object용 파생 Effect는 기존 재질·입자 속도·수명·양쪽 소켓과 원본 공유 Effect를 보존한다.
실제 시간·검증·Product 및 사용자 화면 확인 상태는 [인형 15초 결과 G12](09-13/2026-09-13_KOUKU_DOLL_VARIANTS_OBJECT_SEQUENCE_EFFECT_IMPLEMENTATION_RESULT.md#g12-09-14-object-인형의-실제-불뿜기-15초)를 따른다.

괴기스러운 인형 MN_CDMD_00은 기존 weight·30Hz 교정과 별개로 자식 본 회전 conjugate가 빠져 있었다. 원본 30clip/103,056 quaternion 표본을 대조해 WANM 회전만 고친 후보를 만들었고 geometry/skeleton/material은 byte 동일하다. 외곽불 D/E/F는 WINT1.2 안의 기존 WMA2 slot0에 specular 텍스처가 emissive로 들어간 것을 확인했다. 두 교정 모두 후보 CPU 검증 단계이며 현재 Resources 설치와 사용자 시각 판정은 완료하지 않았다. [상세 근거](09-12/2026-09-12_KOUKU_DOLL_FIRE_REPAIR_RESULT.md).


### 쿠크 재질·상시 표식의 렌더 비용 경계

쿠크 기본 scene의 shadow는 꺼져 있으며 shadow enqueue counter는 actual draw가 아니다. source BG family8은 단순 Character Select shadow family0–5와 구분한다. native 재질 최적화에서도 raw UV, RNM, static shadow, source texture override와 공통 presentation opacity dither를 보존한다. ARTIST particle의 공통 binder 생략은 실제 선택된 shader/carrier/profile에 한정하고 native source packet·clamp·scene 입력과 mesh carrier는 유지한다. 결과 동등성과 실제 FPS는 [맵·캐릭터 성능 결과](09-12/2026-09-12_MAP_CHARACTER_RENDER_PERFORMANCE_RESULT.md)의 후속 구간을 따른다.

### 조명·Alt+V의 반복 계산 재사용 경계

Deferred 조명 shader의 정본은 Engine/Bin/ShaderFiles이며 Product가 EngineSDK를 거쳐 Client로 배포한다. Client 사본만 수정하면 다음 빌드에서 사라진다. 조명 instance는 scene→transient 및 receiver 필터 후 순서를 보존하고 같은 type의 연속 구간만 묶는다. FP16의 개별 blend를 shader 안의 합산으로 바꾸거나 종류별 sorting을 하지 않는다. directional/static shadow는 기존 첫 scene directional 규칙을 유지한다. 400개/112byte record와 pass22–27의 CPU/HLSL 계약을 함께 변경한다.

sprite 입력 재사용은 같은 draw의 카메라와 같은 emitter/profile에 한정한다. ModelCue pose 재사용은 private clone의 동일 explicit seek일 때만 가능하며 world/cue/root transform은 매번 갱신한다. compact animation cursor는 CAnimation clone이 소유하고 공유 CChannel 키를 변경하지 않는다. NPC native binder 생략은 실제 VtxAnimMeshBinary의 base pass caller에만 허용한다. 공유 Effect 때문에 객체별 마지막 uniform 값만으로 bind를 생략하지 않는다. 수치 동등성의 예외와 사용자 FPS 미확인은 [성능 결과 G10–G11](09-12/2026-09-12_MAP_CHARACTER_RENDER_PERFORMANCE_RESULT.md)을 따른다.

### 쿠크 독립 패턴 그룹의 원본·저작 경계

쇼타임의 고정·추적 조준점은 각각4개/3개 source Sprite와 native2484/2485가 화살표·띠 DDS3종을 radial UV로 합성한다. fixed의 EPAL_Z와 tracking의 CAMERA_SQUARE를 구분한다. source native additive2484는 RGB에 opacity가 이미 포함되고 A=0이며, distortion 동반 dispatch가 일반 native의 coverage=1 처리를 빠뜨려 SrcAlpha/One 합성에서 본체가0이 됐다. 설치 교정은2484 scene alpha adapter에 한정하고2485 translucent·원본PS·Distortion 출력을 보존한다. 생성기도 일반/early-return의 source blend 계약을 일치시킨다. CPU count·resource stage만으로 이 결함을 배제할 수 없으며, 실제 source DDS/ABI와 compiled shader의 최종RGB 검사가 필요하다. Product 반영과 사용자 화면 확인 상태는 [쇼타임 조준점 결과](09-13/2026-09-13_KOUKU_SHOWTIME_TARGET_BLEND_RESULT.md)를 따른다.

쿠크 원형 native3600과 도넛 native3601은 한 draw 안에서 고정 경계와 채움을 함께 계산한다. 발탄의 수동 경계2+성장1 구성을 그대로 복제하면 원본 경계가 중복 합성되므로 element 개수로 동등성을 판단하지 않는다. 원형은 `inner`를0→1, 도넛은 고정 `thickness`(내경/외경)→1로 보간하고 projector 크기·중심·깊이는 고정한다. 기존 `SourceTransformTrack.materialParameterTracks`가 native의 이름/타입/row/lane을 검증한 뒤 source 시계로 이 값만 갱신한다. 원본 lifetime·alphaScaleKeys는 유지하고 fade-out 시작까지 확장을 마친 뒤1을 유지한다. 원본 재질 수식과 범위는 재사용하지만 회수되지 않은 원본 엔진의 시간 곡선을 복원했다고 주장하지 않는다. 기존 neutral engine texture 대체 입력도 유지한다. 이전 전체 XZ scale 확장본과 실제 패턴의 기본 warning/impact를 함께 고치고 생성기에서 재발을 막는다. 적용·검증은 [도넛·원형 후속 G10](09-13/2026-09-13_KOUKU_PATTERN_RADIAL_MOTION_RESULT.md#g10-원형도넛의-inner-시간-보간)을 따른다. Mesh Particle 전용 Motion Duration/Ring Fill을 decal에 적용하지 않는다.

서커스공은 원본 큰 공→작은 공의2자식 callback과6세대 크기, LocationDirect 낙하·반동을 재사용한다. nested CDO의 StartSize/ScaleFactor lookup 상속 누락을 후보에서 복구했고 원본 leaf는 보존했다. 최대 수명에 맞춘1.5초 분열 시계와 방사 방향은 독립 편집 정책이며 Server targeting·충돌·원본 종료 우선순위의 복원이 아니다. 세갈래 전격도 source enabled3발과 native 외형·속도·거리, 편집용 방향을 구분한다.

작은 오망성은 drawing8 뒤의 추가 gray floor5를 제거하고 source star_shot7을 같은 문서에 연결한다. 원본 action의 disabled star system을 사용자가 요청한 독립 연출로 조합한 것이므로 원본 enabled cue와 구분한다. 폭발의 source7개·모듈66개·리소스가 있어도 native2811 mesh/2812 sprite의 distortion dispatch가 A=0을 SrcAlpha/One에 넘겨 색만 사라질 수 있었다. 두 additive program의 scene coverage를1로 연결하며 원본 RGB fade·왜곡과 drawing8은 보존한다. 동일 source program을 쓰는8문서40요소의 계약이며 전체 native alpha를 바꾸지 않는다. 손 궤적은 별도 활성 notify의 Cascade Ribbon2+sprite1이며 실제 startcontrol/b_wp_1과 CModel clip을 사용한다. 재생성기는 gray floor를 되살리지 않는다. 설치 문서·수치·사용자 화면 미확인은 [도넛·공·오망성 결과](09-13/2026-09-13_KOUKU_PATTERN_RADIAL_MOTION_RESULT.md)에 기록한다.

무지개 댄스의 Aura는 cylinder mesh·rainbow ring을 포함한 7요소이며 native mask/emissive를
사용한다. Dance Light는 mesh 3개가 아니라 sprite 3개다. 설치 모델에 없는 B_WP_3/4/5를
합성 본으로 위장하지 않고 독립 BOSS root 그룹으로 저작한다. 기존 세이튼댄스 Spotlight는
LightResources의 BOSS 리소스와 패턴 occurrence를 재사용한다.

알비온 4219903의 부채꼴은 한쪽 원본을 직각 네 방향으로 배치한다. notify의 회전은 float3가
아닌 원본 int32 FRotator로 읽어야 한다. 새 그룹은 이 값을 직접 해석하지만 기존 전체 action
importer의 회전 파서는 이 변경의 교정 범위가 아니다. 칼날 댄스의 직접 원본 원형 예고와
원본 geometry를 조합한 도넛 예고도 구분한다. 쇼타임과 칼날 댄스는 크기·색·시간이 다르다.

지팡이 회전 화염파동의 실제 십자 폭발은 본체의 disabled FireWave notify가 아니라 간접 소환한
MN_ISTM_00-4 NPC의 4222003에 있다. 16m×2m 두 줄을 90도로 교차하며, 별도 화염격자
A/B/C의 평행·직각·대각 배치와 혼합하지 않는다. NPC MIC의 static permutation은 상위
material의 texture 배열이 아니라 해당 MIC의 cooked 배열을 소비하므로 실제 index identity를
맞춘다. 새 native 3603–3606은 기존 구간 installer의 추가 설치로 연결한다.

예고와 폭발은 독립 그룹에서 0초 기준으로 시작하며 내부 source delay만 유지한다. NPC 십자
예고의 원본 bKillOnDeactivate는 현재 범용 runtime의 metadata만으로 조기 종료되지 않으므로,
이 그룹에 sourceTransformTrack alpha cut을 두어 원본 3초 종료를 보존한다. 실제 Client codec,
재생·resource staging과 사용자 시각 판정은 별도 증거다. 범위·검증은
[패턴 그룹 결과 G08](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md)을 따른다.

백스탭·화염링 그룹은 실제 hoop인 `Par_G_RPCT_05_FireRing_01_LOC_INT`를 사용한다.
별도 화염링 소환의 Sk_04 cone/half-cylinder와 혼합하지 않는다. 원본 Projectile 421982502의
배율 .7과 높이 1.1m를 유지하고 빠진 TypeData pitch -90도를 새 그룹 mesh detail에 연결했다.
실제 822정점과 재생 행렬의 대표 표본은 수직 XY 면, 외경 약2.19m, 법선 -Z다. 화염포는
기존 Saydon Sk_01 sprite11개를 사용자가 요청한 2배로 확대하며 백스탭과 링 합성이 동일 설정을
소비한다. 링 중심과 분사구는 `[0,1.1,0]m`, 전방은 +Z다. 붉은 분신 브레스는 원본 Breath_01의
native 적색·검정·주황 재질과 notify 배율을 유지한다. 세이튼 트리의 7개 독립 그룹 등록,
수치 검증과 사용자 화면 미확인 범위는 같은 결과의 G09에 기록한다.

### 공통 불뿜기에서 source 분포와 배치 방향을 구분한다

`Par_G_RPCT_05_Fire_01/02_LOC_INT`는 지팡이 action4219951의 불뿜기25요소이며,
기존 백스탭 `Sk_01` 화염11요소와 다른 원본이다. Cooked distribution의 `lookupTable`
앞2개 값은 range header이므로 이를 XYZ로 읽지 않는다. 실제 `CEffectDistribution`과
Playback이 평가한 주 속도는 UE -Y → Client +Z다. neutral shared frame에서 yaw0을
사용하며, 추가 yaw+90는 분사를 +X로 틀어 버린다. 실제 본의 basis·socket과 occurrence의
회전은 그 다음 배치 경계이며 화면에서 요청한 반시계90도와 코드의 한 필드 값을 동일시하지 않는다.

현재 공통 파생 후보는 준비1.56초를 제거하고25요소의 원본 recipe·재질·입자 local/world
정책을 보존한다. 원본 head basis 배율1.7은 neutral 요소 scale에 한 번만 유지한다.
기존 원본42요소의 본 부착·준비 동작과 전조·폭발 기둥·바닥 decal은 같은 파생으로 섞지 않는다.
확인한 방향·등록·설치와 사용자 화면 판정은 대응 작업 RESULT에서 구분한다.

### 맵 연출의 중심·조명·재질 시계

단독 Effect Tool 재생과 같은 Level의 여러 Effect 동시 생성은 다른 admission 조건이다.
Level placement의 합계를 캐릭터 한 명의 owner 예산에 넣으면 정상 문서도 거절될 수 있다.
Level 합계는 기존 scene hard 한도를 사용하고 캐릭터·보스 owner와 remote 제한은 유지한다.
박스의 최대 동시 예약량과 실제 particle 수명을 구분해 검사한다.

여러 source actor를 묶은 공통 원점과 도형 중심은 다를 수 있다. 쿠크 포탈은 오망성/흡입을
독립 cue로 나눠 중심과 local 시작 시각을 소유하며 주변 쥐·금빛 문서는 원본대로 유지한다.
source transform을 가진 local-space fixed-axis sprite의 잠금 축에는 정규화된 emitter basis를
한 번 적용한다. 원래 정상인 world-space sprite·camera-facing·mesh에 같은 회전을 전파하지 않는다.

MAP 광원의 절대좌표를 BOSS/WORLD 상대 offset으로 재사용하지 않는다. 원본 광원 모양과
방향·거리 안에서 대상 높이의 source ray 교차점을 실제 pivot에 맞춘다. 연출 배우가 Deploy인
경우 WorldSequencePlayer의 실제 animation pose를 사용하며 전투 보스 spawn으로 대체하지 않는다.
SCENE03A의 실제 Matinee에 연결된 동적 POINT/SPOT/DIRECTIONAL은 별도 typed light 문서로
재생한다. 기존 맵 조명·사용자 Scene Profile과 이 원본 cue의 소유권은 분리해서 편집한다.

StaticMeshActor의 Matinee 재질 파라미터는 source move와 같은 시계로 샘플한다. 승인된 native
프로그램의 이름·타입·row/lane으로만 SCALAR 또는RGB를 변경하며 vector W와 나머지 packet을
보존한다. 곡선 없는 문서는 기존 입력을 그대로 사용한다. StaticMeshActor를 Cascade particle로
위장하지 않고 source component 근거가 있는 프로그램만 SourceTransformTrack MESH로 승인한다.
실제 masked/one-sided 정적 재질은 원본 discard와 depth write를 함께 사용한다. 같은 재질 계열의
particle용 depth-read 기본값을 정적 가림막에 전파하지 않는다.

쇼타임 총구는 실제 WORLD 총의 설치 WModel 정점과 animated pivot·scale로 측정한다. 본 이름이
비슷하다는 이유로 다른 sub-rig를 같은 총구로 취급하지 않는다. Source ProjectileTrace가 지면
표적·먼지·착탄을 소유하면 이를 임의 비행탄으로 바꾸지 않는다. 바닥 표적은 MAP/actor anchor의
이중 변환과 실제 nav 지면 아래의 높이를 먼저 확인한다. 적용 문서와 수치·게시·화면 판정은
[원본 시퀀스 결과](09-12/2026-09-12_KOUKU_SOURCE_SEQUENCE_RESTORE_RESULT.md)의 후속 기록을 따른다.

## 생성 재질과 셰이더의 빌드 입력 소유

Native material 8종의 큰 C++ 표는 Private *_Tables.inl과 해당 Material.cpp가 소유한다. public descriptor·조회 함수·실제 inline 소비 상수는 public에 유지하며 generator/installer는 native_material_tables.py로 기존 형식을 확장해 읽고 변경된 출력만 저장한다. runtime program ID, 표 순서와 GPU 입력 ABI는 같은 계약이다.

Native shader의 body/case는 같은 ID group의 물리 include를 사용한다. SourceCharacter는 Engine 정본의 common/Base/Light include로 나뉘고 Client 복사본은 PrepareEngineSdk가 전달한다. 이 분리는 수정 전파를 줄이며, 여러 PS에 포함되는 큰 program 평가의 최적화 비용을 없애지는 않는다. GPU 실행 속성이나 alpha 정책을 같은 코드로 간주해 임의 병합하지 않는다. 빌드 검증은 visual fidelity 승인을 대신하지 않는다.

### 맵 연출의 공통 앞 대기 제거

Composition 박스의 시작 시각과 Effect 문서 내부 시작은 따로 조사한다. Box Preview는 박스
시작에 바로 seek하여 Effect age0과 같은 시각의 actor/WORLD를 함께 준비한다. 맵의 독립
SourceTransformTrack 문서에서 공통 대기를 제거할 때는 모든 StartDelay에서 같은 값을 빼고
모든 SourceTimeOrigin에 더해 원본 transform·alpha·named material sample을 보존한다.
Model cue·본 부착·source actor·baked history·다른 clock carrier는 이 변환의 지원 범위가 아니다.
현재 Effect Tool은 Current Effect의 명시 명령으로만 적용하고 Detail 미적용 값과 Save의
기준본 검사를 우회하지 않는다. portal context120개17.134767초 후보, 생성기와 수치 검증 범위는
[원본 시퀀스 결과 G12](09-12/2026-09-12_KOUKU_SOURCE_SEQUENCE_RESTORE_RESULT.md#g12-box-preview와-포탈-context의17초-앞-대기)에 기록했다.

### 쿠크 전체 목록과 실제 보스 미리보기

저작 목록은 Catalog admission과 분리한다. EffectResourceTree는 관문·연출·패턴 분류이며
Catalog·Authored 헤더·트리 참조의 합집합을 표시한다. 원본을 열 때 실제 codec과 canonical 경로를
검사한다. 분류에 여러 사용 문맥 중 하나를 고른 결과를 runtime actor 선택의 정본으로 사용하지 않는다.

쿠크 V1은 sourceModelPreview, 유일한 저장 pattern 연결, 사용자가 명시 선택한 Model View 문맥
순서로 실제 보스와 clip을 준비한다. 플레이어는 배치 원점만 제공하며 알 수 없는 소켓을 합성하지 않는다.
애니메이션 미리보기의 손 무기는 같은 BossCatalog의 모델·재질·pre-transform과 실제 b_wp_1을
사용한다. 본의 위치뿐 아니라 설치 무기 정점을 해당 시점 행렬에 합성해 단위와 기저를 검사한다.
목록·개별 컴파일·실제 모델 CPU 검증과 제품 EXE 반영·사용자 화면 판정은 각각 구분한다.

쇼타임 양손 기관총 World가 실제 표시되는 동안 기본 무기를 숨기는 연결은 같은 BODY model을
기준으로 NPC와 Model View가 공유한다. 두 총 resource의 기존 부착·재질은 유지한다. 각각의
World object가 숨김 등록을 소유하며 종료·실패·pool 반환 시 해제한다. 한쪽만 먼저 끝나거나
preview actor가 바뀌어도 다른 actor의 무기 상태에 영향을 주지 않는다.


World 위치 UI는 고정 위치와 움직이는 World object 연결을 같은 상위 선택 안에서 구분한다.
Sequence Append는 asset의 WORLD 기본값을 빈 object 연결로 만들지 않고 고정 MAP으로 시작한다.
명시 선택한 World box만 연결하며, 기존 WORLD 박스의 Player/Mouse 위치 버튼은 직접 MAP으로
전환한다. Mouse 취소는 이전 편집을 보존하고 유효한 hit에서만 좌표와 부착 참조를 함께 바꾼다.
Resources에서 기존 occurrence를 Preview할 때는 새 박스용 spawn 초기화를 적용하지 않는다.
기존 고정 MAP과 object WORLD의 runtime 소비자는 유지하며, 초기 위치는 실제 player나 사용자가
피킹한 표면으로 결정한다. Created Resources의 한국어 이름은 Composition 별칭이고 assetId가
실제 원본을 참조한다. 같은 Tree 원본 이름을 기본 표시하되 기존 별칭을 자동 덮어쓰지 않는다.

고정 공간에 둔다는 사용자 요청은 MAP·followBoss=false로 저장하며 캐릭터나 World Object에
부착하지 않는다. WORLD의 빈 worldId는 단순히 해당 Effect가 기다리는 상태가 아니라 Product
전체 로드를 실패시키는 구조 오류다. Client codec과 publisher에서 같은 필수 참조를 검사하고,
Product 오류에는 occurrence ID를 남긴다. 여러 정상 Effect가 동시에 사라졌다면 개별 shader를
바꾸기 전에 게시본 전체의 로드·source revision을 먼저 확인한다. 작은 오망성의 기존 native2811/2812
alpha 수정은 설치 CSO와 실제 입력으로 비교하며, 전역 로드 실패만으로 다시 교체하지 않는다.

트럼펫·회전 카드의 독립 그룹은 원본 emitter/재질과 저작용 위치 선택을 따로 기록한다.
compact CEFParticleData의 FRotator는 float 위치 벡터와 같은 필드가 아니며, byte layout을 확인한
해당 occurrence만 해석한다. Projectile sourceTransformTrack은 원본 속도와 명시한 preview 경로를
사용하고 실제 CPU 중심·각속도·시작/종료를 검사한다. 다수 ribbon의 운영 capacity는 실제 수명과
샘플 간격을 수용하도록 계산하며 원본 TypeData reserve를 지우거나 전역 문서 예산을 올리지 않는다.
현재 적용과 수치는 [전체 이펙트 편집 결과](09-13/2026-09-13_KOUKU_EFFECT_LIBRARY_EDITING_IMPLEMENTATION_RESULT.md)를 따른다.

### 쿠크 화염·감전빔·예고 영역의 실제 소비 입력

Sprite 발생 수는 runtime occurrence count로 대신 검증하지 않는다. SpawnRate/Burst가 0이고
SpawnPerUnit만 있는 fire emitter는 고정 origin에서 입자를 만들지 않는다. 실제 설치 actor의
clip·source socket을 샘플하고 초기점 대비 변위를 기존 sourceTransformTrack으로 전달한다.
사용자 height/yaw/scale과 원본 재질·flipbook은 보존하고 임의 spawn rate로 누락 운동을 숨기지 않는다.
이 복구는 선택된 원본 fire emitter의 이동 기반 발생을 연결하며 서로 다른 action의 전체 발생 구성이
같다는 주장이 아니다.

Projectile compact 값은 이름을 추측하지 않고 반영된 속성 layout과 원본 Action/SkillEffect의
MemoryPos·area origin을 함께 읽는다. Trail capacity는 source lifetime·speed·point spacing이 만드는
실제 history를 수용해야 한다. 운영 예산에 맞춘 capacity와 원본 reserve를 구분한다. 원본 구성 비교본의
도착점은 명시 저작 입력이며 실제 플레이에서 기록되지 않은 조준 위치를 자동 복원했다고 하지 않는다.

Kouku native3602 fan은 기존 shader의 내부 반경 `inner`로 채움이 가능하다. 검증된 LocalDecal carrier의
named material track도 기존 바인더에 연결해 외곽선은 유지하고 내부 반경만 시간에 따라 증가시킨다.
직렬화된 원본 시간 곡선이 없으면 프로젝트 저작 곡선으로 표시하며 외곽선을 중복 합성하지 않는다.

장면 수축은 기존 Screen Post의 `screen.scene-collapse.capture.v1` profile로 현재 완료된 HDR/Bloom을
한 번 보존하여 같은 UV로 축소하고 바깥을 검은색으로 그린다. static portal의 native material이나
원작 capture camera와 동일한 구현으로 기록하지 않는다. 코드·후보·제품 적용과 사용자 화면 판정은
[연출 Workbench 결과](09-13/2026-09-13_KOUKU_CINEMATIC_WORKBENCH_IMPLEMENTATION_RESULT.md)를 따른다.


### Object 동시 Effect와 화면 캡처의 합성 경계

Object Motion의 V1 Effect는 기존 CEffectPresentationService와 WorldSequencePlayer를 사용한다.
TIME 0ms와 followObject를 명시하고 실제 Object CModel의 pose를 matching ModelCue anchor로
공급한다. 다른 모델 cue까지 숨기거나 preScale과 occurrence scale을 중복 적용하지 않는다.
월드 커튼 alias의 화면 표현은 기존 V2 ScreenPost/ScreenOverlay이며 월드 위치·직교 투영 UI를
새 경로로 만들지 않는다. 명시 companion을 모델과 같은 절대 clock으로 재생한다.

검은 바깥을 가진 scene capture 수축은 Blend 전에 HDR/Bloom 장면을 대체하고 이후 포탈을 그린다.
일반 후처리는 기존 Blend 뒤 순서를 유지한다. 축소 중심은 source transform의 화면 투영이고
동일한 capture HDR/Bloom의 표시 영역을 줄인다. Alt+V camera mesh의 종착점은 실제 설치된
animated cube의 현재 pose/bounds와 Frame.RootWorld로 맞추며 캡처 RT 해상도는 유지한다.
검은 바깥 배경은 1관문 도입 포탈 연출에만 적용한다. 차원술사 Alt+V는 기존 배경과 source
효과를 유지하며 scene replacement나 별도 검은 배경을 연결하지 않는다.

먼 far plane에서 카메라 이동에 따라 맵 광원이 깜빡이면 world far corner로 만든 평면의
정밀도를 실제 light sphere와 대조한다. CFrustum은 homogeneous view-projection 평면을
사용하고 local covector 변환도 같은 기준을 따른다. CPU 가시성 검증은 사용자 화면 판정을 대신하지 않는다.

소스·수치 검증과 새 데이터 적용·Product 빌드·사용자 화면 상태는
[인형·Object/Sequence Effect·포탈·커튼 결과](09-13/2026-09-13_KOUKU_DOLL_VARIANTS_OBJECT_SEQUENCE_EFFECT_IMPLEMENTATION_RESULT.md)를 따른다.
현재 결과가 STAGED_NOT_APPLIED이면 설치 완료로 해석하지 않는다.


### 일반 v15 문서의 전체 재생 준비

Effect의 format v15 자체는 특수 runtime carrier 존재를 뜻하지 않는다. runtimeCarrier와 baked
history가 없는 정상 문서는 Family와 전체 재생 모두 기존 Stage_Document를 소비한다. 실제 확장이
있는 문서만 기존 DocumentOwnedRuntimeProjection으로 준비하며, 판정은 Codec의
Requires_DocumentOwnedRuntimeProjection을 Tool factory와 Catalog 소비자가 공유한다.
검증된 일반 문서의 전체 재생에서 `no admitted runtime carrier`가 발생하면 버전 기반 분기를
먼저 대조한다. 데이터·재질·시작 시간을 바꾸어 이 준비 실패를 숨기지 않는다. 실제 문서/worker
준비와 사용자 화면 판정은 [연출 Workbench 결과 G13](09-13/2026-09-13_KOUKU_CINEMATIC_WORKBENCH_IMPLEMENTATION_RESULT.md)을 따른다.


### 2026-09-14 전환 시점 고정 캡처와 큐브 축소

현재 `screen.scene-collapse.capture.v1`은 scene distortion resolve 뒤의 HDR/bloom pair를 occurrence별로 한 번 캡처한다. `captureShrinkSeconds`는 수축 소요이며0이면 Timing 수명을 사용한다. 수축이 먼저 끝나면 남은 박스 수명 동안 검정을 유지한다. 원본 camera가167~179도까지 벌어지는 문제를 Sprite lifetime 문제로 혼동하지 않는다.

`screen.scene-capture.cube.v1`은 occurrence 준비 때 cinematic camera 적용 전에 저장한 HDR/bloom pair를 소비한다. 첫 ScreenPost Render에서 다시 캡처하지 않는다. 바깥은 live scene을 합성하고 수축 중심은 화면 중앙(0.5,0.5)으로 유지하며, 끝 크기만 `captureTargetModelCueId`의 실제 CModel 첫 pose bounds에서 구한다. 현재 소비자는 DimensionMaster2050540, target `altv.source.notify036.cube`(2초)이며 중복 camera18/31만 숨겼다. 모델·본·preScale과 root/local/camera 변환을 실제 설치 WModel로 확인했다. 임의 animated pose endpoint 지원으로 확대하지 않는다. 후속 cube texture도 같은 시작 pair를 직접 소비한다.

정확한 Sequence 캡처는 요청 커서를 보존하고 WORLD·animation·camera·Effect exact sample을 전환점에 맞춘 다음, 그 시점의 Late_Update를 거친 렌더를 캡처한다. 준비 프레임에는 캡처하지 않는다. 실패 HRESULT와 A/B suppression을 대기 상태로 숨기지 않는다. 구현·Data 설치와 사용자 시각 판정은 [Cinematic RESULT G14](09-13/2026-09-13_KOUKU_CINEMATIC_WORKBENCH_IMPLEMENTATION_RESULT.md)에서 구분한다.

단일·bundle Preview의 긴 프레임 간격은 capture occurrence의 종료가 아니다. 기존 외부 시계의
forward/rewind 샘플 동안 V1 handle과 고정 HDR/bloom을 유지하고, 박스 종료·Stop·새 Preview만
정리한다. 150ms 초과 sample에서 재생 객체를 재생성하면 캡처를 잃고 같은 전환점으로 되돌아간다.
실제 준비·Bind의 캡처 완료와 이후 저FPS 수명 검증은 [쿠크 재생 결과 G06](09-14/2026-09-14_KOUKU_SEQUENCE_PLAYBACK_EDITOR_IMPLEMENTATION_RESULT.md)에서 구분한다.

알비온4219903의 _24_03은 공중 정지 pose이며 이동delta0이다. `_03` run 뒤 `_04/_05` 착지 run의 원본 하강을 측정해 앞 run에 상승을 배분하는 기존 Preview/publisher 경로를 연결했다. 원본의 절대 공중 높이를 이동량으로 적용하지 않는다. 상세 수치와 source sampling의 float noise 수정은 [Root Motion RESULT G06](09-13/2026-09-13_KOUKU_ANIMATION_SERVER_ROOT_MOTION_RESULT.md)에 있다.


### 쇼타임 짧은 섬광과 외곽불의 현재 연결

쇼타임 gun.signature, gun.signature.world, gun.muzzle, gun.muzzle.world 네 asset과 signature/signalshot의 지정 source identity에서만, 한 update의 중간 fixed step에 살아 있고 최종 표시에서는 사라진75ms 이하 단일 burst를 한 번 표시 후보로 보존한다. lifetime·source clock·simulation state는 늘리지 않으며 Seek/reverse는 exact sample을 유지한다. 대표 substep의 입자 속성과 material clock, element 선언 순서 및 occurrence row count를 함께 보존한다. 재생 후보 검증은 실제 GPU에서 depth와 합성이 보인다는 증거가 아니다. [Workbench 결과 G15](09-13/2026-09-13_KOUKU_CINEMATIC_WORKBENCH_IMPLEMENTATION_RESULT.md).

현재 조명 독립 BG 불은 world.object.kouku.g3.outer_fire.d/e/f의 정확한 mapMaterialBindings 세 곳이다. 정적 배경과 같은 source texture/UV/mask를 사용하고 optional unlit을 true로 지정해 RGB를 기존 emission MRT로 출력한다. 위치 종속 RNM은 이동 객체에 복제하지 않으며 배경·조명·다른 material의 기본동작은 보존한다. authoring→scoped publisher→WorldSequenceDocument→CModel surface→기존 static/animated/instanced shader가 같은 계약을 사용한다. 새 Resources나 별도 model runtime은 추가하지 않았다. [Workbench 결과 G16](09-13/2026-09-13_KOUKU_CINEMATIC_WORKBENCH_IMPLEMENTATION_RESULT.md).

### 단색 화면 암전의 coverage 입력

V2 ScreenPost TexturedOverlay는 PRESENTATION_SCREEN_OVERLAY_DESC의 기본 A coverage를 사용한다. 현재 Presentation_Manager는 BC1을 A coverage 형식으로 허용하지 않는다. 원본 DXT1 흰색의 디코딩 alpha가1이어도 submission admission은 실패하므로 DDS header, 실제 SRV 형식과 채널 허용 목록까지 확인해야 한다.

단색 암전은 전용 불투명 RGBA8 텍스처를 연결해 기존 black tint와 intensity/time 곡선을 소비한다. 공유 원본 DDS나 전역 format 검사를 바꾸지 않는다. 새 Resources 입력과 저작 base 참조를 함께 전달하며, 수치/정적 계약 검증을 실제 화면 표시 PASS로 기록하지 않는다. 쿠크 통합 암전 적용 결과는 [시퀀스 결과 G10](09-12/2026-09-12_KOUKU_SOURCE_SEQUENCE_RESTORE_RESULT.md)을 따른다.


### Composition의 WORLD 카메라와 World Object 참조 구분

기존 Camera shot은 WORLD/빈 worldId를 저장하지만 Sample_CompositionCamera가 shot asset의
좌표를 소비한다. Effect/Light/Collider의 WORLD는 실제 World Object transform을 resolve하므로
worldId가 필수다. 같은 anchor 문자열만 보고 공용 필수 참조 검사를 적용하지 않는다.
authoring/PRODUCT validation·Product reader·Python projection은 이 kind별 경계를 공유하며
카메라의 시간·좌표·저장 원문을 보존한다. 두 Composition 정본의 실제 저장 행을 함께 검사하고,
검증 단계 차단과 캡처 대기·렌더 비용을 구분한다. [회귀 복구 G11](09-14/2026-09-14_KOUKU_SEQUENCE_PLAYBACK_EDITOR_IMPLEMENTATION_RESULT.md).

### Character particle의 현재 world-space 정책과 쿠크 파생 모델 준비

9월 14일 일괄 변경 당시 Artist/DimensionMaster/LanceMaster/Warlord의 Authored 캐릭터 particle localSpace를 false로 설정했다. 이후 사용자 요청으로 Lance V/ALT V dragon 23행과 차원술사 V camera_view 등 확인된 부착 요소를 선택 복구했으므로 현재 전체가 false라는 뜻은 아니다. 여섯 class prefix를 조사했고 당시 Gunslinger/Slayer에는 같은 형식의 restored Authored 문서가 없었다. 원본 Reference/SourceRecipe·notify anchor는 유지한다. false인 생성 입자는 SpawnRootWorld를 유지하고 새로 생성되는 입자의 socket 위치는 기존 계약을 사용한다. 무조건적인 전체 anchor 추종 제거로 확대하지 않는다.

현재 실제 Product CPU stage/Save/Load가 가능한210문서와 합성 anchor 조건의 생성 입자2,862개에서 owner 이동 후 World가 유지됐다. 카탈로그에 남은 차원술사2050230 single-glass-canary/water-burst는 기존 modules 누락으로 로드 실패하며 현재 skillbinding 참조가 없다. 이 두 문서를 정상 검증했다고 기록하지 않는다.

쿠크 작은 오망성의 실제 ID는 pentagram이다. 팝업 curtain 파생 WModel의 embedded texture3개를 원본 Map 동일hash로 복구했으며, renderer override 여부와 CModel 초기화 texture 의존성을 구분한다. 뒤 포탈 capture는 앞선 unrelated occurrence 실패를 전체 abort로 소비하지 않는다. 실제 배포·검증과 사용자 최종 화면 경계는 [쿠크 재생 결과](09-14/2026-09-14_KOUKU_SEQUENCE_PLAYBACK_EDITOR_IMPLEMENTATION_RESULT.md)에 있다.

### 1관문 장면 수축과 암전 overlay의 동시 제출

effect.kouku.gate1.intro.portal-suction.scene-collapse의 authored.scene-image-collapse는
원래 visible=true를 유지한다. 별도 BC1 흰색 DDS를 사용하는 V2 원본 암전과 함께 재생한다.
BC1의 alpha coverage를 잘못 거부하면 원본 암전이 실패하며, 이 실패가 전체 presentation
queue를 지울 때 정상 capture도 소비되지 않아19,819ms에서 대기한다.

BC1/BC1_SRGB의 alpha 지원을 Engine의 format 검사에 반영한다. 명시적인 local isolated
provider 실패는 해당 contribution만 rollback하고, 다른 정상 provider와 그 capture state는
보존한다. 전역 실패/budget 초과는 기존 전체 rollback을 유지한다. 장면 수축은 현재 시점의
resolved HDR/bloom을 캡처하며 차원술사의 생성 시점 캡처로 대체하지 않는다.

단독 Bind뿐 아니라 실제 overlay DDS와 capture를 함께 제출한 Engine renderer 경로를
검증한다. 프레임 처리 성공과 사용자 화면 판정은 구분한다.
[현재 연결·원인·검증 G13](09-14/2026-09-14_KOUKU_SEQUENCE_PLAYBACK_EDITOR_IMPLEMENTATION_RESULT.md).

### 보스 손 소품과 WORLD 총구 Effect의 동일 actor 시각

보스 손 소품의 저장 위치가 정상이어도 Preview의 actor 소유권이 바뀌면 렌더링이 끊길 수 있다.
단일 Bundle이 만든 actor를 외부 Level WORLD가 전역 Model View로 다시 찾지 않는다. 현재
actor BODY/weapon pose를 갱신한 뒤 같은 호출의 resolver로 WORLD를 샘플하고 Effect가 그
WORLD pivot을 소비한다. 대상 불일치를 root/다른 보스의 본으로 치환하지 않는다.
총구 Effect는 보스 root가 아닌 정확한 총 WORLD occurrence와 로컬 offset을 사용하면 보스
회전·손 애니메이션·사용자 총 회전을 함께 따른다. 탄환의 출생 뒤 local/world space는 source
정책으로 별도 보존한다. [코드·데이터·검증 범위](09-10/2026-09-10_KOUKU_HAND_PROPS_RESULT.md#g06-09-14-쇼타임-play와-총-resource-preview의-actor-연결).


### 2026-09-14 capture 성공 뒤 popup handoff 시간 연결

Kouku P4의 capture는 왜곡된 장면의 Color/Bloom을 한 번 고정한다. G13 provider 계약 수정 뒤의 암전·재등장은 capture 경합이 아니라 별도 V2 display fade와 authored shrink 4.130초/Composition 종료의 불일치였다. occurrence end age를 기존 external seek → Effect Object → Renderer로 전달하여 scene-collapse의 shrink를 남은 box 시간으로 제한한다. 공유 prepared cache와 particles는 바꾸지 않는다. 0은 standalone 원래 동작이며 cube/model-cue 종료는 기존 계약이다. P4는 15,559ms box 시작 +4.26초 delay에서 capture하고 21,010ms popup까지 1.191초에 끝낸다. 원본 초반 fade를 보존하고 popup을 가리던 두 번째 display fade만 제거한다. synthetic 수치 검사와 사용자 화면 확인은 구분한다.

### World 이펙트 저장·게시 경계 확인 (2026-09-14)

World Object effectTracks의 followObject/bone/V1_EFFECT는 WorldSequenceDocument와 Map publisher뿐 아니라 Composition의 owner-source validator도 같은 타입과 UTF-8 범위로 지원해야 한다. 실제 런타임 소비자가 지원하는 필드를 게시 단계만 거부하던 누락을 동기화했다. 실제 World owner 입력 deep equality와 유효하지 않은 bool/bone 거부를 검증했고 입력이나 effect 재생 경로는 변경하지 않았다. Camera source의 목록 상한도 Arena/Map과 같은128을 사용한다. 날짜별 결과와 최종 publish는 09-14 Sequence RESULT의 G17/G18에 기록한다.


## Sequence 캡처 경계의 이력·표시 시각 및 포탈 수명

외부 시계 V1의 capture commit도 일반 external update와 동일한 history seek/advance 판단을 소비한다. 같은 render 경계를 기다릴 때 전체 입자를 재생하지 않는다. 입자의 fixed-step 계산 결과를 유지하면서 마지막 정확한 표시 시각, WORLD root 및 source anchor를 stage/validate/commit한다. 4.26초처럼 step 사이의 SCREEN_POST가 빠지지 않아야 하며 zero-delta anchor 변화·실패도 확인한다. 실제 포탈 CPU 검사와 사용자 화면 판정은 별도다. 상세 증거는09-14 Sequence 결과G21을 따른다.

포탈 source 입자의 굴절, 그 굴절이 포함된 캡처 이미지, 별도 context effect의 살아 있는 tail은 서로 다른 소유 상태다. 전환 시 입자 occurrence의 종료와 V1_ELEMENT capture의 수명을 분리하고 후반 source 요소는 stable ID/원래 local 시계를 보존한다. Scene 축소 후 페이드는 기존 V2 display-space overlay를 사용해 HDR 캡처/Bloom 이후에 적용한다. Cube capture나 공유 native distortion shader에 일괄 보정을 넣지 않는다.

### 쇼타임 한 손 총구와 부채꼴의 현재 저작 연결

`gun.signature`는 원본 양손 본 부착22개를 유지하는 `발사 섬광 양 손`, 기존 `gun.signature.world`는 한 손11개·중립 내부 TRS의 `발사 섬광 한 손`으로 구분한다. 총 WORLD에 후자를 연결하고 실제 설치 총구 좌표를 offset으로 사용한다. 외부 anchor만 바꾸면 내부 actionCueAttachment는 계속 적용되므로 원본 양손을 WORLD에 중첩하지 않는다. 각 총을 참조하는 Effect 박스를 같은 시각에 놓고 시간 그룹으로 복제하며, Effect만 복제할 때 총은 추가 생성하지 않는다. 기존4 local/7 world-space 발생기와 짧은 burst 처리 ID는 보존했다. 수치·빌드·사용자 화면 경계는 [손 소품 결과 G07](09-10/2026-09-10_KOUKU_HAND_PROPS_RESULT.md#g07-09-14-양손-발사-섬광과-한-손-총구-섬광)을 따른다.

`showtime.sector.warning.shot`은 예고 Decal1개를 유지한다. 2026-09-15 사용자 원작 관찰에 따라 프로젝트가 추가했던 native3602 `inner`의0→1 보간을 제거하고 첫 시작값0으로 고정하는 정본을 반영했다. V1_ELEMENT도 같은 scalar packet을 소비하며 별도 shader나 runtime 경로는 없다. 반경11m·각도45도·색·위치·기존 fade와 예고 수명을 유지한다. 최신 설치 상태는 쇼타임 예고 결과 G04를 따른다.


### 같은 장면의 capture와 distortion owner 종료 경계

화면 수축과 scene-color distortion이 같은 화면을 참조해도 lifetime owner는 별개일 수 있다. 수축 박스 뒤의 왜곡은 실제 frame의 다른 EFFECT owner와 particle tail부터 확인한다. 전용 capture의140ms 종료와 전반 distortion의종료를 같은전환에연결하고, 후반context는원래source clock을유지한다. 긴묶음전체를반복재생하거나암전프로필로덮지않는다. 화면용후처리목록의frame clear와원본texture생존기간은별개이며, texture가살아있다는이유만으로그프레임에draw됐다고판정하지않는다.

움직이는static instance의재구성은현재runtime visibility를보존한다. source visibility는초기배치입력이고, 재생중Sequence/Level의가시성소유권을모션이덮으면숨긴소품이되살아난다. 대응결과의G23에서UV owner·원본섬광시점·그네재노출의검증을구분한다.


본 부착 Element 그룹의 위치 편집은 기존 Element local translation을 공통 delta로 바꾸는 저작 동작이다. 본별 socket/orientation/runtime slot과 변환 owner를 구분하고, position lerp의 양 끝을 같이 이동한다. 현재 쇼타임 양손은 같은 본을 쓰는 11개씩이며 source track이나 transform inheritance가 없다. Effect Tool의 source Effect Play All/Play Group은 정확한 asset 사용 관계와 actor/Gate/target이 일치하고 지원 본 소품을 가진 유일한 저장 Pattern을 기존 WORLD 경로로 자동 참조한다. 명시 Pattern 소품 선택이 있으면 이를 우선하며 여러 자동 후보는 거절한다. 양손 원본은 두 총을 함께 보면서 Left/Right Group Center를 독립 조절할 수 있고 한 손으로 분리할 필요가 없다. 원본 내부 본 부착과 source animation을 유지하며 단일 총 WORLD root를 중첩하지 않는다. 본-local와 총 WORLD-local 총구 위치는 서로 다른 좌표다. 상세 상태와 수치 검증은 [09-11 Solo/그룹 결과의 G05/G06](09-11/2026-09-11_EFFECT_TOOL_SOLO_AND_SELECTED_GROUP_PLAYBACK_RESULT.md)에 둔다.


### 팝업북 전환·조명 설치와 원본 입력 확인

포탈 capture와 context왜곡은 서로 다른 occurrence 소유자다. 화면 수축 종료 후에도 context native왜곡이살아 있으면매프레임다시출력된다. 전반context/portal 종료→별도capture140ms→popup연속시작을 같은저장본에연결하며, fullcontext삭제로쥐전체를없애지않는다. Source수정과EXE빌드·게시·사용자화면판정을구분한다.

팝업actor811의white_t평면은커튼이아닌후반섬광이며,원본첫값을pre-roll부터그리지않게해당carrier의활성시점만제한한다. P4스포트라이트의exposure/fog는배경광억제와별개로확인한다. 원본책의정확한nativeLOD를해독했을때별도COLOR0버퍼가없으므로일반적인shader지원공백만으로이책의실제누락이라판정하지않는다. 원본공간SH·환경전체동등성은선택texture존재와다른검증경계다. 적용·수치근거는[Sequence G24 결과](09-14/2026-09-14_KOUKU_SEQUENCE_PLAYBACK_EDITOR_IMPLEMENTATION_RESULT.md)에둔다.


쇼타임 공 낙하도 서커스공과 같은 nested CDO 상속 누락이었다. LocationDirect.ScaleFactor1과 StartSize1을 해당 authored recipe에 복구하니 원본 약0.5초 낙하와 기존 대비5배 크기가 회복됐다. 사용자 TRS1.25는 별도로 보존했다. 붉은 sprite는 world-space spawn-only 위치 복사이므로 이전 낙하 위치에 남고, mesh/glow만 provider의 현재 위치를 따른다. 원본 임의 확대나 새 smoke 합성 없이 실제 birth history를 검증했다. 수치·설치 범위는 [공 낙하 G11 결과](09-13/2026-09-13_KOUKU_PATTERN_RADIAL_MOTION_RESULT.md)에 둔다.


공통 화염의 파생 방향은 실제 소비자의 국소 축을 기준으로 정한다. 십자는 공통 +Z를 네 방향으로 복제하고, 3갈래는 원본 socket-local +X에 맞춰 복사본만 yaw90으로 연결한다. 설치 본 basis1.7에는 Detail scale1을 사용한다. 공통 정본에 같은 보정을 다시 넣거나 본과 Detail의 배율을 중복 적용하지 않는다. 준비·예고·조명을 별도 보존하고 원래 생성기에도 파생 함수를 연결한다. 실제 모델 검사와 설치 증거는 [화염 통합 G16 결과](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md)에 둔다.


### 2026-09-14: Ribbon UV1과 socket 본 basis, nested CDO 복구

- 실제 ribbon point/count/alpha가 정상이어도 원본 pixel shader가 사용하는 TEXCOORD 성분이 0이면 화면에는 표시되지 않는다. 원본 VS의 TEXCOORD0 네 성분 pass-through와 PS의 zw 읽기를 함께 대조한다. WaterRibbon의 edge mask는 UV1.x=0에서 0이므로 mesh 전용 UV1 adapter를 beam/trail에 적용하지 않은 경로를 확인해야 한다. 원본 pixel 식을 바꾸거나 RGB/alpha를 강제하지 않고 검증된 VF의 입력을 연결한다. 폭/누적거리 축 배치는 실제 source CPU vertex bytes가 없으면 소비자로부터의 추론과 사용자 최종 화면 판정을 분리한다.
- source socket cm→m만 적용하는 것으로 본 부착 복구가 끝나지 않는다. 실제 설치 WModel bone basis·preScale·스킨 정점과 원본 PSK를 대조한다. 예를 들어 해골 폭탄 FX_01의 양의 source Z는 설치 b_body에서 음의 local Z이며, 이를 그대로 더하면 심지 대신 몸체 아래에서 방출된다. 전체 모델이나 사용자 scale을 돌려 숨기지 않는다.
- serialized RawDistribution에 Distribution=None만 남고 cooked LookupTable이 빠진 경우 exact instance와 원본 archetype/CDO chain을 대조한다. pointer null을 전체 분포 0으로 해석하면 브레스의 radius/rotation/rate, 빔 flare의 alpha가 사라질 수 있다. 확인된 occurrence의 비어 있는 필드만 상속하고 사용자의 비어 있지 않은 분포와 TRS는 보존한다.

### Composition에서 여러 Effect 발생 위치를 함께 저작

서로 다른 시각의 고정 MAP occurrence를 선택 그룹으로 묶어도 각 PositionOffset은 같은 세계좌표다. 그룹 중심 이동은 모든 occurrence에 공통 delta를 더하며 내부 source attachment·회전·scale·입자 시간은 유지한다. BOSS/WORLD는 실제 같은 anchor frame인지 검사한 경우만 공통 offset 이동을 허용한다. 부모 group transform이나 원본 Effect JSON을 추가로 보정하지 않는다. 같은 시각 복제는 기존 occurrence 복사·stable group ID 재발급·staged placement 복사를 재사용하고, 새 ID가 포함된 preview snapshot을 현재 시각에서 다시 준비한다. 소스 저장은 Composition의 기존 원자 Save 경로이며 최종 화면 배치는 사용자가 확인한다. 구현과 검증 범위는 [Effect 그룹 위치 후속 G06.1](09-13/2026-09-13_KOUKU_COLLIDER_SELECTION_GROUP_RESULT.md)에 기록한다.


### 앞 clip의 사라짐 notify와 신규 Effect 등록 경계

동작 끝의 사라짐을 분리할 때 선택된 다음 clip만 검색하지 않는다. 앞 clip의 HidePawn,
Light·particle notify 순서를 확인하고 상대 지연·수명·root snapshot/bone follow를 따로 보존한다.
원본 actor visibility 명령과 독립 Effect의 시각 요소도 구분한다. 설치 WModel의 실제 bone pose로
부착을 검사하며 다음 clip의 root offset을 대신 쓰지 않는다. 신규 Authored metadata의 발견은
runtime Catalog Load와 다르다. 실행 중 Composition을 외부에서 수정하지 않고 사용자 저장 후
다음 Client 실행에서 신규 asset 재생을 확인한다. 설치 범위와 수치·화면 미확인은
[쇼타임 사라지기 G17 결과](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md#g17-쇼타임-쿠크세이튼-사라지기-독립-effect-설치--2026-09-14)에 기록한다.

### FixArea 직사각형 예고와 여러 원본 발생의 투영

영역 폭은 원본 필드명만으로 판단하지 않고 현재 GameArea 소비자·역변환을 함께 확인한다.
직사각형 AreaAngle은 전체 폭이며 LocalDecal의 StartSize x/y는 world XYZ가 아닌 projector
평면 축이다. 원본 FixArea의 채움·페이드·수명은 ScriptStruct의 이름·타입·연결 순서와 raw
bytes를 결합해 해독한다. `len-164` 같은 tail 상수는 sound Timer를 읽을 수 있으므로 쓰지
않는다. 확정한 named timing과 기존 source track의 선형 보간 투영은 구분해 기록한다.
여러 폭발은 각 source timer·TRS·판정 영역이 정의한 occurrence 단위로 ID와 상대 시각을
보존하고 공통 좌표 변환을 한 번만 적용한다. 같은 particle template의 loop 수를 늘리거나
독립 그룹의 전방 변환과 원본 yaw를 중복 적용하지 않는다. 새 element ID를 발급한 복제본은
기존 portable-copy의 `authored-copy:<원본 ID>`로 RNG identity를 보존하고 실제 원본 재생과
대조한다. 예고의 visual 활성 창과 보수적인 전체 Playback duration도 구분한다.
신규 Preview/Append는 기존 Playback의 source particle tail까지 같은 종료 계산을 사용하고 기존 사용자 occurrence duration을 다시 쓰지 않는다.
현재 설치·재질 경계·검증 상태는
[사각형 장판 G18 결과](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md#g18-쇼타임-사각형-예고와-3회-공습-폭발-등록--2026-09-14)에 기록한다.

### 맵 광원과 Typed Effect Lights의 공통 제출 경계

현재 MapLightPresentationRuntime과 이펙트 광원은 모두 Presentation_Manager의
Add_TransientLight를 소비한다. Rendering Workbench의 Typed Effect Lights OFF는
둘 모두를 억제한다. 어두운 scene profile에서 맵 SPOT 하나만 사용하는 화면은 이때
바닥 전체가 검게 보일 수 있으므로 material/UV 복원 여부와 광원의 enabled·제출
허용·실제 제출 개수를 구분해 확인한다. A/B 후처리 초기화와 profile publish는 별도
메모리 토글에 영향을 주지 않는다. 반사광 입력 단절을 발견해도 diffuse까지 소실된
전체 검정 증상의 원인으로 즉시 확대하지 않는다. 실제 사용자 재현 근거는
09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md G10에 있다.

### V1 재생 전 worker 완료 결과와 prewarm pacing을 구분한다

V1 Effect preparation failed의 owned FIFO front 메시지는 material/particle shader 검증
이전에 worker 완료 결과의 소유권을 확인하는 단계에서 발생한다. 후속 target enqueue가
설정한 정상 YIELDED를 structural failure로 처리하지 않는다. 결과 Pop 전에 대기 여부를
확인해 payload와 worker ACK 대기를 보존한 뒤 다음 frame에 exact identity를 검사한다.
양손 발사 섬광·장판 여러 종류가 함께 막히면 개별 저장을 다시 하기 전에 같은 target의
failure receipt와 추가 enqueue 순서를 대조한다. 구현/검증은 09-12의
2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md G19를 따른다.

### GroundEffect 예고의 보스 수신 범위

원본 GroundEffect의 Near/Far ±300cm는 projection volume이며 actor 수신 허용의 근거가 아니다. Upward normal 검사만으로는 보스의 수평·경사 표면도 통과하므로 Light 없는 예고가 보스를 물들이는 경우 receiver를 먼저 확인한다. 쿠크 native3600/3601/3602/3607의 수신 제외를 skinned geometry 표식과 현재 native skin/equipment군까지 확장한 소스를 반영했다. 정적 Map과 공유하는 program30은 일괄 제외하지 않는다. 이는 프로젝트 adapter 정책이며 원본 엔진의 전체 receiver 규칙 복원을 뜻하지 않는다.

Source GBuffer marker5의 depth.z는 프레임 row다. source material writer가 RGBA32_FLOAT PickPos.W의 low8 mantissa에 program을 기록하고 Decal이 정확한 Load로 읽는다. Skinned writer는 animated Map BG를 제외한 marker0/5에 bit8도 기록하며, Decal은 이 marker에서만 actor 표식으로 소비한다. Source static map과 다른 marker의 normal/RNM payload, static-shadow exponent, Picking XYZ는 유지한다. 공용 packed lane 변경은 producer·consumer·RT format과 실제 float32 roundtrip을 함께 검사한다. [쿠크 예고 G03 결과](09-12/2026-09-12_KOUKU_SHOWTIME_WARNING_GROUPS_IMPLEMENTATION_RESULT.md#g03-노란-예고의-쿠크-표면-수신-제외--2026-09-15)에 현재 적용 범위와 Product/화면 확인 경계를 기록한다.


### 원본 overlay의 일반 광택과 추가 반사광은 독립 입력

`use_specular=false`라도 `use_subspecular=true`이면 specular texture/color를 제거하면 안 된다.
subspecular의 view dot은 원본 비정규화 mixed normal을 소비한다. 또한 방향 기반 overlay의
specular half-dot과 vertex-paint overlay의 half-dot은 서로 다른 normal을 사용할 수 있으므로
기존 floor 수식을 전역 교체하지 말고 exact native static permutation을 확인한다. Map parser의
새 family 입력을 허용했다면 Engine Model의 override 검증과 Material texture whitelist, static과
instanced binder/Deferred까지 함께 대조한다. parser 통과만으로 제품 지원을 판정하지 않는다.


### 맵 원본 surface의 유효 입력과 파일 형식

PBR_OPAQUE/PBR_SEAMLESS_OPAQUE는 명시 surfaceDiffuse/surfaceNormal/detailNormal/ORM을
소비한다. 원본 mesh의 dummy 기본 normal이 비어 있어도 이 입력이 정상이면 생성할 수
있어야 한다. CModel override와 CMaterial 생성 검사를 함께 대조하며 사용하지 않는 기본
입력을 강제로 채우지 않는다. 다른 family의 필수 입력과 경로 검사는 보존한다.

추출 texture의 파일명/존재/hash는 실제 DDS 소비 성공의 충분한 근거가 아니다. TGA를
DDS 이름으로 복사한 경우 실제 형식으로 디코드하고 픽셀을 보존한 DDS를 stage하여
설치한다. 기존 CModel/MaterialVariant 생성 성공, Level 입장, 사용자 화면 판정을 따로
기록한다. 현재 Character Select 적용과 127개 생성 검증은
[09-15 G11 결과](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md#g11-character-select-로더-23127과-후속-재질-로딩-오류)를 따른다.

### 동일 방패의 낙하·지속 발생과 본 크기 소비

원본 SD_00 낙하와 SD_01 지속이 같은 mesh/material pair를 사용해도 별도시간계약이다. 기존 사용자ring을 원본순서로 복원할 때 stable ID·반경·수량을 유지하고 원본start/curve를persistent span에 연결한다. 이 clock remap과명시적identity adapter는 project-authored 결합이며 raw source module을 그대로 재생했다는 증거로 쓰지 않는다. clip전이의최종/초기행렬을 같은actualPlayback에서검증한다.

이미m단위인sourceparticle은 combinedbone에 남은import0.01축을 다시곱하지 않는다. 설치WModel·admission·실제clip을 확인한exactasset만기존stricthelper로정규화하고translation은보존한다. 흰색번개미표시를alpha클립으로추정하기전에actualDynamic/Color opacity와월드폭을대조한다. 원본cameraRequiredlocalSpace와사용자가선택한worldspace잔상은서로분리한다. 적용범위·수치는[워로드AltV G16](09-09/2026-09-09_WARLORD_ASVF_FULL_RESTORE_IMPLEMENTATION_RESULT.md#g16-2026-09-15-alt-v-배경낙하번개-재검토)에기록했다.


### 하늘 재질의 엔진 입력과 수치 검증 경계

하늘·LightFunction의 native shader와 MIC가 일치해도 engine/pass가 채우는 diffuse/specular
색 보정, 투영, fade, 동적 LightEnvironment의 실효값까지 확인해야 한다. 수치 검증용
identity나 기존 다른 family의 기본값은 원본 입력 증거가 아니다. 입력 producer가 미확인인
하늘 분기는 검증 세트로 남기고 전역 조명·제품 분위기 복원 완료로 처리하지 않는다.

### 독립 모델 Effect의 본체·소켓·조명 소비

NPC가 생성하는 폭탄 본체와 심지 PS는 서로 다른 원본 owner다. 순수 leaf를 WorldObject가
재사용할 때는 그대로 유지하고, 독립적인 전체 preview는 기존 CModel Cue에 원본 모델·
클립을 연결한다. Cue의 modelAssetId가 CActorCatalog native material 입력을 받고,
modelCueId/실제 bone/socket이 같은 pose clock으로 입자 anchor를 제공하는지 확인한다.
cm preScale,NPC ModelSize와 사용자 추가 배율을 분리하며 전체 자산의 전역 확대·회전으로
본체 누락이나 소켓 위치를 숨기지 않는다.

명시 Cue.Material이 없는 OPAQUE/MASKED CModel Cue는 기존 NONBLEND에서 GBuffer를
쓴 뒤 scene light를 받는다. Has_NonBlendModelCues와 Render_ModelCues가 같은 분류를
사용해야 한다. 명시 Effect material의 native pass7/8과 일반 투명 pass4는 기존 forward
순서를 유지하고 DimensionMaster T의 exact mask/unlit pass11도 보존한다. 실제 모델·
입자 재생 수치 검증과 shader/pass 소비 확인을 사용자 화면 판정과 구분한다.
현재 쇼타임 후보와 기존38cue 분류 근거는 [09-11 결과의 후속15절](09-11/2026-09-11_KOUKU_SHOWTIME_RESOURCE_INVENTORY_RESULT.md#15-09-15-해골폭탄-본체심지-및-cmodel-cue-조명-연결)에 있다.

### Source Modulate와 collision event의 현재 소비 범위

원본 `blend_modulate`의 factor를 Alpha 또는 기존 V2 Multiply로 바꾸지 않는다.
particle alpha가 원본 PS 안에서 factor의 지수로 이미 반영되면 source alpha 0의 중립값은
RGB 1이다. 현재 native 단면 sprite profile은 원본 PS 결과를 emission gain/bloom 변환
없이 `Dst * Src`로 RT0 RGB에만 적용한다. 원본 출력 선언에 없는 RT1/RT2는 쓰지 않으며
RT0 alpha를 보존한다. 기존 profile 번호·문자열은 append-only로 유지하고 native source
material/shape admission을 통과하지 못하는 일반 저작에는 이 pass를 열지 않는다.

source collision completion의 FreezeMovement는 위치·회전·orbit을 멈추되 색·크기·수명
평가를 계속한다. source direct-location 모듈의 후속 update도 동결 위치를 덮어쓰지
못한다. 원본 Collision Event는 실제 world hit에서 기존 bounded event queue로 보내며
수신 위치는 접촉점이다. receiver velocity 상속과 FirstTimeOnly/LastTimeOnly 등 아직
소비자가 없는 옵션은 계속 거부한다. 하나의 PS를 양팔 등 여러 source occurrence에
인스턴스화하면 event 이름을 occurrence별로 함께 바꿔 generator와 receiver가 섞이지 않게 한다.

baked edge history는 source sample의 마지막 시간 안으로 clamp하고 stable history ID로
정렬한다. 실제 원본 sample 폭이 0이면 폭을 만들어 admission이나 시각 성공으로 포장하지 않는다.
실제 OBJ 검증은 native table/header를 함께 최신 소스로 컴파일해야 한다. 이전 OBJ의
program 목록 불일치를 이유로 metadata 검사를 약화하지 않는다. MIC의 NULL texture
override는 같은 MIC의 serialized referenced-texture table에 실재하는 index를 먼저
대조하며 parent의 비슷한 texture를 추측해서 채우지 않는다.

현재 발탄 검증 목록은 catalog 128개와 out-only portal 후보 2개이며 제품 신규 cue는
0개다. 자료 등록·문서 검증·제품 연결·사용자 육안 승인을 서로 구분한다.
실행 증거와 남은 source branch/direct-decal 경계는
[09-15 결과 G15-V](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md#g15-v-원본-modulate-포털과-source-충돌-event-검증)에 기록했다.


### 정적 맵 복원의 배치 범위 확인

오브젝트 복원은 원본 geometry·재질·RNM 입력 확보 뒤에 제품 Level의 로딩 대상을
확인해야 한다. 같은 Area라도 Map Editor는 전체 records를, 제품 Level은 descriptor의
scope를 소비할 수 있다. 원격 assembly는 이름·mesh 일치 외에 sourcePlacementId,
실제 transform, material variant, placementLighting과 scope 포함 여부까지 비교한다.
Character Select의 원격 원판11/별11은 같은 SL00 데이터에 있었으나 이전 중앙 범위에서
제외됐다. 현재 전체 Area 범위는 사용자 빌드와 제품 소스에서 유지한다. 추가 대상의 원본
재질·RNM·환경 입력은 별도로 검증하고 있으며 전체 로드가 미식별 장식이나 원작 visual
fidelity 완료를 뜻하지 않는다. 검증·배포 경계는 해당 맵 복원 RESULT에 둔다.

### 분리된 조각의 native lightmap과 간접광 연결

StaticMeshComponent의 tagged `LightEnvironment`가 비어 있어도 native suffix에는 Type2
RNM이 있을 수 있다. 조각별 texture pair, UV1 영역, average/directional RGB scale을
회수한 뒤 실제 배치의 lightmap type에 맞는 shader를 비교한다. NoLightMap 프로그램이
cache에 있다는 이유만으로 이를 원본 배치의 최종 program으로 선택하지 않는다.

Character Select 원격 원판·별의 native RNM shader는 SH, 반사·확산 에너지 분배,
normal 방향 hemisphere와 ambient를 함께 소비한다. NoLightMap의 반사 방향 hemisphere와
혼용하지 않는다. 직렬화된 SH9, 원본 cube, component tint/angle은 엔진이 shader에 넣는
packed SH7·floor·sin/cos와 구분해 검증한다. Engine 소유 lookup을 일반 재질 texture나
프로젝트 적분 LUT로 대신한 경우 원본 exact 복원으로 기록하지 않는다.

별의 RNM·재질과 원판·별23배치의 HDR02/RGB/minimumRoughness는 기존 PBR 경로에
게시했다. 사용자가 찾은11쌍은 중앙 섬 동쪽60~103m의 가까운 전시 배열로 이동했다.
기존 프로젝트 BRDF와 간접광 수식을 사용하므로 native SH·hemisphere·반사표 전체
복원은 아니다. 원본 component alpha를 additive floor로 복사하지 않는다. 정적 RNM은
조립체와 함께 이동한 원래 tile이며 새 위치에서 구운 그림자로 표시하지 않는다.
동일 입력의 원본 DXBC 비교는 수식 검증이고 실제 원본 입력·배치 화면의 검증은 아니다.
게시·남은 한계는 [맵 복원 결과 G18-CS](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md#g18-cs-중앙-인근-11쌍-배치와-재질-입력-게시)에 둔다.

### 같은 geometry의 바닥 비교와 원본 축소 입력

정적인 PBR 발광은 `emissive.flicker.mode=none`으로 명시하며 기존 mode 부재와 BG의
시간식을 보존한다. native metallic-mask diffuse의 두 분기는 tint/saturation/reflection/
외부 배율 조건이 같아 대수적으로 동등한 경우에만 기존 carrier 배율로 변환한다. source
AO0·brightness0은 실제 shader 소비를 확인해 보존하고 사용자 표시 보정과 구분한다.
원본에 없는 네 번째 장식은 실제 source mesh/UV/재질로 식별한 뒤 대칭 저작한 것으로
기록하며 복사 RNM을 새 위치의 bake로 주장하지 않는다. 가시성으로 되살린 큰 바닥은
실제 지지면으로 navigation도 재베이크하고 낮은 부조 때문에 막힌 셀은 player body·
step 검증 뒤 height paint로 보완한다. 현재 적용과 검증은 맵 복원 RESULT G22를 따른다.

Character Select의11쌍은 원판·별 각각 같은 geometry와 MIC를 사용하지만 원래 배치의
RNM6 atlas pair와 UV/RGB scale이 달라 구운 밝기·그림자가 동일하지 않다. F1의
`Character Select Floor Swap`은 같은 기존 CModel material variant/MapPlacementRuntime
경로로 선택 조립체를 중앙에 stage하고 실패하면 기존 선택을 보존한다. Source 환경은
현재 shader 기준으로 group yaw를 환경 각도에서 빼며, 기본 Center 환경은 중앙 원판의
환경 입력을 복사한다. 직접광은 새 world position을 사용하고 원본 RNM은 그대로다.
이 선택은 session visual override이며 새로운 bake 또는 native SH 복원이 아니다.

관련 DDS8개에서 원본 하위74mip를 회수해 총82mip를 설치했다. 원본과 같은 UModel
decode 경로를 사용하고 기존 mip0 BC bytes·색공간·normal 형식을 보존했다. 실제
D3D11 texture/SRV와 GPU readback으로 전체 mip를 대조했다. mip0만 있는 DDS의
생성 성공이나 Aniso sampler가 올바른 축소 입력의 증거는 아니다. 공용 texture139
material행/64 catalog asset에도 같은 mip가 적용된다. 모자이크 진단에서는 원본 mip,
설치 CSO, 강한 반사·normal aliasing을 구분하고 shader가 최신이라고 시각 문제 해결을
선언하지 않는다. 현재3개 map/deferred CSO는 소스 재컴파일과 byte 동일했다.

소스·리소스 반영, 검증 범위와 사용자 빌드·화면 경계는
[맵 복원 결과 G19-CS](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md#g19-cs-중앙-바닥-교체-ui와-원본-축소-mip-복구)에 기록한다.

### 저작 그룹 회전과 서버 플레이어별 쇼타임 시각자료

ActionWorkbench의 Effect 그룹 회전은 그룹 중심 기준으로 child 위치와 방향을 같은
quaternion delta로 회전한다. shader/asset 전역 보정 없이 저장된 occurrence의 TRS만
바꾸며 시간·scale을 보존한다. 원본 emitter pre-rotation과 사용자 배치 회전은 구분한다.

쇼타임 고정 조준점·낙하·충돌·화염 그룹과 추적 조준점은 저작 occurrence를 template로
선택하고 기존 CombatObject 권위 위치·시계와 PresentationPlayer를 소비한다. template는
상대 시간/XZ 및 기존 지면 기준 Y offset을 보존한다. 2초/이동속도50%는 사용자 튜닝값이다.
수치·저장·Server 계약 검증은 화면 승인과 별개다. 구현·발행·빌드 상태는
[쇼타임 그룹 결과 G20](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md)에 기록한다.

### ModelCue의 낙하 시간과 존재시간

Composition occurrence가 길어져도 ModelCue와 source emitter의 내부 종료 시각은 그대로다.
ModelCue의 holdLastFrame은 원본 clip 마지막 pose를 반복 없이 유지하되 visible duration
안에서만 그린다. 낙하 뒤 유지가 필요한 원본 모델은 source clip을 그대로 두고 해당 visible
duration을 연장한다. source EmitterLoops0의 심지는 유한 emission window만 같이 늘리며
원본 개별 particle 수명과 socket/bone attachment를 보존한다. ModelCue 문서를 level용
무한 loop API에 허용하는 방식으로 우회하지 않는다.

마지막 폭발은 원본 HidePawn/PlayParticleEffect 사이 간격과 parameter override·notify TRS를
대조해 독립 occurrence로 연결한다. 새 폭발의 tail은 사용자 몸체 존재시간과 별도다.
소스 native 재사용과 새 분할 carrier/table, 실제 CModel의 착지 hold·늦은 심지 emission
수치 검사 범위는 [쇼타임 그룹 결과 G21-Bomb](09-12/2026-09-12_KOUKU_GATE3_EFFECT_GROUPS_V1_IMPLEMENTATION_RESULT.md#g21-bomb-저장된-폭탄-수명과-원본-파란-폭발)에 기록한다.


### V1 준비 대기와 terminal failure 전달

원본 shader/mesh 파일 존재만으로 준비 완료를 판단하지 않는다. runtime worker failure는 owner가 FIFO front를 고정한 동안 receipt를 commit하고 해제해야 한다. 개별 failed/unavailable는 terminal 격리이며 queue 자체의 fatal identity 실패는 requested pending scope에서 Preview를 명시 실패시킨다. 정상 준비 후 과거 Preparing 문구도 교체한다. 같은 revision latch를 임의 리셋하거나 timeout으로 짧은 Effect window를 소진시키지 않는다. 근거: `.md/GB/09-14/2026-09-14_KOUKU_SEQUENCE_PLAYBACK_EDITOR_IMPLEMENTATION_RESULT.md` G31.


### 쇼타임 랜덤 세트의 보스 부착과 지면 배치 (2026-09-15)

두 저작 묶음의5요소 발사를 A1/B1/A2/B2/A3/B3 순서의 stable occurrence set으로 연결한다. native 모델·재질·사용자 TRS와 세트 내부 시간을 재작성하지 않는다. MAP XZ만 첫 MAP 행에서 상대화하고 BOSS 총구의 local TRS는 그대로 유지한다. 기존 targeted CombatObject와 PresentationPlayer::Sample이 같은 서버 생성 clock으로 각각 지면 root와 실제 source CNpc root/model을 소비한다. 전체 그룹에 공통 yaw/scale을 강제하지 않는다. source/codec·서버 테스트·게시 상태와 사용자 화면 판정은 대응09-14 RESULT G34에서 구분한다.

### 독립 전기 호출과 native fan 내부 채움

원본 action에서 같은 이름의 전기 효과라도 line-atk와 crack projectile의 호출 묶음을 구분한다. 실제 설치 mesh 끝점·CPU particle World·modelPreScale로 전방을 측정하고 원본 notify의 translation/FRotator/scale과 배우 basis를 순서대로 한 번 합성한다. 새 독립 그룹의 전방 보정은 leaf 전체에 전파하지 않으며 비대칭 side angle을 대칭으로 바꾸지 않는다. 배우 방향을 쓰는 library는 BOSS anchor로 등록한다.

native3602 fan도3600/3601과 같은 material parameter track 소비자가 `inner`를 정확한 native lane에 쓴다. 요청된 알비온 fan만 고정 경계·크기를 보존한 채 채움 곡선을 연결하고, 별도로 확정된 쇼타임 fan의 static inner는 유지한다. 곡선은 프로젝트 저작값이며 원본 엔진 시간 곡선의 복원과 구분한다. 소멸처럼 같은 asset을 재분류할 때는 tree의 중복 reference를 만들지 않고 stable resource ID를 유지한다. 실제 적용과 후보·사용자 화면 판정은09-14 Sequence Implementation RESULT G35에서 구분한다.

### 생성 위치 고정과 독립 갈래의 중심 편집

V1 입자의 Local Space와 Composition의 보스 추적은 서로 다른 설정이다. 생성 위치 고정 요청에서는 runtime 입자 설정과 occurrence의 후속 root 샘플을 함께 확인한다. `BOSS + followBoss=false`는 생성 시 보스 위치·방향과 사용자 offset으로 root를 만든 뒤 유지한다. 원본 Required literal과 source leaf를 다시 쓰지 않고 요청된 authored 문서에만 runtime override를 적용한다.

같은 effect root의 갈래를 따로 이동할 때는 각 원본 호출의 stable element ID 집합을 `manual.*` 그룹으로 저장하고, 기존 Group Center가 해당 ID까지 묶음 기준으로 소비한다. 실제 그룹 이동은 선택한 요소의 위치와 position lerp 끝점에 공통 delta를 더하며 시간·회전·크기·재질은 보존한다. 본별 자동 그룹과 transform owner 검증은 유지한다. 알비온 및 백스텝의 소스 반영·CPU 검사·사용자 화면 경계는09-14 Sequence Implementation RESULT G37을 따른다.

### 원형 채움의 낙뢰 시계와 공중 actor의 루트

경고와 낙뢰가 한 Effect에 들어 있으면 native3600의 `inner`만 해당 문서의 최초 낙뢰 startDelay에 맞춘다. Composition 박스 수명과 내부 낙뢰 시계는 다르므로 전체 `fitEffectToDuration`을 켜서 낙뢰까지 앞당기지 않는다. 알비온 파란 원은 원래2초 낙뢰에 맞춰0→1 채움만 추가하고 기존 alpha fade·색·직경·번개18개 요소를 보존한다. 이는 요청된 연출 시간이며 원본에 동일 곡선이 있었음을 주장하지 않는다.

고정 공중 자세·합성 상승·원본 낙하를 구분한다. 알비온은 같은 원본 정점 높이를 유지한 채200ms 상승 phase를 사용하고, 등장/중앙 착지는 기존 actor root 소비자에서 높이를 계산한다. 원본 root suppression을 풀어 mesh와 actor에 Y를 이중 적용하지 않는다. 착지 후 원본 곡선의 작은 반등은 누적 최저값으로 지면에 고정하며 Preview와 Server가 각각 seek/fixed tick에서 같은 원리를 사용한다. 실제 연결과 CPU 수치·게시·사용자 화면 경계는09-14 Sequence Implementation RESULT G38을 따른다.

### 마리오 칼날과 갈고리의 원본 표현 및 판정 기준

칼날 원본 `par_v_rpct_cutting_pjt_01`과 `par_v_rpct_cutting_pjt_02_loc_int`는 본체 메시를 포함한 서로 다른 payload다. 일반/즉사 Effect를 독립 라이브러리로 복원할 때 기존 World Object 메시 위에 전체 payload를 자동 부착하면 본체가 겹친다. 본체 포함 여부를 먼저 구분하고 원본 native material, Required 모듈 및 archetype/CDO의 반복 기본값을 확인한다.

Object의 시각적 자전과 수평 Collider 방향을 분리한다. 동반 Effect는 optional `inheritObjectRotation=false`로 같은 이동·크기·emission yaw·WORLD placement를 유지하면서 Transform key의 메시 upright와 angularVelocity만 제외할 수 있다. 기본값 true는 기존 본 부착과 다른 Object를 보존하며, 칼날 두 Effect lane만 opt-out 후보로 연결했다. `followObject=false`는 위치도 고정하므로 이 용도로 사용하지 않는다. 갈고리 끝은 설치된 skinned mesh의 실제 끝 정점과 본의 결합을 실측하고, 정규화한 본 basis에 import 이후 미터 offset을 적용한다. 고정 root offset으로 올라가는 본 끝을 대신하지 않는다. 실제 회수 중 본 끝 Y와 플레이어 attachment XYZ를 함께 유지하고, 접촉 창의 끝과 이미 잡힌 객체의 이동 끝을 별도로 계산한다. 측정 수치·연결 범위·사용자 화면 검증 상태는 09-12 World Object Group RESULT에 둔다.


### Current Effect 그룹 중심 회전

Effect Tool의 Group by anchor는 기존 Group Center 이동과 함께 Group Rotation 드래그를 제공한다. 첫 Element의 저장 orientation에서 새 orientation으로 공통 quaternion delta를 계산하고 중심 기준 멤버 위치·회전 및 선형 위치/속도 끝점을 같이 변환한다. bone/source basis, 시간, scale, local/world particle 정책은 유지한다. animation/revolution이 회전을 소유하면 회전 편집을 거절하며 기존 preview의 현재 커서를 transactional commit 뒤 다시 샘플한다. 별도 runtime 그룹 Transform을 덧씌우지 않고 각 Element에 저장한다. 실제 공통 불뿜기·분신 브레스·3방향 불뿜기의 수치 검사와 빌드/화면 상태는09-14 Sequence Implementation RESULT G39를 따른다.


### 전기 폭발의 입자 공간과 Preview 순간이동

알비온 세 갈래 전기 폭발과 파생 백스텝은 source Required literal을 보존하면서 authoring의 detail.particle.localSpace를 끄는 정책을 생성기에도 적용한다. 입자의 Local Space와 Composition의 BOSS follow는 별개의 소유자다. 한쪽을 바꿀 때 다른 쪽의 사용자 설정까지 함께 바꾸지 않는다. 최신 문서에 필드만 적용하는 경로는 JSON 전체 재직렬화로 음수0·사용자 표현까지 바꾸지 않고 실제 대상 토큰만 변경한다.

Logic이 배우를 순간이동시키는 Preview에서는 두 관측 프레임의 단순 보간이 실제 이동을 재현하지 못한다. BOSS Effect의 발생 시점과 particle birth 시점은 같은 source clock의 실제 공간 sampler를 사용한다. 현재 위치를 frozen 발생 위치로 대신하거나50m jump threshold만으로 작은 순간이동을 판별하지 않는다. 원본 socket·bone anchor의 기준과 실제 actor animation은 과거 sampler가 변경하지 않는다. 구현과 검증 범위는09-14 Sequence Implementation RESULT G40/G41을 따른다.

### 발탄 원본 패턴의 재질·공간·시계 연결 범위 (09-16)

Full Restore 검증본39개는 실제 source action/stage/clip index로 재생한다. 본 부착은 설치된
몸·무기·AnimSet의 실제 owner 배율을 유지하고, 바닥 snapshot은 별도 arena 크기를 유지한다.
6방향 도끼와4방향 파편의 masked LocalVF color/opacity prefix, 포탈 본 로컬 offset,
4방향의 별도 int FRotator와 직접 GroundEffect2001/2002를 기존 native runtime에 연결했다.
원본 material/VF/VS/PS 식과 DDS byte 대조, 실제 CModel 골격·CPU 재생, 제한된 GPU shader
검사와 사용자 화면 판정은 각각 별도의 증거다. Product cue 승격은 하지 않았다.

source clip 재생 길이와 stage wall budget은 분리하며 조건부 전환을 원본 확정 종료로 다루지
않는다. 물보라 native distortion이나 additive는 최종 dispatch까지 읽는다. source prefix의
RGBA와 dynamic lane을 기본값으로 지우면 색뿐 아니라 전체 mask가 사라질 수 있다.

개별 정지형 돌은 exact mesh/MIC를 확인한6문서의 재질을 복구했으나 수명·배치와 식별자 없는
주변15요소는 아직 프로젝트 저작값이다. PawnMaterialParam3notify의 대상·시간 의미도
미확정이다. 원작 전체 복원 완료와 Resources 존재를 동일시하지 않는다. 적용 ID·크기·검증 및
남은 경계는 09-15 MAP_AND_VALTAN_FULL_RESTORATION_RESULT G25-V를 따른다.

### 쿠크 화염링 fixed-axis Sprite 회전

화염링과 동일화염포 조립 문서의 Sprite Particle 02(fx_a_noise_001.dds)는 source orientation axis lock을 유지하면서 선택적 sprite.followEmitterAxisRotation을 사용한다. Element/Group Transform의 회전은 최종 carrier의 고정 축 basis에 한 번 합성한다. local particle은 현재 emitter, world particle은 birth emitter basis를 사용한다. SourceTransformTrack/local의 기존 경로는 유지하고 일반 화염의 camera/velocity billboard에는 전파하지 않는다. 쉐이더 변경 없이 authored 선택·codec·Playback·Geometry 소비자를 연결한다. 원본 leaf와 조립 문서를 구분하며 생성기의 current-ring-axis-rotation 후보도 대상 두 요소만 켠다. 수치 결과와 사용자 화면 판정을 구분한다.

### 불투명 맵 shadow와 ambient 준비 범위

Source material의 정적 DEFERRED 표면 중 PBR3/4의 비masked, opaque5, overlay7/BG8의 비masked 완전 불투명 경로는 depth-only shadow를 공유한다. masked/fade, 미확인 source family와 실제 vertex 변형에는 전파하지 않는다. 재질·RNM 바인딩 생략은 geometry 제거가 아니며 전체 shader의 depth/cull/basis 대조와 실제 GPU 시간 측정은 별개다. 발탄 활성 profile의 노출·bloom 튜닝은 원본 LUT/식 복원과 구분한다.

Bern ambient의 exact VelocityCone은 scalar angle/velocity와 유효 direction, 추가 owner scale 없는 emitter-space만 지원한다. source cm/s와 축을 기존 birth basis 전에 한 번 변환한다. 현재 angle의 degree 해석은 추출값에 근거한 재구성이며 원본 CPU random의 완전 회수를 주장하지 않는다. required ambient 준비, Level activation과 사용자 실제 화면은 독립 검증이다. 상세 연결·수치·남은 경계는09-15 MAP_AND_VALTAN_FULL_RESTORATION_RESULT G26에 있다.

### 동반 폭발 system과 Composition tail

동일 clip의 source action이라도 notify enabled 상태가 다를 수 있다. cast/shot만 있는 독립 library에 광주·impact를 연결할 때 원본 action/time/scale와 조합 정책을 기록한다. source Required·nested CDO, emission stop과 살아 있는 particle의 tail을 복구하고 실제 occurrence가 이를 끝까지 허용하는지 확인한다. Resources 존재·GPU 준비를 최종 Draw/원작 화면 일치와 동일시하지 않는다. 작은 오망성 연결 근거는09-13 KOUKU_PATTERN_RADIAL_MOTION_RESULT G14를 따른다.

### Buff 포탈과 지속 분사 연장

원형 바닥 Projectile만으로 포탈 전체를 판정하지 않는다. NPC Action → SkillEffect → SkillBuff → ParticleSoundNew의 별도 PS가 수직 기둥을 소유할 수 있다. Buff ParticleData override는 이름이 일치하는 실제 ParticleParameter 소비자에만 적용하고, 명시적 Size0/Lifetime0을 모든 emitter에 곱하지 않는다. 독립 library의0초와 원래 gameplay 발동 시계를 구분한다. 원본 distortion companion을 추가할 때 shader 함수뿐 아니라 merged descriptor의 depth/resource 요구도 함께 연결한다. 진입 포탈 근거는09-13 RADIAL MOTION RESULT G15다.

이미 저작된 브레스의 전체 길이를 늘릴 때 particle 고유수명이나 모든 곡선을 함께 늘리지 않는다. 지속 emitter의 방출 창과 기존 particle tail을 합쳐 요청한 전체 길이를 맞추고 initial burst/loop는 보존한다. 사용자가 숨긴 요소와 현재 TRS·색·bloom을 최신 저장본에서 유지하며 Composition의 수동 박스 길이를 자동으로 덮어쓰지 않는다. 브레스4279ms는 원본 복원값이 아닌 사용자 저작값이며 검증·설치 경계는같은 RESULT G16에 있다.


### 화염링 단면 Sprite의 저작 양면 옵션

화염링_동일화염포의 native2876/fx_a_noise_001은 원본 Additive One Sided이며 fixed-axis 회전 복구 뒤 뒷면 컬링이 관찰됐다. detail.sprite.twoSided=true는 source material과 descriptor 정본을 유지하는 carrier 저작옵션이다. 일반 Validate와 별도 SourceContract 검증으로 적용범위를 제한하고, 지원되는 source Sprite의 실제 draw에서 Alpha3→1 또는 Additive4→2 기존 패스를 선택한다. shader equation과 blend/depth, Resources를 변경하지 않는다. 사용자 요청으로 ImGui 항목은 추가하지 않았고 화염링_동일화염포 조립문서의 지정 요소 하나만 켰다. 원본 leaf·다른 동일native occurrence에는 전파하지 않는다. 재수입의 기존 Detail 보존 경로가 옵션을 유지한다. 원본 재현값이 아니라 사용자 요청에 따른 양면 저작 변경이며 관련 증거와 제품 적용 경계는09-14 Sequence G47 RESULT에 기록한다.

### 공의 다중 section 재질과 살아 있는 Object의 반복

마리오 본무대 공의 상부 문양 mesh는 MeshMaterial 배열3개와 `bOverrideMaterial=true`를 함께 사용한다. 원본 Cascade의 section 배열이 먼저이고 null일 때만 Required가 fallback이다. sourceMaterialSlots codec은 bool 타입과 원본 배열 경로·개수, 각 native profile 및 실제 CModel 전체 슬롯 coverage를 유지하면서 true도 허용한다. null/누락 슬롯의 fallback은 현재 지원하지 않으며 실패를 숨기지 않는다. 이 규칙은 원본 데이터를 고치는 작업이 아니라 기존 슬롯 소비자의 과도한 거절을 제거하는 것이다.

원본 공의 7.5초 aura처럼 burst와 kill-on-deactivate가 있는 경우 선언상의 emitter duration+particle life가 실제 마지막 입자보다 길 수 있다. 전체 Object 반복은 저장된 model/Effect 창을 주기로 사용하고 실제 CPU 재생 종료와 원본 action 창을 대조한다. source color curve는 요소별로 보존하며 전체 Effect를 임의의 빨강 tint로 바꾸지 않는다. 인형 본 연결은 실제 설치 CModel의 두 입 본과 preScale을 사용해 cm→m 보상을 한 번 검증한다. 이번 개별 native ID·모델 크기·수치 검증은 G49 RESULT에 둔다. 사용자 화면 판정은 별도다.


### 중앙 포탈·쇼타임의 화면 왜곡 수신 표면

중앙 문양이나 폭탄 도화선 자체의 재질과 함께 재생되는 포탈·폭발을 구분한다. 정본6문서의 native 조인에서 직접 SceneColor 샘플은 없었고 별도 distortion14개 중11개는 원본 상수0이었다. texture-dependent native2461/2587/3682의 signed offset만 기존 Target_Distortion의 BA로 보내 actor/표면 경계를 보호한다. 원본 색·시각·크기·native 함수는 보존했다. 이는 사용자 요청에 따른 화면 수신 정책이며 원본 shader 식을 새로 회수한 결과가 아니다.

공통 MRT write mask/coverage와 Engine SceneResolve까지 같은 계약으로 연결해야 한다. destination과 이동된 bilinear footprint의 actor 표식 및 깊이 불연속을 확인하고 HDR/bloom 쌍에 같은 offset을 적용한다. marker가 없는 정적 prop 내부·투명 객체 전체 배제를 주장하지 않는다. 원본 mesh-prefix가 이미 offset을0으로 만드는 native는 별도 복원 과제로 기록하고 이번 화면 보호 수정에 섞지 않는다. 개별 근거·컴파일·WARP 검증과 제품 설치 대기 상태는09-14 Sequence RESULT G50에 있다.


### Pattern 반복 창과 왼손 trail의 원본 입력

같은 Effect를 여러 Pattern이 공유하면 원본 SourceModelPreview와 현재 occurrence animation이 다를 수 있다. 임시 Pattern 문맥은 모델 pose와 source bone sampling 양쪽에 같은 시작 시각을 전달하고 저장 asset은 보존한다. loopEffectToDuration은 occurrence가 opt-in한 source loop0 방출만 유한 창까지 연장하며 source clock·입자수명·finite loop는 보존한다. 실제 종료 owner의 cleanup과 CPU tail 검증은 구분한다.

원본 pixel body가 일치해도 source StartLocation과 본 basis가 trail 중심을 손에서 떨어뜨릴 수 있다. 위치·폭·색·UV·tangent 보간을 따로 확인하고 원본 red/white material scalar를 공통 흰 shader로 바꾸지 않는다. 원본 offset을 손끝에 맞춰 상쇄하는 저작 보정은 원본 복구와 구분한다. 현재 source 연결·수치 및 설치 대기 경계는09-13 KOUKU_PATTERN_RADIAL_MOTION_RESULT의 반복 왼손 trail 항목을 따른다.

### 쿠크 보스 Animation binding과 Effect playback의 별도 admission

쿠크의 body/weapon action animation은 PresentationAssetService의 엄격 patternbindings reader를 거치고 Effect/World 표현은 PresentationPlayer가 별도로 읽는다. 게시 문서에 추가된 targetedCombatVisuals를 전자 root allowlist에서 빠뜨리면 Effect는 재생되어도 CNpc는 idle로 남는다. 공용 문서의 supported optional section은 두 소비자에 함께 반영하며 잘못된 action/clip/revision은 계속 거부한다. Parent/Summon 전개 결함으로 단정하기 전에 실제 게시 binding의 root admission과 대상 보스 생성 상태를 각각 확인한다. 소스·집중 검증·Product·사용자 화면 경계는09-12 재생 복구 RESULT의09-16 항목에 기록한다.

### 공유 저주의식 Effect의 원본 preview와 Trail geometry

쿠크의 저주의식 Effect 원본 SourceModelPreview는 rpct00_att_battle_27_01이다. 같은 Effect를 사용하는 마리오 Pattern 선택이 남았다고 rpct00_att_battle_29_02로 자동 대체하지 않는다. Effect Tool 기본은 저장 source이며 Use current Pattern animation에서만 해당 box의 값 snapshot을 고정한다. pose와 bone sampling은 같은 source 시계를 사용하며 공용 Resources/Effect 문서를 변경하지 않는다.

공통 Render_Trails의 centerline은 이웃 폭 축 연속성, 카메라와 tangent의 평행 fallback, 퇴화 구간의 비연결과 단면 재질의 triangle facing을 함께 보존해야 한다. baked AnimationTrail은 원본 EdgePairs를 유지하고 빈 Points에 centerline tessellation을 적용하지 않는다. 기존60Hz와 저작 width·UV·색·time·원본 material/shader를 전역 변경하지 않는다. 설치 CModel 샘플 수치 검증은 최신 Client 화면 재현을 대신하지 않으며 개별 검사와 빌드 경계는09-16 KOUKU_PATTERN_CLEANUP_AND_TRAIL_IMPLEMENTATION_RESULT를 따른다.

원본 입력의 후속 점검에서 native2379 AnimationTrail의 VS가 전달하는 TEXCOORD0.zw가
번역 PS에서0이 되는 누락을 확인했다. 확인된 PS/VS 조합만 UV1에 연결하며 원본 수식·MIC 값·
blend를 유지한다. baked trail importer는 TypeDataAnimTrail의 tilingdistance를 cm→m로
detail에 전달한다. 기존 문서의 누락 필드는 Codec이 retained source에서 채우되 명시한0 등
사용자 값은 보존한다. Renderer의 기존 거리/tiling 계산과 원본 EdgePairs는 유지한다.

portable world-space CascadeRibbon의 SpawnPerUnit은 실제 bone 이동 구간의 거리 분율로
출생 위치를 나누고 loop 경계의 carry를 유지한다. 발생 시각·회전·age는 기존 fixed tick 값이며
분수 시각까지 원작 CPU runtime을 복제한 것으로 표현하지 않는다. 현재 직접 연결은 쿠크
저주의식 hand/staff와 발탄 Full Restore native2379이고, trail이 없는 별도 carrier-v1 제품 cue를
같이 복구했다고 주장하지 않는다. 소스 반영·원본 DXBC/실제 모델 수치·제품 빌드·사용자 화면은
[후속 Trail 결과](09-17/2026-09-17_TRAIL_RIBBON_NATIVE_RESTORATION_IMPLEMENTATION_RESULT.md)에서 구분한다.

피자의 검정 흐름은 길쭉한 velocity-facing sprite, 별도 원형 alpha 마스크와 반원·원형 방출로
구성되는 층을 포함한다. flow texture 하나의 모양으로 합성 경계를 단정하지 않는다. Cylinder의
부호 제한은 source 축 배치 뒤 적용하며 사용자 복제 구성은 원본 emitter 구성과 별도로 보존한다.
원본 레이어별 근거와 코드 수정 범위는 세이튼09-17 결과 G20의 검정 레이어 후속을 따른다.
### 파괴 소품의 정지 상태와 복구 렌더 비용 (09-16)

일반 맵과 Deploy 소품은 CModel/CMaterial을 공유하지만 개별 GameObject의 shadow cache 참여는 별도다. CDeployPropObject의 intact STATIC만 실제 world/model과 정적 alpha 입력이 같을 때 공통 Renderer depth를 재사용한다. 파괴·숨김·fade·debris·preview·morph·외부 texture override는 동적 경로로 복귀하고 기존 shadow shader/pass를 유지한다. 본체는 최종 camera, shadow는 최종 light 범위로 각각 검사하며 불확실한 bounds는 기존 draw를 유지한다. 장면 복구 후 비용은 이 소비자별로 대조하고, 안개·투명·조명 품질 저하를 기본 해결책으로 삼지 않는다. 실제 FPS의 확정은 사용자 후속 캡처로 남긴다. 수치와 적용 경계는 [G34 결과](09-15/2026-09-15_MAP_AND_VALTAN_FULL_RESTORATION_RESULT.md)를 따른다.

### 카드 projectile 표시와 골격 잔상의 현재 연결

세이튼 카드는 원본 source mesh·sprite·native 재질을 독립 V1 Effect로 재사용한다. 네 문양은 원본 2×2 atlas의 칸을 명시하며 1초 표시 반복을 Server 객체 lifetime과 분리한다. `PURSUIT_PROJECTILES`는 기존 CombatObject spawn/snapshot/event/despawn을 소비한다. Client는 Server의 위치·yaw를 기존 targeted presentation root로 전달하고 접촉 Effect는 신뢰 event에 고정된 위치에서 자연수명으로 재생한다. 원작 발사 수치와 사용자가 지정한 무한 수명·배치 타이밍을 구분한다.

트럼펫 8방향은 두 4방향 source notify의 45도 차이를 보존하며 source socket basis와 개별 native program을 공용 carrier에 유지한다. 동일 source 재질을 사용한다는 이유로 모든 emitter에 전역 회전·크기를 적용하지 않는다. Pattern의 발생 연결 유무를 먼저 확인하고 native source lifetime·sourceScale.lifeTime·실제 CPU 생존 입자·절대 alpha 종료를 함께 대조한다. 요청한 바닥 유지 구간의 수명 override는 원본 recipe를 보존하며 별도 sourceScale로 적용한다. 원작 심볼의 usemesh=false와 atlas sprite 구성을 mesh 누락으로 오인하지 않는다.

세이튼·발탄의 source TrailGhost notify는 실제 skinned CModel의 world·palette 이력으로 연결한다. 공용 `CSkeletalAfterimage`가 제한된 과거 pose를 BLEND에 제출하고 원래 palette/world를 복원한다. 흰색과 fade는 프로젝트 표현값이며 원작 TrailGhost shader ABI 완전 복원으로 기록하지 않는다. 발탄의 현재 적용은 skinned 몸체이며 별도 static axe·armor는 포함하지 않는다. 발생 시각·적용 asset·자동 검증과 사용자 화면 경계는 [해당 결과](09-17/2026-09-17_SAYDON_CARD_TRUMPET_CHARGE_IMPLEMENTATION_RESULT.md)와 [잔상 결과](09-17/2026-09-17_BOSS_CHARGE_AFTERIMAGE_IMPLEMENTATION_RESULT.md)에 둔다.

### 기본 모델 pass와 source group의 런타임 admission

골격 잔상처럼 source material program 선택과 무관한 pass는 기본 FX가 소유한다. `Shader_VtxAnimMeshBinary`의 pass14는 기존 `BINARY_ANIMATED_NATIVE_PASS_POLICY`를 사용해 기본 BASE(1), 여섯 파생 UNAVAILABLE(2)로 등록하고 파생 PS는 NULL로 둔다. 기존 native cue pass7/8/12/13과 같은 계약이다. 양쪽에 BASE를 선언하면 shader 단독 컴파일은 성공하지만 실제 `CShader::Stage_ProgramVariants`가 거절하여 캐릭터 rendering prototype 등록과 Level 진입이 실패한다.

새 공용 pass 복원은 pass 수·이름·입력 signature·변수 ABI 및 기본/파생 정책을 함께 검증한다. 실제 설치 CSO의 `CShader::Create`와 stale source selector 아래 base PS·상수·skin palette 선택까지 기존 headless WARP 검사로 확인하며 admission 실패를 validator 완화로 숨기지 않는다. 컴파일·headless binding 성공은 사용자 화면 확인을 대신하지 않는다. 이번 입장 실패와 수정 증거는 [세이튼 결과 G06](09-17/2026-09-17_SAYDON_CARD_TRUMPET_CHARGE_IMPLEMENTATION_RESULT.md)에 기록한다.


### 원본 라이브러리의 Tree/Catalog 등록 일치

영문 원본 leaf가 Effect Tool에서는 열리지만 Pattern Append에서 metadata absent이면 파일 존재부터가 아니라 EffectResourceTree와 EffectCatalog의 stable ID/authoringPath 연결을 대조한다. 기존 첫 사용 codec/portable runtime 검증을 우회하지 않는다. 라이브 미저장 편집이 있으면 exact source SHA와 semantic 등록 후보만 만들고 저장 후 최신 기준본에 병합한다.

### 2026-09-17 연결 보정: 공급원 Solo와 속도 정렬 상단광

Effect V1 선택 재생은 네 LocationEmitter/Direct class와 transform master의 dependency closure를
유지하고 기존 object-local submission mask로 실제 선택만 그린다. source validation은 유지한다.
현재 rainbow.drop의 공급원 누락은 런타임 경로 오류가 아니라 선택 문서에서 Direct 참조를
수집하지 않은 것이며, 일반 Solo/Family/Group과 Sequencer factory를 같은 방식으로 연결했다.

공 상단광의 particle 중심/속도가 일치해도 최종 quad가 같은 부착점을 유지한다는 뜻은 아니다.
PSA_Velocity용 원본 velocity를 끄면 camera-plane으로 바뀌고, noncentral pivot과 크기 증가가
표시 경계를 이동시킨다. 이 파생 Effect는 원본 velocity를 복구하고 사용자 요청 상단 부착용
pivot을 명시했다. 원본 VS/PS에는 추가 낙하가 없으나 원본 signed-size CPU packing은 미확정이며
공용 material/shader 원본 복원으로 간주하지 않는다. 실제 수치는 세이튼 Circus Ball 결과 G07,
Solo·분열 표시와 후보/등록 상태는 세이튼 Card 결과 G24를 정본으로 한다.

Albion/세이튼 공간 preview의 phase admission은 Server의 실제 fallback과 대조한다.
APPEAR_PLAYER는 사전 SELECT가 없어도 등장 시 target을 선택할 수 있고, 명시한
SELECT_PLAYER+SELECT만 지면을 고정한다. JUMP와 native하강 검증은 보존한다.
추가한 V1 FX가 Logic을 소유한다고 추정하지 않으며 source/검증 경계는 세이튼 결과 G25에 둔다.


### 카드의 owner 회전·native 반복·요청한 표시 보정의 구분

카드 mesh와 local 고정축 sprite를 함께 복원할 때는 실제 설치 geometry의 평면·장축과
원본 TypeData 회전, source 위치, pivot, camera offset을 함께 확인한다. 중심이 같아도
sourceTransformTrack 없는 sprite가 owner 회전을 소비하지 않으면 회전한 occurrence에서
분리된다. 기존 followEmitterAxisRotation을 해당 local 축 잠금 요소에 연결하고 같은
문서의 정상 world-space 효과나 다른 source effect로 전파하지 않는다.

원본 Required의 생략된 loop 값은 상속/CDO 기본값까지 대조하며 입자 수명과 분리한다.
tracking의 bounded-loop duration은 기존 Playback 경로로 소비하고 원본 유한 loop는
유지한다. 반복 경계에서 입자가 보존됐다는 수치와 순간 중첩·화면 판정은 별개다.
원본 shader 식이 이미 일치하면 texture의 회색 영역을 밝히는 authored RGB 조정을
공용 shader 복원이라고 부르지 않는다. 원본에 존재하는 카드 잔상 emitter의 억제도
사용자 요청의 표시 변경으로 분류한다. 세이튼 match 네 무늬·회전 카드의 구체적인
소스와 수치, 저장 후보/설치/화면 확인 경계는
[세이튼 카드 결과 G26](09-17/2026-09-17_SAYDON_CARD_TRUMPET_CHARGE_IMPLEMENTATION_RESULT.md)을 정본으로 한다.

세이튼 쓰리투원투하 공 발사는 저장된 낙하 ball_04의 기존 메시·native 재질·사용자 크기를
공유하는 독립 V1 변형이다. LocationDirect 역곡선은 사용자 저작이며, 공 하단의 무지개는
LocationEmitterDirect와 측정된 geometry 하단·sprite pivot을 소비한다. 별은 같은 provider에서
태어난 뒤 원본 world-space 운동을 유지한다. Catalog·Tree·Composition resource 등록은
패턴 occurrence 추가와 구분한다. 새 Resources·shader·두 번째 runtime 없이 기존 carrier를
재사용했고 원래 drop·World는 보존했다. 정확한 asset·크기·검증과 화면 승인 경계는 위 RESULT
G27에 기록한다.

### 편집 중 후보 검증과 최종 저장본 반영

Effect·Composition·WorldSequence 후보는 사용자가 편집하는 동안 준비하고 검증한다.
반영은 [AGENTS의 편집 중 데이터 반영](../../AGENTS.md#편집-중-데이터-반영)에 따라 최종
저장본 기준 승인 한 번으로 진행한다. 실행 중 Client를 일괄 차단하지 않으며, 최신 stable ID와
필드 차이를 병합해 다른 세션의 등록·사용자 배치·표시 선택을 보존한다. hash와 revision,
실패 rollback·저장 freshness를 유지하고 파일 설치와 도구 Reload·사용자 화면 확인을
서로 다른 완료 상태로 기록한다. 구체적인 적용 증거는 세이튼 카드 RESULT G28에 둔다.

발탄 Effect 탐색은 catalog의 authored ID/path 목록과 canonical 패턴 실행 권한을 구분한다.
게시 writer와의 일시적 경합은 목록 손상으로 취급하지 않으며 기존 snapshot과 독립 문서
탐색을 보존한다. busy 재시도마다 원본 animation index를 다시 parse하지 않고, 독립 다른 boss
선검증이 발탄 writer 수명을 늘리지 않도록 한다. 실제 잠금·컴파일과 화면 확인 경계는
[발탄 트리 경합 결과](09-17/2026-09-17_VALTAN_EFFECT_TREE_PUBLISH_CONTENTION_RESULT.md)에 둔다.

### Trail의 연결 기하와 재질 coverage를 함께 복원한다

Trail은 시간순 중심선 또는 두 경계의 이력을 연결한 면이다. 연속된 triangle list와 정상적인
점 간격을 확인한 뒤에도 원본 material이 읽는 UV·color·dynamic 입력까지 검증해야 한다.
길이와 폭을 뒤집거나 유한 0..1 마스크에 반복 거리 UV를 넣으면 연결된 면에서도 alpha가
중간에 사라진다. WaterRibbon과 AnimationTrail이 같은 native 입력 배치를 쓴다고 가정하지
않는다. 현재 검토한 profile의 coverage/거리 UV 계약은 TEAM ABI 가이드가 정본이다.

원본 PS와 번역 PS가 임의 입력에서 일치하는 것은 변환식 검증이다. 실제 모델/history가 만든
정점과 최종 renderer 입력을 넣어 alpha coverage, 내부 공백, 원래 끝 fade를 구분해야 한다.
후처리 없는 RT에서 동일 결함이 재현되면 그 입력 결함을 먼저 고친다. 별도 scene depth,
occlusion, distortion resolve의 영향까지 모두 없다고 확대하지 않는다. 생성된 group의 최종
consumer와 generator 경로도 확인한다. 반복된 부분 수정의 범위와 최종 검증은
[Trail/Ribbon 결과 G05](09-17/2026-09-17_TRAIL_RIBBON_NATIVE_RESTORATION_IMPLEMENTATION_RESULT.md)에 둔다.

### 원본 검정 Sprite와 요청한 공통 원형 경계를 구분한다

원본 검정 Sprite의 analytic UV alpha가 각 입자에서 타원을 만든다고 해서 effect 전체에
공통 원형 경계가 존재한다고 추정하지 않는다. 원본 요소 복원만으로도 바깥으로 새는지
원본 크기·motion·실제 playback에서 먼저 측정한다. 사용자 편집본은 보존하고 별도 복원본에
필요한 ownerRadialMask를 명시적으로 저작한다. 이는 원본 shader 식 복구와 구분한
PROJECT_AUTHORED coverage이며 native3171의 검정 두 요소에만 연결한다.

### 모자는 모델·재질 식별과 실제 소유자 수명을 함께 연결한다

같은 이름의 bone 존재만으로 세이튼을 분류하지 않는다. 실제 body material과 골격 조합으로
지원 모델을 선택하고, 머리·손 모자의 원본 geometry와 native material을 따로 확인한다.
WModel 단위, CModel preScale, body bone basis, WORLD anchor의 정규화 여부를 함께 측정해
배율을 한 번 적용한다. 손 모자의 실제 owner instance와 visible lease에만 기본 머리 모자를
숨기는 권한을 주며 종료·seek·해제 시 복원한다. DURATION 유지 구간은 기존
ATTACHMENT_HOLD 의미를 사용하고 시간만 있는 미지정 logic을 게시 성공으로 취급하지 않는다.
설치·일반/앵콜/시퀀스 지원과 화면 확인 경계는
[모자 복원 결과](09-17/2026-09-17_SAYDON_HAT_RESTORATION_RESULT.md)에 둔다.

### 무력화 별·외곽 경계의 원본 역할 확정과 선 그리기 이식 (2026-09-17)

- 원본 4219945 stage 5는 clip 2.667s보다 긴 8.1s particle 창을 갖는다. Sk_12_2가 반경 8.25m 꼭짓점 카드,
  Sk_12_9가 LocationDirect tracer 5개로 V5→V1→V4→V2→V3→V5 변을 각 0.625s에 그리고, Sk_12_6/7/8이 r9~10m 링·
  r7~8m 반짝임·r3.4~5.9m 바닥 decal(외곽 경계), Sk_12_4(4.1s 시작, 5.85s 페이드 인)와 Sk_12_3(6.116s 두 번 섬광)이
  완성된 별, stage 6의 Sk_12_5가 중앙 15m 폭발이다. 설치된 source 문서는 emitter delay·burst를 recipe에 보존하므로
  그룹 문서는 시스템 시작 오프셋만 더한다.
- tracer(`enginematerials.defaultparticle`)와 `efparticlemodulelocationemitter` follower는 런타임에 없다. tracer의
  11표본 곡선을 follower 요소의 `sourceTransformTrack` positionKeys(UE3 cm)로 옮기고 `localSpace=false`로 두면
  태어난 입자가 `SpawnRootWorld`에 남아 선이 그려진다. CPU probe에서 다섯 변 모두 coverage 0.01→1.00, 측방 오차
  ≤0.10m, 0/1/2/3/4초 순서를 확인했다.
- 별 위치 폭발은 원본에 없다(PROJECT_AUTHORED). Sk_12_5 핵심 7 emitter를 꼭짓점마다 `sourceScale.size .35`,
  `count .6`으로 반복했으며 별도 sub-group이라 Tool에서 지울 수 있다.

### 발탄 도끼 리본을 저주의식 방식으로 (2026-09-17)

- stage009의 원본 Par_O_RPBF_Trail_01 리본은 rate 50/s만 있고 SpawnPerUnit이 없어 60Hz 표본 위치에 점이
  뭉친다. 저주의식과 같이 SpawnPerUnit(unitscalar 50, emitter_20/22 값)·rate 0·`bspawninitialparticle`로 바꾸면
  실제 본 이동 거리마다 점이 생긴다. 합성 StartControl 이동(7.5m/s) probe에서 0.533s 창에 8점, stage008 세 창에
  4/3/3점이 생겼다.
- native 2410(flowrib)은 `v2.x`를 0..1 길이 gradient, `v2.zw`를 tiled panner로 읽으므로 2379와 같은
  `(coverageUV, distanceUV)` packing이 필요하다. `Render_Trails` coverage 목록과 `Resolve_NativeTrailUV`에 2410을
  추가했다. WARP pixel parity는 실행하지 않았다.
- 버러지 sprite(par_n_rpbf_cast_exp_01 sprite 9종)는 왼손 socket 대신 StartControl/b_wp_r_01 socket 변환을 복사해
  붙이고 rate 60/s(1 tick 1개)·40cm 반지름 1.5회전/s `sourceTransformTrack` 원 궤도로 공전시켰다. `revolutionDegreesPerSecond`는
  요소 자체 회전이라 공전에 쓸 수 없다.

### 발탄 사전 생성 Sprite 검격과 스윙 Trail의 제품 연결

원본 notify 시간만으로 carrier를 단정하지 않고 ParticleSystem의 emitter 종류를 확인한다.
사전 생성 에너지와 스윙별 Trails는 독립 occurrence다. Full Restore에 모두 있어도 제품
cue가 다른 authored clip을 참조하면 제품에서 누락될 수 있다. 실제 cue→asset→sourceNode와
native material·socket 연결을 기준으로 판단한다.

Full Restore의 source particle을 제품 문서로 옮길 때 m 단위 StartSize와 설치 모델의
combined bone basis를 함께 확인한다. 정상 발탄의0.01 basis는 기존 공용 helper에서 한 번
정규화하고 측정된 유령 발탄 unit basis는 유지한다. 다른 효과에 전역 scale을 전파하지 않는다.
현재4연속 공격 제품clip01/02에 Atk_02_08의5종 Sprite를 연결했으며 기존 저작 스윙·충격·
후처리를 보존했다. native Trails4개 및19개 최종 폭발은 이번 연결 범위가 아니다.
[Sprite 복원 결과](09-18/2026-09-18_VALTAN_FOUR_SLASH_SPRITE_RESTORE_RESULT.md)에서
설치 데이터·컴파일·게시와 사용자 최종 화면 판정을 구분한다.


### 캡처 지점의 공유와 시작점 기준 그룹 회전

같은 시각의 SELECT_PLAYER/SELECT와 APPEAR는 기존 airborne 캡처 지면을 공유할 수 있다.
선택된 Effect group은 publisher가 fixed visual template으로 바꾸고 Preview와 Server Play가
각자의 같은 선택 owner를 소비한다. 보스 공중 Transform을 장판 지면 anchor로 대체하지 않는다.

Current Effect의 captured-root 그룹도 local Transform을 편집할 수 있다. 그룹 또는 멤버 하나를
공통 anchor 원점/custom pivot 주위로 회전하면 위치·방향·선형 운동을 같은 delta로 변환한다.
Pivot 선택은 편집 세션이며 결과는 기존 Element TRS에 저장한다. 거미 바닥처럼 원본 pivot이
한쪽 끝인 fixed-axis sprite는 기존 followEmitterAxisRotation을 해당 carrier에 연결해야
최종 quad 전체가 방향을 따른다. source basis와 world birth root를 한 번씩만 소비하며 body-follow
다른 그룹에 전파하지 않는다. 원점/사용자 pivot의 수치 검증과 최종 화면 판정은 구분한다.
구체적인 수정·검증·반영 상태는09-17 세이튼 구현 RESULT G32를 따른다.

### 고정축 Sprite의 quad와 native emitter 좌표계

native shader가 world position을 SourceEmitterWorld 역행렬로 바꾸는 재질에서는 quad의
방향 소비도 함께 점검한다. quad만 world fixed axis로 남으면 occurrence/Element 회전이
내부 무늬에만 반영된다. 배치 방향을 따라야 하는 정확한 emitter에 기존
followEmitterAxisRotation을 연결하여 source pivot을 포함한 quad와 재질 frame을 일치시킨다.
거미 notify008의 주 검은 띠2개와 동반 바닥3개, 같은 source-library 문서에 연결했다.
설치·CPU quad 검증과 사용자 화면 확인은 G32 RESULT의 실제 반영 항목으로 구분한다.

### 위치 track에 의도하지 않은 alpha owner를 만들지 않는다

sourceTransformTrack에서 alphaScaleKeys는 optional이다. 빈 배열도 현재 Codec은
AlphaScale 분포로 생성하며 기본0으로 평가한다. 위치 orbit만 만들 때는 이 필드를 생략해야
원본 입자 alpha over life를 보존한다. 생성 수와 quad 행렬이 정상이더라도 최종 Color.w가
0일 수 있으므로 함께 검사한다.420609 Full Restore의 도끼 주위 후속 Sprite36개에 적용했고
원본5개 Sprite·제품 carrier와 구분했다. 세부 결과는09-18 Sprite 복원 RESULT G06을 따른다.

## 발탄 Sequencer transport·환경 occurrence 연결

발탄 Full Restore의 Play All/Solo/Group은 원본 clip occurrence와 source anchor history를
기존 EffectAuthoringSequencer에 전달한다. 모델 자체 자유 재생과 별도 UI cursor를 함께 실행하면
Pause와 timeline seek가 효과에 전달되지 않으므로 CModel을 명시적으로 sample하는 단일 clock을 유지한다.
arena root scale과 실제 model bone basis 배율을 혼합하지 않는다.

Stage별 Scene Profile/Light는 기존 공통 presentation sampler·profile lease·frame light provider를,
Camera는 기존 cinematic document/controller를 소비한다. Save된 source, published binding, 실행 중
preview draft와 화면 판정은 구분한다. 실제 연결 범위와 검증은
[발탄 Composition 재개 결과](09-09/2026-09-09_VALTAN_COMPOSITION_AUTHORING_PARITY_RESULT.md)를 따른다.

### World Object에 붙인 finite source의 반복

World Effect track과 Composition V1 occurrence는 `loopEffectToDuration`을 opt-in해 원래 source 속도를 유지한다. native loop0는 기존 bounded emission end, finite source는 prepared 전체 duration마다 별도 occurrence를 사용한다. 각 occurrence의 source 시계는 0부터 시작하고 Object follow 시계는 누적 나이를 유지한다. 원본 사용자 scale·삭제 element·bloom과 prepared resource identity는 그대로 보존한다. 박스 끝의 handle 종료와 Object motion epoch 반복은 서로 다른 수명 소유자다. 날짜별 복구·검증 증거는 09-18 KOUKU_WORLD_BLADE_REPAIR_RESULT를 따른다.

### Composition 본 앵커와 Trail carrier

Composition의 본 선택은 실제 CNpc Preview/replication 소유자가 제공하는 BODY/WEAPON 모델을 소비한다. 전역 Animation Tool target은 동일 profile일 때의 보조 경로다. BONE pivot은 실제 본의 정규화된 축과 위치를 사용하므로 root 기준 offset/yaw를 그대로 복사하지 않는다. 원본 socket 이름은 해당 action notify와 실제 skeletal source에서 확인하고, 서로 다른 source action의 socket을 추정으로 연결하지 않는다.

동일 native Trail carrier의 한 색상만 어긋나면 occurrence 전체를 이동하기 전에 해당 element의 source StartLocation과 initialPosition 및 segment clipping을 정상 carrier와 대조한다. shared lifetime과 다른 색/재질/attachment를 유지하면서 잘못된 element만 수정한다.

### 긴 Pattern Preview의 과거 transform 재사용

외부 clock Effect의 fixed-step provider가 동일 배우의 과거 회전·이동을 요청할 때 배우별 완료된30Hz event checkpoint와 최종 시점 pose를 재사용한다. checkpoint는 같은 시각 stage→tracking 순서를 모두 완료한 상태이며 yaw·clip별 yaw·누적 follow offset을 함께 보존한다. observed target은 기존 최초 관측 정책을 유지하고 미관측 미래는 거절한다. 배치/문서 수명이 바뀌면 cache를 무효화하며 정지 clock의 memo는 임의 timestamp 누적으로 커지지 않도록 제한한다. 이 최적화는 source 입자 수명·source socket·bone basis·GPU carrier를 바꾸지 않는다. 실제 적용과 native probe, 사용자 capture 수치는09-18 KOUKU_PATTERN_RUNTIME_REPAIR_RESULT 후속 절에 둔다.

### 2026-09-19 Release 4인 실측 보정

Kouku의 조명·트레일 admission 상한은 실제 정상4인 사용량보다 작았다. G12의 Level별
증가만으로는 Bern·원격 Alt+V·정상 재사용 tail의 임의 거절이 남았다. G13은 전체 Level과
owner/local/remote의 whole-effect admission ceiling을 제거하고 비용 telemetry와 uint overflow,
문서·리소스 유효성 검사를 유지한다. Engine provider/light/post/overlay의 frame 합계 제한과
Map/Kouku의 선행 light truncation도 제거했다. shader400개 배열은 같은 순서의 여러 batch로
사용한다. 문서 전체 보수 예약, 실제 동시 GPU 개수, persistent scene-light 계약은 구분한다.
수치와 비시각 검사 근거는09-18 KOUKU_RAID_COLLIDER_SOUND_INTEGRATION_RESULT G13을 따른다.

원격 local-only visibility sidecar가 없는 element ID를 지목하면 전체 prepared effect spawn이
rollback된다. 현재 catalog1167개에 연결된 sidecar를 함께 검사했고 Artist31930의9개와
Lance34630 clip1의6개 stale 참조만 정리했다. 원본 effect 요소·수명과 유효한 local-only
visibility는 보존하며, 이 결함을 모든 보스 표시 누락의 원인으로 확대하지 않는다.

컷씬 CAMERA/SOUND의 고정 WORLD 좌표와 named World Object는 다르다. P73~77의57행은
worldId 없이 정상 게시되지만 Product reader가 불필요한 sequence ID를 강제해 전체85개
패턴 로드를 중단했다. named World만 join하고 실제 WORLD Effect/Light/Collider의 ID
검사는 보존한다. row parse와 전체 Product stage/commit의 성공을 따로 검증한다.

쇼타임 총구의 targeted source-boss는 일반 animation lane이 없는 독립 표시다. 명시된
SourceModelPreview의 clip·source 시간은 기존 Sample_SourceAnchorWorlds가 Effect-local
clock으로 소비한다. 일반 pattern의 authored lane과 독립 effect의 source animation을
혼동하지 않으며, 본 부착 실패가 묶인 다른 표시까지 정지하는지 로그에서 함께 확인한다.

Bern waterfall mist의 local roll×nonuniform owner는 유효한 affine shear지만 기존
XMMatrixDecompose 실패가 whole-effect isolation으로 이어졌다. sprite billboard에 필요 없는
source quaternion 분해를 없애고 실제 축 길이와 원점을 사용했다. camera/axis orientation,
pivot, native source scale과 다른 carrier는 유지한다. TRS72건 행렬동일·shear4건 수치통과와
사용자의 실제 GPU 화면 판정을 구분한다.

팝업북 Sequence P1/P4의 WORLD 6행씩이 camera/scene보다2.852초 먼저 끝나던 lifetime을
각 scene 종점까지 연장했다. Resources·shader 일괄 변경이 아니라 Sequence65의12필드 수정이다.

### 쿠크 제품 World 생성과 최초 위치 이력

월드 오브젝트가 Preview에서는 존재해도 제품 reliable PLAY를 비동기 V1 준비 중에 버리면
공·칼날은 생성되지 않는다. 연결된 group/NEXT motion의 V1을 먼저 준비하고 수명 안에서
pending cue를 재시도한다. 실제 asset 실패는 따로 기록하고 birth를 기다리는 후속 motion도
같은 cue identity를 유지한다.

Mario parent와 후속 칼날·갈고리 패턴은 run/member가 같을 수 있다. 종료를 pattern sequence에
한정하고 전체 run 종료와 구분해야 다음 패턴의 정상 PLAY를 이전 종료 기록이 거절하지 않는다.

제품 첫 snapshot은 패턴0초 뒤에 올 수 있다. 첫 관측 authoritative pose를 해당 새 occurrence의
초기 birth 구간에만 제공하며 실제 과거 위치를 복원했다고 주장하지 않는다. 이후 관측 이력과
teleport·missing-history 거절은 유지한다. 긴 시계에서 ms→float seconds의 서로 다른 계산이
수 μs 차이를 만들므로 endpoint 비교는 float 정밀도를 반영하고 실제 미래 frame을 허용하지 않는다.
수정·비UI 검증·사용자 화면 확인은09-18 쿠크 패턴 재생 복구 RESULT G09 이후를 따른다.

### 쿠크 조명 A/B와 유령 몸체 검증 경계

- Kouku Rendering restoration의 Imported source lights는 2026-09-11 import의115개 stable ID reference를 현재 receiver(UNBAKED84/ALL31), 관문·팝업 변환으로 소비한다. Current와 비교해 추가 저작 조명의 영향을 분리하며 Scene/Effect light와 tone/LUT는 별도 입력이다. 세션 선택을 저장 문서 변경으로 취급하지 않는다. source reference만 Client96.DataFiles/Rendering의 None으로 노출한다.
- 원본115개 수치가 float32 기준 그대로라면 임의 LUT/광도 보정보다 추가 저작 조명·profile·environment region 입력을 분리해 비교한다. 원본에서 LUT override가 꺼져 있으면 텍스처 존재만으로 LUT를 강제 활성화하지 않는다.
- 유령 발탄 body program84/pass10의 실제 설치 WModel·material·preScale 검사에서 픽셀이 나와도 live renderer group·Server phase·visibility 성공을 대신하지 않는다. Full Restore의 normal BOSS_VALTAN 선택과 실제 아레나 ghost group을 구분한다. shader/alpha 변경은 해당 실패 경로의 근거가 있을 때만 적용한다. 수치·화면·미확정 범위는 09-18 KOUKU_PATTERN_RUNTIME_REPAIR_RESULT G09에 기록한다.

### 발탄 baked-edge trail의 sourceRecipe shape 복구

`animationTrailBakedEdgeV1` native variant는 비활성 SourceRecipe의 rendererShape도 material contract에 포함한다. ordinary effect에 recovered material을 설치하고 과거 sprite metadata를 남기면 문서 전체가 admission에서 실패한다. carrier·program·stable element ID를 확인해 shape만 animationTrail로 맞추고 기존 validator·alpha·history를 보존한다. 발탄420633 native2377/2378/2379의3개 field 교정은 실제 codec 실패→승인, product load-stage,3trail/157edge playback까지 검증했다. 유령 몸체 program84나 실제 GPU 화면 성공과 구분한다. 개별 값·receipt·미확인 범위는09-18 KOUKU_PATTERN_RUNTIME_REPAIR_RESULT G09에 있다.

### 쿠크 원본 표면·구역별 렌더링의 재사용 절차

발탄의 성공은 LUT 이름 하나가 아니라 sourcePostProcess·native material·환경 입력과 프로젝트 조정값을
실제 consumer에 연결한 결과다. Kouku는 원본 volume 46/47의 LUT override가 활성인데 이름 whitelist가
LUT02/01을 버렸던 경로를 확인했다. 두 텍스처는 원본 패키지 raw DDS와 독립 UModel TGA 픽셀까지 대조한다.
LUT02가 중간 밝기를 높이는 성질도 있으므로 LUT 복구와 과도한 조명 제거를 같은 의미로 쓰지 않는다.

Scene alias의 실효 quality는 해당 alias override 또는 Level base quality, alias multiplier,
카메라 environment region 순서로 결정된다. 파일의 globalQuality만 보고 실효 gamma/노출을 단정하지 않는다.
사용자가 저장한 현재 조정값과 원본 비교용 기준을 구분하고, 시작·관문·카드미로의 source volume을 실제 위치로
연결한다. 카드미로의 현재 결과를 유지하는 override는 원본 복원값으로 오기하지 않는다.

원본 DDLExcludeVolume은 시작·1~3관문과 카드미로 사이의 directional 입력 차이를 설명한다.
현재 scene-region을 통한 diffuse/specular 제외는 구역 단위 복구이며 receiver별 convex shader clipping과
동일하지 않다. 원본 character SH 밝기 계수도 uniform map ambient로 곱하지 않는다.
바닥 family1/2의 native Blinn power30/100과 기존 Phong 소비 차이는 해당 유효 specular marker만 수정한다.
1·2관문의 BG8 RNM 재질, 발탄 및 다른 native carrier로 이 보정을 전파하지 않는다.

스킨 모델은 실루엣과 표면 shading을 구분한다. 쿠크/세이튼/큰 세이튼/지팡이/망치는 설치 삼각형이 원본
LOD0와 같아도 ActorX 경유 면 법선 때문에 각져 보일 수 있다. 원본 NORMAL/TANGENT와 tangent.w를
모든 triangle corner에 대조해 복구하고, WINT1.5의80-byte 저장 정점을 기존 CModel 경로에서 소비한다.
원본에 없는 subdivision이나 가짜 smooth normal을 생성하지 않으며 실제 원본 실루엣의 각은 남을 수 있다.

쿠크 Release의 V1/V2·World 준비는 실제 재생 문서의 전체 dependency를 Loading/Level initialize에서 닫는다.
Debug는 클래스·marker·BossCatalog의 기존 준비를 유지하고, 추가된 전체 Pattern/Sequence/World 준비는
기존 on-demand 경로에서 처리한다. Debug 첫 재생의 준비 지연까지 사라졌다는 의미는 아니다.
V1 worker는 이미 병렬이므로 worker 추가 전 같은 corpus의 실제 CPU/GPU 비용을 따로 측정한다.
느린 Effect 로그는50ms 이상만 기록하므로 그 interval을 전체 closure 또는 총입장 시간으로 부르지 않는다.
`Kouku.Loading`의 Begin/Collect/V1Settled/V2Prepared/WorldPrepared/GateReady와 `Kouku.Arena.Ready`로
실제 설정과 경계를 구분한다. 변경과 실측은09-20 KOUKU_PUBLISH_AND_EFFECT_LOADING RESULT를 따른다.
Release/Debug CSO 사전 생성, CPU 문서 로드, GPU resource 생성, 발생별 mutable instance allocation은
서로 다른 단계다. 준비 성공을 실제 전투 프레임 무끊김이나 사용자 화면 PASS로 대신하지 않는다.
source/후보/배포/검증 구분은09-19 KOUKU_RENDERING_QUALITY·KOUKU_BOSS_SOURCE_BASIS RESULT와
09-18 KOUKU_PATTERN_RUNTIME_REPAIR RESULT G10에 기록한다. Resources에는 최종 DDS/WModel만 설치하고
추출 glTF·UPK·DXBC 및 중간 보고서는 out에 둔다.

## Stage 밖 presentation 수명과 쿠크 복구 소비 경계 (2026-09-20)

쿠크 Composition의 Stage는 보스 진행을, Effect/Sound/World/Logic/Summon row는 자신의 수명을 소유한다. projector가 긴 row에 맞춰 마지막 Stage를 자동 연장하지 않는다. Client는 자연 완료 세션의 실제 sound/effect 핸들과 원래 시작 tick을 보존하고, Server는 이전 occurrence의 정의·판정 ledger를 유지한다. 명시 Stop·수동 새 run·죽음·관문 전환은 잔여 수명도 종료한다. 자동 RaidFlow 다음 Entry는 같은 epoch의 잔여 행을 유지한다. 발탄 V2의 `stopWithClip=false`도 이전 Stage effect의 실제 경과 시간을 소비하며 새 Stage age를 사용하지 않는다. editor Save/reopen, publisher admission, 실제 Server consumer와 화면 검증은 구분한다.

G1 밝기 수정은 두 scene alias의 실효 노출을 2에서 1로 조정하고 카드 9 leaf의 scene bloom 기여를 분리한 범위다. source LUT는 유지했고 prebaked light 또는 gamma 중복으로 확정하지 않았다. optional V2 `sceneBloomScale` 기본 1, 범위 0..1은 scene bright-pass RGB만 제어한다. 연출 fog override는 활성 camera가 소유하며 종료 후 기존 region 평가로 돌아간다. G2 암전 구간은 해당 shot에서만 character receiver directional을 복구한다.
후속 G1 전투 화면에서 사용자는 과광 해소와 세이튼 암부 악화를 보고했다. native character의 현재 ambient adapter는 direct diffuse에 곱해져 G1 directional0에서 함께0이 된다. 원본 environment의 CharacterLit/ShadowedIndirectBrightness2.5는 map ambient에서 제거됐으나 캐릭터 전용 SH 입력으로 복원되지는 않았다. 따라서 map/character의 노출을 같이 낮추는 조정과 캐릭터 간접광 누락을 별도 검토해야 한다. G1에만 독립 간접광을 공급하는 수정 방향을 확인했으며 이번 진단에서는 구현하지 않았다. world baseline×2.5 uniform 근사는 PROJECT_TUNED이고 원본 SH/probe 복원으로 기록할 수 없다. 실제 수식·원본 export·사용자 관찰·남은 수정은 09-20 KOUKU_RENDERING_OBJECT_REPAIR_RESULT G06에 기록한다.

World Object 중앙 앵커 이동은 연결된 motion instance들에 같은 delta를 적용하고 preview placement override를 해제한다. 실제 collider·끌림 검사는 저장된 Pattern의 기존 Server audition을 사용한다. finite V1의 명시적 `loopEffectToDuration`은 방출 주기로 새 cycle을 생성하고 particle tail은 겹쳐 유지한다. 휠윈드 망치, 카드미로 연출 망치, 플레이어 카드미로 망치의 Transform은 각자의 occurrence/profile을 소유한다. 연출 망치의 bone은 실제 표시되는 WorldSequence CModel에서 resolve한다.

빙고 폭탄은 원본 1배 본체·심지·폭발을 별도 asset으로 복구하고, 망치 선행/메두사 얼굴은 원본 source curve와 native material을 사용한다. 거미 counter 피격 얼굴은 camera-local screen source의 12 emitter와 크기 곡선을 사용하며 반복 cadence만 PROJECT_TUNED다. 빙고 정상/완성 표시는 보드 원본의 flip/normal hold/bingo hold를 기존 WorldSequencePlayer에 연결한다. 각 원본 식별자·수치·compile/codec 증거와 미검증 화면 항목은 09-20 대응 RESULT가 정본이다.

### 2026-09-20 ALT V 캡처 큐브와 원본 연출 추가 연결

ALT V의 기존 frozen Color/Bloom은 유지하고 native178 normalized UV adapter의 중복 중앙 이동만 재기준화했다. 두 채널의 square crop은 같은 helper를 사용한다. 원본 액자17/12/58/61/62의 mesh/native material을 기존 carrier로 그리며 기존45도·네 방향 수축·속도 저작값과 화면 평면 배치를 연결했다. tuning은 기존27행을 보존하고 빠진12/58만 추가했다. 이 좌표계와 연출 제어는 PROJECT_TUNED이며 숨김 capture18/31이나 미해독 원본 camera CB를 복원한 것으로 확대하지 않는다. 실제 native 함수의 WARP UV 검사, 설치 원본 액자 CModel 배치 검사와 전체 게임 화면 판정은 구분한다. 상세 수치는 09-20 DIMENSIONMASTER_ALTV_CUBE_HANDOFF_RESULT를 따른다.

원본 PlayParticleEffect의 null base와 model modifier는 별도 typed field다. Valtan Respawn_1의 MN_RPBF_02 modifier를 원본 byte 위치로 해독하고 기존 alias를 덮어쓰지 않은 명시 cue/clip join으로 연결했다. 발탄2페이즈는 source420629의 Event02→SCENE02A/52이며 Event01의 SCENE06A/55와 구분한다. 카메라·actor5.5초 복구를 위한 RECOVERY1830ms 증가는 사용자 요청 원본 컷씬 길이 복구이며 긴 FX가 Stage를 강제로 늘리는 일반 정책이 아니다.

발탄 입장 actor64는 Scene의 본체·무기 dead MIC를 명시 연결했다. LookInfo의 일반 MIC와 메시 기본 ghost MIC는 이 장면의 정본이 아니다. native92/93은 실제 Base/Light 함수·packing을 복원하며 source Light varying v2/3/5/6을 해당 프로그램에만 연결했다. 기존 native 함수와 Engine/Client shader mirror를 보존했고 실제 catalog 소비 검사와 별도로 최종 게임 화면 판정이 남는다. 상세 증거는 09-20 VALTAN_ACTOR64_NATIVE_MATERIAL_RESULT를 따른다.

### 2026-09-20 후속 회귀의 실제 연결 범위
G1 SOURCE_CHARACTER의 선택적 sourceCharacterAmbient를 독립 입력으로 연결했다. 기본0/native-map제외이며 원본 계수2.5의 uniform 환경 근사다. 원본 SH 복원으로 기록하지 않는다. Mario1~4는 자체 exposure1 프로필을 쓰며 local phase동안 fog만 억제한다. G3 dark alias exposureMultiplier.5는 G3 전체 look에 영향을 주고 무지개댄스 P38의 nativeHDR/bloom1.3는 유지한다. explicit effect bloomIntensity는 scene intensity와 선택되므로 alias bloom0↔1만으로 모든 효과의 밝기를 설명하지 않는다.
RenderingProfiles가 사용하는 LUT는 기존 `Map/Lighting/KoukuSaydon/`의2DDS로 통일했다. 사용자가 이전한파일의hash만확인하고 재생성하지 않았다. 주사위 P78은 머리 카드V2와다른 V1 card.match4종+emit을 소비하므로 그5문서의 bloomIntensity0을 별도로 연결했다.
ALT V는 capture/image/frame/cube에 같은 camera-relative rig를 적용하고 기존 screen Transform tuning이 실제 endpoint에 전달되게 했다. captureUseModelCenter=false는 같은 깊이의 화면 중심이며 true는 명시적인 월드 모델 중심이다. 실제CModel/bone 검사와 최종사용자화면을구분한다. 09-20 DIMENSIONMASTER 관련RESULT와 RAID_PRESENTATION_REPAIR_IMPLEMENTATION_RESULT G09를참조한다.
body-follow 이펙트가 회전 구간에서 꺾여 보이면 emitter 회전뿐 아니라 부모 캐릭터의 모델 전방과 이동 방향을 먼저 대조한다. Kouku 거미 돌진은 실제 설치 clip의 head→mouth 전방이 +X인데 Server charge yaw에 +90을 더해 몸체가 이동 반대를 향했다. 해당 Logic의 authored 보정만 −90으로 바꾸고 world 이동 벡터와 FX 회전을 유지했다. 실제 clip 샘플·8방향 전방 내적·동반 collider 영역을 검사하며, 수치 성공과 사용자 화면 판정을 구분한다. 상세 증거와 영향 범위는 09-20 RAID_PRESENTATION_REPAIR_IMPLEMENTATION_RESULT G11을 따른다.

### 쿠크 방향광과 시퀀스의 동일 환경 입력 (2026-09-20)

사용자 요청으로 정상 관문의 directional을 복구할 때 노출1과 source LUT는 유지한다. scene profile에도 기존 UNBAKED 수신 계약을 전달해 RNM/native baked map에 직접광을 중복 가산하지 않는다. region의 명시receiver는 profile 기본값과 별도로 보존하므로 정상 카드미로/Mario의 ALL을 유지할 수 있다. 특정 camera shot만 character-only light로 덮는 임시 보정보다 같은 profile/region을 시퀀스와 전투가 소비하게 한다. native character 독립 ambient와 기존 diffuse×ambient의 합을 계산해 밝기 조정 범위를 기록하고 과거2배 exposure의 pixel 일치를 주장하지 않는다. 상세 설치·검증·화면 미확인 경계는09-20 KOUKU_RENDERING_OBJECT_REPAIR_RESULT G08에 있다.

### Rendering Workbench에서 방향광·노출·LUT를 분리해 비교

Recovered map materials 위의 Live rendering comparison은 direction diffuse/specular, exposure 0.5~2.0 slider (0.001 precision)배, LUT grading, FXAA, Bloom을 독립적으로 전환한다. 노출 배율은 이미 해석된 현재 profile/region 입력에 한 번 적용하며 LUT 두 번 실행을 뜻하지 않는다. 방향광을 끌 때 ambient와 캐릭터 독립 ambient는 유지한다. 저장되지 않는 비교 옵션이므로 Reset, Workbench 닫기, Level 변경에서 원래 값을 복원한다. camera-region 품질 override가 있는 Mario에서도 비교가 마지막에 적용된다. 현재 화면 전체의 grading과 특정 native character 조명은 다른 범위이며, 캐릭터만 밝게 한다는 이유로 공통 LUT를 중복 적용하지 않는다.


### 연출 사운드·자막의 원본 복구와 World 소비자

쿠크 연출 텍스트는 기존 Composition SUBTITLE resource/occurrence, 발탄 연출은 기존 WorldSequence soundTracks/subtitleTracks가 정본이다. 한국어 GameMsg 원문과 원본 시간으로 연결하며 PNG나 새 font를 만들지 않는다. 발탄 BALLOON은 해당 instance의 실제 visible 배우 모델 bbox 상단을 투영한다. 원문 추출과 제품에서 연결된 연출은 별도로 기록한다.

AkEvent 복구는 원본 Event→Action→layer/random media, 시작 지연, Stop/fade를 확인한 뒤 Resources 상대 Sound 경로를 사용한다. 이미 전체 음원이 설치된 경우 occurrence 수명만 고치고 같은 WAV를 중복 생성하지 않는다. 기존 Music owner가 있는 BGM은 World scene sound와 중복 연결하지 않는다. 시각 연출보다 긴 audio tail은 자연 종료 시 기존 SoundCue handle만 남기며 카메라/전투 잠금 시간을 늘리지 않는다. 명시 Stop/Seek/Level 정리는 해당 owner handle을 해제한다. 재생 중 continuous sample과 사용자 scrub의 시계 계약도 같이 점검한다.

빙고 입장 화면 균열은 SCENE07A의 PostRenderMaterial 원본 DDS/shader/재질/파편 시점을 추출한 상태다. 기존 V1 파편 참조의 존재 확인은 새 화면 shader carrier 연결이나 GPU 표시 성공을 뜻하지 않는다. 실제 적용 범위와 원본 hash는09-20 KOUKU_BINGO_GLASS_EXTRACTION_RESULT 및 KOUKU_RESOURCE_AND_SUBTITLE_INVENTORY_RESULT를 따른다.

일반 Preview의 내부 누적 시계는 double 소수 밀리초를 유지한다. UI 정수 cursor나 capture resolver의 unchanged 정수 결과를 매 frame 되쓰면 시각 연출이 느려지고 독립 SoundCue와 누적 오차가 생긴다. 외부 clock·명시 Seek·실제 capture 경계 이동은 기존 지정 시각으로 반영하되 일반 sample은 내부 분수부를 보존한다. 단일/묶음 경로와 장시간 여러 FPS 검사를 함께 수행하며 저장 필드 오류와 시계 오류를 구분한다. 실제 적용·검증·미해결 경계는09-20 RAID_PRESENTATION_REPAIR_IMPLEMENTATION_RESULT G21을 따른다.

편집 문서와 실행 중 immutable Preview도 구분한다. 변경 저장 뒤 이전 local Preview를 중단하고, Scrub/Resume는 draft generation이 다르면 새 문서를 준비한다. 새 문서의 준비 실패 뒤 이전 sound handle이 계속 흐르거나 Resume되지 않도록 기존 STOP 경계까지 소비한다. 이는 모든 제품 음원을 mute하는 정책이 아니며 Server의 고정된 run과 자식 Pattern의 합성 음원은 별도 owner다. 일반 Play·저장 후 정리·실패 후 Resume의 실제 검증은 같은 RESULT G22에 기록한다.

### 서버 공포 얼굴과 조건부 SOUND의 현재 연결

쿠크 FEAR result의 optional soundResourceId는 기존 SOUND 리소스를 참조하고, Client는 서버가 확정한 로컬 플레이어의 FEAR 시작 tick·종료 tick·presentation ID로 얼굴과 같은 effectDelayMs에 한 번 재생한다. snapshot 반복과 화면 particle 반복은 새 audio occurrence가 아니다. 사망·FEAR 종료·연결 정리는 기존 session의 SoundCue handle을 해제한다. 회피자·카운터 성공자에게 조건 없이 발음하는 일반 패턴 timeline과 구분한다.

거미 공포 buff의 원본 참조는 Darkness 재질과 ScreenPP particle이며 직접 AkEvent는 없다. 같은 돌진 동작의 원본 보이스를 얼굴에 연결하는 것은 사용자 요청에 따른 PROJECT_TUNED다. shader·carrier·particle 크기와 반복 정책은 변경하지 않는다. 자산·적용·검증 경계는 09-21 KOUKU_SPIDER_PLAY_AND_FEAR_SOUND_RESULT를 따른다.


### 컷씬 배우의 별도 geometry basis

복구한 Character 모델을 material source로 참조하는 worldsequences 배우도 독립된 `modelAssetId`의 WModel geometry를 소비한다. 재질 참조만으로 원본 smooth normal·tangent·handedness가 전파되지 않는다. Gate2Intro, Gate2 maze, Gate3 intro, Bingo encore의 파생 배우는 원본 glTF indexed corner를 대응해 basis만 복구하고 위치·UV·skin·index와 재질·골격·컷씬 animation section을 보존한다. WINT1.5 native decoder 통과는 실제 GPU 표시의 사용자 승인과 구분한다. 후보·설치 hash와 범위는09-21 KOUKU_SAFE_ZONE_CHARGE_CUTSCENE_RESULT에 기록한다.

컷씬을 기존 보스가 수행할 때는 동일 골격 animation set을 기존 CModel에 추가하고 원래 WORLD 배우를 중복 생성하지 않는다. WORLD translation과 suppression 전 b_root를 합친 절대 경로를 사용할 경우 body root의 수평·수직 변위를 다시 더하지 않는다. 무대 밖 연출은 일반 navigation 이동과 구분하며 기존 actor·무기 socket의 실제 world pose를 대조한다.

눈 이펙트는 설치된 좌우 눈 본을 각각 사용하고 BONE pivot의 축을 정규화해 모델 preScale을 이펙트 크기에 다시 곱하지 않는다. source UE velocity의 좌표 변환과 occurrence 회전을 구분한다. V1 원본을 재사용하는 별도 occurrence의 이름·부착 보정은 원본 recipe의 전역 수정 근거가 아니다. 이번 후보와 실측·화면 확인 경계는 `09-21/2026-09-21_KOUKU_PATTERN_FACING_CARD_MAZE_IMPLEMENTATION_PLAN.md` 및 대응 RESULT에 기록한다.

### V2 화면 효과를 V1에 복사할 때의 소비자 확인

ScreenPost는 원본 profile과 intensity 시작·종료값뿐 아니라 lifetime과 보간 방식을 실제 V1 codec/playback/Engine 제출까지 확인한다. V1의 optional `intensityLerp=false`, `intensityEnd=0`은 기존 source dynamic/alpha 곡선의 우선순위를 유지한다. 색수차는 기존 Engine 프로필을 재사용하고 전체 asset·기존 mesh/particle 요소에 화면 보정을 전파하지 않는다. 수동 복사 요소에 실행하지 않는 source recipe를 원본 실행 복원처럼 붙이지 않는다. CPU 시간별 출력 일치와 사용자의 실제 GPU 화면 판정은 구분한다.

### 차원술사 Q CubeSample의 선택적 가독성 보정

CubeSample의 배경 의존 색은 alpha blend 이전에 `lerp(5*C^5,C,facing)`로 계산된다. 밝은 환경에서 본체와 모서리가 tone shoulder로 함께 압축되는 문제는 문서 Bloom 강도와 별도로 다룬다. 기존 재질 profile의 named scalar `project_clarity_strength`(생략/0은 기존 계산, 유한한 0~1)만으로 보정하며 전역 rendering profile·Bloom·Resources를 바꾸지 않는다.

사용자 화면 판정에서 배경 본체와 caustic을 모두 압축한 첫 보정은 밝은 금색을 잃었다. 현재 보정은 배경 transmission만 제한하고 원본 particle의 정규화 chroma를 흡수와 독립 면 발광에 사용한다. caustic과 edge는 원래 식을 유지하며 자체 발광은 제한 밖의 HDR로 보존한다. 밝은 배경의 중간 alpha coverage를 강화하되 alpha0/1과 원본 clip을 유지한다. 자체 발광은 SceneColor/Bloom/black의 세 평가에서 같아야 기존 per-effect Bloom 추출이 실제로 반응한다. 억제된 배경 Bloom을 HDR에서 재생성하지 않는다. 이는 원본 식의 추가 복원이 아닌 PROJECT_TUNED 저작 옵션이다.

첫 적용 대상은 Q `2050100`의 restore 및 실제 animevent가 사용하는 tuning.restore 안의 동일 CubeSample 요소 하나씩이다. full.restore·다른 스킬·검격·파편은 대상이 아니다. 코드·저장본 설치·컴파일·GPU 수치 검증·사용자 화면 판정의 현재 상태는 `09-21/2026-09-21_DIMENSIONMASTER_Q_CUBE_CLARITY_IMPLEMENTATION_RESULT.md`를 따른다.

차원술사 전체 소비 경로를 대조한 결과 직접 SceneColor를 읽지 않는 자체 RGB 요소와 다른 직업의 이펙트도 최종 SceneHDR tone/LUT를 함께 받는다. CubeSample 보정이나 개별 Bloom은 이 공통 grading을 우회하지 않는다. 쿠크 G3의 원본 근거가 있는 LUT01에서 보라색→청색 변환을 GPU로 재현했다. 원본 재질 복원과 자체 색의 맵 grading 독립은 별개이며, 원본 유리의 투과·굴절은 배경 의존으로 유지해야 한다. 현재 V1 3D effect에는 개별 중성 grading 합성 기능이 없으며 화면 overlay로 대체하지 않는다. 실제 입력·region·GPU 검사와 미구현 경계는 같은 RESULT의 G07을 따른다.

### 차원술사 V 시작 유리와 T 환경 조명 복귀

현재 렌더링을 유지하라는 후속 사용자 지시에 따라 전역 grading이나 독립 색 합성을 추가하지 않는다. V 시작 native66의 세 유리 occurrence만 기존 `fresnel_pow`를0.2에서0.5로 조정했다. 실제 DDS/native 출력은 유지하고 정면 alpha coverage를 낮추며 grazing rim은 보존한다. particle의 큰 RGB 입력을 실제 RT0 출력으로 오인하지 않고 shader·텍스처·합성·설치 mesh facing을 함께 대조한다. 같은 native family와 정상 ALT V에는 전파하지 않는다.

T는 이전 사용자 요청의 unlit 정책을 이번 요청으로 되돌렸다. 정확한 dimension_summon ModelCue만 animated pass11에서0으로 선택하여 정상 diffuse/normal/specular와 맵 조명 수신을 복구했다. NONBLEND·MASKED_SURFACE·clip0.3과 공통 tone/LUT는 유지한다. 이는 T의 현재23개 particle 구성 전체를 원본 복원한 것이 아니며 stage0/1의 원본 occurrence 대조는 별도다. 코드 반영·제품 빌드·수치 검사·사용자 화면 확인 경계는09-21 DIMENSIONMASTER_Q_CUBE_CLARITY_IMPLEMENTATION_RESULT의G09 이후를 따른다.

### 쿠크 앵콜·빙고 원본 screen/배치 연결 (2026-09-21)

SCENE07A 화면 균열은 원본 EFInterpTrackPostRenderMaterial의 full-screen LocalVF 재질이다. 기존 V1 native ScreenPost carrier에 원본 DXBC 2627과 네 texture input, viewport aspect, opacity/type 단계를 연결한다. 파편12·spark2는 원본 배우→카메라 부모 체인과 현재1.7 보정을 한 번만 소비한다. 추출만으로 연결 완료를 기록하지 않는다.

빙고 화살표는 FX_TEX_05.fx_g_sy_arrow_03와 fx_itr_10177.par_g_direction_03의 두 source sprite를 사용한다. 부모 scale1.8, source fixed-axis와 particle XY 크기를 최종 quad까지 확인한 occurrence 보정만 적용한다. Skull ChamferBox 경계는 실제 모델 Y bounds와 source 위치로 바닥 기준을 맞추며 나머지 sprite나 mesh의 회전/크기를 변경하지 않는다.

최종엔딩은 Character 모델4클립·배우5배치와 SCENE01B 원본6 FX occurrence를 연결했다. 단일 Orbit update rate의 위상을 실제 재생 delta로 적분한다. 다중 Orbit chain과 rotation-update는 지원한 것으로 확대하지 않는다. 별도 부착 무기의 dead 재질 파라미터 전환은 아직 미완료이며 배우 클립·6 FX 연결과 구분한다. 실제 화면 판정은 사용자 확인을 기다린다. 상세 수치와 설치 기록은 09-21 KOUKU_RAID_REPAIR_RESULT를 따른다.

### MapTool 컷신의 post-update V1 월드 이펙트 샘플

`CMapTool::Update`는 MainApp의 일반 Effect pending commit 뒤에 실행된다. 따라서 편집기 컷신이 같은 프레임에 V1 world-root 이펙트를 만들고 곧바로 absolute seek하면, 새 handle은 아직 pending이다. MapTool target만 새 handle을 scoped commit한 뒤 root와 외부 시계를 샘플한다. 일반 Level/전투 Sequence는 기존 post-update commit을 유지하며, `queued` 상태 문구를 이펙트 문서·재질 실패로 오인하지 않는다. 발탄 버러지 컷신의 667ms actor106이 이 경계를 검증하는 사례다.

### V1 bounded source loop에 포함된 보조 light

원본 Cascade source가 `EmitterLoops=0` sprite/mesh/ribbon emitter와 같은 source visual program의 `LIGHT`/`light` carrier를 함께 가질 수 있다. bounded source-loop 검증은 반복 방출을 수행하는 admitted particle/ribbon carrier를 최소 하나 요구하되, 그 보조 light를 particle/ribbon이 아니라는 이유로 거절해서는 안 된다. 반대로 light만으로 loop0 조건을 만족시키면 안 된다. `effect.valtan.cinematic.trash.actor106.at667`이 이 경우이며, 원본 emitter count나 `loopEffectToDuration`을 변경해 우회하지 않는다.


### 조명 이후 검은 alpha 배경막과 카메라 안팎의 차이

추가 라이트가 반응하지 않는 것처럼 보여도 native 재질 직접광과 최종 투명 합성은 별도다.
Unlit 검정 alpha1은 Blend 단계에서 이전 캐릭터 색을 지운다. 외향 one-sided 원통이면
밖에서는 앞벽이 보이고 안에서는 같은 면이 컬링되므로 camera 이동만으로 밝기가 바뀐다.
Shader 식과 source Color/CDO, 실제 draw state, camera·skin·effect geometry를 연결해 확인한다.

배경 암전을 유지하며 배우를 드러내려는 프로젝트 튜닝은 해당 occurrence에 한정한다.
쿠크에서는 안쪽 면을 가진 파생 geometry를 선택했으나, 최초 생성기가 정점/index만 바꾸고
WGEO 무결성 정보를 갱신하지 않아 제품 로더에서 거부됐다. 따라서 이전의 광선 교차와
단일 삼각형 GPU 검사는 포탈 전체가 정상 로드된다는 증거가 되지 못했다.

파생 geometry는 변경된 payload SHA, bounds, 생성 도구 식별과 metadata SHA를 함께
재생성하고 실제 CWMeshReader → CModel → 해당 effect ResourceStaging까지 검사한다.
reader의 검사를 제거하거나 resource path 존재/JSON 검사만으로 승인하지 않는다. 안쪽
winding을 사용하는 경우에도 실제 pose 구간의 배우 반경과 카메라를 대조한다.
sourceRecipe의 원본 mesh.objectpath는 출처이며 실제 CModel 입력은 meshModel asset ID다.
공유 원본·native 재질·texture·시각의 보존, 파생 Resources 설치와 다른 PC 전달, 사용자
화면 판정을 각각 기록한다. 기존 설치본 실패와 수정 후보의 제품 로드 증거는
[09-22 포탈 결과](09-22/2026-09-22_KOUKU_PORTAL_LIGHTING_RESULT.md)를 따른다.


### 비둘기 native 재질 조명과 피해 앵커

종이 비둘기의 native2893 mesh는 원본 DDS와 재질 입력뿐 아니라 원본 diffuse·normal·specular 출력의 소비 경로를 확인한다. RT0만 번역한 forward carrier는 원본 deferred 후속 조명을 빠뜨릴 수 있으므로 단순 emissiveIntensity 배수로 원본 복원을 판정하지 않는다. 저작 밝기1을 보존한 native2893 전용 조명 연결과 남은 shadow/SH 경계는09-22 SAYDON_CARD_CONTACT_REPAIR RESULT에 기록한다.

망치 effect/카드 뒤집기/플레이어 피해가 같은 본을 쓰더라도 publisher의 judgementKind 분기를 따로 확인한다. ENTER_AREA의 bone 미소비를 occurrence offset의 반복 조정으로 숨기지 않으며 설치된 본·모델 preScale·socket·시간 bake를 기존 경로로 연결한다. 정확한 수정 대상과 수치 증거는09-22 SAYDON_CARD_CONTACT_REPAIR RESULT에 둔다.


### Valtan ghost와 source cinematic 준비의 소비자 경계

native84 translucent의 GPU coverage/alpha는 RGB 복원 증거가 아니다. installed body+현재 cinematic donor, preScale/visual root 및 실제 scene lights를 사용해 RGB를 별도로 검증한다. CBody_Valtan의 F1 진단은 material/bone/light/pass 실패와 CValtan의 숨김 owner를 분리한다. local pattern preview는 명시 authoringPhase와 primary phase3의 ghost part 계약을 맞추고 model 교체 뒤 animation target을 다시 바인딩한다. world sequence V1 준비의 normal pending은 Clear로 terminal failure 처리하지 않고 admitted scene/pool을 보존해 재시도한다. 일리아칸 actor64 원본 body/weapon 설치와 숫자 검증, 미확정 사용자 화면은 [결과](09-22/2026-09-22_VALTAN_EDITOR_VISIBILITY_RESULT.md)를 구분해 따른다.


### 2026-09-22 화면 sampling과 다중 출력 명령의 복원

- 가디언 모션 블러의 원본 LocalToWorld/WorldToView binding은 shader object의 FShaderParameter 배열을 읽고 정확한 VS/PS, CB0 행, serialized offset을 함께 확인한다. runtime은 emitter world와 현재 camera view, 실제 clip Z/W를 공급한다. camera 방향으로 퇴화한 투영과 0 깊이는 해당 pass에서만 격리한다.
- DXBC sincos는 입력을 먼저 고정한 뒤 두 출력을 기록한다. destination/source alias를 순차 C++/HLSL 대입으로 옮기지 않는다. source와 실제 제품 native HLSL의 64×64 gradient WARP 비교 108조건이 통과했다. 1×1 색상 일치만으로 UV 복원 성공을 대신하지 않는다.
- 고대의 바다는 신화와 source mesh/AnimSet이 동일하고 Sea LookInfo의 5개 MIC가 다르다. geometry 재사용 근거와 material override를 분리한다. 상시 aura/mount cue, Stop-only sound, 원본에 없는 clip 참조, 툴에서 새로 저작한 clip을 서로 구분한다. 상세 수치·설치·검증은 `09-22/2026-09-22_ANCIENT_SEA_GUARDIAN_INTEGRATION_RESULT.md`를 따른다.


### TrailGhost live-owner carrier의 현재 범위

TrailGhostEffect의 원본 notify wire22필드는 SHA/길이·enum·finite 검사 후 기존 ModelCue Afterimage로 투영한다. CCharacter의 현재 visible body/equipment/socketed weapon, mounted CPart_Vehicle 및 CharacterPreviewPanel generic part가 같은 CSkeletalAfterimage에 실제 world/palette를 제공한다. emission보다 긴 sample period에서도 첫 실제 pose를 남기고 자연 child lifetime을 보존한다. standalone 고정 모델을 current outfit으로 간주하지 않는다. static weapon은 기존 mesh shader pass23, animated는 pass14이며 source-program variants의 base/disabled pass 계약을 맞춘다. 원본 parameter와 sourceColorIntensity는 소비하지만 원본 rim/fade material ABI와 NONE enum의 세부 의미는 아직 PROJECT_AUTHORED 정책이다. 상세 검증·미완료는 [Workbench RESULT](09-22/2026-09-22_CHARACTER_BONE_WORKBENCH_RESULT.md)에 기록한다.


### 탈것 원본 action 제어와 후처리 consumer

VehicleCatalog v4 lifetime cue와 sound는 실제 mount commit·owner lifetime에서 소비한다. 원본 본/socket 부착과 무한 emitter loop를 기존 EffectPresentationService로 재사용한다. PawnMaterialParam의 selected LookInfo vector는 CModel clone의 native family 전체를 repack하고, source material mutation은 copy-on-write로 다른 mount·prototype을 보존한다. 조명은 RenderingProfileService가 기존 scene을 복원한 뒤 일시 배율을 적용한다. 원본 light/material timer를 project adapter로 해석한 범위와 retail 실행 검증은 구분한다.

EFPostProcessMaterialEffectSkill의 FadeIn/Play/FadeOut/MaxOpacity는 원본 reflection과 serialized tail을 함께 확인한다. SceneColor engine sampler와 CB0 prefix는 source PS instruction SHA 및 정확한 native wire closure로 허용한다. 기존 screenPost carrier에서 source_effect_opacity uniform/track을 engine CB0[0].x로 연결하며 원본 material Opacity는 별도로 유지한다. 명시 null texture는 해당 material의 native texture-parameter expression fallback이 증명된 경우에만 복구한다. 상세 수치와 검증은 09-22 VEHICLE_LIFETIME_SOURCE_CONTROLS_RESULT를 따른다.

### Native 후처리의 생략 반환값과 합성 소유권

4006/4060의 깊이0·screen forward 퇴화는 해당 material이 S_FALSE로 생략한다. Render_ScreenPostPass의 정확한 HRESULT 전달, 합성 성공 때만 target 순서 진행, scene replacement의 생략 시 복사 금지가 같은 계약이다. shader 수치 PASS와 실제 renderer의 skip 처리 PASS를 분리하며, 이전 화면과 다음 정상 pass가 보존되는지 함께 검사한다. [소비자 수치 검증](09-22/2026-09-22_ANCIENT_SEA_GUARDIAN_INTEGRATION_RESULT.md).

### 실제 CModel 입장과 원본 0 수명의 소비자

Guardian native section의 JSON 참조만 검사하면 cooked WModel에 남은 기본 diffuse/normal DDS가 빠질 수 있다. override 이전의 기본 CMaterial 입장부터 검증하고, 원본 model section·native parameter·기본 texture의 합집합을 Resources 전달 manifest로 사용한다. source lifetime 0은 normalized age 0의 의미와 occurrence 종료 정리를 함께 복구한다. 실제 CModel/clone과 원래 clip·bone의 재생/종료 수치를 [가디언 결과](09-22/2026-09-22_GUARDIANKNIGHT_NATIVE_EFFECT_RESTORATION_RESULT.md)에 기록했다.


### Effect OwnerControls의 저장·재생·원복 계약

Effect 문서의 optional `ownerControls`는 캐릭터 재질·방향광·일시 가시성 제어를 기존 Effect 수명에 연결한다. 각 control은 stable `controlId`, `kind`, parameter/target, local-player 조건, 시작 시각과 상대 시간의 float4 keys를 저장한다. 현재 kind는 `MATERIAL_VECTOR`, `DIRECTIONAL_BRIGHTNESS`, `DIRECTIONAL_COLOR`, `IDENTITY_VISIBILITY`, `PAWN_VISIBILITY`다. `mappingBasis=PROJECT_ADAPTER`는 원본 notify 값을 기존 제품 표현에 연결한 범위를 명시한다. optional `sourceValues`는 원본 수치 보존 자료이며 정규화된 keys와 구분하고 편집 UI에서는 읽기 전용으로 보인다.

기존 Effect Tool은 control 선택·추가·삭제·key 편집을 `Try_CommitDocument`의 validate → stage → commit으로 반영하고 같은 dirty document와 Save/원자적 저장 경로를 사용한다. 실패하면 기존 문서와 preview를 보존한다. `build_effect_components`의 split/compile과 `CEffectCatalog`의 assembly 조립도 ownerControls를 보존해야 한다. source/authoring 문서를 변환하면서 elements만 옮기거나 ownerControls를 조용히 버리지 않는다. 제어만 있는 문서는 가짜 particle 없이 승인하며, v15를 사용하면 정식 `runtimeExtensions` 객체도 유지한다. 재생·preview 길이는 각 control의 start + 마지막 key까지 포함한다.

실제 소비는 `CEffectObject`의 sample clock → weak `CCharacter` owner → occurrence token별 제어 적용이다. 제품 경로는 action-start identity도 확인하여 이전 action의 효과가 새 action에 재질·가시성을 다시 적용하지 못하게 한다. preview는 선택한 실제 캐릭터를 같은 소비자로 사용한다. key의 활성 구간은 시작을 포함하고 마지막 key 시각은 제외하며, 화면 숨김·렌더 실패·reset·owner 변경·action 취소·객체 종료는 자기 token을 제거한다. 여러 effect가 겹치면 남은 token을 다시 평가한다.

재질은 제어 직전의 실제 CModel named native constants를 보관하고 copy-on-write로 해당 owner만 변경한다. 종료 때 임의 기본값이나 전체 material override 초기화로 사용자 설정·다른 occurrence를 지우지 않는다. visibility는 기존 stance/장비 visible 상태 위에 일시 hide flag를 적용하며 종료하면 그 flag만 제거한다. 방향광은 매 프레임 원래 RenderingProfile에서 시작한 뒤 로컬 표현 배율을 적용한다. Client presentation 제어이며 Server action·이동·충돌·피해 권위를 바꾸지 않는다. 저장 왕복·실제 owner 취소 원복·컴파일과 사용자 화면 판정은 각각 기록한다. 개별 복원 범위와 증거는 [Guardian 결과](09-22/2026-09-22_GUARDIANKNIGHT_NATIVE_EFFECT_RESTORATION_RESULT.md)를 따른다.


### Character 원본 색 제어의 native register와 Debug 초기화

OwnerControls의 TransColor/BuffColor는 SourceCharacterMaterialParameters의 원본 named-vector 직접 대입과 후속 copy를 추적한 생성 연결표를 소비한다. source family를 비슷한 이름으로 선택하거나 임시 sentinel 값의 차이로 적용 범위를 추측하지 않는다. 기존 CModel/CMaterial override와 owner baseline을 사용하며, material의 native program이 없는 AUTO 슬롯이나 원본이 읽지 않는 parameter는 no-op다. 기존 Guardian body 얼굴·눈의 parser/extra-UV 경계와 native 장비·무기 복원을 구분한다.

원본 native packing 추가 시 generated Configure의 단일 Debug frame이 기본 스택을 넘을 수 있다. 동일한 family 계산식은 별도 call frame으로 생성하고 기본 스택 Product 소비자로 초기화를 확인한다. shader·packing 동일성, 실제 Character 수명, 최종 화면 검증을 분리한다. generator 재실행에서도 family frame과 named-vector 연결표를 함께 갱신한다. 개별 검증은 [Guardian owner control 결과](09-22/2026-09-22_GUARDIAN_OWNER_CONTROL_RESULT.md)에 둔다.


### 유령 발탄의 generic preview와 유지 중인 돌의 dissolve

Character Select의 유령 발탄은 generic CPart_Body를 사용한다. native84는 CBody_Valtan과
같이 단면 반투명 BLEND/pass10에서 forward light를 소비해야 하며, deferred pass0의 ordered
alpha coverage를 정상 유령 표시로 간주하지 않는다. 재질·asset이 같아도 실제 part consumer가
다르면 각각의 render group/pass를 확인한다. 설치 모델과 Character Select 조명을 사용한
수치 검증은 [미리보기 결과](09-22/2026-09-22_GHOST_VALTAN_CHARACTER_SELECT_PREVIEW_RESULT.md)에 둔다.

돌의 native2391은 Dynamic.X를 [-0.25, 1]로 제한한다. 유지 중인 아래쪽 구멍은 실제 noise와
UV radial 항, gap scalar를 합친 값이 clip threshold 아래로 내려가는지 확인한다. 보정은
해당 기둥 occurrence의 authoring scalar에 한정하고 원본 compilerValue를 보존한다. 다른
mesh를 사용하는 파편과 native shader 전체에는 전파하지 않는다. 실제 DDS와 설치 WModel의
UV 검사, 수명 말기의 shader 잔여, 최종 playback draw 제거 및 사용자의 화면 판정을 구분한다.

Product 십자돌은 기존 네 방향의 거리 간격 생성기를 사용한다. native sourceRecipe를 붙일
때 portable mesh의 명시 간격이 birth를 소유하도록 연결하고 source 이동 module과 중복하지
않는다. 땅구르기·피자·발악은 기존 Server combat-object active/hit가 동일 돌을 소비한다.
외형 배율은 현재 mesh transform에만 곱하며 spawn 위치·간격·cover 반경·폭발 시점을 바꾸지
않는다. 반영 상태와 개별 수치는 대응 [돌 구현 계획](09-22/2026-09-22_VALTAN_STONE_PRODUCT_IMPLEMENTATION_PLAN.md) 및 RESULT에 기록한다.

### Clown·MAZE 저작과 World 표식의 재사용

Interaction binding의 effectAssetId는 기존 Character snapshot/action timeline 소비자에 연결한다. 빈 MAZE Q/LMB 공유 문서를 채울 때 모든 class의 동일 stable Effect 참조를 유지하며, raw 좌클릭 핑이 MAZE LMB 명령까지 보내지 않도록 입력 owner에서 소비한다. World 핑 atlas는 fx_l_symbol_21_cl의 3×3 가운데 셀, 과녁은 fx_l_symbol_07_cl의 native2813 채널식을 재사용한다. white 색·고정 크기·재생시간과 Ctrl head-follow 배치는 독립 저작값이며 원본 UI 타이밍 복구로 부르지 않는다.

화염파동의 WandDecal13 바닥에는 FireWave19의 지면 화염·ring(native2873/2874)이 없었다. 원본 재질·EPAL_Z·크기·수명을 보존한 두 ground emitter를 독립 저작 바닥에 추가하며 전체 밝기·전체 asset 회전 보정을 하지 않는다. 바닥과 기둥은 manual.flame-wave.rN.cN 그룹으로 함께 움직이고 저장한다. source·구조·수치 검사와 사용자 화면 판정은 대응 RESULT에서 구분한다.

CPU-only Open의 삭제는 preview 입장을 요구하지 않는다. 활성 preview 변경만 stage/commit을 거치고 마지막 Solo Family 삭제는 유효한 남은 문서로 필터를 바꾼다. 원본 문서의 삭제·저장은 사용자가 수행하며 bug fix 자체가 Trail 삭제 데이터 반영은 아니다.

### Clown 선물상자 source material Light 입력

`MN_PPPP_00`의 source material은 독립 Base/Light native109로 등록한다. Light는 UV=v2, light=v3, view=v5, source position=v6의 legacy direct 입력을 쓰므로 `MakeSourceCharacterInput`의 해당 program 분기까지 연결해야 한다. material parameter 등록과 HLSL 생성 성공만으로 carrier ABI가 연결됐다고 판단하지 않는다. 기존84~108 함수는 유지하고109만 확장했으며, Engine/Client mirror를 같은 변경으로 갱신한다. 실제 선물상자 Resources는 설치됐으며 Product CSO 교체는 실행 파일 점유 해제 후 공식 빌드 대상이다. 설치·CPU 검증·최종 화면 판정은 별도로 기록하며, 상세 증거는 09-22 Clown·MAZE 및 통합 결과를 따른다.

### 주사위 속박·무력화·백스텝의 실제 소비자 연결

주사위 속박의 원본 Spotlight01/02는 Server isPatternBound 상태를 기존 Presentation Player Sample 세션으로 소비하고 해제·사망·퇴장·Reset에서 정리한다. 원본35초 문양을 수동 particle의30초 상한에 맞춰 잘라내지 않는다. source recipe만 finite120초로 허용하며 Codec·Tool의 범위와 particle capacity를 유지한다. 버프 CEFParticleData의 TRS/parameter를 검사하되 FX_Buff_01의 actor-ground 대응을 실제 모델 socket 실측으로 기록하지 않는다.

무력화는 원본 Shield01, Sk12 계열의 별·경계·무지개 폭발을 기존 occurrence에 연결한다. 별 선의 native3008은 기존 ribbon ABI를 유지하면서 원본 sprite 소비를 허용하며 다른 smoke 재질로 대체하지 않는다. 지속 방패는 생략된 Required EmitterLoops 기본값0을 복원하고 기존 loopEffectToDuration으로 저작 window를 따른다. 카드 Sk08_2 Beam2처럼 action projection의 carrier를 복원할 때 kind·rendererShape·Detail.Trail을 source leaf와 함께 맞춘다.

Backstep의 현재 CNpc/CModel 잔상은 공통 CSkeletalAfterimage를 사용한다. 원본 notify의 방출·간격·tail·diffuse intensity와 사용자가 요청한 흰색 appearance를 구분하며, original rim/fade ABI가 확인되지 않은 부분은 PROJECT_AUTHORED로 남긴다. Source float5ms의 작은 표현 오차만 decoder에 허용한다. 설치 모델/본 팔레트/셰이더 바인딩 검사와 최종 화면 판정은 [대응 결과](09-22/2026-09-22_KOUKU_DICE_STAGGER_BACKSTEP_RESULT.md)에 분리한다. 다른 관문에서 복사한 MAP occurrence는 canonical 두 중심의 xyz 차이만 더하고 각 요소의 간격·회전·크기를 보존한다.


### LocationDirect event 자식의 emitter-local 기준

기존 nested ScaleFactor CDO 상속 복구만으로 EventReceiverSpawn의 위치까지 올바르다고 가정하지 않는다. 원본 LocationDirect는 emitter-local 절대 위치를 소유하므로, source event의 부모 위치를 기본 origin으로 더한 뒤 Direct 곡선을 적용하면 부모 높이가 중복된다. 현재 `CEffectPlayback::Spawn_Particles`는 prepared spawn recipe에 활성 `LOCATION_DIRECT`가 있을 때만 event origin을 0으로 초기화한다. event velocity와 일반 non-Direct receiver의 origin 상속은 보존한다.

카드비 낙하 후보는 instance `Distribution=None` delta와 원본 class default의 ScaleFactor lookup 1을 대조해 기존 CDO 절차로 복구했다. 이후 부모·자식 mesh의 실제 CPU playback을 따로 비교해 자식의 중복 높이를 제거했다. 원본 mesh/native material과 Direct 곡선은 유지하며 전체 asset 회전·높이·크기를 임의 보정하지 않는다. 일반 입자 보존, non-Direct event fixture 비교, 실제 설치 모델 본 샘플 및 root transform 검증의 개별 수치는 [카드비·DJ 결과](09-22/2026-09-22_KOUKU_CARDRAIN_DJ_EFFECT_RESULT.md)에 둔다. 코드·저장 데이터 반영과 CPU 검증은 완료 범위이며 최종 GPU 표시·사용자 화면 판정은 별도다.


### Full Restore COMBO와 ALT V 카메라·배경 연결

Full Restore Play All의 COMBO는 실제 선택 모델의 skillbindings와 animevents에서 정확한 clip/stage를 resolve한다. 전체 class를 묶어 조회한 캐시가 비었다는 이유로 선택 class까지 재생 불가로 처리하거나 stage 0으로 추측하지 않는다. 리소스 로드·root·animation sample·renderer의 실패는 시작 직후와 재생 도중 모두 현재 Effect 목록에 표시한다.

Guardian ALT V는 source actor와 camera eye/forward/up을 같은 UE→runtime basis로 변환한다. 원본 카메라의 1.5초 줌아웃 시작을 별도 연속 row로 보여주되 곡선과 3.8초 STOP을 보존한다. 실제 opaque static mesh carrier에는 compositionLayer=sceneBackdrop을 지정하여 다른 ALT V와 같은 활성 carrier 기반 배경 숨김을 사용한다. 전체 용/particle 회전이나 지면 높이 clamp로 잘못된 camera basis를 숨기지 않는다. 수치와 미확정 gameplay 방향 차이는 [카메라 결과](09-22/2026-09-22_GUARDIAN_ALTV_CAMERA_RESULT.md), 재생 경계는 [재생 결과](09-22/2026-09-22_GUARDIAN_PLAYBACK_REPAIR_RESULT.md)에 둔다.


### 원본 World 준비영역 오라와 상태별 source-loop

World prop 복원은 DeployData stable prop→Prop DB LookInfo→상태별 ParticleSystem→source emitter/material/geometry 순으로 조사한다. Gate3의 On/GoOff/Off는 이름이 아니라 실제 ColorOverLife와 EmitterLoops로 녹색 지속/파랑 지속/종료1회로 구분한다. 원본 square WModel의 정점 범위와 modelPreScale·StartSize로 trigger와 렌더 bounds를 맞추고 전체 mesh 회전 보정을 적용하지 않는다. source sprite/mesh의10초 authoring window와 owner-sustained 런타임 수명은 별개 계약이다. zero-CB0/engine-only texture 및 별도 distortion companion도 기존 native decoder로 복원한다. 실제 등록 ID·소스 증거·검증 범위는 [Gate3 결과](09-22/2026-09-22_GATE3_WORLD_AURA_IMPLEMENTATION_RESULT.md)를 따른다.


### 원본 native 다중 carrier와 ParticleMacroUV 입력

복원 검증은 source recipe→C++ material admission→실제 shader family/group/carrier→function/dispatch/HasProfile→texture·engine cbuffer→pixel 순서로 연결한다. 원본 PS가 여러 VF에서 재사용되면 exact material/PS/VF/VS identity로 guard를 확장한다. 세 생성 guard 중 하나만 바꾸면 실제 carrier는 여전히 clip될 수 있다. 정본 installer/generator와 생성 leaf를 함께 교정하고 원본 수식·기존 carrier 보존을 확인한다.

ParticleMacroUV가 있는 원본 fire PS는 projected occurrence center, signed inverse projected diameter, external opacity를 서로 다른 engine row에서 읽을 수 있다. 원본 CB0 binding closure와 출력 alpha 명령을 먼저 읽고, 기존 MacroUV CPU/renderer 입력을 재사용한다. radius는 원본 world-space cm→m만 변환하며 actor scale을 곱하지 않는다. ParticleSystem instance override와 CDO를 실제 패키지에서 확인하고 같은 native를 공유하는 모든 설치 source recipe에 literal을 전달한다.

현재 Kouku native3008 sprite는 기존 ribbon PS/ABI를 그대로 재사용하며, native2873은 원본 MacroUV/opacity prefix를 복구했다. native2874 바닥은 기존 emitter WorldToLocal 경로를 보존했다. 실제 설치 DDS를 사용하는 headless particle pass의 출력·alpha0 control까지 확인했으며 게임 지면/lighting/bloom과 사용자 화면 판정은 별도다. 세부 asset ID·픽셀 수·설치 및 tail 경계는 [화염파동 결과](09-22/2026-09-22_KOUKU_FLAME_WAVE_RESULT.md), [Dice/무력화/백스텝 결과](09-22/2026-09-22_KOUKU_DICE_STAGGER_BACKSTEP_RESULT.md)를 따른다.

### Guardian native carrier와 prefix 복구 범위

Guardian source native3968 이후의 Trail/Decal은 이전3967 pixel 상한 때문에 generic
fallback을 소비했다. Native 상한은 C++ drawable/material 검증과 installer에 동기화한다.
새 source material에 generic 흰 texture를 만들어 연결 실패를 숨기지 않는다.

동일 증상은 shader equation, exact VS/PS, engine-owned CB0 prefix, UV0.zw,
particle color/dynamic, WorldToLocal/camera/MacroUV를 분리해 대조한다. Masked LocalVF의
RGBA prefix와 translucent scalar opacity를 구분한다. Source ABI별로 복원하며 전역
alpha/rotation/scale 보정은 하지 않는다. 원본DXBC WARP 일치는 수식 검증이며 실제
DDS와 GPU carrier·실모델 부착·사용자 화면 판정과는 별도 증거다.

D 바닥 volume decal은 projection volume 높이를 임의로 낮추지 않고 기존 floor receiver가
skinned actor와 rigid Guardian material을 배제하도록 연결한다. 상세 slot과 검증범위는
[Guardian carrier RESULT](09-22/2026-09-22_GUARDIAN_CARRIER_RENDER_REPAIR_IMPLEMENTATION_RESULT.md)를 따른다.


### Guardian 일반 S의 source actor와 F의 IDENTITY 표시

일반 S49220은 사용자 승인에 따라 원본49290의 SK_DDK_DRR_01 head/neck, Cast/Trail/Mouth/Eye를
기존 ModelCue와 실제 본 부착으로 연결했다. target skill ID와 source action provenance를 따로
유지한다. 원본13 joints와 cooked mesh root를 포함한 CModel14 bones, actor offset·scale·playRate,
material track를 함께 확인했으며 source animation과 모델 이름의 유사성으로 대체하지 않았다.
일반 F의 세 원본 End 제거 notify는 구간 전체 hide로 투영하지 않고 기존 owner control token의
IDENTITY 표시 overlay로 소비한다. Tool preview는 requiredStance와 종료 시 원복을 함께 적용한다.

신규 source material은 color PS와 distortion companion을 모두 원본 DXBC와 대조한다. source native
테이블만 추가하고 companion pass를 누락하면 왜곡이 빠진다. CPU 문서 왕복·실제 CModel 생성·GPU
수식 대조와 사용자의 최종 화면 확인을 구분한다. 상세 수치와 설치 범위는
[09-22 Guardian preview/parts 결과](09-22/2026-09-22_GUARDIAN_PREVIEW_PARTS_RESULT.md)에 둔다.

### Effect ModelCue 그림자와 별도 projectile decal

원본 Action의 직접 particle 목록만으로 후속 그림자/화염 누락을 판정하지 않는다. SkillEffect와
Projectile의 생성 effect·timer를 추적한다. Guardian ALT V는 원본 DragonDecal을 별도 element로
복원했으며, 움직이는 skeletal 모델은 optional castsShadow로 기존 SHADOW 경로에 참여한다.
같은 CModel pose/root/material track과 native coverage를 사용하고 재질의 scene camera와 raster의
light 행렬을 분리한다. generic CDO 기본값과 notify별 native 정책을 구분한다. 현재5개 opt-in,
late decal의5.19초 시작 및 검증 증거는 [Guardian 통합 RESULT](09-22/2026-09-22_GUARDIAN_PLAYBACK_REPAIR_RESULT.md)에 둔다.

### Effect Tool 회전의 pivot과 fixed-axis 면 전달

개별 Element와 Group 회전은 같은 quaternion delta로 pivot 주위 위치·방향·선형 운동을 처리한다. 기본 pivot은 anchor origin이며 결과만 기존 Element 필드에 저장한다. 명시적 회전 편집에서 source fixed/rotate-axis sprite의 기존 followEmitterAxisRotation을 켜면 재질 좌표와 최종 면이 같은 emitter basis를 소비한다. 원본 기본 재생과 camera/velocity billboard까지 전역 변경하지 않는다. source/master track의 별도 소유자는 유지하고 mixed-parent 그룹의 raw 중심을 단일 대상의 공통 pivot으로 사용하지 않는다. 현재 fog는9개 중고정축6개만 보완했다. 실제 helper·Codec·Playback·final quad와 팡파레 현재cursor 재stage 검증, 실제 설치·Product 링크 및 사용자 화면 경계는 [회전 결과](09-22/2026-09-22_EFFECT_ROTATION_PIVOT_IMPLEMENTATION_RESULT.md)에 기록한다.

### PBR RNM/IBL의 합성과 동적 그림자

PBR map marker3의 간접광은 기존 MRT geometry RGB로 분리하고 emissive에는 실제 발광만 저장한다.
Deferred가 간접광을 SSAO·fog와 함께 합성하므로 ambient만 차폐되던 소비 누락을 닫는다.
optional shadow.dynamicBakedStrength(기본0)는 static-only depth와 최종 depth의 차이로 움직이는
caster의 RNM/IBL 차폐를 근사하며 source GI 복원으로 기록하지 않는다. static 캐시가 없으면
추가 차폐만 생략한다. 다른 marker·캐릭터 재질·발광의 에너지는 유지한다.
새 geometry pass 없이 기존 shadow cache에 SRV를 연결한다. 수치·UI 설명·기법 경계는
[빛·그림자 가이드](09-22/2026-09-22_RENDERING_LIGHT_SHADOW_GUIDE.md), 설치·검증 상태는
[Guardian 통합 RESULT](09-22/2026-09-22_GUARDIAN_PLAYBACK_REPAIR_RESULT.md)를 따른다.

### Guardian 후속 source trail·편집·S 연결

native AnimationTrail baked history의 clamp는 min(notify window, 실제 마지막 source sample)이다.
실제 제품 document-owned projection의 검증과 playback supplemental 검증을 함께 수정했으며
일반 authored trail의 strict equality는 유지한다. Guardian LMB0/1·Q·R2·D2가 이 경계에서
실패하며 Kouku 입장 준비까지 막았던 것이 확인됐다. sourceRecipe.enabled 금지는 해제하지 않는다.

All Effects preview clone의 stance 복원은 presentation getter를 사용하고 scene Character를
수정하지 않는다. class별 enrichment를 격리하고 Saved Unified 문서에도 기존 Element tree를 연결한다.

후속 사용자 지정은 HUMAN S49220을 원본 FlameScale의 즉시 입력으로 되돌리고 source49290 용을
DRAGON S49230으로 옮기는 것이다. 기존 G05의 일반 S ground-target 연결을 대체한다. 원본
Projectile492200의16emitters와 source 속도/거리/lifetime을 기존 transform track에 연결했다.
transform·방패 크기·ALT V 높이·camera socket의 사용자 보정은 원본 recipe 복원과 따로 기록한다.
정확한 슬롯·개수·설치·검증 경계는 [최신 통합 결과](09-22/2026-09-22_GUARDIAN_PLAYBACK_REPAIR_RESULT.md)의 G06 이후를 따른다.

### Guardian 모델110/111의 engine BRDF 미연결 경계

S49290 head/neck의 원본 e56633f Base PS는 engine-owned `t5/s6` lookup을 사용하고 해당
lookup의 두 번째 채널 역수를 취한다. 미복구 lookup을0으로 번역하면0*Inf가 표면RGB NaN으로
전파되므로 해당 source identity의0분모만0으로 처리한다. 유효한lookup은 원본DXBC와 같은
수식을 유지한다.110/111의 environment suffix CB24/25도 기존 scene color/rotation에 연결한다.
원본 lookup payload·SH 미복구는 유지하며 이 변경을 원작 전체IBL 복원으로 인증하지 않는다.
수식검사와 사용자 GPU 화면 판정을 구분한다.

근거: [S 용 재질·Kouku 준비 검증](09-22/2026-09-22_GUARDIAN_DRAGON_SHADER_RAID_GATE_RESULT.md).

### 건슬링어·슬레이어 기본 외형의 native material 입력

GunSlinger와 Slayer의 기존 기본 body/equipment/weapon14모델31slot을 CharacterCatalog의
per-character native descriptor로 연결한다. 원본 GPU-skin Base/Light shader와 uniform packing이
같은 family를 재사용하고, 건슬링어 arm의 별도 masked permutation은 program112와 기존 cohort84를
사용한다. 식별되지 않은 얼굴 MIC를 이름 유사성으로 골라 넣지 않으며, 기존 선택 의상 root override의
별도 작업 범위를 기본 의상 복원 완료와 혼동하지 않는다.

두 본체의 눈 UV1/UV2와 머리 UV1은 실제 source mesh에서 회수한다. position/UV0가 겹치는
머리 seam은 triangle corner를 같이 join한다. geometry·skin·skeleton·animation payload를 보존한
WModel과 native source texture는 catalog와 함께 설치해야 한다. 재질 native register 연결은
원작의 scene 환경·camera·최종 화면 동등성 판정과 별개다. 설치 및 사용자 미확인 범위는
[기본 의상 결과](09-22/2026-09-22_CHARACTER_EQUIPMENT_MATERIAL_RESTORE_RESULT.md)에 기록한다.

공용 native family의 생성 분기는 긴 else-if chain으로 누적하지 않는다. 각 family가 유일한
nonzero program을 설정하는 계약을 확인한 guarded sibling 분기로 첫 일치와 공통 검증·commit을
보존한다. 생성기·설치기와 named-vector/face parameter reader를 함께 바꾸고, unknown family와
잘못된 parameter가 기존 result를 유지하는지 실제 C++ consumer로 검사한다.

### 쿠크 투사체의 마지막 위치·원본 보호막·눈 부착

Action의 Particle notify만 복원하면 투척 이후의 공·착탄이 누락될 수 있다. Projectile 생성
particle과 death/timer 폭발을 함께 추적하며, 작은 착탄·큰 지연 폭발은 서로의 대체재가 아니다.
원본 발사 시각·속도·최대 높이와 프로젝트가 저작한 목표점/포물선을 분리해 기록한다.
복합 Effect의 마지막 입자가 애니메이션 뒤에 남으면 clip을 늘이거나 속도를 바꾸지 않고
기존 Pattern duration의 후속 수명으로 끝까지 재생한다.

파란 구체는 이름만 비슷한 붉은 variant나 additive hemisphere로 대체하지 않는다. 원본의
SceneColor·alpha carrier, EmitterLoops와 설치 mesh의 modelPreScale를 포함한 반경을 확인한다.
눈 경고도 기존 문서의 표시 이름보다 source eye_01/eye_02와 실제 설치 모델의 양안 본을
먼저 확인한다. Lifetime0을 짧은 양수로 바꾸지 않고 명시적인 owner window를 유지한다.

2관문8개 복원 문서는 Composition revision2198과 함께 설치했다. 실제 게시·실행 중 메모리·사용자 화면은 별도로 확인한다. native
재질·6방향·입자 시계·실제 눈 본·source trail의 서로 다른 검증 범위는
[2관문 결과](09-22/2026-09-22_KOUKU_GATE2_PATTERN_RESTORATION_RESULT.md)를 따른다.

원형·직선·준비 음표는 같은 나팔 family여도 직접 Action notify를 대조한다. 이번 원형은
B_02이며 D_02는 별도 도넛 Action이다. 활성 세 발생기의 원본45/90/135도와 disabled 호출을
복원하며 임의60도 등간격으로 바꾸지 않는다. compact notify의 검증된 int32 FRotator 위치와
UE→runtime snapshot basis를 사용하고 leaf TRS는 유지한다. 서로 다른 notify의 원형→직선
시차도 compound 내부 시계에 보존한다. 직접 notify가 없는 전방 음표 재사용은 저작 배치로
명시하고 원본 호출의 복원으로 기록하지 않는다.

나팔의 원본 SkillDecal rectangle은 기존 native 경고 carrier에서 원본 크기·fade/fill 시계를
사용한다. AreaOffsetAngle이 있는 BOX는 방향뿐 아니라 전방 절반 길이의 중심도 함께 회전해야
방사형 경고와 실제 피격 범위가 일치한다. 공유 leaf를 수정하거나 전체 emitter를 같은 각도로
돌려 오류를 숨기지 않는다.

등장 공의 고정 대상 Effect와 발사·폭발음은 동일 SHOWTIME fixed group으로 묶는다. 해당
Duration이 소유한 MAP 그룹에만 SOUND를 허용하고 MAP EFFECT 최소 하나를 요구한다.
Server CombatObject의 생성 위치·시각을 기존 targeted visual/SoundCue가 함께 소비하며,
일반 선택 그룹·tracking·Collider의 종류 제약은 유지한다. 원본 Action 사운드와 사용자가
편집한 사운드는 별도 소유 범위다. source cue 개수 차이나 retime을 자동 누락으로 판단해
사용자 소유 패턴에 새 행을 추가하지 않는다.

### 차원술사 Alt V의 중앙 capture skin island

ModelCue 하나나 submesh 하나가 화면상 오브젝트 하나라는 뜻은 아니다. 설치된
`sk_swp_cub_00_sk`는 한 submesh 안에25개의 rigid cube를 갖는다. 캡처 texture를 cue 전체에
바인딩하면 주변24개에도 장면이 반복된다. 정확한 모델의 중앙 `b_cube_1_02` 이름에서
runtime bone index를 찾고 vertex의 실제 skin weight로 native178 capture RGB항만 한정한다.
Color/Bloom 양쪽에 같은 mask를 쓰고 주변의 geometry·animation·aura·edge·alpha를 보존한다.
static capture mesh의 기존 한 인자 material 호출은 weight1을 유지한다.

새 중앙 본 uniform은 animated base와 모든 SourceGroup의 같은 ABI로 배포한다. 개별 FXC
성공만으로 완료하지 않고 설치 CSO의 FX 변수·SetRawValue와 CShader의 전체 variant 계약을
확인한다. 빌드 도중 source가 바뀐 경우에는 output mtime 대신 실제 compiler 입력 세대도
대조한다.

UV crop 검사와 실제 skin island별 capture 표면 선택 검사는 별개다. 설치 모델의24개 중앙
정점/576개 주변 정점 근거와 headless·Product·사용자 화면 경계는
[Alt V 결과 G05](09-20/2026-09-20_DIMENSIONMASTER_ALTV_CUBE_HANDOFF_RESULT.md#g05-큐브25개-전체에-퍼진-화면-캡처의-중앙-큐브-한정)에 기록한다.

### 선택 의상·무기와 원본 추가 UV의 소비 연결

CharacterCatalog의 root `modelMaterialOverrides`는 선택 장비 경로의 source descriptor를
소유하며 기본 character 소유 경로와 중복할 수 없다. EquipmentPresentationService는 기존
CModel/CMaterial 경로에 이 descriptor를 전달한다. family 재사용은 원본 PS identity뿐 아니라
uniform packing과 texture expression layout도 동일해야 한다. translucent native는 static
socket weapon과 skinned part 모두 기존 forward light 입력을 소비해야 한다.

추가 UV는 원본 vertex와 triangle corner에서 회수한다. 재질 이름이 dummy인 glTF도 설치
geometry와 position/UV0·normal·triangle을 검증할 수 있지만, UV가 다른 동형 `_old` export는
LookInfo 없이 선택하지 않는다. 원본 자체가 단일 UV인 hair의 disabled two-tone 예외는 실제
program7의 양 stage 상수와 수식에 한정한다. shader 오류를 숨기는 임의 UV 복제와 다르며
없는 UV를 요구하는 runtime 재활성화도 거절한다. 범위·검증·Resources 전달 증거는
[장비 복원 결과](09-22/2026-09-22_CHARACTER_EQUIPMENT_MATERIAL_RESTORE_RESULT.md)를 따른다.


### PBR 간접광 에너지와 장면 색 합성 단계

원본 DXBC 대조로 확인된 RNM diffuse의 metallic/BRDF 에너지 분배를 공용 map PBR
경로에 복구한다. lookup이 연결되지 않은 재질은 metallic mask만 복구하고 가짜 lookup을
주입하지 않는다. SH9 원본 데이터의 존재, native7-row packing, hemisphere 동적 owner와
실제 runtime 연결은 별개의 완료 조건이다. 일부 native character에서도 이 입력이0이며
map 부분식은 해당 SH 곱을 생략한다. 환경 cube만 있다는 사실로 공통 GI가 복구됐다고 하지 않는다.

Rendering Benchmark는 map PBR RNM/IBL/직접 확산광 분리와 전체 SceneHDR/톤 이후/그레이딩
이후를 제공한다. 전체 SceneHDR에는 그 시점까지의 fog·scene post가 포함되므로 순수 조명
버퍼라고 설명하지 않는다. PBR gain/normal/roughness 비교는 session·Level에만 적용하고
도구 종료/Level 전환 때 진입 재질 설정 전체를 복원한다. 자원 이름32개 표 대신 실제
유효한 합성 설정과 선택한 한 PBR surface의 바인딩 값을 표시한다. 원본 상수와 실제 픽셀별
texture/vertex 샘플은 구분한다. 날짜별 수치·컴파일·WARP·화면 미확인 경계는
[Character Select 결과](09-22/2026-09-22_CHARACTER_SELECT_MATERIAL_RESTORE_IMPLEMENTATION_RESULT.md)를 따른다.

### Character Select 미리보기 바닥과 클래스 소개 연출

11개 검은 floor/star의 원본 brightness0은 미리보기의 정상 입력이다. 원래 멀리 분리한
미리보기 자리와 소개용 배경 Area를 분리해서 조사한다. 가디언은 SCENE01의 ChangeClass9,
Matinee75→74, SL10이 소개 연출이며 SL00의 짧은 customization zoom이 아니다.

일반 캐릭터의 기본 장비와 컷신 actor의 정확한 mesh/MIC·추가 hair bones·별도 무기 socket을
구분한다. 컷신 부품 설치나 native shader 컴파일로 일반 미리보기의 완전 복원을 선언하지 않는다.
camera source time, animation freeze, source particle age는 각각 원본 곡선을 따른다. 반복 때
particle handle을 재생성하지 않고 원본 owner window를 연장하며, finite emitter의 Lifetime0은
새 입자 출생을 늘리지 않고 기존 burst를 유지한다.

현재 SL10·배우·FX 설치, 연출 게시와 Workbench WORLD 연결 및 남은 원본 복원 범위는
[가디언 선택 연출 결과](09-22/2026-09-22_GUARDIAN_CLASS_SELECTION_CINEMATIC_IMPLEMENTATION_RESULT.md)에
구분했다. 추가 원본 소품·환경·후처리와 최종 화면 검증이 남아 있어 제품 전체 복원 완료가 아니다.

연속 loop에서는 actor player와 PSC handle을 유지한다. Lifetime0 burst의 종료 시각은 현재
camera loop 끝보다 한 구간 이상 앞까지 확보해 float 반올림으로 소멸하지 않게 한다. 재생 제어의
소유권은 Level 주소가 아니라 Play/Seek마다 바뀌는 token으로 구분하고 내부 loop는 token을
유지한다. editor float slider의 끝값은 실제 double phase duration으로 clamp해야 한다.

장시간 bone bake는 Engine WANM의 clip당 총 TRS key 1,000,000개 상한을 지킨다. 정지 포즈의
반복 키는 각 component의 packed-float 값이 같은 run 내부만 무손실 압축하며 시간 구간의
양끝은 보존한다. Python reader 성공과 실제 Engine decoder·CModel donor attach 성공은 별도
검증이다. donor의 body/wing/hair palette와 추가 hair rest bones도 각각 검증한다.

SL10 native214..234/237의 RNM은 Catalog/publisher, CModel material override, CMaterial
초기 admission 및 texture binding의 허용 범위가 일치해야 한다. 배경 준비 실패는 optional
연출만 격리해 기존 Character Select 입장은 유지하므로, 정상 아레나 표시를 연출 준비 성공으로
간주하지 않는다. 실제 Loader의 설치된 CModel/Create_MaterialVariant 검사로 배경을 검증한다.

배경 모델 검증 뒤에는 실제 Object/Layer 배치와 연출 Initialize/Play까지 연결을 확인한다.
WorldSequence 준비는 Area의 optional Deploy 문서 두 개가 모두 없는 경우 빈 대상 집합을
허용하되 한쪽 누락·손상·존재하지 않는 Deploy binding은 거부한다. 모델·배치 단계 성공을
전체 연출 재생 성공으로 보고하지 않고 최초 실패 단계가 Workbench 상태로 전달되어야 한다.

### V2 ScreenPost의 PNG 텍스처 소비

V2 ScreenPost LEAF의 texture 준비는 `CEffectV2Object::Acquire_Texture`가 소유한다. DDS는 기존 DirectXTK DDS loader, PNG 등 WIC 형식은 WIC loader를 사용하고 같은 authored sRGB 정책과 성공 후 cache commit을 유지한다. WIC 경로는 device-only이며 기존 MainApp/Loader COM 초기화를 소비한다. UI Resources에 정상 파일이 있다는 사실과 실제 decoder·SRV·alpha coverage admission 성공은 별개다. DDS 변환 없이 기존 PNG를 소비하며, 구체적인 설치 파일·Prewarm 검증·Product 반영 경계는 [카드비·DJ 결과](09-22/2026-09-22_KOUKU_CARDRAIN_DJ_EFFECT_RESULT.md)에 둔다.

### 병렬 복원에서 native program ID 충돌

서로 다른 branch의 추가 program은 ID뿐 아니라 material/PS/layout identity를 비교한 뒤 통합한다. 다른 재질에 중복 발급된 ID는 해당 cohort의 authored profile·C++ table·HLSLI 함수·dispatch·selected switch를 같은 변경에서 이동하며 수식·리소스·transform은 보존한다. 다른 cohort의 동일 숫자를 전역 치환하거나 한 parent 전체를 선택하면 실제 재질이 바뀐다. 지원된64-ID group과 기존 renderer를 사용하고 새 runtime 경로를 만들지 않는다. 구체적인4633..4647 이동과 검증은 [PR449 병합 결과](09-22/2026-09-22_PR449_MAIN_MERGE_RESULT.md)를 따른다.

### 쿠크 빙고 블랙홀·메두사 원본 복원과 중앙 anchor

빙고 P107은 MN_RPCT_07 action4219927의 구체·빔·폭발·그로기 clip과 notify를 소비한다.
기존 눈 레이저 action4219983의 별도 P62/P123은 보존한다. 새 복원은 기존 V1 native material과
carrier를 그대로 사용한다. boss24개 element와 중앙 MAP40개 element로 분리하여 구체·폭발은
앵콜세이튼 시작 위치에 고정하고, 보스는 사용자 지정 위치에서 그 시작 위치를 바라본다.
source notify의 가로 오프셋은 고정 MAP 중심에 맞춘 그룹만 바꾸고 수직·크기·material 값은 보존한다.

P94는 사용자가 바꾼 MN_RPCT_05 action42198102의 공격·잔상·폭발19개 element를 복원한다.
공포는 별도 Client 판정이 아니라 원본 공격 notify 창과 기존 GAZE_REAL_BOSS 실패 FEAR를 잇는다.
기존 리소스162개를 재사용하며 새 이미지·모델·음원은 없다. CEffectCatalog의 실제 stage와
CEffectPlayback 수치 검증은 GPU 화면 확인과 구분한다. 상세 설치·Release·배포 증거는
[결과 문서](09-23/2026-09-23_KOUKU_GATE3_BINGO_FLOW_RESULT.md)에 기록한다.
