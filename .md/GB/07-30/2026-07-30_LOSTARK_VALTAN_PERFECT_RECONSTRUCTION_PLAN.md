# 발탄 완전 복원 계획 — 최종본 이동 안내

이 초안의 `(Y,Z,X)` 좌표 계약과 raw Euler 저장 방식은 검토 결과 폐기됐다.
구현에 사용하지 않는다.

최종 마스터 계획서는 다음 파일 하나다.

- `C:/Users/user/Desktop/LostArk/.md/GB/07-30/2026-07-30_LOSTARK_HEARTRB_EXACT_STATIC_MAP_RECONSTRUCTION_PLAN.md`

최종본은 `(X,Z,-Y)` 위치, `(Sx,Sz,Sy)` signed scale, basis-conjugated quaternion,
`ACTIVE.maparea`, 원본 `sourcePlacementId`, source-level Engine Layer,
음수 축 `5,042`/실제 reflection `4,521`, central → SL00 → all Gate를 사용한다.
