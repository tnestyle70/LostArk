#pragma once

#include "Client_Defines.h"
#include "Engine_Defines.h"
#include "Effect_Types.h"

#include <filesystem>

NS_BEGIN(Client)

class CEffect_AssetIO final
{
public:
	/*
	 * Human readable authoring text. Both the tool and the recipe importer
	 * write this format, so an effect can be generated by a script and then
	 * hand tuned in the editor.
	 */
	static bool_t Save_Authoring(const filesystem::path& Path,
		const EFFECT_ASSET_DESC& Asset, string* pError = nullptr);
	static bool_t Load_Authoring(const filesystem::path& Path,
		EFFECT_ASSET_DESC& OutAsset, string* pError = nullptr);
	static bool_t Save_Binary(const filesystem::path& Path,
		const EFFECT_ASSET_DESC& Asset, string* pError = nullptr);
	static bool_t Load_Binary(const filesystem::path& Path,
		EFFECT_ASSET_DESC& OutAsset, string* pError = nullptr);
	static bool_t Validate_RoundTrip(const EFFECT_ASSET_DESC& Asset,
		string* pError = nullptr);
};

NS_END
