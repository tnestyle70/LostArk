// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_me_flowtrail_01_ts_tr_Constants0 : register(b0)
{
    float4 cb0[20];
};
cbuffer Shade_Ue3_fx_k_me_flowtrail_01_ts_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_k_me_flowtrail_01_ts_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_me_flowtrail_01_ts_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_me_flowtrail_01_ts_tr_OUTPUT Shade_Ue3_fx_k_me_flowtrail_01_ts_tr(Shade_Ue3_fx_k_me_flowtrail_01_ts_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.x, v4.y, cb0[9].y
    r0.x = v4.y + cb0[9].y;
    // mul r0.x, r0.x, cb0[16].z
    r0.x = r0.x * cb0[16].z;
    // mul r0.x, r0.x, l(6.283185)
    r0.x = r0.x * 6.283185f;
    // sincos r0.x, null, r0.x
    float stage1_x = r0.x;
    r0.x = sin(stage1_x);
    // mul r0.y, cb0[8].z, cb0[16].w
    r0.y = cb0[8].z * cb0[16].w;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // add r0.y, -v4.y, l(1.000000)
    r0.y = -v4.y + 1.0f;
    // mul r0.y, r0.y, v4.y
    r0.y = r0.y * v4.y;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // add r0.y, v4.x, l(-0.500000)
    r0.y = v4.x + -0.5f;
    // mad r0.x, r0.x, l(4.000000), r0.y
    r0.x = r0.x * 4.0f + r0.y;
    // mad r0.zw, v4.xxxy, cb0[6].xxxy, cb0[7].xxxy
    float stage2_z = v4.x * cb0[6].x + cb0[7].x;
    float stage2_w = v4.y * cb0[6].y + cb0[7].y;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.zw, r0.zwzz, t2.zwxy, s0, l(0.000000)
    float4 fetch3 = t2.SampleBias(s0, r0.zw, 0.0f);
    r0.z = fetch3.x;
    r0.w = fetch3.y;
    // mad r0.zw, r0.zzzw, l(0.000000, 0.000000, 2.000000, 2.000000), l(0.000000, 0.000000, -1.000000, -1.000000)
    float stage4_z = r0.z * 2.0f + -1.0f;
    float stage4_w = r0.w * 2.0f + -1.0f;
    r0.z = stage4_z;
    r0.w = stage4_w;
    // mad r1.xy, v4.xyxx, cb0[5].xyxx, l(0.000000, -1.000000, 0.000000, 0.000000)
    float stage5_x = v4.x * cb0[5].x + 0.0f;
    float stage5_y = v4.y * cb0[5].y + -1.0f;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // mad r0.zw, cb0[15].zzzz, r0.zzzw, r1.xxxy
    float stage6_z = cb0[15].z * r0.z + r1.x;
    float stage6_w = cb0[15].z * r0.w + r1.y;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // add r1.x, r0.z, cb0[15].w
    r1.x = r0.z + cb0[15].w;
    // add r1.y, r0.w, cb0[8].x
    r1.y = r0.w + cb0[8].x;
    // mov r0.y, v4.y
    r0.y = v4.y;
    // mad r0.xy, cb0[17].xxxx, r0.xyxx, r1.xyxx
    float stage7_x = cb0[17].x * r0.x + r1.x;
    float stage7_y = cb0[17].x * r0.y + r1.y;
    r0.x = stage7_x;
    r0.y = stage7_y;
    // add r0.xy, r0.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage8_x = r0.x + -0.5f;
    float stage8_y = r0.y + -0.5f;
    r0.x = stage8_x;
    r0.y = stage8_y;
    // dp2 r1.x, cb0[10].xyxx, r0.xyxx
    r1.x = dot(cb0[10].xy, r0.xy);
    // dp2 r1.y, cb0[11].xyxx, r0.xyxx
    r1.y = dot(cb0[11].xy, r0.xy);
    // add r0.xy, r1.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage9_x = r1.x + 0.5f;
    float stage9_y = r1.y + 0.5f;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t3.xyzw, s1, l(0.000000)
    float4 fetch10 = t3.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch10.x;
    r0.y = fetch10.y;
    r0.z = fetch10.z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xyzx, r0.wwww
    float stage11_x = -r0.x + r0.w;
    float stage11_y = -r0.y + r0.w;
    float stage11_z = -r0.z + r0.w;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    // mad r0.xyz, cb0[17].yyyy, r1.xyzx, r0.xyzx
    float stage12_x = cb0[17].y * r1.x + r0.x;
    float stage12_y = cb0[17].y * r1.y + r0.y;
    float stage12_z = cb0[17].y * r1.z + r0.z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage13_x = max(abs(r0.x), 1e-06f);
    float stage13_y = max(abs(r0.y), 1e-06f);
    float stage13_z = max(abs(r0.z), 1e-06f);
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // log r0.xyz, r0.xyzx
    float stage14_x = log2(r0.x);
    float stage14_y = log2(r0.y);
    float stage14_z = log2(r0.z);
    r0.x = stage14_x;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // mul r0.xyz, r0.xyzx, cb0[17].zzzz
    float stage15_x = r0.x * cb0[17].z;
    float stage15_y = r0.y * cb0[17].z;
    float stage15_z = r0.z * cb0[17].z;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // exp r0.xyz, r0.xyzx
    float stage16_x = exp2(r0.x);
    float stage16_y = exp2(r0.y);
    float stage16_z = exp2(r0.z);
    r0.x = stage16_x;
    r0.y = stage16_y;
    r0.z = stage16_z;
    // mul r0.xyz, r0.xyzx, cb0[17].wwww
    float stage17_x = r0.x * cb0[17].w;
    float stage17_y = r0.y * cb0[17].w;
    float stage17_z = r0.z * cb0[17].w;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // add r1.xyz, -cb0[3].xyzx, cb0[4].xyzx
    float stage18_x = -cb0[3].x + cb0[4].x;
    float stage18_y = -cb0[3].y + cb0[4].y;
    float stage18_z = -cb0[3].z + cb0[4].z;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    // mad r1.xyz, v4.yyyy, r1.xyzx, cb0[3].xyzx
    float stage19_x = v4.y * r1.x + cb0[3].x;
    float stage19_y = v4.y * r1.y + cb0[3].y;
    float stage19_z = v4.y * r1.z + cb0[3].z;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // mul r0.xyz, r0.xyzx, r1.xyzx
    float stage20_x = r0.x * r1.x;
    float stage20_y = r0.y * r1.y;
    float stage20_z = r0.z * r1.z;
    r0.x = stage20_x;
    r0.y = stage20_y;
    r0.z = stage20_z;
    // mad r0.xyz, r0.xyzx, cb0[1].xyzx, cb0[2].xyzx
    float stage21_x = r0.x * cb0[1].x + cb0[2].x;
    float stage21_y = r0.y * cb0[1].y + cb0[2].y;
    float stage21_z = r0.z * cb0[1].z + cb0[2].z;
    r0.x = stage21_x;
    r0.y = stage21_y;
    r0.z = stage21_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage22_x = r0.x * v5.w + v5.x;
    float stage22_y = r0.y * v5.w + v5.y;
    float stage22_z = r0.z * v5.w + v5.z;
    o0.x = stage22_x;
    o0.y = stage22_y;
    o0.z = stage22_z;
    // mul r0.x, v4.x, cb0[12].x
    r0.x = v4.x * cb0[12].x;
    // add r0.x, r0.x, cb0[18].w
    r0.x = r0.x + cb0[18].w;
    // mad r0.z, v4.y, cb0[12].y, cb0[8].y
    r0.z = v4.y * cb0[12].y + cb0[8].y;
    // add r0.xy, r0.xzxx, l(-0.500000, -1.500000, 0.000000, 0.000000)
    float stage23_x = r0.x + -0.5f;
    float stage23_y = r0.z + -1.5f;
    r0.x = stage23_x;
    r0.y = stage23_y;
    // dp2 r1.x, cb0[13].xyxx, r0.xyxx
    r1.x = dot(cb0[13].xy, r0.xy);
    // dp2 r1.y, cb0[14].xyxx, r0.xyxx
    r1.y = dot(cb0[14].xy, r0.xy);
    // add r0.xy, r1.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage24_x = r1.x + 0.5f;
    float stage24_y = r1.y + 0.5f;
    r0.x = stage24_x;
    r0.y = stage24_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s3, l(0.000000)
    float4 fetch25 = t1.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch25.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, v4.xyxx, t0.yxzw, s2, l(0.000000)
    float4 fetch26 = t0.SampleBias(s2, v4.xy, 0.0f);
    r0.y = fetch26.x;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul_sat r0.x, r0.x, cb0[19].x
    r0.x = saturate(r0.x * cb0[19].x);
    // mul r0.x, r0.x, cb0[1].w
    r0.x = r0.x * cb0[1].w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage27_x = r0.x * v1.x;
    float stage27_y = r0.x * v1.y;
    float stage27_z = r0.x * v1.z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage28_x = r0.w * v0.z;
    float stage28_y = r0.w * v0.x;
    float stage28_z = r0.w * v0.y;
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage29_x = r0.x * r1.x;
    float stage29_y = r0.x * r1.y;
    float stage29_z = r0.x * r1.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage30_x = r0.x / r0.w;
    float stage30_y = r0.y / r0.w;
    r0.x = stage30_x;
    r0.y = stage30_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage31_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage31_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage31_x;
    r1.y = stage31_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage32_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage32_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage32_x;
    r1.y = stage32_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage33_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage33_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage33_x;
    r1.y = stage33_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage34_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage34_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage35_x = r0.x * 0.5f + 0.5f;
    float stage35_y = r0.y * 0.5f + 0.5f;
    o2.x = stage35_x;
    o2.y = stage35_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage36_z = 1.0f;
    float stage36_w = asfloat(0u);
    o2.z = stage36_z;
    o2.w = stage36_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage37_x = cb2[3].x;
    float stage37_y = cb2[3].y;
    float stage37_z = cb2[3].z;
    o3.x = stage37_x;
    o3.y = stage37_y;
    o3.z = stage37_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage38_x = asfloat(0u);
    float stage38_y = asfloat(0u);
    float stage38_z = asfloat(0u);
    float stage38_w = asfloat(0u);
    o4.x = stage38_x;
    o4.y = stage38_y;
    o4.z = stage38_z;
    o4.w = stage38_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage39_x = asfloat(0u);
    float stage39_y = asfloat(0u);
    float stage39_z = asfloat(0u);
    float stage39_w = asfloat(0u);
    o5.x = stage39_x;
    o5.y = stage39_y;
    o5.z = stage39_z;
    o5.w = stage39_w;

    Shade_Ue3_fx_k_me_flowtrail_01_ts_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
