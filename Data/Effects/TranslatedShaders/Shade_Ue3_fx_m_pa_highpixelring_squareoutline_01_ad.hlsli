// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_pa_highpixelring_squareoutline_01_ad_Constants0 : register(b0)
{
    float4 cb0[20];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_m_pa_highpixelring_squareoutline_01_ad_INPUT
{
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_m_pa_highpixelring_squareoutline_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_m_pa_highpixelring_squareoutline_01_ad_OUTPUT Shade_Ue3_fx_m_pa_highpixelring_squareoutline_01_ad(Shade_Ue3_fx_m_pa_highpixelring_squareoutline_01_ad_INPUT stage)
{
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;

    // mad r0.x, cb0[8].x, v4.x, cb0[8].y
    r0.x = cb0[8].x * v4.x + cb0[8].y;
    // mad r0.y, cb0[8].z, v4.y, cb0[9].x
    r0.y = cb0[8].z * v4.y + cb0[9].x;
    // add r0.xy, r0.xyxx, cb0[3].zzzz
    float stage1_x = r0.x + cb0[3].z;
    float stage1_y = r0.y + cb0[3].z;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xy, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch2 = t0.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch2.x;
    r0.y = fetch2.y;
    // mad r0.xy, r0.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage3_x = r0.x * 2.0f + -1.0f;
    float stage3_y = r0.y * 2.0f + -1.0f;
    r0.x = stage3_x;
    r0.y = stage3_y;
    // mul r0.zw, r0.xxxy, cb0[9].yyyy
    float stage4_z = r0.x * cb0[9].y;
    float stage4_w = r0.y * cb0[9].y;
    r0.z = stage4_z;
    r0.w = stage4_w;
    // mad r1.x, cb0[3].w, cb0[10].x, cb0[10].y
    r1.x = cb0[3].w * cb0[10].x + cb0[10].y;
    // mad r1.x, v4.y, cb0[9].w, r1.x
    r1.x = v4.y * cb0[9].w + r1.x;
    // add r1.x, r1.x, cb0[10].w
    r1.x = r1.x + cb0[10].w;
    // mad r1.y, cb0[3].w, cb0[11].z, cb0[11].w
    r1.y = cb0[3].w * cb0[11].z + cb0[11].w;
    // mad r1.y, cb0[12].x, v4.x, r1.y
    r1.y = cb0[12].x * v4.x + r1.y;
    // add r1.y, r1.y, cb0[12].y
    r1.y = r1.y + cb0[12].y;
    // mad r2.x, cb0[11].x, r1.x, r1.y
    r2.x = cb0[11].x * r1.x + r1.y;
    // mad r2.y, r1.y, cb0[12].z, r1.x
    r2.y = r1.y * cb0[12].z + r1.x;
    // mad r0.zw, cb0[12].wwww, r0.zzzw, r2.xxxy
    float stage5_z = cb0[12].w * r0.z + r2.x;
    float stage5_w = cb0[12].w * r0.w + r2.y;
    r0.z = stage5_z;
    r0.w = stage5_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r0.zwzz, t2.xyzw, s2, l(0.000000)
    float4 fetch6 = t2.SampleBias(s2, r0.zw, 0.0f);
    r1.x = fetch6.x;
    r1.y = fetch6.y;
    r1.z = fetch6.z;
    // max r1.xyzw, |r1.xxyz|, l(0.000001, 0.000001, 0.000001, 0.000001)
    float stage7_x = max(abs(r1.x), 1e-06f);
    float stage7_y = max(abs(r1.x), 1e-06f);
    float stage7_z = max(abs(r1.y), 1e-06f);
    float stage7_w = max(abs(r1.z), 1e-06f);
    r1.x = stage7_x;
    r1.y = stage7_y;
    r1.z = stage7_z;
    r1.w = stage7_w;
    // log r1.xyzw, r1.xyzw
    float stage8_x = log2(r1.x);
    float stage8_y = log2(r1.y);
    float stage8_z = log2(r1.z);
    float stage8_w = log2(r1.w);
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    r1.w = stage8_w;
    // mul r1.xyzw, r1.xyzw, cb0[13].xxxx
    float stage9_x = r1.x * cb0[13].x;
    float stage9_y = r1.y * cb0[13].x;
    float stage9_z = r1.z * cb0[13].x;
    float stage9_w = r1.w * cb0[13].x;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    r1.w = stage9_w;
    // exp r1.xyzw, r1.xyzw
    float stage10_x = exp2(r1.x);
    float stage10_y = exp2(r1.y);
    float stage10_z = exp2(r1.z);
    float stage10_w = exp2(r1.w);
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    r1.w = stage10_w;
    // mul r1.xyzw, r1.xyzw, cb0[13].yyyy
    float stage11_x = r1.x * cb0[13].y;
    float stage11_y = r1.y * cb0[13].y;
    float stage11_z = r1.z * cb0[13].y;
    float stage11_w = r1.w * cb0[13].y;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    r1.w = stage11_w;
    // mad r2.x, cb0[6].z, v4.x, cb0[6].w
    r2.x = cb0[6].z * v4.x + cb0[6].w;
    // mad r2.y, v4.y, cb0[7].x, cb0[7].z
    r2.y = v4.y * cb0[7].x + cb0[7].z;
    // add r0.zw, r2.xxxy, cb0[3].zzzz
    float stage12_z = r2.x + cb0[3].z;
    float stage12_w = r2.y + cb0[3].z;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mad r0.xy, cb0[9].yyyy, r0.xyxx, r0.zwzz
    float stage13_x = cb0[9].y * r0.x + r0.z;
    float stage13_y = cb0[9].y * r0.y + r0.w;
    r0.x = stage13_x;
    r0.y = stage13_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch14 = t1.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch14.x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[9].z
    r0.y = r0.y * cb0[9].z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // add r0.y, cb0[3].y, cb0[13].z
    r0.y = cb0[3].y + cb0[13].z;
    // mad r0.xyzw, r0.xxxx, r1.xyzw, -r0.yyyy
    float stage15_x = r0.x * r1.x + -r0.y;
    float stage15_y = r0.x * r1.y + -r0.y;
    float stage15_z = r0.x * r1.z + -r0.y;
    float stage15_w = r0.x * r1.w + -r0.y;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    r0.w = stage15_w;
    // mul_sat r1.xyz, r0.yzwy, l(8.000000, 8.000000, 8.000000, 0.000000)
    float stage16_x = saturate(r0.y * 8.0f);
    float stage16_y = saturate(r0.z * 8.0f);
    float stage16_z = saturate(r0.w * 8.0f);
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mul_sat r0.xyzw, r0.xyzw, l(10.000000, 10.000000, 10.000000, 10.000000)
    float stage17_x = saturate(r0.x * 10.0f);
    float stage17_y = saturate(r0.y * 10.0f);
    float stage17_z = saturate(r0.z * 10.0f);
    float stage17_w = saturate(r0.w * 10.0f);
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // add r0.yzw, -r1.xxyz, r0.yyzw
    float stage18_y = -r1.x + r0.y;
    float stage18_z = -r1.y + r0.z;
    float stage18_w = -r1.z + r0.w;
    r0.y = stage18_y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mul r0.yzw, r0.yyzw, cb0[13].wwww
    float stage19_y = r0.y * cb0[13].w;
    float stage19_z = r0.z * cb0[13].w;
    float stage19_w = r0.w * cb0[13].w;
    r0.y = stage19_y;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // max r0.yzw, |r0.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage20_y = max(abs(r0.y), 1e-06f);
    float stage20_z = max(abs(r0.z), 1e-06f);
    float stage20_w = max(abs(r0.w), 1e-06f);
    r0.y = stage20_y;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // log r0.yzw, r0.yyzw
    float stage21_y = log2(r0.y);
    float stage21_z = log2(r0.z);
    float stage21_w = log2(r0.w);
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mul r0.yzw, r0.yyzw, cb0[14].xxxx
    float stage22_y = r0.y * cb0[14].x;
    float stage22_z = r0.z * cb0[14].x;
    float stage22_w = r0.w * cb0[14].x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // exp r0.yzw, r0.yyzw
    float stage23_y = exp2(r0.y);
    float stage23_z = exp2(r0.z);
    float stage23_w = exp2(r0.w);
    r0.y = stage23_y;
    r0.z = stage23_z;
    r0.w = stage23_w;
    // mul r1.xyz, cb0[1].xyzx, cb0[4].xyzx
    float stage24_x = cb0[1].x * cb0[4].x;
    float stage24_y = cb0[1].y * cb0[4].y;
    float stage24_z = cb0[1].z * cb0[4].z;
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // mad r2.xy, v4.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), cb0[5].xyxx
    float stage25_x = v4.x * 2.0f + cb0[5].x;
    float stage25_y = v4.y * 2.0f + cb0[5].y;
    r2.x = stage25_x;
    r2.y = stage25_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.w, r2.xyxx, t3.yzwx, s3, l(0.000000)
    float4 fetch26 = t3.SampleBias(s3, r2.xy, 0.0f);
    r1.w = fetch26.x;
    // mul r2.x, |r1.w|, |r1.w|
    r2.x = abs(r1.w) * abs(r1.w);
    // lt r1.w, |r1.w|, l(0.000001)
    r1.w = asfloat((abs(r1.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.xyz, r1.xyzx, r2.xxxx
    float stage27_x = r1.x * r2.x;
    float stage27_y = r1.y * r2.x;
    float stage27_z = r1.z * r2.x;
    r1.x = stage27_x;
    r1.y = stage27_y;
    r1.z = stage27_z;
    // movc r1.xyz, r1.wwww, l(0,0,0,0), r1.xyzx
    float stage28_x = asuint(r1.w) != 0u ? asfloat(0u) : r1.x;
    float stage28_y = asuint(r1.w) != 0u ? asfloat(0u) : r1.y;
    float stage28_z = asuint(r1.w) != 0u ? asfloat(0u) : r1.z;
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // mad r0.yzw, r0.yyzw, r1.xxyz, l(0.000000, 0.010000, 0.010000, 0.010000)
    float stage29_y = r0.y * r1.x + 0.01f;
    float stage29_z = r0.z * r1.y + 0.01f;
    float stage29_w = r0.w * r1.z + 0.01f;
    r0.y = stage29_y;
    r0.z = stage29_z;
    r0.w = stage29_w;
    // mad r1.xy, v4.xyxx, cb0[15].yzyy, cb0[16].xzxx
    float stage30_x = v4.x * cb0[15].y + cb0[16].x;
    float stage30_y = v4.y * cb0[15].z + cb0[16].z;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t4.xyzw, s4, l(0.000000)
    float4 fetch31 = t4.SampleBias(s4, r1.xy, 0.0f);
    r1.x = fetch31.x;
    // mad r1.xy, r1.xxxx, cb0[16].wwww, v4.xyxx
    float stage32_x = r1.x * cb0[16].w + v4.x;
    float stage32_y = r1.x * cb0[16].w + v4.y;
    r1.x = stage32_x;
    r1.y = stage32_y;
    // mad r1.xy, r1.xyxx, cb0[17].xyxx, cb0[17].zwzz
    float stage33_x = r1.x * cb0[17].x + cb0[17].z;
    float stage33_y = r1.y * cb0[17].y + cb0[17].w;
    r1.x = stage33_x;
    r1.y = stage33_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t5.xyzw, s5, l(0.000000)
    float4 fetch34 = t5.SampleBias(s5, r1.xy, 0.0f);
    r1.x = fetch34.x;
    r1.y = fetch34.y;
    r1.z = fetch34.z;
    // mul r1.xyz, r1.xyzx, cb0[18].xxxx
    float stage35_x = r1.x * cb0[18].x;
    float stage35_y = r1.y * cb0[18].x;
    float stage35_z = r1.z * cb0[18].x;
    r1.x = stage35_x;
    r1.y = stage35_y;
    r1.z = stage35_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage36_x = max(abs(r1.x), 1e-06f);
    float stage36_y = max(abs(r1.y), 1e-06f);
    float stage36_z = max(abs(r1.z), 1e-06f);
    r1.x = stage36_x;
    r1.y = stage36_y;
    r1.z = stage36_z;
    // log r1.xyz, r1.xyzx
    float stage37_x = log2(r1.x);
    float stage37_y = log2(r1.y);
    float stage37_z = log2(r1.z);
    r1.x = stage37_x;
    r1.y = stage37_y;
    r1.z = stage37_z;
    // mul r1.xyz, r1.xyzx, cb0[18].yyyy
    float stage38_x = r1.x * cb0[18].y;
    float stage38_y = r1.y * cb0[18].y;
    float stage38_z = r1.z * cb0[18].y;
    r1.x = stage38_x;
    r1.y = stage38_y;
    r1.z = stage38_z;
    // exp r1.xyz, r1.xyzx
    float stage39_x = exp2(r1.x);
    float stage39_y = exp2(r1.y);
    float stage39_z = exp2(r1.z);
    r1.x = stage39_x;
    r1.y = stage39_y;
    r1.z = stage39_z;
    // dp3 r1.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r2.xyz, -r1.xyzx, r1.wwww
    float stage40_x = -r1.x + r1.w;
    float stage40_y = -r1.y + r1.w;
    float stage40_z = -r1.z + r1.w;
    r2.x = stage40_x;
    r2.y = stage40_y;
    r2.z = stage40_z;
    // mad r1.xyz, cb0[18].zzzz, r2.xyzx, r1.xyzx
    float stage41_x = cb0[18].z * r2.x + r1.x;
    float stage41_y = cb0[18].z * r2.y + r1.y;
    float stage41_z = cb0[18].z * r2.z + r1.z;
    r1.x = stage41_x;
    r1.y = stage41_y;
    r1.z = stage41_z;
    // add r0.yzw, r0.yyzw, r1.xxyz
    float stage42_y = r0.y + r1.x;
    float stage42_z = r0.z + r1.y;
    float stage42_w = r0.w + r1.z;
    r0.y = stage42_y;
    r0.z = stage42_z;
    r0.w = stage42_w;
    // mad r0.yzw, r0.yyzw, cb0[1].xxyz, cb0[2].xxyz
    float stage43_y = r0.y * cb0[1].x + cb0[2].x;
    float stage43_z = r0.z * cb0[1].y + cb0[2].y;
    float stage43_w = r0.w * cb0[1].z + cb0[2].z;
    r0.y = stage43_y;
    r0.z = stage43_z;
    r0.w = stage43_w;
    // mul r0.yzw, r0.yyzw, v5.wwww
    float stage44_y = r0.y * v5.w;
    float stage44_z = r0.z * v5.w;
    float stage44_w = r0.w * v5.w;
    r0.y = stage44_y;
    r0.z = stage44_z;
    r0.w = stage44_w;
    // mul r1.x, v4.y, cb0[18].w
    r1.x = v4.y * cb0[18].w;
    // frc r1.x, r1.x
    r1.x = frac(r1.x);
    // add r1.y, -r1.x, l(1.000000)
    r1.y = -r1.x + 1.0f;
    // mul r1.z, cb0[3].x, cb0[19].x
    r1.z = cb0[3].x * cb0[19].x;
    // mul r1.z, r1.z, cb0[19].y
    r1.z = r1.z * cb0[19].y;
    // div r1.xy, r1.xyxx, r1.zzzz
    float stage45_x = r1.x / r1.z;
    float stage45_y = r1.y / r1.z;
    r1.x = stage45_x;
    r1.y = stage45_y;
    // round_ni r1.xy, r1.xyxx
    float stage46_x = floor(r1.x);
    float stage46_y = floor(r1.y);
    r1.x = stage46_x;
    r1.y = stage46_y;
    // mul r1.x, r1.y, r1.x
    r1.x = r1.y * r1.x;
    // mul_sat r0.x, r0.x, r1.x
    r0.x = saturate(r0.x * r1.x);
    // mul r0.x, r0.x, cb0[1].w
    r0.x = r0.x * cb0[1].w;
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // mul o0.xyz, r0.xxxx, r0.yzwy
    float stage47_x = r0.x * r0.y;
    float stage47_y = r0.x * r0.z;
    float stage47_z = r0.x * r0.w;
    o0.x = stage47_x;
    o0.y = stage47_y;
    o0.z = stage47_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_m_pa_highpixelring_squareoutline_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
