// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0_Constants0 : register(b0)
{
    float4 cb0[21];
};
cbuffer Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0_OUTPUT Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0(Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mad r0.x, cb0[3].y, cb0[9].w, cb0[10].x
    r0.x = cb0[3].y * cb0[9].w + cb0[10].x;
    // mad r0.x, v4.y, cb0[9].z, r0.x
    r0.x = v4.y * cb0[9].z + r0.x;
    // mad r0.y, cb0[3].y, cb0[10].z, cb0[10].w
    r0.y = cb0[3].y * cb0[10].z + cb0[10].w;
    // mad r0.y, cb0[11].x, v4.x, r0.y
    r0.y = cb0[11].x * v4.x + r0.y;
    // mad r1.x, cb0[10].y, r0.x, r0.y
    r1.x = cb0[10].y * r0.x + r0.y;
    // mad r1.y, r0.y, cb0[11].y, r0.x
    r1.y = r0.y * cb0[11].y + r0.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xy, r1.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch1 = t1.SampleBias(s1, r1.xy, 0.0f);
    r0.x = fetch1.x;
    r0.y = fetch1.y;
    // mul r0.xy, r0.xyxx, cb0[11].zzzz
    float stage2_x = r0.x * cb0[11].z;
    float stage2_y = r0.y * cb0[11].z;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // mad r0.z, cb0[3].y, cb0[7].z, cb0[7].w
    r0.z = cb0[3].y * cb0[7].z + cb0[7].w;
    // mad r0.z, v4.y, cb0[7].y, r0.z
    r0.z = v4.y * cb0[7].y + r0.z;
    // mad r0.w, cb0[3].y, cb0[8].y, cb0[8].z
    r0.w = cb0[3].y * cb0[8].y + cb0[8].z;
    // mad r0.w, cb0[8].w, v4.x, r0.w
    r0.w = cb0[8].w * v4.x + r0.w;
    // mad r1.x, cb0[8].x, r0.z, r0.w
    r1.x = cb0[8].x * r0.z + r0.w;
    // mad r1.y, r0.w, cb0[9].x, r0.z
    r1.y = r0.w * cb0[9].x + r0.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.zw, r1.xyxx, t0.zwxy, s0, l(0.000000)
    float4 fetch3 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.z = fetch3.x;
    r0.w = fetch3.y;
    // mad r0.xy, cb0[9].yyyy, r0.zwzz, r0.xyxx
    float stage4_x = cb0[9].y * r0.z + r0.x;
    float stage4_y = cb0[9].y * r0.w + r0.y;
    r0.x = stage4_x;
    r0.y = stage4_y;
    // mul r0.xy, r0.xyxx, cb0[3].zzzz
    float stage5_x = r0.x * cb0[3].z;
    float stage5_y = r0.y * cb0[3].z;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // mad r0.z, cb0[3].x, cb0[12].y, cb0[12].z
    r0.z = cb0[3].x * cb0[12].y + cb0[12].z;
    // mad r0.z, v4.y, cb0[12].x, r0.z
    r0.z = v4.y * cb0[12].x + r0.z;
    // mad r0.w, cb0[3].x, cb0[13].x, cb0[13].y
    r0.w = cb0[3].x * cb0[13].x + cb0[13].y;
    // mad r0.w, cb0[13].z, v4.x, r0.w
    r0.w = cb0[13].z * v4.x + r0.w;
    // mad r1.x, cb0[12].w, r0.z, r0.w
    r1.x = cb0[12].w * r0.z + r0.w;
    // mad r1.y, r0.w, cb0[13].w, r0.z
    r1.y = r0.w * cb0[13].w + r0.z;
    // mad r0.zw, cb0[14].xxxx, r0.xxxy, r1.xxxy
    float stage6_z = cb0[14].x * r0.x + r1.x;
    float stage6_w = cb0[14].x * r0.y + r1.y;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r0.zwzz, t2.xyzw, s3, l(0.000000)
    float4 fetch7 = t2.SampleBias(s3, r0.zw, 0.0f);
    r1.x = fetch7.x;
    r1.y = fetch7.y;
    r1.z = fetch7.z;
    // mul r1.xyzw, r1.xxyz, cb0[14].yyyy
    float stage8_x = r1.x * cb0[14].y;
    float stage8_y = r1.x * cb0[14].y;
    float stage8_z = r1.y * cb0[14].y;
    float stage8_w = r1.z * cb0[14].y;
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    r1.w = stage8_w;
    // mad r0.z, cb0[3].x, cb0[14].w, cb0[15].x
    r0.z = cb0[3].x * cb0[14].w + cb0[15].x;
    // mad r0.z, v4.y, cb0[14].z, r0.z
    r0.z = v4.y * cb0[14].z + r0.z;
    // mad r0.w, cb0[3].x, cb0[15].z, cb0[15].w
    r0.w = cb0[3].x * cb0[15].z + cb0[15].w;
    // mad r0.w, cb0[16].x, v4.x, r0.w
    r0.w = cb0[16].x * v4.x + r0.w;
    // mad r2.x, cb0[15].y, r0.z, r0.w
    r2.x = cb0[15].y * r0.z + r0.w;
    // mad r2.y, r0.w, cb0[16].y, r0.z
    r2.y = r0.w * cb0[16].y + r0.z;
    // mad r0.zw, cb0[16].zzzz, r0.xxxy, r2.xxxy
    float stage9_z = cb0[16].z * r0.x + r2.x;
    float stage9_w = cb0[16].z * r0.y + r2.y;
    r0.z = stage9_z;
    r0.w = stage9_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.zwzz, t3.xyzw, s4, l(0.000000)
    float4 fetch10 = t3.SampleBias(s4, r0.zw, 0.0f);
    r2.x = fetch10.x;
    r2.y = fetch10.y;
    r2.z = fetch10.z;
    // mul r2.xyzw, r2.xxyz, cb0[16].wwww
    float stage11_x = r2.x * cb0[16].w;
    float stage11_y = r2.x * cb0[16].w;
    float stage11_z = r2.y * cb0[16].w;
    float stage11_w = r2.z * cb0[16].w;
    r2.x = stage11_x;
    r2.y = stage11_y;
    r2.z = stage11_z;
    r2.w = stage11_w;
    // mul r1.xyzw, r1.xyzw, r2.xyzw
    float stage12_x = r1.x * r2.x;
    float stage12_y = r1.y * r2.y;
    float stage12_z = r1.z * r2.z;
    float stage12_w = r1.w * r2.w;
    r1.x = stage12_x;
    r1.y = stage12_y;
    r1.z = stage12_z;
    r1.w = stage12_w;
    // mad r0.z, cb0[3].x, cb0[17].y, cb0[17].z
    r0.z = cb0[3].x * cb0[17].y + cb0[17].z;
    // mad r0.z, v4.y, cb0[17].x, r0.z
    r0.z = v4.y * cb0[17].x + r0.z;
    // mad r0.w, cb0[3].x, cb0[18].x, cb0[18].y
    r0.w = cb0[3].x * cb0[18].x + cb0[18].y;
    // mad r0.w, cb0[18].z, v4.x, r0.w
    r0.w = cb0[18].z * v4.x + r0.w;
    // mad r2.x, cb0[17].w, r0.z, r0.w
    r2.x = cb0[17].w * r0.z + r0.w;
    // mad r2.y, r0.w, cb0[18].w, r0.z
    r2.y = r0.w * cb0[18].w + r0.z;
    // mad r0.zw, cb0[19].xxxx, r0.xxxy, r2.xxxy
    float stage13_z = cb0[19].x * r0.x + r2.x;
    float stage13_w = cb0[19].x * r0.y + r2.y;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.zwzz, t4.xyzw, s5, l(0.000000)
    float4 fetch14 = t4.SampleBias(s5, r0.zw, 0.0f);
    r2.x = fetch14.x;
    r2.y = fetch14.y;
    r2.z = fetch14.z;
    // mul r1.xyzw, r1.xyzw, r2.xxyz
    float stage15_x = r1.x * r2.x;
    float stage15_y = r1.y * r2.x;
    float stage15_z = r1.z * r2.y;
    float stage15_w = r1.w * r2.z;
    r1.x = stage15_x;
    r1.y = stage15_y;
    r1.z = stage15_z;
    r1.w = stage15_w;
    // max r1.xyzw, |r1.xyzw|, l(0.000001, 0.000001, 0.000001, 0.000001)
    float stage16_x = max(abs(r1.x), 1e-06f);
    float stage16_y = max(abs(r1.y), 1e-06f);
    float stage16_z = max(abs(r1.z), 1e-06f);
    float stage16_w = max(abs(r1.w), 1e-06f);
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    r1.w = stage16_w;
    // log r1.xyzw, r1.xyzw
    float stage17_x = log2(r1.x);
    float stage17_y = log2(r1.y);
    float stage17_z = log2(r1.z);
    float stage17_w = log2(r1.w);
    r1.x = stage17_x;
    r1.y = stage17_y;
    r1.z = stage17_z;
    r1.w = stage17_w;
    // mul r0.z, cb0[3].w, cb0[19].y
    r0.z = cb0[3].w * cb0[19].y;
    // mul r1.xyzw, r1.xyzw, r0.zzzz
    float stage18_x = r1.x * r0.z;
    float stage18_y = r1.y * r0.z;
    float stage18_z = r1.z * r0.z;
    float stage18_w = r1.w * r0.z;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    r1.w = stage18_w;
    // exp r1.xyzw, r1.xyzw
    float stage19_x = exp2(r1.x);
    float stage19_y = exp2(r1.y);
    float stage19_z = exp2(r1.z);
    float stage19_w = exp2(r1.w);
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    r1.w = stage19_w;
    // mul_sat r1.xyzw, r1.xyzw, cb0[19].zzzz
    float stage20_x = saturate(r1.x * cb0[19].z);
    float stage20_y = saturate(r1.y * cb0[19].z);
    float stage20_z = saturate(r1.z * cb0[19].z);
    float stage20_w = saturate(r1.w * cb0[19].z);
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.z = stage20_z;
    r1.w = stage20_w;
    // mad r0.z, cb0[3].x, cb0[4].y, cb0[4].z
    r0.z = cb0[3].x * cb0[4].y + cb0[4].z;
    // mad r0.z, v4.y, cb0[4].x, r0.z
    r0.z = v4.y * cb0[4].x + r0.z;
    // add r0.z, r0.z, cb0[5].y
    r0.z = r0.z + cb0[5].y;
    // mad r0.w, cb0[3].x, cb0[6].x, cb0[6].y
    r0.w = cb0[3].x * cb0[6].x + cb0[6].y;
    // mad r0.w, cb0[6].z, v4.x, r0.w
    r0.w = cb0[6].z * v4.x + r0.w;
    // add r0.w, r0.w, cb0[6].w
    r0.w = r0.w + cb0[6].w;
    // mad r2.x, cb0[5].z, r0.z, r0.w
    r2.x = cb0[5].z * r0.z + r0.w;
    // mad r2.y, r0.w, cb0[7].x, r0.z
    r2.y = r0.w * cb0[7].x + r0.z;
    // mad r0.xy, cb0[11].wwww, r0.xyxx, r2.xyxx
    float stage21_x = cb0[11].w * r0.x + r2.x;
    float stage21_y = cb0[11].w * r0.y + r2.y;
    r0.x = stage21_x;
    r0.y = stage21_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t5.xyzw, s2, l(0.000000)
    float4 fetch22 = t5.SampleBias(s2, r0.xy, 0.0f);
    r0.x = fetch22.x;
    r0.y = fetch22.y;
    r0.z = fetch22.z;
    // mul r2.xyz, r1.yzwy, r0.xyzx
    float stage23_x = r1.y * r0.x;
    float stage23_y = r1.z * r0.y;
    float stage23_z = r1.w * r0.z;
    r2.x = stage23_x;
    r2.y = stage23_y;
    r2.z = stage23_z;
    // dp3 r0.w, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r0.xyz, -r0.xyzx, r1.yzwy, r0.wwww
    float stage24_x = -r0.x * r1.y + r0.w;
    float stage24_y = -r0.y * r1.z + r0.w;
    float stage24_z = -r0.z * r1.w + r0.w;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // mul_sat r0.w, r1.x, cb0[1].w
    r0.w = saturate(r1.x * cb0[1].w);
    // mul o0.w, r0.w, cb0[0].x
    o0.w = r0.w * cb0[0].x;
    // mad r0.xyz, cb0[19].wwww, r0.xyzx, r2.xyzx
    float stage25_x = cb0[19].w * r0.x + r2.x;
    float stage25_y = cb0[19].w * r0.y + r2.y;
    float stage25_z = cb0[19].w * r0.z + r2.z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage26_x = max(abs(r0.x), 1e-06f);
    float stage26_y = max(abs(r0.y), 1e-06f);
    float stage26_z = max(abs(r0.z), 1e-06f);
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // log r0.xyz, r0.xyzx
    float stage27_x = log2(r0.x);
    float stage27_y = log2(r0.y);
    float stage27_z = log2(r0.z);
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // mul r0.xyz, r0.xyzx, cb0[20].xxxx
    float stage28_x = r0.x * cb0[20].x;
    float stage28_y = r0.y * cb0[20].x;
    float stage28_z = r0.z * cb0[20].x;
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // exp r0.xyz, r0.xyzx
    float stage29_x = exp2(r0.x);
    float stage29_y = exp2(r0.y);
    float stage29_z = exp2(r0.z);
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mad r0.xyz, cb0[20].yyyy, r0.xyzx, cb0[20].zzzz
    float stage30_x = cb0[20].y * r0.x + cb0[20].z;
    float stage30_y = cb0[20].y * r0.y + cb0[20].z;
    float stage30_z = cb0[20].y * r0.z + cb0[20].z;
    r0.x = stage30_x;
    r0.y = stage30_y;
    r0.z = stage30_z;
    // mad r0.xyz, r0.xyzx, cb0[1].xyzx, cb0[2].xyzx
    float stage31_x = r0.x * cb0[1].x + cb0[2].x;
    float stage31_y = r0.y * cb0[1].y + cb0[2].y;
    float stage31_z = r0.z * cb0[1].z + cb0[2].z;
    r0.x = stage31_x;
    r0.y = stage31_y;
    r0.z = stage31_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage32_x = r0.x * v5.w + v5.x;
    float stage32_y = r0.y * v5.w + v5.y;
    float stage32_z = r0.z * v5.w + v5.z;
    o0.x = stage32_x;
    o0.y = stage32_y;
    o0.z = stage32_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage33_x = r0.x * v1.x;
    float stage33_y = r0.x * v1.y;
    float stage33_z = r0.x * v1.z;
    r0.x = stage33_x;
    r0.y = stage33_y;
    r0.z = stage33_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage34_x = r0.w * v0.z;
    float stage34_y = r0.w * v0.x;
    float stage34_z = r0.w * v0.y;
    r1.x = stage34_x;
    r1.y = stage34_y;
    r1.z = stage34_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage35_x = r0.x * r1.x;
    float stage35_y = r0.x * r1.y;
    float stage35_z = r0.x * r1.z;
    r0.x = stage35_x;
    r0.y = stage35_y;
    r0.z = stage35_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage36_x = r0.x / r0.w;
    float stage36_y = r0.y / r0.w;
    r0.x = stage36_x;
    r0.y = stage36_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage37_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage37_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage37_x;
    r1.y = stage37_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage38_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage38_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage38_x;
    r1.y = stage38_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage39_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage39_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage39_x;
    r1.y = stage39_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage40_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage40_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage40_x;
    r0.y = stage40_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage41_x = r0.x * 0.5f + 0.5f;
    float stage41_y = r0.y * 0.5f + 0.5f;
    o2.x = stage41_x;
    o2.y = stage41_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage42_z = 1.0f;
    float stage42_w = asfloat(0u);
    o2.z = stage42_z;
    o2.w = stage42_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage43_x = cb2[3].x;
    float stage43_y = cb2[3].y;
    float stage43_z = cb2[3].z;
    o3.x = stage43_x;
    o3.y = stage43_y;
    o3.z = stage43_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage44_x = asfloat(0u);
    float stage44_y = asfloat(0u);
    float stage44_z = asfloat(0u);
    float stage44_w = asfloat(0u);
    o4.x = stage44_x;
    o4.y = stage44_y;
    o4.z = stage44_z;
    o4.w = stage44_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage45_x = asfloat(0u);
    float stage45_y = asfloat(0u);
    float stage45_z = asfloat(0u);
    float stage45_w = asfloat(0u);
    o5.x = stage45_x;
    o5.y = stage45_y;
    o5.z = stage45_z;
    o5.w = stage45_w;

    Shade_Ue3_fx_m_basemaster_01_tr_5ee339d674b9b5a3d081fa1ca17b4887c6d0d99bf2590d7c74bba88d574ab0e0_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
