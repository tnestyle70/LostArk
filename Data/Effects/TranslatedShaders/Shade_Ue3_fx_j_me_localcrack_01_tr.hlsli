// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_j_me_localcrack_01_tr_Constants0 : register(b0)
{
    float4 cb0[12];
};
cbuffer Shade_Ue3_fx_j_me_localcrack_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);

struct Shade_Ue3_fx_j_me_localcrack_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
    float4 v7;
};

struct Shade_Ue3_fx_j_me_localcrack_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_j_me_localcrack_01_tr_OUTPUT Shade_Ue3_fx_j_me_localcrack_01_tr(Shade_Ue3_fx_j_me_localcrack_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // div r0.xy, v7.xyxx, v7.wwww
    float stage1_x = v7.x / v7.w;
    float stage1_y = v7.y / v7.w;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, r0.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage2_x = r0.x * cb2[0].x + cb2[0].w;
    float stage2_y = r0.y * cb2[0].y + cb2[0].z;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // mul r0.zw, v4.xxxy, cb0[8].xxxy
    float stage3_z = v4.x * cb0[8].x;
    float stage3_w = v4.y * cb0[8].y;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.zw, r0.zwzz, t0.zwxy, s0, l(0.000000)
    float4 fetch4 = t0.SampleBias(s0, r0.zw, 0.0f);
    r0.z = fetch4.x;
    r0.w = fetch4.y;
    // mad r1.xy, r0.zwzz, l(2.000000, 2.000000, 0.000000, 0.000000), l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage5_x = r0.z * 2.0f + -1.0f;
    float stage5_y = r0.w * 2.0f + -1.0f;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // dp2 r0.z, r1.xyxx, r1.xyxx
    r0.z = dot(r1.xy, r1.xy);
    // add r0.z, -r0.z, l(1.000000)
    r0.z = -r0.z + 1.0f;
    // max r0.z, r0.z, l(0.000000)
    r0.z = max(r0.z, 0.0f);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // add r1.z, r0.z, l(0.000010)
    r1.z = r0.z + 1e-05f;
    // dp3 r0.z, r1.xyzx, r1.xyzx
    r0.z = dot(r1.xyz, r1.xyz);
    // rsq r0.z, r0.z
    r0.z = rsqrt(r0.z);
    // mul r1.xyz, r0.zzzz, r1.xyzx
    float stage6_x = r0.z * r1.x;
    float stage6_y = r0.z * r1.y;
    float stage6_z = r0.z * r1.z;
    r1.x = stage6_x;
    r1.y = stage6_y;
    r1.z = stage6_z;
    // dp3 r0.z, v6.xyzx, v6.xyzx
    r0.z = dot(v6.xyz, v6.xyz);
    // rsq r0.z, r0.z
    r0.z = rsqrt(r0.z);
    // mul r2.xyz, r0.zzzz, v6.xyzx
    float stage7_x = r0.z * v6.x;
    float stage7_y = r0.z * v6.y;
    float stage7_z = r0.z * v6.z;
    r2.x = stage7_x;
    r2.y = stage7_y;
    r2.z = stage7_z;
    // dp3 r0.z, r1.xyzx, r2.xyzx
    r0.z = dot(r1.xyz, r2.xyz);
    // mul r3.xyz, r0.zzzz, r1.xyzx
    float stage8_x = r0.z * r1.x;
    float stage8_y = r0.z * r1.y;
    float stage8_z = r0.z * r1.z;
    r3.x = stage8_x;
    r3.y = stage8_y;
    r3.z = stage8_z;
    // mad r3.xyz, r3.xyzx, l(2.000000, 2.000000, 2.000000, 0.000000), -r2.xyzx
    float stage9_x = r3.x * 2.0f + -r2.x;
    float stage9_y = r3.y * 2.0f + -r2.y;
    float stage9_z = r3.z * 2.0f + -r2.z;
    r3.x = stage9_x;
    r3.y = stage9_y;
    r3.z = stage9_z;
    // dp3 r0.z, r2.xyzx, r3.xyzx
    r0.z = dot(r2.xyz, r3.xyz);
    // add r2.xy, r3.xyxx, cb0[3].xyxx
    float stage10_x = r3.x + cb0[3].x;
    float stage10_y = r3.y + cb0[3].y;
    r2.x = stage10_x;
    r2.y = stage10_y;
    // div r2.xy, r2.xyxx, cb0[9].yyyy
    float stage11_x = r2.x / cb0[9].y;
    float stage11_y = r2.y / cb0[9].y;
    r2.x = stage11_x;
    r2.y = stage11_y;
    // mad r2.xy, r2.xyxx, cb0[9].zwzz, cb0[10].xyxx
    float stage12_x = r2.x * cb0[9].z + cb0[10].x;
    float stage12_y = r2.y * cb0[9].w + cb0[10].y;
    r2.x = stage12_x;
    r2.y = stage12_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyw, r2.xyxx, t1.xywz, s1, l(0.000000)
    float4 fetch13 = t1.SampleBias(s1, r2.xy, 0.0f);
    r2.x = fetch13.x;
    r2.y = fetch13.y;
    r2.w = fetch13.z;
    // max r0.w, r2.z, l(0.000000)
    r0.w = max(r2.z, 0.0f);
    // add r0.w, -r0.w, l(1.000000)
    r0.w = -r0.w + 1.0f;
    // add r1.w, -|r0.z|, l(1.000000)
    r1.w = -(abs(r0.z)) + 1.0f;
    // mad r0.xy, r1.wwww, cb0[5].xxxx, r0.xyxx
    float stage14_x = r1.w * cb0[5].x + r0.x;
    float stage14_y = r1.w * cb0[5].x + r0.y;
    r0.x = stage14_x;
    r0.y = stage14_y;
    // add r1.w, cb0[5].y, cb0[5].y
    r1.w = cb0[5].y + cb0[5].y;
    // div r0.xy, r0.xyxx, r1.wwww
    float stage15_x = r0.x / r1.w;
    float stage15_y = r0.y / r1.w;
    r0.x = stage15_x;
    r0.y = stage15_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r3.xyz, r0.xyxx, t2.xyzw, s2, l(0.000000)
    float4 fetch16 = t2.SampleBias(s2, r0.xy, 0.0f);
    r3.x = fetch16.x;
    r3.y = fetch16.y;
    r3.z = fetch16.z;
    // dp3 r0.x, r3.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.x = dot(r3.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r4.xyz, -r3.xyzx, r0.xxxx
    float stage17_x = -r3.x + r0.x;
    float stage17_y = -r3.y + r0.x;
    float stage17_z = -r3.z + r0.x;
    r4.x = stage17_x;
    r4.y = stage17_y;
    r4.z = stage17_z;
    // mad r3.xyz, r4.xyzx, l(0.880000, 0.880000, 0.880000, 0.000000), r3.xyzx
    float stage18_x = r4.x * 0.88f + r3.x;
    float stage18_y = r4.y * 0.88f + r3.y;
    float stage18_z = r4.z * 0.88f + r3.z;
    r3.x = stage18_x;
    r3.y = stage18_y;
    r3.z = stage18_z;
    // max r4.xyz, |r3.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage19_x = max(abs(r3.x), 1e-06f);
    float stage19_y = max(abs(r3.y), 1e-06f);
    float stage19_z = max(abs(r3.z), 1e-06f);
    r4.x = stage19_x;
    r4.y = stage19_y;
    r4.z = stage19_z;
    // mul r4.xyz, r4.xyzx, r4.xyzx
    float stage20_x = r4.x * r4.x;
    float stage20_y = r4.y * r4.y;
    float stage20_z = r4.z * r4.z;
    r4.x = stage20_x;
    r4.y = stage20_y;
    r4.z = stage20_z;
    // mul r4.xyz, r4.xyzx, r4.xyzx
    float stage21_x = r4.x * r4.x;
    float stage21_y = r4.y * r4.y;
    float stage21_z = r4.z * r4.z;
    r4.x = stage21_x;
    r4.y = stage21_y;
    r4.z = stage21_z;
    // mad r3.xyz, -r4.xyzx, l(4.000000, 4.000000, 4.000000, 0.000000), r3.xyzx
    float stage22_x = -r4.x * 4.0f + r3.x;
    float stage22_y = -r4.y * 4.0f + r3.y;
    float stage22_z = -r4.z * 4.0f + r3.z;
    r3.x = stage22_x;
    r3.y = stage22_y;
    r3.z = stage22_z;
    // mul r4.xyz, r4.xyzx, l(4.000000, 4.000000, 4.000000, 0.000000)
    float stage23_x = r4.x * 4.0f;
    float stage23_y = r4.y * 4.0f;
    float stage23_z = r4.z * 4.0f;
    r4.x = stage23_x;
    r4.y = stage23_y;
    r4.z = stage23_z;
    // mad_sat r0.xyz, |r0.zzzz|, r3.xyzx, r4.xyzx
    float stage24_x = saturate(abs(r0.z) * r3.x + r4.x);
    float stage24_y = saturate(abs(r0.z) * r3.y + r4.y);
    float stage24_z = saturate(abs(r0.z) * r3.z + r4.z);
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // mul r0.xyz, r0.xyzx, cb0[1].xyzx
    float stage25_x = r0.x * cb0[1].x;
    float stage25_y = r0.y * cb0[1].y;
    float stage25_z = r0.z * cb0[1].z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // mul r1.w, |r0.w|, |r0.w|
    r1.w = abs(r0.w) * abs(r0.w);
    // mul r1.w, r1.w, r1.w
    r1.w = r1.w * r1.w;
    // mul r1.w, |r0.w|, r1.w
    r1.w = abs(r0.w) * r1.w;
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.w, r0.w, l(0), r1.w
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.w;
    // mul r3.xyz, r0.wwww, cb0[7].xyzx
    float stage26_x = r0.w * cb0[7].x;
    float stage26_y = r0.w * cb0[7].y;
    float stage26_z = r0.w * cb0[7].z;
    r3.x = stage26_x;
    r3.y = stage26_y;
    r3.z = stage26_z;
    // mul r0.w, r0.w, l(5.000000)
    r0.w = r0.w * 5.0f;
    // min r0.w, r0.w, l(1.000000)
    r0.w = min(r0.w, 1.0f);
    // mad r0.xyz, r0.xyzx, cb0[6].xyzx, r3.xyzx
    float stage27_x = r0.x * cb0[6].x + r3.x;
    float stage27_y = r0.y * cb0[6].y + r3.y;
    float stage27_z = r0.z * cb0[6].z + r3.z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // dp3 r1.w, r2.xywx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.w = dot(r2.xyw, float3(0.3f, 0.59f, 0.11f));
    // add r3.xyz, -r2.xywx, r1.wwww
    float stage28_x = -r2.x + r1.w;
    float stage28_y = -r2.y + r1.w;
    float stage28_z = -r2.w + r1.w;
    r3.x = stage28_x;
    r3.y = stage28_y;
    r3.z = stage28_z;
    // mad r2.xyz, cb0[10].zzzz, r3.xyzx, r2.xywx
    float stage29_x = cb0[10].z * r3.x + r2.x;
    float stage29_y = cb0[10].z * r3.y + r2.y;
    float stage29_z = cb0[10].z * r3.z + r2.w;
    r2.x = stage29_x;
    r2.y = stage29_y;
    r2.z = stage29_z;
    // max r2.xyz, |r2.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage30_x = max(abs(r2.x), 1e-06f);
    float stage30_y = max(abs(r2.y), 1e-06f);
    float stage30_z = max(abs(r2.z), 1e-06f);
    r2.x = stage30_x;
    r2.y = stage30_y;
    r2.z = stage30_z;
    // log r2.xyz, r2.xyzx
    float stage31_x = log2(r2.x);
    float stage31_y = log2(r2.y);
    float stage31_z = log2(r2.z);
    r2.x = stage31_x;
    r2.y = stage31_y;
    r2.z = stage31_z;
    // mul r2.xyz, r2.xyzx, cb0[10].wwww
    float stage32_x = r2.x * cb0[10].w;
    float stage32_y = r2.y * cb0[10].w;
    float stage32_z = r2.z * cb0[10].w;
    r2.x = stage32_x;
    r2.y = stage32_y;
    r2.z = stage32_z;
    // exp r2.xyz, r2.xyzx
    float stage33_x = exp2(r2.x);
    float stage33_y = exp2(r2.y);
    float stage33_z = exp2(r2.z);
    r2.x = stage33_x;
    r2.y = stage33_y;
    r2.z = stage33_z;
    // mad r0.xyz, r2.xyzx, cb0[4].xyzx, r0.xyzx
    float stage34_x = r2.x * cb0[4].x + r0.x;
    float stage34_y = r2.y * cb0[4].y + r0.y;
    float stage34_z = r2.z * cb0[4].z + r0.z;
    r0.x = stage34_x;
    r0.y = stage34_y;
    r0.z = stage34_z;
    // add r0.xyz, r0.xyzx, cb0[2].xyzx
    float stage35_x = r0.x + cb0[2].x;
    float stage35_y = r0.y + cb0[2].y;
    float stage35_z = r0.z + cb0[2].z;
    r0.x = stage35_x;
    r0.y = stage35_y;
    r0.z = stage35_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage36_x = r0.x * v5.w + v5.x;
    float stage36_y = r0.y * v5.w + v5.y;
    float stage36_z = r0.z * v5.w + v5.z;
    o0.x = stage36_x;
    o0.y = stage36_y;
    o0.z = stage36_z;
    // log r0.x, r0.w
    r0.x = log2(r0.w);
    // lt r0.y, r0.w, l(0.000001)
    r0.y = asfloat((r0.w < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.x, r0.x, cb0[11].x
    r0.x = r0.x * cb0[11].x;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mov_sat r0.z, cb0[1].w
    r0.z = saturate(cb0[1].w);
    // mul r0.x, r0.x, r0.z
    r0.x = r0.x * r0.z;
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // movc o0.w, r0.y, l(0), r0.x
    o0.w = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage37_x = r0.x * v1.x;
    float stage37_y = r0.x * v1.y;
    float stage37_z = r0.x * v1.z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r2.xyz, r0.wwww, v0.xyzx
    float stage38_x = r0.w * v0.x;
    float stage38_y = r0.w * v0.y;
    float stage38_z = r0.w * v0.z;
    r2.x = stage38_x;
    r2.y = stage38_y;
    r2.z = stage38_z;
    // mul r3.xyz, r0.zxyz, r2.yzxy
    float stage39_x = r0.z * r2.y;
    float stage39_y = r0.x * r2.z;
    float stage39_z = r0.y * r2.x;
    r3.x = stage39_x;
    r3.y = stage39_y;
    r3.z = stage39_z;
    // mad r3.xyz, r0.yzxy, r2.zxyz, -r3.xyzx
    float stage40_x = r0.y * r2.z + -r3.x;
    float stage40_y = r0.z * r2.x + -r3.y;
    float stage40_z = r0.x * r2.y + -r3.z;
    r3.x = stage40_x;
    r3.y = stage40_y;
    r3.z = stage40_z;
    // dp3 r0.z, r0.xyzx, r1.xyzx
    r0.z = dot(r0.xyz, r1.xyz);
    // dp3 r0.x, r2.xyzx, r1.xyzx
    r0.x = dot(r2.xyz, r1.xyz);
    // mul r2.xyz, r3.xyzx, v1.wwww
    float stage41_x = r3.x * v1.w;
    float stage41_y = r3.y * v1.w;
    float stage41_z = r3.z * v1.w;
    r2.x = stage41_x;
    r2.y = stage41_y;
    r2.z = stage41_z;
    // dp3 r0.y, r2.xyzx, r1.xyzx
    r0.y = dot(r2.xyz, r1.xyz);
    // dp3 r0.w, r0.xyzx, r0.xyzx
    r0.w = dot(r0.xyz, r0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r0.xyz, r0.wwww, r0.xyzx
    float stage42_x = r0.w * r0.x;
    float stage42_y = r0.w * r0.y;
    float stage42_z = r0.w * r0.z;
    r0.x = stage42_x;
    r0.y = stage42_y;
    r0.z = stage42_z;
    // ge r0.w, l(0.000000), r0.z
    r0.w = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // dp3 r0.z, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.z = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.zzzz
    float stage43_x = r0.x / r0.z;
    float stage43_y = r0.y / r0.z;
    r0.x = stage43_x;
    r0.y = stage43_y;
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage44_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage44_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage44_x;
    r1.y = stage44_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage45_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage45_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage45_x;
    r1.y = stage45_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage46_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage46_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage46_x;
    r1.y = stage46_y;
    // movc r0.xy, r0.wwww, r1.xyxx, r0.xyxx
    float stage47_x = asuint(r0.w) != 0u ? r1.x : r0.x;
    float stage47_y = asuint(r0.w) != 0u ? r1.y : r0.y;
    r0.x = stage47_x;
    r0.y = stage47_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage48_x = r0.x * 0.5f + 0.5f;
    float stage48_y = r0.y * 0.5f + 0.5f;
    o2.x = stage48_x;
    o2.y = stage48_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage49_z = 1.0f;
    float stage49_w = asfloat(0u);
    o2.z = stage49_z;
    o2.w = stage49_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage50_x = cb2[3].x;
    float stage50_y = cb2[3].y;
    float stage50_z = cb2[3].z;
    o3.x = stage50_x;
    o3.y = stage50_y;
    o3.z = stage50_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage51_x = asfloat(0u);
    float stage51_y = asfloat(0u);
    float stage51_z = asfloat(0u);
    float stage51_w = asfloat(0u);
    o4.x = stage51_x;
    o4.y = stage51_y;
    o4.z = stage51_z;
    o4.w = stage51_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage52_x = asfloat(0u);
    float stage52_y = asfloat(0u);
    float stage52_z = asfloat(0u);
    float stage52_w = asfloat(0u);
    o5.x = stage52_x;
    o5.y = stage52_y;
    o5.z = stage52_z;
    o5.w = stage52_w;

    Shade_Ue3_fx_j_me_localcrack_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
