// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_mm_basic_01_tr_Constants0 : register(b0)
{
    float4 cb0[6];
};
cbuffer Shade_Ue3_fx_mm_basic_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
Texture2D t0 : register(t0);

struct Shade_Ue3_fx_mm_basic_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v5;
};

struct Shade_Ue3_fx_mm_basic_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_mm_basic_01_tr_OUTPUT Shade_Ue3_fx_mm_basic_01_tr(Shade_Ue3_fx_mm_basic_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mad r0.x, cb0[4].x, cb0[3].x, l(-1.000000)
    r0.x = cb0[4].x * cb0[3].x + -1.0f;
    // mul r0.x, r0.x, l(0.500000)
    r0.x = r0.x * 0.5f;
    // mul r0.y, cb0[3].x, cb0[4].x
    r0.y = cb0[3].x * cb0[4].x;
    // mad r0.xy, r0.yyyy, v2.xyxx, -r0.xxxx
    float stage1_x = r0.y * v2.x + -r0.x;
    float stage1_y = r0.y * v2.y + -r0.x;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyzw, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch2 = t0.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch2.x;
    r0.y = fetch2.y;
    r0.z = fetch2.z;
    r0.w = fetch2.w;
    // log r1.x, |r0.w|
    r1.x = log2(abs(r0.w));
    // mul r1.x, r1.x, cb0[5].y
    r1.x = r1.x * cb0[5].y;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r1.x, r1.x, cb0[1].w
    r1.x = r1.x * cb0[1].w;
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.w, r0.w, l(0), |r1.x|
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : abs(r1.x);
    // log r1.x, r0.w
    r1.x = log2(r0.w);
    // lt r0.w, r0.w, l(0.000001)
    r0.w = asfloat((r0.w < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, cb0[3].y
    r1.x = r1.x * cb0[3].y;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // min r1.x, r1.x, l(1.000000)
    r1.x = min(r1.x, 1.0f);
    // mul r1.x, r1.x, cb0[0].x
    r1.x = r1.x * cb0[0].x;
    // movc o0.w, r0.w, l(0), r1.x
    o0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.x;
    // mul r1.xyz, r0.xyzx, cb0[4].yyyy
    float stage3_x = r0.x * cb0[4].y;
    float stage3_y = r0.y * cb0[4].y;
    float stage3_z = r0.z * cb0[4].y;
    r1.x = stage3_x;
    r1.y = stage3_y;
    r1.z = stage3_z;
    // dp3 r0.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r0.xyz, -cb0[4].yyyy, r0.xyzx, r0.wwww
    float stage4_x = -cb0[4].y * r0.x + r0.w;
    float stage4_y = -cb0[4].y * r0.y + r0.w;
    float stage4_z = -cb0[4].y * r0.z + r0.w;
    r0.x = stage4_x;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // mad r0.xyz, cb0[4].zzzz, r0.xyzx, r1.xyzx
    float stage5_x = cb0[4].z * r0.x + r1.x;
    float stage5_y = cb0[4].z * r0.y + r1.y;
    float stage5_z = cb0[4].z * r0.z + r1.z;
    r0.x = stage5_x;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // mul r1.xyz, r0.xyzx, cb0[4].wwww
    float stage6_x = r0.x * cb0[4].w;
    float stage6_y = r0.y * cb0[4].w;
    float stage6_z = r0.z * cb0[4].w;
    r1.x = stage6_x;
    r1.y = stage6_y;
    r1.z = stage6_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage7_x = max(abs(r1.x), 1e-06f);
    float stage7_y = max(abs(r1.y), 1e-06f);
    float stage7_z = max(abs(r1.z), 1e-06f);
    r1.x = stage7_x;
    r1.y = stage7_y;
    r1.z = stage7_z;
    // log r1.xyz, r1.xyzx
    float stage8_x = log2(r1.x);
    float stage8_y = log2(r1.y);
    float stage8_z = log2(r1.z);
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    // mul r1.xyz, r1.xyzx, cb0[5].xxxx
    float stage9_x = r1.x * cb0[5].x;
    float stage9_y = r1.y * cb0[5].x;
    float stage9_z = r1.z * cb0[5].x;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    // exp r1.xyz, r1.xyzx
    float stage10_x = exp2(r1.x);
    float stage10_y = exp2(r1.y);
    float stage10_z = exp2(r1.z);
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // dp3 r0.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r0.xyz, r0.xyzx, r0.wwww
    float stage11_x = r0.x + r0.w;
    float stage11_y = r0.y + r0.w;
    float stage11_z = r0.z + r0.w;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mad r0.xyz, r0.xyzx, cb0[1].xyzx, cb0[2].xyzx
    float stage12_x = r0.x * cb0[1].x + cb0[2].x;
    float stage12_y = r0.y * cb0[1].y + cb0[2].y;
    float stage12_z = r0.z * cb0[1].z + cb0[2].z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage13_x = r0.x * v5.w + v5.x;
    float stage13_y = r0.y * v5.w + v5.y;
    float stage13_z = r0.z * v5.w + v5.z;
    o0.x = stage13_x;
    o0.y = stage13_y;
    o0.z = stage13_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage14_x = r0.x * v1.x;
    float stage14_y = r0.x * v1.y;
    float stage14_z = r0.x * v1.z;
    r0.x = stage14_x;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage15_x = r0.w * v0.z;
    float stage15_y = r0.w * v0.x;
    float stage15_z = r0.w * v0.y;
    r1.x = stage15_x;
    r1.y = stage15_y;
    r1.z = stage15_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage16_x = r0.x * r1.x;
    float stage16_y = r0.x * r1.y;
    float stage16_z = r0.x * r1.z;
    r0.x = stage16_x;
    r0.y = stage16_y;
    r0.z = stage16_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage17_x = r0.x / r0.w;
    float stage17_y = r0.y / r0.w;
    r0.x = stage17_x;
    r0.y = stage17_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage18_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage18_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage18_x;
    r1.y = stage18_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage19_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage19_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage19_x;
    r1.y = stage19_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage20_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage20_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage20_x;
    r1.y = stage20_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage21_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage21_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage21_x;
    r0.y = stage21_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage22_x = r0.x * 0.5f + 0.5f;
    float stage22_y = r0.y * 0.5f + 0.5f;
    o2.x = stage22_x;
    o2.y = stage22_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage23_z = 1.0f;
    float stage23_w = asfloat(0u);
    o2.z = stage23_z;
    o2.w = stage23_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage24_x = cb2[3].x;
    float stage24_y = cb2[3].y;
    float stage24_z = cb2[3].z;
    o3.x = stage24_x;
    o3.y = stage24_y;
    o3.z = stage24_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage25_x = asfloat(0u);
    float stage25_y = asfloat(0u);
    float stage25_z = asfloat(0u);
    float stage25_w = asfloat(0u);
    o4.x = stage25_x;
    o4.y = stage25_y;
    o4.z = stage25_z;
    o4.w = stage25_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage26_x = asfloat(0u);
    float stage26_y = asfloat(0u);
    float stage26_z = asfloat(0u);
    float stage26_w = asfloat(0u);
    o5.x = stage26_x;
    o5.y = stage26_y;
    o5.z = stage26_z;
    o5.w = stage26_w;

    Shade_Ue3_fx_mm_basic_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
