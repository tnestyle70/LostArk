// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_e_pa_electric_01_tr_Constants0 : register(b0)
{
    float4 cb0[11];
};
cbuffer Shade_Ue3_fx_e_pa_electric_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_e_pa_electric_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_e_pa_electric_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_e_pa_electric_01_tr_OUTPUT Shade_Ue3_fx_e_pa_electric_01_tr(Shade_Ue3_fx_e_pa_electric_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.xy, v4.yzyy, cb0[8].xwxx
    float stage1_x = v4.y * cb0[8].x;
    float stage1_y = v4.z * cb0[8].w;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mul r0.y, r0.y, cb0[7].x
    r0.y = r0.y * cb0[7].x;
    // mul r0.xz, r0.xxxx, v2.xxyx
    float stage2_x = r0.x * v2.x;
    float stage2_z = r0.x * v2.y;
    r0.x = stage2_x;
    r0.z = stage2_z;
    // mul r0.yw, r0.yyyy, l(0.000000, 0.700000, 0.000000, 0.500000)
    float stage3_y = r0.y * 0.7f;
    float stage3_w = r0.y * 0.5f;
    r0.y = stage3_y;
    r0.w = stage3_w;
    // frc r0.yw, r0.yyyw
    float stage4_y = frac(r0.y);
    float stage4_w = frac(r0.w);
    r0.y = stage4_y;
    r0.w = stage4_w;
    // add r0.yw, -r0.yyyw, l(0.000000, 1.000000, 0.000000, 1.000000)
    float stage5_y = -r0.y + 1.0f;
    float stage5_w = -r0.w + 1.0f;
    r0.y = stage5_y;
    r0.w = stage5_w;
    // mul r0.yw, r0.yyyw, cb0[9].xxxy
    float stage6_y = r0.y * cb0[9].x;
    float stage6_w = r0.w * cb0[9].y;
    r0.y = stage6_y;
    r0.w = stage6_w;
    // add r0.y, r0.w, r0.y
    r0.y = r0.w + r0.y;
    // mad r0.yw, r0.xxxz, l(0.000000, 0.200000, 0.000000, 0.200000), r0.yyyy
    float stage7_y = r0.x * 0.2f + r0.y;
    float stage7_w = r0.z * 0.2f + r0.y;
    r0.y = stage7_y;
    r0.w = stage7_w;
    // mad r0.xz, r0.xxzx, l(0.200000, 0.000000, 0.200000, 0.000000), cb0[6].xxyx
    float stage8_x = r0.x * 0.2f + cb0[6].x;
    float stage8_z = r0.z * 0.2f + cb0[6].y;
    r0.x = stage8_x;
    r0.z = stage8_z;
    // add r0.xz, r0.yywy, r0.xxzx
    float stage9_x = r0.y + r0.x;
    float stage9_z = r0.w + r0.z;
    r0.x = stage9_x;
    r0.z = stage9_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.yw, r0.ywyy, t1.zxwy, s1, l(0.000000)
    float4 fetch10 = t1.SampleBias(s1, r0.yw, 0.0f);
    r0.y = fetch10.x;
    r0.w = fetch10.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xz, r0.xzxx, t0.xzyw, s0, l(0.000000)
    float4 fetch11 = t0.SampleBias(s0, r0.xz, 0.0f);
    r0.x = fetch11.x;
    r0.z = fetch11.y;
    // add r0.yw, -r0.xxxz, r0.yyyw
    float stage12_y = -r0.x + r0.y;
    float stage12_w = -r0.z + r0.w;
    r0.y = stage12_y;
    r0.w = stage12_w;
    // mad r0.yw, r0.yyyw, l(0.000000, 0.500000, 0.000000, 0.500000), r0.xxxz
    float stage13_y = r0.y * 0.5f + r0.x;
    float stage13_w = r0.w * 0.5f + r0.z;
    r0.y = stage13_y;
    r0.w = stage13_w;
    // add r1.xy, -v2.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage14_x = -v2.x + 0.5f;
    float stage14_y = -v2.y + 0.5f;
    r1.x = stage14_x;
    r1.y = stage14_y;
    // mul r1.xy, r1.xyxx, cb0[2].xyxx
    float stage15_x = r1.x * cb0[2].x;
    float stage15_y = r1.y * cb0[2].y;
    r1.x = stage15_x;
    r1.y = stage15_y;
    // dp2 r1.z, cb0[4].xyxx, r1.xyxx
    r1.z = dot(cb0[4].xy, r1.xy);
    // dp2 r1.x, cb0[3].xyxx, r1.xyxx
    r1.x = dot(cb0[3].xy, r1.xy);
    // add r1.y, r1.z, cb0[5].y
    r1.y = r1.z + cb0[5].y;
    // add r1.xy, r1.xyxx, l(0.300000, 0.500000, 0.000000, 0.000000)
    float stage16_x = r1.x + 0.3f;
    float stage16_y = r1.y + 0.5f;
    r1.x = stage16_x;
    r1.y = stage16_y;
    // add r0.xyzw, r0.xyzw, r1.xxyy
    float stage17_x = r0.x + r1.x;
    float stage17_y = r0.y + r1.x;
    float stage17_z = r0.z + r1.y;
    float stage17_w = r0.w + r1.y;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r0.xzxx, t3.xyzw, s3, l(0.000000)
    float4 fetch18 = t3.SampleBias(s3, r0.xz, 0.0f);
    r1.x = fetch18.x;
    r1.y = fetch18.y;
    r1.z = fetch18.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.ywyy, t2.xyzw, s2, l(0.000000)
    float4 fetch19 = t2.SampleBias(s2, r0.yw, 0.0f);
    r0.x = fetch19.x;
    r0.y = fetch19.y;
    r0.z = fetch19.z;
    // add r1.xyzw, -r0.xxyz, r1.xxyz
    float stage20_x = -r0.x + r1.x;
    float stage20_y = -r0.x + r1.x;
    float stage20_z = -r0.y + r1.y;
    float stage20_w = -r0.z + r1.z;
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.z = stage20_z;
    r1.w = stage20_w;
    // mad r0.xyzw, cb0[10].xxxx, r1.xyzw, r0.xxyz
    float stage21_x = cb0[10].x * r1.x + r0.x;
    float stage21_y = cb0[10].x * r1.y + r0.x;
    float stage21_z = cb0[10].x * r1.z + r0.y;
    float stage21_w = cb0[10].x * r1.w + r0.z;
    r0.x = stage21_x;
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mul r0.x, r0.x, v4.w
    r0.x = r0.x * v4.w;
    // mad r0.yzw, r0.yyzw, v3.xxyz, cb0[1].xxyz
    float stage22_y = r0.y * v3.x + cb0[1].x;
    float stage22_z = r0.z * v3.y + cb0[1].y;
    float stage22_w = r0.w * v3.z + cb0[1].z;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // mad o0.xyz, r0.yzwy, v5.wwww, v5.xyzx
    float stage23_x = r0.y * v5.w + v5.x;
    float stage23_y = r0.z * v5.w + v5.y;
    float stage23_z = r0.w * v5.w + v5.z;
    o0.x = stage23_x;
    o0.y = stage23_y;
    o0.z = stage23_z;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[10].y
    r0.y = r0.y * cb0[10].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul_sat r0.y, r0.y, v3.w
    r0.y = saturate(r0.y * v3.w);
    // mul r0.y, r0.y, cb0[0].x
    r0.y = r0.y * cb0[0].x;
    // movc o0.w, r0.x, l(0), r0.y
    o0.w = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage24_x = r0.x * v1.x;
    float stage24_y = r0.x * v1.y;
    float stage24_z = r0.x * v1.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage25_x = r0.w * v0.z;
    float stage25_y = r0.w * v0.x;
    float stage25_z = r0.w * v0.y;
    r1.x = stage25_x;
    r1.y = stage25_y;
    r1.z = stage25_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage26_x = r0.x * r1.x;
    float stage26_y = r0.x * r1.y;
    float stage26_z = r0.x * r1.z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage27_x = r0.x / r0.w;
    float stage27_y = r0.y / r0.w;
    r0.x = stage27_x;
    r0.y = stage27_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage28_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage28_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage28_x;
    r1.y = stage28_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage29_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage29_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage29_x;
    r1.y = stage29_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage30_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage30_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage31_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage31_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage31_x;
    r0.y = stage31_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage32_x = r0.x * 0.5f + 0.5f;
    float stage32_y = r0.y * 0.5f + 0.5f;
    o2.x = stage32_x;
    o2.y = stage32_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage33_z = 1.0f;
    float stage33_w = asfloat(0u);
    o2.z = stage33_z;
    o2.w = stage33_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage34_x = cb2[3].x;
    float stage34_y = cb2[3].y;
    float stage34_z = cb2[3].z;
    o3.x = stage34_x;
    o3.y = stage34_y;
    o3.z = stage34_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage35_x = asfloat(0u);
    float stage35_y = asfloat(0u);
    float stage35_z = asfloat(0u);
    float stage35_w = asfloat(0u);
    o4.x = stage35_x;
    o4.y = stage35_y;
    o4.z = stage35_z;
    o4.w = stage35_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage36_x = asfloat(0u);
    float stage36_y = asfloat(0u);
    float stage36_z = asfloat(0u);
    float stage36_w = asfloat(0u);
    o5.x = stage36_x;
    o5.y = stage36_y;
    o5.z = stage36_z;
    o5.w = stage36_w;

    Shade_Ue3_fx_e_pa_electric_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
