// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_pa_noise_01_tr_Constants0 : register(b0)
{
    float4 cb0[5];
};
cbuffer Shade_Ue3_fx_m_pa_noise_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);

struct Shade_Ue3_fx_m_pa_noise_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_m_pa_noise_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_m_pa_noise_01_tr_OUTPUT Shade_Ue3_fx_m_pa_noise_01_tr(Shade_Ue3_fx_m_pa_noise_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // mad r0.x, -r0.x, l(2.222222), l(1.000000)
    r0.x = -r0.x * 2.222222f + 1.0f;
    // mul r0.x, r0.x, l(0.166667)
    r0.x = r0.x * 0.166667f;
    // max r0.x, r0.x, l(0.000000)
    r0.x = max(r0.x, 0.0f);
    // mul r0.x, r0.x, l(0.500000)
    r0.x = r0.x * 0.5f;
    // mov r1.xz, l(0,0,0,0)
    float stage2_x = asfloat(0u);
    float stage2_z = asfloat(0u);
    r1.x = stage2_x;
    r1.z = stage2_z;
    // mul r1.yw, v4.zzzz, l(0.000000, 0.600000, 0.000000, 0.400000)
    float stage3_y = v4.z * 0.6f;
    float stage3_w = v4.z * 0.4f;
    r1.y = stage3_y;
    r1.w = stage3_w;
    // mad r0.yz, v2.xxyx, l(0.000000, 1.000000, 2.000000, 0.000000), r1.xxyx
    float stage4_y = v2.x * 1.0f + r1.x;
    float stage4_z = v2.y * 2.0f + r1.y;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // mad r1.xy, v2.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), r1.zwzz
    float stage5_x = v2.x * 2.0f + r1.z;
    float stage5_y = v2.y * 2.0f + r1.w;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t0.yzwx, s1, l(0.000000)
    float4 fetch6 = t0.SampleBias(s1, r1.xy, 0.0f);
    r0.w = fetch6.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.yz, r0.yzyy, t0.zxyw, s1, l(0.000000)
    float4 fetch7 = t0.SampleBias(s1, r0.yz, 0.0f);
    r0.y = fetch7.x;
    r0.z = fetch7.y;
    // add r1.xy, v4.wyww, l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage8_x = v4.w + -1.0f;
    float stage8_y = v4.y + -1.0f;
    r1.x = stage8_x;
    r1.y = stage8_y;
    // mad r1.xz, r1.xxxx, r0.yyzy, v2.xxyx
    float stage9_x = r1.x * r0.y + v2.x;
    float stage9_z = r1.x * r0.z + v2.y;
    r1.x = stage9_x;
    r1.z = stage9_z;
    // add r0.z, r0.w, -r1.y
    r0.z = r0.w + -r1.y;
    // mul_sat r0.z, r0.z, l(3.000000)
    r0.z = saturate(r0.z * 3.0f);
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xzxx, t1.yzwx, s2, l(0.000000)
    float4 fetch10 = t1.SampleBias(s2, r1.xz, 0.0f);
    r0.w = fetch10.x;
    // mul r1.x, r0.y, l(3.000000)
    r1.x = r0.y * 3.0f;
    // mul r0.w, r0.w, r1.x
    r0.w = r0.w * r1.x;
    // mad r0.x, r0.x, r0.y, r0.w
    r0.x = r0.x * r0.y + r0.w;
    // mul r0.x, r0.z, r0.x
    r0.x = r0.z * r0.x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[4].y
    r0.y = r0.y * cb0[4].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, cb0[4].z
    r0.y = r0.y * cb0[4].z;
    // mul r0.y, r0.y, v3.w
    r0.y = r0.y * v3.w;
    // mul r0.y, r0.y, cb0[0].x
    r0.y = r0.y * cb0[0].x;
    // movc o0.w, r0.x, l(0), r0.y
    o0.w = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // add r0.xy, v2.xyxx, cb0[2].xyxx
    float stage11_x = v2.x + cb0[2].x;
    float stage11_y = v2.y + cb0[2].y;
    r0.x = stage11_x;
    r0.y = stage11_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s0, l(0.000000)
    float4 fetch12 = t2.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch12.x;
    // add r0.yz, v2.xxyx, cb0[3].xxyx
    float stage13_y = v2.x + cb0[3].x;
    float stage13_z = v2.y + cb0[3].y;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t2.yxzw, s0, l(0.000000)
    float4 fetch14 = t2.SampleBias(s0, r0.yz, 0.0f);
    r0.y = fetch14.x;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul r0.x, r0.x, l(15.000000)
    r0.x = r0.x * 15.0f;
    // mad r0.xyz, r0.xxxx, v3.xyzx, cb0[1].xyzx
    float stage15_x = r0.x * v3.x + cb0[1].x;
    float stage15_y = r0.x * v3.y + cb0[1].y;
    float stage15_z = r0.x * v3.z + cb0[1].z;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage16_x = r0.x * v5.w + v5.x;
    float stage16_y = r0.y * v5.w + v5.y;
    float stage16_z = r0.z * v5.w + v5.z;
    o0.x = stage16_x;
    o0.y = stage16_y;
    o0.z = stage16_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage17_x = r0.x * v1.x;
    float stage17_y = r0.x * v1.y;
    float stage17_z = r0.x * v1.z;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage18_x = r0.w * v0.z;
    float stage18_y = r0.w * v0.x;
    float stage18_z = r0.w * v0.y;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage19_x = r0.x * r1.x;
    float stage19_y = r0.x * r1.y;
    float stage19_z = r0.x * r1.z;
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage20_x = r0.x / r0.w;
    float stage20_y = r0.y / r0.w;
    r0.x = stage20_x;
    r0.y = stage20_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage21_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage21_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage21_x;
    r1.y = stage21_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage22_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage22_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage22_x;
    r1.y = stage22_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage23_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage23_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage23_x;
    r1.y = stage23_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage24_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage24_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage24_x;
    r0.y = stage24_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage25_x = r0.x * 0.5f + 0.5f;
    float stage25_y = r0.y * 0.5f + 0.5f;
    o2.x = stage25_x;
    o2.y = stage25_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage26_z = 1.0f;
    float stage26_w = asfloat(0u);
    o2.z = stage26_z;
    o2.w = stage26_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage27_x = cb2[3].x;
    float stage27_y = cb2[3].y;
    float stage27_z = cb2[3].z;
    o3.x = stage27_x;
    o3.y = stage27_y;
    o3.z = stage27_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage28_x = asfloat(0u);
    float stage28_y = asfloat(0u);
    float stage28_z = asfloat(0u);
    float stage28_w = asfloat(0u);
    o4.x = stage28_x;
    o4.y = stage28_y;
    o4.z = stage28_z;
    o4.w = stage28_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage29_x = asfloat(0u);
    float stage29_y = asfloat(0u);
    float stage29_z = asfloat(0u);
    float stage29_w = asfloat(0u);
    o5.x = stage29_x;
    o5.y = stage29_y;
    o5.z = stage29_z;
    o5.w = stage29_w;

    Shade_Ue3_fx_m_pa_noise_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
