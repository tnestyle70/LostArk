// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_bfx_d_pa_circ_01_ad_Constants0 : register(b0)
{
    float4 cb0[6];
};
cbuffer Shade_Ue3_bfx_d_pa_circ_01_ad_Constants2 : register(b2)
{
    float4 cb2[2];
};
SamplerState s0 : register(s0);
Texture2D t0 : register(t0);

struct Shade_Ue3_bfx_d_pa_circ_01_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v5;
    float4 v7;
};

struct Shade_Ue3_bfx_d_pa_circ_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_bfx_d_pa_circ_01_ad_OUTPUT Shade_Ue3_bfx_d_pa_circ_01_ad(Shade_Ue3_bfx_d_pa_circ_01_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v5 = stage.v5;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 o0 = (float4)0;

    // div r0.xy, v7.xyxx, v7.wwww
    float stage1_x = v7.x / v7.w;
    float stage1_y = v7.y / v7.w;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, r0.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage2_x = r0.x * cb2[0].x + cb2[0].w;
    float stage2_y = r0.y * cb2[0].y + cb2[0].z;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch3 = t0.SampleLevel(s0, r0.xy, 0.0f);
    r0.x = fetch3.x;
    // min r0.x, r0.x, l(0.999000)
    r0.x = min(r0.x, 0.999f);
    // mad r0.y, r0.x, cb2[1].z, -cb2[1].w
    r0.y = r0.x * cb2[1].z + -cb2[1].w;
    // mad r0.x, r0.x, cb2[1].x, cb2[1].y
    r0.x = r0.x * cb2[1].x + cb2[1].y;
    // div r0.y, l(1.000000, 1.000000, 1.000000, 1.000000), r0.y
    r0.y = 1.0f / r0.y;
    // add r0.x, r0.y, r0.x
    r0.x = r0.y + r0.x;
    // add r0.x, r0.x, -v7.w
    r0.x = r0.x + -v7.w;
    // add r0.y, -cb0[5].y, l(1.000000)
    r0.y = -cb0[5].y + 1.0f;
    // mul r0.y, r0.y, l(100.000000)
    r0.y = r0.y * 100.0f;
    // max r0.y, r0.y, l(0.001000)
    r0.y = max(r0.y, 0.001f);
    // div_sat r0.x, r0.x, r0.y
    r0.x = saturate(r0.x / r0.y);
    // add r0.yz, v2.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage4_y = v2.x + -0.5f;
    float stage4_z = v2.y + -0.5f;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // dp2 r0.y, r0.yzyy, r0.yzyy
    r0.y = dot(r0.yz, r0.yz);
    // sqrt r0.y, r0.y
    r0.y = sqrt(r0.y);
    // mad r0.y, -r0.y, cb0[2].z, l(1.000000)
    r0.y = -r0.y * cb0[2].z + 1.0f;
    // mul_sat r0.y, r0.y, cb0[3].w
    r0.y = saturate(r0.y * cb0[3].w);
    // log r0.z, r0.y
    r0.z = log2(r0.y);
    // lt r0.y, r0.y, l(0.000001)
    r0.y = asfloat((r0.y < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[4].y
    r0.z = r0.z * cb0[4].y;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // mul_sat r0.z, r0.z, cb0[4].z
    r0.z = saturate(r0.z * cb0[4].z);
    // mul_sat r0.z, r0.z, v3.w
    r0.z = saturate(r0.z * v3.w);
    // mul r0.x, r0.x, r0.z
    r0.x = r0.x * r0.z;
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // movc r0.x, r0.y, l(0), r0.x
    r0.x = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // add r0.yzw, v3.xxyz, cb0[1].xxyz
    float stage5_y = v3.x + cb0[1].x;
    float stage5_z = v3.y + cb0[1].y;
    float stage5_w = v3.z + cb0[1].z;
    r0.y = stage5_y;
    r0.z = stage5_z;
    r0.w = stage5_w;
    // mul r0.yzw, r0.yyzw, v5.wwww
    float stage6_y = r0.y * v5.w;
    float stage6_z = r0.z * v5.w;
    float stage6_w = r0.w * v5.w;
    r0.y = stage6_y;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // mul o0.xyz, r0.xxxx, r0.yzwy
    float stage7_x = r0.x * r0.y;
    float stage7_y = r0.x * r0.z;
    float stage7_z = r0.x * r0.w;
    o0.x = stage7_x;
    o0.y = stage7_y;
    o0.z = stage7_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_bfx_d_pa_circ_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
