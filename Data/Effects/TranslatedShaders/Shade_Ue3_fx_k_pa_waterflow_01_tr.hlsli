// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_pa_waterflow_01_tr_Constants0 : register(b0)
{
    float4 cb0[14];
};
cbuffer Shade_Ue3_fx_k_pa_waterflow_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_k_pa_waterflow_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_pa_waterflow_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_pa_waterflow_01_tr_OUTPUT Shade_Ue3_fx_k_pa_waterflow_01_tr(Shade_Ue3_fx_k_pa_waterflow_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.yxyy, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.y + -0.5f;
    float stage1_y = v2.x + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // max r0.z, |r0.y|, |r0.x|
    r0.z = max(abs(r0.y), abs(r0.x));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r0.y|, |r0.x|
    r0.w = min(abs(r0.y), abs(r0.x));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.x, r0.w, l(0.020835), l(-0.085133)
    r1.x = r0.w * 0.020835f + -0.085133f;
    // mad r1.x, r0.w, r1.x, l(0.180141)
    r1.x = r0.w * r1.x + 0.180141f;
    // mad r1.x, r0.w, r1.x, l(-0.330299)
    r1.x = r0.w * r1.x + -0.330299f;
    // mad r0.w, r0.w, r1.x, l(0.999866)
    r0.w = r0.w * r1.x + 0.999866f;
    // mul r1.x, r0.w, r0.z
    r1.x = r0.w * r0.z;
    // mad r1.x, r1.x, l(-2.000000), l(1.570796)
    r1.x = r1.x * -2.0f + 1.570796f;
    // lt r1.y, |r0.y|, |r0.x|
    r1.y = asfloat((abs(r0.y) < abs(r0.x)) ? 0xffffffffu : 0u);
    // and r1.x, r1.y, r1.x
    r1.x = asfloat(asuint(r1.y) & asuint(r1.x));
    // mad r0.z, r0.z, r0.w, r1.x
    r0.z = r0.z * r0.w + r1.x;
    // lt r0.w, r0.y, -r0.y
    r0.w = asfloat((r0.y < -r0.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r0.y, r0.x
    r0.w = min(r0.y, r0.x);
    // lt r0.w, r0.w, -r0.w
    r0.w = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // max r1.x, r0.y, r0.x
    r1.x = max(r0.y, r0.x);
    // dp2 r0.x, -r0.xyxx, -r0.xyxx
    r0.x = dot(-r0.xy, -r0.xy);
    // sqrt r0.y, r0.x
    r0.y = sqrt(r0.x);
    // ge r1.x, r1.x, -r1.x
    r1.x = asfloat((r1.x >= -r1.x) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, r1.x
    r0.w = asfloat(asuint(r0.w) & asuint(r1.x));
    // movc r0.z, r0.w, -r0.z, r0.z
    r0.z = asuint(r0.w) != 0u ? -r0.z : r0.z;
    // mad r0.z, r0.z, l(0.318471), l(1.000000)
    r0.z = r0.z * 0.318471f + 1.0f;
    // mul r0.w, r0.y, cb0[9].x
    r0.w = r0.y * cb0[9].x;
    // mad r0.x, r0.z, l(0.500000), r0.w
    r0.x = r0.z * 0.5f + r0.w;
    // mul r0.zw, r0.xxxy, cb0[3].xxxy
    float stage2_z = r0.x * cb0[3].x;
    float stage2_w = r0.y * cb0[3].y;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r0.zwzz, t1.yzxw, s0, l(0.000000)
    float4 fetch3 = t1.SampleBias(s0, r0.zw, 0.0f);
    r0.z = fetch3.x;
    // mov r1.w, cb0[9].w
    r1.w = cb0[9].w;
    // mov r1.xz, l(0,0,0,0)
    float stage4_x = asfloat(0u);
    float stage4_z = asfloat(0u);
    r1.x = stage4_x;
    r1.z = stage4_z;
    // mad r2.xyzw, r0.xyxy, cb0[3].xyxy, r1.wzzw
    float stage5_x = r0.x * cb0[3].x + r1.w;
    float stage5_y = r0.y * cb0[3].y + r1.z;
    float stage5_z = r0.x * cb0[3].x + r1.z;
    float stage5_w = r0.y * cb0[3].y + r1.w;
    r2.x = stage5_x;
    r2.y = stage5_y;
    r2.z = stage5_z;
    r2.w = stage5_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r2.xyxx, t1.yzwx, s0, l(0.000000)
    float4 fetch6 = t1.SampleBias(s0, r2.xy, 0.0f);
    r0.w = fetch6.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.z, r2.zwzz, t1.yzxw, s0, l(0.000000)
    float4 fetch7 = t1.SampleBias(s0, r2.zw, 0.0f);
    r1.z = fetch7.x;
    // add r2.y, -r0.z, r1.z
    r2.y = -r0.z + r1.z;
    // add r2.x, -r0.z, r0.w
    r2.x = -r0.z + r0.w;
    // mul r2.xy, r2.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage8_x = r2.x * 8.0f;
    float stage8_y = r2.y * 8.0f;
    r2.x = stage8_x;
    r2.y = stage8_y;
    // mov r2.z, l(0)
    r2.z = asfloat(0u);
    // add r2.xyz, -r2.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage9_x = -r2.x + 0.0f;
    float stage9_y = -r2.y + 0.0f;
    float stage9_z = -r2.z + 1.0f;
    r2.x = stage9_x;
    r2.y = stage9_y;
    r2.z = stage9_z;
    // dp3 r0.z, r2.xyzx, r2.xyzx
    r0.z = dot(r2.xyz, r2.xyz);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // div r0.zw, r2.xxxy, r0.zzzz
    float stage10_z = r2.x / r0.z;
    float stage10_w = r2.y / r0.z;
    r0.z = stage10_z;
    r0.w = stage10_w;
    // mad r0.zw, r0.zzzw, l(0.000000, 0.000000, 0.500000, 0.500000), l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage11_z = r0.z * 0.5f + 0.5f;
    float stage11_w = r0.w * 0.5f + 0.5f;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // mov r1.y, v4.w
    r1.y = v4.w;
    // mad r1.xy, r0.xyxx, cb0[8].xyxx, r1.xyxx
    float stage12_x = r0.x * cb0[8].x + r1.x;
    float stage12_y = r0.y * cb0[8].y + r1.y;
    r1.x = stage12_x;
    r1.y = stage12_y;
    // mad r1.xy, v4.xxxx, r0.zwzz, r1.xyxx
    float stage13_x = v4.x * r0.z + r1.x;
    float stage13_y = v4.x * r0.w + r1.y;
    r1.x = stage13_x;
    r1.y = stage13_y;
    // mul r0.zw, r0.zzzw, v4.xxxx
    float stage14_z = r0.z * v4.x;
    float stage14_w = r0.w * v4.x;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t2.xyzw, s4, l(0.000000)
    float4 fetch15 = t2.SampleBias(s4, r1.xy, 0.0f);
    r1.x = fetch15.x;
    // max r1.x, |r1.x|, l(0.000001)
    r1.x = max(abs(r1.x), 1e-06f);
    // log r1.x, r1.x
    r1.x = log2(r1.x);
    // mul r1.x, r1.x, cb0[12].z
    r1.x = r1.x * cb0[12].z;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mad r1.yz, r0.xxyx, cb0[7].xxyx, r0.zzwz
    float stage16_y = r0.x * cb0[7].x + r0.z;
    float stage16_z = r0.y * cb0[7].y + r0.w;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mad r0.xy, r0.xyxx, cb0[2].xyxx, r0.zwzz
    float stage17_x = r0.x * cb0[2].x + r0.z;
    float stage17_y = r0.y * cb0[2].y + r0.w;
    r0.x = stage17_x;
    r0.y = stage17_y;
    // add r0.xy, r0.xyxx, cb0[4].xyxx
    float stage18_x = r0.x + cb0[4].x;
    float stage18_y = r0.y + cb0[4].y;
    r0.x = stage18_x;
    r0.y = stage18_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t4.xyzw, s1, l(0.000000)
    float4 fetch19 = t4.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch19.x;
    r0.y = fetch19.y;
    r0.z = fetch19.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.yzw, r1.yzyy, t3.wxyz, s2, l(0.000000)
    float4 fetch20 = t3.SampleBias(s2, r1.yz, 0.0f);
    r1.y = fetch20.x;
    r1.z = fetch20.y;
    r1.w = fetch20.z;
    // add r0.w, r1.y, cb0[12].w
    r0.w = r1.y + cb0[12].w;
    // max r1.yzw, |r1.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage21_y = max(abs(r1.y), 1e-06f);
    float stage21_z = max(abs(r1.z), 1e-06f);
    float stage21_w = max(abs(r1.w), 1e-06f);
    r1.y = stage21_y;
    r1.z = stage21_z;
    r1.w = stage21_w;
    // log r1.yzw, r1.yyzw
    float stage22_y = log2(r1.y);
    float stage22_z = log2(r1.z);
    float stage22_w = log2(r1.w);
    r1.y = stage22_y;
    r1.z = stage22_z;
    r1.w = stage22_w;
    // mul r1.yzw, r1.yyzw, cb0[11].zzzz
    float stage23_y = r1.y * cb0[11].z;
    float stage23_z = r1.z * cb0[11].z;
    float stage23_w = r1.w * cb0[11].z;
    r1.y = stage23_y;
    r1.z = stage23_z;
    r1.w = stage23_w;
    // exp r1.yzw, r1.yyzw
    float stage24_y = exp2(r1.y);
    float stage24_z = exp2(r1.z);
    float stage24_w = exp2(r1.w);
    r1.y = stage24_y;
    r1.z = stage24_z;
    r1.w = stage24_w;
    // mad r1.yzw, -cb0[11].wwww, r1.yyzw, l(0.000000, 1.000000, 1.000000, 1.000000)
    float stage25_y = -cb0[11].w * r1.y + 1.0f;
    float stage25_z = -cb0[11].w * r1.z + 1.0f;
    float stage25_w = -cb0[11].w * r1.w + 1.0f;
    r1.y = stage25_y;
    r1.z = stage25_z;
    r1.w = stage25_w;
    // mul r0.w, r0.w, cb0[13].x
    r0.w = r0.w * cb0[13].x;
    // mul r0.w, r0.w, v4.z
    r0.w = r0.w * v4.z;
    // mul r0.w, r0.w, l(6.283185)
    r0.w = r0.w * 6.283185f;
    // sincos r0.w, null, r0.w
    float stage26_w = r0.w;
    r0.w = sin(stage26_w);
    // max r0.w, |r0.w|, l(0.000001)
    r0.w = max(abs(r0.w), 1e-06f);
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, cb0[13].y
    r0.w = r0.w * cb0[13].y;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul_sat r0.w, r0.w, cb0[13].z
    r0.w = saturate(r0.w * cb0[13].z);
    // mul r0.w, r0.w, r1.x
    r0.w = r0.w * r1.x;
    // mul r0.w, r0.w, cb0[13].w
    r0.w = r0.w * cb0[13].w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, v2.xyxx, t0.xyzw, s3, l(0.000000)
    float4 fetch27 = t0.SampleBias(s3, v2.xy, 0.0f);
    r1.x = fetch27.x;
    // mul_sat r0.w, r0.w, r1.x
    r0.w = saturate(r0.w * r1.x);
    // mul r0.w, r0.w, v3.w
    r0.w = r0.w * v3.w;
    // mul o0.w, r0.w, cb0[0].x
    o0.w = r0.w * cb0[0].x;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r2.xyz, -r0.xyzx, r0.wwww
    float stage28_x = -r0.x + r0.w;
    float stage28_y = -r0.y + r0.w;
    float stage28_z = -r0.z + r0.w;
    r2.x = stage28_x;
    r2.y = stage28_y;
    r2.z = stage28_z;
    // mad r0.xyz, cb0[10].yyyy, r2.xyzx, r0.xyzx
    float stage29_x = cb0[10].y * r2.x + r0.x;
    float stage29_y = cb0[10].y * r2.y + r0.y;
    float stage29_z = cb0[10].y * r2.z + r0.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage30_x = max(abs(r0.x), 1e-06f);
    float stage30_y = max(abs(r0.y), 1e-06f);
    float stage30_z = max(abs(r0.z), 1e-06f);
    r0.x = stage30_x;
    r0.y = stage30_y;
    r0.z = stage30_z;
    // log r0.xyz, r0.xyzx
    float stage31_x = log2(r0.x);
    float stage31_y = log2(r0.y);
    float stage31_z = log2(r0.z);
    r0.x = stage31_x;
    r0.y = stage31_y;
    r0.z = stage31_z;
    // mul r0.xyz, r0.xyzx, cb0[10].zzzz
    float stage32_x = r0.x * cb0[10].z;
    float stage32_y = r0.y * cb0[10].z;
    float stage32_z = r0.z * cb0[10].z;
    r0.x = stage32_x;
    r0.y = stage32_y;
    r0.z = stage32_z;
    // exp r0.xyz, r0.xyzx
    float stage33_x = exp2(r0.x);
    float stage33_y = exp2(r0.y);
    float stage33_z = exp2(r0.z);
    r0.x = stage33_x;
    r0.y = stage33_y;
    r0.z = stage33_z;
    // mul r0.xyz, r0.xyzx, cb0[10].wwww
    float stage34_x = r0.x * cb0[10].w;
    float stage34_y = r0.y * cb0[10].w;
    float stage34_z = r0.z * cb0[10].w;
    r0.x = stage34_x;
    r0.y = stage34_y;
    r0.z = stage34_z;
    // add r2.xyz, -cb0[5].xyzx, cb0[6].xyzx
    float stage35_x = -cb0[5].x + cb0[6].x;
    float stage35_y = -cb0[5].y + cb0[6].y;
    float stage35_z = -cb0[5].z + cb0[6].z;
    r2.x = stage35_x;
    r2.y = stage35_y;
    r2.z = stage35_z;
    // mad r1.xyz, r1.yzwy, r2.xyzx, cb0[5].xyzx
    float stage36_x = r1.y * r2.x + cb0[5].x;
    float stage36_y = r1.z * r2.y + cb0[5].y;
    float stage36_z = r1.w * r2.z + cb0[5].z;
    r1.x = stage36_x;
    r1.y = stage36_y;
    r1.z = stage36_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, r1.xyzx
    float stage37_x = r0.x * v3.x + r1.x;
    float stage37_y = r0.y * v3.y + r1.y;
    float stage37_z = r0.z * v3.z + r1.z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // add r0.xyz, r0.xyzx, cb0[1].xyzx
    float stage38_x = r0.x + cb0[1].x;
    float stage38_y = r0.y + cb0[1].y;
    float stage38_z = r0.z + cb0[1].z;
    r0.x = stage38_x;
    r0.y = stage38_y;
    r0.z = stage38_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage39_x = r0.x * v5.w + v5.x;
    float stage39_y = r0.y * v5.w + v5.y;
    float stage39_z = r0.z * v5.w + v5.z;
    o0.x = stage39_x;
    o0.y = stage39_y;
    o0.z = stage39_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage40_x = r0.x * v1.x;
    float stage40_y = r0.x * v1.y;
    float stage40_z = r0.x * v1.z;
    r0.x = stage40_x;
    r0.y = stage40_y;
    r0.z = stage40_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage41_x = r0.w * v0.z;
    float stage41_y = r0.w * v0.x;
    float stage41_z = r0.w * v0.y;
    r1.x = stage41_x;
    r1.y = stage41_y;
    r1.z = stage41_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage42_x = r0.x * r1.x;
    float stage42_y = r0.x * r1.y;
    float stage42_z = r0.x * r1.z;
    r0.x = stage42_x;
    r0.y = stage42_y;
    r0.z = stage42_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage43_x = r0.x / r0.w;
    float stage43_y = r0.y / r0.w;
    r0.x = stage43_x;
    r0.y = stage43_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage44_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage44_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage44_x;
    r1.y = stage44_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage45_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage45_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage45_x;
    r1.y = stage45_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage46_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage46_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage46_x;
    r1.y = stage46_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage47_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage47_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage47_x;
    r0.y = stage47_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage48_x = r0.x * 0.5f + 0.5f;
    float stage48_y = r0.y * 0.5f + 0.5f;
    o2.x = stage48_x;
    o2.y = stage48_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage49_z = 1.0f;
    float stage49_w = asfloat(0u);
    o2.z = stage49_z;
    o2.w = stage49_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage50_x = cb2[3].x;
    float stage50_y = cb2[3].y;
    float stage50_z = cb2[3].z;
    o3.x = stage50_x;
    o3.y = stage50_y;
    o3.z = stage50_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage51_x = asfloat(0u);
    float stage51_y = asfloat(0u);
    float stage51_z = asfloat(0u);
    float stage51_w = asfloat(0u);
    o4.x = stage51_x;
    o4.y = stage51_y;
    o4.z = stage51_z;
    o4.w = stage51_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage52_x = asfloat(0u);
    float stage52_y = asfloat(0u);
    float stage52_z = asfloat(0u);
    float stage52_w = asfloat(0u);
    o5.x = stage52_x;
    o5.y = stage52_y;
    o5.z = stage52_z;
    o5.w = stage52_w;

    Shade_Ue3_fx_k_pa_waterflow_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
