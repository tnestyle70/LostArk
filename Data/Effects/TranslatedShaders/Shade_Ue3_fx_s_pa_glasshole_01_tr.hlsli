// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_s_pa_glasshole_01_tr_Constants0 : register(b0)
{
    float4 cb0[21];
};
cbuffer Shade_Ue3_fx_s_pa_glasshole_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
SamplerState s6 : register(s6);
SamplerState s7 : register(s7);
SamplerState s8 : register(s8);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);
Texture2D t6 : register(t6);
Texture2D t7 : register(t7);
Texture2D t8 : register(t8);

struct Shade_Ue3_fx_s_pa_glasshole_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    float4 v6;
    float4 v7;
};

struct Shade_Ue3_fx_s_pa_glasshole_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_s_pa_glasshole_01_tr_OUTPUT Shade_Ue3_fx_s_pa_glasshole_01_tr(Shade_Ue3_fx_s_pa_glasshole_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.yxyy, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.y + -0.5f;
    float stage1_y = v2.x + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // max r0.z, |r0.y|, |r0.x|
    r0.z = max(abs(r0.y), abs(r0.x));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r0.y|, |r0.x|
    r0.w = min(abs(r0.y), abs(r0.x));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.x, r0.w, l(0.020835), l(-0.085133)
    r1.x = r0.w * 0.020835f + -0.085133f;
    // mad r1.x, r0.w, r1.x, l(0.180141)
    r1.x = r0.w * r1.x + 0.180141f;
    // mad r1.x, r0.w, r1.x, l(-0.330299)
    r1.x = r0.w * r1.x + -0.330299f;
    // mad r0.w, r0.w, r1.x, l(0.999866)
    r0.w = r0.w * r1.x + 0.999866f;
    // mul r1.x, r0.w, r0.z
    r1.x = r0.w * r0.z;
    // mad r1.x, r1.x, l(-2.000000), l(1.570796)
    r1.x = r1.x * -2.0f + 1.570796f;
    // lt r1.y, |r0.y|, |r0.x|
    r1.y = asfloat((abs(r0.y) < abs(r0.x)) ? 0xffffffffu : 0u);
    // and r1.x, r1.y, r1.x
    r1.x = asfloat(asuint(r1.y) & asuint(r1.x));
    // mad r0.z, r0.z, r0.w, r1.x
    r0.z = r0.z * r0.w + r1.x;
    // lt r0.w, r0.y, -r0.y
    r0.w = asfloat((r0.y < -r0.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r0.y, r0.x
    r0.w = min(r0.y, r0.x);
    // lt r0.w, r0.w, -r0.w
    r0.w = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // max r1.x, r0.y, r0.x
    r1.x = max(r0.y, r0.x);
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // ge r0.y, r1.x, -r1.x
    r0.y = asfloat((r1.x >= -r1.x) ? 0xffffffffu : 0u);
    // and r0.y, r0.y, r0.w
    r0.y = asfloat(asuint(r0.y) & asuint(r0.w));
    // movc r0.y, r0.y, -r0.z, r0.z
    r0.y = asuint(r0.y) != 0u ? -r0.z : r0.z;
    // mul r0.y, r0.y, cb0[16].x
    r0.y = r0.y * cb0[16].x;
    // add r0.z, -r0.x, l(0.500000)
    r0.z = -r0.x + 0.5f;
    // dp2 r0.z, r0.zzzz, cb0[16].yyyy
    r0.z = dot(r0.zz, cb0[16].yy);
    // mad r0.y, r0.y, l(0.318310), r0.z
    r0.y = r0.y * 0.31831f + r0.z;
    // add r0.y, r0.y, cb0[16].z
    r0.y = r0.y + cb0[16].z;
    // mul r1.x, r0.y, cb0[17].y
    r1.x = r0.y * cb0[17].y;
    // add r0.y, r0.x, r0.x
    r0.y = r0.x + r0.x;
    // log r0.y, r0.y
    r0.y = log2(r0.y);
    // mul r0.y, r0.y, cb0[16].w
    r0.y = r0.y * cb0[16].w;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, v4.w
    r0.y = r0.y * v4.w;
    // lt r0.z, r0.x, l(0.000000)
    r0.z = asfloat((r0.x < 0.0f) ? 0xffffffffu : 0u);
    // movc r0.y, r0.z, l(0), r0.y
    r0.y = asuint(r0.z) != 0u ? asfloat(0u) : r0.y;
    // add r0.z, v4.z, cb0[17].x
    r0.z = v4.z + cb0[17].x;
    // mad r0.y, r0.z, l(-0.400000), r0.y
    r0.y = r0.z * -0.4f + r0.y;
    // mul r0.z, v4.w, cb0[18].x
    r0.z = v4.w * cb0[18].x;
    // mad r1.y, r0.y, cb0[17].z, r0.z
    r1.y = r0.y * cb0[17].z + r0.z;
    // add r0.yz, r1.xxyx, cb0[9].xxyx
    float stage2_y = r1.x + cb0[9].x;
    float stage2_z = r1.y + cb0[9].y;
    r0.y = stage2_y;
    r0.z = stage2_z;
    // add r1.xy, r1.xyxx, cb0[10].xyxx
    float stage3_x = r1.x + cb0[10].x;
    float stage3_y = r1.y + cb0[10].y;
    r1.x = stage3_x;
    r1.y = stage3_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t6.xyzw, s5, l(-1.000000)
    float4 fetch4 = t6.SampleLevel(s5, r1.xy, -1.0f);
    r1.x = fetch4.x;
    r1.y = fetch4.y;
    r1.z = fetch4.z;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.yzw, r0.yzyy, t5.wxyz, s4, l(-1.000000)
    float4 fetch5 = t5.SampleLevel(s4, r0.yz, -1.0f);
    r0.y = fetch5.x;
    r0.z = fetch5.y;
    r0.w = fetch5.z;
    // mul r2.xyz, r1.xyzx, r0.yzwy
    float stage6_x = r1.x * r0.y;
    float stage6_y = r1.y * r0.z;
    float stage6_z = r1.z * r0.w;
    r2.x = stage6_x;
    r2.y = stage6_y;
    r2.z = stage6_z;
    // add r0.yzw, r1.xxyz, r0.yyzw
    float stage7_y = r1.x + r0.y;
    float stage7_z = r1.y + r0.z;
    float stage7_w = r1.z + r0.w;
    r0.y = stage7_y;
    r0.z = stage7_z;
    r0.w = stage7_w;
    // max r1.xyz, |r2.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage8_x = max(abs(r2.x), 1e-06f);
    float stage8_y = max(abs(r2.y), 1e-06f);
    float stage8_z = max(abs(r2.z), 1e-06f);
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    // log r1.xyz, r1.xyzx
    float stage9_x = log2(r1.x);
    float stage9_y = log2(r1.y);
    float stage9_z = log2(r1.z);
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    // mul r1.xyz, r1.xyzx, cb0[19].yyyy
    float stage10_x = r1.x * cb0[19].y;
    float stage10_y = r1.y * cb0[19].y;
    float stage10_z = r1.z * cb0[19].y;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // exp r1.xyz, r1.xyzx
    float stage11_x = exp2(r1.x);
    float stage11_y = exp2(r1.y);
    float stage11_z = exp2(r1.z);
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    // add r2.xy, v2.xyxx, -cb0[4].xyxx
    float stage12_x = v2.x + -cb0[4].x;
    float stage12_y = v2.y + -cb0[4].y;
    r2.x = stage12_x;
    r2.y = stage12_y;
    // max r1.w, |r2.y|, |r2.x|
    r1.w = max(abs(r2.y), abs(r2.x));
    // div r1.w, l(1.000000, 1.000000, 1.000000, 1.000000), r1.w
    r1.w = 1.0f / r1.w;
    // min r2.z, |r2.y|, |r2.x|
    r2.z = min(abs(r2.y), abs(r2.x));
    // mul r1.w, r1.w, r2.z
    r1.w = r1.w * r2.z;
    // mul r2.z, r1.w, r1.w
    r2.z = r1.w * r1.w;
    // mad r2.w, r2.z, l(0.020835), l(-0.085133)
    r2.w = r2.z * 0.020835f + -0.085133f;
    // mad r2.w, r2.z, r2.w, l(0.180141)
    r2.w = r2.z * r2.w + 0.180141f;
    // mad r2.w, r2.z, r2.w, l(-0.330299)
    r2.w = r2.z * r2.w + -0.330299f;
    // mad r2.z, r2.z, r2.w, l(0.999866)
    r2.z = r2.z * r2.w + 0.999866f;
    // mul r2.w, r1.w, r2.z
    r2.w = r1.w * r2.z;
    // mad r2.w, r2.w, l(-2.000000), l(1.570796)
    r2.w = r2.w * -2.0f + 1.570796f;
    // lt r3.x, |r2.y|, |r2.x|
    r3.x = asfloat((abs(r2.y) < abs(r2.x)) ? 0xffffffffu : 0u);
    // and r2.w, r2.w, r3.x
    r2.w = asfloat(asuint(r2.w) & asuint(r3.x));
    // mad r1.w, r1.w, r2.z, r2.w
    r1.w = r1.w * r2.z + r2.w;
    // lt r2.z, r2.y, -r2.y
    r2.z = asfloat((r2.y < -r2.y) ? 0xffffffffu : 0u);
    // and r2.z, r2.z, l(0xc0490fdb)
    r2.z = asfloat(asuint(r2.z) & asuint(-3.1415927410125732f));
    // add r1.w, r1.w, r2.z
    r1.w = r1.w + r2.z;
    // min r2.z, r2.y, r2.x
    r2.z = min(r2.y, r2.x);
    // max r2.x, r2.y, r2.x
    r2.x = max(r2.y, r2.x);
    // ge r2.x, r2.x, -r2.x
    r2.x = asfloat((r2.x >= -r2.x) ? 0xffffffffu : 0u);
    // lt r2.y, r2.z, -r2.z
    r2.y = asfloat((r2.z < -r2.z) ? 0xffffffffu : 0u);
    // and r2.x, r2.x, r2.y
    r2.x = asfloat(asuint(r2.x) & asuint(r2.y));
    // movc r1.w, r2.x, -r1.w, r1.w
    r1.w = asuint(r2.x) != 0u ? -r1.w : r1.w;
    // mad r2.x, r1.w, l(0.159155), l(0.500000)
    r2.x = r1.w * 0.159155f + 0.5f;
    // mad r3.xy, v2.xyxx, cb0[5].xyxx, cb0[6].xyxx
    float stage13_x = v2.x * cb0[5].x + cb0[6].x;
    float stage13_y = v2.y * cb0[5].y + cb0[6].y;
    r3.x = stage13_x;
    r3.y = stage13_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.w, r3.xyxx, t0.yzwx, s1, l(0.000000)
    float4 fetch14 = t0.SampleBias(s1, r3.xy, 0.0f);
    r1.w = fetch14.x;
    // mad r1.w, r1.w, l(2.000000), l(-1.000000)
    r1.w = r1.w * 2.0f + -1.0f;
    // mad r3.xy, v2.xyxx, cb0[5].xyxx, cb0[7].xyxx
    float stage15_x = v2.x * cb0[5].x + cb0[7].x;
    float stage15_y = v2.y * cb0[5].y + cb0[7].y;
    r3.x = stage15_x;
    r3.y = stage15_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.w, r3.xyxx, t0.yzwx, s1, l(0.000000)
    float4 fetch16 = t0.SampleBias(s1, r3.xy, 0.0f);
    r2.w = fetch16.x;
    // mad r2.w, r2.w, l(2.000000), l(-1.000000)
    r2.w = r2.w * 2.0f + -1.0f;
    // add r3.xy, v4.xyxx, l(-0.500000, -0.900000, 0.000000, 0.000000)
    float stage17_x = v4.x + -0.5f;
    float stage17_y = v4.y + -0.9f;
    r3.x = stage17_x;
    r3.y = stage17_y;
    // mul r2.w, r2.w, r3.y
    r2.w = r2.w * r3.y;
    // mad r1.w, r3.y, r1.w, r2.w
    r1.w = r3.y * r1.w + r2.w;
    // mad r2.w, r0.x, l(3.000000), -r3.x
    r2.w = r0.x * 3.0f + -r3.x;
    // mad r0.x, -r0.x, l(2.000000), l(1.000000)
    r0.x = -r0.x * 2.0f + 1.0f;
    // max r2.w, r2.w, l(-0.100000)
    r2.w = max(r2.w, -0.1f);
    // min r2.y, r2.w, l(10.000000)
    r2.y = min(r2.w, 10.0f);
    // mul r1.w, r1.w, cb0[14].x
    r1.w = r1.w * cb0[14].x;
    // mad r3.xy, r0.xxxx, r1.wwww, r2.xyxx
    float stage18_x = r0.x * r1.w + r2.x;
    float stage18_y = r0.x * r1.w + r2.y;
    r3.x = stage18_x;
    r3.y = stage18_y;
    // max r0.x, r0.x, l(0.000000)
    r0.x = max(r0.x, 0.0f);
    // sample_l_indexable(texture2d)(float,float,float,float) r2.yz, r3.xyxx, t1.yzxw, s2, l(-1.000000)
    float4 fetch19 = t1.SampleLevel(s2, r3.xy, -1.0f);
    r2.y = fetch19.z;
    r2.z = fetch19.x;
    // mul r3.xyz, r0.yzwy, r2.zzzz
    float stage20_x = r0.y * r2.z;
    float stage20_y = r0.z * r2.z;
    float stage20_z = r0.w * r2.z;
    r3.x = stage20_x;
    r3.y = stage20_y;
    r3.z = stage20_z;
    // mul r3.xyz, r3.xyzx, l(3.000000, 3.000000, 3.000000, 0.000000)
    float stage21_x = r3.x * 3.0f;
    float stage21_y = r3.y * 3.0f;
    float stage21_z = r3.z * 3.0f;
    r3.x = stage21_x;
    r3.y = stage21_y;
    r3.z = stage21_z;
    // mad r1.xyz, cb0[19].zzzz, r1.xyzx, r3.xyzx
    float stage22_x = cb0[19].z * r1.x + r3.x;
    float stage22_y = cb0[19].z * r1.y + r3.y;
    float stage22_z = cb0[19].z * r1.z + r3.z;
    r1.x = stage22_x;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // mad r2.xw, r2.xxxz, l(7.000000, 0.000000, 0.000000, 1.000000), cb0[12].xxxy
    float stage23_x = r2.x * 7.0f + cb0[12].x;
    float stage23_w = r2.z * 1.0f + cb0[12].y;
    r2.x = stage23_x;
    r2.w = stage23_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r3.xyz, r2.xwxx, t7.xyzw, s6, l(0.000000)
    float4 fetch24 = t7.SampleBias(s6, r2.xw, 0.0f);
    r3.x = fetch24.x;
    r3.y = fetch24.y;
    r3.z = fetch24.z;
    // mul r3.xyz, r2.zzzz, r3.xyzx
    float stage25_x = r2.z * r3.x;
    float stage25_y = r2.z * r3.y;
    float stage25_z = r2.z * r3.z;
    r3.x = stage25_x;
    r3.y = stage25_y;
    r3.z = stage25_z;
    // mul_sat r3.xyz, r3.xyzx, l(4.000000, 4.000000, 4.000000, 0.000000)
    float stage26_x = saturate(r3.x * 4.0f);
    float stage26_y = saturate(r3.y * 4.0f);
    float stage26_z = saturate(r3.z * 4.0f);
    r3.x = stage26_x;
    r3.y = stage26_y;
    r3.z = stage26_z;
    // mul r3.xyz, r3.xyzx, r3.xyzx
    float stage27_x = r3.x * r3.x;
    float stage27_y = r3.y * r3.y;
    float stage27_z = r3.z * r3.z;
    r3.x = stage27_x;
    r3.y = stage27_y;
    r3.z = stage27_z;
    // mad r1.xyz, r1.xyzx, cb0[11].xyzx, r3.xyzx
    float stage28_x = r1.x * cb0[11].x + r3.x;
    float stage28_y = r1.y * cb0[11].y + r3.y;
    float stage28_z = r1.z * cb0[11].z + r3.z;
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // dp3 r0.w, v6.xyzx, v6.xyzx
    r0.w = dot(v6.xyz, v6.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r2.xw, r0.wwww, v6.xxxy
    float stage29_x = r0.w * v6.x;
    float stage29_w = r0.w * v6.y;
    r2.x = stage29_x;
    r2.w = stage29_w;
    // mad r0.yz, r0.yyzy, l(0.000000, 0.200000, 0.200000, 0.000000), r2.xxwx
    float stage30_y = r0.y * 0.2f + r2.x;
    float stage30_z = r0.z * 0.2f + r2.w;
    r0.y = stage30_y;
    r0.z = stage30_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.yzw, r0.yzyy, t8.wxyz, s7, l(0.000000)
    float4 fetch31 = t8.SampleBias(s7, r0.yz, 0.0f);
    r0.y = fetch31.x;
    r0.z = fetch31.y;
    r0.w = fetch31.z;
    // add r0.yzw, r0.yyzw, r1.xxyz
    float stage32_y = r0.y + r1.x;
    float stage32_z = r0.z + r1.y;
    float stage32_w = r0.w + r1.z;
    r0.y = stage32_y;
    r0.z = stage32_z;
    r0.w = stage32_w;
    // dp3 r1.x, r0.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.yzw, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.yzwy, r1.xxxx
    float stage33_x = -r0.y + r1.x;
    float stage33_y = -r0.z + r1.x;
    float stage33_z = -r0.w + r1.x;
    r1.x = stage33_x;
    r1.y = stage33_y;
    r1.z = stage33_z;
    // mad r0.yzw, cb0[20].yyyy, r1.xxyz, r0.yyzw
    float stage34_y = cb0[20].y * r1.x + r0.y;
    float stage34_z = cb0[20].y * r1.y + r0.z;
    float stage34_w = cb0[20].y * r1.z + r0.w;
    r0.y = stage34_y;
    r0.z = stage34_z;
    r0.w = stage34_w;
    // div r1.xy, v7.xyxx, v7.wwww
    float stage35_x = v7.x / v7.w;
    float stage35_y = v7.y / v7.w;
    r1.x = stage35_x;
    r1.y = stage35_y;
    // mad r1.xy, r1.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage36_x = r1.x * cb2[0].x + cb2[0].w;
    float stage36_y = r1.y * cb2[0].y + cb2[0].z;
    r1.x = stage36_x;
    r1.y = stage36_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t2.xyzw, s0, l(0.000000)
    float4 fetch37 = t2.SampleLevel(s0, r1.xy, 0.0f);
    r1.x = fetch37.x;
    // min r1.x, r1.x, l(0.999000)
    r1.x = min(r1.x, 0.999f);
    // mad r1.y, r1.x, cb2[1].z, -cb2[1].w
    r1.y = r1.x * cb2[1].z + -cb2[1].w;
    // mad r1.x, r1.x, cb2[1].x, cb2[1].y
    r1.x = r1.x * cb2[1].x + cb2[1].y;
    // div r1.y, l(1.000000, 1.000000, 1.000000, 1.000000), r1.y
    r1.y = 1.0f / r1.y;
    // add r1.x, r1.y, r1.x
    r1.x = r1.y + r1.x;
    // add r1.x, r1.x, -v7.w
    r1.x = r1.x + -v7.w;
    // mul_sat r1.xy, r1.xxxx, l(0.034483, 0.066667, 0.000000, 0.000000)
    float stage38_x = saturate(r1.x * 0.034483f);
    float stage38_y = saturate(r1.x * 0.066667f);
    r1.x = stage38_x;
    r1.y = stage38_y;
    // add r1.y, -r1.y, l(1.000000)
    r1.y = -r1.y + 1.0f;
    // mul r1.z, r1.y, r1.y
    r1.z = r1.y * r1.y;
    // lt r1.y, r1.y, l(0.000001)
    r1.y = asfloat((r1.y < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.z, r1.z, r1.z
    r1.z = r1.z * r1.z;
    // mul r1.z, r1.z, r1.z
    r1.z = r1.z * r1.z;
    // mul r3.xyz, r1.zzzz, l(10.000000, 10.000000, 20.000000, 0.000000)
    float stage39_x = r1.z * 10.0f;
    float stage39_y = r1.z * 10.0f;
    float stage39_z = r1.z * 20.0f;
    r3.x = stage39_x;
    r3.y = stage39_y;
    r3.z = stage39_z;
    // movc r1.yzw, r1.yyyy, l(0,0,0,0), r3.xxyz
    float stage40_y = asuint(r1.y) != 0u ? asfloat(0u) : r3.x;
    float stage40_z = asuint(r1.y) != 0u ? asfloat(0u) : r3.y;
    float stage40_w = asuint(r1.y) != 0u ? asfloat(0u) : r3.z;
    r1.y = stage40_y;
    r1.z = stage40_z;
    r1.w = stage40_w;
    // mad r0.yzw, r2.yyyy, r0.yyzw, r1.yyzw
    float stage41_y = r2.y * r0.y + r1.y;
    float stage41_z = r2.y * r0.z + r1.z;
    float stage41_w = r2.y * r0.w + r1.w;
    r0.y = stage41_y;
    r0.z = stage41_z;
    r0.w = stage41_w;
    // mul r0.yzw, r0.yyzw, v3.xxyz
    float stage42_y = r0.y * v3.x;
    float stage42_z = r0.z * v3.y;
    float stage42_w = r0.w * v3.z;
    r0.y = stage42_y;
    r0.z = stage42_z;
    r0.w = stage42_w;
    // log r1.y, r0.x
    r1.y = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.y, r1.y, cb0[13].x
    r1.y = r1.y * cb0[13].x;
    // exp r1.y, r1.y
    r1.y = exp2(r1.y);
    // mul r1.y, r1.y, cb0[13].z
    r1.y = r1.y * cb0[13].z;
    // add r1.zw, -v2.xxxy, l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage43_z = -v2.x + 0.5f;
    float stage43_w = -v2.y + 0.5f;
    r1.z = stage43_z;
    r1.w = stage43_w;
    // mul r1.yz, r1.zzwz, r1.yyyy
    float stage44_y = r1.z * r1.y;
    float stage44_z = r1.w * r1.y;
    r1.y = stage44_y;
    r1.z = stage44_z;
    // movc r1.yz, r0.xxxx, l(0,0,0,0), r1.yyzy
    float stage45_y = asuint(r0.x) != 0u ? asfloat(0u) : r1.y;
    float stage45_z = asuint(r0.x) != 0u ? asfloat(0u) : r1.z;
    r1.y = stage45_y;
    r1.z = stage45_z;
    // add r1.yz, r1.yyzy, v2.xxyx
    float stage46_y = r1.y + v2.x;
    float stage46_z = r1.z + v2.y;
    r1.y = stage46_y;
    r1.z = stage46_z;
    // mad r1.yz, cb0[13].wwww, r1.yyzy, cb0[2].xxyx
    float stage47_y = cb0[13].w * r1.y + cb0[2].x;
    float stage47_z = cb0[13].w * r1.z + cb0[2].y;
    r1.y = stage47_y;
    r1.z = stage47_z;
    // add r1.yz, r1.yyzy, cb0[3].xxyx
    float stage48_y = r1.y + cb0[3].x;
    float stage48_z = r1.z + cb0[3].y;
    r1.y = stage48_y;
    r1.z = stage48_z;
    // add r0.x, -r2.y, l(1.000000)
    r0.x = -r2.y + 1.0f;
    // add_sat r1.w, r0.x, r2.z
    r1.w = saturate(r0.x + r2.z);
    // mul r1.x, r1.x, r1.w
    r1.x = r1.x * r1.w;
    // mul r1.x, r1.x, v3.w
    r1.x = r1.x * v3.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.yz, v2.xyxx, t0.zxyw, s1, l(0.000000)
    float4 fetch49 = t0.SampleBias(s1, v2.xy, 0.0f);
    r2.y = fetch49.x;
    r2.z = fetch49.y;
    // mad r2.yz, r2.yyzy, l(0.000000, 2.000000, 2.000000, 0.000000), l(0.000000, -1.000000, -1.000000, 0.000000)
    float stage50_y = r2.y * 2.0f + -1.0f;
    float stage50_z = r2.z * 2.0f + -1.0f;
    r2.y = stage50_y;
    r2.z = stage50_z;
    // mul r2.yz, r0.xxxx, r2.yyzy
    float stage51_y = r0.x * r2.y;
    float stage51_z = r0.x * r2.z;
    r2.y = stage51_y;
    r2.z = stage51_z;
    // mad r1.yz, cb0[14].yyyy, r2.yyzy, r1.yyzy
    float stage52_y = cb0[14].y * r2.y + r1.y;
    float stage52_z = cb0[14].y * r2.z + r1.z;
    r1.y = stage52_y;
    r1.z = stage52_z;
    // mad r1.w, cb0[14].z, l(0.500000), l(-0.250000)
    r1.w = cb0[14].z * 0.5f + -0.25f;
    // mad r1.yz, r1.wwww, r2.xxwx, r1.yyzy
    float stage53_y = r1.w * r2.x + r1.y;
    float stage53_z = r1.w * r2.w + r1.z;
    r1.y = stage53_y;
    r1.z = stage53_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.yzw, r1.yzyy, t4.wxyz, s3, l(0.000000)
    float4 fetch54 = t4.SampleBias(s3, r1.yz, 0.0f);
    r1.y = fetch54.x;
    r1.z = fetch54.y;
    r1.w = fetch54.z;
    // max r1.yzw, |r1.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage55_y = max(abs(r1.y), 1e-06f);
    float stage55_z = max(abs(r1.z), 1e-06f);
    float stage55_w = max(abs(r1.w), 1e-06f);
    r1.y = stage55_y;
    r1.z = stage55_z;
    r1.w = stage55_w;
    // log r1.yzw, r1.yyzw
    float stage56_y = log2(r1.y);
    float stage56_z = log2(r1.z);
    float stage56_w = log2(r1.w);
    r1.y = stage56_y;
    r1.z = stage56_z;
    r1.w = stage56_w;
    // mul r1.yzw, r1.yyzw, cb0[14].wwww
    float stage57_y = r1.y * cb0[14].w;
    float stage57_z = r1.z * cb0[14].w;
    float stage57_w = r1.w * cb0[14].w;
    r1.y = stage57_y;
    r1.z = stage57_z;
    r1.w = stage57_w;
    // exp r1.yzw, r1.yyzw
    float stage58_y = exp2(r1.y);
    float stage58_z = exp2(r1.z);
    float stage58_w = exp2(r1.w);
    r1.y = stage58_y;
    r1.z = stage58_z;
    r1.w = stage58_w;
    // mul r2.xyz, r1.yzwy, cb0[15].xxxx
    float stage59_x = r1.y * cb0[15].x;
    float stage59_y = r1.z * cb0[15].x;
    float stage59_z = r1.w * cb0[15].x;
    r2.x = stage59_x;
    r2.y = stage59_y;
    r2.z = stage59_z;
    // dp3 r2.w, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r2.w = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.yzw, -cb0[15].xxxx, r1.yyzw, r2.wwww
    float stage60_y = -cb0[15].x * r1.y + r2.w;
    float stage60_z = -cb0[15].x * r1.z + r2.w;
    float stage60_w = -cb0[15].x * r1.w + r2.w;
    r1.y = stage60_y;
    r1.z = stage60_z;
    r1.w = stage60_w;
    // mad r1.yzw, cb0[15].yyyy, r1.yyzw, r2.xxyz
    float stage61_y = cb0[15].y * r1.y + r2.x;
    float stage61_z = cb0[15].y * r1.z + r2.y;
    float stage61_w = cb0[15].y * r1.w + r2.z;
    r1.y = stage61_y;
    r1.z = stage61_z;
    r1.w = stage61_w;
    // mul r1.yzw, r1.yyzw, cb0[8].xxyz
    float stage62_y = r1.y * cb0[8].x;
    float stage62_z = r1.z * cb0[8].y;
    float stage62_w = r1.w * cb0[8].z;
    r1.y = stage62_y;
    r1.z = stage62_z;
    r1.w = stage62_w;
    // mad r0.xyz, r0.xxxx, r1.yzwy, r0.yzwy
    float stage63_x = r0.x * r1.y + r0.y;
    float stage63_y = r0.x * r1.z + r0.z;
    float stage63_z = r0.x * r1.w + r0.w;
    r0.x = stage63_x;
    r0.y = stage63_y;
    r0.z = stage63_z;
    // add r0.xyz, r0.xyzx, cb0[1].xyzx
    float stage64_x = r0.x + cb0[1].x;
    float stage64_y = r0.y + cb0[1].y;
    float stage64_z = r0.z + cb0[1].z;
    r0.x = stage64_x;
    r0.y = stage64_y;
    r0.z = stage64_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage65_x = r0.x * v5.w + v5.x;
    float stage65_y = r0.y * v5.w + v5.y;
    float stage65_z = r0.z * v5.w + v5.z;
    o0.x = stage65_x;
    o0.y = stage65_y;
    o0.z = stage65_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, v2.xyxx, t3.xyzw, s8, l(0.000000)
    float4 fetch66 = t3.SampleBias(s8, v2.xy, 0.0f);
    r0.x = fetch66.x;
    // mul_sat r0.x, r1.x, r0.x
    r0.x = saturate(r1.x * r0.x);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage67_x = r0.x * v1.x;
    float stage67_y = r0.x * v1.y;
    float stage67_z = r0.x * v1.z;
    r0.x = stage67_x;
    r0.y = stage67_y;
    r0.z = stage67_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage68_x = r0.w * v0.z;
    float stage68_y = r0.w * v0.x;
    float stage68_z = r0.w * v0.y;
    r1.x = stage68_x;
    r1.y = stage68_y;
    r1.z = stage68_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage69_x = r0.x * r1.x;
    float stage69_y = r0.x * r1.y;
    float stage69_z = r0.x * r1.z;
    r0.x = stage69_x;
    r0.y = stage69_y;
    r0.z = stage69_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage70_x = r0.x / r0.w;
    float stage70_y = r0.y / r0.w;
    r0.x = stage70_x;
    r0.y = stage70_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage71_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage71_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage71_x;
    r1.y = stage71_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage72_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage72_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage72_x;
    r1.y = stage72_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage73_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage73_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage73_x;
    r1.y = stage73_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage74_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage74_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage74_x;
    r0.y = stage74_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage75_x = r0.x * 0.5f + 0.5f;
    float stage75_y = r0.y * 0.5f + 0.5f;
    o2.x = stage75_x;
    o2.y = stage75_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage76_z = 1.0f;
    float stage76_w = asfloat(0u);
    o2.z = stage76_z;
    o2.w = stage76_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage77_x = cb2[3].x;
    float stage77_y = cb2[3].y;
    float stage77_z = cb2[3].z;
    o3.x = stage77_x;
    o3.y = stage77_y;
    o3.z = stage77_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage78_x = asfloat(0u);
    float stage78_y = asfloat(0u);
    float stage78_z = asfloat(0u);
    float stage78_w = asfloat(0u);
    o4.x = stage78_x;
    o4.y = stage78_y;
    o4.z = stage78_z;
    o4.w = stage78_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage79_x = asfloat(0u);
    float stage79_y = asfloat(0u);
    float stage79_z = asfloat(0u);
    float stage79_w = asfloat(0u);
    o5.x = stage79_x;
    o5.y = stage79_y;
    o5.z = stage79_z;
    o5.w = stage79_w;

    Shade_Ue3_fx_s_pa_glasshole_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
