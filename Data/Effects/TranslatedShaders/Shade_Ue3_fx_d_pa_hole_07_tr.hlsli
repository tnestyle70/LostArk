// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_hole_07_tr_Constants0 : register(b0)
{
    float4 cb0[14];
};
cbuffer Shade_Ue3_fx_d_pa_hole_07_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_d_pa_hole_07_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    float4 v6;
};

struct Shade_Ue3_fx_d_pa_hole_07_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_d_pa_hole_07_tr_OUTPUT Shade_Ue3_fx_d_pa_hole_07_tr(Shade_Ue3_fx_d_pa_hole_07_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.xy, v2.xyxx, cb0[7].yzyy
    float stage1_x = v2.x * cb0[7].y;
    float stage1_y = v2.y * cb0[7].z;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, cb0[6].yyyy, cb0[7].xwxx, r0.xyxx
    float stage2_x = cb0[6].y * cb0[7].x + r0.x;
    float stage2_y = cb0[6].y * cb0[7].w + r0.y;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t2.xyzw, s1, l(0.000000)
    float4 fetch3 = t2.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch3.x;
    r0.y = fetch3.y;
    r0.z = fetch3.z;
    // add r0.x, r0.y, r0.x
    r0.x = r0.y + r0.x;
    // add r0.x, r0.z, r0.x
    r0.x = r0.z + r0.x;
    // mul r0.x, r0.x, cb0[8].x
    r0.x = r0.x * cb0[8].x;
    // mad r0.x, r0.x, l(0.330000), cb0[8].y
    r0.x = r0.x * 0.33f + cb0[8].y;
    // mad r0.x, r0.x, l(0.050000), l(-0.025000)
    r0.x = r0.x * 0.05f + -0.025f;
    // dp3 r0.y, v6.xyzx, v6.xyzx
    r0.y = dot(v6.xyz, v6.xyz);
    // rsq r0.y, r0.y
    r0.y = rsqrt(r0.y);
    // mul r0.yz, r0.yyyy, v6.xxyx
    float stage4_y = r0.y * v6.x;
    float stage4_z = r0.y * v6.y;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // mad r0.xy, r0.xxxx, r0.yzyy, v2.xyxx
    float stage5_x = r0.x * r0.y + v2.x;
    float stage5_y = r0.x * r0.z + v2.y;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // mul r0.zw, r0.xxxy, cb0[6].zzzw
    float stage6_z = r0.x * cb0[6].z;
    float stage6_w = r0.y * cb0[6].w;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // mad r1.x, cb0[6].y, cb0[6].x, r0.z
    r1.x = cb0[6].y * cb0[6].x + r0.z;
    // mad r1.y, cb0[6].y, cb0[8].z, r0.w
    r1.y = cb0[6].y * cb0[8].z + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.xyxx, t3.yzxw, s2, l(0.000000)
    float4 fetch7 = t3.SampleBias(s2, r1.xy, 0.0f);
    r0.z = fetch7.x;
    // mul r1.xy, r0.xyxx, cb0[9].yzyy
    float stage8_x = r0.x * cb0[9].y;
    float stage8_y = r0.y * cb0[9].z;
    r1.x = stage8_x;
    r1.y = stage8_y;
    // mov_sat r0.xy, r0.xyxx
    float stage9_x = saturate(r0.x);
    float stage9_y = saturate(r0.y);
    r0.x = stage9_x;
    r0.y = stage9_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t0.xyzw, s3, l(0.000000)
    float4 fetch10 = t0.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch10.x;
    // mul_sat r0.x, r0.x, cb0[10].z
    r0.x = saturate(r0.x * cb0[10].z);
    // mad r0.yw, cb0[6].yyyy, cb0[9].xxxw, r1.xxxy
    float stage11_y = cb0[6].y * cb0[9].x + r1.x;
    float stage11_w = cb0[6].y * cb0[9].w + r1.y;
    r0.y = stage11_y;
    r0.w = stage11_w;
    // mul r1.x, v4.w, cb0[8].w
    r1.x = v4.w * cb0[8].w;
    // mad r0.yz, r0.zzzz, r1.xxxx, r0.yywy
    float stage12_y = r0.z * r1.x + r0.y;
    float stage12_z = r0.z * r1.x + r0.w;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mul r1.xy, r0.yzyy, l(2.300000, 2.300000, 0.000000, 0.000000)
    float stage13_x = r0.y * 2.3f;
    float stage13_y = r0.z * 2.3f;
    r1.x = stage13_x;
    r1.y = stage13_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.yzw, r0.yzyy, t4.wxyz, s0, l(-1.000000)
    float4 fetch14 = t4.SampleLevel(s0, r0.yz, -1.0f);
    r0.y = fetch14.x;
    r0.z = fetch14.y;
    r0.w = fetch14.z;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t4.xyzw, s0, l(-1.000000)
    float4 fetch15 = t4.SampleLevel(s0, r1.xy, -1.0f);
    r1.x = fetch15.x;
    r1.y = fetch15.y;
    r1.z = fetch15.z;
    // mul r1.xyz, r1.xyzx, l(0.500000, 0.500000, 0.500000, 0.000000)
    float stage16_x = r1.x * 0.5f;
    float stage16_y = r1.y * 0.5f;
    float stage16_z = r1.z * 0.5f;
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mad r0.yzw, r0.yyzw, l(0.000000, 0.500000, 0.500000, 0.500000), r1.xxyz
    float stage17_y = r0.y * 0.5f + r1.x;
    float stage17_z = r0.z * 0.5f + r1.y;
    float stage17_w = r0.w * 0.5f + r1.z;
    r0.y = stage17_y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // dp3 r1.x, r0.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.yzw, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.yzwy, r1.xxxx
    float stage18_x = -r0.y + r1.x;
    float stage18_y = -r0.z + r1.x;
    float stage18_z = -r0.w + r1.x;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    // mad r0.yzw, cb0[10].xxxx, r1.xxyz, r0.yyzw
    float stage19_y = cb0[10].x * r1.x + r0.y;
    float stage19_z = cb0[10].x * r1.y + r0.z;
    float stage19_w = cb0[10].x * r1.z + r0.w;
    r0.y = stage19_y;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // max r0.yzw, |r0.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage20_y = max(abs(r0.y), 1e-06f);
    float stage20_z = max(abs(r0.z), 1e-06f);
    float stage20_w = max(abs(r0.w), 1e-06f);
    r0.y = stage20_y;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // log r0.yzw, r0.yyzw
    float stage21_y = log2(r0.y);
    float stage21_z = log2(r0.z);
    float stage21_w = log2(r0.w);
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mul r0.yzw, r0.yyzw, cb0[10].yyyy
    float stage22_y = r0.y * cb0[10].y;
    float stage22_z = r0.z * cb0[10].y;
    float stage22_w = r0.w * cb0[10].y;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // exp r0.yzw, r0.yyzw
    float stage23_y = exp2(r0.y);
    float stage23_z = exp2(r0.z);
    float stage23_w = exp2(r0.w);
    r0.y = stage23_y;
    r0.z = stage23_z;
    r0.w = stage23_w;
    // mul r1.xyz, cb0[2].xyzx, cb0[2].wwww
    float stage24_x = cb0[2].x * cb0[2].w;
    float stage24_y = cb0[2].y * cb0[2].w;
    float stage24_z = cb0[2].z * cb0[2].w;
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // mul r2.xyz, r0.yzwy, r1.xyzx
    float stage25_x = r0.y * r1.x;
    float stage25_y = r0.z * r1.y;
    float stage25_z = r0.w * r1.z;
    r2.x = stage25_x;
    r2.y = stage25_y;
    r2.z = stage25_z;
    // mul r3.xyz, cb0[3].xyzx, cb0[3].wwww
    float stage26_x = cb0[3].x * cb0[3].w;
    float stage26_y = cb0[3].y * cb0[3].w;
    float stage26_z = cb0[3].z * cb0[3].w;
    r3.x = stage26_x;
    r3.y = stage26_y;
    r3.z = stage26_z;
    // mul_sat r2.xyz, r2.xyzx, r3.xyzx
    float stage27_x = saturate(r2.x * r3.x);
    float stage27_y = saturate(r2.y * r3.y);
    float stage27_z = saturate(r2.z * r3.z);
    r2.x = stage27_x;
    r2.y = stage27_y;
    r2.z = stage27_z;
    // mad r0.yzw, r0.yyzw, r1.xxyz, -r2.xxyz
    float stage28_y = r0.y * r1.x + -r2.x;
    float stage28_z = r0.z * r1.y + -r2.y;
    float stage28_w = r0.w * r1.z + -r2.z;
    r0.y = stage28_y;
    r0.z = stage28_z;
    r0.w = stage28_w;
    // log r1.x, r0.x
    r1.x = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, cb0[10].w
    r1.x = r1.x * cb0[10].w;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // movc r0.x, r0.x, l(0), r1.x
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r1.x;
    // mad r0.xyz, r0.xxxx, r0.yzwy, r2.xyzx
    float stage29_x = r0.x * r0.y + r2.x;
    float stage29_y = r0.x * r0.z + r2.y;
    float stage29_z = r0.x * r0.w + r2.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mul r1.xy, v2.xyxx, cb0[4].xyxx
    float stage30_x = v2.x * cb0[4].x;
    float stage30_y = v2.y * cb0[4].y;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // mul r1.zw, r1.xxxy, l(0.000000, 0.000000, 1.660000, 1.660000)
    float stage31_z = r1.x * 1.66f;
    float stage31_w = r1.y * 1.66f;
    r1.z = stage31_z;
    r1.w = stage31_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r1.xyxx, t5.xyzw, s4, l(0.000000)
    float4 fetch32 = t5.SampleBias(s4, r1.xy, 0.0f);
    r2.x = fetch32.x;
    r2.y = fetch32.y;
    r2.z = fetch32.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.zwzz, t5.xyzw, s4, l(0.000000)
    float4 fetch33 = t5.SampleBias(s4, r1.zw, 0.0f);
    r1.x = fetch33.x;
    r1.y = fetch33.y;
    r1.z = fetch33.z;
    // add r1.xyz, r1.xyzx, r2.xyzx
    float stage34_x = r1.x + r2.x;
    float stage34_y = r1.y + r2.y;
    float stage34_z = r1.z + r2.z;
    r1.x = stage34_x;
    r1.y = stage34_y;
    r1.z = stage34_z;
    // mul r2.xyz, r1.xyzx, l(0.500000, 0.500000, 0.500000, 0.000000)
    float stage35_x = r1.x * 0.5f;
    float stage35_y = r1.y * 0.5f;
    float stage35_z = r1.z * 0.5f;
    r2.x = stage35_x;
    r2.y = stage35_y;
    r2.z = stage35_z;
    // dp3 r0.w, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r1.xyzx, l(0.500000, 0.500000, 0.500000, 0.000000), r0.wwww
    float stage36_x = -r1.x * 0.5f + r0.w;
    float stage36_y = -r1.y * 0.5f + r0.w;
    float stage36_z = -r1.z * 0.5f + r0.w;
    r1.x = stage36_x;
    r1.y = stage36_y;
    r1.z = stage36_z;
    // mad r1.xyz, cb0[11].zzzz, r1.xyzx, r2.xyzx
    float stage37_x = cb0[11].z * r1.x + r2.x;
    float stage37_y = cb0[11].z * r1.y + r2.y;
    float stage37_z = cb0[11].z * r1.z + r2.z;
    r1.x = stage37_x;
    r1.y = stage37_y;
    r1.z = stage37_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage38_x = max(abs(r1.x), 1e-06f);
    float stage38_y = max(abs(r1.y), 1e-06f);
    float stage38_z = max(abs(r1.z), 1e-06f);
    r1.x = stage38_x;
    r1.y = stage38_y;
    r1.z = stage38_z;
    // log r1.xyz, r1.xyzx
    float stage39_x = log2(r1.x);
    float stage39_y = log2(r1.y);
    float stage39_z = log2(r1.z);
    r1.x = stage39_x;
    r1.y = stage39_y;
    r1.z = stage39_z;
    // mul r1.xyz, r1.xyzx, cb0[11].wwww
    float stage40_x = r1.x * cb0[11].w;
    float stage40_y = r1.y * cb0[11].w;
    float stage40_z = r1.z * cb0[11].w;
    r1.x = stage40_x;
    r1.y = stage40_y;
    r1.z = stage40_z;
    // exp r1.xyz, r1.xyzx
    float stage41_x = exp2(r1.x);
    float stage41_y = exp2(r1.y);
    float stage41_z = exp2(r1.z);
    r1.x = stage41_x;
    r1.y = stage41_y;
    r1.z = stage41_z;
    // mul r2.xyz, cb0[5].xyzx, cb0[5].wwww
    float stage42_x = cb0[5].x * cb0[5].w;
    float stage42_y = cb0[5].y * cb0[5].w;
    float stage42_z = cb0[5].z * cb0[5].w;
    r2.x = stage42_x;
    r2.y = stage42_y;
    r2.z = stage42_z;
    // mad r1.xyz, r1.xyzx, r2.xyzx, -r0.xyzx
    float stage43_x = r1.x * r2.x + -r0.x;
    float stage43_y = r1.y * r2.y + -r0.y;
    float stage43_z = r1.z * r2.z + -r0.z;
    r1.x = stage43_x;
    r1.y = stage43_y;
    r1.z = stage43_z;
    // add r2.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage44_x = v2.x + -0.5f;
    float stage44_y = v2.y + -0.5f;
    r2.x = stage44_x;
    r2.y = stage44_y;
    // dp2 r0.w, r2.xyxx, r2.xyxx
    r0.w = dot(r2.xy, r2.xy);
    // sqrt r0.w, r0.w
    r0.w = sqrt(r0.w);
    // mad r1.w, r0.w, l(2.000000), -v4.x
    r1.w = r0.w * 2.0f + -v4.x;
    // mad r0.w, -r0.w, l(2.000000), v4.y
    r0.w = -r0.w * 2.0f + v4.y;
    // add_sat r0.w, r0.w, l(1.000000)
    r0.w = saturate(r0.w + 1.0f);
    // add_sat r1.w, r1.w, l(1.000000)
    r1.w = saturate(r1.w + 1.0f);
    // mul r2.xy, v2.xyxx, cb0[12].xxxx
    float stage45_x = v2.x * cb0[12].x;
    float stage45_y = v2.y * cb0[12].x;
    r2.x = stage45_x;
    r2.y = stage45_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.x, r2.xyxx, t1.xyzw, s5, l(0.000000)
    float4 fetch46 = t1.SampleBias(s5, r2.xy, 0.0f);
    r2.x = fetch46.x;
    // add r1.w, r1.w, r2.x
    r1.w = r1.w + r2.x;
    // add r0.w, r0.w, r2.x
    r0.w = r0.w + r2.x;
    // log r2.x, |r1.w|
    r2.x = log2(abs(r1.w));
    // lt r1.w, |r1.w|, l(0.000001)
    r1.w = asfloat((abs(r1.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r2.x, r2.x, cb0[12].y
    r2.x = r2.x * cb0[12].y;
    // exp r2.x, r2.x
    r2.x = exp2(r2.x);
    // mul_sat r2.x, r2.x, cb0[12].z
    r2.x = saturate(r2.x * cb0[12].z);
    // movc r1.w, r1.w, l(0), r2.x
    r1.w = asuint(r1.w) != 0u ? asfloat(0u) : r2.x;
    // mad r0.xyz, r1.wwww, r1.xyzx, r0.xyzx
    float stage47_x = r1.w * r1.x + r0.x;
    float stage47_y = r1.w * r1.y + r0.y;
    float stage47_z = r1.w * r1.z + r0.z;
    r0.x = stage47_x;
    r0.y = stage47_y;
    r0.z = stage47_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage48_x = r0.x * v3.x + cb0[1].x;
    float stage48_y = r0.y * v3.y + cb0[1].y;
    float stage48_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage48_x;
    r0.y = stage48_y;
    r0.z = stage48_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage49_x = r0.x * v5.w + v5.x;
    float stage49_y = r0.y * v5.w + v5.y;
    float stage49_z = r0.z * v5.w + v5.z;
    o0.x = stage49_x;
    o0.y = stage49_y;
    o0.z = stage49_z;
    // log r0.x, |r0.w|
    r0.x = log2(abs(r0.w));
    // lt r0.y, |r0.w|, l(0.000001)
    r0.y = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.x, r0.x, cb0[12].y
    r0.x = r0.x * cb0[12].y;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul_sat r0.x, r0.x, cb0[12].z
    r0.x = saturate(r0.x * cb0[12].z);
    // movc r0.x, r0.y, l(0), r0.x
    r0.x = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, v2.xyxx, t0.yxzw, s3, l(0.000000)
    float4 fetch50 = t0.SampleBias(s3, v2.xy, 0.0f);
    r0.y = fetch50.x;
    // mul_sat r0.y, r0.y, cb0[12].w
    r0.y = saturate(r0.y * cb0[12].w);
    // log r0.z, r0.y
    r0.z = log2(r0.y);
    // lt r0.y, r0.y, l(0.000001)
    r0.y = asfloat((r0.y < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[13].x
    r0.z = r0.z * cb0[13].x;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // mul r0.x, r0.x, r0.z
    r0.x = r0.x * r0.z;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // movc o0.w, r0.y, l(0), r0.x
    o0.w = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage51_x = r0.x * v1.x;
    float stage51_y = r0.x * v1.y;
    float stage51_z = r0.x * v1.z;
    r0.x = stage51_x;
    r0.y = stage51_y;
    r0.z = stage51_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage52_x = r0.w * v0.z;
    float stage52_y = r0.w * v0.x;
    float stage52_z = r0.w * v0.y;
    r1.x = stage52_x;
    r1.y = stage52_y;
    r1.z = stage52_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage53_x = r0.x * r1.x;
    float stage53_y = r0.x * r1.y;
    float stage53_z = r0.x * r1.z;
    r0.x = stage53_x;
    r0.y = stage53_y;
    r0.z = stage53_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage54_x = r0.x / r0.w;
    float stage54_y = r0.y / r0.w;
    r0.x = stage54_x;
    r0.y = stage54_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage55_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage55_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage55_x;
    r1.y = stage55_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage56_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage56_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage56_x;
    r1.y = stage56_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage57_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage57_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage57_x;
    r1.y = stage57_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage58_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage58_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage58_x;
    r0.y = stage58_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage59_x = r0.x * 0.5f + 0.5f;
    float stage59_y = r0.y * 0.5f + 0.5f;
    o2.x = stage59_x;
    o2.y = stage59_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage60_z = 1.0f;
    float stage60_w = asfloat(0u);
    o2.z = stage60_z;
    o2.w = stage60_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage61_x = cb2[3].x;
    float stage61_y = cb2[3].y;
    float stage61_z = cb2[3].z;
    o3.x = stage61_x;
    o3.y = stage61_y;
    o3.z = stage61_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage62_x = asfloat(0u);
    float stage62_y = asfloat(0u);
    float stage62_z = asfloat(0u);
    float stage62_w = asfloat(0u);
    o4.x = stage62_x;
    o4.y = stage62_y;
    o4.z = stage62_z;
    o4.w = stage62_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage63_x = asfloat(0u);
    float stage63_y = asfloat(0u);
    float stage63_z = asfloat(0u);
    float stage63_w = asfloat(0u);
    o5.x = stage63_x;
    o5.y = stage63_y;
    o5.z = stage63_z;
    o5.w = stage63_w;

    Shade_Ue3_fx_d_pa_hole_07_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
