// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_pa_flowmask_01_tr_Constants0 : register(b0)
{
    float4 cb0[16];
};
cbuffer Shade_Ue3_fx_k_pa_flowmask_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_k_pa_flowmask_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_pa_flowmask_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_pa_flowmask_01_tr_OUTPUT Shade_Ue3_fx_k_pa_flowmask_01_tr(Shade_Ue3_fx_k_pa_flowmask_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mov r0.y, cb0[12].z
    r0.y = cb0[12].z;
    // mad r1.xy, v2.xyxx, cb0[4].xyxx, cb0[5].xyxx
    float stage1_x = v2.x * cb0[4].x + cb0[5].x;
    float stage1_y = v2.y * cb0[4].y + cb0[5].y;
    r1.x = stage1_x;
    r1.y = stage1_y;
    // mov r0.xz, l(0,0,0,0)
    float stage2_x = asfloat(0u);
    float stage2_z = asfloat(0u);
    r0.x = stage2_x;
    r0.z = stage2_z;
    // add r2.xyzw, r0.yxxy, r1.xyxy
    float stage3_x = r0.y + r1.x;
    float stage3_y = r0.x + r1.y;
    float stage3_z = r0.x + r1.x;
    float stage3_w = r0.y + r1.y;
    r2.x = stage3_x;
    r2.y = stage3_y;
    r2.z = stage3_z;
    r2.w = stage3_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r1.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch4 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.x = fetch4.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r2.xyxx, t0.yxzw, s0, l(0.000000)
    float4 fetch5 = t0.SampleBias(s0, r2.xy, 0.0f);
    r0.y = fetch5.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r2.zwzz, t0.xyzw, s0, l(0.000000)
    float4 fetch6 = t0.SampleBias(s0, r2.zw, 0.0f);
    r1.x = fetch6.x;
    // add r1.y, -r0.x, r1.x
    r1.y = -r0.x + r1.x;
    // add r1.x, -r0.x, r0.y
    r1.x = -r0.x + r0.y;
    // mul r1.xy, r1.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage7_x = r1.x * 8.0f;
    float stage7_y = r1.y * 8.0f;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // mov r1.z, l(0)
    r1.z = asfloat(0u);
    // add r1.xyz, -r1.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage8_x = -r1.x + 0.0f;
    float stage8_y = -r1.y + 0.0f;
    float stage8_z = -r1.z + 1.0f;
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // div r1.xyzw, r1.xyxy, r0.xxxx
    float stage9_x = r1.x / r0.x;
    float stage9_y = r1.y / r0.x;
    float stage9_z = r1.x / r0.x;
    float stage9_w = r1.y / r0.x;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    r1.w = stage9_w;
    // mad r1.xyzw, r1.xyzw, l(0.500000, 0.500000, 0.500000, 0.500000), l(0.500000, 0.500000, 0.500000, 0.500000)
    float stage10_x = r1.x * 0.5f + 0.5f;
    float stage10_y = r1.y * 0.5f + 0.5f;
    float stage10_z = r1.z * 0.5f + 0.5f;
    float stage10_w = r1.w * 0.5f + 0.5f;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    r1.w = stage10_w;
    // mul r1.xyzw, r1.xyzw, v4.xxxx
    float stage11_x = r1.x * v4.x;
    float stage11_y = r1.y * v4.x;
    float stage11_z = r1.z * v4.x;
    float stage11_w = r1.w * v4.x;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    r1.w = stage11_w;
    // max r0.x, v4.z, l(0.000010)
    r0.x = max(v4.z, 1e-05f);
    // div r0.x, l(1.000000, 1.000000, 1.000000, 1.000000), r0.x
    r0.x = 1.0f / r0.x;
    // add r2.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage12_x = v2.x + -0.5f;
    float stage12_y = v2.y + -0.5f;
    r2.x = stage12_x;
    r2.y = stage12_y;
    // dp2 r0.y, r2.xyxx, r2.xyxx
    r0.y = dot(r2.xy, r2.xy);
    // sqrt r0.y, r0.y
    r0.y = sqrt(r0.y);
    // mad r0.x, -r0.y, r0.x, l(1.000000)
    r0.x = -r0.y * r0.x + 1.0f;
    // add_sat r0.x, r0.x, r0.x
    r0.x = saturate(r0.x + r0.x);
    // mul r1.xyzw, r1.xyzw, r0.xxxx
    float stage13_x = r1.x * r0.x;
    float stage13_y = r1.y * r0.x;
    float stage13_z = r1.z * r0.x;
    float stage13_w = r1.w * r0.x;
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    r1.w = stage13_w;
    // mul r1.xy, r1.xyxx, l(0.200000, 1.000000, 0.000000, 0.000000)
    float stage14_x = r1.x * 0.2f;
    float stage14_y = r1.y * 1.0f;
    r1.x = stage14_x;
    r1.y = stage14_y;
    // mad r1.zw, r1.zzzw, l(0.000000, 0.000000, 0.100000, 0.500000), v2.xxxy
    float stage15_z = r1.z * 0.1f + v2.x;
    float stage15_w = r1.w * 0.5f + v2.y;
    r1.z = stage15_z;
    r1.w = stage15_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.z, r1.zwzz, t1.yzxw, s3, l(0.000000)
    float4 fetch16 = t1.SampleBias(s3, r1.zw, 0.0f);
    r1.z = fetch16.x;
    // mad r2.xy, v2.xyxx, cb0[3].xyxx, r1.xyxx
    float stage17_x = v2.x * cb0[3].x + r1.x;
    float stage17_y = v2.y * cb0[3].y + r1.y;
    r2.x = stage17_x;
    r2.y = stage17_y;
    // mov r0.w, v4.w
    r0.w = v4.w;
    // add r0.zw, r0.zzzw, r2.xxxy
    float stage18_z = r0.z + r2.x;
    float stage18_w = r0.w + r2.y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // add r0.zw, r0.zzzw, cb0[6].xxxy
    float stage19_z = r0.z + cb0[6].x;
    float stage19_w = r0.w + cb0[6].y;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.zwzz, t3.xyzw, s1, l(0.000000)
    float4 fetch20 = t3.SampleBias(s1, r0.zw, 0.0f);
    r2.x = fetch20.x;
    r2.y = fetch20.y;
    r2.z = fetch20.z;
    // mad r0.zw, v2.xxxy, cb0[7].xxxy, r1.xxxy
    float stage21_z = v2.x * cb0[7].x + r1.x;
    float stage21_w = v2.y * cb0[7].y + r1.y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // add r0.zw, r0.zzzw, cb0[8].xxxy
    float stage22_z = r0.z + cb0[8].x;
    float stage22_w = r0.w + cb0[8].y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r3.xyz, r0.zwzz, t4.xyzw, s2, l(0.000000)
    float4 fetch23 = t4.SampleBias(s2, r0.zw, 0.0f);
    r3.x = fetch23.x;
    r3.y = fetch23.y;
    r3.z = fetch23.z;
    // mul r4.xyz, r2.xyzx, r3.xyzx
    float stage24_x = r2.x * r3.x;
    float stage24_y = r2.y * r3.y;
    float stage24_z = r2.z * r3.z;
    r4.x = stage24_x;
    r4.y = stage24_y;
    r4.z = stage24_z;
    // dp3 r0.z, r4.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.z = dot(r4.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r2.xyz, -r2.xyzx, r3.xyzx, r0.zzzz
    float stage25_x = -r2.x * r3.x + r0.z;
    float stage25_y = -r2.y * r3.y + r0.z;
    float stage25_z = -r2.z * r3.z + r0.z;
    r2.x = stage25_x;
    r2.y = stage25_y;
    r2.z = stage25_z;
    // mad r2.xyz, cb0[13].xxxx, r2.xyzx, r4.xyzx
    float stage26_x = cb0[13].x * r2.x + r4.x;
    float stage26_y = cb0[13].x * r2.y + r4.y;
    float stage26_z = cb0[13].x * r2.z + r4.z;
    r2.x = stage26_x;
    r2.y = stage26_y;
    r2.z = stage26_z;
    // max r2.xyz, |r2.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage27_x = max(abs(r2.x), 1e-06f);
    float stage27_y = max(abs(r2.y), 1e-06f);
    float stage27_z = max(abs(r2.z), 1e-06f);
    r2.x = stage27_x;
    r2.y = stage27_y;
    r2.z = stage27_z;
    // log r2.xyz, r2.xyzx
    float stage28_x = log2(r2.x);
    float stage28_y = log2(r2.y);
    float stage28_z = log2(r2.z);
    r2.x = stage28_x;
    r2.y = stage28_y;
    r2.z = stage28_z;
    // mul r2.xyz, r2.xyzx, cb0[13].yyyy
    float stage29_x = r2.x * cb0[13].y;
    float stage29_y = r2.y * cb0[13].y;
    float stage29_z = r2.z * cb0[13].y;
    r2.x = stage29_x;
    r2.y = stage29_y;
    r2.z = stage29_z;
    // exp r2.xyz, r2.xyzx
    float stage30_x = exp2(r2.x);
    float stage30_y = exp2(r2.y);
    float stage30_z = exp2(r2.z);
    r2.x = stage30_x;
    r2.y = stage30_y;
    r2.z = stage30_z;
    // mad r2.xyz, cb0[13].zzzz, r2.xyzx, cb0[2].xyzx
    float stage31_x = cb0[13].z * r2.x + cb0[2].x;
    float stage31_y = cb0[13].z * r2.y + cb0[2].y;
    float stage31_z = cb0[13].z * r2.z + cb0[2].z;
    r2.x = stage31_x;
    r2.y = stage31_y;
    r2.z = stage31_z;
    // mul r0.xzw, r0.xxxx, r2.xxyz
    float stage32_x = r0.x * r2.x;
    float stage32_z = r0.x * r2.y;
    float stage32_w = r0.x * r2.z;
    r0.x = stage32_x;
    r0.z = stage32_z;
    r0.w = stage32_w;
    // mad r0.xzw, v3.xxyz, r0.xxzw, cb0[1].xxyz
    float stage33_x = v3.x * r0.x + cb0[1].x;
    float stage33_z = v3.y * r0.z + cb0[1].y;
    float stage33_w = v3.z * r0.w + cb0[1].z;
    r0.x = stage33_x;
    r0.z = stage33_z;
    r0.w = stage33_w;
    // mad o0.xyz, r0.xzwx, v5.wwww, v5.xyzx
    float stage34_x = r0.x * v5.w + v5.x;
    float stage34_y = r0.z * v5.w + v5.y;
    float stage34_z = r0.w * v5.w + v5.z;
    o0.x = stage34_x;
    o0.y = stage34_y;
    o0.z = stage34_z;
    // add r0.x, v4.z, l(0.100000)
    r0.x = v4.z + 0.1f;
    // max r0.x, r0.x, l(0.000010)
    r0.x = max(r0.x, 1e-05f);
    // div r0.x, l(1.000000, 1.000000, 1.000000, 1.000000), r0.x
    r0.x = 1.0f / r0.x;
    // mad r0.x, -r0.y, r0.x, l(1.000000)
    r0.x = -r0.y * r0.x + 1.0f;
    // add_sat r0.x, r0.x, r0.x
    r0.x = saturate(r0.x + r0.x);
    // mul r0.x, r1.z, r0.x
    r0.x = r1.z * r0.x;
    // mad r0.yz, v2.xxyx, cb0[9].xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage35_y = v2.x * cb0[9].x + -0.5f;
    float stage35_z = v2.y * cb0[9].y + -0.5f;
    r0.y = stage35_y;
    r0.z = stage35_z;
    // dp2 r0.w, cb0[11].xyxx, r0.yzyy
    r0.w = dot(cb0[11].xy, r0.yz);
    // dp2 r2.x, cb0[10].xyxx, r0.yzyy
    r2.x = dot(cb0[10].xy, r0.yz);
    // add r2.y, r0.w, v4.y
    r2.y = r0.w + v4.y;
    // add r0.yz, r2.xxyx, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage36_y = r2.x + 0.5f;
    float stage36_z = r2.y + 0.5f;
    r0.y = stage36_y;
    r0.z = stage36_z;
    // mad r0.yz, cb0[14].zzzz, r1.xxyx, r0.yyzy
    float stage37_y = cb0[14].z * r1.x + r0.y;
    float stage37_z = cb0[14].z * r1.y + r0.z;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t2.yxzw, s4, l(0.000000)
    float4 fetch38 = t2.SampleBias(s4, r0.yz, 0.0f);
    r0.y = fetch38.x;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // max r0.x, |r0.x|, l(0.000001)
    r0.x = max(abs(r0.x), 1e-06f);
    // log r0.x, r0.x
    r0.x = log2(r0.x);
    // mul r0.x, r0.x, cb0[14].w
    r0.x = r0.x * cb0[14].w;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul_sat r0.x, r0.x, cb0[15].x
    r0.x = saturate(r0.x * cb0[15].x);
    // mul r0.x, r0.x, v3.w
    r0.x = r0.x * v3.w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage39_x = r0.x * v1.x;
    float stage39_y = r0.x * v1.y;
    float stage39_z = r0.x * v1.z;
    r0.x = stage39_x;
    r0.y = stage39_y;
    r0.z = stage39_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage40_x = r0.w * v0.z;
    float stage40_y = r0.w * v0.x;
    float stage40_z = r0.w * v0.y;
    r1.x = stage40_x;
    r1.y = stage40_y;
    r1.z = stage40_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage41_x = r0.x * r1.x;
    float stage41_y = r0.x * r1.y;
    float stage41_z = r0.x * r1.z;
    r0.x = stage41_x;
    r0.y = stage41_y;
    r0.z = stage41_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage42_x = r0.x / r0.w;
    float stage42_y = r0.y / r0.w;
    r0.x = stage42_x;
    r0.y = stage42_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage43_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage43_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage43_x;
    r1.y = stage43_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage44_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage44_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage44_x;
    r1.y = stage44_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage45_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage45_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage45_x;
    r1.y = stage45_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage46_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage46_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage46_x;
    r0.y = stage46_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage47_x = r0.x * 0.5f + 0.5f;
    float stage47_y = r0.y * 0.5f + 0.5f;
    o2.x = stage47_x;
    o2.y = stage47_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage48_z = 1.0f;
    float stage48_w = asfloat(0u);
    o2.z = stage48_z;
    o2.w = stage48_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage49_x = cb2[3].x;
    float stage49_y = cb2[3].y;
    float stage49_z = cb2[3].z;
    o3.x = stage49_x;
    o3.y = stage49_y;
    o3.z = stage49_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage50_x = asfloat(0u);
    float stage50_y = asfloat(0u);
    float stage50_z = asfloat(0u);
    float stage50_w = asfloat(0u);
    o4.x = stage50_x;
    o4.y = stage50_y;
    o4.z = stage50_z;
    o4.w = stage50_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage51_x = asfloat(0u);
    float stage51_y = asfloat(0u);
    float stage51_z = asfloat(0u);
    float stage51_w = asfloat(0u);
    o5.x = stage51_x;
    o5.y = stage51_y;
    o5.z = stage51_z;
    o5.w = stage51_w;

    Shade_Ue3_fx_k_pa_flowmask_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
