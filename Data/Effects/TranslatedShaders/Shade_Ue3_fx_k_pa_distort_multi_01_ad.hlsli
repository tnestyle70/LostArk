// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_pa_distort_multi_01_ad_Constants0 : register(b0)
{
    float4 cb0[10];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_k_pa_distort_multi_01_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_pa_distort_multi_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_k_pa_distort_multi_01_ad_OUTPUT Shade_Ue3_fx_k_pa_distort_multi_01_ad(Shade_Ue3_fx_k_pa_distort_multi_01_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 o0 = (float4)0;

    // mad r0.xy, v2.xyxx, cb0[3].xyxx, v4.yyyy
    float stage1_x = v2.x * cb0[3].x + v4.y;
    float stage1_y = v2.y * cb0[3].y + v4.y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // add r0.xy, r0.xyxx, cb0[4].xyxx
    float stage2_x = r0.x + cb0[4].x;
    float stage2_y = r0.y + cb0[4].y;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch3 = t0.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch3.x;
    // mad r0.yz, v2.xxyx, cb0[5].xxyx, v4.yyyy
    float stage4_y = v2.x * cb0[5].x + v4.y;
    float stage4_z = v2.y * cb0[5].y + v4.y;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // add r0.yz, r0.yyzy, cb0[6].xxyx
    float stage5_y = r0.y + cb0[6].x;
    float stage5_z = r0.z + cb0[6].y;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t1.xyzw, s1, l(0.000000)
    float4 fetch6 = t1.SampleBias(s1, r0.yz, 0.0f);
    r0.y = fetch6.y;
    // add r0.x, r0.y, r0.x
    r0.x = r0.y + r0.x;
    // mad r0.yz, cb0[8].wwww, r0.xxxx, v2.xxyx
    float stage7_y = cb0[8].w * r0.x + v2.x;
    float stage7_z = cb0[8].w * r0.x + v2.y;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r0.yzyy, t3.yzwx, s4, l(0.000000)
    float4 fetch8 = t3.SampleBias(s4, r0.yz, 0.0f);
    r0.w = fetch8.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t2.yxzw, s3, l(0.000000)
    float4 fetch9 = t2.SampleBias(s3, r0.yz, 0.0f);
    r0.y = fetch9.x;
    // add r0.z, -r0.y, r0.w
    r0.z = -r0.y + r0.w;
    // mad r0.y, v4.x, r0.z, r0.y
    r0.y = v4.x * r0.z + r0.y;
    // max r0.y, |r0.y|, l(0.000001)
    r0.y = max(abs(r0.y), 1e-06f);
    // log r0.y, r0.y
    r0.y = log2(r0.y);
    // mul r0.y, r0.y, cb0[9].z
    r0.y = r0.y * cb0[9].z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, cb0[9].w
    r0.y = r0.y * cb0[9].w;
    // mul r0.y, r0.y, v3.w
    r0.y = r0.y * v3.w;
    // mul r0.y, r0.y, cb0[0].x
    r0.y = r0.y * cb0[0].x;
    // mad r0.zw, v2.xxxy, cb0[2].xxxy, v4.yyyy
    float stage10_z = v2.x * cb0[2].x + v4.y;
    float stage10_w = v2.y * cb0[2].y + v4.y;
    r0.z = stage10_z;
    r0.w = stage10_w;
    // mad r0.xz, cb0[8].wwww, r0.xxxx, r0.zzwz
    float stage11_x = cb0[8].w * r0.x + r0.z;
    float stage11_z = cb0[8].w * r0.x + r0.w;
    r0.x = stage11_x;
    r0.z = stage11_z;
    // add r0.xz, r0.xxzx, cb0[7].xxyx
    float stage12_x = r0.x + cb0[7].x;
    float stage12_z = r0.z + cb0[7].y;
    r0.x = stage12_x;
    r0.z = stage12_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xzw, r0.xzxx, t4.xwyz, s2, l(0.000000)
    float4 fetch13 = t4.SampleBias(s2, r0.xz, 0.0f);
    r0.x = fetch13.x;
    r0.z = fetch13.y;
    r0.w = fetch13.z;
    // max r0.xzw, |r0.xxzw|, l(0.000001, 0.000000, 0.000001, 0.000001)
    float stage14_x = max(abs(r0.x), 1e-06f);
    float stage14_z = max(abs(r0.z), 1e-06f);
    float stage14_w = max(abs(r0.w), 1e-06f);
    r0.x = stage14_x;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // log r0.xzw, r0.xxzw
    float stage15_x = log2(r0.x);
    float stage15_z = log2(r0.z);
    float stage15_w = log2(r0.w);
    r0.x = stage15_x;
    r0.z = stage15_z;
    r0.w = stage15_w;
    // mul r0.xzw, r0.xxzw, cb0[9].xxxx
    float stage16_x = r0.x * cb0[9].x;
    float stage16_z = r0.z * cb0[9].x;
    float stage16_w = r0.w * cb0[9].x;
    r0.x = stage16_x;
    r0.z = stage16_z;
    r0.w = stage16_w;
    // exp r0.xzw, r0.xxzw
    float stage17_x = exp2(r0.x);
    float stage17_z = exp2(r0.z);
    float stage17_w = exp2(r0.w);
    r0.x = stage17_x;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // mul r0.xzw, r0.xxzw, cb0[9].yyyy
    float stage18_x = r0.x * cb0[9].y;
    float stage18_z = r0.z * cb0[9].y;
    float stage18_w = r0.w * cb0[9].y;
    r0.x = stage18_x;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mad r0.xzw, r0.xxzw, v3.xxyz, cb0[1].xxyz
    float stage19_x = r0.x * v3.x + cb0[1].x;
    float stage19_z = r0.z * v3.y + cb0[1].y;
    float stage19_w = r0.w * v3.z + cb0[1].z;
    r0.x = stage19_x;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // mul r0.xzw, r0.xxzw, v5.wwww
    float stage20_x = r0.x * v5.w;
    float stage20_z = r0.z * v5.w;
    float stage20_w = r0.w * v5.w;
    r0.x = stage20_x;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // mul o0.xyz, r0.yyyy, r0.xzwx
    float stage21_x = r0.y * r0.x;
    float stage21_y = r0.y * r0.z;
    float stage21_z = r0.y * r0.w;
    o0.x = stage21_x;
    o0.y = stage21_y;
    o0.z = stage21_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_k_pa_distort_multi_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
