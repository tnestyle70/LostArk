// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_master_01_tr_Constants0 : register(b0)
{
    float4 cb0[14];
};
cbuffer Shade_Ue3_fx_d_pa_master_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_d_pa_master_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    float4 v6;
    float4 v7;
};

struct Shade_Ue3_fx_d_pa_master_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_d_pa_master_01_tr_OUTPUT Shade_Ue3_fx_d_pa_master_01_tr(Shade_Ue3_fx_d_pa_master_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.xy, v2.xyxx, cb0[8].xyxx
    float stage1_x = v2.x * cb0[8].x;
    float stage1_y = v2.y * cb0[8].y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mul r0.z, cb0[6].z, cb0[6].w
    r0.z = cb0[6].z * cb0[6].w;
    // mad r1.x, r0.z, cb0[7].w, r0.x
    r1.x = r0.z * cb0[7].w + r0.x;
    // mad r1.y, r0.z, cb0[8].z, r0.y
    r1.y = r0.z * cb0[8].z + r0.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xy, r1.xyxx, t0.xyzw, s1, l(0.000000)
    float4 fetch2 = t0.SampleBias(s1, r1.xy, 0.0f);
    r0.x = fetch2.x;
    r0.y = fetch2.y;
    // mad r0.xy, cb0[9].xxxx, r0.xyxx, v2.xyxx
    float stage3_x = cb0[9].x * r0.x + v2.x;
    float stage3_y = cb0[9].x * r0.y + v2.y;
    r0.x = stage3_x;
    r0.y = stage3_y;
    // mul r0.w, r0.z, cb0[9].z
    r0.w = r0.z * cb0[9].z;
    // mad r1.x, cb0[9].w, r0.x, r0.w
    r1.x = cb0[9].w * r0.x + r0.w;
    // mul r0.w, r0.y, cb0[10].x
    r0.w = r0.y * cb0[10].x;
    // mad r1.y, r0.z, cb0[10].y, r0.w
    r1.y = r0.z * cb0[10].y + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t4.xyzw, s3, l(0.000000)
    float4 fetch4 = t4.SampleBias(s3, r1.xy, 0.0f);
    r1.x = fetch4.x;
    r1.y = fetch4.y;
    r1.z = fetch4.z;
    // mul r2.xy, r0.xyxx, cb0[7].yzyy
    float stage5_x = r0.x * cb0[7].y;
    float stage5_y = r0.y * cb0[7].z;
    r2.x = stage5_x;
    r2.y = stage5_y;
    // add r0.xy, r0.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage6_x = r0.x + -0.5f;
    float stage6_y = r0.y + -0.5f;
    r0.x = stage6_x;
    r0.y = stage6_y;
    // mad r3.x, r0.z, cb0[7].x, r2.x
    r3.x = r0.z * cb0[7].x + r2.x;
    // mad r3.y, r0.z, cb0[9].y, r2.y
    r3.y = r0.z * cb0[9].y + r2.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r3.xyxx, t3.xyzw, s2, l(0.000000)
    float4 fetch7 = t3.SampleBias(s2, r3.xy, 0.0f);
    r2.x = fetch7.x;
    r2.y = fetch7.y;
    r2.z = fetch7.z;
    // mul r3.xyz, r1.xyzx, r2.xyzx
    float stage8_x = r1.x * r2.x;
    float stage8_y = r1.y * r2.y;
    float stage8_z = r1.z * r2.z;
    r3.x = stage8_x;
    r3.y = stage8_y;
    r3.z = stage8_z;
    // dp3 r0.z, r3.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.z = dot(r3.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r2.xyzx, r1.xyzx, r0.zzzz
    float stage9_x = -r2.x * r1.x + r0.z;
    float stage9_y = -r2.y * r1.y + r0.z;
    float stage9_z = -r2.z * r1.z + r0.z;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    // mad r1.xyz, cb0[10].zzzz, r1.xyzx, r3.xyzx
    float stage10_x = cb0[10].z * r1.x + r3.x;
    float stage10_y = cb0[10].z * r1.y + r3.y;
    float stage10_z = cb0[10].z * r1.z + r3.z;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage11_x = max(abs(r1.x), 1e-06f);
    float stage11_y = max(abs(r1.y), 1e-06f);
    float stage11_z = max(abs(r1.z), 1e-06f);
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    // log r1.xyz, r1.xyzx
    float stage12_x = log2(r1.x);
    float stage12_y = log2(r1.y);
    float stage12_z = log2(r1.z);
    r1.x = stage12_x;
    r1.y = stage12_y;
    r1.z = stage12_z;
    // mul r1.xyz, r1.xyzx, cb0[10].wwww
    float stage13_x = r1.x * cb0[10].w;
    float stage13_y = r1.y * cb0[10].w;
    float stage13_z = r1.z * cb0[10].w;
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    // exp r1.xyz, r1.xyzx
    float stage14_x = exp2(r1.x);
    float stage14_y = exp2(r1.y);
    float stage14_z = exp2(r1.z);
    r1.x = stage14_x;
    r1.y = stage14_y;
    r1.z = stage14_z;
    // mad r0.z, cb0[6].w, cb0[11].y, cb0[11].z
    r0.z = cb0[6].w * cb0[11].y + cb0[11].z;
    // sincos r2.x, r3.x, r0.z
    float stage15_x = r0.z;
    r2.x = sin(stage15_x);
    r3.x = cos(stage15_x);
    // mov r4.x, -r2.x
    r4.x = -r2.x;
    // mov r4.y, r3.x
    r4.y = r3.x;
    // mov r4.z, r2.x
    r4.z = r2.x;
    // dp2 r0.z, r4.zyzz, r0.xyxx
    r0.z = dot(r4.zy, r0.xy);
    // dp2 r0.x, r4.yxyy, r0.xyxx
    r0.x = dot(r4.yx, r0.xy);
    // mul r2.z, r0.z, cb0[4].y
    r2.z = r0.z * cb0[4].y;
    // add r0.y, v4.y, l(-1.000000)
    r0.y = v4.y + -1.0f;
    // mad r2.x, r0.x, cb0[4].x, r0.y
    r2.x = r0.x * cb0[4].x + r0.y;
    // add r0.xy, r2.xzxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage16_x = r2.x + 0.5f;
    float stage16_y = r2.z + 0.5f;
    r0.x = stage16_x;
    r0.y = stage16_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s4, l(0.000000)
    float4 fetch17 = t1.SampleBias(s4, r0.xy, 0.0f);
    r0.x = fetch17.x;
    // mul r0.yzw, cb0[3].xxyz, cb0[3].wwww
    float stage18_y = cb0[3].x * cb0[3].w;
    float stage18_z = cb0[3].y * cb0[3].w;
    float stage18_w = cb0[3].z * cb0[3].w;
    r0.y = stage18_y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mad r2.xy, r1.xyxx, r0.yzyy, r0.xxxx
    float stage19_x = r1.x * r0.y + r0.x;
    float stage19_y = r1.y * r0.z + r0.x;
    r2.x = stage19_x;
    r2.y = stage19_y;
    // mul r0.yzw, r0.yyzw, r1.xxyz
    float stage20_y = r0.y * r1.x;
    float stage20_z = r0.z * r1.y;
    float stage20_w = r0.w * r1.z;
    r0.y = stage20_y;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // mul r1.xy, r2.xyxx, cb0[12].yyyy
    float stage21_x = r2.x * cb0[12].y;
    float stage21_y = r2.y * cb0[12].y;
    r1.x = stage21_x;
    r1.y = stage21_y;
    // dp3 r1.z, v6.xyzx, v6.xyzx
    r1.z = dot(v6.xyz, v6.xyz);
    // rsq r1.z, r1.z
    r1.z = rsqrt(r1.z);
    // mul r1.zw, r1.zzzz, v6.xxxy
    float stage22_z = r1.z * v6.x;
    float stage22_w = r1.z * v6.y;
    r1.z = stage22_z;
    r1.w = stage22_w;
    // mad r1.xy, r1.zwzz, cb0[2].xyxx, r1.xyxx
    float stage23_x = r1.z * cb0[2].x + r1.x;
    float stage23_y = r1.w * cb0[2].y + r1.y;
    r1.x = stage23_x;
    r1.y = stage23_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t5.xyzw, s5, l(0.000000)
    float4 fetch24 = t5.SampleBias(s5, r1.xy, 0.0f);
    r1.x = fetch24.x;
    r1.y = fetch24.y;
    r1.z = fetch24.z;
    // dp3 r1.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r2.xyz, -r1.xyzx, r1.wwww
    float stage25_x = -r1.x + r1.w;
    float stage25_y = -r1.y + r1.w;
    float stage25_z = -r1.z + r1.w;
    r2.x = stage25_x;
    r2.y = stage25_y;
    r2.z = stage25_z;
    // mad r1.xyz, cb0[12].zzzz, r2.xyzx, r1.xyzx
    float stage26_x = cb0[12].z * r2.x + r1.x;
    float stage26_y = cb0[12].z * r2.y + r1.y;
    float stage26_z = cb0[12].z * r2.z + r1.z;
    r1.x = stage26_x;
    r1.y = stage26_y;
    r1.z = stage26_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage27_x = max(abs(r1.x), 1e-06f);
    float stage27_y = max(abs(r1.y), 1e-06f);
    float stage27_z = max(abs(r1.z), 1e-06f);
    r1.x = stage27_x;
    r1.y = stage27_y;
    r1.z = stage27_z;
    // log r1.xyz, r1.xyzx
    float stage28_x = log2(r1.x);
    float stage28_y = log2(r1.y);
    float stage28_z = log2(r1.z);
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // mul r1.xyz, r1.xyzx, cb0[12].wwww
    float stage29_x = r1.x * cb0[12].w;
    float stage29_y = r1.y * cb0[12].w;
    float stage29_z = r1.z * cb0[12].w;
    r1.x = stage29_x;
    r1.y = stage29_y;
    r1.z = stage29_z;
    // exp r1.xyz, r1.xyzx
    float stage30_x = exp2(r1.x);
    float stage30_y = exp2(r1.y);
    float stage30_z = exp2(r1.z);
    r1.x = stage30_x;
    r1.y = stage30_y;
    r1.z = stage30_z;
    // mul r2.xyz, cb0[5].xyzx, cb0[5].wwww
    float stage31_x = cb0[5].x * cb0[5].w;
    float stage31_y = cb0[5].y * cb0[5].w;
    float stage31_z = cb0[5].z * cb0[5].w;
    r2.x = stage31_x;
    r2.y = stage31_y;
    r2.z = stage31_z;
    // mad r0.yzw, r1.xxyz, r2.xxyz, r0.yyzw
    float stage32_y = r1.x * r2.x + r0.y;
    float stage32_z = r1.y * r2.y + r0.z;
    float stage32_w = r1.z * r2.z + r0.w;
    r0.y = stage32_y;
    r0.z = stage32_z;
    r0.w = stage32_w;
    // mad r0.yzw, r0.yyzw, v3.xxyz, cb0[1].xxyz
    float stage33_y = r0.y * v3.x + cb0[1].x;
    float stage33_z = r0.z * v3.y + cb0[1].y;
    float stage33_w = r0.w * v3.z + cb0[1].z;
    r0.y = stage33_y;
    r0.z = stage33_z;
    r0.w = stage33_w;
    // mad o0.xyz, r0.yzwy, v5.wwww, v5.xyzx
    float stage34_x = r0.y * v5.w + v5.x;
    float stage34_y = r0.z * v5.w + v5.y;
    float stage34_z = r0.w * v5.w + v5.z;
    o0.x = stage34_x;
    o0.y = stage34_y;
    o0.z = stage34_z;
    // add r0.y, -v4.x, l(1.000000)
    r0.y = -v4.x + 1.0f;
    // add r0.x, -r0.y, r0.x
    r0.x = -r0.y + r0.x;
    // mul_sat r0.x, r0.x, cb0[13].x
    r0.x = saturate(r0.x * cb0[13].x);
    // log r0.y, r0.x
    r0.y = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[13].y
    r0.y = r0.y * cb0[13].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // div r0.zw, v7.xxxy, v7.wwww
    float stage35_z = v7.x / v7.w;
    float stage35_w = v7.y / v7.w;
    r0.z = stage35_z;
    r0.w = stage35_w;
    // mad r0.zw, r0.zzzw, cb2[0].xxxy, cb2[0].wwwz
    float stage36_z = r0.z * cb2[0].x + cb2[0].w;
    float stage36_w = r0.w * cb2[0].y + cb2[0].z;
    r0.z = stage36_z;
    r0.w = stage36_w;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.z, r0.zwzz, t2.yzxw, s0, l(0.000000)
    float4 fetch37 = t2.SampleLevel(s0, r0.zw, 0.0f);
    r0.z = fetch37.x;
    // min r0.z, r0.z, l(0.999000)
    r0.z = min(r0.z, 0.999f);
    // mad r0.w, r0.z, cb2[1].z, -cb2[1].w
    r0.w = r0.z * cb2[1].z + -cb2[1].w;
    // mad r0.z, r0.z, cb2[1].x, cb2[1].y
    r0.z = r0.z * cb2[1].x + cb2[1].y;
    // div r0.w, l(1.000000, 1.000000, 1.000000, 1.000000), r0.w
    r0.w = 1.0f / r0.w;
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // add r0.z, r0.z, -v7.w
    r0.z = r0.z + -v7.w;
    // add r0.w, -cb0[13].z, l(1.000000)
    r0.w = -cb0[13].z + 1.0f;
    // max r0.w, -r0.w, l(0.001000)
    r0.w = max(-r0.w, 0.001f);
    // div_sat r0.z, r0.z, r0.w
    r0.z = saturate(r0.z / r0.w);
    // mul r0.y, r0.z, r0.y
    r0.y = r0.z * r0.y;
    // mul_sat r0.y, r0.y, v3.w
    r0.y = saturate(r0.y * v3.w);
    // mul r0.y, r0.y, cb0[0].x
    r0.y = r0.y * cb0[0].x;
    // movc o0.w, r0.x, l(0), r0.y
    o0.w = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage38_x = r0.x * v1.x;
    float stage38_y = r0.x * v1.y;
    float stage38_z = r0.x * v1.z;
    r0.x = stage38_x;
    r0.y = stage38_y;
    r0.z = stage38_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage39_x = r0.w * v0.z;
    float stage39_y = r0.w * v0.x;
    float stage39_z = r0.w * v0.y;
    r1.x = stage39_x;
    r1.y = stage39_y;
    r1.z = stage39_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage40_x = r0.x * r1.x;
    float stage40_y = r0.x * r1.y;
    float stage40_z = r0.x * r1.z;
    r0.x = stage40_x;
    r0.y = stage40_y;
    r0.z = stage40_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage41_x = r0.x / r0.w;
    float stage41_y = r0.y / r0.w;
    r0.x = stage41_x;
    r0.y = stage41_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage42_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage42_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage42_x;
    r1.y = stage42_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage43_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage43_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage43_x;
    r1.y = stage43_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage44_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage44_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage44_x;
    r1.y = stage44_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage45_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage45_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage45_x;
    r0.y = stage45_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage46_x = r0.x * 0.5f + 0.5f;
    float stage46_y = r0.y * 0.5f + 0.5f;
    o2.x = stage46_x;
    o2.y = stage46_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage47_z = 1.0f;
    float stage47_w = asfloat(0u);
    o2.z = stage47_z;
    o2.w = stage47_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage48_x = cb2[3].x;
    float stage48_y = cb2[3].y;
    float stage48_z = cb2[3].z;
    o3.x = stage48_x;
    o3.y = stage48_y;
    o3.z = stage48_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage49_x = asfloat(0u);
    float stage49_y = asfloat(0u);
    float stage49_z = asfloat(0u);
    float stage49_w = asfloat(0u);
    o4.x = stage49_x;
    o4.y = stage49_y;
    o4.z = stage49_z;
    o4.w = stage49_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage50_x = asfloat(0u);
    float stage50_y = asfloat(0u);
    float stage50_z = asfloat(0u);
    float stage50_w = asfloat(0u);
    o5.x = stage50_x;
    o5.y = stage50_y;
    o5.z = stage50_z;
    o5.w = stage50_w;

    Shade_Ue3_fx_d_pa_master_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
