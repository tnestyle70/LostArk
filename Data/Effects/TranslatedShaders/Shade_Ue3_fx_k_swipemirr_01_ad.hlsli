// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_swipemirr_01_ad_Constants0 : register(b0)
{
    float4 cb0[5];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);

struct Shade_Ue3_fx_k_swipemirr_01_ad_INPUT
{
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_swipemirr_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_k_swipemirr_01_ad_OUTPUT Shade_Ue3_fx_k_swipemirr_01_ad(Shade_Ue3_fx_k_swipemirr_01_ad_INPUT stage)
{
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;

    // add r0.xyzw, v4.xyxy, l(-0.350000, -0.500000, -0.450000, -0.500000)
    float stage1_x = v4.x + -0.35f;
    float stage1_y = v4.y + -0.5f;
    float stage1_z = v4.x + -0.45f;
    float stage1_w = v4.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    r0.z = stage1_z;
    r0.w = stage1_w;
    // mul r1.xy, r0.yyyy, l(2.500000, -2.500000, 0.000000, 0.000000)
    float stage2_x = r0.y * 2.5f;
    float stage2_y = r0.y * -2.5f;
    r1.x = stage2_x;
    r1.y = stage2_y;
    // mov_sat r1.yz, r1.xxyx
    float stage3_y = saturate(r1.x);
    float stage3_z = saturate(r1.y);
    r1.y = stage3_y;
    r1.z = stage3_z;
    // add r1.y, r1.z, -r1.y
    r1.y = r1.z + -r1.y;
    // log r1.z, |r1.y|
    r1.z = log2(abs(r1.y));
    // lt r1.y, |r1.y|, l(0.000001)
    r1.y = asfloat((abs(r1.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.z, r1.z, l(0.400000)
    r1.z = r1.z * 0.4f;
    // exp r1.z, r1.z
    r1.z = exp2(r1.z);
    // movc r2.y, r1.y, l(0), r1.z
    r2.y = asuint(r1.y) != 0u ? asfloat(0u) : r1.z;
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // dp2 r0.y, r0.zwzz, r0.zwzz
    r0.y = dot(r0.zw, r0.zw);
    // sqrt r0.xy, r0.xyxx
    float stage4_x = sqrt(r0.x);
    float stage4_y = sqrt(r0.y);
    r0.x = stage4_x;
    r0.y = stage4_y;
    // mad r0.y, -r0.y, l(2.500000), l(1.000000)
    r0.y = -r0.y * 2.5f + 1.0f;
    // mul_sat r0.y, r0.y, l(1.111111)
    r0.y = saturate(r0.y * 1.111111f);
    // add r0.y, -r0.y, l(1.000000)
    r0.y = -r0.y + 1.0f;
    // mul r0.y, r0.y, r1.x
    r0.y = r0.y * r1.x;
    // mad r0.x, -r0.x, l(1.666667), l(1.000000)
    r0.x = -r0.x * 1.666667f + 1.0f;
    // mul_sat r0.x, r0.x, l(1.111111)
    r0.x = saturate(r0.x * 1.111111f);
    // mad r2.x, r0.x, l(0.850000), v4.x
    r2.x = r0.x * 0.85f + v4.x;
    // mad r0.xz, r2.xxyx, l(1.000000, 0.000000, 0.700000, 0.000000), cb0[3].xxyx
    float stage5_x = r2.x * 1.0f + cb0[3].x;
    float stage5_z = r2.y * 0.7f + cb0[3].y;
    r0.x = stage5_x;
    r0.z = stage5_z;
    // mad r1.xy, r2.xyxx, l(1.000000, 0.700000, 0.000000, 0.000000), cb0[4].xyxx
    float stage6_x = r2.x * 1.0f + cb0[4].x;
    float stage6_y = r2.y * 0.7f + cb0[4].y;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t2.yzwx, s2, l(0.000000)
    float4 fetch7 = t2.SampleBias(s2, r1.xy, 0.0f);
    r0.w = fetch7.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xzxx, t1.xyzw, s1, l(0.000000)
    float4 fetch8 = t1.SampleBias(s1, r0.xz, 0.0f);
    r0.x = fetch8.x;
    // add r0.x, r0.w, r0.x
    r0.x = r0.w + r0.x;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul r0.x, r0.x, l(1.500000)
    r0.x = r0.x * 1.5f;
    // lt r0.y, |r0.x|, l(0.000001)
    r0.y = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.x, |r0.x|, |r0.x|
    r0.x = abs(r0.x) * abs(r0.x);
    // movc r0.x, r0.y, l(0), r0.x
    r0.x = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, v4.xyxx, t0.yxzw, s0, l(0.000000)
    float4 fetch9 = t0.SampleBias(s0, v4.xy, 0.0f);
    r0.y = fetch9.x;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mad r0.xyz, r0.xxxx, cb0[1].xyzx, cb0[2].xyzx
    float stage10_x = r0.x * cb0[1].x + cb0[2].x;
    float stage10_y = r0.x * cb0[1].y + cb0[2].y;
    float stage10_z = r0.x * cb0[1].z + cb0[2].z;
    r0.x = stage10_x;
    r0.y = stage10_y;
    r0.z = stage10_z;
    // mul r0.xyz, r0.xyzx, v5.wwww
    float stage11_x = r0.x * v5.w;
    float stage11_y = r0.y * v5.w;
    float stage11_z = r0.z * v5.w;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mul r0.w, cb0[0].x, cb0[1].w
    r0.w = cb0[0].x * cb0[1].w;
    // mul o0.xyz, r0.wwww, r0.xyzx
    float stage12_x = r0.w * r0.x;
    float stage12_y = r0.w * r0.y;
    float stage12_z = r0.w * r0.z;
    o0.x = stage12_x;
    o0.y = stage12_y;
    o0.z = stage12_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_k_swipemirr_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
