// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_c_pa_glitter_01_tr_Constants0 : register(b0)
{
    float4 cb0[4];
};
cbuffer Shade_Ue3_fx_c_pa_glitter_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_c_pa_glitter_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_c_pa_glitter_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_c_pa_glitter_01_tr_OUTPUT Shade_Ue3_fx_c_pa_glitter_01_tr(Shade_Ue3_fx_c_pa_glitter_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.xyzw, v2.xyxy, l(0.500000, 0.500000, 0.700000, 0.700000)
    float stage1_x = v2.x * 0.5f;
    float stage1_y = v2.y * 0.5f;
    float stage1_z = v2.x * 0.7f;
    float stage1_w = v2.y * 0.7f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    r0.z = stage1_z;
    r0.w = stage1_w;
    // mad r0.xy, v4.wwww, l(0.100000, -0.100000, 0.000000, 0.000000), r0.xyxx
    float stage2_x = v4.w * 0.1f + r0.x;
    float stage2_y = v4.w * -0.1f + r0.y;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch3 = t0.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch3.x;
    // mad r0.yz, r0.xxxx, l(0.000000, 0.400000, 0.400000, 0.000000), r0.zzwz
    float stage4_y = r0.x * 0.4f + r0.z;
    float stage4_z = r0.x * 0.4f + r0.w;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r0.yzyy, t1.yzwx, s4, l(0.000000)
    float4 fetch5 = t1.SampleBias(s4, r0.yz, 0.0f);
    r0.w = fetch5.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t4.yxzw, s1, l(0.000000)
    float4 fetch6 = t4.SampleBias(s1, r0.yz, 0.0f);
    r0.y = fetch6.x;
    // add r0.z, r0.w, r0.w
    r0.z = r0.w + r0.w;
    // mul r0.w, r0.x, l(0.200000)
    r0.w = r0.x * 0.2f;
    // add r1.xy, r0.xxxx, v2.xyxx
    float stage7_x = r0.x + v2.x;
    float stage7_y = r0.x + v2.y;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r1.xyxx, t5.xyzw, s2, l(0.000000)
    float4 fetch8 = t5.SampleBias(s2, r1.xy, 0.0f);
    r0.x = fetch8.x;
    // mad r1.xy, v2.xyxx, l(0.950000, 0.950000, 0.000000, 0.000000), r0.wwww
    float stage9_x = v2.x * 0.95f + r0.w;
    float stage9_y = v2.y * 0.95f + r0.w;
    r1.x = stage9_x;
    r1.y = stage9_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t2.yzwx, s5, l(0.000000)
    float4 fetch10 = t2.SampleBias(s5, r1.xy, 0.0f);
    r0.w = fetch10.x;
    // mul r0.z, r0.w, r0.z
    r0.z = r0.w * r0.z;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mul r0.w, r0.w, r0.w
    r0.w = r0.w * r0.w;
    // mul r1.x, r0.z, l(5.000000)
    r1.x = r0.z * 5.0f;
    // mad r0.w, r0.w, l(150.000000), r1.x
    r0.w = r0.w * 150.0f + r1.x;
    // mul r1.xyzw, v2.xyxy, cb0[2].zzww
    float stage11_x = v2.x * cb0[2].z;
    float stage11_y = v2.y * cb0[2].z;
    float stage11_z = v2.x * cb0[2].w;
    float stage11_w = v2.y * cb0[2].w;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    r1.w = stage11_w;
    // mad r1.xy, cb0[2].yyyy, cb0[2].xxxx, r1.xyxx
    float stage12_x = cb0[2].y * cb0[2].x + r1.x;
    float stage12_y = cb0[2].y * cb0[2].x + r1.y;
    r1.x = stage12_x;
    r1.y = stage12_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.z, r1.zwzz, t3.yzxw, s3, l(0.000000)
    float4 fetch13 = t3.SampleBias(s3, r1.zw, 0.0f);
    r1.z = fetch13.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t3.xyzw, s3, l(0.000000)
    float4 fetch14 = t3.SampleBias(s3, r1.xy, 0.0f);
    r1.x = fetch14.x;
    // mul r1.x, r1.z, r1.x
    r1.x = r1.z * r1.x;
    // mul r1.x, r1.x, cb0[3].x
    r1.x = r1.x * cb0[3].x;
    // mul r0.w, r0.w, r1.x
    r0.w = r0.w * r1.x;
    // mul r1.x, r0.x, r1.x
    r1.x = r0.x * r1.x;
    // mad r0.y, r1.x, l(30.000000), r0.y
    r0.y = r1.x * 30.0f + r0.y;
    // add r0.x, r0.x, r0.y
    r0.x = r0.x + r0.y;
    // mad r1.xyz, r0.xxxx, v3.xyzx, cb0[1].xyzx
    float stage15_x = r0.x * v3.x + cb0[1].x;
    float stage15_y = r0.x * v3.y + cb0[1].y;
    float stage15_z = r0.x * v3.z + cb0[1].z;
    r1.x = stage15_x;
    r1.y = stage15_y;
    r1.z = stage15_z;
    // mad o0.xyz, r1.xyzx, v5.wwww, v5.xyzx
    float stage16_x = r1.x * v5.w + v5.x;
    float stage16_y = r1.y * v5.w + v5.y;
    float stage16_z = r1.z * v5.w + v5.z;
    o0.x = stage16_x;
    o0.y = stage16_y;
    o0.z = stage16_z;
    // mul r0.x, r0.w, r0.z
    r0.x = r0.w * r0.z;
    // mul r0.x, r0.x, v3.w
    r0.x = r0.x * v3.w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage17_x = r0.x * v1.x;
    float stage17_y = r0.x * v1.y;
    float stage17_z = r0.x * v1.z;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage18_x = r0.w * v0.z;
    float stage18_y = r0.w * v0.x;
    float stage18_z = r0.w * v0.y;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage19_x = r0.x * r1.x;
    float stage19_y = r0.x * r1.y;
    float stage19_z = r0.x * r1.z;
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage20_x = r0.x / r0.w;
    float stage20_y = r0.y / r0.w;
    r0.x = stage20_x;
    r0.y = stage20_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage21_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage21_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage21_x;
    r1.y = stage21_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage22_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage22_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage22_x;
    r1.y = stage22_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage23_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage23_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage23_x;
    r1.y = stage23_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage24_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage24_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage24_x;
    r0.y = stage24_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage25_x = r0.x * 0.5f + 0.5f;
    float stage25_y = r0.y * 0.5f + 0.5f;
    o2.x = stage25_x;
    o2.y = stage25_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage26_z = 1.0f;
    float stage26_w = asfloat(0u);
    o2.z = stage26_z;
    o2.w = stage26_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage27_x = cb2[3].x;
    float stage27_y = cb2[3].y;
    float stage27_z = cb2[3].z;
    o3.x = stage27_x;
    o3.y = stage27_y;
    o3.z = stage27_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage28_x = asfloat(0u);
    float stage28_y = asfloat(0u);
    float stage28_z = asfloat(0u);
    float stage28_w = asfloat(0u);
    o4.x = stage28_x;
    o4.y = stage28_y;
    o4.z = stage28_z;
    o4.w = stage28_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage29_x = asfloat(0u);
    float stage29_y = asfloat(0u);
    float stage29_z = asfloat(0u);
    float stage29_w = asfloat(0u);
    o5.x = stage29_x;
    o5.y = stage29_y;
    o5.z = stage29_z;
    o5.w = stage29_w;

    Shade_Ue3_fx_c_pa_glitter_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
