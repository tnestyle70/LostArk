// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_me_makeflow_03_tr_Constants0 : register(b0)
{
    float4 cb0[17];
};
cbuffer Shade_Ue3_fx_k_me_makeflow_03_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_k_me_makeflow_03_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
};

struct Shade_Ue3_fx_k_me_makeflow_03_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_me_makeflow_03_tr_OUTPUT Shade_Ue3_fx_k_me_makeflow_03_tr(Shade_Ue3_fx_k_me_makeflow_03_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mov r0.y, cb0[12].z
    r0.y = cb0[12].z;
    // mov r0.x, l(0)
    r0.x = asfloat(0u);
    // mad r0.zw, v4.xxxy, cb0[4].xxxy, cb0[5].xxxy
    float stage1_z = v4.x * cb0[4].x + cb0[5].x;
    float stage1_w = v4.y * cb0[4].y + cb0[5].y;
    r0.z = stage1_z;
    r0.w = stage1_w;
    // add r1.xyzw, r0.zwzw, r0.yxxy
    float stage2_x = r0.z + r0.y;
    float stage2_y = r0.w + r0.x;
    float stage2_z = r0.z + r0.x;
    float stage2_w = r0.w + r0.y;
    r1.x = stage2_x;
    r1.y = stage2_y;
    r1.z = stage2_z;
    r1.w = stage2_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.zwzz, t0.xyzw, s0, l(0.000000)
    float4 fetch3 = t0.SampleBias(s0, r0.zw, 0.0f);
    r0.x = fetch3.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.xyxx, t0.yxzw, s0, l(0.000000)
    float4 fetch4 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.y = fetch4.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.zwzz, t0.yzxw, s0, l(0.000000)
    float4 fetch5 = t0.SampleBias(s0, r1.zw, 0.0f);
    r0.z = fetch5.x;
    // add r1.xy, -r0.xxxx, r0.yzyy
    float stage6_x = -r0.x + r0.y;
    float stage6_y = -r0.x + r0.z;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // mul r0.xy, r1.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage7_x = r1.x * 8.0f;
    float stage7_y = r1.y * 8.0f;
    r0.x = stage7_x;
    r0.y = stage7_y;
    // mov r0.z, l(0)
    r0.z = asfloat(0u);
    // add r0.xyz, -r0.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage8_x = -r0.x + 0.0f;
    float stage8_y = -r0.y + 0.0f;
    float stage8_z = -r0.z + 1.0f;
    r0.x = stage8_x;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // dp3 r0.z, r0.xyzx, r0.xyzx
    r0.z = dot(r0.xyz, r0.xyz);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // div r0.xy, r0.xyxx, r0.zzzz
    float stage9_x = r0.x / r0.z;
    float stage9_y = r0.y / r0.z;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // mad r0.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage10_x = r0.x * 0.5f + 0.5f;
    float stage10_y = r0.y * 0.5f + 0.5f;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // lt r0.z, |cb0[12].x|, l(0.000001)
    r0.z = asfloat((abs(cb0[12].x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, |cb0[12].x|, |cb0[12].x|
    r0.w = abs(cb0[12].x) * abs(cb0[12].x);
    // movc r0.z, r0.z, l(0), r0.w
    r0.z = asuint(r0.z) != 0u ? asfloat(0u) : r0.w;
    // mul r1.y, r0.z, v4.y
    r1.y = r0.z * v4.y;
    // mul r1.x, v4.x, cb0[12].y
    r1.x = v4.x * cb0[12].y;
    // mad r0.zw, cb0[6].xxxx, r0.xxxy, r1.xxxy
    float stage11_z = cb0[6].x * r0.x + r1.x;
    float stage11_w = cb0[6].x * r0.y + r1.y;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // add r0.w, r0.w, cb0[6].w
    r0.w = r0.w + cb0[6].w;
    // add r0.zw, r0.zzzw, cb0[7].xxxy
    float stage12_z = r0.z + cb0[7].x;
    float stage12_w = r0.w + cb0[7].y;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r0.zwzz, t2.xyzw, s1, l(0.000000)
    float4 fetch13 = t2.SampleBias(s1, r0.zw, 0.0f);
    r1.x = fetch13.x;
    r1.y = fetch13.y;
    r1.z = fetch13.z;
    // lt r0.z, |cb0[13].z|, l(0.000001)
    r0.z = asfloat((abs(cb0[13].z) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, |cb0[13].z|, |cb0[13].z|
    r0.w = abs(cb0[13].z) * abs(cb0[13].z);
    // movc r0.z, r0.z, l(0), r0.w
    r0.z = asuint(r0.z) != 0u ? asfloat(0u) : r0.w;
    // mul r2.y, r0.z, v4.y
    r2.y = r0.z * v4.y;
    // mul r2.x, v4.x, cb0[13].w
    r2.x = v4.x * cb0[13].w;
    // mad r0.zw, cb0[6].xxxx, r0.xxxy, r2.xxxy
    float stage14_z = cb0[6].x * r0.x + r2.x;
    float stage14_w = cb0[6].x * r0.y + r2.y;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // mul r0.xy, r0.xyxx, cb0[6].xxxx
    float stage15_x = r0.x * cb0[6].x;
    float stage15_y = r0.y * cb0[6].x;
    r0.x = stage15_x;
    r0.y = stage15_y;
    // add r0.zw, r0.zzzw, cb0[8].xxxy
    float stage16_z = r0.z + cb0[8].x;
    float stage16_w = r0.w + cb0[8].y;
    r0.z = stage16_z;
    r0.w = stage16_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.zwzz, t3.xyzw, s2, l(0.000000)
    float4 fetch17 = t3.SampleBias(s2, r0.zw, 0.0f);
    r2.x = fetch17.x;
    r2.y = fetch17.y;
    r2.z = fetch17.z;
    // mul r3.xyz, r1.xyzx, r2.xyzx
    float stage18_x = r1.x * r2.x;
    float stage18_y = r1.y * r2.y;
    float stage18_z = r1.z * r2.z;
    r3.x = stage18_x;
    r3.y = stage18_y;
    r3.z = stage18_z;
    // dp3 r0.z, r3.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.z = dot(r3.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r1.xyzx, r2.xyzx, r0.zzzz
    float stage19_x = -r1.x * r2.x + r0.z;
    float stage19_y = -r1.y * r2.y + r0.z;
    float stage19_z = -r1.z * r2.z + r0.z;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // mad r1.xyz, cb0[14].xxxx, r1.xyzx, r3.xyzx
    float stage20_x = cb0[14].x * r1.x + r3.x;
    float stage20_y = cb0[14].x * r1.y + r3.y;
    float stage20_z = cb0[14].x * r1.z + r3.z;
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.z = stage20_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage21_x = max(abs(r1.x), 1e-06f);
    float stage21_y = max(abs(r1.y), 1e-06f);
    float stage21_z = max(abs(r1.z), 1e-06f);
    r1.x = stage21_x;
    r1.y = stage21_y;
    r1.z = stage21_z;
    // log r1.xyz, r1.xyzx
    float stage22_x = log2(r1.x);
    float stage22_y = log2(r1.y);
    float stage22_z = log2(r1.z);
    r1.x = stage22_x;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // mul r1.xyz, r1.xyzx, cb0[14].yyyy
    float stage23_x = r1.x * cb0[14].y;
    float stage23_y = r1.y * cb0[14].y;
    float stage23_z = r1.z * cb0[14].y;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // exp r1.xyz, r1.xyzx
    float stage24_x = exp2(r1.x);
    float stage24_y = exp2(r1.y);
    float stage24_z = exp2(r1.z);
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // mul r1.xyz, r1.xyzx, cb0[14].zzzz
    float stage25_x = r1.x * cb0[14].z;
    float stage25_y = r1.y * cb0[14].z;
    float stage25_z = r1.z * cb0[14].z;
    r1.x = stage25_x;
    r1.y = stage25_y;
    r1.z = stage25_z;
    // mad r1.xyz, cb0[1].xyzx, r1.xyzx, cb0[3].xyzx
    float stage26_x = cb0[1].x * r1.x + cb0[3].x;
    float stage26_y = cb0[1].y * r1.y + cb0[3].y;
    float stage26_z = cb0[1].z * r1.z + cb0[3].z;
    r1.x = stage26_x;
    r1.y = stage26_y;
    r1.z = stage26_z;
    // add r1.xyz, r1.xyzx, cb0[2].xyzx
    float stage27_x = r1.x + cb0[2].x;
    float stage27_y = r1.y + cb0[2].y;
    float stage27_z = r1.z + cb0[2].z;
    r1.x = stage27_x;
    r1.y = stage27_y;
    r1.z = stage27_z;
    // mad o0.xyz, r1.xyzx, v5.wwww, v5.xyzx
    float stage28_x = r1.x * v5.w + v5.x;
    float stage28_y = r1.y * v5.w + v5.y;
    float stage28_z = r1.z * v5.w + v5.z;
    o0.x = stage28_x;
    o0.y = stage28_y;
    o0.z = stage28_z;
    // mad r0.zw, v4.xxxy, cb0[9].xxxy, l(0.000000, 0.000000, -0.500000, -0.500000)
    float stage29_z = v4.x * cb0[9].x + -0.5f;
    float stage29_w = v4.y * cb0[9].y + -0.5f;
    r0.z = stage29_z;
    r0.w = stage29_w;
    // dp2 r1.x, cb0[10].xyxx, r0.zwzz
    r1.x = dot(cb0[10].xy, r0.zw);
    // dp2 r1.y, cb0[11].xyxx, r0.zwzz
    r1.y = dot(cb0[11].xy, r0.zw);
    // add r0.zw, r1.xxxy, cb0[6].zzzy
    float stage30_z = r1.x + cb0[6].z;
    float stage30_w = r1.y + cb0[6].y;
    r0.z = stage30_z;
    r0.w = stage30_w;
    // add r0.zw, r0.zzzw, l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage31_z = r0.z + 0.5f;
    float stage31_w = r0.w + 0.5f;
    r0.z = stage31_z;
    r0.w = stage31_w;
    // mad r0.xy, cb0[15].wwww, r0.xyxx, r0.zwzz
    float stage32_x = cb0[15].w * r0.x + r0.z;
    float stage32_y = cb0[15].w * r0.y + r0.w;
    r0.x = stage32_x;
    r0.y = stage32_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s3, l(0.000000)
    float4 fetch33 = t1.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch33.x;
    // mad r0.y, v4.y, l(2.000000), l(-1.000000)
    r0.y = v4.y * 2.0f + -1.0f;
    // add r0.y, -|r0.y|, l(1.000000)
    r0.y = -(abs(r0.y)) + 1.0f;
    // log r0.z, |r0.y|
    r0.z = log2(abs(r0.y));
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[16].x
    r0.z = r0.z * cb0[16].x;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // dp3 r0.y, v6.xyzx, v6.xyzx
    r0.y = dot(v6.xyz, v6.xyz);
    // rsq r0.y, r0.y
    r0.y = rsqrt(r0.y);
    // mul r0.y, r0.y, v6.z
    r0.y = r0.y * v6.z;
    // log r0.z, |r0.y|
    r0.z = log2(abs(r0.y));
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[14].w
    r0.z = r0.z * cb0[14].w;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // max r0.x, |r0.x|, l(0.000001)
    r0.x = max(abs(r0.x), 1e-06f);
    // log r0.x, r0.x
    r0.x = log2(r0.x);
    // mul r0.x, r0.x, cb0[16].y
    r0.x = r0.x * cb0[16].y;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul_sat r0.x, r0.x, cb0[16].z
    r0.x = saturate(r0.x * cb0[16].z);
    // mul r0.x, r0.x, cb0[1].w
    r0.x = r0.x * cb0[1].w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage34_x = r0.x * v1.x;
    float stage34_y = r0.x * v1.y;
    float stage34_z = r0.x * v1.z;
    r0.x = stage34_x;
    r0.y = stage34_y;
    r0.z = stage34_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage35_x = r0.w * v0.z;
    float stage35_y = r0.w * v0.x;
    float stage35_z = r0.w * v0.y;
    r1.x = stage35_x;
    r1.y = stage35_y;
    r1.z = stage35_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage36_x = r0.x * r1.x;
    float stage36_y = r0.x * r1.y;
    float stage36_z = r0.x * r1.z;
    r0.x = stage36_x;
    r0.y = stage36_y;
    r0.z = stage36_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage37_x = r0.x / r0.w;
    float stage37_y = r0.y / r0.w;
    r0.x = stage37_x;
    r0.y = stage37_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage38_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage38_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage38_x;
    r1.y = stage38_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage39_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage39_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage39_x;
    r1.y = stage39_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage40_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage40_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage40_x;
    r1.y = stage40_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage41_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage41_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage41_x;
    r0.y = stage41_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage42_x = r0.x * 0.5f + 0.5f;
    float stage42_y = r0.y * 0.5f + 0.5f;
    o2.x = stage42_x;
    o2.y = stage42_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage43_z = 1.0f;
    float stage43_w = asfloat(0u);
    o2.z = stage43_z;
    o2.w = stage43_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage44_x = cb2[3].x;
    float stage44_y = cb2[3].y;
    float stage44_z = cb2[3].z;
    o3.x = stage44_x;
    o3.y = stage44_y;
    o3.z = stage44_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage45_x = asfloat(0u);
    float stage45_y = asfloat(0u);
    float stage45_z = asfloat(0u);
    float stage45_w = asfloat(0u);
    o4.x = stage45_x;
    o4.y = stage45_y;
    o4.z = stage45_z;
    o4.w = stage45_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage46_x = asfloat(0u);
    float stage46_y = asfloat(0u);
    float stage46_z = asfloat(0u);
    float stage46_w = asfloat(0u);
    o5.x = stage46_x;
    o5.y = stage46_y;
    o5.z = stage46_z;
    o5.w = stage46_w;

    Shade_Ue3_fx_k_me_makeflow_03_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
