// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_a_me_panning_02_ad_Constants0 : register(b0)
{
    float4 cb0[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_a_me_panning_02_ad_INPUT
{
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_a_me_panning_02_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_a_me_panning_02_ad_OUTPUT Shade_Ue3_fx_a_me_panning_02_ad(Shade_Ue3_fx_a_me_panning_02_ad_INPUT stage)
{
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;

    // mov r0.xz, v4.xxxx
    float stage1_x = v4.x;
    float stage1_z = v4.x;
    r0.x = stage1_x;
    r0.z = stage1_z;
    // mul r0.yw, v4.yyyy, cb0[3].yyyy
    float stage2_y = v4.y * cb0[3].y;
    float stage2_w = v4.y * cb0[3].y;
    r0.y = stage2_y;
    r0.w = stage2_w;
    // mad r0.xyzw, cb0[3].xxxx, l(0.100000, -0.500000, -0.100000, -0.750000), r0.xyzw
    float stage3_x = cb0[3].x * 0.1f + r0.x;
    float stage3_y = cb0[3].x * -0.5f + r0.y;
    float stage3_z = cb0[3].x * -0.1f + r0.z;
    float stage3_w = cb0[3].x * -0.75f + r0.w;
    r0.x = stage3_x;
    r0.y = stage3_y;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // mul r1.xyzw, v4.xyxy, l(3.000000, 1.000000, 4.000000, 1.000000)
    float stage4_x = v4.x * 3.0f;
    float stage4_y = v4.y * 1.0f;
    float stage4_z = v4.x * 4.0f;
    float stage4_w = v4.y * 1.0f;
    r1.x = stage4_x;
    r1.y = stage4_y;
    r1.z = stage4_z;
    r1.w = stage4_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t2.xyzw, s2, l(0.000000)
    float4 fetch5 = t2.SampleBias(s2, r1.xy, 0.0f);
    r1.x = fetch5.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.y, r1.zwzz, t2.yxzw, s2, l(0.000000)
    float4 fetch6 = t2.SampleBias(s2, r1.zw, 0.0f);
    r1.y = fetch6.x;
    // mad r0.zw, r1.yyyy, l(0.000000, 0.000000, 0.300000, 0.300000), r0.zzzw
    float stage7_z = r1.y * 0.3f + r0.z;
    float stage7_w = r1.y * 0.3f + r0.w;
    r0.z = stage7_z;
    r0.w = stage7_w;
    // mad r0.xy, r1.xxxx, l(0.600000, 0.600000, 0.000000, 0.000000), r0.xyxx
    float stage8_x = r1.x * 0.6f + r0.x;
    float stage8_y = r1.x * 0.6f + r0.y;
    r0.x = stage8_x;
    r0.y = stage8_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r0.xyxx, t3.xyzw, s3, l(0.000000)
    float4 fetch9 = t3.SampleBias(s3, r0.xy, 0.0f);
    r1.x = fetch9.x;
    r1.y = fetch9.y;
    r1.z = fetch9.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.zwzz, t4.xyzw, s4, l(0.000000)
    float4 fetch10 = t4.SampleBias(s4, r0.zw, 0.0f);
    r0.x = fetch10.x;
    r0.y = fetch10.y;
    r0.z = fetch10.z;
    // add r0.xyz, r0.xyzx, r1.xyzx
    float stage11_x = r0.x + r1.x;
    float stage11_y = r0.y + r1.y;
    float stage11_z = r0.z + r1.z;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mul r0.xyz, r0.xyzx, cb0[1].xyzx
    float stage12_x = r0.x * cb0[1].x;
    float stage12_y = r0.y * cb0[1].y;
    float stage12_z = r0.z * cb0[1].z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, v4.xyxx, t5.yzwx, s5, l(0.000000)
    float4 fetch13 = t5.SampleBias(s5, v4.xy, 0.0f);
    r0.w = fetch13.x;
    // mul r0.xyz, r0.xyzx, r0.wwww
    float stage14_x = r0.x * r0.w;
    float stage14_y = r0.y * r0.w;
    float stage14_z = r0.z * r0.w;
    r0.x = stage14_x;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // add r1.xy, v4.xyxx, v4.xyxx
    float stage15_x = v4.x + v4.x;
    float stage15_y = v4.y + v4.y;
    r1.x = stage15_x;
    r1.y = stage15_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t1.xzwy, s1, l(0.000000)
    float4 fetch16 = t1.SampleBias(s1, r1.xy, 0.0f);
    r0.w = fetch16.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, v4.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch17 = t0.SampleBias(s0, v4.xy, 0.0f);
    r1.x = fetch17.x;
    // add r0.w, r0.w, r1.x
    r0.w = r0.w + r1.x;
    // add r0.w, r0.w, cb0[3].z
    r0.w = r0.w + cb0[3].z;
    // log r1.x, |r0.w|
    r1.x = log2(abs(r0.w));
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, cb0[3].w
    r1.x = r1.x * cb0[3].w;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // movc r0.w, r0.w, l(0), r1.x
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.x;
    // mad r0.xyz, r0.wwww, r0.xyzx, cb0[2].xyzx
    float stage18_x = r0.w * r0.x + cb0[2].x;
    float stage18_y = r0.w * r0.y + cb0[2].y;
    float stage18_z = r0.w * r0.z + cb0[2].z;
    r0.x = stage18_x;
    r0.y = stage18_y;
    r0.z = stage18_z;
    // mul r0.xyz, r0.xyzx, v5.wwww
    float stage19_x = r0.x * v5.w;
    float stage19_y = r0.y * v5.w;
    float stage19_z = r0.z * v5.w;
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    // mul r0.w, cb0[0].x, cb0[1].w
    r0.w = cb0[0].x * cb0[1].w;
    // mul o0.xyz, r0.wwww, r0.xyzx
    float stage20_x = r0.w * r0.x;
    float stage20_y = r0.w * r0.y;
    float stage20_z = r0.w * r0.z;
    o0.x = stage20_x;
    o0.y = stage20_y;
    o0.z = stage20_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_a_me_panning_02_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
