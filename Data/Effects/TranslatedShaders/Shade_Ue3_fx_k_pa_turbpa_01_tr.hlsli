// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_pa_turbpa_01_tr_Constants0 : register(b0)
{
    float4 cb0[12];
};
cbuffer Shade_Ue3_fx_k_pa_turbpa_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_k_pa_turbpa_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_pa_turbpa_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_pa_turbpa_01_tr_OUTPUT Shade_Ue3_fx_k_pa_turbpa_01_tr(Shade_Ue3_fx_k_pa_turbpa_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // dp2 r1.x, cb0[3].xyxx, r0.xyxx
    r1.x = dot(cb0[3].xy, r0.xy);
    // dp2 r1.y, cb0[4].xyxx, r0.xyxx
    r1.y = dot(cb0[4].xy, r0.xy);
    // add r0.xy, r1.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage2_x = r1.x + 0.5f;
    float stage2_y = r1.y + 0.5f;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch3 = t1.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch3.x;
    // mul r0.x, r0.x, cb0[8].x
    r0.x = r0.x * cb0[8].x;
    // mul r1.xyzw, v2.xyxy, cb0[2].xyxy
    float stage4_x = v2.x * cb0[2].x;
    float stage4_y = v2.y * cb0[2].y;
    float stage4_z = v2.x * cb0[2].x;
    float stage4_w = v2.y * cb0[2].y;
    r1.x = stage4_x;
    r1.y = stage4_y;
    r1.z = stage4_z;
    r1.w = stage4_w;
    // mul r1.xyzw, r1.xyzw, l(0.500000, 0.500000, 0.300000, 0.300000)
    float stage5_x = r1.x * 0.5f;
    float stage5_y = r1.y * 0.5f;
    float stage5_z = r1.z * 0.3f;
    float stage5_w = r1.w * 0.3f;
    r1.x = stage5_x;
    r1.y = stage5_y;
    r1.z = stage5_z;
    r1.w = stage5_w;
    // mad r1.xyzw, v4.yyyy, l(0.100000, -0.100000, -0.070000, 0.100000), r1.xyzw
    float stage6_x = v4.y * 0.1f + r1.x;
    float stage6_y = v4.y * -0.1f + r1.y;
    float stage6_z = v4.y * -0.07f + r1.z;
    float stage6_w = v4.y * 0.1f + r1.w;
    r1.x = stage6_x;
    r1.y = stage6_y;
    r1.z = stage6_z;
    r1.w = stage6_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.zwzz, t0.xyzw, s0, l(0.000000)
    float4 fetch7 = t0.SampleBias(s0, r1.zw, 0.0f);
    r0.y = fetch7.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.xyxx, t0.yzxw, s0, l(0.000000)
    float4 fetch8 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.z = fetch8.x;
    // add r0.y, -r0.z, r0.y
    r0.y = -r0.z + r0.y;
    // mad r0.x, r0.x, r0.y, r0.z
    r0.x = r0.x * r0.y + r0.z;
    // mul r0.y, r0.x, v4.x
    r0.y = r0.x * v4.x;
    // mad r0.yz, v2.xxyx, cb0[7].xxyx, r0.yyyy
    float stage9_y = v2.x * cb0[7].x + r0.y;
    float stage9_z = v2.y * cb0[7].y + r0.y;
    r0.y = stage9_y;
    r0.z = stage9_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.yzw, r0.yzyy, t5.wxyz, s3, l(0.000000)
    float4 fetch10 = t5.SampleBias(s3, r0.yz, 0.0f);
    r0.y = fetch10.x;
    r0.z = fetch10.y;
    r0.w = fetch10.z;
    // max r0.yzw, |r0.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage11_y = max(abs(r0.y), 1e-06f);
    float stage11_z = max(abs(r0.z), 1e-06f);
    float stage11_w = max(abs(r0.w), 1e-06f);
    r0.y = stage11_y;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // log r0.yzw, r0.yyzw
    float stage12_y = log2(r0.y);
    float stage12_z = log2(r0.z);
    float stage12_w = log2(r0.w);
    r0.y = stage12_y;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mul r0.yzw, r0.yyzw, cb0[9].wwww
    float stage13_y = r0.y * cb0[9].w;
    float stage13_z = r0.z * cb0[9].w;
    float stage13_w = r0.w * cb0[9].w;
    r0.y = stage13_y;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // exp r0.yzw, r0.yyzw
    float stage14_y = exp2(r0.y);
    float stage14_z = exp2(r0.z);
    float stage14_w = exp2(r0.w);
    r0.y = stage14_y;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // mul r1.xy, v2.xyxx, cb0[5].xyxx
    float stage15_x = v2.x * cb0[5].x;
    float stage15_y = v2.y * cb0[5].y;
    r1.x = stage15_x;
    r1.y = stage15_y;
    // mad r1.xy, r0.xxxx, cb0[8].yyyy, r1.xyxx
    float stage16_x = r0.x * cb0[8].y + r1.x;
    float stage16_y = r0.x * cb0[8].y + r1.y;
    r1.x = stage16_x;
    r1.y = stage16_y;
    // mad r1.zw, r0.xxxx, cb0[11].zzzz, v2.xxxy
    float stage17_z = r0.x * cb0[11].z + v2.x;
    float stage17_w = r0.x * cb0[11].z + v2.y;
    r1.z = stage17_z;
    r1.w = stage17_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r1.zwzz, t3.xyzw, s5, l(0.000000)
    float4 fetch18 = t3.SampleBias(s5, r1.zw, 0.0f);
    r0.x = fetch18.x;
    // add r1.zw, r1.xxxy, cb0[6].xxxy
    float stage19_z = r1.x + cb0[6].x;
    float stage19_w = r1.y + cb0[6].y;
    r1.z = stage19_z;
    r1.w = stage19_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t2.xyzw, s4, l(0.000000)
    float4 fetch20 = t2.SampleBias(s4, r1.xy, 0.0f);
    r1.x = fetch20.x;
    // max r1.x, |r1.x|, l(0.000001)
    r1.x = max(abs(r1.x), 1e-06f);
    // log r1.x, r1.x
    r1.x = log2(r1.x);
    // mul r1.x, r1.x, cb0[11].x
    r1.x = r1.x * cb0[11].x;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r1.x, r1.x, cb0[11].y
    r1.x = r1.x * cb0[11].y;
    // mul_sat r0.x, r0.x, r1.x
    r0.x = saturate(r0.x * r1.x);
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.zwzz, t4.xyzw, s2, l(0.000000)
    float4 fetch21 = t4.SampleBias(s2, r1.zw, 0.0f);
    r1.x = fetch21.x;
    r1.y = fetch21.y;
    r1.z = fetch21.z;
    // mad r0.xyz, cb0[10].xxxx, r0.yzwy, r1.xyzx
    float stage22_x = cb0[10].x * r0.y + r1.x;
    float stage22_y = cb0[10].x * r0.z + r1.y;
    float stage22_z = cb0[10].x * r0.w + r1.z;
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xyzx, r0.wwww
    float stage23_x = -r0.x + r0.w;
    float stage23_y = -r0.y + r0.w;
    float stage23_z = -r0.z + r0.w;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // mad r0.xyz, cb0[10].yyyy, r1.xyzx, r0.xyzx
    float stage24_x = cb0[10].y * r1.x + r0.x;
    float stage24_y = cb0[10].y * r1.y + r0.y;
    float stage24_z = cb0[10].y * r1.z + r0.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage25_x = max(abs(r0.x), 1e-06f);
    float stage25_y = max(abs(r0.y), 1e-06f);
    float stage25_z = max(abs(r0.z), 1e-06f);
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // log r0.xyz, r0.xyzx
    float stage26_x = log2(r0.x);
    float stage26_y = log2(r0.y);
    float stage26_z = log2(r0.z);
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // mul r0.xyz, r0.xyzx, cb0[10].zzzz
    float stage27_x = r0.x * cb0[10].z;
    float stage27_y = r0.y * cb0[10].z;
    float stage27_z = r0.z * cb0[10].z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // exp r0.xyz, r0.xyzx
    float stage28_x = exp2(r0.x);
    float stage28_y = exp2(r0.y);
    float stage28_z = exp2(r0.z);
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // mul r0.xyz, r0.xyzx, cb0[10].wwww
    float stage29_x = r0.x * cb0[10].w;
    float stage29_y = r0.y * cb0[10].w;
    float stage29_z = r0.z * cb0[10].w;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage30_x = r0.x * v3.x + cb0[1].x;
    float stage30_y = r0.y * v3.y + cb0[1].y;
    float stage30_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage30_x;
    r0.y = stage30_y;
    r0.z = stage30_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage31_x = r0.x * v5.w + v5.x;
    float stage31_y = r0.y * v5.w + v5.y;
    float stage31_z = r0.z * v5.w + v5.z;
    o0.x = stage31_x;
    o0.y = stage31_y;
    o0.z = stage31_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage32_x = r0.x * v1.x;
    float stage32_y = r0.x * v1.y;
    float stage32_z = r0.x * v1.z;
    r0.x = stage32_x;
    r0.y = stage32_y;
    r0.z = stage32_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage33_x = r0.w * v0.z;
    float stage33_y = r0.w * v0.x;
    float stage33_z = r0.w * v0.y;
    r1.x = stage33_x;
    r1.y = stage33_y;
    r1.z = stage33_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage34_x = r0.x * r1.x;
    float stage34_y = r0.x * r1.y;
    float stage34_z = r0.x * r1.z;
    r0.x = stage34_x;
    r0.y = stage34_y;
    r0.z = stage34_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage35_x = r0.x / r0.w;
    float stage35_y = r0.y / r0.w;
    r0.x = stage35_x;
    r0.y = stage35_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage36_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage36_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage36_x;
    r1.y = stage36_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage37_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage37_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage37_x;
    r1.y = stage37_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage38_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage38_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage38_x;
    r1.y = stage38_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage39_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage39_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage39_x;
    r0.y = stage39_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage40_x = r0.x * 0.5f + 0.5f;
    float stage40_y = r0.y * 0.5f + 0.5f;
    o2.x = stage40_x;
    o2.y = stage40_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage41_z = 1.0f;
    float stage41_w = asfloat(0u);
    o2.z = stage41_z;
    o2.w = stage41_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage42_x = cb2[3].x;
    float stage42_y = cb2[3].y;
    float stage42_z = cb2[3].z;
    o3.x = stage42_x;
    o3.y = stage42_y;
    o3.z = stage42_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage43_x = asfloat(0u);
    float stage43_y = asfloat(0u);
    float stage43_z = asfloat(0u);
    float stage43_w = asfloat(0u);
    o4.x = stage43_x;
    o4.y = stage43_y;
    o4.z = stage43_z;
    o4.w = stage43_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage44_x = asfloat(0u);
    float stage44_y = asfloat(0u);
    float stage44_z = asfloat(0u);
    float stage44_w = asfloat(0u);
    o5.x = stage44_x;
    o5.y = stage44_y;
    o5.z = stage44_z;
    o5.w = stage44_w;

    Shade_Ue3_fx_k_pa_turbpa_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
