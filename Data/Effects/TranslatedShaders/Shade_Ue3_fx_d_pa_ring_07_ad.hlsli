// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_ring_07_ad_Constants0 : register(b0)
{
    float4 cb0[12];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_d_pa_ring_07_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_d_pa_ring_07_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_d_pa_ring_07_ad_OUTPUT Shade_Ue3_fx_d_pa_ring_07_ad(Shade_Ue3_fx_d_pa_ring_07_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;

    // add r0.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // max r0.z, |r0.y|, |r0.x|
    r0.z = max(abs(r0.y), abs(r0.x));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r0.y|, |r0.x|
    r0.w = min(abs(r0.y), abs(r0.x));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.x, r0.w, l(0.020835), l(-0.085133)
    r1.x = r0.w * 0.020835f + -0.085133f;
    // mad r1.x, r0.w, r1.x, l(0.180141)
    r1.x = r0.w * r1.x + 0.180141f;
    // mad r1.x, r0.w, r1.x, l(-0.330299)
    r1.x = r0.w * r1.x + -0.330299f;
    // mad r0.w, r0.w, r1.x, l(0.999866)
    r0.w = r0.w * r1.x + 0.999866f;
    // mul r1.x, r0.w, r0.z
    r1.x = r0.w * r0.z;
    // mad r1.x, r1.x, l(-2.000000), l(1.570796)
    r1.x = r1.x * -2.0f + 1.570796f;
    // lt r1.y, |r0.y|, |r0.x|
    r1.y = asfloat((abs(r0.y) < abs(r0.x)) ? 0xffffffffu : 0u);
    // and r1.x, r1.y, r1.x
    r1.x = asfloat(asuint(r1.y) & asuint(r1.x));
    // mad r0.z, r0.z, r0.w, r1.x
    r0.z = r0.z * r0.w + r1.x;
    // lt r0.w, r0.y, -r0.y
    r0.w = asfloat((r0.y < -r0.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r0.y, r0.x
    r0.w = min(r0.y, r0.x);
    // lt r0.w, r0.w, -r0.w
    r0.w = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // max r1.x, r0.y, r0.x
    r1.x = max(r0.y, r0.x);
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // add r0.x, r0.x, r0.x
    r0.x = r0.x + r0.x;
    // min r0.x, r0.x, l(1.000000)
    r0.x = min(r0.x, 1.0f);
    // ge r0.y, r1.x, -r1.x
    r0.y = asfloat((r1.x >= -r1.x) ? 0xffffffffu : 0u);
    // and r0.y, r0.y, r0.w
    r0.y = asfloat(asuint(r0.y) & asuint(r0.w));
    // movc r0.y, r0.y, -r0.z, r0.z
    r0.y = asuint(r0.y) != 0u ? -r0.z : r0.z;
    // mad r0.y, r0.y, l(0.159155), l(0.500000)
    r0.y = r0.y * 0.159155f + 0.5f;
    // mad r1.x, cb0[10].z, r0.y, cb0[4].x
    r1.x = cb0[10].z * r0.y + cb0[4].x;
    // mad r2.x, cb0[6].z, r0.y, cb0[2].x
    r2.x = cb0[6].z * r0.y + cb0[2].x;
    // mul r0.y, v2.x, cb0[7].w
    r0.y = v2.x * cb0[7].w;
    // mad r3.x, cb0[5].y, cb0[7].z, r0.y
    r3.x = cb0[5].y * cb0[7].z + r0.y;
    // mul r0.y, v2.y, cb0[8].x
    r0.y = v2.y * cb0[8].x;
    // mad r3.y, cb0[5].y, cb0[8].y, r0.y
    r3.y = cb0[5].y * cb0[8].y + r0.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r3.xyxx, t0.yxzw, s1, l(0.000000)
    float4 fetch2 = t0.SampleBias(s1, r3.xy, 0.0f);
    r0.y = fetch2.x;
    // log r0.z, r0.x
    r0.z = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, v4.z
    r0.z = r0.z * v4.z;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // movc r0.x, r0.x, l(0), r0.z
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.z;
    // add r0.z, v4.x, l(-1.000000)
    r0.z = v4.x + -1.0f;
    // mad r1.y, cb0[11].y, r0.x, r0.z
    r1.y = cb0[11].y * r0.x + r0.z;
    // mad r2.y, cb0[7].y, r0.x, r0.z
    r2.y = cb0[7].y * r0.x + r0.z;
    // mad r0.xz, r0.yyyy, cb0[8].wwww, r2.xxyx
    float stage3_x = r0.y * cb0[8].w + r2.x;
    float stage3_z = r0.y * cb0[8].w + r2.y;
    r0.x = stage3_x;
    r0.z = stage3_z;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.xzw, r0.xzxx, t3.xwyz, s0, cb0[5].x
    float4 fetch4 = t3.SampleLevel(s0, r0.xz, cb0[5].x);
    r0.x = fetch4.x;
    r0.z = fetch4.y;
    r0.w = fetch4.z;
    // mad r1.xy, r0.yyyy, cb0[8].wwww, r1.xyxx
    float stage5_x = r0.y * cb0[8].w + r1.x;
    float stage5_y = r0.y * cb0[8].w + r1.y;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // mad r1.zw, r0.yyyy, cb0[8].wwww, v2.xxxy
    float stage6_z = r0.y * cb0[8].w + v2.x;
    float stage6_w = r0.y * cb0[8].w + v2.y;
    r1.z = stage6_z;
    r1.w = stage6_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.zwzz, t2.yxzw, s3, l(0.000000)
    float4 fetch7 = t2.SampleBias(s3, r1.zw, 0.0f);
    r0.y = fetch7.x;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t1.xyzw, s2, cb0[5].x
    float4 fetch8 = t1.SampleLevel(s2, r1.xy, cb0[5].x);
    r1.x = fetch8.x;
    // mul_sat r1.x, r1.x, cb0[11].z
    r1.x = saturate(r1.x * cb0[11].z);
    // mov_sat r1.y, v4.y
    r1.y = saturate(v4.y);
    // add r1.y, -r1.y, l(1.000000)
    r1.y = -r1.y + 1.0f;
    // add r1.x, -r1.y, r1.x
    r1.x = -r1.y + r1.x;
    // max r1.x, r1.x, l(0.000000)
    r1.x = max(r1.x, 0.0f);
    // mul_sat r0.y, r0.y, r1.x
    r0.y = saturate(r0.y * r1.x);
    // log r1.x, r0.y
    r1.x = log2(r0.y);
    // lt r0.y, r0.y, l(0.000001)
    r0.y = asfloat((r0.y < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, cb0[11].w
    r1.x = r1.x * cb0[11].w;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul_sat r1.x, r1.x, v3.w
    r1.x = saturate(r1.x * v3.w);
    // mul r1.x, r1.x, cb0[0].x
    r1.x = r1.x * cb0[0].x;
    // movc r0.y, r0.y, l(0), r1.x
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r1.x;
    // dp3 r1.x, r0.xzwx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.xzw, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xzwx, r1.xxxx
    float stage9_x = -r0.x + r1.x;
    float stage9_y = -r0.z + r1.x;
    float stage9_z = -r0.w + r1.x;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    // mad r0.xzw, cb0[9].xxxx, r1.xxyz, r0.xxzw
    float stage10_x = cb0[9].x * r1.x + r0.x;
    float stage10_z = cb0[9].x * r1.y + r0.z;
    float stage10_w = cb0[9].x * r1.z + r0.w;
    r0.x = stage10_x;
    r0.z = stage10_z;
    r0.w = stage10_w;
    // max r0.xzw, |r0.xxzw|, l(0.000001, 0.000000, 0.000001, 0.000001)
    float stage11_x = max(abs(r0.x), 1e-06f);
    float stage11_z = max(abs(r0.z), 1e-06f);
    float stage11_w = max(abs(r0.w), 1e-06f);
    r0.x = stage11_x;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // log r0.xzw, r0.xxzw
    float stage12_x = log2(r0.x);
    float stage12_z = log2(r0.z);
    float stage12_w = log2(r0.w);
    r0.x = stage12_x;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mul r0.xzw, r0.xxzw, cb0[9].yyyy
    float stage13_x = r0.x * cb0[9].y;
    float stage13_z = r0.z * cb0[9].y;
    float stage13_w = r0.w * cb0[9].y;
    r0.x = stage13_x;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // exp r0.xzw, r0.xxzw
    float stage14_x = exp2(r0.x);
    float stage14_z = exp2(r0.z);
    float stage14_w = exp2(r0.w);
    r0.x = stage14_x;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // mul r1.xyz, cb0[3].xyzx, cb0[3].wwww
    float stage15_x = cb0[3].x * cb0[3].w;
    float stage15_y = cb0[3].y * cb0[3].w;
    float stage15_z = cb0[3].z * cb0[3].w;
    r1.x = stage15_x;
    r1.y = stage15_y;
    r1.z = stage15_z;
    // mul r0.xzw, r0.xxzw, r1.xxyz
    float stage16_x = r0.x * r1.x;
    float stage16_z = r0.z * r1.y;
    float stage16_w = r0.w * r1.z;
    r0.x = stage16_x;
    r0.z = stage16_z;
    r0.w = stage16_w;
    // mad r0.xzw, r0.xxzw, v3.xxyz, cb0[1].xxyz
    float stage17_x = r0.x * v3.x + cb0[1].x;
    float stage17_z = r0.z * v3.y + cb0[1].y;
    float stage17_w = r0.w * v3.z + cb0[1].z;
    r0.x = stage17_x;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // mul r0.xzw, r0.xxzw, v5.wwww
    float stage18_x = r0.x * v5.w;
    float stage18_z = r0.z * v5.w;
    float stage18_w = r0.w * v5.w;
    r0.x = stage18_x;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mul o0.xyz, r0.yyyy, r0.xzwx
    float stage19_x = r0.y * r0.x;
    float stage19_y = r0.y * r0.z;
    float stage19_z = r0.y * r0.w;
    o0.x = stage19_x;
    o0.y = stage19_y;
    o0.z = stage19_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_d_pa_ring_07_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
