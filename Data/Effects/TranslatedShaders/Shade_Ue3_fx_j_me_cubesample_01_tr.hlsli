// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_j_me_cubesample_01_tr_Constants0 : register(b0)
{
    float4 cb0[6];
};
cbuffer Shade_Ue3_fx_j_me_cubesample_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_j_me_cubesample_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
    float4 v7;
};

struct Shade_Ue3_fx_j_me_cubesample_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_j_me_cubesample_01_tr_OUTPUT Shade_Ue3_fx_j_me_cubesample_01_tr(Shade_Ue3_fx_j_me_cubesample_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // div r0.xy, v7.xyxx, v7.wwww
    float stage1_x = v7.x / v7.w;
    float stage1_y = v7.y / v7.w;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, r0.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage2_x = r0.x * cb2[0].x + cb2[0].w;
    float stage2_y = r0.y * cb2[0].y + cb2[0].z;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // mul r0.z, cb0[4].x, l(0.100000)
    r0.z = cb0[4].x * 0.1f;
    // dp3 r0.w, v6.xyzx, v6.xyzx
    r0.w = dot(v6.xyz, v6.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v6.xyzx
    float stage3_x = r0.w * v6.x;
    float stage3_y = r0.w * v6.y;
    float stage3_z = r0.w * v6.z;
    r1.x = stage3_x;
    r1.y = stage3_y;
    r1.z = stage3_z;
    // add r0.w, -|r1.z|, l(1.000000)
    r0.w = -(abs(r1.z)) + 1.0f;
    // mul r2.xy, r0.wwww, r1.yxyy
    float stage4_x = r0.w * r1.y;
    float stage4_y = r0.w * r1.x;
    r2.x = stage4_x;
    r2.y = stage4_y;
    // mad r0.xy, r0.zzzz, r2.xyxx, r0.xyxx
    float stage5_x = r0.z * r2.x + r0.x;
    float stage5_y = r0.z * r2.y + r0.y;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // sample_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t1.xyzw, s0
    float4 fetch6 = t1.Sample(s0, r0.xy);
    r0.x = fetch6.x;
    r0.y = fetch6.y;
    r0.z = fetch6.z;
    // max r2.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage7_x = max(abs(r0.x), 1e-06f);
    float stage7_y = max(abs(r0.y), 1e-06f);
    float stage7_z = max(abs(r0.z), 1e-06f);
    r2.x = stage7_x;
    r2.y = stage7_y;
    r2.z = stage7_z;
    // mul r3.xyz, r2.xyzx, r2.xyzx
    float stage8_x = r2.x * r2.x;
    float stage8_y = r2.y * r2.y;
    float stage8_z = r2.z * r2.z;
    r3.x = stage8_x;
    r3.y = stage8_y;
    r3.z = stage8_z;
    // mul r3.xyz, r3.xyzx, r3.xyzx
    float stage9_x = r3.x * r3.x;
    float stage9_y = r3.y * r3.y;
    float stage9_z = r3.z * r3.z;
    r3.x = stage9_x;
    r3.y = stage9_y;
    r3.z = stage9_z;
    // mul r2.xyz, r2.xyzx, r3.xyzx
    float stage10_x = r2.x * r3.x;
    float stage10_y = r2.y * r3.y;
    float stage10_z = r2.z * r3.z;
    r2.x = stage10_x;
    r2.y = stage10_y;
    r2.z = stage10_z;
    // mad r0.xyz, -r2.xyzx, l(5.000000, 5.000000, 5.000000, 0.000000), r0.xyzx
    float stage11_x = -r2.x * 5.0f + r0.x;
    float stage11_y = -r2.y * 5.0f + r0.y;
    float stage11_z = -r2.z * 5.0f + r0.z;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mul r2.xyz, r2.xyzx, l(5.000000, 5.000000, 5.000000, 0.000000)
    float stage12_x = r2.x * 5.0f;
    float stage12_y = r2.y * 5.0f;
    float stage12_z = r2.z * 5.0f;
    r2.x = stage12_x;
    r2.y = stage12_y;
    r2.z = stage12_z;
    // mad r0.xyz, |r1.zzzz|, r0.xyzx, r2.xyzx
    float stage13_x = abs(r1.z) * r0.x + r2.x;
    float stage13_y = abs(r1.z) * r0.y + r2.y;
    float stage13_z = abs(r1.z) * r0.z + r2.z;
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // mul r1.xy, r1.xyxx, l(3.000000, 3.000000, 0.000000, 0.000000)
    float stage14_x = r1.x * 3.0f;
    float stage14_y = r1.y * 3.0f;
    r1.x = stage14_x;
    r1.y = stage14_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t0.xyzw, s1, l(0.000000)
    float4 fetch15 = t0.SampleBias(s1, r1.xy, 0.0f);
    r1.x = fetch15.x;
    r1.y = fetch15.y;
    r1.z = fetch15.z;
    // log r1.w, |r0.w|
    r1.w = log2(abs(r0.w));
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.w, r1.w, l(7.000000)
    r1.w = r1.w * 7.0f;
    // exp r1.w, r1.w
    r1.w = exp2(r1.w);
    // movc r0.w, r0.w, l(0), r1.w
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.w;
    // mul r1.xyz, r1.xyzx, r0.wwww
    float stage16_x = r1.x * r0.w;
    float stage16_y = r1.y * r0.w;
    float stage16_z = r1.z * r0.w;
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mad r0.xyz, cb0[5].xxxx, r1.xyzx, r0.xyzx
    float stage17_x = cb0[5].x * r1.x + r0.x;
    float stage17_y = cb0[5].x * r1.y + r0.y;
    float stage17_z = cb0[5].x * r1.z + r0.z;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // add r1.xy, v4.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage18_x = v4.x + -0.5f;
    float stage18_y = v4.y + -0.5f;
    r1.x = stage18_x;
    r1.y = stage18_y;
    // add r1.xy, r1.xyxx, r1.xyxx
    float stage19_x = r1.x + r1.x;
    float stage19_y = r1.y + r1.y;
    r1.x = stage19_x;
    r1.y = stage19_y;
    // log r1.zw, |r1.xxxy|
    float stage20_z = log2(abs(r1.x));
    float stage20_w = log2(abs(r1.y));
    r1.z = stage20_z;
    r1.w = stage20_w;
    // lt r1.xy, |r1.xyxx|, l(0.000001, 0.000001, 0.000000, 0.000000)
    float stage21_x = asfloat((abs(r1.x) < 1e-06f) ? 0xffffffffu : 0u);
    float stage21_y = asfloat((abs(r1.y) < 1e-06f) ? 0xffffffffu : 0u);
    r1.x = stage21_x;
    r1.y = stage21_y;
    // mul r1.zw, r1.zzzw, cb0[5].yyyy
    float stage22_z = r1.z * cb0[5].y;
    float stage22_w = r1.w * cb0[5].y;
    r1.z = stage22_z;
    r1.w = stage22_w;
    // exp r1.zw, r1.zzzw
    float stage23_z = exp2(r1.z);
    float stage23_w = exp2(r1.w);
    r1.z = stage23_z;
    r1.w = stage23_w;
    // movc r1.xy, r1.xyxx, l(0,0,0,0), r1.zwzz
    float stage24_x = asuint(r1.x) != 0u ? asfloat(0u) : r1.z;
    float stage24_y = asuint(r1.y) != 0u ? asfloat(0u) : r1.w;
    r1.x = stage24_x;
    r1.y = stage24_y;
    // add r0.w, r1.y, r1.x
    r0.w = r1.y + r1.x;
    // min r0.w, r0.w, l(1.000000)
    r0.w = min(r0.w, 1.0f);
    // mul r0.w, r0.w, cb0[5].z
    r0.w = r0.w * cb0[5].z;
    // mul r1.xyz, r0.wwww, cb0[1].xyzx
    float stage25_x = r0.w * cb0[1].x;
    float stage25_y = r0.w * cb0[1].y;
    float stage25_z = r0.w * cb0[1].z;
    r1.x = stage25_x;
    r1.y = stage25_y;
    r1.z = stage25_z;
    // mul r2.xyz, cb0[3].xyzx, cb0[3].wwww
    float stage26_x = cb0[3].x * cb0[3].w;
    float stage26_y = cb0[3].y * cb0[3].w;
    float stage26_z = cb0[3].z * cb0[3].w;
    r2.x = stage26_x;
    r2.y = stage26_y;
    r2.z = stage26_z;
    // mad r0.xyz, r2.xyzx, r0.xyzx, r1.xyzx
    float stage27_x = r2.x * r0.x + r1.x;
    float stage27_y = r2.y * r0.y + r1.y;
    float stage27_z = r2.z * r0.z + r1.z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // add r0.xyz, r0.xyzx, cb0[2].xyzx
    float stage28_x = r0.x + cb0[2].x;
    float stage28_y = r0.y + cb0[2].y;
    float stage28_z = r0.z + cb0[2].z;
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage29_x = r0.x * v5.w + v5.x;
    float stage29_y = r0.y * v5.w + v5.y;
    float stage29_z = r0.z * v5.w + v5.z;
    o0.x = stage29_x;
    o0.y = stage29_y;
    o0.z = stage29_z;
    // mov_sat r0.x, cb0[1].w
    r0.x = saturate(cb0[1].w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage30_x = r0.x * v1.x;
    float stage30_y = r0.x * v1.y;
    float stage30_z = r0.x * v1.z;
    r0.x = stage30_x;
    r0.y = stage30_y;
    r0.z = stage30_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage31_x = r0.w * v0.z;
    float stage31_y = r0.w * v0.x;
    float stage31_z = r0.w * v0.y;
    r1.x = stage31_x;
    r1.y = stage31_y;
    r1.z = stage31_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage32_x = r0.x * r1.x;
    float stage32_y = r0.x * r1.y;
    float stage32_z = r0.x * r1.z;
    r0.x = stage32_x;
    r0.y = stage32_y;
    r0.z = stage32_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage33_x = r0.x / r0.w;
    float stage33_y = r0.y / r0.w;
    r0.x = stage33_x;
    r0.y = stage33_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage34_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage34_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage34_x;
    r1.y = stage34_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage35_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage35_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage35_x;
    r1.y = stage35_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage36_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage36_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage36_x;
    r1.y = stage36_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage37_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage37_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage37_x;
    r0.y = stage37_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage38_x = r0.x * 0.5f + 0.5f;
    float stage38_y = r0.y * 0.5f + 0.5f;
    o2.x = stage38_x;
    o2.y = stage38_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage39_z = 1.0f;
    float stage39_w = asfloat(0u);
    o2.z = stage39_z;
    o2.w = stage39_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage40_x = cb2[3].x;
    float stage40_y = cb2[3].y;
    float stage40_z = cb2[3].z;
    o3.x = stage40_x;
    o3.y = stage40_y;
    o3.z = stage40_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage41_x = asfloat(0u);
    float stage41_y = asfloat(0u);
    float stage41_z = asfloat(0u);
    float stage41_w = asfloat(0u);
    o4.x = stage41_x;
    o4.y = stage41_y;
    o4.z = stage41_z;
    o4.w = stage41_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage42_x = asfloat(0u);
    float stage42_y = asfloat(0u);
    float stage42_z = asfloat(0u);
    float stage42_w = asfloat(0u);
    o5.x = stage42_x;
    o5.y = stage42_y;
    o5.z = stage42_z;
    o5.w = stage42_w;

    Shade_Ue3_fx_j_me_cubesample_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
