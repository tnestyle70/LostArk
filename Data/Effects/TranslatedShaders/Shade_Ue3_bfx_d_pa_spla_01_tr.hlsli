// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_bfx_d_pa_spla_01_tr_Constants0 : register(b0)
{
    float4 cb0[11];
};
cbuffer Shade_Ue3_bfx_d_pa_spla_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_bfx_d_pa_spla_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v5;
};

struct Shade_Ue3_bfx_d_pa_spla_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_bfx_d_pa_spla_01_tr_OUTPUT Shade_Ue3_bfx_d_pa_spla_01_tr(Shade_Ue3_bfx_d_pa_spla_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mad r0.xy, cb0[7].yyyy, v2.xyxx, cb0[3].xyxx
    float stage1_x = cb0[7].y * v2.x + cb0[3].x;
    float stage1_y = cb0[7].y * v2.y + cb0[3].y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s0, l(0.000000)
    float4 fetch2 = t2.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch2.x;
    // mad r0.yz, cb0[7].yyyy, v2.xxyx, cb0[4].xxyx
    float stage3_y = cb0[7].y * v2.x + cb0[4].x;
    float stage3_z = cb0[7].y * v2.y + cb0[4].y;
    r0.y = stage3_y;
    r0.z = stage3_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t3.yxzw, s1, l(0.000000)
    float4 fetch4 = t3.SampleBias(s1, r0.yz, 0.0f);
    r0.y = fetch4.x;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul r0.yzw, cb0[2].xxyz, cb0[2].wwww
    float stage5_y = cb0[2].x * cb0[2].w;
    float stage5_z = cb0[2].y * cb0[2].w;
    float stage5_w = cb0[2].z * cb0[2].w;
    r0.y = stage5_y;
    r0.z = stage5_z;
    r0.w = stage5_w;
    // mul r0.xyz, r0.yzwy, r0.xxxx
    float stage6_x = r0.y * r0.x;
    float stage6_y = r0.z * r0.x;
    float stage6_z = r0.w * r0.x;
    r0.x = stage6_x;
    r0.y = stage6_y;
    r0.z = stage6_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage7_x = max(abs(r0.x), 1e-06f);
    float stage7_y = max(abs(r0.y), 1e-06f);
    float stage7_z = max(abs(r0.z), 1e-06f);
    r0.x = stage7_x;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // log r0.xyz, r0.xyzx
    float stage8_x = log2(r0.x);
    float stage8_y = log2(r0.y);
    float stage8_z = log2(r0.z);
    r0.x = stage8_x;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // mul r0.xyz, r0.xyzx, cb0[8].zzzz
    float stage9_x = r0.x * cb0[8].z;
    float stage9_y = r0.y * cb0[8].z;
    float stage9_z = r0.z * cb0[8].z;
    r0.x = stage9_x;
    r0.y = stage9_y;
    r0.z = stage9_z;
    // exp r0.xyz, r0.xyzx
    float stage10_x = exp2(r0.x);
    float stage10_y = exp2(r0.y);
    float stage10_z = exp2(r0.z);
    r0.x = stage10_x;
    r0.y = stage10_y;
    r0.z = stage10_z;
    // mul r1.xyz, cb0[5].xyzx, cb0[5].wwww
    float stage11_x = cb0[5].x * cb0[5].w;
    float stage11_y = cb0[5].y * cb0[5].w;
    float stage11_z = cb0[5].z * cb0[5].w;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    // mad r0.xyz, cb0[8].wwww, r0.xyzx, r1.xyzx
    float stage12_x = cb0[8].w * r0.x + r1.x;
    float stage12_y = cb0[8].w * r0.y + r1.y;
    float stage12_z = cb0[8].w * r0.z + r1.z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage13_x = r0.x * v3.x + cb0[1].x;
    float stage13_y = r0.y * v3.y + cb0[1].y;
    float stage13_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage14_x = r0.x * v5.w + v5.x;
    float stage14_y = r0.y * v5.w + v5.y;
    float stage14_z = r0.z * v5.w + v5.z;
    o0.x = stage14_x;
    o0.y = stage14_y;
    o0.z = stage14_z;
    // mul r0.xy, v2.xyxx, cb0[9].yzyy
    float stage15_x = v2.x * cb0[9].y;
    float stage15_y = v2.y * cb0[9].z;
    r0.x = stage15_x;
    r0.y = stage15_y;
    // mad r0.xy, cb0[6].yyyy, cb0[9].xwxx, r0.xyxx
    float stage16_x = cb0[6].y * cb0[9].x + r0.x;
    float stage16_y = cb0[6].y * cb0[9].w + r0.y;
    r0.x = stage16_x;
    r0.y = stage16_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t0.xyzw, s2, l(0.000000)
    float4 fetch17 = t0.SampleBias(s2, r0.xy, 0.0f);
    r0.x = fetch17.x;
    // mad r0.xy, r0.xxxx, cb0[10].xxxx, v2.xyxx
    float stage18_x = r0.x * cb0[10].x + v2.x;
    float stage18_y = r0.x * cb0[10].x + v2.y;
    r0.x = stage18_x;
    r0.y = stage18_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.yxzw, s3, l(0.000000)
    float4 fetch19 = t1.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch19.y;
    // mov_sat r0.x, r0.x
    r0.x = saturate(r0.x);
    // log r0.y, r0.x
    r0.y = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[10].y
    r0.y = r0.y * cb0[10].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul_sat r0.y, r0.y, cb0[10].z
    r0.y = saturate(r0.y * cb0[10].z);
    // mul_sat r0.y, r0.y, v3.w
    r0.y = saturate(r0.y * v3.w);
    // mul r0.y, r0.y, cb0[0].x
    r0.y = r0.y * cb0[0].x;
    // movc o0.w, r0.x, l(0), r0.y
    o0.w = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage20_x = r0.x * v1.x;
    float stage20_y = r0.x * v1.y;
    float stage20_z = r0.x * v1.z;
    r0.x = stage20_x;
    r0.y = stage20_y;
    r0.z = stage20_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage21_x = r0.w * v0.z;
    float stage21_y = r0.w * v0.x;
    float stage21_z = r0.w * v0.y;
    r1.x = stage21_x;
    r1.y = stage21_y;
    r1.z = stage21_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage22_x = r0.x * r1.x;
    float stage22_y = r0.x * r1.y;
    float stage22_z = r0.x * r1.z;
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage23_x = r0.x / r0.w;
    float stage23_y = r0.y / r0.w;
    r0.x = stage23_x;
    r0.y = stage23_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage24_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage24_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage24_x;
    r1.y = stage24_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage25_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage25_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage25_x;
    r1.y = stage25_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage26_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage26_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage26_x;
    r1.y = stage26_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage27_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage27_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage27_x;
    r0.y = stage27_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage28_x = r0.x * 0.5f + 0.5f;
    float stage28_y = r0.y * 0.5f + 0.5f;
    o2.x = stage28_x;
    o2.y = stage28_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage29_z = 1.0f;
    float stage29_w = asfloat(0u);
    o2.z = stage29_z;
    o2.w = stage29_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage30_x = cb2[3].x;
    float stage30_y = cb2[3].y;
    float stage30_z = cb2[3].z;
    o3.x = stage30_x;
    o3.y = stage30_y;
    o3.z = stage30_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage31_x = asfloat(0u);
    float stage31_y = asfloat(0u);
    float stage31_z = asfloat(0u);
    float stage31_w = asfloat(0u);
    o4.x = stage31_x;
    o4.y = stage31_y;
    o4.z = stage31_z;
    o4.w = stage31_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage32_x = asfloat(0u);
    float stage32_y = asfloat(0u);
    float stage32_z = asfloat(0u);
    float stage32_w = asfloat(0u);
    o5.x = stage32_x;
    o5.y = stage32_y;
    o5.z = stage32_z;
    o5.w = stage32_w;

    Shade_Ue3_bfx_d_pa_spla_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
