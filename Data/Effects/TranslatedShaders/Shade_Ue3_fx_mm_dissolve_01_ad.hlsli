// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_mm_dissolve_01_ad_Constants0 : register(b0)
{
    float4 cb0[5];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_mm_dissolve_01_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_mm_dissolve_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_mm_dissolve_01_ad_OUTPUT Shade_Ue3_fx_mm_dissolve_01_ad(Shade_Ue3_fx_mm_dissolve_01_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;

    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, v2.xyxx, t1.xyzw, s0, l(0.000000)
    float4 fetch1 = t1.SampleBias(s0, v2.xy, 0.0f);
    r0.x = fetch1.x;
    // mul r0.x, r0.x, cb0[3].x
    r0.x = r0.x * cb0[3].x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[3].y
    r0.y = r0.y * cb0[3].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mad r0.y, v4.x, l(2.000000), l(-1.000000)
    r0.y = v4.x * 2.0f + -1.0f;
    // add r0.x, r0.x, r0.y
    r0.x = r0.x + r0.y;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[3].z
    r0.y = r0.y * cb0[3].z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // min r0.y, r0.y, l(1.000000)
    r0.y = min(r0.y, 1.0f);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mad r0.y, cb0[3].w, v4.z, l(-1.000000)
    r0.y = cb0[3].w * v4.z + -1.0f;
    // mul r0.y, r0.y, l(0.500000)
    r0.y = r0.y * 0.5f;
    // mul r0.z, v4.z, cb0[3].w
    r0.z = v4.z * cb0[3].w;
    // mad r0.yz, r0.zzzz, v2.xxyx, -r0.yyyy
    float stage2_y = r0.z * v2.x + -r0.y;
    float stage2_z = r0.z * v2.y + -r0.y;
    r0.y = stage2_y;
    r0.z = stage2_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyzw, r0.yzyy, t0.xyzw, s1, l(0.000000)
    float4 fetch3 = t0.SampleBias(s1, r0.yz, 0.0f);
    r1.x = fetch3.x;
    r1.y = fetch3.y;
    r1.z = fetch3.z;
    r1.w = fetch3.w;
    // log r0.y, |r1.w|
    r0.y = log2(abs(r1.w));
    // mul r0.y, r0.y, cb0[4].z
    r0.y = r0.y * cb0[4].z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, v3.w
    r0.y = r0.y * v3.w;
    // lt r0.z, |r1.w|, l(0.000001)
    r0.z = asfloat((abs(r1.w) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.y, r0.z, l(0), |r0.y|
    r0.y = asuint(r0.z) != 0u ? asfloat(0u) : abs(r0.y);
    // log r0.z, r0.y
    r0.z = log2(r0.y);
    // lt r0.y, r0.y, l(0.000001)
    r0.y = asfloat((r0.y < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, v4.y
    r0.z = r0.z * v4.y;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // mul r0.z, r0.x, r0.z
    r0.z = r0.x * r0.z;
    // ge r0.x, l(0.250000), r0.x
    r0.x = asfloat((0.25f >= r0.x) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[0].x
    r0.z = r0.z * cb0[0].x;
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r2.xyz, r1.xyzx, cb0[4].xxxx
    float stage4_x = r1.x * cb0[4].x;
    float stage4_y = r1.y * cb0[4].x;
    float stage4_z = r1.z * cb0[4].x;
    r2.x = stage4_x;
    r2.y = stage4_y;
    r2.z = stage4_z;
    // dp3 r0.z, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.z = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -cb0[4].xxxx, r1.xyzx, r0.zzzz
    float stage5_x = -cb0[4].x * r1.x + r0.z;
    float stage5_y = -cb0[4].x * r1.y + r0.z;
    float stage5_z = -cb0[4].x * r1.z + r0.z;
    r1.x = stage5_x;
    r1.y = stage5_y;
    r1.z = stage5_z;
    // mad r1.xyz, cb0[4].yyyy, r1.xyzx, r2.xyzx
    float stage6_x = cb0[4].y * r1.x + r2.x;
    float stage6_y = cb0[4].y * r1.y + r2.y;
    float stage6_z = cb0[4].y * r1.z + r2.z;
    r1.x = stage6_x;
    r1.y = stage6_y;
    r1.z = stage6_z;
    // mul r1.xyz, r1.xyzx, v3.xyzx
    float stage7_x = r1.x * v3.x;
    float stage7_y = r1.y * v3.y;
    float stage7_z = r1.z * v3.z;
    r1.x = stage7_x;
    r1.y = stage7_y;
    r1.z = stage7_z;
    // mul r2.xyz, cb0[2].xyzx, cb0[2].wwww
    float stage8_x = cb0[2].x * cb0[2].w;
    float stage8_y = cb0[2].y * cb0[2].w;
    float stage8_z = cb0[2].z * cb0[2].w;
    r2.x = stage8_x;
    r2.y = stage8_y;
    r2.z = stage8_z;
    // movc r0.xzw, r0.xxxx, r2.xxyz, r1.xxyz
    float stage9_x = asuint(r0.x) != 0u ? r2.x : r1.x;
    float stage9_z = asuint(r0.x) != 0u ? r2.y : r1.y;
    float stage9_w = asuint(r0.x) != 0u ? r2.z : r1.z;
    r0.x = stage9_x;
    r0.z = stage9_z;
    r0.w = stage9_w;
    // add r0.xzw, r0.xxzw, cb0[1].xxyz
    float stage10_x = r0.x + cb0[1].x;
    float stage10_z = r0.z + cb0[1].y;
    float stage10_w = r0.w + cb0[1].z;
    r0.x = stage10_x;
    r0.z = stage10_z;
    r0.w = stage10_w;
    // mul r0.xzw, r0.xxzw, v5.wwww
    float stage11_x = r0.x * v5.w;
    float stage11_z = r0.z * v5.w;
    float stage11_w = r0.w * v5.w;
    r0.x = stage11_x;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // mul o0.xyz, r0.yyyy, r0.xzwx
    float stage12_x = r0.y * r0.x;
    float stage12_y = r0.y * r0.z;
    float stage12_z = r0.y * r0.w;
    o0.x = stage12_x;
    o0.y = stage12_y;
    o0.z = stage12_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_mm_dissolve_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
