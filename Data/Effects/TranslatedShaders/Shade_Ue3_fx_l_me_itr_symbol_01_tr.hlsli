// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_l_me_itr_symbol_01_tr_Constants0 : register(b0)
{
    float4 cb0[17];
};
cbuffer Shade_Ue3_fx_l_me_itr_symbol_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_l_me_itr_symbol_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
};

struct Shade_Ue3_fx_l_me_itr_symbol_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_l_me_itr_symbol_01_tr_OUTPUT Shade_Ue3_fx_l_me_itr_symbol_01_tr(Shade_Ue3_fx_l_me_itr_symbol_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // dp3 r0.x, v6.xyzx, v6.xyzx
    r0.x = dot(v6.xyz, v6.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xy, r0.xxxx, v6.xyxx
    float stage1_x = r0.x * v6.x;
    float stage1_y = r0.x * v6.y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mul r1.xyzw, v4.xyxy, l(3.000000, 2.000000, 3.000000, 3.000000)
    float stage2_x = v4.x * 3.0f;
    float stage2_y = v4.y * 2.0f;
    float stage2_z = v4.x * 3.0f;
    float stage2_w = v4.y * 3.0f;
    r1.x = stage2_x;
    r1.y = stage2_y;
    r1.z = stage2_z;
    r1.w = stage2_w;
    // mad r0.zw, r0.xxxy, l(0.000000, 0.000000, 0.025000, 0.025000), r1.xxxy
    float stage3_z = r0.x * 0.025f + r1.x;
    float stage3_w = r0.y * 0.025f + r1.y;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // mad r1.xy, r0.xyxx, l(-0.075000, -0.075000, 0.000000, 0.000000), r1.zwzz
    float stage4_x = r0.x * -0.075f + r1.z;
    float stage4_y = r0.y * -0.075f + r1.w;
    r1.x = stage4_x;
    r1.y = stage4_y;
    // mul r0.xy, r0.xyxx, l(-0.225000, -0.225000, 0.000000, 0.000000)
    float stage5_x = r0.x * -0.225f;
    float stage5_y = r0.y * -0.225f;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // mad r0.xy, v4.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), r0.xyxx
    float stage6_x = v4.x * 2.0f + r0.x;
    float stage6_y = v4.y * 2.0f + r0.y;
    r0.x = stage6_x;
    r0.y = stage6_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.xyxx, t2.xyzw, s0, l(0.000000)
    float4 fetch7 = t2.SampleBias(s0, r0.xy, 0.0f);
    r2.x = fetch7.x;
    r2.y = fetch7.y;
    r2.z = fetch7.z;
    // add r0.xy, r1.xyxx, cb0[5].xyxx
    float stage8_x = r1.x + cb0[5].x;
    float stage8_y = r1.y + cb0[5].y;
    r0.x = stage8_x;
    r0.y = stage8_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r0.xyxx, t4.xyzw, s2, l(0.000000)
    float4 fetch9 = t4.SampleBias(s2, r0.xy, 0.0f);
    r1.x = fetch9.x;
    r1.y = fetch9.y;
    r1.z = fetch9.z;
    // add r0.xy, r0.zwzz, cb0[4].xyxx
    float stage10_x = r0.z + cb0[4].x;
    float stage10_y = r0.w + cb0[4].y;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t4.xyzw, s2, l(0.000000)
    float4 fetch11 = t4.SampleBias(s2, r0.xy, 0.0f);
    r0.x = fetch11.x;
    r0.y = fetch11.y;
    r0.z = fetch11.z;
    // mul r0.xyz, r1.xyzx, r0.xyzx
    float stage12_x = r1.x * r0.x;
    float stage12_y = r1.y * r0.y;
    float stage12_z = r1.z * r0.z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mul r0.xyz, r0.xyzx, cb0[6].xyzx
    float stage13_x = r0.x * cb0[6].x;
    float stage13_y = r0.y * cb0[6].y;
    float stage13_z = r0.z * cb0[6].z;
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // add r1.xy, v4.xyxx, cb0[3].xyxx
    float stage14_x = v4.x + cb0[3].x;
    float stage14_y = v4.y + cb0[3].y;
    r1.x = stage14_x;
    r1.y = stage14_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t3.xyzw, s1, l(0.000000)
    float4 fetch15 = t3.SampleBias(s1, r1.xy, 0.0f);
    r1.x = fetch15.x;
    r1.y = fetch15.y;
    r1.z = fetch15.z;
    // mul r1.xyz, r1.xyzx, l(0.400000, 0.400000, 0.400000, 0.000000)
    float stage16_x = r1.x * 0.4f;
    float stage16_y = r1.y * 0.4f;
    float stage16_z = r1.z * 0.4f;
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mad r1.xyz, r2.xyzx, l(0.050000, 0.050000, 0.050000, 0.000000), r1.xyzx
    float stage17_x = r2.x * 0.05f + r1.x;
    float stage17_y = r2.y * 0.05f + r1.y;
    float stage17_z = r2.z * 0.05f + r1.z;
    r1.x = stage17_x;
    r1.y = stage17_y;
    r1.z = stage17_z;
    // mul r2.xyz, r1.xyzx, l(5.000000, 5.000000, 5.000000, 0.000000)
    float stage18_x = r1.x * 5.0f;
    float stage18_y = r1.y * 5.0f;
    float stage18_z = r1.z * 5.0f;
    r2.x = stage18_x;
    r2.y = stage18_y;
    r2.z = stage18_z;
    // dp3 r0.w, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r1.xyzx, l(5.000000, 5.000000, 5.000000, 0.000000), r0.wwww
    float stage19_x = -r1.x * 5.0f + r0.w;
    float stage19_y = -r1.y * 5.0f + r0.w;
    float stage19_z = -r1.z * 5.0f + r0.w;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // mad r1.xyz, cb0[12].wwww, r1.xyzx, r2.xyzx
    float stage20_x = cb0[12].w * r1.x + r2.x;
    float stage20_y = cb0[12].w * r1.y + r2.y;
    float stage20_z = cb0[12].w * r1.z + r2.z;
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.z = stage20_z;
    // mad r0.xyz, r0.xyzx, l(100.000000, 100.000000, 100.000000, 0.000000), r1.xyzx
    float stage21_x = r0.x * 100.0f + r1.x;
    float stage21_y = r0.y * 100.0f + r1.y;
    float stage21_z = r0.z * 100.0f + r1.z;
    r0.x = stage21_x;
    r0.y = stage21_y;
    r0.z = stage21_z;
    // mad r0.xyz, r0.xyzx, cb0[1].xyzx, cb0[2].xyzx
    float stage22_x = r0.x * cb0[1].x + cb0[2].x;
    float stage22_y = r0.y * cb0[1].y + cb0[2].y;
    float stage22_z = r0.z * cb0[1].z + cb0[2].z;
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage23_x = r0.x * v5.w + v5.x;
    float stage23_y = r0.y * v5.w + v5.y;
    float stage23_z = r0.z * v5.w + v5.z;
    o0.x = stage23_x;
    o0.y = stage23_y;
    o0.z = stage23_z;
    // add r0.xy, v4.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage24_x = v4.x + -0.5f;
    float stage24_y = v4.y + -0.5f;
    r0.x = stage24_x;
    r0.y = stage24_y;
    // dp2 r1.x, cb0[7].xyxx, r0.xyxx
    r1.x = dot(cb0[7].xy, r0.xy);
    // dp2 r1.y, cb0[8].xyxx, r0.xyxx
    r1.y = dot(cb0[8].xy, r0.xy);
    // add r0.zw, r1.xxxy, l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage25_z = r1.x + 0.5f;
    float stage25_w = r1.y + 0.5f;
    r0.z = stage25_z;
    r0.w = stage25_w;
    // mul r0.zw, r0.zzzw, cb0[13].yyyz
    float stage26_z = r0.z * cb0[13].y;
    float stage26_w = r0.w * cb0[13].z;
    r0.z = stage26_z;
    r0.w = stage26_w;
    // mad r1.x, cb0[12].x, cb0[13].x, r0.z
    r1.x = cb0[12].x * cb0[13].x + r0.z;
    // mad r1.y, cb0[12].x, cb0[14].y, r0.w
    r1.y = cb0[12].x * cb0[14].y + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.xyxx, t0.yzxw, s3, l(0.000000)
    float4 fetch27 = t0.SampleBias(s3, r1.xy, 0.0f);
    r0.z = fetch27.x;
    // dp2 r1.x, cb0[9].xyxx, r0.xyxx
    r1.x = dot(cb0[9].xy, r0.xy);
    // dp2 r1.y, cb0[10].xyxx, r0.xyxx
    r1.y = dot(cb0[10].xy, r0.xy);
    // dp2 r0.x, r1.xyxx, r1.xyxx
    r0.x = dot(r1.xy, r1.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // mad_sat r0.x, -r0.x, cb0[15].w, l(1.000000)
    r0.x = saturate(-r0.x * cb0[15].w + 1.0f);
    // mad r0.x, r0.z, cb0[14].z, r0.x
    r0.x = r0.z * cb0[14].z + r0.x;
    // add r0.x, r0.x, l(0.100000)
    r0.x = r0.x + 0.1f;
    // max r0.y, cb0[11].x, l(-0.250000)
    r0.y = max(cb0[11].x, -0.25f);
    // min r0.y, r0.y, l(1.000000)
    r0.y = min(r0.y, 1.0f);
    // add r0.y, -r0.y, l(1.000000)
    r0.y = -r0.y + 1.0f;
    // add r0.x, -r0.y, r0.x
    r0.x = -r0.y + r0.x;
    // add_sat r0.x, r0.x, cb0[16].x
    r0.x = saturate(r0.x + cb0[16].x);
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, v4.xyxx, t1.xwyz, s4, l(0.000000)
    float4 fetch28 = t1.SampleBias(s4, v4.xy, 0.0f);
    r0.y = fetch28.w;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul r0.x, r0.x, cb0[1].w
    r0.x = r0.x * cb0[1].w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage29_x = r0.x * v1.x;
    float stage29_y = r0.x * v1.y;
    float stage29_z = r0.x * v1.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage30_x = r0.w * v0.z;
    float stage30_y = r0.w * v0.x;
    float stage30_z = r0.w * v0.y;
    r1.x = stage30_x;
    r1.y = stage30_y;
    r1.z = stage30_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage31_x = r0.x * r1.x;
    float stage31_y = r0.x * r1.y;
    float stage31_z = r0.x * r1.z;
    r0.x = stage31_x;
    r0.y = stage31_y;
    r0.z = stage31_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage32_x = r0.x / r0.w;
    float stage32_y = r0.y / r0.w;
    r0.x = stage32_x;
    r0.y = stage32_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage33_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage33_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage33_x;
    r1.y = stage33_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage34_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage34_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage34_x;
    r1.y = stage34_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage35_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage35_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage35_x;
    r1.y = stage35_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage36_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage36_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage36_x;
    r0.y = stage36_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage37_x = r0.x * 0.5f + 0.5f;
    float stage37_y = r0.y * 0.5f + 0.5f;
    o2.x = stage37_x;
    o2.y = stage37_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage38_z = 1.0f;
    float stage38_w = asfloat(0u);
    o2.z = stage38_z;
    o2.w = stage38_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage39_x = cb2[3].x;
    float stage39_y = cb2[3].y;
    float stage39_z = cb2[3].z;
    o3.x = stage39_x;
    o3.y = stage39_y;
    o3.z = stage39_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage40_x = asfloat(0u);
    float stage40_y = asfloat(0u);
    float stage40_z = asfloat(0u);
    float stage40_w = asfloat(0u);
    o4.x = stage40_x;
    o4.y = stage40_y;
    o4.z = stage40_z;
    o4.w = stage40_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage41_x = asfloat(0u);
    float stage41_y = asfloat(0u);
    float stage41_z = asfloat(0u);
    float stage41_w = asfloat(0u);
    o5.x = stage41_x;
    o5.y = stage41_y;
    o5.z = stage41_z;
    o5.w = stage41_w;

    Shade_Ue3_fx_l_me_itr_symbol_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
