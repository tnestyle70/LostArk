// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_bfx_d_pa_spla_05_tr_Constants0 : register(b0)
{
    float4 cb0[16];
};
cbuffer Shade_Ue3_bfx_d_pa_spla_05_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_bfx_d_pa_spla_05_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_bfx_d_pa_spla_05_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_bfx_d_pa_spla_05_tr_OUTPUT Shade_Ue3_bfx_d_pa_spla_05_tr(Shade_Ue3_bfx_d_pa_spla_05_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.xy, v2.xyxx, cb0[10].zwzz
    float stage1_x = v2.x * cb0[10].z;
    float stage1_y = v2.y * cb0[10].w;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r1.x, cb0[9].y, cb0[10].y, r0.x
    r1.x = cb0[9].y * cb0[10].y + r0.x;
    // mad r1.y, cb0[9].y, cb0[11].x, r0.y
    r1.y = cb0[9].y * cb0[11].x + r0.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r1.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch2 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.x = fetch2.x;
    // mul r0.y, v4.z, cb0[11].y
    r0.y = v4.z * cb0[11].y;
    // mad r0.xy, r0.xxxx, r0.yyyy, v2.xyxx
    float stage3_x = r0.x * r0.y + v2.x;
    float stage3_y = r0.x * r0.y + v2.y;
    r0.x = stage3_x;
    r0.y = stage3_y;
    // mul r0.z, r0.x, cb0[14].w
    r0.z = r0.x * cb0[14].w;
    // mad r1.x, cb0[9].y, cb0[14].z, r0.z
    r1.x = cb0[9].y * cb0[14].z + r0.z;
    // mul r0.z, r0.y, cb0[15].x
    r0.z = r0.y * cb0[15].x;
    // mad r1.y, cb0[9].y, cb0[15].y, r0.z
    r1.y = cb0[9].y * cb0[15].y + r0.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.xyxx, t2.yzxw, s3, l(0.000000)
    float4 fetch4 = t2.SampleBias(s3, r1.xy, 0.0f);
    r0.z = fetch4.x;
    // mov_sat r0.w, v4.y
    r0.w = saturate(v4.y);
    // add r0.w, -r0.w, l(1.000000)
    r0.w = -r0.w + 1.0f;
    // add r1.xy, r0.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage5_x = r0.x + -0.5f;
    float stage5_y = r0.y + -0.5f;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // dp2 r2.x, cb0[6].xyxx, r1.xyxx
    r2.x = dot(cb0[6].xy, r1.xy);
    // dp2 r2.y, cb0[7].xyxx, r1.xyxx
    r2.y = dot(cb0[7].xy, r1.xy);
    // add r1.xy, r2.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage6_x = r2.x + 0.5f;
    float stage6_y = r2.y + 0.5f;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // mul r1.xy, r1.xyxx, cb0[8].xyxx
    float stage7_x = r1.x * cb0[8].x;
    float stage7_y = r1.y * cb0[8].y;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t1.xyzw, s2, l(0.000000)
    float4 fetch8 = t1.SampleBias(s2, r1.xy, 0.0f);
    r1.x = fetch8.x;
    // mad r0.z, r1.x, r0.z, -r0.w
    r0.z = r1.x * r0.z + -r0.w;
    // add r0.w, -r1.x, cb0[14].y
    r0.w = -r1.x + cb0[14].y;
    // add_sat r0.w, r0.w, l(1.000000)
    r0.w = saturate(r0.w + 1.0f);
    // mul_sat r0.z, r0.z, cb0[15].z
    r0.z = saturate(r0.z * cb0[15].z);
    // log r1.x, r0.z
    r1.x = log2(r0.z);
    // lt r0.z, r0.z, l(0.000001)
    r0.z = asfloat((r0.z < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, cb0[15].w
    r1.x = r1.x * cb0[15].w;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r0.w, r0.w, r1.x
    r0.w = r0.w * r1.x;
    // mul_sat r0.w, r0.w, v3.w
    r0.w = saturate(r0.w * v3.w);
    // mul r0.w, r0.w, cb0[0].x
    r0.w = r0.w * cb0[0].x;
    // movc o0.w, r0.z, l(0), r0.w
    o0.w = asuint(r0.z) != 0u ? asfloat(0u) : r0.w;
    // mad r0.zw, cb0[11].zzzz, r0.xxxy, cb0[2].xxxy
    float stage9_z = cb0[11].z * r0.x + cb0[2].x;
    float stage9_w = cb0[11].z * r0.y + cb0[2].y;
    r0.z = stage9_z;
    r0.w = stage9_w;
    // add r0.xy, r0.xyxx, cb0[3].xyxx
    float stage10_x = r0.x + cb0[3].x;
    float stage10_y = r0.y + cb0[3].y;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t3.xyzw, s1, l(0.000000)
    float4 fetch11 = t3.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch11.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.zwzz, t3.yxzw, s1, l(0.000000)
    float4 fetch12 = t3.SampleBias(s1, r0.zw, 0.0f);
    r0.y = fetch12.x;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul r0.x, r0.x, cb0[12].w
    r0.x = r0.x * cb0[12].w;
    // lt r0.y, |r0.x|, l(0.000001)
    r0.y = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // log r0.x, |r0.x|
    r0.x = log2(abs(r0.x));
    // mul r0.x, r0.x, cb0[13].x
    r0.x = r0.x * cb0[13].x;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul r0.xzw, r0.xxxx, cb0[4].xxyz
    float stage13_x = r0.x * cb0[4].x;
    float stage13_z = r0.x * cb0[4].y;
    float stage13_w = r0.x * cb0[4].z;
    r0.x = stage13_x;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // movc r0.xyz, r0.yyyy, l(0,0,0,0), r0.xzwx
    float stage14_x = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    float stage14_y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    float stage14_z = asuint(r0.y) != 0u ? asfloat(0u) : r0.w;
    r0.x = stage14_x;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // add r0.xyz, r0.xyzx, cb0[5].xyzx
    float stage15_x = r0.x + cb0[5].x;
    float stage15_y = r0.y + cb0[5].y;
    float stage15_z = r0.z + cb0[5].z;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage16_x = r0.x * v3.x + cb0[1].x;
    float stage16_y = r0.y * v3.y + cb0[1].y;
    float stage16_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage16_x;
    r0.y = stage16_y;
    r0.z = stage16_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage17_x = r0.x * v5.w + v5.x;
    float stage17_y = r0.y * v5.w + v5.y;
    float stage17_z = r0.z * v5.w + v5.z;
    o0.x = stage17_x;
    o0.y = stage17_y;
    o0.z = stage17_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage18_x = r0.x * v1.x;
    float stage18_y = r0.x * v1.y;
    float stage18_z = r0.x * v1.z;
    r0.x = stage18_x;
    r0.y = stage18_y;
    r0.z = stage18_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage19_x = r0.w * v0.z;
    float stage19_y = r0.w * v0.x;
    float stage19_z = r0.w * v0.y;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage20_x = r0.x * r1.x;
    float stage20_y = r0.x * r1.y;
    float stage20_z = r0.x * r1.z;
    r0.x = stage20_x;
    r0.y = stage20_y;
    r0.z = stage20_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage21_x = r0.x / r0.w;
    float stage21_y = r0.y / r0.w;
    r0.x = stage21_x;
    r0.y = stage21_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage22_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage22_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage22_x;
    r1.y = stage22_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage23_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage23_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage23_x;
    r1.y = stage23_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage24_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage24_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage24_x;
    r1.y = stage24_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage25_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage25_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage25_x;
    r0.y = stage25_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage26_x = r0.x * 0.5f + 0.5f;
    float stage26_y = r0.y * 0.5f + 0.5f;
    o2.x = stage26_x;
    o2.y = stage26_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage27_z = 1.0f;
    float stage27_w = asfloat(0u);
    o2.z = stage27_z;
    o2.w = stage27_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage28_x = cb2[3].x;
    float stage28_y = cb2[3].y;
    float stage28_z = cb2[3].z;
    o3.x = stage28_x;
    o3.y = stage28_y;
    o3.z = stage28_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage29_x = asfloat(0u);
    float stage29_y = asfloat(0u);
    float stage29_z = asfloat(0u);
    float stage29_w = asfloat(0u);
    o4.x = stage29_x;
    o4.y = stage29_y;
    o4.z = stage29_z;
    o4.w = stage29_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage30_x = asfloat(0u);
    float stage30_y = asfloat(0u);
    float stage30_z = asfloat(0u);
    float stage30_w = asfloat(0u);
    o5.x = stage30_x;
    o5.y = stage30_y;
    o5.z = stage30_z;
    o5.w = stage30_w;

    Shade_Ue3_bfx_d_pa_spla_05_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
