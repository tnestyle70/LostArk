// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_mm_basic_01_ad_Constants0 : register(b0)
{
    float4 cb0[3];
};
SamplerState s0 : register(s0);
Texture2D t0 : register(t0);

struct Shade_Ue3_fx_mm_basic_01_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_mm_basic_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_mm_basic_01_ad_OUTPUT Shade_Ue3_fx_mm_basic_01_ad(Shade_Ue3_fx_mm_basic_01_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;

    // mad r0.x, cb0[2].x, v4.x, l(-1.000000)
    r0.x = cb0[2].x * v4.x + -1.0f;
    // mul r0.x, r0.x, l(0.500000)
    r0.x = r0.x * 0.5f;
    // mul r0.y, v4.x, cb0[2].x
    r0.y = v4.x * cb0[2].x;
    // mad r0.xy, r0.yyyy, v2.xyxx, -r0.xxxx
    float stage1_x = r0.y * v2.x + -r0.x;
    float stage1_y = r0.y * v2.y + -r0.x;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch2 = t0.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch2.x;
    r0.y = fetch2.y;
    r0.z = fetch2.z;
    // log r0.w, |r0.x|
    r0.w = log2(abs(r0.x));
    // mul r0.w, r0.w, cb0[2].w
    r0.w = r0.w * cb0[2].w;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul r0.w, r0.w, v3.w
    r0.w = r0.w * v3.w;
    // lt r1.x, |r0.x|, l(0.000001)
    r1.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.w, r1.x, l(0), |r0.w|
    r0.w = asuint(r1.x) != 0u ? asfloat(0u) : abs(r0.w);
    // log r1.x, r0.w
    r1.x = log2(r0.w);
    // lt r0.w, r0.w, l(0.000001)
    r0.w = asfloat((r0.w < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, v4.y
    r1.x = r1.x * v4.y;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r1.x, r1.x, cb0[0].x
    r1.x = r1.x * cb0[0].x;
    // movc r0.w, r0.w, l(0), r1.x
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.x;
    // mul r1.xyz, r0.xyzx, cb0[2].yyyy
    float stage3_x = r0.x * cb0[2].y;
    float stage3_y = r0.y * cb0[2].y;
    float stage3_z = r0.z * cb0[2].y;
    r1.x = stage3_x;
    r1.y = stage3_y;
    r1.z = stage3_z;
    // dp3 r1.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r0.xyz, -cb0[2].yyyy, r0.xyzx, r1.wwww
    float stage4_x = -cb0[2].y * r0.x + r1.w;
    float stage4_y = -cb0[2].y * r0.y + r1.w;
    float stage4_z = -cb0[2].y * r0.z + r1.w;
    r0.x = stage4_x;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // mad r0.xyz, cb0[2].zzzz, r0.xyzx, r1.xyzx
    float stage5_x = cb0[2].z * r0.x + r1.x;
    float stage5_y = cb0[2].z * r0.y + r1.y;
    float stage5_z = cb0[2].z * r0.z + r1.z;
    r0.x = stage5_x;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage6_x = r0.x * v3.x + cb0[1].x;
    float stage6_y = r0.y * v3.y + cb0[1].y;
    float stage6_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage6_x;
    r0.y = stage6_y;
    r0.z = stage6_z;
    // mul r0.xyz, r0.xyzx, v5.wwww
    float stage7_x = r0.x * v5.w;
    float stage7_y = r0.y * v5.w;
    float stage7_z = r0.z * v5.w;
    r0.x = stage7_x;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // mul o0.xyz, r0.wwww, r0.xyzx
    float stage8_x = r0.w * r0.x;
    float stage8_y = r0.w * r0.y;
    float stage8_z = r0.w * r0.z;
    o0.x = stage8_x;
    o0.y = stage8_y;
    o0.z = stage8_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_mm_basic_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
