// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_bfx_i_pa_glow_01_ad_Constants0 : register(b0)
{
    float4 cb0[4];
};
cbuffer Shade_Ue3_bfx_i_pa_glow_01_ad_Constants2 : register(b2)
{
    float4 cb2[2];
};
SamplerState s0 : register(s0);
Texture2D t0 : register(t0);

struct Shade_Ue3_bfx_i_pa_glow_01_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v5;
    float4 v7;
};

struct Shade_Ue3_bfx_i_pa_glow_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_bfx_i_pa_glow_01_ad_OUTPUT Shade_Ue3_bfx_i_pa_glow_01_ad(Shade_Ue3_bfx_i_pa_glow_01_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v5 = stage.v5;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;

    // add r0.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // mad r0.x, -r0.x, l(2.000000), l(1.000000)
    r0.x = -r0.x * 2.0f + 1.0f;
    // max r0.y, r0.x, l(0.000000)
    r0.y = max(r0.x, 0.0f);
    // mul r0.x, r0.x, l(0.500000)
    r0.x = r0.x * 0.5f;
    // max r0.x, r0.x, l(0.000000)
    r0.x = max(r0.x, 0.0f);
    // log r0.z, r0.y
    r0.z = log2(r0.y);
    // lt r0.y, r0.y, l(0.000001)
    r0.y = asfloat((r0.y < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[2].x
    r0.z = r0.z * cb0[2].x;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // mul r0.z, r0.z, cb0[2].y
    r0.z = r0.z * cb0[2].y;
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // log r0.z, r0.x
    r0.z = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[2].z
    r0.z = r0.z * cb0[2].z;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // movc r0.x, r0.x, l(0), r0.z
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.z;
    // add r0.x, r0.x, r0.y
    r0.x = r0.x + r0.y;
    // mul r0.x, r0.x, cb0[2].w
    r0.x = r0.x * cb0[2].w;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[3].x
    r0.y = r0.y * cb0[3].x;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.yzw, r0.yyyy, v3.xxyz
    float stage2_y = r0.y * v3.x;
    float stage2_z = r0.y * v3.y;
    float stage2_w = r0.y * v3.z;
    r0.y = stage2_y;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // movc r0.xyz, r0.xxxx, l(0,0,0,0), r0.yzwy
    float stage3_x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    float stage3_y = asuint(r0.x) != 0u ? asfloat(0u) : r0.z;
    float stage3_z = asuint(r0.x) != 0u ? asfloat(0u) : r0.w;
    r0.x = stage3_x;
    r0.y = stage3_y;
    r0.z = stage3_z;
    // add r0.xyz, r0.xyzx, cb0[1].xyzx
    float stage4_x = r0.x + cb0[1].x;
    float stage4_y = r0.y + cb0[1].y;
    float stage4_z = r0.z + cb0[1].z;
    r0.x = stage4_x;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // mul r0.xyz, r0.xyzx, v5.wwww
    float stage5_x = r0.x * v5.w;
    float stage5_y = r0.y * v5.w;
    float stage5_z = r0.z * v5.w;
    r0.x = stage5_x;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // div r1.xy, v7.xyxx, v7.wwww
    float stage6_x = v7.x / v7.w;
    float stage6_y = v7.y / v7.w;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // mad r1.xy, r1.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage7_x = r1.x * cb2[0].x + cb2[0].w;
    float stage7_y = r1.y * cb2[0].y + cb2[0].z;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t0.yzwx, s0, l(0.000000)
    float4 fetch8 = t0.SampleLevel(s0, r1.xy, 0.0f);
    r0.w = fetch8.x;
    // min r0.w, r0.w, l(0.999000)
    r0.w = min(r0.w, 0.999f);
    // mad r1.x, r0.w, cb2[1].z, -cb2[1].w
    r1.x = r0.w * cb2[1].z + -cb2[1].w;
    // mad r0.w, r0.w, cb2[1].x, cb2[1].y
    r0.w = r0.w * cb2[1].x + cb2[1].y;
    // div r1.x, l(1.000000, 1.000000, 1.000000, 1.000000), r1.x
    r1.x = 1.0f / r1.x;
    // add r0.w, r0.w, r1.x
    r0.w = r0.w + r1.x;
    // add r0.w, r0.w, -v7.w
    r0.w = r0.w + -v7.w;
    // mul_sat r0.w, r0.w, l(0.020000)
    r0.w = saturate(r0.w * 0.02f);
    // mul r0.w, r0.w, v3.w
    r0.w = r0.w * v3.w;
    // mul r0.w, r0.w, cb0[0].x
    r0.w = r0.w * cb0[0].x;
    // mul o0.xyz, r0.wwww, r0.xyzx
    float stage9_x = r0.w * r0.x;
    float stage9_y = r0.w * r0.y;
    float stage9_z = r0.w * r0.z;
    o0.x = stage9_x;
    o0.y = stage9_y;
    o0.z = stage9_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_bfx_i_pa_glow_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
