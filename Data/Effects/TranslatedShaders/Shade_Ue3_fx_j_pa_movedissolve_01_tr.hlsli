// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_j_pa_movedissolve_01_tr_Constants0 : register(b0)
{
    float4 cb0[12];
};
cbuffer Shade_Ue3_fx_j_pa_movedissolve_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_j_pa_movedissolve_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_j_pa_movedissolve_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_j_pa_movedissolve_01_tr_OUTPUT Shade_Ue3_fx_j_pa_movedissolve_01_tr(Shade_Ue3_fx_j_pa_movedissolve_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.x, -v2.y, l(1.000000)
    r0.x = -v2.y + 1.0f;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, l(1.200000)
    r0.y = r0.y * 1.2f;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, l(10.000000)
    r0.y = r0.y * 10.0f;
    // min r0.y, r0.y, l(1.000000)
    r0.y = min(r0.y, 1.0f);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mov r0.y, v2.x
    r0.y = v2.x;
    // mad r1.x, r0.y, cb0[4].y, cb0[5].w
    r1.x = r0.y * cb0[4].y + cb0[5].w;
    // mul r0.y, v2.y, v4.y
    r0.y = v2.y * v4.y;
    // mad r1.y, r0.y, cb0[4].z, cb0[6].w
    r1.y = r0.y * cb0[4].z + cb0[6].w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.xyxx, t2.yxzw, s1, l(0.000000)
    float4 fetch1 = t2.SampleBias(s1, r1.xy, 0.0f);
    r0.y = fetch1.x;
    // mul r0.y, r0.y, cb0[7].x
    r0.y = r0.y * cb0[7].x;
    // mul r0.z, r0.x, r0.y
    r0.z = r0.x * r0.y;
    // mad_sat r0.z, -r0.z, l(0.700000), l(1.000000)
    r0.z = saturate(-r0.z * 0.7f + 1.0f);
    // ge r0.z, r0.z, v4.x
    r0.z = asfloat((r0.z >= v4.x) ? 0xffffffffu : 0u);
    // mad_sat r0.w, -r0.y, r0.x, l(1.000000)
    r0.w = saturate(-r0.y * r0.x + 1.0f);
    // mul r0.x, r0.x, v3.w
    r0.x = r0.x * v3.w;
    // mul r0.y, r0.y, r0.y
    r0.y = r0.y * r0.y;
    // ge r0.w, r0.w, v4.x
    r0.w = asfloat((r0.w >= v4.x) ? 0xffffffffu : 0u);
    // movc r0.zw, r0.zzzw, l(0,0,-0.000000,0), l(0,0,-1.000000,1.000000)
    float stage2_z = asuint(r0.z) != 0u ? -0.0f : -1.0f;
    float stage2_w = asuint(r0.w) != 0u ? asfloat(0u) : 1.0f;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // add r0.z, r0.z, r0.w
    r0.z = r0.z + r0.w;
    // mul r1.xyz, v3.xyzx, cb0[3].xyzx
    float stage3_x = v3.x * cb0[3].x;
    float stage3_y = v3.y * cb0[3].y;
    float stage3_z = v3.z * cb0[3].z;
    r1.x = stage3_x;
    r1.y = stage3_y;
    r1.z = stage3_z;
    // mul r1.xyz, r1.xyzx, l(20.000000, 20.000000, 20.000000, 0.000000)
    float stage4_x = r1.x * 20.0f;
    float stage4_y = r1.y * 20.0f;
    float stage4_z = r1.z * 20.0f;
    r1.x = stage4_x;
    r1.y = stage4_y;
    r1.z = stage4_z;
    // mad r2.xyz, r0.zzzz, r1.xyzx, r0.wwww
    float stage5_x = r0.z * r1.x + r0.w;
    float stage5_y = r0.z * r1.y + r0.w;
    float stage5_z = r0.z * r1.z + r0.w;
    r2.x = stage5_x;
    r2.y = stage5_y;
    r2.z = stage5_z;
    // add r0.z, r0.z, r0.w
    r0.z = r0.z + r0.w;
    // mul r0.w, r0.y, l(0.700000)
    r0.w = r0.y * 0.7f;
    // min r0.yw, r0.yyyw, l(0.000000, 1.000000, 0.000000, 1.000000)
    float stage6_y = min(r0.y, 1.0f);
    float stage6_w = min(r0.w, 1.0f);
    r0.y = stage6_y;
    r0.w = stage6_w;
    // ge r0.yw, r0.yyyw, cb0[7].yyyy
    float stage7_y = asfloat((r0.y >= cb0[7].y) ? 0xffffffffu : 0u);
    float stage7_w = asfloat((r0.w >= cb0[7].y) ? 0xffffffffu : 0u);
    r0.y = stage7_y;
    r0.w = stage7_w;
    // movc r0.yw, r0.yyyw, l(0,0,0,-0.000000), l(0,1.000000,0,-1.000000)
    float stage8_y = asuint(r0.y) != 0u ? asfloat(0u) : 1.0f;
    float stage8_w = asuint(r0.w) != 0u ? -0.0f : -1.0f;
    r0.y = stage8_y;
    r0.w = stage8_w;
    // add r0.w, r0.w, r0.y
    r0.w = r0.w + r0.y;
    // mad r1.xyz, r0.wwww, r1.xyzx, r0.yyyy
    float stage9_x = r0.w * r1.x + r0.y;
    float stage9_y = r0.w * r1.y + r0.y;
    float stage9_z = r0.w * r1.z + r0.y;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    // mul r0.y, r0.y, r0.z
    r0.y = r0.y * r0.z;
    // mul r1.xyz, r1.xyzx, r2.xyzx
    float stage10_x = r1.x * r2.x;
    float stage10_y = r1.y * r2.y;
    float stage10_z = r1.z * r2.z;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, v2.xyxx, t3.xyzw, s0, l(0.000000)
    float4 fetch11 = t3.SampleBias(s0, v2.xy, 0.0f);
    r2.x = fetch11.x;
    r2.y = fetch11.y;
    r2.z = fetch11.z;
    // dp3 r0.z, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.z = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r3.xyz, -r2.xyzx, r0.zzzz
    float stage12_x = -r2.x + r0.z;
    float stage12_y = -r2.y + r0.z;
    float stage12_z = -r2.z + r0.z;
    r3.x = stage12_x;
    r3.y = stage12_y;
    r3.z = stage12_z;
    // mad r2.xyz, cb0[4].xxxx, r3.xyzx, r2.xyzx
    float stage13_x = cb0[4].x * r3.x + r2.x;
    float stage13_y = cb0[4].x * r3.y + r2.y;
    float stage13_z = cb0[4].x * r3.z + r2.z;
    r2.x = stage13_x;
    r2.y = stage13_y;
    r2.z = stage13_z;
    // mul r2.xyz, r2.xyzx, cb0[2].xyzx
    float stage14_x = r2.x * cb0[2].x;
    float stage14_y = r2.y * cb0[2].y;
    float stage14_z = r2.z * cb0[2].z;
    r2.x = stage14_x;
    r2.y = stage14_y;
    r2.z = stage14_z;
    // mul r1.xyz, r1.xyzx, r2.xyzx
    float stage15_x = r1.x * r2.x;
    float stage15_y = r1.y * r2.y;
    float stage15_z = r1.z * r2.z;
    r1.x = stage15_x;
    r1.y = stage15_y;
    r1.z = stage15_z;
    // mad r1.xyz, r1.xyzx, v3.xyzx, cb0[1].xyzx
    float stage16_x = r1.x * v3.x + cb0[1].x;
    float stage16_y = r1.y * v3.y + cb0[1].y;
    float stage16_z = r1.z * v3.z + cb0[1].z;
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mad o0.xyz, r1.xyzx, v5.wwww, v5.xyzx
    float stage17_x = r1.x * v5.w + v5.x;
    float stage17_y = r1.y * v5.w + v5.y;
    float stage17_z = r1.z * v5.w + v5.z;
    o0.x = stage17_x;
    o0.y = stage17_y;
    o0.z = stage17_z;
    // mad r0.zw, v2.xxxy, cb0[7].zzzw, cb0[8].yyyw
    float stage18_z = v2.x * cb0[7].z + cb0[8].y;
    float stage18_w = v2.y * cb0[7].w + cb0[8].w;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.zw, r0.zwzz, t0.zwxy, s2, l(0.000000)
    float4 fetch19 = t0.SampleBias(s2, r0.zw, 0.0f);
    r0.z = fetch19.x;
    r0.w = fetch19.y;
    // mad r0.zw, r0.zzzw, l(0.000000, 0.000000, 2.000000, 2.000000), l(0.000000, 0.000000, -1.000000, -1.000000)
    float stage20_z = r0.z * 2.0f + -1.0f;
    float stage20_w = r0.w * 2.0f + -1.0f;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // add r1.x, v4.z, cb0[9].x
    r1.x = v4.z + cb0[9].x;
    // add r1.x, r1.x, l(-1.000000)
    r1.x = r1.x + -1.0f;
    // mad r0.zw, r1.xxxx, r0.zzzw, v2.xxxy
    float stage21_z = r1.x * r0.z + v2.x;
    float stage21_w = r1.x * r0.w + v2.y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mad r1.x, v4.w, cb0[9].w, cb0[10].x
    r1.x = v4.w * cb0[9].w + cb0[10].x;
    // mad r1.y, v4.w, cb0[10].y, cb0[10].z
    r1.y = v4.w * cb0[10].y + cb0[10].z;
    // mad r0.zw, r0.zzzw, cb0[9].yyyz, r1.xxxy
    float stage22_z = r0.z * cb0[9].y + r1.x;
    float stage22_w = r0.w * cb0[9].z + r1.y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r0.zwzz, t1.yzxw, s3, l(0.000000)
    float4 fetch23 = t1.SampleBias(s3, r0.zw, 0.0f);
    r0.z = fetch23.x;
    // mul r0.y, r0.z, r0.y
    r0.y = r0.z * r0.y;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // log r0.y, |r0.z|
    r0.y = log2(abs(r0.z));
    // lt r0.z, |r0.z|, l(0.000001)
    r0.z = asfloat((abs(r0.z) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[10].w
    r0.y = r0.y * cb0[10].w;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, cb0[11].x
    r0.y = r0.y * cb0[11].x;
    // movc r0.y, r0.z, l(0), r0.y
    r0.y = asuint(r0.z) != 0u ? asfloat(0u) : r0.y;
    // mul_sat r0.x, r0.x, r0.y
    r0.x = saturate(r0.x * r0.y);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage24_x = r0.x * v1.x;
    float stage24_y = r0.x * v1.y;
    float stage24_z = r0.x * v1.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage25_x = r0.w * v0.z;
    float stage25_y = r0.w * v0.x;
    float stage25_z = r0.w * v0.y;
    r1.x = stage25_x;
    r1.y = stage25_y;
    r1.z = stage25_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage26_x = r0.x * r1.x;
    float stage26_y = r0.x * r1.y;
    float stage26_z = r0.x * r1.z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage27_x = r0.x / r0.w;
    float stage27_y = r0.y / r0.w;
    r0.x = stage27_x;
    r0.y = stage27_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage28_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage28_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage28_x;
    r1.y = stage28_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage29_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage29_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage29_x;
    r1.y = stage29_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage30_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage30_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage31_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage31_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage31_x;
    r0.y = stage31_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage32_x = r0.x * 0.5f + 0.5f;
    float stage32_y = r0.y * 0.5f + 0.5f;
    o2.x = stage32_x;
    o2.y = stage32_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage33_z = 1.0f;
    float stage33_w = asfloat(0u);
    o2.z = stage33_z;
    o2.w = stage33_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage34_x = cb2[3].x;
    float stage34_y = cb2[3].y;
    float stage34_z = cb2[3].z;
    o3.x = stage34_x;
    o3.y = stage34_y;
    o3.z = stage34_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage35_x = asfloat(0u);
    float stage35_y = asfloat(0u);
    float stage35_z = asfloat(0u);
    float stage35_w = asfloat(0u);
    o4.x = stage35_x;
    o4.y = stage35_y;
    o4.z = stage35_z;
    o4.w = stage35_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage36_x = asfloat(0u);
    float stage36_y = asfloat(0u);
    float stage36_z = asfloat(0u);
    float stage36_w = asfloat(0u);
    o5.x = stage36_x;
    o5.y = stage36_y;
    o5.z = stage36_z;
    o5.w = stage36_w;

    Shade_Ue3_fx_j_pa_movedissolve_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
