// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_pa_ribbonmaster_03_tr_Constants0 : register(b0)
{
    float4 cb0[17];
};
cbuffer Shade_Ue3_fx_m_pa_ribbonmaster_03_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_m_pa_ribbonmaster_03_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_m_pa_ribbonmaster_03_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_m_pa_ribbonmaster_03_tr_OUTPUT Shade_Ue3_fx_m_pa_ribbonmaster_03_tr(Shade_Ue3_fx_m_pa_ribbonmaster_03_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.xy, v2.xyxx, cb0[9].yzyy
    float stage1_x = v2.x * cb0[9].y;
    float stage1_y = v2.y * cb0[9].z;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, cb0[8].yyyy, cb0[9].xwxx, r0.xyxx
    float stage2_x = cb0[8].y * cb0[9].x + r0.x;
    float stage2_y = cb0[8].y * cb0[9].w + r0.y;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xy, r0.xyxx, t3.xyzw, s0, l(0.000000)
    float4 fetch3 = t3.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch3.x;
    r0.y = fetch3.y;
    // mad r0.xy, cb0[10].xxxx, r0.xyxx, v2.xyxx
    float stage4_x = cb0[10].x * r0.x + v2.x;
    float stage4_y = cb0[10].x * r0.y + v2.y;
    r0.x = stage4_x;
    r0.y = stage4_y;
    // add r0.xy, r0.xyxx, v4.xxxx
    float stage5_x = r0.x + v4.x;
    float stage5_y = r0.y + v4.x;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // mul r0.xy, r0.xyxx, cb0[8].zwzz
    float stage6_x = r0.x * cb0[8].z;
    float stage6_y = r0.y * cb0[8].w;
    r0.x = stage6_x;
    r0.y = stage6_y;
    // mad r1.x, cb0[8].y, cb0[8].x, r0.x
    r1.x = cb0[8].y * cb0[8].x + r0.x;
    // mad r1.y, cb0[8].y, cb0[10].y, r0.y
    r1.y = cb0[8].y * cb0[10].y + r0.y;
    // add r0.xy, r1.xyxx, cb0[2].xyxx
    float stage7_x = r1.x + cb0[2].x;
    float stage7_y = r1.y + cb0[2].y;
    r0.x = stage7_x;
    r0.y = stage7_y;
    // mov r1.x, v4.w
    r1.x = v4.w;
    // mov r1.y, l(0)
    r1.y = asfloat(0u);
    // add r0.xy, r0.xyxx, r1.xyxx
    float stage8_x = r0.x + r1.x;
    float stage8_y = r0.y + r1.y;
    r0.x = stage8_x;
    r0.y = stage8_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t4.xyzw, s1, l(0.000000)
    float4 fetch9 = t4.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch9.x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // mul r0.y, r0.y, cb0[11].y
    r0.y = r0.y * cb0[11].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, cb0[11].z
    r0.y = r0.y * cb0[11].z;
    // lt r0.z, |r0.x|, l(0.000001)
    r0.z = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.y, r0.z, l(0), r0.y
    r0.y = asuint(r0.z) != 0u ? asfloat(0u) : r0.y;
    // mad r0.x, r0.x, cb0[11].x, r0.y
    r0.x = r0.x * cb0[11].x + r0.y;
    // mad r1.x, cb0[12].x, v2.x, cb0[3].x
    r1.x = cb0[12].x * v2.x + cb0[3].x;
    // mad r1.y, cb0[12].y, v2.y, cb0[4].y
    r1.y = cb0[12].y * v2.y + cb0[4].y;
    // add r0.yz, r1.xxyx, cb0[5].xxyx
    float stage10_y = r1.x + cb0[5].x;
    float stage10_z = r1.y + cb0[5].y;
    r0.y = stage10_y;
    r0.z = stage10_z;
    // add r0.yz, r0.yyzy, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage11_y = r0.y + -0.5f;
    float stage11_z = r0.z + -0.5f;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // dp2 r1.x, cb0[6].xyxx, r0.yzyy
    r1.x = dot(cb0[6].xy, r0.yz);
    // dp2 r1.y, cb0[7].xyxx, r0.yzyy
    r1.y = dot(cb0[7].xy, r0.yz);
    // add r0.yz, r1.xxyx, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage12_y = r1.x + 0.5f;
    float stage12_z = r1.y + 0.5f;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mul r0.w, v2.x, cb0[13].w
    r0.w = v2.x * cb0[13].w;
    // mad r1.x, cb0[8].y, cb0[13].z, r0.w
    r1.x = cb0[8].y * cb0[13].z + r0.w;
    // mul r0.w, v2.y, cb0[14].x
    r0.w = v2.y * cb0[14].x;
    // mad r1.y, cb0[8].y, cb0[14].y, r0.w
    r1.y = cb0[8].y * cb0[14].y + r0.w;
    // add r1.xy, r1.xyxx, v4.yyyy
    float stage13_x = r1.x + v4.y;
    float stage13_y = r1.y + v4.y;
    r1.x = stage13_x;
    r1.y = stage13_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xy, r1.xyxx, t0.xyzw, s2, l(0.000000)
    float4 fetch14 = t0.SampleBias(s2, r1.xy, 0.0f);
    r1.x = fetch14.x;
    r1.y = fetch14.y;
    // mad r0.yz, cb0[14].zzzz, r1.xxyx, r0.yyzy
    float stage15_y = cb0[14].z * r1.x + r0.y;
    float stage15_z = cb0[14].z * r1.y + r0.z;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t1.yxzw, s3, l(0.000000)
    float4 fetch16 = t1.SampleBias(s3, r0.yz, 0.0f);
    r0.y = fetch16.x;
    // mad r0.x, cb0[14].w, r0.y, r0.x
    r0.x = cb0[14].w * r0.y + r0.x;
    // mad r0.xzw, r0.xxxx, v3.xxyz, cb0[1].xxyz
    float stage17_x = r0.x * v3.x + cb0[1].x;
    float stage17_z = r0.x * v3.y + cb0[1].y;
    float stage17_w = r0.x * v3.z + cb0[1].z;
    r0.x = stage17_x;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // mad o0.xyz, r0.xzwx, v5.wwww, v5.xyzx
    float stage18_x = r0.x * v5.w + v5.x;
    float stage18_y = r0.z * v5.w + v5.y;
    float stage18_z = r0.w * v5.w + v5.z;
    o0.x = stage18_x;
    o0.y = stage18_y;
    o0.z = stage18_z;
    // mul r0.xz, v2.xxyx, cb0[15].yyzy
    float stage19_x = v2.x * cb0[15].y;
    float stage19_z = v2.y * cb0[15].z;
    r0.x = stage19_x;
    r0.z = stage19_z;
    // mad r0.xz, cb0[8].yyyy, cb0[15].xxwx, r0.xxzx
    float stage20_x = cb0[8].y * cb0[15].x + r0.x;
    float stage20_z = cb0[8].y * cb0[15].w + r0.z;
    r0.x = stage20_x;
    r0.z = stage20_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xzxx, t2.xyzw, s4, l(0.000000)
    float4 fetch21 = t2.SampleBias(s4, r0.xz, 0.0f);
    r0.x = fetch21.x;
    // add r0.x, r0.x, l(0.200000)
    r0.x = r0.x + 0.2f;
    // add r0.z, -v4.z, l(1.000000)
    r0.z = -v4.z + 1.0f;
    // add r0.x, -r0.z, r0.x
    r0.x = -r0.z + r0.x;
    // mul_sat r0.x, r0.x, cb0[16].x
    r0.x = saturate(r0.x * cb0[16].x);
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul r0.x, r0.x, cb0[16].y
    r0.x = r0.x * cb0[16].y;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage22_x = r0.x * v1.x;
    float stage22_y = r0.x * v1.y;
    float stage22_z = r0.x * v1.z;
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage23_x = r0.w * v0.z;
    float stage23_y = r0.w * v0.x;
    float stage23_z = r0.w * v0.y;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage24_x = r0.x * r1.x;
    float stage24_y = r0.x * r1.y;
    float stage24_z = r0.x * r1.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage25_x = r0.x / r0.w;
    float stage25_y = r0.y / r0.w;
    r0.x = stage25_x;
    r0.y = stage25_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage26_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage26_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage26_x;
    r1.y = stage26_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage27_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage27_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage27_x;
    r1.y = stage27_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage28_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage28_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage28_x;
    r1.y = stage28_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage29_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage29_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage29_x;
    r0.y = stage29_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage30_x = r0.x * 0.5f + 0.5f;
    float stage30_y = r0.y * 0.5f + 0.5f;
    o2.x = stage30_x;
    o2.y = stage30_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage31_z = 1.0f;
    float stage31_w = asfloat(0u);
    o2.z = stage31_z;
    o2.w = stage31_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage32_x = cb2[3].x;
    float stage32_y = cb2[3].y;
    float stage32_z = cb2[3].z;
    o3.x = stage32_x;
    o3.y = stage32_y;
    o3.z = stage32_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage33_x = asfloat(0u);
    float stage33_y = asfloat(0u);
    float stage33_z = asfloat(0u);
    float stage33_w = asfloat(0u);
    o4.x = stage33_x;
    o4.y = stage33_y;
    o4.z = stage33_z;
    o4.w = stage33_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage34_x = asfloat(0u);
    float stage34_y = asfloat(0u);
    float stage34_z = asfloat(0u);
    float stage34_w = asfloat(0u);
    o5.x = stage34_x;
    o5.y = stage34_y;
    o5.z = stage34_z;
    o5.w = stage34_w;

    Shade_Ue3_fx_m_pa_ribbonmaster_03_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
