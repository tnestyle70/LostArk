// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_me_superactionspace_tr_Constants0 : register(b0)
{
    float4 cb0[15];
};
cbuffer Shade_Ue3_fx_d_me_superactionspace_tr_Constants2 : register(b2)
{
    float4 cb2[7];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_d_me_superactionspace_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v6;
};

struct Shade_Ue3_fx_d_me_superactionspace_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_d_me_superactionspace_tr_OUTPUT Shade_Ue3_fx_d_me_superactionspace_tr(Shade_Ue3_fx_d_me_superactionspace_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v6 = stage.v6;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.z, cb0[10].x, cb0[10].y
    r0.z = cb0[10].x * cb0[10].y;
    // div r0.x, cb2[6].x, cb2[6].y
    r0.x = cb2[6].x / cb2[6].y;
    // div r1.xy, v6.xyxx, v6.wwww
    float stage1_x = v6.x / v6.w;
    float stage1_y = v6.y / v6.w;
    r1.x = stage1_x;
    r1.y = stage1_y;
    // mad r1.xy, r1.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage2_x = r1.x * cb2[0].x + cb2[0].w;
    float stage2_y = r1.y * cb2[0].y + cb2[0].z;
    r1.x = stage2_x;
    r1.y = stage2_y;
    // mov r0.yw, l(0,1.000000,0,0.500000)
    float stage3_y = 1.0f;
    float stage3_w = 0.5f;
    r0.y = stage3_y;
    r0.w = stage3_w;
    // mad r0.xy, r1.xyxx, r0.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage4_x = r1.x * r0.x + -0.5f;
    float stage4_y = r1.y * r0.y + -0.5f;
    r0.x = stage4_x;
    r0.y = stage4_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch5 = t0.SampleLevel(s0, r1.xy, 0.0f);
    r1.x = fetch5.x;
    // min r1.x, r1.x, l(0.999000)
    r1.x = min(r1.x, 0.999f);
    // mad r0.xy, r0.xyxx, l(1.500000, 1.500000, 0.000000, 0.000000), cb0[4].xyxx
    float stage6_x = r0.x * 1.5f + cb0[4].x;
    float stage6_y = r0.y * 1.5f + cb0[4].y;
    r0.x = stage6_x;
    r0.y = stage6_y;
    // mul r1.y, cb0[3].x, l(-0.523599)
    r1.y = cb0[3].x * -0.523599f;
    // sincos r2.x, r3.x, r1.y
    float stage7_x = r1.y;
    r2.x = sin(stage7_x);
    r3.x = cos(stage7_x);
    // mov r4.x, -r2.x
    r4.x = -r2.x;
    // mov r4.y, r3.x
    r4.y = r3.x;
    // mov r4.z, r2.x
    r4.z = r2.x;
    // dp2 r2.y, r4.zyzz, r0.xyxx
    r2.y = dot(r4.zy, r0.xy);
    // dp2 r2.x, r4.yxyy, r0.xyxx
    r2.x = dot(r4.yx, r0.xy);
    // mad r0.xy, r2.xyxx, cb0[5].xyxx, r0.zwzz
    float stage8_x = r2.x * cb0[5].x + r0.z;
    float stage8_y = r2.y * cb0[5].y + r0.w;
    r0.x = stage8_x;
    r0.y = stage8_y;
    // add r3.x, r0.x, l(0.500000)
    r3.x = r0.x + 0.5f;
    // mad r3.y, cb0[10].y, cb0[11].z, r0.y
    r3.y = cb0[10].y * cb0[11].z + r0.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r3.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch9 = t1.SampleBias(s1, r3.xy, 0.0f);
    r0.x = fetch9.x;
    r0.y = fetch9.y;
    r0.z = fetch9.z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r1.yzw, -r0.xxyz, r0.wwww
    float stage10_y = -r0.x + r0.w;
    float stage10_z = -r0.y + r0.w;
    float stage10_w = -r0.z + r0.w;
    r1.y = stage10_y;
    r1.z = stage10_z;
    r1.w = stage10_w;
    // mad r0.xyz, cb0[11].wwww, r1.yzwy, r0.xyzx
    float stage11_x = cb0[11].w * r1.y + r0.x;
    float stage11_y = cb0[11].w * r1.z + r0.y;
    float stage11_z = cb0[11].w * r1.w + r0.z;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mul r3.x, cb0[10].y, cb0[12].x
    r3.x = cb0[10].y * cb0[12].x;
    // mov r3.z, l(0.500000)
    r3.z = 0.5f;
    // mad r1.yz, r2.xxyx, cb0[6].xxyx, r3.xxzx
    float stage12_y = r2.x * cb0[6].x + r3.x;
    float stage12_z = r2.y * cb0[6].y + r3.z;
    r1.y = stage12_y;
    r1.z = stage12_z;
    // add r3.x, r1.y, l(0.500000)
    r3.x = r1.y + 0.5f;
    // mad r3.y, cb0[10].y, cb0[12].w, r1.z
    r3.y = cb0[10].y * cb0[12].w + r1.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.yzw, r3.xyxx, t2.wxyz, s2, l(0.000000)
    float4 fetch13 = t2.SampleBias(s2, r3.xy, 0.0f);
    r1.y = fetch13.x;
    r1.z = fetch13.y;
    r1.w = fetch13.z;
    // dp3 r0.w, r1.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r1.yzw, float3(0.3f, 0.59f, 0.11f));
    // add r3.xyz, -r1.yzwy, r0.wwww
    float stage14_x = -r1.y + r0.w;
    float stage14_y = -r1.z + r0.w;
    float stage14_z = -r1.w + r0.w;
    r3.x = stage14_x;
    r3.y = stage14_y;
    r3.z = stage14_z;
    // mad r1.yzw, cb0[13].xxxx, r3.xxyz, r1.yyzw
    float stage15_y = cb0[13].x * r3.x + r1.y;
    float stage15_z = cb0[13].x * r3.y + r1.z;
    float stage15_w = cb0[13].x * r3.z + r1.w;
    r1.y = stage15_y;
    r1.z = stage15_z;
    r1.w = stage15_w;
    // mul r0.xyz, r0.xyzx, r1.yzwy
    float stage16_x = r0.x * r1.y;
    float stage16_y = r0.y * r1.z;
    float stage16_z = r0.z * r1.w;
    r0.x = stage16_x;
    r0.y = stage16_y;
    r0.z = stage16_z;
    // mul r1.yzw, cb0[7].xxyz, cb0[7].wwww
    float stage17_y = cb0[7].x * cb0[7].w;
    float stage17_z = cb0[7].y * cb0[7].w;
    float stage17_w = cb0[7].z * cb0[7].w;
    r1.y = stage17_y;
    r1.z = stage17_z;
    r1.w = stage17_w;
    // mul r1.yzw, r0.xxyz, r1.yyzw
    float stage18_y = r0.x * r1.y;
    float stage18_z = r0.y * r1.z;
    float stage18_w = r0.z * r1.w;
    r1.y = stage18_y;
    r1.z = stage18_z;
    r1.w = stage18_w;
    // mul r3.xyz, cb0[8].xyzx, cb0[8].wwww
    float stage19_x = cb0[8].x * cb0[8].w;
    float stage19_y = cb0[8].y * cb0[8].w;
    float stage19_z = cb0[8].z * cb0[8].w;
    r3.x = stage19_x;
    r3.y = stage19_y;
    r3.z = stage19_z;
    // mad r0.xyz, r0.xyzx, r3.xyzx, -r1.yzwy
    float stage20_x = r0.x * r3.x + -r1.y;
    float stage20_y = r0.y * r3.y + -r1.z;
    float stage20_z = r0.z * r3.z + -r1.w;
    r0.x = stage20_x;
    r0.y = stage20_y;
    r0.z = stage20_z;
    // mul r3.x, cb0[10].y, cb0[13].y
    r3.x = cb0[10].y * cb0[13].y;
    // mov r3.y, l(0.500000)
    r3.y = 0.5f;
    // mad r2.xy, r2.xyxx, cb0[9].xyxx, r3.xyxx
    float stage21_x = r2.x * cb0[9].x + r3.x;
    float stage21_y = r2.y * cb0[9].y + r3.y;
    r2.x = stage21_x;
    r2.y = stage21_y;
    // add r3.x, r2.x, l(0.500000)
    r3.x = r2.x + 0.5f;
    // mad r3.y, cb0[10].y, cb0[14].x, r2.y
    r3.y = cb0[10].y * cb0[14].x + r2.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r3.xyxx, t3.xyzw, s3, l(0.000000)
    float4 fetch22 = t3.SampleBias(s3, r3.xy, 0.0f);
    r2.x = fetch22.x;
    r2.y = fetch22.y;
    r2.z = fetch22.z;
    // add r0.w, r2.y, r2.x
    r0.w = r2.y + r2.x;
    // add r0.w, r2.z, r0.w
    r0.w = r2.z + r0.w;
    // mul r0.w, r0.w, cb0[14].y
    r0.w = r0.w * cb0[14].y;
    // mul_sat r0.w, r0.w, l(0.333330)
    r0.w = saturate(r0.w * 0.33333f);
    // mad r0.xyz, r0.wwww, r0.xyzx, r1.yzwy
    float stage23_x = r0.w * r0.x + r1.y;
    float stage23_y = r0.w * r0.y + r1.z;
    float stage23_z = r0.w * r0.z + r1.w;
    r0.x = stage23_x;
    r0.y = stage23_y;
    r0.z = stage23_z;
    // mad r0.xyz, r0.xyzx, cb0[1].xyzx, cb0[2].xyzx
    float stage24_x = r0.x * cb0[1].x + cb0[2].x;
    float stage24_y = r0.y * cb0[1].y + cb0[2].y;
    float stage24_z = r0.z * cb0[1].z + cb0[2].z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // mad o0.xyz, r0.xyzx, v4.wwww, v4.xyzx
    float stage25_x = r0.x * v4.w + v4.x;
    float stage25_y = r0.y * v4.w + v4.y;
    float stage25_z = r0.z * v4.w + v4.z;
    o0.x = stage25_x;
    o0.y = stage25_y;
    o0.z = stage25_z;
    // mad r0.x, r1.x, cb2[1].z, -cb2[1].w
    r0.x = r1.x * cb2[1].z + -cb2[1].w;
    // mad r0.y, r1.x, cb2[1].x, cb2[1].y
    r0.y = r1.x * cb2[1].x + cb2[1].y;
    // div r0.x, l(1.000000, 1.000000, 1.000000, 1.000000), r0.x
    r0.x = 1.0f / r0.x;
    // add r0.x, r0.x, r0.y
    r0.x = r0.x + r0.y;
    // add r0.x, r0.x, -v6.w
    r0.x = r0.x + -v6.w;
    // mul_sat r0.x, r0.x, l(0.111111)
    r0.x = saturate(r0.x * 0.111111f);
    // add r0.x, -r0.x, l(1.000000)
    r0.x = -r0.x + 1.0f;
    // mad r0.x, -r0.x, r0.x, l(1.000000)
    r0.x = -r0.x * r0.x + 1.0f;
    // mul_sat r0.x, r0.x, cb0[1].w
    r0.x = saturate(r0.x * cb0[1].w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage26_x = r0.x * v1.x;
    float stage26_y = r0.x * v1.y;
    float stage26_z = r0.x * v1.z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage27_x = r0.w * v0.z;
    float stage27_y = r0.w * v0.x;
    float stage27_z = r0.w * v0.y;
    r1.x = stage27_x;
    r1.y = stage27_y;
    r1.z = stage27_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage28_x = r0.x * r1.x;
    float stage28_y = r0.x * r1.y;
    float stage28_z = r0.x * r1.z;
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage29_x = r0.x / r0.w;
    float stage29_y = r0.y / r0.w;
    r0.x = stage29_x;
    r0.y = stage29_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage30_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage30_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage30_x;
    r1.y = stage30_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage31_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage31_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage31_x;
    r1.y = stage31_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage32_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage32_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage32_x;
    r1.y = stage32_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage33_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage33_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage33_x;
    r0.y = stage33_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage34_x = r0.x * 0.5f + 0.5f;
    float stage34_y = r0.y * 0.5f + 0.5f;
    o2.x = stage34_x;
    o2.y = stage34_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage35_z = 1.0f;
    float stage35_w = asfloat(0u);
    o2.z = stage35_z;
    o2.w = stage35_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage36_x = cb2[3].x;
    float stage36_y = cb2[3].y;
    float stage36_z = cb2[3].z;
    o3.x = stage36_x;
    o3.y = stage36_y;
    o3.z = stage36_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage37_x = asfloat(0u);
    float stage37_y = asfloat(0u);
    float stage37_z = asfloat(0u);
    float stage37_w = asfloat(0u);
    o4.x = stage37_x;
    o4.y = stage37_y;
    o4.z = stage37_z;
    o4.w = stage37_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage38_x = asfloat(0u);
    float stage38_y = asfloat(0u);
    float stage38_z = asfloat(0u);
    float stage38_w = asfloat(0u);
    o5.x = stage38_x;
    o5.y = stage38_y;
    o5.z = stage38_z;
    o5.w = stage38_w;

    Shade_Ue3_fx_d_me_superactionspace_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
