// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_j_me_splitline_01_1_ad_Constants0 : register(b0)
{
    float4 cb0[4];
};
cbuffer Shade_Ue3_fx_j_me_splitline_01_1_ad_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_j_me_splitline_01_1_ad_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
    float4 v7;
};

struct Shade_Ue3_fx_j_me_splitline_01_1_ad_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_j_me_splitline_01_1_ad_OUTPUT Shade_Ue3_fx_j_me_splitline_01_1_ad(Shade_Ue3_fx_j_me_splitline_01_1_ad_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 r5 = (float4)0;
    float4 r6 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage1_x = r0.x * v1.x;
    float stage1_y = r0.x * v1.y;
    float stage1_z = r0.x * v1.z;
    r0.x = stage1_x;
    r0.y = stage1_y;
    r0.z = stage1_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.xyzx
    float stage2_x = r0.w * v0.x;
    float stage2_y = r0.w * v0.y;
    float stage2_z = r0.w * v0.z;
    r1.x = stage2_x;
    r1.y = stage2_y;
    r1.z = stage2_z;
    // mul r0.w, r0.y, r1.x
    r0.w = r0.y * r1.x;
    // mad r0.w, r0.x, r1.y, -r0.w
    r0.w = r0.x * r1.y + -r0.w;
    // mul r0.y, r0.w, v1.w
    r0.y = r0.w * v1.w;
    // dp3 r0.w, v6.xyzx, v6.xyzx
    r0.w = dot(v6.xyz, v6.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul_sat r0.w, r0.w, v6.z
    r0.w = saturate(r0.w * v6.z);
    // div r1.xy, v7.xyxx, v7.wwww
    float stage3_x = v7.x / v7.w;
    float stage3_y = v7.y / v7.w;
    r1.x = stage3_x;
    r1.y = stage3_y;
    // mad r1.xy, r1.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage4_x = r1.x * cb2[0].x + cb2[0].w;
    float stage4_y = r1.y * cb2[0].y + cb2[0].z;
    r1.x = stage4_x;
    r1.y = stage4_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.w, r1.xyxx, t0.yzwx, s0, l(0.000000)
    float4 fetch5 = t0.SampleLevel(s0, r1.xy, 0.0f);
    r1.w = fetch5.x;
    // min r1.w, r1.w, l(0.999000)
    r1.w = min(r1.w, 0.999f);
    // mad r2.x, r1.w, cb2[1].x, cb2[1].y
    r2.x = r1.w * cb2[1].x + cb2[1].y;
    // mad r1.w, r1.w, cb2[1].z, -cb2[1].w
    r1.w = r1.w * cb2[1].z + -cb2[1].w;
    // div r1.w, l(1.000000, 1.000000, 1.000000, 1.000000), r1.w
    r1.w = 1.0f / r1.w;
    // add r1.w, r1.w, r2.x
    r1.w = r1.w + r2.x;
    // add r1.w, r1.w, -v7.w
    r1.w = r1.w + -v7.w;
    // mul_sat r1.w, r1.w, l(0.036364)
    r1.w = saturate(r1.w * 0.036364f);
    // mul r2.x, r0.w, v4.y
    r2.x = r0.w * v4.y;
    // mul r2.x, r2.x, cb0[1].w
    r2.x = r2.x * cb0[1].w;
    // mul r1.w, r1.w, r2.x
    r1.w = r1.w * r2.x;
    // mul o0.w, r1.w, cb0[0].x
    o0.w = r1.w * cb0[0].x;
    // add r1.w, v4.x, l(-0.500000)
    r1.w = v4.x + -0.5f;
    // add r2.x, |r1.w|, |r1.w|
    r2.x = abs(r1.w) + abs(r1.w);
    // lt r1.w, |r1.w|, l(0.000000)
    r1.w = asfloat((abs(r1.w) < 0.0f) ? 0xffffffffu : 0u);
    // log r2.x, r2.x
    r2.x = log2(r2.x);
    // mul r2.x, r2.x, l(10.000000)
    r2.x = r2.x * 10.0f;
    // exp r2.x, r2.x
    r2.x = exp2(r2.x);
    // mul r2.x, r2.x, l(0.035000)
    r2.x = r2.x * 0.035f;
    // movc r1.w, r1.w, l(0), r2.x
    r1.w = asuint(r1.w) != 0u ? asfloat(0u) : r2.x;
    // mad r1.w, cb0[3].x, l(0.010000), r1.w
    r1.w = cb0[3].x * 0.01f + r1.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r1.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch6 = t1.SampleBias(s1, r1.xy, 0.0f);
    r2.x = fetch6.x;
    r2.y = fetch6.y;
    r2.z = fetch6.z;
    // add r3.xy, r1.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage7_x = r1.x + -0.5f;
    float stage7_y = r1.y + -0.5f;
    r3.x = stage7_x;
    r3.y = stage7_y;
    // mul r3.xy, r3.xyxx, l(0.000000, 1.000000, 0.000000, 0.000000)
    float stage8_x = r3.x * 0.0f;
    float stage8_y = r3.y * 1.0f;
    r3.x = stage8_x;
    r3.y = stage8_y;
    // mul r1.w, r1.w, l(0.100000)
    r1.w = r1.w * 0.1f;
    // mov r3.zw, r1.yyyx
    float stage9_z = r1.y;
    float stage9_w = r1.x;
    r3.z = stage9_z;
    r3.w = stage9_w;
    // mov r4.x, r2.x
    r4.x = r2.x;
    // mov r4.y, l(0)
    r4.y = asfloat(0u);
    // loop
    while (true) {
    // ge r2.w, r4.y, l(3.000000)
    r2.w = asfloat((r4.y >= 3.0f) ? 0xffffffffu : 0u);
    // breakc_nz r2.w
    if (asuint(r2.w) != 0u) break;
    // mad r3.zw, -r3.yyyx, r1.wwww, r3.zzzw
    float stage10_z = -r3.y * r1.w + r3.z;
    float stage10_w = -r3.x * r1.w + r3.w;
    r3.z = stage10_z;
    r3.w = stage10_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.w, r3.wzww, t1.yzwx, s1, l(0.000000)
    float4 fetch11 = t1.SampleBias(s1, r3.wz, 0.0f);
    r2.w = fetch11.x;
    // add r4.x, r2.w, r4.x
    r4.x = r2.w + r4.x;
    // add r4.y, r4.y, l(1.000000)
    r4.y = r4.y + 1.0f;
    // endloop
    }
    // mov r2.w, r1.x
    r2.w = r1.x;
    // mov r2.x, r3.z
    r2.x = r3.z;
    // mov r5.x, r2.y
    r5.x = r2.y;
    // mov r5.y, l(0)
    r5.y = asfloat(0u);
    // loop
    while (true) {
    // ge r1.y, r5.y, l(3.000000)
    r1.y = asfloat((r5.y >= 3.0f) ? 0xffffffffu : 0u);
    // breakc_nz r1.y
    if (asuint(r1.y) != 0u) break;
    // mad r2.xw, -r3.yyyx, r1.wwww, r2.xxxw
    float stage12_x = -r3.y * r1.w + r2.x;
    float stage12_w = -r3.x * r1.w + r2.w;
    r2.x = stage12_x;
    r2.w = stage12_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.y, r2.wxww, t1.xyzw, s1, l(0.000000)
    float4 fetch13 = t1.SampleBias(s1, r2.wx, 0.0f);
    r1.y = fetch13.y;
    // add r5.x, r1.y, r5.x
    r5.x = r1.y + r5.x;
    // add r5.y, r5.y, l(1.000000)
    r5.y = r5.y + 1.0f;
    // endloop
    }
    // mov r4.y, r5.x
    r4.y = r5.x;
    // mov r5.x, r1.x
    r5.x = r1.x;
    // mov r5.y, r2.x
    r5.y = r2.x;
    // mov r6.x, r2.z
    r6.x = r2.z;
    // mov r6.y, l(0)
    r6.y = asfloat(0u);
    // loop
    while (true) {
    // ge r1.y, r6.y, l(3.000000)
    r1.y = asfloat((r6.y >= 3.0f) ? 0xffffffffu : 0u);
    // breakc_nz r1.y
    if (asuint(r1.y) != 0u) break;
    // mad r5.xy, -r3.xyxx, r1.wwww, r5.xyxx
    float stage14_x = -r3.x * r1.w + r5.x;
    float stage14_y = -r3.y * r1.w + r5.y;
    r5.x = stage14_x;
    r5.y = stage14_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.y, r5.xyxx, t1.xzyw, s1, l(0.000000)
    float4 fetch15 = t1.SampleBias(s1, r5.xy, 0.0f);
    r1.y = fetch15.z;
    // add r6.x, r1.y, r6.x
    r6.x = r1.y + r6.x;
    // add r6.y, r6.y, l(1.000000)
    r6.y = r6.y + 1.0f;
    // endloop
    }
    // mov r4.z, r6.x
    r4.z = r6.x;
    // mul r1.xyw, r4.xyxz, l(0.250000, 0.250000, 0.000000, 0.250000)
    float stage16_x = r4.x * 0.25f;
    float stage16_y = r4.y * 0.25f;
    float stage16_w = r4.z * 0.25f;
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.w = stage16_w;
    // dp3 r2.x, r1.xywx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r2.x = dot(r1.xyw, float3(0.3f, 0.59f, 0.11f));
    // mad r2.xyz, -r4.xyzx, l(0.250000, 0.250000, 0.250000, 0.000000), r2.xxxx
    float stage17_x = -r4.x * 0.25f + r2.x;
    float stage17_y = -r4.y * 0.25f + r2.x;
    float stage17_z = -r4.z * 0.25f + r2.x;
    r2.x = stage17_x;
    r2.y = stage17_y;
    r2.z = stage17_z;
    // mad r1.xyw, cb0[3].yyyy, r2.xyxz, r1.xyxw
    float stage18_x = cb0[3].y * r2.x + r1.x;
    float stage18_y = cb0[3].y * r2.y + r1.y;
    float stage18_w = cb0[3].y * r2.z + r1.w;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.w = stage18_w;
    // lt r2.x, r0.w, l(0.000001)
    r2.x = asfloat((r0.w < 1e-06f) ? 0xffffffffu : 0u);
    // mul r2.y, r0.w, r0.w
    r2.y = r0.w * r0.w;
    // mul r2.y, r0.w, r2.y
    r2.y = r0.w * r2.y;
    // add r0.w, -r0.w, l(1.000000)
    r0.w = -r0.w + 1.0f;
    // lt r2.z, r0.w, l(0.000001)
    r2.z = asfloat((r0.w < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, r0.w, r0.w
    r0.w = r0.w * r0.w;
    // mul r0.w, r0.w, r2.y
    r0.w = r0.w * r2.y;
    // or r2.x, r2.x, r2.z
    r2.x = asfloat(asuint(r2.x) | asuint(r2.z));
    // movc r0.w, r2.x, l(0), r0.w
    r0.w = asuint(r2.x) != 0u ? asfloat(0u) : r0.w;
    // add r1.xyw, r0.wwww, r1.xyxw
    float stage19_x = r0.w + r1.x;
    float stage19_y = r0.w + r1.y;
    float stage19_w = r0.w + r1.w;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.w = stage19_w;
    // mad r1.xyw, cb0[1].xyxz, r1.xyxw, cb0[2].xyxz
    float stage20_x = cb0[1].x * r1.x + cb0[2].x;
    float stage20_y = cb0[1].y * r1.y + cb0[2].y;
    float stage20_w = cb0[1].z * r1.w + cb0[2].z;
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.w = stage20_w;
    // mad o0.xyz, r1.xywx, v5.wwww, v5.xyzx
    float stage21_x = r1.x * v5.w + v5.x;
    float stage21_y = r1.y * v5.w + v5.y;
    float stage21_z = r1.w * v5.w + v5.z;
    o0.x = stage21_x;
    o0.y = stage21_y;
    o0.z = stage21_z;
    // mov r0.x, r1.z
    r0.x = r1.z;
    // dp3 r0.w, r0.xyzx, r0.xyzx
    r0.w = dot(r0.xyz, r0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r0.xyz, r0.wwww, r0.xyzx
    float stage22_x = r0.w * r0.x;
    float stage22_y = r0.w * r0.y;
    float stage22_z = r0.w * r0.z;
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage23_x = r0.x / r0.w;
    float stage23_y = r0.y / r0.w;
    r0.x = stage23_x;
    r0.y = stage23_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage24_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage24_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage24_x;
    r1.y = stage24_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage25_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage25_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage25_x;
    r1.y = stage25_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage26_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage26_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage26_x;
    r1.y = stage26_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage27_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage27_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage27_x;
    r0.y = stage27_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage28_x = r0.x * 0.5f + 0.5f;
    float stage28_y = r0.y * 0.5f + 0.5f;
    o2.x = stage28_x;
    o2.y = stage28_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage29_z = 1.0f;
    float stage29_w = asfloat(0u);
    o2.z = stage29_z;
    o2.w = stage29_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage30_x = cb2[3].x;
    float stage30_y = cb2[3].y;
    float stage30_z = cb2[3].z;
    o3.x = stage30_x;
    o3.y = stage30_y;
    o3.z = stage30_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage31_x = asfloat(0u);
    float stage31_y = asfloat(0u);
    float stage31_z = asfloat(0u);
    float stage31_w = asfloat(0u);
    o4.x = stage31_x;
    o4.y = stage31_y;
    o4.z = stage31_z;
    o4.w = stage31_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage32_x = asfloat(0u);
    float stage32_y = asfloat(0u);
    float stage32_z = asfloat(0u);
    float stage32_w = asfloat(0u);
    o5.x = stage32_x;
    o5.y = stage32_y;
    o5.z = stage32_z;
    o5.w = stage32_w;

    Shade_Ue3_fx_j_me_splitline_01_1_ad_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
