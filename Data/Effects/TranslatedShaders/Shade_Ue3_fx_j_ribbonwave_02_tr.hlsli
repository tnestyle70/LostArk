// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_j_ribbonwave_02_tr_Constants0 : register(b0)
{
    float4 cb0[16];
};
cbuffer Shade_Ue3_fx_j_ribbonwave_02_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_j_ribbonwave_02_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_j_ribbonwave_02_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_j_ribbonwave_02_tr_OUTPUT Shade_Ue3_fx_j_ribbonwave_02_tr(Shade_Ue3_fx_j_ribbonwave_02_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.x, v2.x, cb0[10].w
    r0.x = v2.x * cb0[10].w;
    // mul r0.y, v2.y, cb0[11].x
    r0.y = v2.y * cb0[11].x;
    // add r0.xy, r0.xyxx, cb0[11].yzyy
    float stage1_x = r0.x + cb0[11].y;
    float stage1_y = r0.y + cb0[11].z;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xy, r0.xyxx, t0.xyzw, s1, l(0.000000)
    float4 fetch2 = t0.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch2.x;
    r0.y = fetch2.y;
    // mad r0.xy, r0.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage3_x = r0.x * 2.0f + -1.0f;
    float stage3_y = r0.y * 2.0f + -1.0f;
    r0.x = stage3_x;
    r0.y = stage3_y;
    // mul r0.xy, r0.xyxx, v4.xxxx
    float stage4_x = r0.x * v4.x;
    float stage4_y = r0.y * v4.x;
    r0.x = stage4_x;
    r0.y = stage4_y;
    // mad r0.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage5_x = r0.x * 0.5f + 0.5f;
    float stage5_y = r0.y * 0.5f + 0.5f;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // mul r0.z, v4.z, cb0[9].w
    r0.z = v4.z * cb0[9].w;
    // add r1.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage6_x = v2.x + -0.5f;
    float stage6_y = v2.y + -0.5f;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // mad r1.xy, r1.xyxx, cb0[9].xzxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage7_x = r1.x * cb0[9].x + 0.5f;
    float stage7_y = r1.y * cb0[9].z + 0.5f;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // mul r2.x, r0.z, r1.x
    r2.x = r0.z * r1.x;
    // mul r2.y, r1.y, cb0[10].x
    r2.y = r1.y * cb0[10].x;
    // add r0.zw, r2.xxxy, cb0[4].xxxy
    float stage8_z = r2.x + cb0[4].x;
    float stage8_w = r2.y + cb0[4].y;
    r0.z = stage8_z;
    r0.w = stage8_w;
    // mad r0.xy, r0.xyxx, l(1.000000, 0.000000, 0.000000, 0.000000), r0.zwzz
    float stage9_x = r0.x * 1.0f + r0.z;
    float stage9_y = r0.y * 0.0f + r0.w;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // mad r0.z, v2.x, l(3.000000), cb0[5].x
    r0.z = v2.x * 3.0f + cb0[5].x;
    // mul r0.z, r0.z, l(6.283185)
    r0.z = r0.z * 6.283185f;
    // sincos r0.z, null, r0.z
    float stage10_z = r0.z;
    r0.z = sin(stage10_z);
    // add r0.z, r0.z, l(0.500000)
    r0.z = r0.z + 0.5f;
    // mul r1.xy, v2.xyxx, l(1.000000, 1.500000, 0.000000, 0.000000)
    float stage11_x = v2.x * 1.0f;
    float stage11_y = v2.y * 1.5f;
    r1.x = stage11_x;
    r1.y = stage11_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t1.yzwx, s2, l(0.000000)
    float4 fetch12 = t1.SampleBias(s2, r1.xy, 0.0f);
    r0.w = fetch12.x;
    // add r0.w, r0.w, l(-0.500000)
    r0.w = r0.w + -0.5f;
    // mul r0.w, r0.w, cb0[13].x
    r0.w = r0.w * cb0[13].x;
    // add r1.x, v4.w, cb0[12].w
    r1.x = v4.w + cb0[12].w;
    // add r1.x, r1.x, l(-1.000000)
    r1.x = r1.x + -1.0f;
    // mad r1.y, r0.z, r1.x, r0.w
    r1.y = r0.z * r1.x + r0.w;
    // log r0.z, |v2.x|
    r0.z = log2(abs(v2.x));
    // mul r0.z, r0.z, cb0[11].w
    r0.z = r0.z * cb0[11].w;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // lt r0.w, |v2.x|, l(0.000001)
    r0.w = asfloat((abs(v2.x) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.z, r0.w, l(0), r0.z
    r0.z = asuint(r0.w) != 0u ? asfloat(0u) : r0.z;
    // mov r1.x, l(0)
    r1.x = asfloat(0u);
    // mad r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage13_x = r0.z * r1.x + r0.x;
    float stage13_y = r0.z * r1.y + r0.y;
    r0.x = stage13_x;
    r0.y = stage13_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s3, l(0.000000)
    float4 fetch14 = t2.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch14.x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[13].y
    r0.y = r0.y * cb0[13].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul_sat r0.y, r0.y, cb0[13].z
    r0.y = saturate(r0.y * cb0[13].z);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // add r0.yz, v2.xxyx, cb0[6].xxyx
    float stage15_y = v2.x + cb0[6].x;
    float stage15_z = v2.y + cb0[6].y;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // add r0.yz, r0.yyzy, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage16_y = r0.y + -0.5f;
    float stage16_z = r0.z + -0.5f;
    r0.y = stage16_y;
    r0.z = stage16_z;
    // mul r0.yz, r0.yyzy, r0.yyzy
    float stage17_y = r0.y * r0.y;
    float stage17_z = r0.z * r0.z;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // add r0.y, r0.z, r0.y
    r0.y = r0.z + r0.y;
    // add r0.y, -r0.y, l(1.000000)
    r0.y = -r0.y + 1.0f;
    // log r0.z, |r0.y|
    r0.z = log2(abs(r0.y));
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[14].x
    r0.z = r0.z * cb0[14].x;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // mul r0.z, r0.z, cb0[14].y
    r0.z = r0.z * cb0[14].y;
    // mul_sat r0.x, r0.z, r0.x
    r0.x = saturate(r0.z * r0.x);
    // movc r0.x, r0.y, l(0), r0.x
    r0.x = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // add r0.y, -v2.x, l(1.000000)
    r0.y = -v2.x + 1.0f;
    // mul r0.z, |r0.y|, |r0.y|
    r0.z = abs(r0.y) * abs(r0.y);
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, r0.z
    r0.z = r0.z * r0.z;
    // mul r0.z, r0.z, r0.z
    r0.z = r0.z * r0.z;
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r0.z, r0.x, r0.y
    r0.z = r0.x * r0.y;
    // mad r0.x, r0.z, l(7.000000), r0.x
    r0.x = r0.z * 7.0f + r0.x;
    // min r0.x, r0.x, l(1.000000)
    r0.x = min(r0.x, 1.0f);
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul r0.z, v2.x, cb0[14].w
    r0.z = v2.x * cb0[14].w;
    // mad r1.x, cb0[12].x, cb0[14].z, r0.z
    r1.x = cb0[12].x * cb0[14].z + r0.z;
    // mul r0.z, v2.y, cb0[15].x
    r0.z = v2.y * cb0[15].x;
    // mad r1.y, cb0[12].x, cb0[15].y, r0.z
    r1.y = cb0[12].x * cb0[15].y + r0.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.xyxx, t3.yzxw, s4, l(0.000000)
    float4 fetch18 = t3.SampleBias(s4, r1.xy, 0.0f);
    r0.z = fetch18.x;
    // add r0.z, r0.z, l(0.200000)
    r0.z = r0.z + 0.2f;
    // add r0.w, v4.y, -cb0[15].z
    r0.w = v4.y + -cb0[15].z;
    // add r0.w, -r0.w, l(1.000000)
    r0.w = -r0.w + 1.0f;
    // add r0.z, -r0.w, r0.z
    r0.z = -r0.w + r0.z;
    // mul_sat r0.z, r0.z, cb0[15].w
    r0.z = saturate(r0.z * cb0[15].w);
    // mul r0.x, r0.z, r0.x
    r0.x = r0.z * r0.x;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // mad r0.xz, v2.xxyx, cb0[7].xxyx, cb0[2].xxyx
    float stage19_x = v2.x * cb0[7].x + cb0[2].x;
    float stage19_z = v2.y * cb0[7].y + cb0[2].y;
    r0.x = stage19_x;
    r0.z = stage19_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xzw, r0.xzxx, t4.xwyz, s0, l(0.000000)
    float4 fetch20 = t4.SampleBias(s0, r0.xz, 0.0f);
    r0.x = fetch20.x;
    r0.z = fetch20.y;
    r0.w = fetch20.z;
    // dp3 r1.x, r0.xzwx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.xzw, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xzwx, r1.xxxx
    float stage21_x = -r0.x + r1.x;
    float stage21_y = -r0.z + r1.x;
    float stage21_z = -r0.w + r1.x;
    r1.x = stage21_x;
    r1.y = stage21_y;
    r1.z = stage21_z;
    // mad r0.xzw, cb0[8].xxxx, r1.xxyz, r0.xxzw
    float stage22_x = cb0[8].x * r1.x + r0.x;
    float stage22_z = cb0[8].x * r1.y + r0.z;
    float stage22_w = cb0[8].x * r1.z + r0.w;
    r0.x = stage22_x;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // max r0.xzw, |r0.xxzw|, l(0.000001, 0.000000, 0.000001, 0.000001)
    float stage23_x = max(abs(r0.x), 1e-06f);
    float stage23_z = max(abs(r0.z), 1e-06f);
    float stage23_w = max(abs(r0.w), 1e-06f);
    r0.x = stage23_x;
    r0.z = stage23_z;
    r0.w = stage23_w;
    // log r0.xzw, r0.xxzw
    float stage24_x = log2(r0.x);
    float stage24_z = log2(r0.z);
    float stage24_w = log2(r0.w);
    r0.x = stage24_x;
    r0.z = stage24_z;
    r0.w = stage24_w;
    // mul r0.xzw, r0.xxzw, cb0[8].yyyy
    float stage25_x = r0.x * cb0[8].y;
    float stage25_z = r0.z * cb0[8].y;
    float stage25_w = r0.w * cb0[8].y;
    r0.x = stage25_x;
    r0.z = stage25_z;
    r0.w = stage25_w;
    // exp r0.xzw, r0.xxzw
    float stage26_x = exp2(r0.x);
    float stage26_z = exp2(r0.z);
    float stage26_w = exp2(r0.w);
    r0.x = stage26_x;
    r0.z = stage26_z;
    r0.w = stage26_w;
    // mul r0.xzw, r0.xxzw, cb0[3].xxyz
    float stage27_x = r0.x * cb0[3].x;
    float stage27_z = r0.z * cb0[3].y;
    float stage27_w = r0.w * cb0[3].z;
    r0.x = stage27_x;
    r0.z = stage27_z;
    r0.w = stage27_w;
    // mad_sat r0.xyz, cb0[8].zzzz, r0.xzwx, r0.yyyy
    float stage28_x = saturate(cb0[8].z * r0.x + r0.y);
    float stage28_y = saturate(cb0[8].z * r0.z + r0.y);
    float stage28_z = saturate(cb0[8].z * r0.w + r0.y);
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage29_x = r0.x * v3.x + cb0[1].x;
    float stage29_y = r0.y * v3.y + cb0[1].y;
    float stage29_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage30_x = r0.x * v5.w + v5.x;
    float stage30_y = r0.y * v5.w + v5.y;
    float stage30_z = r0.z * v5.w + v5.z;
    o0.x = stage30_x;
    o0.y = stage30_y;
    o0.z = stage30_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage31_x = r0.x * v1.x;
    float stage31_y = r0.x * v1.y;
    float stage31_z = r0.x * v1.z;
    r0.x = stage31_x;
    r0.y = stage31_y;
    r0.z = stage31_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage32_x = r0.w * v0.z;
    float stage32_y = r0.w * v0.x;
    float stage32_z = r0.w * v0.y;
    r1.x = stage32_x;
    r1.y = stage32_y;
    r1.z = stage32_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage33_x = r0.x * r1.x;
    float stage33_y = r0.x * r1.y;
    float stage33_z = r0.x * r1.z;
    r0.x = stage33_x;
    r0.y = stage33_y;
    r0.z = stage33_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage34_x = r0.x / r0.w;
    float stage34_y = r0.y / r0.w;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage35_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage35_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage35_x;
    r1.y = stage35_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage36_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage36_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage36_x;
    r1.y = stage36_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage37_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage37_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage37_x;
    r1.y = stage37_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage38_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage38_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage38_x;
    r0.y = stage38_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage39_x = r0.x * 0.5f + 0.5f;
    float stage39_y = r0.y * 0.5f + 0.5f;
    o2.x = stage39_x;
    o2.y = stage39_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage40_z = 1.0f;
    float stage40_w = asfloat(0u);
    o2.z = stage40_z;
    o2.w = stage40_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage41_x = cb2[3].x;
    float stage41_y = cb2[3].y;
    float stage41_z = cb2[3].z;
    o3.x = stage41_x;
    o3.y = stage41_y;
    o3.z = stage41_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage42_x = asfloat(0u);
    float stage42_y = asfloat(0u);
    float stage42_z = asfloat(0u);
    float stage42_w = asfloat(0u);
    o4.x = stage42_x;
    o4.y = stage42_y;
    o4.z = stage42_z;
    o4.w = stage42_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage43_x = asfloat(0u);
    float stage43_y = asfloat(0u);
    float stage43_z = asfloat(0u);
    float stage43_w = asfloat(0u);
    o5.x = stage43_x;
    o5.y = stage43_y;
    o5.z = stage43_z;
    o5.w = stage43_w;

    Shade_Ue3_fx_j_ribbonwave_02_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
