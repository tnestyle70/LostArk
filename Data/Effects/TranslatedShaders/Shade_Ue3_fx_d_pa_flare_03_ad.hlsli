// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_flare_03_ad_Constants0 : register(b0)
{
    float4 cb0[2];
};

struct Shade_Ue3_fx_d_pa_flare_03_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v5;
};

struct Shade_Ue3_fx_d_pa_flare_03_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_d_pa_flare_03_ad_OUTPUT Shade_Ue3_fx_d_pa_flare_03_ad(Shade_Ue3_fx_d_pa_flare_03_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;

    // add r0.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // add r0.xy, r0.xyxx, r0.xyxx
    float stage2_x = r0.x + r0.x;
    float stage2_y = r0.y + r0.y;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // mad r0.z, -|r0.x|, |r0.y|, l(1.000000)
    r0.z = -(abs(r0.x)) * abs(r0.y) + 1.0f;
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // add r0.x, -r0.x, l(1.000000)
    r0.x = -r0.x + 1.0f;
    // max r0.x, r0.x, l(0.000000)
    r0.x = max(r0.x, 0.0f);
    // log r0.y, |r0.z|
    r0.y = log2(abs(r0.z));
    // lt r0.z, |r0.z|, l(0.000001)
    r0.z = asfloat((abs(r0.z) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, l(800.000000)
    r0.y = r0.y * 800.0f;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // movc r0.y, r0.z, l(0), r0.y
    r0.y = asuint(r0.z) != 0u ? asfloat(0u) : r0.y;
    // mul r0.z, r0.x, r0.x
    r0.z = r0.x * r0.x;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mul r0.w, r0.w, l(0.150000)
    r0.w = r0.w * 0.15f;
    // lt r1.x, r0.x, l(0.000001)
    r1.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mad r0.x, r0.y, l(2.000000), r0.x
    r0.x = r0.y * 2.0f + r0.x;
    // mad r1.yzw, r0.xxxx, v3.xxyz, cb0[1].xxyz
    float stage3_y = r0.x * v3.x + cb0[1].x;
    float stage3_z = r0.x * v3.y + cb0[1].y;
    float stage3_w = r0.x * v3.z + cb0[1].z;
    r1.y = stage3_y;
    r1.z = stage3_z;
    r1.w = stage3_w;
    // mul r1.yzw, r1.yyzw, v5.wwww
    float stage4_y = r1.y * v5.w;
    float stage4_z = r1.z * v5.w;
    float stage4_w = r1.w * v5.w;
    r1.y = stage4_y;
    r1.z = stage4_z;
    r1.w = stage4_w;
    // movc r0.xz, r1.xxxx, l(0,0,0,0), r0.wwzw
    float stage5_x = asuint(r1.x) != 0u ? asfloat(0u) : r0.w;
    float stage5_z = asuint(r1.x) != 0u ? asfloat(0u) : r0.z;
    r0.x = stage5_x;
    r0.z = stage5_z;
    // mad r0.x, r0.z, r0.y, r0.x
    r0.x = r0.z * r0.y + r0.x;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // mul o0.xyz, r0.xxxx, r1.yzwy
    float stage6_x = r0.x * r1.y;
    float stage6_y = r0.x * r1.z;
    float stage6_z = r0.x * r1.w;
    o0.x = stage6_x;
    o0.y = stage6_y;
    o0.z = stage6_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_d_pa_flare_03_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
