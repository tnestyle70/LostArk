// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_c_pa_symbol_01_tr_Constants0 : register(b0)
{
    float4 cb0[6];
};
cbuffer Shade_Ue3_fx_c_pa_symbol_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_c_pa_symbol_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_c_pa_symbol_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_c_pa_symbol_01_tr_OUTPUT Shade_Ue3_fx_c_pa_symbol_01_tr(Shade_Ue3_fx_c_pa_symbol_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.xyxx, cb0[2].xyxx
    float stage1_x = v2.x + cb0[2].x;
    float stage1_y = v2.y + cb0[2].y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch2 = t0.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch2.x;
    // mad r0.xyzw, r0.xxxx, l(0.030000, 0.030000, 0.100000, 0.100000), v2.xyxy
    float stage3_x = r0.x * 0.03f + v2.x;
    float stage3_y = r0.x * 0.03f + v2.y;
    float stage3_z = r0.x * 0.1f + v2.x;
    float stage3_w = r0.x * 0.1f + v2.y;
    r0.x = stage3_x;
    r0.y = stage3_y;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // add r0.zw, r0.zzzw, l(0.000000, 0.000000, -0.500000, -0.500000)
    float stage4_z = r0.z + -0.5f;
    float stage4_w = r0.w + -0.5f;
    r0.z = stage4_z;
    r0.w = stage4_w;
    // mul r1.xy, r0.zwzz, cb0[4].yyyy
    float stage5_x = r0.z * cb0[4].y;
    float stage5_y = r0.w * cb0[4].y;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // dp2 r0.z, r0.zwzz, r0.zwzz
    r0.z = dot(r0.zw, r0.zw);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // max r0.w, |r1.y|, |r1.x|
    r0.w = max(abs(r1.y), abs(r1.x));
    // div r0.w, l(1.000000, 1.000000, 1.000000, 1.000000), r0.w
    r0.w = 1.0f / r0.w;
    // min r1.z, |r1.y|, |r1.x|
    r1.z = min(abs(r1.y), abs(r1.x));
    // mul r0.w, r0.w, r1.z
    r0.w = r0.w * r1.z;
    // mul r1.z, r0.w, r0.w
    r1.z = r0.w * r0.w;
    // mad r1.w, r1.z, l(0.020835), l(-0.085133)
    r1.w = r1.z * 0.020835f + -0.085133f;
    // mad r1.w, r1.z, r1.w, l(0.180141)
    r1.w = r1.z * r1.w + 0.180141f;
    // mad r1.w, r1.z, r1.w, l(-0.330299)
    r1.w = r1.z * r1.w + -0.330299f;
    // mad r1.z, r1.z, r1.w, l(0.999866)
    r1.z = r1.z * r1.w + 0.999866f;
    // mul r1.w, r0.w, r1.z
    r1.w = r0.w * r1.z;
    // mad r1.w, r1.w, l(-2.000000), l(1.570796)
    r1.w = r1.w * -2.0f + 1.570796f;
    // lt r2.x, |r1.y|, |r1.x|
    r2.x = asfloat((abs(r1.y) < abs(r1.x)) ? 0xffffffffu : 0u);
    // and r1.w, r1.w, r2.x
    r1.w = asfloat(asuint(r1.w) & asuint(r2.x));
    // mad r0.w, r0.w, r1.z, r1.w
    r0.w = r0.w * r1.z + r1.w;
    // lt r1.z, r1.y, -r1.y
    r1.z = asfloat((r1.y < -r1.y) ? 0xffffffffu : 0u);
    // and r1.z, r1.z, l(0xc0490fdb)
    r1.z = asfloat(asuint(r1.z) & asuint(-3.1415927410125732f));
    // add r0.w, r0.w, r1.z
    r0.w = r0.w + r1.z;
    // min r1.z, r1.y, r1.x
    r1.z = min(r1.y, r1.x);
    // max r1.x, r1.y, r1.x
    r1.x = max(r1.y, r1.x);
    // ge r1.x, r1.x, -r1.x
    r1.x = asfloat((r1.x >= -r1.x) ? 0xffffffffu : 0u);
    // lt r1.y, r1.z, -r1.z
    r1.y = asfloat((r1.z < -r1.z) ? 0xffffffffu : 0u);
    // and r1.x, r1.x, r1.y
    r1.x = asfloat(asuint(r1.x) & asuint(r1.y));
    // movc r0.w, r1.x, -r0.w, r0.w
    r0.w = asuint(r1.x) != 0u ? -r0.w : r0.w;
    // mul r1.x, cb0[4].w, l(3.141592)
    r1.x = cb0[4].w * 3.141592f;
    // div r0.w, r0.w, r1.x
    r0.w = r0.w / r1.x;
    // add r0.w, r0.w, l(1.000000)
    r0.w = r0.w + 1.0f;
    // mul r1.x, r0.w, l(0.500000)
    r1.x = r0.w * 0.5f;
    // log r0.w, r0.z
    r0.w = log2(r0.z);
    // lt r0.z, r0.z, l(0.000001)
    r0.z = asfloat((r0.z < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, r0.w, cb0[5].x
    r0.w = r0.w * cb0[5].x;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // movc r1.y, r0.z, l(0), r0.w
    r1.y = asuint(r0.z) != 0u ? asfloat(0u) : r0.w;
    // mul r0.w, v4.x, l(0.300000)
    r0.w = v4.x * 0.3f;
    // mov r0.z, l(0)
    r0.z = asfloat(0u);
    // add r0.zw, r1.xxxy, r0.zzzw
    float stage6_z = r1.x + r0.z;
    float stage6_w = r1.y + r0.w;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // mad r1.xy, v4.xxxx, l(0.010000, 0.250000, 0.000000, 0.000000), r1.xyxx
    float stage7_x = v4.x * 0.01f + r1.x;
    float stage7_y = v4.x * 0.25f + r1.y;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t3.xyzw, s1, l(0.000000)
    float4 fetch8 = t3.SampleBias(s1, r1.xy, 0.0f);
    r1.x = fetch8.x;
    r1.y = fetch8.y;
    r1.z = fetch8.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.zwzz, t4.xyzw, s2, l(0.000000)
    float4 fetch9 = t4.SampleBias(s2, r0.zw, 0.0f);
    r2.x = fetch9.x;
    r2.y = fetch9.y;
    r2.z = fetch9.z;
    // mad r1.xyz, r1.xyzx, l(3.000000, 3.000000, 3.000000, 0.000000), r2.xyzx
    float stage10_x = r1.x * 3.0f + r2.x;
    float stage10_y = r1.y * 3.0f + r2.y;
    float stage10_z = r1.z * 3.0f + r2.z;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // add r0.zw, v2.yyyx, l(0.000000, 0.000000, -0.500000, -0.500000)
    float stage11_z = v2.y + -0.5f;
    float stage11_w = v2.x + -0.5f;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // dp2 r1.w, r0.zwzz, r0.zwzz
    r1.w = dot(r0.zw, r0.zw);
    // mad r2.x, -r1.w, l(1.500000), l(1.000000)
    r2.x = -r1.w * 1.5f + 1.0f;
    // sqrt r1.w, r1.w
    r1.w = sqrt(r1.w);
    // max r2.x, r2.x, l(0.000000)
    r2.x = max(r2.x, 0.0f);
    // log r2.y, r2.x
    r2.y = log2(r2.x);
    // lt r2.x, r2.x, l(0.000001)
    r2.x = asfloat((r2.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r2.y, r2.y, l(7.000000)
    r2.y = r2.y * 7.0f;
    // exp r2.y, r2.y
    r2.y = exp2(r2.y);
    // movc r2.x, r2.x, l(0), r2.y
    r2.x = asuint(r2.x) != 0u ? asfloat(0u) : r2.y;
    // div r0.zw, r0.zzzw, r1.wwww
    float stage12_z = r0.z / r1.w;
    float stage12_w = r0.w / r1.w;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mad r1.w, -r1.w, l(2.000000), l(1.000000)
    r1.w = -r1.w * 2.0f + 1.0f;
    // mul r1.w, r1.w, l(0.909091)
    r1.w = r1.w * 0.909091f;
    // max r1.w, r1.w, l(0.000000)
    r1.w = max(r1.w, 0.0f);
    // mad r0.zw, r0.zzzw, l(0.000000, 0.000000, 0.500000, -0.500000), l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage13_z = r0.z * 0.5f + 0.5f;
    float stage13_w = r0.w * -0.5f + 0.5f;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // add r0.zw, -r0.xxxy, r0.zzzw
    float stage14_z = -r0.x + r0.z;
    float stage14_w = -r0.y + r0.w;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // mul r0.zw, r0.zzzw, r2.xxxx
    float stage15_z = r0.z * r2.x;
    float stage15_w = r0.w * r2.x;
    r0.z = stage15_z;
    r0.w = stage15_w;
    // add r2.xy, -v4.zyzz, l(1.000000, 1.000000, 0.000000, 0.000000)
    float stage16_x = -v4.z + 1.0f;
    float stage16_y = -v4.y + 1.0f;
    r2.x = stage16_x;
    r2.y = stage16_y;
    // mad r0.xy, r2.xxxx, r0.zwzz, r0.xyxx
    float stage17_x = r2.x * r0.z + r0.x;
    float stage17_y = r2.x * r0.w + r0.y;
    r0.x = stage17_x;
    r0.y = stage17_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyzw, r0.xyxx, t1.xyzw, s3, l(0.000000)
    float4 fetch18 = t1.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch18.x;
    r0.y = fetch18.y;
    r0.z = fetch18.z;
    r0.w = fetch18.w;
    // mul r0.w, r0.w, cb0[5].y
    r0.w = r0.w * cb0[5].y;
    // mul r3.xyzw, v2.xyxy, l(5.000000, 5.000000, 3.000000, 3.000000)
    float stage19_x = v2.x * 5.0f;
    float stage19_y = v2.y * 5.0f;
    float stage19_z = v2.x * 3.0f;
    float stage19_w = v2.y * 3.0f;
    r3.x = stage19_x;
    r3.y = stage19_y;
    r3.z = stage19_z;
    r3.w = stage19_w;
    // mad r3.xyzw, v4.xxxx, l(0.050000, 0.010000, -0.010000, -0.040000), r3.xyzw
    float stage20_x = v4.x * 0.05f + r3.x;
    float stage20_y = v4.x * 0.01f + r3.y;
    float stage20_z = v4.x * -0.01f + r3.z;
    float stage20_w = v4.x * -0.04f + r3.w;
    r3.x = stage20_x;
    r3.y = stage20_y;
    r3.z = stage20_z;
    r3.w = stage20_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.x, r3.xyxx, t2.xyzw, s4, l(0.000000)
    float4 fetch21 = t2.SampleBias(s4, r3.xy, 0.0f);
    r2.x = fetch21.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.z, r3.zwzz, t2.yzxw, s4, l(0.000000)
    float4 fetch22 = t2.SampleBias(s4, r3.zw, 0.0f);
    r2.z = fetch22.x;
    // add r2.x, r2.z, r2.x
    r2.x = r2.z + r2.x;
    // mul r1.w, r1.w, r2.x
    r1.w = r1.w * r2.x;
    // add_sat r2.x, -r2.y, r2.x
    r2.x = saturate(-r2.y + r2.x);
    // round_pi r2.x, r2.x
    r2.x = ceil(r2.x);
    // mad r1.w, r1.w, l(24.000000), l(0.150000)
    r1.w = r1.w * 24.0f + 0.15f;
    // mul r2.y, r1.w, l(1.300000)
    r2.y = r1.w * 1.3f;
    // round_ni r1.w, r1.w
    r1.w = floor(r1.w);
    // sqrt r2.y, r2.y
    r2.y = sqrt(r2.y);
    // round_ni r2.y, r2.y
    r2.y = floor(r2.y);
    // mul r2.z, r2.y, l(8.000000)
    r2.z = r2.y * 8.0f;
    // add_sat r1.w, -r1.w, r2.y
    r1.w = saturate(-r1.w + r2.y);
    // min r2.y, r2.z, l(1.000000)
    r2.y = min(r2.z, 1.0f);
    // mul r2.y, r0.w, r2.y
    r2.y = r0.w * r2.y;
    // mul r0.w, r0.w, v3.w
    r0.w = r0.w * v3.w;
    // mul r0.w, r2.x, r0.w
    r0.w = r2.x * r0.w;
    // mul o0.w, r0.w, cb0[0].x
    o0.w = r0.w * cb0[0].x;
    // mul r0.w, r1.w, r2.y
    r0.w = r1.w * r2.y;
    // mul r2.xyz, r0.wwww, cb0[3].xyzx
    float stage23_x = r0.w * cb0[3].x;
    float stage23_y = r0.w * cb0[3].y;
    float stage23_z = r0.w * cb0[3].z;
    r2.x = stage23_x;
    r2.y = stage23_y;
    r2.z = stage23_z;
    // mad r0.xyz, r1.xyzx, r0.xyzx, r2.xyzx
    float stage24_x = r1.x * r0.x + r2.x;
    float stage24_y = r1.y * r0.y + r2.y;
    float stage24_z = r1.z * r0.z + r2.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage25_x = r0.x * v3.x + cb0[1].x;
    float stage25_y = r0.y * v3.y + cb0[1].y;
    float stage25_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage26_x = r0.x * v5.w + v5.x;
    float stage26_y = r0.y * v5.w + v5.y;
    float stage26_z = r0.z * v5.w + v5.z;
    o0.x = stage26_x;
    o0.y = stage26_y;
    o0.z = stage26_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage27_x = r0.x * v1.x;
    float stage27_y = r0.x * v1.y;
    float stage27_z = r0.x * v1.z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage28_x = r0.w * v0.z;
    float stage28_y = r0.w * v0.x;
    float stage28_z = r0.w * v0.y;
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage29_x = r0.x * r1.x;
    float stage29_y = r0.x * r1.y;
    float stage29_z = r0.x * r1.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage30_x = r0.x / r0.w;
    float stage30_y = r0.y / r0.w;
    r0.x = stage30_x;
    r0.y = stage30_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage31_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage31_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage31_x;
    r1.y = stage31_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage32_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage32_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage32_x;
    r1.y = stage32_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage33_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage33_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage33_x;
    r1.y = stage33_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage34_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage34_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage35_x = r0.x * 0.5f + 0.5f;
    float stage35_y = r0.y * 0.5f + 0.5f;
    o2.x = stage35_x;
    o2.y = stage35_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage36_z = 1.0f;
    float stage36_w = asfloat(0u);
    o2.z = stage36_z;
    o2.w = stage36_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage37_x = cb2[3].x;
    float stage37_y = cb2[3].y;
    float stage37_z = cb2[3].z;
    o3.x = stage37_x;
    o3.y = stage37_y;
    o3.z = stage37_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage38_x = asfloat(0u);
    float stage38_y = asfloat(0u);
    float stage38_z = asfloat(0u);
    float stage38_w = asfloat(0u);
    o4.x = stage38_x;
    o4.y = stage38_y;
    o4.z = stage38_z;
    o4.w = stage38_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage39_x = asfloat(0u);
    float stage39_y = asfloat(0u);
    float stage39_z = asfloat(0u);
    float stage39_w = asfloat(0u);
    o5.x = stage39_x;
    o5.y = stage39_y;
    o5.z = stage39_z;
    o5.w = stage39_w;

    Shade_Ue3_fx_c_pa_symbol_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
