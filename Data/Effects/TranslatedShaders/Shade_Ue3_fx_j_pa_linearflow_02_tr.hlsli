// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_j_pa_linearflow_02_tr_Constants0 : register(b0)
{
    float4 cb0[30];
};
cbuffer Shade_Ue3_fx_j_pa_linearflow_02_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);

struct Shade_Ue3_fx_j_pa_linearflow_02_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
};

struct Shade_Ue3_fx_j_pa_linearflow_02_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_j_pa_linearflow_02_tr_OUTPUT Shade_Ue3_fx_j_pa_linearflow_02_tr(Shade_Ue3_fx_j_pa_linearflow_02_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mad r0.xy, v4.wzww, cb0[11].xyxx, cb0[12].xyxx
    float stage1_x = v4.w * cb0[11].x + cb0[12].x;
    float stage1_y = v4.z * cb0[11].y + cb0[12].y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // add r1.xyzw, r0.xyxy, l(0.200000, 0.000000, 0.000000, 0.200000)
    float stage2_x = r0.x + 0.2f;
    float stage2_y = r0.y + 0.0f;
    float stage2_z = r0.x + 0.0f;
    float stage2_w = r0.y + 0.2f;
    r1.x = stage2_x;
    r1.y = stage2_y;
    r1.z = stage2_z;
    r1.w = stage2_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s4, l(0.000000)
    float4 fetch3 = t2.SampleBias(s4, r0.xy, 0.0f);
    r0.x = fetch3.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.xyxx, t2.yxzw, s4, l(0.000000)
    float4 fetch4 = t2.SampleBias(s4, r1.xy, 0.0f);
    r0.y = fetch4.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.zwzz, t2.yzxw, s4, l(0.000000)
    float4 fetch5 = t2.SampleBias(s4, r1.zw, 0.0f);
    r0.z = fetch5.x;
    // add r1.xy, -r0.xxxx, r0.yzyy
    float stage6_x = -r0.x + r0.y;
    float stage6_y = -r0.x + r0.z;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // mul r0.xy, r1.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage7_x = r1.x * 8.0f;
    float stage7_y = r1.y * 8.0f;
    r0.x = stage7_x;
    r0.y = stage7_y;
    // mov r0.z, l(0)
    r0.z = asfloat(0u);
    // add r0.xyz, -r0.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage8_x = -r0.x + 0.0f;
    float stage8_y = -r0.y + 0.0f;
    float stage8_z = -r0.z + 1.0f;
    r0.x = stage8_x;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // dp3 r0.z, r0.xyzx, r0.xyzx
    r0.z = dot(r0.xyz, r0.xyz);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // div r0.xy, r0.xyxx, r0.zzzz
    float stage9_x = r0.x / r0.z;
    float stage9_y = r0.y / r0.z;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // mad r0.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage10_x = r0.x * 0.5f + 0.5f;
    float stage10_y = r0.y * 0.5f + 0.5f;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // add r0.z, cb0[3].y, cb0[22].w
    r0.z = cb0[3].y + cb0[22].w;
    // add r1.xy, v4.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage11_x = v4.x + -0.5f;
    float stage11_y = v4.y + -0.5f;
    r1.x = stage11_x;
    r1.y = stage11_y;
    // mad r1.zw, cb0[21].wwww, r1.xxxy, l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage12_z = cb0[21].w * r1.x + 0.5f;
    float stage12_w = cb0[21].w * r1.y + 0.5f;
    r1.z = stage12_z;
    r1.w = stage12_w;
    // mad r2.y, r1.w, cb0[22].y, r0.z
    r2.y = r1.w * cb0[22].y + r0.z;
    // mad r2.x, r1.z, cb0[22].x, cb0[22].z
    r2.x = r1.z * cb0[22].x + cb0[22].z;
    // add r0.zw, r2.xxxy, l(0.000000, 0.000000, -0.500000, -1.500000)
    float stage13_z = r2.x + -0.5f;
    float stage13_w = r2.y + -1.5f;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // dp2 r2.x, cb0[9].xyxx, r0.zwzz
    r2.x = dot(cb0[9].xy, r0.zw);
    // dp2 r2.y, cb0[10].xyxx, r0.zwzz
    r2.y = dot(cb0[10].xy, r0.zw);
    // add r0.zw, r2.xxxy, l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage14_z = r2.x + 0.5f;
    float stage14_w = r2.y + 0.5f;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // mul r1.z, cb0[3].w, cb0[23].z
    r1.z = cb0[3].w * cb0[23].z;
    // mad r0.xy, r1.zzzz, r0.xyxx, r0.zwzz
    float stage15_x = r1.z * r0.x + r0.z;
    float stage15_y = r1.z * r0.y + r0.w;
    r0.x = stage15_x;
    r0.y = stage15_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t3.xyzw, s3, l(-1.000000)
    float4 fetch16 = t3.SampleLevel(s3, r0.xy, -1.0f);
    r0.x = fetch16.x;
    r0.y = fetch16.y;
    r0.z = fetch16.z;
    // mad r0.xyzw, cb0[24].xxxx, -r0.xxyz, r0.xxyz
    float stage17_x = cb0[24].x * -r0.x + r0.x;
    float stage17_y = cb0[24].x * -r0.x + r0.x;
    float stage17_z = cb0[24].x * -r0.y + r0.y;
    float stage17_w = cb0[24].x * -r0.z + r0.z;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // mul r0.xyzw, r0.xyzw, cb0[24].yyyy
    float stage18_x = r0.x * cb0[24].y;
    float stage18_y = r0.y * cb0[24].y;
    float stage18_z = r0.z * cb0[24].y;
    float stage18_w = r0.w * cb0[24].y;
    r0.x = stage18_x;
    r0.y = stage18_y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // max r0.xyzw, |r0.xyzw|, l(0.000001, 0.000001, 0.000001, 0.000001)
    float stage19_x = max(abs(r0.x), 1e-06f);
    float stage19_y = max(abs(r0.y), 1e-06f);
    float stage19_z = max(abs(r0.z), 1e-06f);
    float stage19_w = max(abs(r0.w), 1e-06f);
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // log r0.xyzw, r0.xyzw
    float stage20_x = log2(r0.x);
    float stage20_y = log2(r0.y);
    float stage20_z = log2(r0.z);
    float stage20_w = log2(r0.w);
    r0.x = stage20_x;
    r0.y = stage20_y;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // mul r0.xyzw, r0.xyzw, cb0[24].zzzz
    float stage21_x = r0.x * cb0[24].z;
    float stage21_y = r0.y * cb0[24].z;
    float stage21_z = r0.z * cb0[24].z;
    float stage21_w = r0.w * cb0[24].z;
    r0.x = stage21_x;
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // exp r0.xyzw, r0.xyzw
    float stage22_x = exp2(r0.x);
    float stage22_y = exp2(r0.y);
    float stage22_z = exp2(r0.z);
    float stage22_w = exp2(r0.w);
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // mad r1.zw, v4.wwwz, cb0[6].xxxy, cb0[7].xxxy
    float stage23_z = v4.w * cb0[6].x + cb0[7].x;
    float stage23_w = v4.z * cb0[6].y + cb0[7].y;
    r1.z = stage23_z;
    r1.w = stage23_w;
    // add r2.xyzw, r1.zwzw, l(0.200000, 0.000000, 0.000000, 0.200000)
    float stage24_x = r1.z + 0.2f;
    float stage24_y = r1.w + 0.0f;
    float stage24_z = r1.z + 0.0f;
    float stage24_w = r1.w + 0.2f;
    r2.x = stage24_x;
    r2.y = stage24_y;
    r2.z = stage24_z;
    r2.w = stage24_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.z, r1.zwzz, t0.yzxw, s2, l(0.000000)
    float4 fetch25 = t0.SampleBias(s2, r1.zw, 0.0f);
    r1.z = fetch25.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.w, r2.xyxx, t0.yzwx, s2, l(0.000000)
    float4 fetch26 = t0.SampleBias(s2, r2.xy, 0.0f);
    r1.w = fetch26.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.x, r2.zwzz, t0.xyzw, s2, l(0.000000)
    float4 fetch27 = t0.SampleBias(s2, r2.zw, 0.0f);
    r2.x = fetch27.x;
    // add r2.y, -r1.z, r2.x
    r2.y = -r1.z + r2.x;
    // add r2.x, -r1.z, r1.w
    r2.x = -r1.z + r1.w;
    // mul r2.xy, r2.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage28_x = r2.x * 8.0f;
    float stage28_y = r2.y * 8.0f;
    r2.x = stage28_x;
    r2.y = stage28_y;
    // mov r2.z, l(0)
    r2.z = asfloat(0u);
    // add r2.xyz, -r2.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage29_x = -r2.x + 0.0f;
    float stage29_y = -r2.y + 0.0f;
    float stage29_z = -r2.z + 1.0f;
    r2.x = stage29_x;
    r2.y = stage29_y;
    r2.z = stage29_z;
    // dp3 r1.z, r2.xyzx, r2.xyzx
    r1.z = dot(r2.xyz, r2.xyz);
    // sqrt r1.z, r1.z
    r1.z = sqrt(r1.z);
    // div r1.zw, r2.xxxy, r1.zzzz
    float stage30_z = r2.x / r1.z;
    float stage30_w = r2.y / r1.z;
    r1.z = stage30_z;
    r1.w = stage30_w;
    // mad r1.zw, r1.zzzw, l(0.000000, 0.000000, 0.500000, 0.500000), l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage31_z = r1.z * 0.5f + 0.5f;
    float stage31_w = r1.w * 0.5f + 0.5f;
    r1.z = stage31_z;
    r1.w = stage31_w;
    // add r2.x, cb0[3].x, cb0[18].x
    r2.x = cb0[3].x + cb0[18].x;
    // mad r2.yz, cb0[17].xxxx, r1.xxyx, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage32_y = cb0[17].x * r1.x + 0.5f;
    float stage32_z = cb0[17].x * r1.y + 0.5f;
    r2.y = stage32_y;
    r2.z = stage32_z;
    // mad r3.y, r2.z, cb0[17].z, r2.x
    r3.y = r2.z * cb0[17].z + r2.x;
    // mad r3.x, r2.y, cb0[17].y, cb0[17].w
    r3.x = r2.y * cb0[17].y + cb0[17].w;
    // add r2.xy, r3.xyxx, l(-0.500000, -1.500000, 0.000000, 0.000000)
    float stage33_x = r3.x + -0.5f;
    float stage33_y = r3.y + -1.5f;
    r2.x = stage33_x;
    r2.y = stage33_y;
    // dp2 r3.x, cb0[4].xyxx, r2.xyxx
    r3.x = dot(cb0[4].xy, r2.xy);
    // dp2 r3.y, cb0[5].xyxx, r2.xyxx
    r3.y = dot(cb0[5].xy, r2.xy);
    // add r2.xy, r3.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage34_x = r3.x + 0.5f;
    float stage34_y = r3.y + 0.5f;
    r2.x = stage34_x;
    r2.y = stage34_y;
    // mul r2.z, cb0[3].z, cb0[19].x
    r2.z = cb0[3].z * cb0[19].x;
    // mad r1.zw, r2.zzzz, r1.zzzw, r2.xxxy
    float stage35_z = r2.z * r1.z + r2.x;
    float stage35_w = r2.z * r1.w + r2.y;
    r1.z = stage35_z;
    r1.w = stage35_w;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r1.zwzz, t1.xyzw, s1, l(-1.000000)
    float4 fetch36 = t1.SampleLevel(s1, r1.zw, -1.0f);
    r2.x = fetch36.x;
    r2.y = fetch36.y;
    r2.z = fetch36.z;
    // mad r2.xyzw, cb0[19].zzzz, -r2.xxyz, r2.xxyz
    float stage37_x = cb0[19].z * -r2.x + r2.x;
    float stage37_y = cb0[19].z * -r2.x + r2.x;
    float stage37_z = cb0[19].z * -r2.y + r2.y;
    float stage37_w = cb0[19].z * -r2.z + r2.z;
    r2.x = stage37_x;
    r2.y = stage37_y;
    r2.z = stage37_z;
    r2.w = stage37_w;
    // mul r2.xyzw, r2.xyzw, cb0[19].wwww
    float stage38_x = r2.x * cb0[19].w;
    float stage38_y = r2.y * cb0[19].w;
    float stage38_z = r2.z * cb0[19].w;
    float stage38_w = r2.w * cb0[19].w;
    r2.x = stage38_x;
    r2.y = stage38_y;
    r2.z = stage38_z;
    r2.w = stage38_w;
    // max r2.xyzw, |r2.xyzw|, l(0.000001, 0.000001, 0.000001, 0.000001)
    float stage39_x = max(abs(r2.x), 1e-06f);
    float stage39_y = max(abs(r2.y), 1e-06f);
    float stage39_z = max(abs(r2.z), 1e-06f);
    float stage39_w = max(abs(r2.w), 1e-06f);
    r2.x = stage39_x;
    r2.y = stage39_y;
    r2.z = stage39_z;
    r2.w = stage39_w;
    // log r2.xyzw, r2.xyzw
    float stage40_x = log2(r2.x);
    float stage40_y = log2(r2.y);
    float stage40_z = log2(r2.z);
    float stage40_w = log2(r2.w);
    r2.x = stage40_x;
    r2.y = stage40_y;
    r2.z = stage40_z;
    r2.w = stage40_w;
    // mul r2.xyzw, r2.xyzw, cb0[20].xxxx
    float stage41_x = r2.x * cb0[20].x;
    float stage41_y = r2.y * cb0[20].x;
    float stage41_z = r2.z * cb0[20].x;
    float stage41_w = r2.w * cb0[20].x;
    r2.x = stage41_x;
    r2.y = stage41_y;
    r2.z = stage41_z;
    r2.w = stage41_w;
    // exp r2.xyzw, r2.xyzw
    float stage42_x = exp2(r2.x);
    float stage42_y = exp2(r2.y);
    float stage42_z = exp2(r2.z);
    float stage42_w = exp2(r2.w);
    r2.x = stage42_x;
    r2.y = stage42_y;
    r2.z = stage42_z;
    r2.w = stage42_w;
    // add r0.x, r0.x, -r2.x
    r0.x = r0.x + -r2.x;
    // mad r0.x, cb0[25].z, r0.x, r2.x
    r0.x = cb0[25].z * r0.x + r2.x;
    // max r0.x, |r0.x|, l(0.000001)
    r0.x = max(abs(r0.x), 1e-06f);
    // log r0.x, r0.x
    r0.x = log2(r0.x);
    // mul r0.x, r0.x, cb0[25].w
    r0.x = r0.x * cb0[25].w;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul r0.x, r0.x, cb0[26].x
    r0.x = r0.x * cb0[26].x;
    // mad r1.z, cb0[26].y, l(10.000000), l(10.000000)
    r1.z = cb0[26].y * 10.0f + 10.0f;
    // dp2 r1.w, r1.xyxx, r1.xyxx
    r1.w = dot(r1.xy, r1.xy);
    // add r1.w, -r1.w, l(1.000000)
    r1.w = -r1.w + 1.0f;
    // log r2.x, |r1.w|
    r2.x = log2(abs(r1.w));
    // lt r1.w, |r1.w|, l(0.000001)
    r1.w = asfloat((abs(r1.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.z, r1.z, r2.x
    r1.z = r1.z * r2.x;
    // exp r1.z, r1.z
    r1.z = exp2(r1.z);
    // mul r1.z, r1.z, cb0[26].z
    r1.z = r1.z * cb0[26].z;
    // movc r1.z, r1.w, l(0), r1.z
    r1.z = asuint(r1.w) != 0u ? asfloat(0u) : r1.z;
    // mul r0.x, r0.x, r1.z
    r0.x = r0.x * r1.z;
    // dp2 r3.x, cb0[14].xyxx, r1.xyxx
    r3.x = dot(cb0[14].xy, r1.xy);
    // dp2 r3.y, cb0[15].xyxx, r1.xyxx
    r3.y = dot(cb0[15].xy, r1.xy);
    // add r1.xy, r3.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage43_x = r3.x + 0.5f;
    float stage43_y = r3.y + 0.5f;
    r1.x = stage43_x;
    r1.y = stage43_y;
    // mul r1.xy, r1.xyxx, cb0[27].xyxx
    float stage44_x = r1.x * cb0[27].x;
    float stage44_y = r1.y * cb0[27].y;
    r1.x = stage44_x;
    r1.y = stage44_y;
    // mad r3.x, cb0[18].z, cb0[26].w, r1.x
    r3.x = cb0[18].z * cb0[26].w + r1.x;
    // mad r3.y, cb0[18].z, cb0[28].w, r1.y
    r3.y = cb0[18].z * cb0[28].w + r1.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r3.xyxx, t4.xyzw, s5, l(0.000000)
    float4 fetch45 = t4.SampleBias(s5, r3.xy, 0.0f);
    r1.x = fetch45.x;
    // add r1.x, r1.x, l(0.200000)
    r1.x = r1.x + 0.2f;
    // add r1.y, -cb0[1].w, l(1.000000)
    r1.y = -cb0[1].w + 1.0f;
    // add r1.x, -r1.y, r1.x
    r1.x = -r1.y + r1.x;
    // mul_sat r1.x, r1.x, cb0[29].x
    r1.x = saturate(r1.x * cb0[29].x);
    // mul r1.x, r1.x, cb0[1].w
    r1.x = r1.x * cb0[1].w;
    // mul_sat r0.x, r0.x, r1.x
    r0.x = saturate(r0.x * r1.x);
    // dp3 r1.x, v6.xyzx, v6.xyzx
    r1.x = dot(v6.xyz, v6.xyz);
    // rsq r1.x, r1.x
    r1.x = rsqrt(r1.x);
    // mul r1.x, r1.x, v6.z
    r1.x = r1.x * v6.z;
    // log r1.y, |r1.x|
    r1.y = log2(abs(r1.x));
    // lt r1.x, |r1.x|, l(0.000001)
    r1.x = asfloat((abs(r1.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.y, r1.y, cb0[29].y
    r1.y = r1.y * cb0[29].y;
    // exp r1.y, r1.y
    r1.y = exp2(r1.y);
    // mul r0.x, r0.x, r1.y
    r0.x = r0.x * r1.y;
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // movc o0.w, r1.x, l(0), r0.x
    o0.w = asuint(r1.x) != 0u ? asfloat(0u) : r0.x;
    // dp3 r0.x, r2.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.x = dot(r2.yzw, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r2.yzwy, r0.xxxx
    float stage46_x = -r2.y + r0.x;
    float stage46_y = -r2.z + r0.x;
    float stage46_z = -r2.w + r0.x;
    r1.x = stage46_x;
    r1.y = stage46_y;
    r1.z = stage46_z;
    // mad r1.xyz, cb0[20].yyyy, r1.xyzx, r2.yzwy
    float stage47_x = cb0[20].y * r1.x + r2.y;
    float stage47_y = cb0[20].y * r1.y + r2.z;
    float stage47_z = cb0[20].y * r1.z + r2.w;
    r1.x = stage47_x;
    r1.y = stage47_y;
    r1.z = stage47_z;
    // mad r2.xy, v4.xyxx, cb0[16].xyxx, l(1.000000, 1.000000, 0.000000, 0.000000)
    float stage48_x = v4.x * cb0[16].x + 1.0f;
    float stage48_y = v4.y * cb0[16].y + 1.0f;
    r2.x = stage48_x;
    r2.y = stage48_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r2.xyxx, t5.xyzw, s0, l(-1.000000)
    float4 fetch49 = t5.SampleLevel(s0, r2.xy, -1.0f);
    r2.x = fetch49.x;
    r2.y = fetch49.y;
    r2.z = fetch49.z;
    // dp3 r0.x, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.x = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r3.xyz, -r2.xyzx, r0.xxxx
    float stage50_x = -r2.x + r0.x;
    float stage50_y = -r2.y + r0.x;
    float stage50_z = -r2.z + r0.x;
    r3.x = stage50_x;
    r3.y = stage50_y;
    r3.z = stage50_z;
    // mad r2.xyz, cb0[16].zzzz, r3.xyzx, r2.xyzx
    float stage51_x = cb0[16].z * r3.x + r2.x;
    float stage51_y = cb0[16].z * r3.y + r2.y;
    float stage51_z = cb0[16].z * r3.z + r2.z;
    r2.x = stage51_x;
    r2.y = stage51_y;
    r2.z = stage51_z;
    // mul r1.xyz, r1.xyzx, r2.xyzx
    float stage52_x = r1.x * r2.x;
    float stage52_y = r1.y * r2.y;
    float stage52_z = r1.z * r2.z;
    r1.x = stage52_x;
    r1.y = stage52_y;
    r1.z = stage52_z;
    // dp3 r0.x, r0.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.x = dot(r0.yzw, float3(0.3f, 0.59f, 0.11f));
    // add r3.xyz, -r0.yzwy, r0.xxxx
    float stage53_x = -r0.y + r0.x;
    float stage53_y = -r0.z + r0.x;
    float stage53_z = -r0.w + r0.x;
    r3.x = stage53_x;
    r3.y = stage53_y;
    r3.z = stage53_z;
    // mad r0.xyz, cb0[24].wwww, r3.xyzx, r0.yzwy
    float stage54_x = cb0[24].w * r3.x + r0.y;
    float stage54_y = cb0[24].w * r3.y + r0.z;
    float stage54_z = cb0[24].w * r3.z + r0.w;
    r0.x = stage54_x;
    r0.y = stage54_y;
    r0.z = stage54_z;
    // mul r0.xyz, r2.xyzx, r0.xyzx
    float stage55_x = r2.x * r0.x;
    float stage55_y = r2.y * r0.y;
    float stage55_z = r2.z * r0.z;
    r0.x = stage55_x;
    r0.y = stage55_y;
    r0.z = stage55_z;
    // mul r0.xyz, r0.xyzx, cb0[13].xyzx
    float stage56_x = r0.x * cb0[13].x;
    float stage56_y = r0.y * cb0[13].y;
    float stage56_z = r0.z * cb0[13].z;
    r0.x = stage56_x;
    r0.y = stage56_y;
    r0.z = stage56_z;
    // mad r0.xyz, r1.xyzx, cb0[8].xyzx, r0.xyzx
    float stage57_x = r1.x * cb0[8].x + r0.x;
    float stage57_y = r1.y * cb0[8].y + r0.y;
    float stage57_z = r1.z * cb0[8].z + r0.z;
    r0.x = stage57_x;
    r0.y = stage57_y;
    r0.z = stage57_z;
    // mul r0.xyz, r0.xyzx, cb0[25].xxxx
    float stage58_x = r0.x * cb0[25].x;
    float stage58_y = r0.y * cb0[25].x;
    float stage58_z = r0.z * cb0[25].x;
    r0.x = stage58_x;
    r0.y = stage58_y;
    r0.z = stage58_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage59_x = max(abs(r0.x), 1e-06f);
    float stage59_y = max(abs(r0.y), 1e-06f);
    float stage59_z = max(abs(r0.z), 1e-06f);
    r0.x = stage59_x;
    r0.y = stage59_y;
    r0.z = stage59_z;
    // log r0.xyz, r0.xyzx
    float stage60_x = log2(r0.x);
    float stage60_y = log2(r0.y);
    float stage60_z = log2(r0.z);
    r0.x = stage60_x;
    r0.y = stage60_y;
    r0.z = stage60_z;
    // mul r0.xyz, r0.xyzx, cb0[25].yyyy
    float stage61_x = r0.x * cb0[25].y;
    float stage61_y = r0.y * cb0[25].y;
    float stage61_z = r0.z * cb0[25].y;
    r0.x = stage61_x;
    r0.y = stage61_y;
    r0.z = stage61_z;
    // exp r0.xyz, r0.xyzx
    float stage62_x = exp2(r0.x);
    float stage62_y = exp2(r0.y);
    float stage62_z = exp2(r0.z);
    r0.x = stage62_x;
    r0.y = stage62_y;
    r0.z = stage62_z;
    // mad r0.xyz, r0.xyzx, cb0[1].xyzx, cb0[2].xyzx
    float stage63_x = r0.x * cb0[1].x + cb0[2].x;
    float stage63_y = r0.y * cb0[1].y + cb0[2].y;
    float stage63_z = r0.z * cb0[1].z + cb0[2].z;
    r0.x = stage63_x;
    r0.y = stage63_y;
    r0.z = stage63_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage64_x = r0.x * v5.w + v5.x;
    float stage64_y = r0.y * v5.w + v5.y;
    float stage64_z = r0.z * v5.w + v5.z;
    o0.x = stage64_x;
    o0.y = stage64_y;
    o0.z = stage64_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage65_x = r0.x * v1.x;
    float stage65_y = r0.x * v1.y;
    float stage65_z = r0.x * v1.z;
    r0.x = stage65_x;
    r0.y = stage65_y;
    r0.z = stage65_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage66_x = r0.w * v0.z;
    float stage66_y = r0.w * v0.x;
    float stage66_z = r0.w * v0.y;
    r1.x = stage66_x;
    r1.y = stage66_y;
    r1.z = stage66_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage67_x = r0.x * r1.x;
    float stage67_y = r0.x * r1.y;
    float stage67_z = r0.x * r1.z;
    r0.x = stage67_x;
    r0.y = stage67_y;
    r0.z = stage67_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage68_x = r0.x / r0.w;
    float stage68_y = r0.y / r0.w;
    r0.x = stage68_x;
    r0.y = stage68_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage69_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage69_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage69_x;
    r1.y = stage69_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage70_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage70_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage70_x;
    r1.y = stage70_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage71_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage71_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage71_x;
    r1.y = stage71_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage72_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage72_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage72_x;
    r0.y = stage72_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage73_x = r0.x * 0.5f + 0.5f;
    float stage73_y = r0.y * 0.5f + 0.5f;
    o2.x = stage73_x;
    o2.y = stage73_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage74_z = 1.0f;
    float stage74_w = asfloat(0u);
    o2.z = stage74_z;
    o2.w = stage74_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage75_x = cb2[3].x;
    float stage75_y = cb2[3].y;
    float stage75_z = cb2[3].z;
    o3.x = stage75_x;
    o3.y = stage75_y;
    o3.z = stage75_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage76_x = asfloat(0u);
    float stage76_y = asfloat(0u);
    float stage76_z = asfloat(0u);
    float stage76_w = asfloat(0u);
    o4.x = stage76_x;
    o4.y = stage76_y;
    o4.z = stage76_z;
    o4.w = stage76_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage77_x = asfloat(0u);
    float stage77_y = asfloat(0u);
    float stage77_z = asfloat(0u);
    float stage77_w = asfloat(0u);
    o5.x = stage77_x;
    o5.y = stage77_y;
    o5.z = stage77_z;
    o5.w = stage77_w;

    Shade_Ue3_fx_j_pa_linearflow_02_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
