// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_me_master_01_ma_Constants0 : register(b0)
{
    float4 cb0[12];
};
cbuffer Shade_Ue3_fx_d_me_master_01_ma_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_d_me_master_01_ma_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    uint4 vCoverage;
};

struct Shade_Ue3_fx_d_me_master_01_ma_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
    uint oMask;
};

Shade_Ue3_fx_d_me_master_01_ma_OUTPUT Shade_Ue3_fx_d_me_master_01_ma(Shade_Ue3_fx_d_me_master_01_ma_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    uint4 vCoverage = stage.vCoverage;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;
    uint oMask = 0xffffffffu;

    // add r0.x, v4.y, l(-1.000000)
    r0.x = v4.y + -1.0f;
    // mad r0.y, cb0[6].y, cb0[10].z, cb0[10].w
    r0.y = cb0[6].y * cb0[10].z + cb0[10].w;
    // sincos r1.x, r2.x, r0.y
    float stage1_x = r0.y;
    r1.x = sin(stage1_x);
    r2.x = cos(stage1_x);
    // mov r3.x, -r1.x
    r3.x = -r1.x;
    // add r0.yz, v2.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage2_y = v2.x + -0.5f;
    float stage2_z = v2.y + -0.5f;
    r0.y = stage2_y;
    r0.z = stage2_z;
    // mov r3.y, r2.x
    r3.y = r2.x;
    // mov r3.z, r1.x
    r3.z = r1.x;
    // dp2 r0.w, r3.zyzz, r0.yzyy
    r0.w = dot(r3.zy, r0.yz);
    // dp2 r0.y, r3.yxyy, r0.yzyy
    r0.y = dot(r3.yx, r0.yz);
    // mad r0.x, r0.y, cb0[5].x, r0.x
    r0.x = r0.y * cb0[5].x + r0.x;
    // mul r0.z, r0.w, cb0[5].y
    r0.z = r0.w * cb0[5].y;
    // add r0.xy, r0.xzxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage3_x = r0.x + 0.5f;
    float stage3_y = r0.z + 0.5f;
    r0.x = stage3_x;
    r0.y = stage3_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s3, l(0.000000)
    float4 fetch4 = t2.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch4.x;
    // mul r0.yz, v2.xxyx, cb0[9].zzwz
    float stage5_y = v2.x * cb0[9].z;
    float stage5_z = v2.y * cb0[9].w;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // mad r1.x, cb0[6].y, cb0[9].y, r0.y
    r1.x = cb0[6].y * cb0[9].y + r0.y;
    // mad r1.y, cb0[6].y, cb0[10].x, r0.z
    r1.y = cb0[6].y * cb0[10].x + r0.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.xyxx, t1.yxzw, s2, l(0.000000)
    float4 fetch6 = t1.SampleBias(s2, r1.xy, 0.0f);
    r0.y = fetch6.x;
    // add r0.x, r0.x, r0.y
    r0.x = r0.x + r0.y;
    // mov_sat r0.y, v3.w
    r0.y = saturate(v3.w);
    // add r0.y, -r0.y, l(1.000000)
    r0.y = -r0.y + 1.0f;
    // add_sat r0.y, -r0.y, r0.x
    r0.y = saturate(-r0.y + r0.x);
    // mul_sat r0.x, r0.x, cb0[11].z
    r0.x = saturate(r0.x * cb0[11].z);
    // log r0.z, r0.x
    r0.z = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[11].w
    r0.z = r0.z * cb0[11].w;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // mul r0.y, r0.z, r0.y
    r0.y = r0.z * r0.y;
    // min r0.y, r0.y, l(1.000000)
    r0.y = min(r0.y, 1.0f);
    // add r0.y, r0.y, l(-0.166000)
    r0.y = r0.y + -0.166f;
    // lt r0.y, r0.y, l(0.000000)
    r0.y = asfloat((r0.y < 0.0f) ? 0xffffffffu : 0u);
    // or r0.x, r0.y, r0.x
    r0.x = asfloat(asuint(r0.y) | asuint(r0.x));
    // discard_nz r0.x
    if (asuint(r0.x) != 0u) discard;
    // mov oMask, vCoverage.x
    oMask = asuint(vCoverage.x);
    // dp3 r0.x, v5.xyzx, v5.xyzx
    r0.x = dot(v5.xyz, v5.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xy, r0.xxxx, v5.xyxx
    float stage7_x = r0.x * v5.x;
    float stage7_y = r0.x * v5.y;
    r0.x = stage7_x;
    r0.y = stage7_y;
    // mul r0.zw, v2.xxxy, cb0[6].zzzw
    float stage8_z = v2.x * cb0[6].z;
    float stage8_w = v2.y * cb0[6].w;
    r0.z = stage8_z;
    r0.w = stage8_w;
    // mad r1.x, cb0[6].y, cb0[6].x, r0.z
    r1.x = cb0[6].y * cb0[6].x + r0.z;
    // mad r1.y, cb0[6].y, cb0[7].x, r0.w
    r1.y = cb0[6].y * cb0[7].x + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.zw, r1.xyxx, t0.zwxy, s0, l(0.000000)
    float4 fetch9 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.z = fetch9.x;
    r0.w = fetch9.y;
    // mad r1.xy, r0.zwzz, l(2.000000, 2.000000, 0.000000, 0.000000), l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage10_x = r0.z * 2.0f + -1.0f;
    float stage10_y = r0.w * 2.0f + -1.0f;
    r1.x = stage10_x;
    r1.y = stage10_y;
    // dp2 r0.z, r1.xyxx, r1.xyxx
    r0.z = dot(r1.xy, r1.xy);
    // add r0.z, -r0.z, l(1.000000)
    r0.z = -r0.z + 1.0f;
    // max r0.z, r0.z, l(0.000000)
    r0.z = max(r0.z, 0.0f);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // add r1.z, r0.z, l(0.000010)
    r1.z = r0.z + 1e-05f;
    // mul r1.xyz, r1.xyzx, cb0[1].xyzx
    float stage11_x = r1.x * cb0[1].x;
    float stage11_y = r1.y * cb0[1].y;
    float stage11_z = r1.z * cb0[1].z;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    // mad r0.zw, cb0[3].wwww, cb0[3].xxxy, r1.xxxy
    float stage12_z = cb0[3].w * cb0[3].x + r1.x;
    float stage12_w = cb0[3].w * cb0[3].y + r1.y;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mul r0.zw, r0.zzzw, cb0[8].xxxx
    float stage13_z = r0.z * cb0[8].x;
    float stage13_w = r0.w * cb0[8].x;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // mad r0.xy, r0.xyxx, cb0[2].xyxx, r0.zwzz
    float stage14_x = r0.x * cb0[2].x + r0.z;
    float stage14_y = r0.y * cb0[2].y + r0.w;
    r0.x = stage14_x;
    r0.y = stage14_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t3.xyzw, s1, l(0.000000)
    float4 fetch15 = t3.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch15.x;
    r0.y = fetch15.y;
    r0.z = fetch15.z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r2.xyz, -r0.xyzx, r0.wwww
    float stage16_x = -r0.x + r0.w;
    float stage16_y = -r0.y + r0.w;
    float stage16_z = -r0.z + r0.w;
    r2.x = stage16_x;
    r2.y = stage16_y;
    r2.z = stage16_z;
    // mad r0.xyz, cb0[8].yyyy, r2.xyzx, r0.xyzx
    float stage17_x = cb0[8].y * r2.x + r0.x;
    float stage17_y = cb0[8].y * r2.y + r0.y;
    float stage17_z = cb0[8].y * r2.z + r0.z;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage18_x = max(abs(r0.x), 1e-06f);
    float stage18_y = max(abs(r0.y), 1e-06f);
    float stage18_z = max(abs(r0.z), 1e-06f);
    r0.x = stage18_x;
    r0.y = stage18_y;
    r0.z = stage18_z;
    // log r0.xyz, r0.xyzx
    float stage19_x = log2(r0.x);
    float stage19_y = log2(r0.y);
    float stage19_z = log2(r0.z);
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    // mul r0.xyz, r0.xyzx, cb0[8].zzzz
    float stage20_x = r0.x * cb0[8].z;
    float stage20_y = r0.y * cb0[8].z;
    float stage20_z = r0.z * cb0[8].z;
    r0.x = stage20_x;
    r0.y = stage20_y;
    r0.z = stage20_z;
    // exp r0.xyz, r0.xyzx
    float stage21_x = exp2(r0.x);
    float stage21_y = exp2(r0.y);
    float stage21_z = exp2(r0.z);
    r0.x = stage21_x;
    r0.y = stage21_y;
    r0.z = stage21_z;
    // mul r2.xyz, cb0[4].xyzx, cb0[4].wwww
    float stage22_x = cb0[4].x * cb0[4].w;
    float stage22_y = cb0[4].y * cb0[4].w;
    float stage22_z = cb0[4].z * cb0[4].w;
    r2.x = stage22_x;
    r2.y = stage22_y;
    r2.z = stage22_z;
    // mul r0.xyz, r0.xyzx, r2.xyzx
    float stage23_x = r0.x * r2.x;
    float stage23_y = r0.y * r2.y;
    float stage23_z = r0.z * r2.z;
    r0.x = stage23_x;
    r0.y = stage23_y;
    r0.z = stage23_z;
    // dp3 r0.w, v1.xyzx, v1.xyzx
    r0.w = dot(v1.xyz, v1.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mad r1.w, v1.z, r0.w, l(1.000000)
    r1.w = v1.z * r0.w + 1.0f;
    // mul r2.xyz, r0.wwww, v1.xyzx
    float stage24_x = r0.w * v1.x;
    float stage24_y = r0.w * v1.y;
    float stage24_z = r0.w * v1.z;
    r2.x = stage24_x;
    r2.y = stage24_y;
    r2.z = stage24_z;
    // mul_sat r0.w, r1.w, l(0.500000)
    r0.w = saturate(r1.w * 0.5f);
    // mad r0.w, r0.w, l(0.950000), l(0.050000)
    r0.w = r0.w * 0.95f + 0.05f;
    // dp3 r0.w, r1.xyzx, r0.wwww
    r0.w = dot(r1.xyz, r0.www);
    // log r1.w, |r0.w|
    r1.w = log2(abs(r0.w));
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.w, r1.w, cb0[8].w
    r1.w = r1.w * cb0[8].w;
    // exp r1.w, r1.w
    r1.w = exp2(r1.w);
    // mul r1.w, r1.w, cb0[9].x
    r1.w = r1.w * cb0[9].x;
    // movc r0.w, r0.w, l(0), r1.w
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.w;
    // mul r3.xyz, cb0[3].xyzx, cb0[3].wwww
    float stage25_x = cb0[3].x * cb0[3].w;
    float stage25_y = cb0[3].y * cb0[3].w;
    float stage25_z = cb0[3].z * cb0[3].w;
    r3.x = stage25_x;
    r3.y = stage25_y;
    r3.z = stage25_z;
    // mad r0.xyz, r0.wwww, r0.xyzx, r3.xyzx
    float stage26_x = r0.w * r0.x + r3.x;
    float stage26_y = r0.w * r0.y + r3.y;
    float stage26_z = r0.w * r0.z + r3.z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // mul r0.xyz, r0.xyzx, r0.wwww
    float stage27_x = r0.x * r0.w;
    float stage27_y = r0.y * r0.w;
    float stage27_z = r0.z * r0.w;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // mad o0.xyz, r0.xyzx, v3.xyzx, cb0[0].xyzx
    float stage28_x = r0.x * v3.x + cb0[0].x;
    float stage28_y = r0.y * v3.y + cb0[0].y;
    float stage28_z = r0.z * v3.z + cb0[0].z;
    o0.x = stage28_x;
    o0.y = stage28_y;
    o0.z = stage28_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage29_x = r0.x * r1.x;
    float stage29_y = r0.x * r1.y;
    float stage29_z = r0.x * r1.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.xyzx
    float stage30_x = r0.w * v0.x;
    float stage30_y = r0.w * v0.y;
    float stage30_z = r0.w * v0.z;
    r1.x = stage30_x;
    r1.y = stage30_y;
    r1.z = stage30_z;
    // mul r3.xyz, r1.yzxy, r2.zxyz
    float stage31_x = r1.y * r2.z;
    float stage31_y = r1.z * r2.x;
    float stage31_z = r1.x * r2.y;
    r3.x = stage31_x;
    r3.y = stage31_y;
    r3.z = stage31_z;
    // mad r3.xyz, r2.yzxy, r1.zxyz, -r3.xyzx
    float stage32_x = r2.y * r1.z + -r3.x;
    float stage32_y = r2.z * r1.x + -r3.y;
    float stage32_z = r2.x * r1.y + -r3.z;
    r3.x = stage32_x;
    r3.y = stage32_y;
    r3.z = stage32_z;
    // dp3 r2.z, r2.xyzx, r0.xyzx
    r2.z = dot(r2.xyz, r0.xyz);
    // dp3 r2.x, r1.xyzx, r0.xyzx
    r2.x = dot(r1.xyz, r0.xyz);
    // mul r1.xyz, r3.xyzx, v1.wwww
    float stage33_x = r3.x * v1.w;
    float stage33_y = r3.y * v1.w;
    float stage33_z = r3.z * v1.w;
    r1.x = stage33_x;
    r1.y = stage33_y;
    r1.z = stage33_z;
    // dp3 r2.y, r1.xyzx, r0.xyzx
    r2.y = dot(r1.xyz, r0.xyz);
    // dp3 r0.x, r2.xyzx, r2.xyzx
    r0.x = dot(r2.xyz, r2.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r2.xyzx
    float stage34_x = r0.x * r2.x;
    float stage34_y = r0.x * r2.y;
    float stage34_z = r0.x * r2.z;
    r0.x = stage34_x;
    r0.y = stage34_y;
    r0.z = stage34_z;
    // ge r0.w, l(0.000000), r0.z
    r0.w = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // dp3 r0.z, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.z = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.zzzz
    float stage35_x = r0.x / r0.z;
    float stage35_y = r0.y / r0.z;
    r0.x = stage35_x;
    r0.y = stage35_y;
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage36_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage36_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage36_x;
    r1.y = stage36_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage37_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage37_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage37_x;
    r1.y = stage37_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage38_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage38_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage38_x;
    r1.y = stage38_y;
    // movc r0.xy, r0.wwww, r1.xyxx, r0.xyxx
    float stage39_x = asuint(r0.w) != 0u ? r1.x : r0.x;
    float stage39_y = asuint(r0.w) != 0u ? r1.y : r0.y;
    r0.x = stage39_x;
    r0.y = stage39_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage40_x = r0.x * 0.5f + 0.5f;
    float stage40_y = r0.y * 0.5f + 0.5f;
    o2.x = stage40_x;
    o2.y = stage40_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage41_z = 1.0f;
    float stage41_w = asfloat(0u);
    o2.z = stage41_z;
    o2.w = stage41_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage42_x = cb2[3].x;
    float stage42_y = cb2[3].y;
    float stage42_z = cb2[3].z;
    o3.x = stage42_x;
    o3.y = stage42_y;
    o3.z = stage42_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage43_x = asfloat(0u);
    float stage43_y = asfloat(0u);
    float stage43_z = asfloat(0u);
    float stage43_w = asfloat(0u);
    o4.x = stage43_x;
    o4.y = stage43_y;
    o4.z = stage43_z;
    o4.w = stage43_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage44_x = asfloat(0u);
    float stage44_y = asfloat(0u);
    float stage44_z = asfloat(0u);
    float stage44_w = asfloat(0u);
    o5.x = stage44_x;
    o5.y = stage44_y;
    o5.z = stage44_z;
    o5.w = stage44_w;

    Shade_Ue3_fx_d_me_master_01_ma_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    result.oMask = oMask;
    return result;
}
