// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_pa_ninjaflow_01_tr_Constants0 : register(b0)
{
    float4 cb0[9];
};
cbuffer Shade_Ue3_fx_k_pa_ninjaflow_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_k_pa_ninjaflow_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_pa_ninjaflow_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_pa_ninjaflow_01_tr_OUTPUT Shade_Ue3_fx_k_pa_ninjaflow_01_tr(Shade_Ue3_fx_k_pa_ninjaflow_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // sample_b_indexable(texture2d)(float,float,float,float) r0.xy, v2.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch1 = t0.SampleBias(s0, v2.xy, 0.0f);
    r0.x = fetch1.x;
    r0.y = fetch1.y;
    // add r0.xy, r0.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage2_x = r0.x + -0.5f;
    float stage2_y = r0.y + -0.5f;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // mad r0.xy, r0.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage3_x = r0.x * 2.0f + -0.5f;
    float stage3_y = r0.y * 2.0f + -0.5f;
    r0.x = stage3_x;
    r0.y = stage3_y;
    // mul r0.zw, v2.xxxy, cb0[3].xxxy
    float stage4_z = v2.x * cb0[3].x;
    float stage4_w = v2.y * cb0[3].y;
    r0.z = stage4_z;
    r0.w = stage4_w;
    // mad r1.xy, -r0.xyxx, cb0[5].zzzz, -r0.zwzz
    float stage5_x = -r0.x * cb0[5].z + -r0.z;
    float stage5_y = -r0.y * cb0[5].z + -r0.w;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // mad r0.zw, v4.xxxx, r1.xxxy, r0.zzzw
    float stage6_z = v4.x * r1.x + r0.z;
    float stage6_w = v4.x * r1.y + r0.w;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // add r0.zw, r0.zzzw, v4.yyyy
    float stage7_z = r0.z + v4.y;
    float stage7_w = r0.w + v4.y;
    r0.z = stage7_z;
    r0.w = stage7_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r0.zwzz, t1.yzxw, s2, l(0.000000)
    float4 fetch8 = t1.SampleBias(s2, r0.zw, 0.0f);
    r0.z = fetch8.x;
    // mul r0.w, r0.z, cb0[8].x
    r0.w = r0.z * cb0[8].x;
    // max r0.z, |r0.z|, l(0.000001)
    r0.z = max(abs(r0.z), 1e-06f);
    // log r0.z, r0.z
    r0.z = log2(r0.z);
    // mul r0.z, r0.z, cb0[7].x
    r0.z = r0.z * cb0[7].x;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // mul r0.z, r0.z, cb0[7].y
    r0.z = r0.z * cb0[7].y;
    // mad r1.xy, v2.xyxx, cb0[4].xyxx, r0.wwww
    float stage9_x = v2.x * cb0[4].x + r0.w;
    float stage9_y = v2.y * cb0[4].y + r0.w;
    r1.x = stage9_x;
    r1.y = stage9_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t2.yzwx, s3, l(0.000000)
    float4 fetch10 = t2.SampleBias(s3, r1.xy, 0.0f);
    r0.w = fetch10.x;
    // max r0.w, |r0.w|, l(0.000001)
    r0.w = max(abs(r0.w), 1e-06f);
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, cb0[8].y
    r0.w = r0.w * cb0[8].y;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul r0.w, r0.w, cb0[8].z
    r0.w = r0.w * cb0[8].z;
    // mul r0.z, r0.w, r0.z
    r0.z = r0.w * r0.z;
    // mad r1.xy, v2.yxyy, l(2.000000, 2.000000, 0.000000, 0.000000), l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage11_x = v2.y * 2.0f + -1.0f;
    float stage11_y = v2.x * 2.0f + -1.0f;
    r1.x = stage11_x;
    r1.y = stage11_y;
    // add r1.xy, -|r1.xyxx|, l(1.000000, 1.000000, 0.000000, 0.000000)
    float stage12_x = -(abs(r1.x)) + 1.0f;
    float stage12_y = -(abs(r1.y)) + 1.0f;
    r1.x = stage12_x;
    r1.y = stage12_y;
    // log r1.zw, |r1.xxxy|
    float stage13_z = log2(abs(r1.x));
    float stage13_w = log2(abs(r1.y));
    r1.z = stage13_z;
    r1.w = stage13_w;
    // lt r1.xy, |r1.xyxx|, l(0.000001, 0.000001, 0.000000, 0.000000)
    float stage14_x = asfloat((abs(r1.x) < 1e-06f) ? 0xffffffffu : 0u);
    float stage14_y = asfloat((abs(r1.y) < 1e-06f) ? 0xffffffffu : 0u);
    r1.x = stage14_x;
    r1.y = stage14_y;
    // mul r1.zw, r1.zzzw, cb0[8].wwww
    float stage15_z = r1.z * cb0[8].w;
    float stage15_w = r1.w * cb0[8].w;
    r1.z = stage15_z;
    r1.w = stage15_w;
    // exp r1.zw, r1.zzzw
    float stage16_z = exp2(r1.z);
    float stage16_w = exp2(r1.w);
    r1.z = stage16_z;
    r1.w = stage16_w;
    // movc r1.xy, r1.xyxx, l(0,0,0,0), r1.zwzz
    float stage17_x = asuint(r1.x) != 0u ? asfloat(0u) : r1.z;
    float stage17_y = asuint(r1.y) != 0u ? asfloat(0u) : r1.w;
    r1.x = stage17_x;
    r1.y = stage17_y;
    // mul r0.w, r1.y, r1.x
    r0.w = r1.y * r1.x;
    // mul r0.w, r0.w, l(3.000000)
    r0.w = r0.w * 3.0f;
    // min r0.w, r0.w, l(1.000000)
    r0.w = min(r0.w, 1.0f);
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul_sat r0.z, r0.z, v3.w
    r0.z = saturate(r0.z * v3.w);
    // mul o0.w, r0.z, cb0[0].x
    o0.w = r0.z * cb0[0].x;
    // mul r0.zw, v2.xxxy, cb0[2].xxxy
    float stage18_z = v2.x * cb0[2].x;
    float stage18_w = v2.y * cb0[2].y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mad r0.xy, -r0.xyxx, cb0[5].zzzz, -r0.zwzz
    float stage19_x = -r0.x * cb0[5].z + -r0.z;
    float stage19_y = -r0.y * cb0[5].z + -r0.w;
    r0.x = stage19_x;
    r0.y = stage19_y;
    // mad r0.xy, v4.xxxx, r0.xyxx, r0.zwzz
    float stage20_x = v4.x * r0.x + r0.z;
    float stage20_y = v4.x * r0.y + r0.w;
    r0.x = stage20_x;
    r0.y = stage20_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t3.xyzw, s1, l(0.000000)
    float4 fetch21 = t3.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch21.x;
    r0.y = fetch21.y;
    r0.z = fetch21.z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xyzx, r0.wwww
    float stage22_x = -r0.x + r0.w;
    float stage22_y = -r0.y + r0.w;
    float stage22_z = -r0.z + r0.w;
    r1.x = stage22_x;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // mad r0.xyz, cb0[5].wwww, r1.xyzx, r0.xyzx
    float stage23_x = cb0[5].w * r1.x + r0.x;
    float stage23_y = cb0[5].w * r1.y + r0.y;
    float stage23_z = cb0[5].w * r1.z + r0.z;
    r0.x = stage23_x;
    r0.y = stage23_y;
    r0.z = stage23_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage24_x = max(abs(r0.x), 1e-06f);
    float stage24_y = max(abs(r0.y), 1e-06f);
    float stage24_z = max(abs(r0.z), 1e-06f);
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // log r0.xyz, r0.xyzx
    float stage25_x = log2(r0.x);
    float stage25_y = log2(r0.y);
    float stage25_z = log2(r0.z);
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // mul r0.xyz, r0.xyzx, cb0[6].xxxx
    float stage26_x = r0.x * cb0[6].x;
    float stage26_y = r0.y * cb0[6].x;
    float stage26_z = r0.z * cb0[6].x;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // exp r0.xyz, r0.xyzx
    float stage27_x = exp2(r0.x);
    float stage27_y = exp2(r0.y);
    float stage27_z = exp2(r0.z);
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // mul r0.xyz, r0.xyzx, cb0[6].yyyy
    float stage28_x = r0.x * cb0[6].y;
    float stage28_y = r0.y * cb0[6].y;
    float stage28_z = r0.z * cb0[6].y;
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage29_x = r0.x * v3.x + cb0[1].x;
    float stage29_y = r0.y * v3.y + cb0[1].y;
    float stage29_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage30_x = r0.x * v5.w + v5.x;
    float stage30_y = r0.y * v5.w + v5.y;
    float stage30_z = r0.z * v5.w + v5.z;
    o0.x = stage30_x;
    o0.y = stage30_y;
    o0.z = stage30_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage31_x = r0.x * v1.x;
    float stage31_y = r0.x * v1.y;
    float stage31_z = r0.x * v1.z;
    r0.x = stage31_x;
    r0.y = stage31_y;
    r0.z = stage31_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage32_x = r0.w * v0.z;
    float stage32_y = r0.w * v0.x;
    float stage32_z = r0.w * v0.y;
    r1.x = stage32_x;
    r1.y = stage32_y;
    r1.z = stage32_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage33_x = r0.x * r1.x;
    float stage33_y = r0.x * r1.y;
    float stage33_z = r0.x * r1.z;
    r0.x = stage33_x;
    r0.y = stage33_y;
    r0.z = stage33_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage34_x = r0.x / r0.w;
    float stage34_y = r0.y / r0.w;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage35_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage35_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage35_x;
    r1.y = stage35_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage36_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage36_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage36_x;
    r1.y = stage36_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage37_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage37_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage37_x;
    r1.y = stage37_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage38_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage38_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage38_x;
    r0.y = stage38_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage39_x = r0.x * 0.5f + 0.5f;
    float stage39_y = r0.y * 0.5f + 0.5f;
    o2.x = stage39_x;
    o2.y = stage39_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage40_z = 1.0f;
    float stage40_w = asfloat(0u);
    o2.z = stage40_z;
    o2.w = stage40_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage41_x = cb2[3].x;
    float stage41_y = cb2[3].y;
    float stage41_z = cb2[3].z;
    o3.x = stage41_x;
    o3.y = stage41_y;
    o3.z = stage41_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage42_x = asfloat(0u);
    float stage42_y = asfloat(0u);
    float stage42_z = asfloat(0u);
    float stage42_w = asfloat(0u);
    o4.x = stage42_x;
    o4.y = stage42_y;
    o4.z = stage42_z;
    o4.w = stage42_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage43_x = asfloat(0u);
    float stage43_y = asfloat(0u);
    float stage43_z = asfloat(0u);
    float stage43_w = asfloat(0u);
    o5.x = stage43_x;
    o5.y = stage43_y;
    o5.z = stage43_z;
    o5.w = stage43_w;

    Shade_Ue3_fx_k_pa_ninjaflow_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
