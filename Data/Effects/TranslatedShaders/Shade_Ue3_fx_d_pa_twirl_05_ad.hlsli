// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_twirl_05_ad_Constants0 : register(b0)
{
    float4 cb0[12];
};
cbuffer Shade_Ue3_fx_d_pa_twirl_05_ad_Constants2 : register(b2)
{
    float4 cb2[2];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_d_pa_twirl_05_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    float4 v7;
};

struct Shade_Ue3_fx_d_pa_twirl_05_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_d_pa_twirl_05_ad_OUTPUT Shade_Ue3_fx_d_pa_twirl_05_ad(Shade_Ue3_fx_d_pa_twirl_05_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;

    // div r0.xy, v7.xyxx, v7.wwww
    float stage1_x = v7.x / v7.w;
    float stage1_y = v7.y / v7.w;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, r0.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage2_x = r0.x * cb2[0].x + cb2[0].w;
    float stage2_y = r0.y * cb2[0].y + cb2[0].z;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t3.xyzw, s0, l(0.000000)
    float4 fetch3 = t3.SampleLevel(s0, r0.xy, 0.0f);
    r0.x = fetch3.x;
    // min r0.x, r0.x, l(0.999000)
    r0.x = min(r0.x, 0.999f);
    // mad r0.y, r0.x, cb2[1].z, -cb2[1].w
    r0.y = r0.x * cb2[1].z + -cb2[1].w;
    // mad r0.x, r0.x, cb2[1].x, cb2[1].y
    r0.x = r0.x * cb2[1].x + cb2[1].y;
    // div r0.y, l(1.000000, 1.000000, 1.000000, 1.000000), r0.y
    r0.y = 1.0f / r0.y;
    // add r0.x, r0.y, r0.x
    r0.x = r0.y + r0.x;
    // add r0.x, r0.x, -v7.w
    r0.x = r0.x + -v7.w;
    // add r0.y, -cb0[11].x, l(1.000000)
    r0.y = -cb0[11].x + 1.0f;
    // mul r0.y, r0.y, l(100.000000)
    r0.y = r0.y * 100.0f;
    // max r0.y, r0.y, l(0.001000)
    r0.y = max(r0.y, 0.001f);
    // div_sat r0.x, r0.x, r0.y
    r0.x = saturate(r0.x / r0.y);
    // add r0.yz, v2.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage4_y = v2.x + -0.5f;
    float stage4_z = v2.y + -0.5f;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // add r1.xy, r0.yzyy, r0.yzyy
    float stage5_x = r0.y + r0.y;
    float stage5_y = r0.z + r0.z;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // dp2 r0.y, r0.yzyy, r0.yzyy
    r0.y = dot(r0.yz, r0.yz);
    // sqrt r0.y, r0.y
    r0.y = sqrt(r0.y);
    // max r0.z, |r1.x|, |r1.y|
    r0.z = max(abs(r1.x), abs(r1.y));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r1.x|, |r1.y|
    r0.w = min(abs(r1.x), abs(r1.y));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.z, r0.w, l(0.020835), l(-0.085133)
    r1.z = r0.w * 0.020835f + -0.085133f;
    // mad r1.z, r0.w, r1.z, l(0.180141)
    r1.z = r0.w * r1.z + 0.180141f;
    // mad r1.z, r0.w, r1.z, l(-0.330299)
    r1.z = r0.w * r1.z + -0.330299f;
    // mad r0.w, r0.w, r1.z, l(0.999866)
    r0.w = r0.w * r1.z + 0.999866f;
    // mul r1.z, r0.w, r0.z
    r1.z = r0.w * r0.z;
    // mad r1.z, r1.z, l(-2.000000), l(1.570796)
    r1.z = r1.z * -2.0f + 1.570796f;
    // lt r1.w, |r1.x|, |r1.y|
    r1.w = asfloat((abs(r1.x) < abs(r1.y)) ? 0xffffffffu : 0u);
    // and r1.z, r1.w, r1.z
    r1.z = asfloat(asuint(r1.w) & asuint(r1.z));
    // mad r0.z, r0.z, r0.w, r1.z
    r0.z = r0.z * r0.w + r1.z;
    // lt r0.w, r1.x, -r1.x
    r0.w = asfloat((r1.x < -r1.x) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r1.x, r1.y
    r0.w = min(r1.x, r1.y);
    // lt r0.w, r0.w, -r0.w
    r0.w = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // max r1.z, r1.x, r1.y
    r1.z = max(r1.x, r1.y);
    // dp2 r1.x, r1.xyxx, r1.xyxx
    r1.x = dot(r1.xy, r1.xy);
    // min r1.x, r1.x, l(1.000000)
    r1.x = min(r1.x, 1.0f);
    // ge r1.y, r1.z, -r1.z
    r1.y = asfloat((r1.z >= -r1.z) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, r1.y
    r0.w = asfloat(asuint(r0.w) & asuint(r1.y));
    // movc r0.z, r0.w, -r0.z, r0.z
    r0.z = asuint(r0.w) != 0u ? -r0.z : r0.z;
    // add r0.w, -r0.y, l(0.500000)
    r0.w = -r0.y + 0.5f;
    // add r0.w, r0.w, r0.w
    r0.w = r0.w + r0.w;
    // mul r1.y, v4.w, cb0[5].z
    r1.y = v4.w * cb0[5].z;
    // mul r0.w, r0.w, r1.y
    r0.w = r0.w * r1.y;
    // log r1.y, |r0.w|
    r1.y = log2(abs(r0.w));
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.y, r1.y, cb0[5].w
    r1.y = r1.y * cb0[5].w;
    // exp r1.y, r1.y
    r1.y = exp2(r1.y);
    // movc r0.w, r0.w, l(0), r1.y
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.y;
    // mad r2.x, r0.z, l(0.318310), r0.w
    r2.x = r0.z * 0.31831f + r0.w;
    // add r0.z, r0.y, r0.y
    r0.z = r0.y + r0.y;
    // log r0.z, r0.z
    r0.z = log2(r0.z);
    // mul r0.z, r0.z, v4.z
    r0.z = r0.z * v4.z;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // lt r0.w, r0.y, l(0.000000)
    r0.w = asfloat((r0.y < 0.0f) ? 0xffffffffu : 0u);
    // mad r0.y, -r0.y, cb0[9].x, l(1.000000)
    r0.y = -r0.y * cb0[9].x + 1.0f;
    // mul_sat r0.y, r0.y, cb0[10].x
    r0.y = saturate(r0.y * cb0[10].x);
    // movc r2.y, r0.w, l(0), r0.z
    r2.y = asuint(r0.w) != 0u ? asfloat(0u) : r0.z;
    // mul r0.zw, v2.xxxy, cb0[4].zzzw
    float stage6_z = v2.x * cb0[4].z;
    float stage6_w = v2.y * cb0[4].w;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // mul r1.y, cb0[3].x, cb0[3].y
    r1.y = cb0[3].x * cb0[3].y;
    // mad r3.x, r1.y, cb0[4].y, r0.z
    r3.x = r1.y * cb0[4].y + r0.z;
    // mad r3.y, r1.y, cb0[5].x, r0.w
    r3.y = r1.y * cb0[5].x + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r3.xyxx, t0.yzxw, s2, l(0.000000)
    float4 fetch7 = t0.SampleBias(s2, r3.xy, 0.0f);
    r0.z = fetch7.x;
    // mad r0.zw, r0.zzzz, cb0[5].yyyy, r2.xxxy
    float stage8_z = r0.z * cb0[5].y + r2.x;
    float stage8_w = r0.z * cb0[5].y + r2.y;
    r0.z = stage8_z;
    r0.w = stage8_w;
    // mul r1.z, r0.z, cb0[3].w
    r1.z = r0.z * cb0[3].w;
    // mad r2.x, r1.y, cb0[3].z, r1.z
    r2.x = r1.y * cb0[3].z + r1.z;
    // mul r1.z, r1.y, cb0[6].x
    r1.z = r1.y * cb0[6].x;
    // mad r2.y, cb0[4].x, r0.w, r1.z
    r2.y = cb0[4].x * r0.w + r1.z;
    // mul r0.zw, r0.zzzw, cb0[6].zzzw
    float stage9_z = r0.z * cb0[6].z;
    float stage9_w = r0.w * cb0[6].w;
    r0.z = stage9_z;
    r0.w = stage9_w;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r2.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch10 = t1.SampleLevel(s1, r2.xy, 0.0f);
    r2.x = fetch10.x;
    r2.y = fetch10.y;
    r2.z = fetch10.z;
    // mad r3.x, r1.y, cb0[6].y, r0.z
    r3.x = r1.y * cb0[6].y + r0.z;
    // mad r3.y, r1.y, cb0[7].x, r0.w
    r3.y = r1.y * cb0[7].x + r0.w;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.yzw, r3.xyxx, t2.wxyz, s3, l(0.000000)
    float4 fetch11 = t2.SampleLevel(s3, r3.xy, 0.0f);
    r1.y = fetch11.x;
    r1.z = fetch11.y;
    r1.w = fetch11.z;
    // add r0.z, -r2.x, r1.y
    r0.z = -r2.x + r1.y;
    // mad r0.z, r0.z, l(0.500000), r2.x
    r0.z = r0.z * 0.5f + r2.x;
    // add r0.w, -r1.x, l(1.000000)
    r0.w = -r1.x + 1.0f;
    // mul r0.w, r1.x, r0.w
    r0.w = r1.x * r0.w;
    // mul_sat r1.x, r1.x, cb0[8].x
    r1.x = saturate(r1.x * cb0[8].x);
    // mul r0.z, r0.w, r0.z
    r0.z = r0.w * r0.z;
    // log r0.w, r1.x
    r0.w = log2(r1.x);
    // lt r1.x, r1.x, l(0.000001)
    r1.x = asfloat((r1.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, r0.w, cb0[8].y
    r0.w = r0.w * cb0[8].y;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul r0.y, r0.y, r0.w
    r0.y = r0.y * r0.w;
    // movc r0.y, r1.x, l(0), r0.y
    r0.y = asuint(r1.x) != 0u ? asfloat(0u) : r0.y;
    // mad_sat r0.y, r0.z, cb0[7].w, r0.y
    r0.y = saturate(r0.z * cb0[7].w + r0.y);
    // log r0.z, r0.y
    r0.z = log2(r0.y);
    // lt r0.y, r0.y, l(0.000001)
    r0.y = asfloat((r0.y < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[10].y
    r0.z = r0.z * cb0[10].y;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // min r0.z, r0.z, l(1.000000)
    r0.z = min(r0.z, 1.0f);
    // mul r0.x, r0.x, r0.z
    r0.x = r0.x * r0.z;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // movc r0.x, r0.y, l(0), r0.x
    r0.x = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // mul r0.yzw, r1.yyzw, r2.xxyz
    float stage12_y = r1.y * r2.x;
    float stage12_z = r1.z * r2.y;
    float stage12_w = r1.w * r2.z;
    r0.y = stage12_y;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // dp3 r1.x, r0.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.yzw, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r2.xyzx, r1.yzwy, r1.xxxx
    float stage13_x = -r2.x * r1.y + r1.x;
    float stage13_y = -r2.y * r1.z + r1.x;
    float stage13_z = -r2.z * r1.w + r1.x;
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    // mad r0.yzw, cb0[7].yyyy, r1.xxyz, r0.yyzw
    float stage14_y = cb0[7].y * r1.x + r0.y;
    float stage14_z = cb0[7].y * r1.y + r0.z;
    float stage14_w = cb0[7].y * r1.z + r0.w;
    r0.y = stage14_y;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // max r0.yzw, |r0.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage15_y = max(abs(r0.y), 1e-06f);
    float stage15_z = max(abs(r0.z), 1e-06f);
    float stage15_w = max(abs(r0.w), 1e-06f);
    r0.y = stage15_y;
    r0.z = stage15_z;
    r0.w = stage15_w;
    // log r0.yzw, r0.yyzw
    float stage16_y = log2(r0.y);
    float stage16_z = log2(r0.z);
    float stage16_w = log2(r0.w);
    r0.y = stage16_y;
    r0.z = stage16_z;
    r0.w = stage16_w;
    // mul r0.yzw, r0.yyzw, cb0[7].zzzz
    float stage17_y = r0.y * cb0[7].z;
    float stage17_z = r0.z * cb0[7].z;
    float stage17_w = r0.w * cb0[7].z;
    r0.y = stage17_y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // exp r0.yzw, r0.yyzw
    float stage18_y = exp2(r0.y);
    float stage18_z = exp2(r0.z);
    float stage18_w = exp2(r0.w);
    r0.y = stage18_y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mul r1.xyz, cb0[2].xyzx, cb0[2].wwww
    float stage19_x = cb0[2].x * cb0[2].w;
    float stage19_y = cb0[2].y * cb0[2].w;
    float stage19_z = cb0[2].z * cb0[2].w;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // mul r0.yzw, r0.yyzw, r1.xxyz
    float stage20_y = r0.y * r1.x;
    float stage20_z = r0.z * r1.y;
    float stage20_w = r0.w * r1.z;
    r0.y = stage20_y;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // mad r0.yzw, v3.xxyz, r0.yyzw, cb0[1].xxyz
    float stage21_y = v3.x * r0.y + cb0[1].x;
    float stage21_z = v3.y * r0.z + cb0[1].y;
    float stage21_w = v3.z * r0.w + cb0[1].z;
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mul r0.yzw, r0.yyzw, v5.wwww
    float stage22_y = r0.y * v5.w;
    float stage22_z = r0.z * v5.w;
    float stage22_w = r0.w * v5.w;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // mul o0.xyz, r0.xxxx, r0.yzwy
    float stage23_x = r0.x * r0.y;
    float stage23_y = r0.x * r0.z;
    float stage23_z = r0.x * r0.w;
    o0.x = stage23_x;
    o0.y = stage23_y;
    o0.z = stage23_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_d_pa_twirl_05_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
