// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf_Constants0 : register(b0)
{
    float4 cb0[16];
};
cbuffer Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf_OUTPUT Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf(Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mov r0.y, cb0[11].z
    r0.y = cb0[11].z;
    // mov r0.x, l(0)
    r0.x = asfloat(0u);
    // mad r0.zw, v4.xxxy, cb0[3].xxxy, cb0[4].xxxy
    float stage1_z = v4.x * cb0[3].x + cb0[4].x;
    float stage1_w = v4.y * cb0[3].y + cb0[4].y;
    r0.z = stage1_z;
    r0.w = stage1_w;
    // add r1.xyzw, r0.zwzw, r0.yxxy
    float stage2_x = r0.z + r0.y;
    float stage2_y = r0.w + r0.x;
    float stage2_z = r0.z + r0.x;
    float stage2_w = r0.w + r0.y;
    r1.x = stage2_x;
    r1.y = stage2_y;
    r1.z = stage2_z;
    r1.w = stage2_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.zwzz, t0.xyzw, s0, l(0.000000)
    float4 fetch3 = t0.SampleBias(s0, r0.zw, 0.0f);
    r0.x = fetch3.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.xyxx, t0.yxzw, s0, l(0.000000)
    float4 fetch4 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.y = fetch4.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r1.zwzz, t0.yzxw, s0, l(0.000000)
    float4 fetch5 = t0.SampleBias(s0, r1.zw, 0.0f);
    r0.z = fetch5.x;
    // add r1.xy, -r0.xxxx, r0.yzyy
    float stage6_x = -r0.x + r0.y;
    float stage6_y = -r0.x + r0.z;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // mul r0.xy, r1.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage7_x = r1.x * 8.0f;
    float stage7_y = r1.y * 8.0f;
    r0.x = stage7_x;
    r0.y = stage7_y;
    // mov r0.z, l(0)
    r0.z = asfloat(0u);
    // add r0.xyz, -r0.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage8_x = -r0.x + 0.0f;
    float stage8_y = -r0.y + 0.0f;
    float stage8_z = -r0.z + 1.0f;
    r0.x = stage8_x;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // dp3 r0.z, r0.xyzx, r0.xyzx
    r0.z = dot(r0.xyz, r0.xyz);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // div r0.xy, r0.xyxx, r0.zzzz
    float stage9_x = r0.x / r0.z;
    float stage9_y = r0.y / r0.z;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // mad r0.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage10_x = r0.x * 0.5f + 0.5f;
    float stage10_y = r0.y * 0.5f + 0.5f;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // lt r0.z, |cb0[12].z|, l(0.000001)
    r0.z = asfloat((abs(cb0[12].z) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, |cb0[12].z|, |cb0[12].z|
    r0.w = abs(cb0[12].z) * abs(cb0[12].z);
    // movc r0.z, r0.z, l(0), r0.w
    r0.z = asuint(r0.z) != 0u ? asfloat(0u) : r0.w;
    // mul r1.y, r0.z, v4.y
    r1.y = r0.z * v4.y;
    // mul r1.x, v4.x, cb0[12].w
    r1.x = v4.x * cb0[12].w;
    // mad r0.zw, cb0[5].xxxx, r0.xxxy, r1.xxxy
    float stage11_z = cb0[5].x * r0.x + r1.x;
    float stage11_w = cb0[5].x * r0.y + r1.y;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // add r0.zw, r0.zzzw, cb0[7].xxxy
    float stage12_z = r0.z + cb0[7].x;
    float stage12_w = r0.w + cb0[7].y;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r0.zwzz, t3.xyzw, s2, l(0.000000)
    float4 fetch13 = t3.SampleBias(s2, r0.zw, 0.0f);
    r1.x = fetch13.x;
    r1.y = fetch13.y;
    r1.z = fetch13.z;
    // lt r0.z, |cb0[11].x|, l(0.000001)
    r0.z = asfloat((abs(cb0[11].x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, |cb0[11].x|, |cb0[11].x|
    r0.w = abs(cb0[11].x) * abs(cb0[11].x);
    // movc r0.z, r0.z, l(0), r0.w
    r0.z = asuint(r0.z) != 0u ? asfloat(0u) : r0.w;
    // mul r2.y, r0.z, v4.y
    r2.y = r0.z * v4.y;
    // mul r2.x, v4.x, cb0[11].y
    r2.x = v4.x * cb0[11].y;
    // mad r0.xy, cb0[5].xxxx, r0.xyxx, r2.xyxx
    float stage14_x = cb0[5].x * r0.x + r2.x;
    float stage14_y = cb0[5].x * r0.y + r2.y;
    r0.x = stage14_x;
    r0.y = stage14_y;
    // add r0.y, r0.y, cb0[5].w
    r0.y = r0.y + cb0[5].w;
    // add r0.xy, r0.xyxx, cb0[6].xyxx
    float stage15_x = r0.x + cb0[6].x;
    float stage15_y = r0.y + cb0[6].y;
    r0.x = stage15_x;
    r0.y = stage15_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch16 = t1.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch16.x;
    r0.y = fetch16.y;
    r0.z = fetch16.z;
    // mul r2.xyz, r1.xyzx, r0.xyzx
    float stage17_x = r1.x * r0.x;
    float stage17_y = r1.y * r0.y;
    float stage17_z = r1.z * r0.z;
    r2.x = stage17_x;
    r2.y = stage17_y;
    r2.z = stage17_z;
    // dp3 r0.w, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r0.yzw, -r0.xxyz, r1.xxyz, r0.wwww
    float stage18_y = -r0.x * r1.x + r0.w;
    float stage18_z = -r0.y * r1.y + r0.w;
    float stage18_w = -r0.z * r1.z + r0.w;
    r0.y = stage18_y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mad r0.yzw, cb0[13].xxxx, r0.yyzw, r2.xxyz
    float stage19_y = cb0[13].x * r0.y + r2.x;
    float stage19_z = cb0[13].x * r0.z + r2.y;
    float stage19_w = cb0[13].x * r0.w + r2.z;
    r0.y = stage19_y;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // max r0.yzw, |r0.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage20_y = max(abs(r0.y), 1e-06f);
    float stage20_z = max(abs(r0.z), 1e-06f);
    float stage20_w = max(abs(r0.w), 1e-06f);
    r0.y = stage20_y;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // log r0.yzw, r0.yyzw
    float stage21_y = log2(r0.y);
    float stage21_z = log2(r0.z);
    float stage21_w = log2(r0.w);
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mul r0.yzw, r0.yyzw, cb0[13].yyyy
    float stage22_y = r0.y * cb0[13].y;
    float stage22_z = r0.z * cb0[13].y;
    float stage22_w = r0.w * cb0[13].y;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // exp r0.yzw, r0.yyzw
    float stage23_y = exp2(r0.y);
    float stage23_z = exp2(r0.z);
    float stage23_w = exp2(r0.w);
    r0.y = stage23_y;
    r0.z = stage23_z;
    r0.w = stage23_w;
    // mul r0.yzw, r0.yyzw, cb0[13].zzzz
    float stage24_y = r0.y * cb0[13].z;
    float stage24_z = r0.z * cb0[13].z;
    float stage24_w = r0.w * cb0[13].z;
    r0.y = stage24_y;
    r0.z = stage24_z;
    r0.w = stage24_w;
    // mad r0.yzw, cb0[1].xxyz, r0.yyzw, cb0[2].xxyz
    float stage25_y = cb0[1].x * r0.y + cb0[2].x;
    float stage25_z = cb0[1].y * r0.z + cb0[2].y;
    float stage25_w = cb0[1].z * r0.w + cb0[2].z;
    r0.y = stage25_y;
    r0.z = stage25_z;
    r0.w = stage25_w;
    // mad o0.xyz, r0.yzwy, v5.wwww, v5.xyzx
    float stage26_x = r0.y * v5.w + v5.x;
    float stage26_y = r0.z * v5.w + v5.y;
    float stage26_z = r0.w * v5.w + v5.z;
    o0.x = stage26_x;
    o0.y = stage26_y;
    o0.z = stage26_z;
    // mad r0.yz, v4.xxyx, cb0[8].xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage27_y = v4.x * cb0[8].x + -0.5f;
    float stage27_z = v4.y * cb0[8].y + -0.5f;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // dp2 r1.x, cb0[9].xyxx, r0.yzyy
    r1.x = dot(cb0[9].xy, r0.yz);
    // dp2 r1.y, cb0[10].xyxx, r0.yzyy
    r1.y = dot(cb0[10].xy, r0.yz);
    // add r0.yz, r1.xxyx, cb0[5].zzyz
    float stage28_y = r1.x + cb0[5].z;
    float stage28_z = r1.y + cb0[5].y;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // add r0.yz, r0.yyzy, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage29_y = r0.y + 0.5f;
    float stage29_z = r0.z + 0.5f;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mad r0.xy, r0.xxxx, cb0[14].zzzz, r0.yzyy
    float stage30_x = r0.x * cb0[14].z + r0.y;
    float stage30_y = r0.x * cb0[14].z + r0.z;
    r0.x = stage30_x;
    r0.y = stage30_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s3, l(0.000000)
    float4 fetch31 = t2.SampleBias(s3, r0.xy, 0.0f);
    r0.x = fetch31.x;
    // mad r0.y, v4.y, l(2.000000), l(-1.000000)
    r0.y = v4.y * 2.0f + -1.0f;
    // add r0.y, -|r0.y|, l(1.000000)
    r0.y = -(abs(r0.y)) + 1.0f;
    // log r0.z, |r0.y|
    r0.z = log2(abs(r0.y));
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[14].w
    r0.z = r0.z * cb0[14].w;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // max r0.x, |r0.x|, l(0.000001)
    r0.x = max(abs(r0.x), 1e-06f);
    // log r0.x, r0.x
    r0.x = log2(r0.x);
    // mul r0.x, r0.x, cb0[15].x
    r0.x = r0.x * cb0[15].x;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul_sat r0.x, r0.x, cb0[15].y
    r0.x = saturate(r0.x * cb0[15].y);
    // mul r0.x, r0.x, cb0[1].w
    r0.x = r0.x * cb0[1].w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage32_x = r0.x * v1.x;
    float stage32_y = r0.x * v1.y;
    float stage32_z = r0.x * v1.z;
    r0.x = stage32_x;
    r0.y = stage32_y;
    r0.z = stage32_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage33_x = r0.w * v0.z;
    float stage33_y = r0.w * v0.x;
    float stage33_z = r0.w * v0.y;
    r1.x = stage33_x;
    r1.y = stage33_y;
    r1.z = stage33_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage34_x = r0.x * r1.x;
    float stage34_y = r0.x * r1.y;
    float stage34_z = r0.x * r1.z;
    r0.x = stage34_x;
    r0.y = stage34_y;
    r0.z = stage34_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage35_x = r0.x / r0.w;
    float stage35_y = r0.y / r0.w;
    r0.x = stage35_x;
    r0.y = stage35_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage36_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage36_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage36_x;
    r1.y = stage36_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage37_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage37_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage37_x;
    r1.y = stage37_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage38_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage38_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage38_x;
    r1.y = stage38_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage39_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage39_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage39_x;
    r0.y = stage39_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage40_x = r0.x * 0.5f + 0.5f;
    float stage40_y = r0.y * 0.5f + 0.5f;
    o2.x = stage40_x;
    o2.y = stage40_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage41_z = 1.0f;
    float stage41_w = asfloat(0u);
    o2.z = stage41_z;
    o2.w = stage41_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage42_x = cb2[3].x;
    float stage42_y = cb2[3].y;
    float stage42_z = cb2[3].z;
    o3.x = stage42_x;
    o3.y = stage42_y;
    o3.z = stage42_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage43_x = asfloat(0u);
    float stage43_y = asfloat(0u);
    float stage43_z = asfloat(0u);
    float stage43_w = asfloat(0u);
    o4.x = stage43_x;
    o4.y = stage43_y;
    o4.z = stage43_z;
    o4.w = stage43_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage44_x = asfloat(0u);
    float stage44_y = asfloat(0u);
    float stage44_z = asfloat(0u);
    float stage44_w = asfloat(0u);
    o5.x = stage44_x;
    o5.y = stage44_y;
    o5.z = stage44_z;
    o5.w = stage44_w;

    Shade_Ue3_fx_k_me_makeflow_02_tr_b0cc6c600c41a442b78cb59653ff7fa9d5795bc24595098cc512374cf73ccbaf_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
