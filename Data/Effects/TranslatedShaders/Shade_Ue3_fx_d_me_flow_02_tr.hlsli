// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_me_flow_02_tr_Constants0 : register(b0)
{
    float4 cb0[13];
};
cbuffer Shade_Ue3_fx_d_me_flow_02_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_d_me_flow_02_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_d_me_flow_02_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_d_me_flow_02_tr_OUTPUT Shade_Ue3_fx_d_me_flow_02_tr(Shade_Ue3_fx_d_me_flow_02_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.x, cb0[5].w, cb0[9].w
    r0.x = cb0[5].w * cb0[9].w;
    // add r0.yz, v4.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage1_y = v4.x + -0.5f;
    float stage1_z = v4.y + -0.5f;
    r0.y = stage1_y;
    r0.z = stage1_z;
    // dp2 r1.x, cb0[3].xyxx, r0.yzyy
    r1.x = dot(cb0[3].xy, r0.yz);
    // dp2 r1.y, cb0[4].xyxx, r0.yzyy
    r1.y = dot(cb0[4].xy, r0.yz);
    // add r2.xy, r1.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage2_x = r1.x + 0.5f;
    float stage2_y = r1.y + 0.5f;
    r2.x = stage2_x;
    r2.y = stage2_y;
    // dp2 r0.y, r1.xyxx, r1.xyxx
    r0.y = dot(r1.xy, r1.xy);
    // sqrt r0.y, r0.y
    r0.y = sqrt(r0.y);
    // add r0.y, r0.y, r0.y
    r0.y = r0.y + r0.y;
    // min r0.y, r0.y, l(1.000000)
    r0.y = min(r0.y, 1.0f);
    // add r0.y, -r0.y, l(1.000000)
    r0.y = -r0.y + 1.0f;
    // mul r0.zw, r2.xxxy, cb0[9].xxxy
    float stage3_z = r2.x * cb0[9].x;
    float stage3_w = r2.y * cb0[9].y;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // mad r3.x, cb0[7].y, cb0[8].w, r0.z
    r3.x = cb0[7].y * cb0[8].w + r0.z;
    // mad r3.y, cb0[7].y, cb0[9].z, r0.w
    r3.y = cb0[7].y * cb0[9].z + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r3.xyxx, t0.yzxw, s1, l(0.000000)
    float4 fetch4 = t0.SampleBias(s1, r3.xy, 0.0f);
    r0.z = fetch4.x;
    // mul r0.w, cb0[5].z, cb0[8].z
    r0.w = cb0[5].z * cb0[8].z;
    // mad_sat r2.z, r1.y, r0.w, l(0.500000)
    r2.z = saturate(r1.y * r0.w + 0.5f);
    // mad r0.xz, r0.zzzz, r0.xxxx, r2.xxzx
    float stage5_x = r0.z * r0.x + r2.x;
    float stage5_z = r0.z * r0.x + r2.z;
    r0.x = stage5_x;
    r0.z = stage5_z;
    // mul r1.xy, r0.xzxx, cb0[7].zwzz
    float stage6_x = r0.x * cb0[7].z;
    float stage6_y = r0.z * cb0[7].w;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // mad r2.x, cb0[7].y, cb0[7].x, r1.x
    r2.x = cb0[7].y * cb0[7].x + r1.x;
    // mad r2.y, cb0[7].y, cb0[10].x, r1.y
    r2.y = cb0[7].y * cb0[10].x + r1.y;
    // mul r1.xy, r2.xyxx, l(1.660000, 1.660000, 0.000000, 0.000000)
    float stage7_x = r2.x * 1.66f;
    float stage7_y = r2.y * 1.66f;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r2.xyxx, t2.xyzw, s0, l(-1.000000)
    float4 fetch8 = t2.SampleLevel(s0, r2.xy, -1.0f);
    r2.x = fetch8.x;
    r2.y = fetch8.y;
    r2.z = fetch8.z;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t2.xyzw, s0, l(-1.000000)
    float4 fetch9 = t2.SampleLevel(s0, r1.xy, -1.0f);
    r1.x = fetch9.x;
    r1.y = fetch9.y;
    r1.z = fetch9.z;
    // mul r3.xyz, r1.xyzx, r2.xyzx
    float stage10_x = r1.x * r2.x;
    float stage10_y = r1.y * r2.y;
    float stage10_z = r1.z * r2.z;
    r3.x = stage10_x;
    r3.y = stage10_y;
    r3.z = stage10_z;
    // dp3 r0.w, r3.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r3.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r2.xyzx, r1.xyzx, r0.wwww
    float stage11_x = -r2.x * r1.x + r0.w;
    float stage11_y = -r2.y * r1.y + r0.w;
    float stage11_z = -r2.z * r1.z + r0.w;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    // mad r1.xyz, cb0[10].yyyy, r1.xyzx, r3.xyzx
    float stage12_x = cb0[10].y * r1.x + r3.x;
    float stage12_y = cb0[10].y * r1.y + r3.y;
    float stage12_z = cb0[10].y * r1.z + r3.z;
    r1.x = stage12_x;
    r1.y = stage12_y;
    r1.z = stage12_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage13_x = max(abs(r1.x), 1e-06f);
    float stage13_y = max(abs(r1.y), 1e-06f);
    float stage13_z = max(abs(r1.z), 1e-06f);
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    // log r1.xyz, r1.xyzx
    float stage14_x = log2(r1.x);
    float stage14_y = log2(r1.y);
    float stage14_z = log2(r1.z);
    r1.x = stage14_x;
    r1.y = stage14_y;
    r1.z = stage14_z;
    // mul r1.xyz, r1.xyzx, cb0[10].zzzz
    float stage15_x = r1.x * cb0[10].z;
    float stage15_y = r1.y * cb0[10].z;
    float stage15_z = r1.z * cb0[10].z;
    r1.x = stage15_x;
    r1.y = stage15_y;
    r1.z = stage15_z;
    // exp r1.xyz, r1.xyzx
    float stage16_x = exp2(r1.x);
    float stage16_y = exp2(r1.y);
    float stage16_z = exp2(r1.z);
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mul r2.xyz, cb0[6].xyzx, cb0[6].wwww
    float stage17_x = cb0[6].x * cb0[6].w;
    float stage17_y = cb0[6].y * cb0[6].w;
    float stage17_z = cb0[6].z * cb0[6].w;
    r2.x = stage17_x;
    r2.y = stage17_y;
    r2.z = stage17_z;
    // mul r1.xyz, r1.xyzx, r2.xyzx
    float stage18_x = r1.x * r2.x;
    float stage18_y = r1.y * r2.y;
    float stage18_z = r1.z * r2.z;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    // add r0.w, cb0[5].y, l(-1.000000)
    r0.w = cb0[5].y + -1.0f;
    // add r0.x, r0.x, r0.w
    r0.x = r0.x + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r0.xzxx, t3.yzwx, s2, l(0.000000)
    float4 fetch19 = t3.SampleBias(s2, r0.xz, 0.0f);
    r0.w = fetch19.x;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.x, r0.xzxx, t1.xyzw, s3, l(-1.000000)
    float4 fetch20 = t1.SampleLevel(s3, r0.xz, -1.0f);
    r0.x = fetch20.x;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul_sat r0.x, r0.x, cb0[11].y
    r0.x = saturate(r0.x * cb0[11].y);
    // log r0.y, |r0.w|
    r0.y = log2(abs(r0.w));
    // lt r0.z, |r0.w|, l(0.000001)
    r0.z = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[10].w
    r0.y = r0.y * cb0[10].w;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, cb0[11].x
    r0.y = r0.y * cb0[11].x;
    // movc r0.y, r0.z, l(0), r0.y
    r0.y = asuint(r0.z) != 0u ? asfloat(0u) : r0.y;
    // mad r0.yzw, r0.yyyy, r1.xxyz, r1.xxyz
    float stage21_y = r0.y * r1.x + r1.x;
    float stage21_z = r0.y * r1.y + r1.y;
    float stage21_w = r0.y * r1.z + r1.z;
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mad r0.yzw, r0.yyzw, cb0[1].xxyz, cb0[2].xxyz
    float stage22_y = r0.y * cb0[1].x + cb0[2].x;
    float stage22_z = r0.z * cb0[1].y + cb0[2].y;
    float stage22_w = r0.w * cb0[1].z + cb0[2].z;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // mad o0.xyz, r0.yzwy, v5.wwww, v5.xyzx
    float stage23_x = r0.y * v5.w + v5.x;
    float stage23_y = r0.z * v5.w + v5.y;
    float stage23_z = r0.w * v5.w + v5.z;
    o0.x = stage23_x;
    o0.y = stage23_y;
    o0.z = stage23_z;
    // log r0.y, r0.x
    r0.y = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[11].z
    r0.y = r0.y * cb0[11].z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul_sat r0.y, r0.y, cb0[1].w
    r0.y = saturate(r0.y * cb0[1].w);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // add r0.x, r0.x, cb0[12].x
    r0.x = r0.x + cb0[12].x;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage24_x = r0.x * v1.x;
    float stage24_y = r0.x * v1.y;
    float stage24_z = r0.x * v1.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage25_x = r0.w * v0.z;
    float stage25_y = r0.w * v0.x;
    float stage25_z = r0.w * v0.y;
    r1.x = stage25_x;
    r1.y = stage25_y;
    r1.z = stage25_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage26_x = r0.x * r1.x;
    float stage26_y = r0.x * r1.y;
    float stage26_z = r0.x * r1.z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage27_x = r0.x / r0.w;
    float stage27_y = r0.y / r0.w;
    r0.x = stage27_x;
    r0.y = stage27_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage28_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage28_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage28_x;
    r1.y = stage28_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage29_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage29_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage29_x;
    r1.y = stage29_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage30_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage30_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage31_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage31_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage31_x;
    r0.y = stage31_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage32_x = r0.x * 0.5f + 0.5f;
    float stage32_y = r0.y * 0.5f + 0.5f;
    o2.x = stage32_x;
    o2.y = stage32_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage33_z = 1.0f;
    float stage33_w = asfloat(0u);
    o2.z = stage33_z;
    o2.w = stage33_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage34_x = cb2[3].x;
    float stage34_y = cb2[3].y;
    float stage34_z = cb2[3].z;
    o3.x = stage34_x;
    o3.y = stage34_y;
    o3.z = stage34_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage35_x = asfloat(0u);
    float stage35_y = asfloat(0u);
    float stage35_z = asfloat(0u);
    float stage35_w = asfloat(0u);
    o4.x = stage35_x;
    o4.y = stage35_y;
    o4.z = stage35_z;
    o4.w = stage35_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage36_x = asfloat(0u);
    float stage36_y = asfloat(0u);
    float stage36_z = asfloat(0u);
    float stage36_w = asfloat(0u);
    o5.x = stage36_x;
    o5.y = stage36_y;
    o5.z = stage36_z;
    o5.w = stage36_w;

    Shade_Ue3_fx_d_me_flow_02_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
