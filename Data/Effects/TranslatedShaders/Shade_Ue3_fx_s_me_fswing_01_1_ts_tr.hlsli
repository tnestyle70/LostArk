// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_s_me_fswing_01_1_ts_tr_Constants0 : register(b0)
{
    float4 cb0[21];
};
cbuffer Shade_Ue3_fx_s_me_fswing_01_1_ts_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_s_me_fswing_01_1_ts_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_s_me_fswing_01_1_ts_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_s_me_fswing_01_1_ts_tr_OUTPUT Shade_Ue3_fx_s_me_fswing_01_1_ts_tr(Shade_Ue3_fx_s_me_fswing_01_1_ts_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.x, v4.y, cb0[19].x
    r0.x = v4.y * cb0[19].x;
    // mad r0.x, cb0[12].x, cb0[19].y, r0.x
    r0.x = cb0[12].x * cb0[19].y + r0.x;
    // mul r0.x, r0.x, l(6.283185)
    r0.x = r0.x * 6.283185f;
    // sincos r0.x, null, r0.x
    float stage1_x = r0.x;
    r0.x = sin(stage1_x);
    // add r0.x, r0.x, l(0.800000)
    r0.x = r0.x + 0.8f;
    // mul r0.yz, v4.xxyx, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage2_y = v4.x * 0.5f;
    float stage2_z = v4.y * 0.5f;
    r0.y = stage2_y;
    r0.z = stage2_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t0.yxzw, s3, l(0.000000)
    float4 fetch3 = t0.SampleBias(s3, r0.yz, 0.0f);
    r0.y = fetch3.x;
    // add r0.y, r0.y, l(-0.300000)
    r0.y = r0.y + -0.3f;
    // mul r0.y, r0.y, l(0.300000)
    r0.y = r0.y * 0.3f;
    // mad r0.x, r0.x, l(0.700000), r0.y
    r0.x = r0.x * 0.7f + r0.y;
    // mul r0.y, cb0[8].z, cb0[19].z
    r0.y = cb0[8].z * cb0[19].z;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul r1.xy, v4.xyxx, cb0[18].zwzz
    float stage4_x = v4.x * cb0[18].z;
    float stage4_y = v4.y * cb0[18].w;
    r1.x = stage4_x;
    r1.y = stage4_y;
    // log r0.y, |r1.y|
    r0.y = log2(abs(r1.y));
    // mul r0.yz, r0.yyyy, l(0.000000, 0.450000, 0.800000, 0.000000)
    float stage5_y = r0.y * 0.45f;
    float stage5_z = r0.y * 0.8f;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // exp r0.yz, r0.yyzy
    float stage6_y = exp2(r0.y);
    float stage6_z = exp2(r0.z);
    r0.y = stage6_y;
    r0.z = stage6_z;
    // lt r0.w, |r1.y|, l(0.000001)
    r0.w = asfloat((abs(r1.y) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.yz, r0.wwww, l(0,0,0,0), r0.yyzy
    float stage7_y = asuint(r0.w) != 0u ? asfloat(0u) : r0.y;
    float stage7_z = asuint(r0.w) != 0u ? asfloat(0u) : r0.z;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // mul r2.x, r0.x, r0.z
    r2.x = r0.x * r0.z;
    // add r0.x, r0.y, cb0[8].x
    r0.x = r0.y + cb0[8].x;
    // mul r1.z, r0.x, cb0[8].y
    r1.z = r0.x * cb0[8].y;
    // add r0.xy, r1.xzxx, cb0[9].xyxx
    float stage8_x = r1.x + cb0[9].x;
    float stage8_y = r1.z + cb0[9].y;
    r0.x = stage8_x;
    r0.y = stage8_y;
    // mov r2.y, l(0)
    r2.y = asfloat(0u);
    // add r0.xy, r0.xyxx, r2.xyxx
    float stage9_x = r0.x + r2.x;
    float stage9_y = r0.y + r2.y;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // add r0.xy, r0.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage10_x = r0.x + -0.5f;
    float stage10_y = r0.y + -0.5f;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // dp2 r1.x, cb0[10].xyxx, r0.xyxx
    r1.x = dot(cb0[10].xy, r0.xy);
    // dp2 r1.y, cb0[11].xyxx, r0.xyxx
    r1.y = dot(cb0[11].xy, r0.xy);
    // add r0.xy, r1.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage11_x = r1.x + 0.5f;
    float stage11_y = r1.y + 0.5f;
    r0.x = stage11_x;
    r0.y = stage11_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s4, l(0.000000)
    float4 fetch12 = t1.SampleBias(s4, r0.xy, 0.0f);
    r0.x = fetch12.x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[19].w
    r0.y = r0.y * cb0[19].w;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mad r0.y, v4.y, l(0.500000), l(0.500000)
    r0.y = v4.y * 0.5f + 0.5f;
    // add r0.y, -|r0.y|, l(1.000000)
    r0.y = -(abs(r0.y)) + 1.0f;
    // log r0.z, |r0.y|
    r0.z = log2(abs(r0.y));
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, l(1.700000)
    r0.z = r0.z * 1.7f;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // min r0.z, r0.z, l(1.000000)
    r0.z = min(r0.z, 1.0f);
    // mul r0.w, |v4.y|, |v4.y|
    r0.w = abs(v4.y) * abs(v4.y);
    // mul r0.z, r0.w, r0.z
    r0.z = r0.w * r0.z;
    // lt r0.w, |v4.y|, l(0.000001)
    r0.w = asfloat((abs(v4.y) < 1e-06f) ? 0xffffffffu : 0u);
    // or r0.y, r0.w, r0.y
    r0.y = asfloat(asuint(r0.w) | asuint(r0.y));
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul r0.x, r0.x, l(50.000000)
    r0.x = r0.x * 50.0f;
    // add r0.y, -v4.y, l(1.000000)
    r0.y = -v4.y + 1.0f;
    // mul r0.z, |r0.y|, |r0.y|
    r0.z = abs(r0.y) * abs(r0.y);
    // mul r0.z, r0.z, |r0.y|
    r0.z = r0.z * abs(r0.y);
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r0.y, r0.y, r0.x
    r0.y = r0.y * r0.x;
    // mad r0.x, r0.y, cb0[20].x, r0.x
    r0.x = r0.y * cb0[20].x + r0.x;
    // mul r0.x, r0.x, cb0[1].w
    r0.x = r0.x * cb0[1].w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // mul r0.xy, v4.xyxx, cb0[15].xyxx
    float stage13_x = v4.x * cb0[15].x;
    float stage13_y = v4.y * cb0[15].y;
    r0.x = stage13_x;
    r0.y = stage13_y;
    // mad r0.zw, r0.xxxy, l(0.000000, 0.000000, 0.300000, 0.300000), cb0[4].xxxy
    float stage14_z = r0.x * 0.3f + cb0[4].x;
    float stage14_w = r0.y * 0.3f + cb0[4].y;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r0.zwzz, t3.yzxw, s1, l(0.000000)
    float4 fetch15 = t3.SampleBias(s1, r0.zw, 0.0f);
    r0.z = fetch15.x;
    // mul r0.xy, r0.xyxx, r0.zzzz
    float stage16_x = r0.x * r0.z;
    float stage16_y = r0.y * r0.z;
    r0.x = stage16_x;
    r0.y = stage16_y;
    // mul r0.xy, r0.xyxx, cb0[16].xxxx
    float stage17_x = r0.x * cb0[16].x;
    float stage17_y = r0.y * cb0[16].x;
    r0.x = stage17_x;
    r0.y = stage17_y;
    // mad r0.xy, v4.xyxx, cb0[14].zwzz, r0.xyxx
    float stage18_x = v4.x * cb0[14].z + r0.x;
    float stage18_y = v4.y * cb0[14].w + r0.y;
    r0.x = stage18_x;
    r0.y = stage18_y;
    // add r0.xy, r0.xyxx, cb0[5].xyxx
    float stage19_x = r0.x + cb0[5].x;
    float stage19_y = r0.y + cb0[5].y;
    r0.x = stage19_x;
    r0.y = stage19_y;
    // add r0.xy, r0.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage20_x = r0.x + -0.5f;
    float stage20_y = r0.y + -0.5f;
    r0.x = stage20_x;
    r0.y = stage20_y;
    // dp2 r1.x, cb0[6].xyxx, r0.xyxx
    r1.x = dot(cb0[6].xy, r0.xy);
    // dp2 r1.y, cb0[7].xyxx, r0.xyxx
    r1.y = dot(cb0[7].xy, r0.xy);
    // add r0.xy, r1.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage21_x = r1.x + 0.5f;
    float stage21_y = r1.y + 0.5f;
    r0.x = stage21_x;
    r0.y = stage21_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t4.xyzw, s2, l(0.000000)
    float4 fetch22 = t4.SampleBias(s2, r0.xy, 0.0f);
    r0.x = fetch22.x;
    r0.y = fetch22.y;
    r0.z = fetch22.z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xyzx, r0.wwww
    float stage23_x = -r0.x + r0.w;
    float stage23_y = -r0.y + r0.w;
    float stage23_z = -r0.z + r0.w;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // mad r0.xyz, cb0[16].zzzz, r1.xyzx, r0.xyzx
    float stage24_x = cb0[16].z * r1.x + r0.x;
    float stage24_y = cb0[16].z * r1.y + r0.y;
    float stage24_z = cb0[16].z * r1.z + r0.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage25_x = max(abs(r0.x), 1e-06f);
    float stage25_y = max(abs(r0.y), 1e-06f);
    float stage25_z = max(abs(r0.z), 1e-06f);
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // log r0.xyz, r0.xyzx
    float stage26_x = log2(r0.x);
    float stage26_y = log2(r0.y);
    float stage26_z = log2(r0.z);
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // mul r0.xyz, r0.xyzx, cb0[16].wwww
    float stage27_x = r0.x * cb0[16].w;
    float stage27_y = r0.y * cb0[16].w;
    float stage27_z = r0.z * cb0[16].w;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // exp r0.xyz, r0.xyzx
    float stage28_x = exp2(r0.x);
    float stage28_y = exp2(r0.y);
    float stage28_z = exp2(r0.z);
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // mul r0.xyz, r0.xyzx, cb0[17].xxxx
    float stage29_x = r0.x * cb0[17].x;
    float stage29_y = r0.y * cb0[17].x;
    float stage29_z = r0.z * cb0[17].x;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mad r1.xy, v4.xyxx, l(1.000000, 4.000000, 0.000000, 0.000000), cb0[3].xyxx
    float stage30_x = v4.x * 1.0f + cb0[3].x;
    float stage30_y = v4.y * 4.0f + cb0[3].y;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t2.xyzw, s0, l(0.000000)
    float4 fetch31 = t2.SampleBias(s0, r1.xy, 0.0f);
    r1.x = fetch31.x;
    r1.y = fetch31.y;
    r1.z = fetch31.z;
    // dp3 r0.w, r1.xyzx, r1.xyzx
    r0.w = dot(r1.xyz, r1.xyz);
    // dp3 r1.x, r0.wwww, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.www, float3(0.3f, 0.59f, 0.11f));
    // add r1.x, -r0.w, r1.x
    r1.x = -r0.w + r1.x;
    // mad r0.w, cb0[13].y, r1.x, r0.w
    r0.w = cb0[13].y * r1.x + r0.w;
    // max r0.w, |r0.w|, l(0.000001)
    r0.w = max(abs(r0.w), 1e-06f);
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, cb0[13].z
    r0.w = r0.w * cb0[13].z;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mad r0.xyz, cb0[13].wwww, r0.wwww, r0.xyzx
    float stage32_x = cb0[13].w * r0.w + r0.x;
    float stage32_y = cb0[13].w * r0.w + r0.y;
    float stage32_z = cb0[13].w * r0.w + r0.z;
    r0.x = stage32_x;
    r0.y = stage32_y;
    r0.z = stage32_z;
    // mad r0.xyz, r0.xyzx, cb0[1].xyzx, cb0[2].xyzx
    float stage33_x = r0.x * cb0[1].x + cb0[2].x;
    float stage33_y = r0.y * cb0[1].y + cb0[2].y;
    float stage33_z = r0.z * cb0[1].z + cb0[2].z;
    r0.x = stage33_x;
    r0.y = stage33_y;
    r0.z = stage33_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage34_x = r0.x * v5.w + v5.x;
    float stage34_y = r0.y * v5.w + v5.y;
    float stage34_z = r0.z * v5.w + v5.z;
    o0.x = stage34_x;
    o0.y = stage34_y;
    o0.z = stage34_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage35_x = r0.x * v1.x;
    float stage35_y = r0.x * v1.y;
    float stage35_z = r0.x * v1.z;
    r0.x = stage35_x;
    r0.y = stage35_y;
    r0.z = stage35_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage36_x = r0.w * v0.z;
    float stage36_y = r0.w * v0.x;
    float stage36_z = r0.w * v0.y;
    r1.x = stage36_x;
    r1.y = stage36_y;
    r1.z = stage36_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage37_x = r0.x * r1.x;
    float stage37_y = r0.x * r1.y;
    float stage37_z = r0.x * r1.z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage38_x = r0.x / r0.w;
    float stage38_y = r0.y / r0.w;
    r0.x = stage38_x;
    r0.y = stage38_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage39_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage39_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage39_x;
    r1.y = stage39_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage40_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage40_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage40_x;
    r1.y = stage40_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage41_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage41_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage41_x;
    r1.y = stage41_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage42_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage42_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage42_x;
    r0.y = stage42_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage43_x = r0.x * 0.5f + 0.5f;
    float stage43_y = r0.y * 0.5f + 0.5f;
    o2.x = stage43_x;
    o2.y = stage43_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage44_z = 1.0f;
    float stage44_w = asfloat(0u);
    o2.z = stage44_z;
    o2.w = stage44_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage45_x = cb2[3].x;
    float stage45_y = cb2[3].y;
    float stage45_z = cb2[3].z;
    o3.x = stage45_x;
    o3.y = stage45_y;
    o3.z = stage45_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage46_x = asfloat(0u);
    float stage46_y = asfloat(0u);
    float stage46_z = asfloat(0u);
    float stage46_w = asfloat(0u);
    o4.x = stage46_x;
    o4.y = stage46_y;
    o4.z = stage46_z;
    o4.w = stage46_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage47_x = asfloat(0u);
    float stage47_y = asfloat(0u);
    float stage47_z = asfloat(0u);
    float stage47_w = asfloat(0u);
    o5.x = stage47_x;
    o5.y = stage47_y;
    o5.z = stage47_z;
    o5.w = stage47_w;

    Shade_Ue3_fx_s_me_fswing_01_1_ts_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
