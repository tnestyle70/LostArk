// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_pa_makeflow_03_tr_Constants0 : register(b0)
{
    float4 cb0[13];
};
cbuffer Shade_Ue3_fx_k_pa_makeflow_03_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_k_pa_makeflow_03_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_pa_makeflow_03_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_pa_makeflow_03_tr_OUTPUT Shade_Ue3_fx_k_pa_makeflow_03_tr(Shade_Ue3_fx_k_pa_makeflow_03_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.yxyy, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.y + -0.5f;
    float stage1_y = v2.x + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // max r0.z, |r0.y|, |r0.x|
    r0.z = max(abs(r0.y), abs(r0.x));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r0.y|, |r0.x|
    r0.w = min(abs(r0.y), abs(r0.x));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.x, r0.w, l(0.020835), l(-0.085133)
    r1.x = r0.w * 0.020835f + -0.085133f;
    // mad r1.x, r0.w, r1.x, l(0.180141)
    r1.x = r0.w * r1.x + 0.180141f;
    // mad r1.x, r0.w, r1.x, l(-0.330299)
    r1.x = r0.w * r1.x + -0.330299f;
    // mad r0.w, r0.w, r1.x, l(0.999866)
    r0.w = r0.w * r1.x + 0.999866f;
    // mul r1.x, r0.w, r0.z
    r1.x = r0.w * r0.z;
    // mad r1.x, r1.x, l(-2.000000), l(1.570796)
    r1.x = r1.x * -2.0f + 1.570796f;
    // lt r1.y, |r0.y|, |r0.x|
    r1.y = asfloat((abs(r0.y) < abs(r0.x)) ? 0xffffffffu : 0u);
    // and r1.x, r1.y, r1.x
    r1.x = asfloat(asuint(r1.y) & asuint(r1.x));
    // mad r0.z, r0.z, r0.w, r1.x
    r0.z = r0.z * r0.w + r1.x;
    // lt r0.w, r0.y, -r0.y
    r0.w = asfloat((r0.y < -r0.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r0.y, r0.x
    r0.w = min(r0.y, r0.x);
    // lt r0.w, r0.w, -r0.w
    r0.w = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // max r1.x, r0.y, r0.x
    r1.x = max(r0.y, r0.x);
    // dp2 r0.x, -r0.xyxx, -r0.xyxx
    r0.x = dot(-r0.xy, -r0.xy);
    // sqrt r0.y, r0.x
    r0.y = sqrt(r0.x);
    // ge r1.x, r1.x, -r1.x
    r1.x = asfloat((r1.x >= -r1.x) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, r1.x
    r0.w = asfloat(asuint(r0.w) & asuint(r1.x));
    // movc r0.z, r0.w, -r0.z, r0.z
    r0.z = asuint(r0.w) != 0u ? -r0.z : r0.z;
    // mad r0.z, r0.z, l(0.318471), l(1.000000)
    r0.z = r0.z * 0.318471f + 1.0f;
    // mul r0.x, r0.z, l(0.500000)
    r0.x = r0.z * 0.5f;
    // mad r0.zw, r0.xxxy, cb0[3].xxxy, cb0[4].xxxy
    float stage2_z = r0.x * cb0[3].x + cb0[4].x;
    float stage2_w = r0.y * cb0[3].y + cb0[4].y;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r0.zwzz, t0.xyzw, s0, l(0.000000)
    float4 fetch3 = t0.SampleBias(s0, r0.zw, 0.0f);
    r1.x = fetch3.x;
    // mov r2.w, cb0[9].z
    r2.w = cb0[9].z;
    // mov r2.yz, l(0,0,0,0)
    float stage4_y = asfloat(0u);
    float stage4_z = asfloat(0u);
    r2.y = stage4_y;
    r2.z = stage4_z;
    // add r3.xyzw, r0.zwzw, r2.wzzw
    float stage5_x = r0.z + r2.w;
    float stage5_y = r0.w + r2.z;
    float stage5_z = r0.z + r2.z;
    float stage5_w = r0.w + r2.w;
    r3.x = stage5_x;
    r3.y = stage5_y;
    r3.z = stage5_z;
    r3.w = stage5_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r3.xyxx, t0.yzxw, s0, l(0.000000)
    float4 fetch6 = t0.SampleBias(s0, r3.xy, 0.0f);
    r0.z = fetch6.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r3.zwzz, t0.yzwx, s0, l(0.000000)
    float4 fetch7 = t0.SampleBias(s0, r3.zw, 0.0f);
    r0.w = fetch7.x;
    // add r3.xy, -r1.xxxx, r0.zwzz
    float stage8_x = -r1.x + r0.z;
    float stage8_y = -r1.x + r0.w;
    r3.x = stage8_x;
    r3.y = stage8_y;
    // mul r1.xy, r3.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage9_x = r3.x * 8.0f;
    float stage9_y = r3.y * 8.0f;
    r1.x = stage9_x;
    r1.y = stage9_y;
    // mov r1.z, l(0)
    r1.z = asfloat(0u);
    // add r1.xyz, -r1.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage10_x = -r1.x + 0.0f;
    float stage10_y = -r1.y + 0.0f;
    float stage10_z = -r1.z + 1.0f;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // dp3 r0.z, r1.xyzx, r1.xyzx
    r0.z = dot(r1.xyz, r1.xyz);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // div r0.zw, r1.xxxy, r0.zzzz
    float stage11_z = r1.x / r0.z;
    float stage11_w = r1.y / r0.z;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // mad r0.zw, r0.zzzw, l(0.000000, 0.000000, 0.500000, 0.500000), l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage12_z = r0.z * 0.5f + 0.5f;
    float stage12_w = r0.w * 0.5f + 0.5f;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mul r0.zw, r0.zzzw, v4.xxxx
    float stage13_z = r0.z * v4.x;
    float stage13_w = r0.w * v4.x;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // mad r1.xy, r0.xyxx, cb0[2].xyxx, r0.zwzz
    float stage14_x = r0.x * cb0[2].x + r0.z;
    float stage14_y = r0.y * cb0[2].y + r0.w;
    r1.x = stage14_x;
    r1.y = stage14_y;
    // mov r3.x, v4.w
    r3.x = v4.w;
    // mov r3.y, l(0)
    r3.y = asfloat(0u);
    // add r1.xy, r1.xyxx, r3.xyxx
    float stage15_x = r1.x + r3.x;
    float stage15_y = r1.y + r3.y;
    r1.x = stage15_x;
    r1.y = stage15_y;
    // add r1.xy, r1.xyxx, cb0[5].xyxx
    float stage16_x = r1.x + cb0[5].x;
    float stage16_y = r1.y + cb0[5].y;
    r1.x = stage16_x;
    r1.y = stage16_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t2.xyzw, s1, l(0.000000)
    float4 fetch17 = t2.SampleBias(s1, r1.xy, 0.0f);
    r1.x = fetch17.x;
    r1.y = fetch17.y;
    r1.z = fetch17.z;
    // mad r2.zw, r0.xxxy, cb0[6].xxxy, r0.zzzw
    float stage18_z = r0.x * cb0[6].x + r0.z;
    float stage18_w = r0.y * cb0[6].y + r0.w;
    r2.z = stage18_z;
    r2.w = stage18_w;
    // add r2.zw, r2.zzzw, cb0[7].xxxy
    float stage19_z = r2.z + cb0[7].x;
    float stage19_w = r2.w + cb0[7].y;
    r2.z = stage19_z;
    r2.w = stage19_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r3.xyz, r2.zwzz, t3.xyzw, s2, l(0.000000)
    float4 fetch20 = t3.SampleBias(s2, r2.zw, 0.0f);
    r3.x = fetch20.x;
    r3.y = fetch20.y;
    r3.z = fetch20.z;
    // mul r4.xyz, r1.xyzx, r3.xyzx
    float stage21_x = r1.x * r3.x;
    float stage21_y = r1.y * r3.y;
    float stage21_z = r1.z * r3.z;
    r4.x = stage21_x;
    r4.y = stage21_y;
    r4.z = stage21_z;
    // dp3 r1.w, r4.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.w = dot(r4.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r1.xyzx, r3.xyzx, r1.wwww
    float stage22_x = -r1.x * r3.x + r1.w;
    float stage22_y = -r1.y * r3.y + r1.w;
    float stage22_z = -r1.z * r3.z + r1.w;
    r1.x = stage22_x;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // mad r1.xyz, cb0[10].zzzz, r1.xyzx, r4.xyzx
    float stage23_x = cb0[10].z * r1.x + r4.x;
    float stage23_y = cb0[10].z * r1.y + r4.y;
    float stage23_z = cb0[10].z * r1.z + r4.z;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage24_x = max(abs(r1.x), 1e-06f);
    float stage24_y = max(abs(r1.y), 1e-06f);
    float stage24_z = max(abs(r1.z), 1e-06f);
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // log r1.xyz, r1.xyzx
    float stage25_x = log2(r1.x);
    float stage25_y = log2(r1.y);
    float stage25_z = log2(r1.z);
    r1.x = stage25_x;
    r1.y = stage25_y;
    r1.z = stage25_z;
    // mul r1.xyz, r1.xyzx, cb0[10].wwww
    float stage26_x = r1.x * cb0[10].w;
    float stage26_y = r1.y * cb0[10].w;
    float stage26_z = r1.z * cb0[10].w;
    r1.x = stage26_x;
    r1.y = stage26_y;
    r1.z = stage26_z;
    // exp r1.xyz, r1.xyzx
    float stage27_x = exp2(r1.x);
    float stage27_y = exp2(r1.y);
    float stage27_z = exp2(r1.z);
    r1.x = stage27_x;
    r1.y = stage27_y;
    r1.z = stage27_z;
    // mul r1.xyz, r1.xyzx, cb0[11].xxxx
    float stage28_x = r1.x * cb0[11].x;
    float stage28_y = r1.y * cb0[11].x;
    float stage28_z = r1.z * cb0[11].x;
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // mad r1.xyz, v3.xyzx, r1.xyzx, cb0[1].xyzx
    float stage29_x = v3.x * r1.x + cb0[1].x;
    float stage29_y = v3.y * r1.y + cb0[1].y;
    float stage29_z = v3.z * r1.z + cb0[1].z;
    r1.x = stage29_x;
    r1.y = stage29_y;
    r1.z = stage29_z;
    // mad o0.xyz, r1.xyzx, v5.wwww, v5.xyzx
    float stage30_x = r1.x * v5.w + v5.x;
    float stage30_y = r1.y * v5.w + v5.y;
    float stage30_z = r1.z * v5.w + v5.z;
    o0.x = stage30_x;
    o0.y = stage30_y;
    o0.z = stage30_z;
    // mov r2.x, v4.y
    r2.x = v4.y;
    // mad r1.xy, r0.xyxx, cb0[8].xyxx, r2.xyxx
    float stage31_x = r0.x * cb0[8].x + r2.x;
    float stage31_y = r0.y * cb0[8].y + r2.y;
    r1.x = stage31_x;
    r1.y = stage31_y;
    // mad r0.yz, cb0[11].wwww, r0.zzwz, r1.xxyx
    float stage32_y = cb0[11].w * r0.z + r1.x;
    float stage32_z = cb0[11].w * r0.w + r1.y;
    r0.y = stage32_y;
    r0.z = stage32_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t1.yxzw, s3, l(0.000000)
    float4 fetch33 = t1.SampleBias(s3, r0.yz, 0.0f);
    r0.y = fetch33.x;
    // max r0.y, |r0.y|, l(0.000001)
    r0.y = max(abs(r0.y), 1e-06f);
    // log r0.y, r0.y
    r0.y = log2(r0.y);
    // mul r0.y, r0.y, cb0[12].x
    r0.y = r0.y * cb0[12].x;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // add r0.z, -|r0.x|, l(1.000000)
    r0.z = -(abs(r0.x)) + 1.0f;
    // mul r0.x, r0.z, |r0.x|
    r0.x = r0.z * abs(r0.x);
    // lt r0.z, |r0.x|, l(0.000001)
    r0.z = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // log r0.x, |r0.x|
    r0.x = log2(abs(r0.x));
    // mul r0.xy, r0.xyxx, cb0[12].zyzz
    float stage34_x = r0.x * cb0[12].z;
    float stage34_y = r0.y * cb0[12].y;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // movc r0.x, r0.z, l(0), r0.x
    r0.x = asuint(r0.z) != 0u ? asfloat(0u) : r0.x;
    // mul_sat r0.x, r0.y, r0.x
    r0.x = saturate(r0.y * r0.x);
    // mul r0.x, r0.x, v3.w
    r0.x = r0.x * v3.w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage35_x = r0.x * v1.x;
    float stage35_y = r0.x * v1.y;
    float stage35_z = r0.x * v1.z;
    r0.x = stage35_x;
    r0.y = stage35_y;
    r0.z = stage35_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage36_x = r0.w * v0.z;
    float stage36_y = r0.w * v0.x;
    float stage36_z = r0.w * v0.y;
    r1.x = stage36_x;
    r1.y = stage36_y;
    r1.z = stage36_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage37_x = r0.x * r1.x;
    float stage37_y = r0.x * r1.y;
    float stage37_z = r0.x * r1.z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage38_x = r0.x / r0.w;
    float stage38_y = r0.y / r0.w;
    r0.x = stage38_x;
    r0.y = stage38_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage39_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage39_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage39_x;
    r1.y = stage39_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage40_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage40_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage40_x;
    r1.y = stage40_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage41_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage41_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage41_x;
    r1.y = stage41_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage42_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage42_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage42_x;
    r0.y = stage42_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage43_x = r0.x * 0.5f + 0.5f;
    float stage43_y = r0.y * 0.5f + 0.5f;
    o2.x = stage43_x;
    o2.y = stage43_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage44_z = 1.0f;
    float stage44_w = asfloat(0u);
    o2.z = stage44_z;
    o2.w = stage44_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage45_x = cb2[3].x;
    float stage45_y = cb2[3].y;
    float stage45_z = cb2[3].z;
    o3.x = stage45_x;
    o3.y = stage45_y;
    o3.z = stage45_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage46_x = asfloat(0u);
    float stage46_y = asfloat(0u);
    float stage46_z = asfloat(0u);
    float stage46_w = asfloat(0u);
    o4.x = stage46_x;
    o4.y = stage46_y;
    o4.z = stage46_z;
    o4.w = stage46_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage47_x = asfloat(0u);
    float stage47_y = asfloat(0u);
    float stage47_z = asfloat(0u);
    float stage47_w = asfloat(0u);
    o5.x = stage47_x;
    o5.y = stage47_y;
    o5.z = stage47_z;
    o5.w = stage47_w;

    Shade_Ue3_fx_k_pa_makeflow_03_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
