// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_s_pa_glasshole_02_tr_Constants0 : register(b0)
{
    float4 cb0[11];
};
cbuffer Shade_Ue3_fx_s_pa_glasshole_02_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
SamplerState s6 : register(s6);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);
Texture2D t6 : register(t6);

struct Shade_Ue3_fx_s_pa_glasshole_02_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    float4 v6;
    float4 v7;
};

struct Shade_Ue3_fx_s_pa_glasshole_02_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_s_pa_glasshole_02_tr_OUTPUT Shade_Ue3_fx_s_pa_glasshole_02_tr(Shade_Ue3_fx_s_pa_glasshole_02_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.xyxx, -cb0[2].xyxx
    float stage1_x = v2.x + -cb0[2].x;
    float stage1_y = v2.y + -cb0[2].y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // max r0.z, |r0.y|, |r0.x|
    r0.z = max(abs(r0.y), abs(r0.x));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r0.y|, |r0.x|
    r0.w = min(abs(r0.y), abs(r0.x));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.x, r0.w, l(0.020835), l(-0.085133)
    r1.x = r0.w * 0.020835f + -0.085133f;
    // mad r1.x, r0.w, r1.x, l(0.180141)
    r1.x = r0.w * r1.x + 0.180141f;
    // mad r1.x, r0.w, r1.x, l(-0.330299)
    r1.x = r0.w * r1.x + -0.330299f;
    // mad r0.w, r0.w, r1.x, l(0.999866)
    r0.w = r0.w * r1.x + 0.999866f;
    // mul r1.x, r0.w, r0.z
    r1.x = r0.w * r0.z;
    // mad r1.x, r1.x, l(-2.000000), l(1.570796)
    r1.x = r1.x * -2.0f + 1.570796f;
    // lt r1.y, |r0.y|, |r0.x|
    r1.y = asfloat((abs(r0.y) < abs(r0.x)) ? 0xffffffffu : 0u);
    // and r1.x, r1.y, r1.x
    r1.x = asfloat(asuint(r1.y) & asuint(r1.x));
    // mad r0.z, r0.z, r0.w, r1.x
    r0.z = r0.z * r0.w + r1.x;
    // lt r0.w, r0.y, -r0.y
    r0.w = asfloat((r0.y < -r0.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r0.y, r0.x
    r0.w = min(r0.y, r0.x);
    // max r0.x, r0.y, r0.x
    r0.x = max(r0.y, r0.x);
    // ge r0.x, r0.x, -r0.x
    r0.x = asfloat((r0.x >= -r0.x) ? 0xffffffffu : 0u);
    // lt r0.y, r0.w, -r0.w
    r0.y = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // and r0.x, r0.x, r0.y
    r0.x = asfloat(asuint(r0.x) & asuint(r0.y));
    // movc r0.x, r0.x, -r0.z, r0.z
    r0.x = asuint(r0.x) != 0u ? -r0.z : r0.z;
    // mad r0.x, r0.x, l(0.159155), l(0.500000)
    r0.x = r0.x * 0.159155f + 0.5f;
    // mad r1.xy, v2.xyxx, cb0[3].xyxx, cb0[4].xyxx
    float stage2_x = v2.x * cb0[3].x + cb0[4].x;
    float stage2_y = v2.y * cb0[3].y + cb0[4].y;
    r1.x = stage2_x;
    r1.y = stage2_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.xyxx, t0.yzwx, s2, l(0.000000)
    float4 fetch3 = t0.SampleBias(s2, r1.xy, 0.0f);
    r0.w = fetch3.x;
    // mad r0.w, r0.w, l(2.000000), l(-1.000000)
    r0.w = r0.w * 2.0f + -1.0f;
    // mad r1.xy, v2.xyxx, cb0[3].xyxx, cb0[5].xyxx
    float stage4_x = v2.x * cb0[3].x + cb0[5].x;
    float stage4_y = v2.y * cb0[3].y + cb0[5].y;
    r1.x = stage4_x;
    r1.y = stage4_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r1.xyxx, t0.xyzw, s2, l(0.000000)
    float4 fetch5 = t0.SampleBias(s2, r1.xy, 0.0f);
    r1.x = fetch5.x;
    // mad r1.x, r1.x, l(2.000000), l(-1.000000)
    r1.x = r1.x * 2.0f + -1.0f;
    // add r1.yz, v4.xxyx, l(0.000000, -0.500000, -0.900000, 0.000000)
    float stage6_y = v4.x + -0.5f;
    float stage6_z = v4.y + -0.9f;
    r1.y = stage6_y;
    r1.z = stage6_z;
    // mul r1.x, r1.x, r1.z
    r1.x = r1.x * r1.z;
    // mad r0.w, r1.z, r0.w, r1.x
    r0.w = r1.z * r0.w + r1.x;
    // mul r0.w, r0.w, cb0[9].y
    r0.w = r0.w * cb0[9].y;
    // add r1.xz, v2.xxyx, l(-0.500000, 0.000000, -0.500000, 0.000000)
    float stage7_x = v2.x + -0.5f;
    float stage7_z = v2.y + -0.5f;
    r1.x = stage7_x;
    r1.z = stage7_z;
    // dp2 r1.x, r1.xzxx, r1.xzxx
    r1.x = dot(r1.xz, r1.xz);
    // sqrt r1.x, r1.x
    r1.x = sqrt(r1.x);
    // mul r1.x, r1.x, v4.z
    r1.x = r1.x * v4.z;
    // mad r1.z, -r1.x, l(2.000000), l(1.000000)
    r1.z = -r1.x * 2.0f + 1.0f;
    // mad r1.x, r1.x, l(3.000000), -r1.y
    r1.x = r1.x * 3.0f + -r1.y;
    // max r1.x, r1.x, l(-0.100000)
    r1.x = max(r1.x, -0.1f);
    // min r0.y, r1.x, l(10.000000)
    r0.y = min(r1.x, 10.0f);
    // mad r1.xyzw, r1.zzzz, r0.wwww, r0.xyxy
    float stage8_x = r1.z * r0.w + r0.x;
    float stage8_y = r1.z * r0.w + r0.y;
    float stage8_z = r1.z * r0.w + r0.x;
    float stage8_w = r1.z * r0.w + r0.y;
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    r1.w = stage8_w;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.yz, r1.zwzz, t1.yzxw, s4, l(-1.000000)
    float4 fetch9 = t1.SampleLevel(s4, r1.zw, -1.0f);
    r0.y = fetch9.z;
    r0.z = fetch9.x;
    // mad r0.xw, r0.xxxz, l(7.000000, 0.000000, 0.000000, 1.000000), cb0[8].xxxy
    float stage10_x = r0.x * 7.0f + cb0[8].x;
    float stage10_w = r0.z * 1.0f + cb0[8].y;
    r0.x = stage10_x;
    r0.w = stage10_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.xwxx, t5.xyzw, s5, l(0.000000)
    float4 fetch11 = t5.SampleBias(s5, r0.xw, 0.0f);
    r2.x = fetch11.x;
    r2.y = fetch11.y;
    r2.z = fetch11.z;
    // mul r2.xyz, r0.zzzz, r2.xyzx
    float stage12_x = r0.z * r2.x;
    float stage12_y = r0.z * r2.y;
    float stage12_z = r0.z * r2.z;
    r2.x = stage12_x;
    r2.y = stage12_y;
    r2.z = stage12_z;
    // mul_sat r2.xyz, r2.xyzx, l(4.000000, 4.000000, 4.000000, 0.000000)
    float stage13_x = saturate(r2.x * 4.0f);
    float stage13_y = saturate(r2.y * 4.0f);
    float stage13_z = saturate(r2.z * 4.0f);
    r2.x = stage13_x;
    r2.y = stage13_y;
    r2.z = stage13_z;
    // mad r0.xw, r1.xxxy, l(7.000000, 0.000000, 0.000000, 1.000000), cb0[6].xxxy
    float stage14_x = r1.x * 7.0f + cb0[6].x;
    float stage14_w = r1.y * 1.0f + cb0[6].y;
    r0.x = stage14_x;
    r0.w = stage14_w;
    // mad r1.xy, r1.zwzz, l(7.000000, 0.500000, 0.000000, 0.000000), cb0[7].xyxx
    float stage15_x = r1.z * 7.0f + cb0[7].x;
    float stage15_y = r1.w * 0.5f + cb0[7].y;
    r1.x = stage15_x;
    r1.y = stage15_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t4.xyzw, s3, l(-1.000000)
    float4 fetch16 = t4.SampleLevel(s3, r1.xy, -1.0f);
    r1.x = fetch16.x;
    r1.y = fetch16.y;
    r1.z = fetch16.z;
    // sample_l_indexable(texture2d)(float,float,float,float) r3.xyz, r0.xwxx, t3.xyzw, s1, l(-1.000000)
    float4 fetch17 = t3.SampleLevel(s1, r0.xw, -1.0f);
    r3.x = fetch17.x;
    r3.y = fetch17.y;
    r3.z = fetch17.z;
    // mul r4.xyz, r1.xyzx, r3.xyzx
    float stage18_x = r1.x * r3.x;
    float stage18_y = r1.y * r3.y;
    float stage18_z = r1.z * r3.z;
    r4.x = stage18_x;
    r4.y = stage18_y;
    r4.z = stage18_z;
    // add r1.xyz, r1.xyzx, r3.xyzx
    float stage19_x = r1.x + r3.x;
    float stage19_y = r1.y + r3.y;
    float stage19_z = r1.z + r3.z;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // mul r3.xyz, r0.zzzz, r1.xyzx
    float stage20_x = r0.z * r1.x;
    float stage20_y = r0.z * r1.y;
    float stage20_z = r0.z * r1.z;
    r3.x = stage20_x;
    r3.y = stage20_y;
    r3.z = stage20_z;
    // mul r0.xw, r1.xxxy, l(0.200000, 0.000000, 0.000000, 0.200000)
    float stage21_x = r1.x * 0.2f;
    float stage21_w = r1.y * 0.2f;
    r0.x = stage21_x;
    r0.w = stage21_w;
    // mul r1.xyz, r3.xyzx, l(3.000000, 3.000000, 3.000000, 0.000000)
    float stage22_x = r3.x * 3.0f;
    float stage22_y = r3.y * 3.0f;
    float stage22_z = r3.z * 3.0f;
    r1.x = stage22_x;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // mad r1.xyz, r4.xyzx, l(4.000000, 4.000000, 4.000000, 0.000000), r1.xyzx
    float stage23_x = r4.x * 4.0f + r1.x;
    float stage23_y = r4.y * 4.0f + r1.y;
    float stage23_z = r4.z * 4.0f + r1.z;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // mad r1.xyz, r2.xyzx, r2.xyzx, r1.xyzx
    float stage24_x = r2.x * r2.x + r1.x;
    float stage24_y = r2.y * r2.y + r1.y;
    float stage24_z = r2.z * r2.z + r1.z;
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // dp3 r1.w, v6.xyzx, v6.xyzx
    r1.w = dot(v6.xyz, v6.xyz);
    // rsq r1.w, r1.w
    r1.w = rsqrt(r1.w);
    // mad r0.xw, v6.xxxy, r1.wwww, r0.xxxw
    float stage25_x = v6.x * r1.w + r0.x;
    float stage25_w = v6.y * r1.w + r0.w;
    r0.x = stage25_x;
    r0.w = stage25_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r0.xwxx, t6.xyzw, s6, l(0.000000)
    float4 fetch26 = t6.SampleBias(s6, r0.xw, 0.0f);
    r2.x = fetch26.x;
    r2.y = fetch26.y;
    r2.z = fetch26.z;
    // add r1.xyz, r1.xyzx, r2.xyzx
    float stage27_x = r1.x + r2.x;
    float stage27_y = r1.y + r2.y;
    float stage27_z = r1.z + r2.z;
    r1.x = stage27_x;
    r1.y = stage27_y;
    r1.z = stage27_z;
    // dp3 r0.x, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.x = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r2.xyz, -r1.xyzx, r0.xxxx
    float stage28_x = -r1.x + r0.x;
    float stage28_y = -r1.y + r0.x;
    float stage28_z = -r1.z + r0.x;
    r2.x = stage28_x;
    r2.y = stage28_y;
    r2.z = stage28_z;
    // mad r1.xyz, cb0[10].xxxx, r2.xyzx, r1.xyzx
    float stage29_x = cb0[10].x * r2.x + r1.x;
    float stage29_y = cb0[10].x * r2.y + r1.y;
    float stage29_z = cb0[10].x * r2.z + r1.z;
    r1.x = stage29_x;
    r1.y = stage29_y;
    r1.z = stage29_z;
    // div r0.xw, v7.xxxy, v7.wwww
    float stage30_x = v7.x / v7.w;
    float stage30_w = v7.y / v7.w;
    r0.x = stage30_x;
    r0.w = stage30_w;
    // mad r0.xw, r0.xxxw, cb2[0].xxxy, cb2[0].wwwz
    float stage31_x = r0.x * cb2[0].x + cb2[0].w;
    float stage31_w = r0.w * cb2[0].y + cb2[0].z;
    r0.x = stage31_x;
    r0.w = stage31_w;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.x, r0.xwxx, t2.xyzw, s0, l(0.000000)
    float4 fetch32 = t2.SampleLevel(s0, r0.xw, 0.0f);
    r0.x = fetch32.x;
    // min r0.x, r0.x, l(0.999000)
    r0.x = min(r0.x, 0.999f);
    // mad r0.w, r0.x, cb2[1].z, -cb2[1].w
    r0.w = r0.x * cb2[1].z + -cb2[1].w;
    // mad r0.x, r0.x, cb2[1].x, cb2[1].y
    r0.x = r0.x * cb2[1].x + cb2[1].y;
    // div r0.w, l(1.000000, 1.000000, 1.000000, 1.000000), r0.w
    r0.w = 1.0f / r0.w;
    // add r0.x, r0.w, r0.x
    r0.x = r0.w + r0.x;
    // add r0.x, r0.x, -v7.w
    r0.x = r0.x + -v7.w;
    // mul_sat r0.xw, r0.xxxx, l(0.034483, 0.000000, 0.000000, 0.066667)
    float stage33_x = saturate(r0.x * 0.034483f);
    float stage33_w = saturate(r0.x * 0.066667f);
    r0.x = stage33_x;
    r0.w = stage33_w;
    // add r0.w, -r0.w, l(1.000000)
    r0.w = -r0.w + 1.0f;
    // mul r1.w, r0.w, r0.w
    r1.w = r0.w * r0.w;
    // lt r0.w, r0.w, l(0.000001)
    r0.w = asfloat((r0.w < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.w, r1.w, r1.w
    r1.w = r1.w * r1.w;
    // mul r1.w, r1.w, r1.w
    r1.w = r1.w * r1.w;
    // mul r2.xyz, r1.wwww, l(10.000000, 10.000000, 20.000000, 0.000000)
    float stage34_x = r1.w * 10.0f;
    float stage34_y = r1.w * 10.0f;
    float stage34_z = r1.w * 20.0f;
    r2.x = stage34_x;
    r2.y = stage34_y;
    r2.z = stage34_z;
    // movc r2.xyz, r0.wwww, l(0,0,0,0), r2.xyzx
    float stage35_x = asuint(r0.w) != 0u ? asfloat(0u) : r2.x;
    float stage35_y = asuint(r0.w) != 0u ? asfloat(0u) : r2.y;
    float stage35_z = asuint(r0.w) != 0u ? asfloat(0u) : r2.z;
    r2.x = stage35_x;
    r2.y = stage35_y;
    r2.z = stage35_z;
    // mad r1.xyz, r0.yyyy, r1.xyzx, r2.xyzx
    float stage36_x = r0.y * r1.x + r2.x;
    float stage36_y = r0.y * r1.y + r2.y;
    float stage36_z = r0.y * r1.z + r2.z;
    r1.x = stage36_x;
    r1.y = stage36_y;
    r1.z = stage36_z;
    // add r0.y, -r0.y, r0.z
    r0.y = -r0.y + r0.z;
    // add_sat r0.y, r0.y, l(1.000000)
    r0.y = saturate(r0.y + 1.0f);
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // mad r0.xyz, r1.xyzx, v3.xyzx, cb0[1].xyzx
    float stage37_x = r1.x * v3.x + cb0[1].x;
    float stage37_y = r1.y * v3.y + cb0[1].y;
    float stage37_z = r1.z * v3.z + cb0[1].z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage38_x = r0.x * v5.w + v5.x;
    float stage38_y = r0.y * v5.w + v5.y;
    float stage38_z = r0.z * v5.w + v5.z;
    o0.x = stage38_x;
    o0.y = stage38_y;
    o0.z = stage38_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage39_x = r0.x * v1.x;
    float stage39_y = r0.x * v1.y;
    float stage39_z = r0.x * v1.z;
    r0.x = stage39_x;
    r0.y = stage39_y;
    r0.z = stage39_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage40_x = r0.w * v0.z;
    float stage40_y = r0.w * v0.x;
    float stage40_z = r0.w * v0.y;
    r1.x = stage40_x;
    r1.y = stage40_y;
    r1.z = stage40_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage41_x = r0.x * r1.x;
    float stage41_y = r0.x * r1.y;
    float stage41_z = r0.x * r1.z;
    r0.x = stage41_x;
    r0.y = stage41_y;
    r0.z = stage41_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage42_x = r0.x / r0.w;
    float stage42_y = r0.y / r0.w;
    r0.x = stage42_x;
    r0.y = stage42_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage43_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage43_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage43_x;
    r1.y = stage43_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage44_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage44_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage44_x;
    r1.y = stage44_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage45_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage45_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage45_x;
    r1.y = stage45_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage46_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage46_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage46_x;
    r0.y = stage46_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage47_x = r0.x * 0.5f + 0.5f;
    float stage47_y = r0.y * 0.5f + 0.5f;
    o2.x = stage47_x;
    o2.y = stage47_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage48_z = 1.0f;
    float stage48_w = asfloat(0u);
    o2.z = stage48_z;
    o2.w = stage48_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage49_x = cb2[3].x;
    float stage49_y = cb2[3].y;
    float stage49_z = cb2[3].z;
    o3.x = stage49_x;
    o3.y = stage49_y;
    o3.z = stage49_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage50_x = asfloat(0u);
    float stage50_y = asfloat(0u);
    float stage50_z = asfloat(0u);
    float stage50_w = asfloat(0u);
    o4.x = stage50_x;
    o4.y = stage50_y;
    o4.z = stage50_z;
    o4.w = stage50_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage51_x = asfloat(0u);
    float stage51_y = asfloat(0u);
    float stage51_z = asfloat(0u);
    float stage51_w = asfloat(0u);
    o5.x = stage51_x;
    o5.y = stage51_y;
    o5.z = stage51_z;
    o5.w = stage51_w;

    Shade_Ue3_fx_s_pa_glasshole_02_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
