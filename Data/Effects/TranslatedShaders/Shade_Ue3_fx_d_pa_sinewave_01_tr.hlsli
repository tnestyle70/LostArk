// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_sinewave_01_tr_Constants0 : register(b0)
{
    float4 cb0[12];
};
cbuffer Shade_Ue3_fx_d_pa_sinewave_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);

struct Shade_Ue3_fx_d_pa_sinewave_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_d_pa_sinewave_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_d_pa_sinewave_01_tr_OUTPUT Shade_Ue3_fx_d_pa_sinewave_01_tr(Shade_Ue3_fx_d_pa_sinewave_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.x, v4.x, cb0[8].w
    r0.x = v4.x * cb0[8].w;
    // add r1.xyzw, v2.xyxy, l(-0.500000, -0.500000, -0.500000, -0.500000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    float stage1_z = v2.x + -0.5f;
    float stage1_w = v2.y + -0.5f;
    r1.x = stage1_x;
    r1.y = stage1_y;
    r1.z = stage1_z;
    r1.w = stage1_w;
    // dp2 r0.y, cb0[3].xyxx, r1.zwzz
    r0.y = dot(cb0[3].xy, r1.zw);
    // dp4 r0.z, cb0[2].xyxy, r1.xyzw
    r0.z = dot(cb0[2].xyxy, r1.xyzw);
    // add r1.y, r0.y, l(0.500000)
    r1.y = r0.y + 0.5f;
    // add r1.zw, r1.yyyy, v4.yyyz
    float stage2_z = r1.y + v4.y;
    float stage2_w = r1.y + v4.z;
    r1.z = stage2_z;
    r1.w = stage2_w;
    // mul r0.y, r1.z, cb0[8].z
    r0.y = r1.z * cb0[8].z;
    // mul r0.y, r0.y, l(6.283185)
    r0.y = r0.y * 6.283185f;
    // sincos r0.y, null, r0.y
    float stage3_y = r0.y;
    r0.y = sin(stage3_y);
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // add r0.y, -r1.y, l(1.000000)
    r0.y = -r1.y + 1.0f;
    // mul r0.w, r0.y, r1.y
    r0.w = r0.y * r1.y;
    // mul r0.w, r0.w, l(4.000000)
    r0.w = r0.w * 4.0f;
    // mad r0.x, r0.x, r0.w, r0.z
    r0.x = r0.x * r0.w + r0.z;
    // div r0.x, r0.x, r0.y
    r0.x = r0.x / r0.y;
    // mad_sat r0.x, r0.x, l(0.500000), l(0.500000)
    r0.x = saturate(r0.x * 0.5f + 0.5f);
    // add r0.x, r0.x, l(-0.500000)
    r0.x = r0.x + -0.5f;
    // mad_sat r1.x, r0.x, cb0[9].x, l(0.500000)
    r1.x = saturate(r0.x * cb0[9].x + 0.5f);
    // add r0.xy, r1.xwxx, l(0.000000, -1.000000, 0.000000, 0.000000)
    float stage4_x = r1.x + 0.0f;
    float stage4_y = r1.w + -1.0f;
    r0.x = stage4_x;
    r0.y = stage4_y;
    // add r1.xy, r1.xyxx, cb0[4].xyxx
    float stage5_x = r1.x + cb0[4].x;
    float stage5_y = r1.y + cb0[4].y;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // mul r1.xy, r1.xyxx, cb0[5].xyxx
    float stage6_x = r1.x * cb0[5].x;
    float stage6_y = r1.y * cb0[5].y;
    r1.x = stage6_x;
    r1.y = stage6_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s2, l(0.000000)
    float4 fetch7 = t1.SampleBias(s2, r0.xy, 0.0f);
    r0.x = fetch7.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.xyxx, t0.yxzw, s1, l(0.000000)
    float4 fetch8 = t0.SampleBias(s1, r1.xy, 0.0f);
    r0.y = fetch8.x;
    // mul r1.xy, r1.xyxx, cb0[7].zwzz
    float stage9_x = r1.x * cb0[7].z;
    float stage9_y = r1.y * cb0[7].w;
    r1.x = stage9_x;
    r1.y = stage9_y;
    // add r0.z, -v3.w, l(1.000000)
    r0.z = -v3.w + 1.0f;
    // add_sat r0.y, -r0.z, r0.y
    r0.y = saturate(-r0.z + r0.y);
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul_sat r0.x, r0.x, cb0[11].x
    r0.x = saturate(r0.x * cb0[11].x);
    // log r0.y, r0.x
    r0.y = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[11].y
    r0.y = r0.y * cb0[11].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mul_sat r0.x, r0.w, r0.x
    r0.x = saturate(r0.w * r0.x);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // mad r0.x, cb0[7].y, cb0[7].x, r1.x
    r0.x = cb0[7].y * cb0[7].x + r1.x;
    // mad r0.y, cb0[7].y, cb0[10].y, r1.y
    r0.y = cb0[7].y * cb0[10].y + r1.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t2.xyzw, s0, l(0.000000)
    float4 fetch10 = t2.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch10.x;
    r0.y = fetch10.y;
    r0.z = fetch10.z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xyzx, r0.wwww
    float stage11_x = -r0.x + r0.w;
    float stage11_y = -r0.y + r0.w;
    float stage11_z = -r0.z + r0.w;
    r1.x = stage11_x;
    r1.y = stage11_y;
    r1.z = stage11_z;
    // mad r0.xyz, cb0[10].zzzz, r1.xyzx, r0.xyzx
    float stage12_x = cb0[10].z * r1.x + r0.x;
    float stage12_y = cb0[10].z * r1.y + r0.y;
    float stage12_z = cb0[10].z * r1.z + r0.z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage13_x = max(abs(r0.x), 1e-06f);
    float stage13_y = max(abs(r0.y), 1e-06f);
    float stage13_z = max(abs(r0.z), 1e-06f);
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // log r0.xyz, r0.xyzx
    float stage14_x = log2(r0.x);
    float stage14_y = log2(r0.y);
    float stage14_z = log2(r0.z);
    r0.x = stage14_x;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // mul r0.xyz, r0.xyzx, cb0[10].wwww
    float stage15_x = r0.x * cb0[10].w;
    float stage15_y = r0.y * cb0[10].w;
    float stage15_z = r0.z * cb0[10].w;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // exp r0.xyz, r0.xyzx
    float stage16_x = exp2(r0.x);
    float stage16_y = exp2(r0.y);
    float stage16_z = exp2(r0.z);
    r0.x = stage16_x;
    r0.y = stage16_y;
    r0.z = stage16_z;
    // mul r1.xyz, cb0[6].xyzx, cb0[6].wwww
    float stage17_x = cb0[6].x * cb0[6].w;
    float stage17_y = cb0[6].y * cb0[6].w;
    float stage17_z = cb0[6].z * cb0[6].w;
    r1.x = stage17_x;
    r1.y = stage17_y;
    r1.z = stage17_z;
    // mul r0.xyz, r0.xyzx, r1.xyzx
    float stage18_x = r0.x * r1.x;
    float stage18_y = r0.y * r1.y;
    float stage18_z = r0.z * r1.z;
    r0.x = stage18_x;
    r0.y = stage18_y;
    r0.z = stage18_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage19_x = r0.x * v3.x + cb0[1].x;
    float stage19_y = r0.y * v3.y + cb0[1].y;
    float stage19_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage20_x = r0.x * v5.w + v5.x;
    float stage20_y = r0.y * v5.w + v5.y;
    float stage20_z = r0.z * v5.w + v5.z;
    o0.x = stage20_x;
    o0.y = stage20_y;
    o0.z = stage20_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage21_x = r0.x * v1.x;
    float stage21_y = r0.x * v1.y;
    float stage21_z = r0.x * v1.z;
    r0.x = stage21_x;
    r0.y = stage21_y;
    r0.z = stage21_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage22_x = r0.w * v0.z;
    float stage22_y = r0.w * v0.x;
    float stage22_z = r0.w * v0.y;
    r1.x = stage22_x;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage23_x = r0.x * r1.x;
    float stage23_y = r0.x * r1.y;
    float stage23_z = r0.x * r1.z;
    r0.x = stage23_x;
    r0.y = stage23_y;
    r0.z = stage23_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage24_x = r0.x / r0.w;
    float stage24_y = r0.y / r0.w;
    r0.x = stage24_x;
    r0.y = stage24_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage25_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage25_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage25_x;
    r1.y = stage25_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage26_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage26_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage26_x;
    r1.y = stage26_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage27_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage27_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage27_x;
    r1.y = stage27_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage28_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage28_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage28_x;
    r0.y = stage28_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage29_x = r0.x * 0.5f + 0.5f;
    float stage29_y = r0.y * 0.5f + 0.5f;
    o2.x = stage29_x;
    o2.y = stage29_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage30_z = 1.0f;
    float stage30_w = asfloat(0u);
    o2.z = stage30_z;
    o2.w = stage30_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage31_x = cb2[3].x;
    float stage31_y = cb2[3].y;
    float stage31_z = cb2[3].z;
    o3.x = stage31_x;
    o3.y = stage31_y;
    o3.z = stage31_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage32_x = asfloat(0u);
    float stage32_y = asfloat(0u);
    float stage32_z = asfloat(0u);
    float stage32_w = asfloat(0u);
    o4.x = stage32_x;
    o4.y = stage32_y;
    o4.z = stage32_z;
    o4.w = stage32_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage33_x = asfloat(0u);
    float stage33_y = asfloat(0u);
    float stage33_z = asfloat(0u);
    float stage33_w = asfloat(0u);
    o5.x = stage33_x;
    o5.y = stage33_y;
    o5.z = stage33_z;
    o5.w = stage33_w;

    Shade_Ue3_fx_d_pa_sinewave_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
