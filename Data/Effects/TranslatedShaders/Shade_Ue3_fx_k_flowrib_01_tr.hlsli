// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_flowrib_01_tr_Constants0 : register(b0)
{
    float4 cb0[11];
};
cbuffer Shade_Ue3_fx_k_flowrib_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);

struct Shade_Ue3_fx_k_flowrib_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_flowrib_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_flowrib_01_tr_OUTPUT Shade_Ue3_fx_k_flowrib_01_tr(Shade_Ue3_fx_k_flowrib_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mov r0.y, cb0[9].w
    r0.y = cb0[9].w;
    // mad r1.xy, v2.xyxx, cb0[5].xyxx, cb0[6].xyxx
    float stage1_x = v2.x * cb0[5].x + cb0[6].x;
    float stage1_y = v2.y * cb0[5].y + cb0[6].y;
    r1.x = stage1_x;
    r1.y = stage1_y;
    // mov r0.xz, l(0,0,1.000000,0)
    float stage2_x = asfloat(0u);
    float stage2_z = 1.0f;
    r0.x = stage2_x;
    r0.z = stage2_z;
    // add r2.xyzw, r0.yxxy, r1.xyxy
    float stage3_x = r0.y + r1.x;
    float stage3_y = r0.x + r1.y;
    float stage3_z = r0.x + r1.x;
    float stage3_w = r0.y + r1.y;
    r2.x = stage3_x;
    r2.y = stage3_y;
    r2.z = stage3_z;
    r2.w = stage3_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r1.xyxx, t0.xyzw, s1, l(0.000000)
    float4 fetch4 = t0.SampleBias(s1, r1.xy, 0.0f);
    r0.x = fetch4.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r2.xyxx, t0.yxzw, s1, l(0.000000)
    float4 fetch5 = t0.SampleBias(s1, r2.xy, 0.0f);
    r0.y = fetch5.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r2.zwzz, t0.xyzw, s1, l(0.000000)
    float4 fetch6 = t0.SampleBias(s1, r2.zw, 0.0f);
    r1.x = fetch6.x;
    // add r1.y, -r0.x, r1.x
    r1.y = -r0.x + r1.x;
    // add r1.x, -r0.x, r0.y
    r1.x = -r0.x + r0.y;
    // mul r1.xy, r1.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage7_x = r1.x * 8.0f;
    float stage7_y = r1.y * 8.0f;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // mov r1.z, l(0)
    r1.z = asfloat(0u);
    // add r1.xyz, -r1.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage8_x = -r1.x + 0.0f;
    float stage8_y = -r1.y + 0.0f;
    float stage8_z = -r1.z + 1.0f;
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // div r0.xy, r1.xyxx, r0.xxxx
    float stage9_x = r1.x / r0.x;
    float stage9_y = r1.y / r0.x;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // mad r0.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage10_x = r0.x * 0.5f + 0.5f;
    float stage10_y = r0.y * 0.5f + 0.5f;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // mov r0.w, v4.w
    r0.w = v4.w;
    // mul r0.xy, r0.zwzz, r0.xyxx
    float stage11_x = r0.z * r0.x;
    float stage11_y = r0.w * r0.y;
    r0.x = stage11_x;
    r0.y = stage11_y;
    // mul r1.x, v2.x, cb0[8].w
    r1.x = v2.x * cb0[8].w;
    // mul r1.y, v2.y, cb0[9].x
    r1.y = v2.y * cb0[9].x;
    // add r0.zw, r1.xxxy, cb0[4].xxxy
    float stage12_z = r1.x + cb0[4].x;
    float stage12_w = r1.y + cb0[4].y;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mov r1.x, v4.w
    r1.x = v4.w;
    // mov r1.y, l(0.500000)
    r1.y = 0.5f;
    // mad r0.xy, r1.xyxx, r0.xyxx, r0.zwzz
    float stage13_x = r1.x * r0.x + r0.z;
    float stage13_y = r1.y * r0.y + r0.w;
    r0.x = stage13_x;
    r0.y = stage13_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s2, l(0.000000)
    float4 fetch14 = t1.SampleBias(s2, r0.xy, 0.0f);
    r0.x = fetch14.x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, v4.z
    r0.y = r0.y * v4.z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.y, r0.y, cb0[10].y
    r0.y = r0.y * cb0[10].y;
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mad r0.y, v2.x, l(2.000000), l(-1.000000)
    r0.y = v2.x * 2.0f + -1.0f;
    // add r0.y, -|r0.y|, l(1.000000)
    r0.y = -(abs(r0.y)) + 1.0f;
    // log r0.z, |r0.y|
    r0.z = log2(abs(r0.y));
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, cb0[10].z
    r0.z = r0.z * cb0[10].z;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul_sat r0.x, r0.y, r0.x
    r0.x = saturate(r0.y * r0.x);
    // add r0.y, -v2.x, l(1.000000)
    r0.y = -v2.x + 1.0f;
    // mul r0.z, |r0.y|, |r0.y|
    r0.z = abs(r0.y) * abs(r0.y);
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, r0.z
    r0.z = r0.z * r0.z;
    // mul r0.z, r0.z, r0.z
    r0.z = r0.z * r0.z;
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // mul r0.z, r0.x, r0.y
    r0.z = r0.x * r0.y;
    // mad r0.x, r0.z, l(7.000000), r0.x
    r0.x = r0.z * 7.0f + r0.x;
    // min r0.x, r0.x, l(1.000000)
    r0.x = min(r0.x, 1.0f);
    // mul r0.x, r0.x, v3.w
    r0.x = r0.x * v3.w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // mad r0.xz, v2.xxyx, cb0[7].xxyx, cb0[2].xxyx
    float stage15_x = v2.x * cb0[7].x + cb0[2].x;
    float stage15_z = v2.y * cb0[7].y + cb0[2].y;
    r0.x = stage15_x;
    r0.z = stage15_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xzw, r0.xzxx, t2.xwyz, s0, l(0.000000)
    float4 fetch16 = t2.SampleBias(s0, r0.xz, 0.0f);
    r0.x = fetch16.x;
    r0.z = fetch16.y;
    r0.w = fetch16.z;
    // dp3 r1.x, r0.xzwx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.xzw, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xzwx, r1.xxxx
    float stage17_x = -r0.x + r1.x;
    float stage17_y = -r0.z + r1.x;
    float stage17_z = -r0.w + r1.x;
    r1.x = stage17_x;
    r1.y = stage17_y;
    r1.z = stage17_z;
    // mad r0.xzw, cb0[8].xxxx, r1.xxyz, r0.xxzw
    float stage18_x = cb0[8].x * r1.x + r0.x;
    float stage18_z = cb0[8].x * r1.y + r0.z;
    float stage18_w = cb0[8].x * r1.z + r0.w;
    r0.x = stage18_x;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // max r0.xzw, |r0.xxzw|, l(0.000001, 0.000000, 0.000001, 0.000001)
    float stage19_x = max(abs(r0.x), 1e-06f);
    float stage19_z = max(abs(r0.z), 1e-06f);
    float stage19_w = max(abs(r0.w), 1e-06f);
    r0.x = stage19_x;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // log r0.xzw, r0.xxzw
    float stage20_x = log2(r0.x);
    float stage20_z = log2(r0.z);
    float stage20_w = log2(r0.w);
    r0.x = stage20_x;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // mul r0.xzw, r0.xxzw, cb0[8].yyyy
    float stage21_x = r0.x * cb0[8].y;
    float stage21_z = r0.z * cb0[8].y;
    float stage21_w = r0.w * cb0[8].y;
    r0.x = stage21_x;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // exp r0.xzw, r0.xxzw
    float stage22_x = exp2(r0.x);
    float stage22_z = exp2(r0.z);
    float stage22_w = exp2(r0.w);
    r0.x = stage22_x;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // mul r0.xzw, r0.xxzw, cb0[3].xxyz
    float stage23_x = r0.x * cb0[3].x;
    float stage23_z = r0.z * cb0[3].y;
    float stage23_w = r0.w * cb0[3].z;
    r0.x = stage23_x;
    r0.z = stage23_z;
    r0.w = stage23_w;
    // mad_sat r0.xyz, cb0[8].zzzz, r0.xzwx, r0.yyyy
    float stage24_x = saturate(cb0[8].z * r0.x + r0.y);
    float stage24_y = saturate(cb0[8].z * r0.z + r0.y);
    float stage24_z = saturate(cb0[8].z * r0.w + r0.y);
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage25_x = r0.x * v3.x + cb0[1].x;
    float stage25_y = r0.y * v3.y + cb0[1].y;
    float stage25_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage26_x = r0.x * v5.w + v5.x;
    float stage26_y = r0.y * v5.w + v5.y;
    float stage26_z = r0.z * v5.w + v5.z;
    o0.x = stage26_x;
    o0.y = stage26_y;
    o0.z = stage26_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage27_x = r0.x * v1.x;
    float stage27_y = r0.x * v1.y;
    float stage27_z = r0.x * v1.z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage28_x = r0.w * v0.z;
    float stage28_y = r0.w * v0.x;
    float stage28_z = r0.w * v0.y;
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage29_x = r0.x * r1.x;
    float stage29_y = r0.x * r1.y;
    float stage29_z = r0.x * r1.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage30_x = r0.x / r0.w;
    float stage30_y = r0.y / r0.w;
    r0.x = stage30_x;
    r0.y = stage30_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage31_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage31_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage31_x;
    r1.y = stage31_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage32_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage32_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage32_x;
    r1.y = stage32_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage33_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage33_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage33_x;
    r1.y = stage33_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage34_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage34_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage35_x = r0.x * 0.5f + 0.5f;
    float stage35_y = r0.y * 0.5f + 0.5f;
    o2.x = stage35_x;
    o2.y = stage35_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage36_z = 1.0f;
    float stage36_w = asfloat(0u);
    o2.z = stage36_z;
    o2.w = stage36_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage37_x = cb2[3].x;
    float stage37_y = cb2[3].y;
    float stage37_z = cb2[3].z;
    o3.x = stage37_x;
    o3.y = stage37_y;
    o3.z = stage37_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage38_x = asfloat(0u);
    float stage38_y = asfloat(0u);
    float stage38_z = asfloat(0u);
    float stage38_w = asfloat(0u);
    o4.x = stage38_x;
    o4.y = stage38_y;
    o4.z = stage38_z;
    o4.w = stage38_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage39_x = asfloat(0u);
    float stage39_y = asfloat(0u);
    float stage39_z = asfloat(0u);
    float stage39_w = asfloat(0u);
    o5.x = stage39_x;
    o5.y = stage39_y;
    o5.z = stage39_z;
    o5.w = stage39_w;

    Shade_Ue3_fx_k_flowrib_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
