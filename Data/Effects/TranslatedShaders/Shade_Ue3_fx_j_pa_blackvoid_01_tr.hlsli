// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_j_pa_blackvoid_01_tr_Constants0 : register(b0)
{
    float4 cb0[8];
};
cbuffer Shade_Ue3_fx_j_pa_blackvoid_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
Texture2D t0 : register(t0);

struct Shade_Ue3_fx_j_pa_blackvoid_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_j_pa_blackvoid_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_j_pa_blackvoid_01_tr_OUTPUT Shade_Ue3_fx_j_pa_blackvoid_01_tr(Shade_Ue3_fx_j_pa_blackvoid_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.x, v4.x, cb0[6].w
    r0.x = v4.x + cb0[6].w;
    // add r0.yz, v2.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage1_y = v2.x + -0.5f;
    float stage1_z = v2.y + -0.5f;
    r0.y = stage1_y;
    r0.z = stage1_z;
    // dp2 r1.x, cb0[2].xyxx, r0.yzyy
    r1.x = dot(cb0[2].xy, r0.yz);
    // dp2 r1.y, cb0[3].xyxx, r0.yzyy
    r1.y = dot(cb0[3].xy, r0.yz);
    // dp2 r0.y, r0.yzyy, r0.yzyy
    r0.y = dot(r0.yz, r0.yz);
    // add r0.y, -r0.y, l(1.000000)
    r0.y = -r0.y + 1.0f;
    // add r0.zw, r1.xxxy, l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage2_z = r1.x + 0.5f;
    float stage2_w = r1.y + 0.5f;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // mad r0.x, r0.w, cb0[6].y, r0.x
    r0.x = r0.w * cb0[6].y + r0.x;
    // mad r1.x, r0.z, cb0[6].x, cb0[6].z
    r1.x = r0.z * cb0[6].x + cb0[6].z;
    // mov r0.z, l(-1.000000)
    r0.z = -1.0f;
    // add r1.y, r0.x, r0.z
    r1.y = r0.x + r0.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r1.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch3 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.x = fetch3.x;
    // mad r0.zw, v2.xxxy, l(0.000000, 0.000000, 2.000000, 2.000000), l(0.000000, 0.000000, -1.000000, -1.000000)
    float stage4_z = v2.x * 2.0f + -1.0f;
    float stage4_w = v2.y * 2.0f + -1.0f;
    r0.z = stage4_z;
    r0.w = stage4_w;
    // dp2 r0.z, -r0.zwzz, -r0.zwzz
    r0.z = dot(-r0.zw, -r0.zw);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // mad_sat r0.z, -r0.z, cb0[5].z, l(1.000000)
    r0.z = saturate(-r0.z * cb0[5].z + 1.0f);
    // mul r1.xy, v4.wzww, cb0[7].xzxx
    float stage5_x = v4.w * cb0[7].x;
    float stage5_y = v4.z * cb0[7].z;
    r1.x = stage5_x;
    r1.y = stage5_y;
    // mad r0.x, r0.x, r1.x, r0.z
    r0.x = r0.x * r1.x + r0.z;
    // mad r0.x, -cb0[7].y, v4.y, r0.x
    r0.x = -cb0[7].y * v4.y + r0.x;
    // mov_sat r0.z, r0.x
    r0.z = saturate(r0.x);
    // div_sat r0.x, |r0.x|, r1.y
    r0.x = saturate(abs(r0.x) / r1.y);
    // add r0.x, -r0.x, l(1.000000)
    r0.x = -r0.x + 1.0f;
    // mul r1.xyz, r0.xxxx, cb0[4].xyzx
    float stage6_x = r0.x * cb0[4].x;
    float stage6_y = r0.x * cb0[4].y;
    float stage6_z = r0.x * cb0[4].z;
    r1.x = stage6_x;
    r1.y = stage6_y;
    r1.z = stage6_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage7_x = max(abs(r1.x), 1e-06f);
    float stage7_y = max(abs(r1.y), 1e-06f);
    float stage7_z = max(abs(r1.z), 1e-06f);
    r1.x = stage7_x;
    r1.y = stage7_y;
    r1.z = stage7_z;
    // log r1.xyz, r1.xyzx
    float stage8_x = log2(r1.x);
    float stage8_y = log2(r1.y);
    float stage8_z = log2(r1.z);
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    // mul r1.xyz, r1.xyzx, cb0[7].wwww
    float stage9_x = r1.x * cb0[7].w;
    float stage9_y = r1.y * cb0[7].w;
    float stage9_z = r1.z * cb0[7].w;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    // exp r1.xyz, r1.xyzx
    float stage10_x = exp2(r1.x);
    float stage10_y = exp2(r1.y);
    float stage10_z = exp2(r1.z);
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // round_pi r0.x, r0.z
    r0.x = ceil(r0.z);
    // log r0.z, |r0.y|
    r0.z = log2(abs(r0.y));
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, l(9.000000)
    r0.z = r0.z * 9.0f;
    // exp r0.z, r0.z
    r0.z = exp2(r0.z);
    // movc r0.y, r0.y, l(0), r0.z
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r0.z;
    // lt r0.z, r0.y, l(0.000000)
    r0.z = asfloat((r0.y < 0.0f) ? 0xffffffffu : 0u);
    // add r0.y, r0.y, r0.y
    r0.y = r0.y + r0.y;
    // mul r0.w, r0.y, r0.y
    r0.w = r0.y * r0.y;
    // mul r0.y, r0.w, r0.y
    r0.y = r0.w * r0.y;
    // movc r0.y, r0.z, l(0), r0.y
    r0.y = asuint(r0.z) != 0u ? asfloat(0u) : r0.y;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // max r0.xyz, v3.xyzx, l(-5.000000, -5.000000, -5.000000, 0.000000)
    float stage11_x = max(v3.x, -5.0f);
    float stage11_y = max(v3.y, -5.0f);
    float stage11_z = max(v3.z, -5.0f);
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // min r0.xyz, r0.xyzx, l(1000.000000, 1000.000000, 1000.000000, 0.000000)
    float stage12_x = min(r0.x, 1000.0f);
    float stage12_y = min(r0.y, 1000.0f);
    float stage12_z = min(r0.z, 1000.0f);
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mad r0.xyz, r0.xyzx, r1.xyzx, cb0[1].xyzx
    float stage13_x = r0.x * r1.x + cb0[1].x;
    float stage13_y = r0.y * r1.y + cb0[1].y;
    float stage13_z = r0.z * r1.z + cb0[1].z;
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage14_x = r0.x * v5.w + v5.x;
    float stage14_y = r0.y * v5.w + v5.y;
    float stage14_z = r0.z * v5.w + v5.z;
    o0.x = stage14_x;
    o0.y = stage14_y;
    o0.z = stage14_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage15_x = r0.x * v1.x;
    float stage15_y = r0.x * v1.y;
    float stage15_z = r0.x * v1.z;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage16_x = r0.w * v0.z;
    float stage16_y = r0.w * v0.x;
    float stage16_z = r0.w * v0.y;
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage17_x = r0.x * r1.x;
    float stage17_y = r0.x * r1.y;
    float stage17_z = r0.x * r1.z;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage18_x = r0.x / r0.w;
    float stage18_y = r0.y / r0.w;
    r0.x = stage18_x;
    r0.y = stage18_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage19_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage19_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage19_x;
    r1.y = stage19_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage20_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage20_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage20_x;
    r1.y = stage20_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage21_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage21_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage21_x;
    r1.y = stage21_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage22_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage22_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage22_x;
    r0.y = stage22_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage23_x = r0.x * 0.5f + 0.5f;
    float stage23_y = r0.y * 0.5f + 0.5f;
    o2.x = stage23_x;
    o2.y = stage23_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage24_z = 1.0f;
    float stage24_w = asfloat(0u);
    o2.z = stage24_z;
    o2.w = stage24_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage25_x = cb2[3].x;
    float stage25_y = cb2[3].y;
    float stage25_z = cb2[3].z;
    o3.x = stage25_x;
    o3.y = stage25_y;
    o3.z = stage25_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage26_x = asfloat(0u);
    float stage26_y = asfloat(0u);
    float stage26_z = asfloat(0u);
    float stage26_w = asfloat(0u);
    o4.x = stage26_x;
    o4.y = stage26_y;
    o4.z = stage26_z;
    o4.w = stage26_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage27_x = asfloat(0u);
    float stage27_y = asfloat(0u);
    float stage27_z = asfloat(0u);
    float stage27_w = asfloat(0u);
    o5.x = stage27_x;
    o5.y = stage27_y;
    o5.z = stage27_z;
    o5.w = stage27_w;

    Shade_Ue3_fx_j_pa_blackvoid_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
