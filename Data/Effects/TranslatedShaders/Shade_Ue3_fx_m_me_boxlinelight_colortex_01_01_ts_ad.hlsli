// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_me_boxlinelight_colortex_01_01_ts_ad_Constants0 : register(b0)
{
    float4 cb0[11];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_m_me_boxlinelight_colortex_01_01_ts_ad_INPUT
{
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_m_me_boxlinelight_colortex_01_01_ts_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_m_me_boxlinelight_colortex_01_01_ts_ad_OUTPUT Shade_Ue3_fx_m_me_boxlinelight_colortex_01_01_ts_ad(Shade_Ue3_fx_m_me_boxlinelight_colortex_01_01_ts_ad_INPUT stage)
{
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;

    // mad r0.x, v4.x, cb0[5].z, cb0[6].z
    r0.x = v4.x * cb0[5].z + cb0[6].z;
    // mad r0.y, v4.y, cb0[5].w, cb0[7].x
    r0.y = v4.y * cb0[5].w + cb0[7].x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch1 = t0.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch1.x;
    // mad r0.xy, r0.xxxx, cb0[7].yyyy, v4.xyxx
    float stage2_x = r0.x * cb0[7].y + v4.x;
    float stage2_y = r0.x * cb0[7].y + v4.y;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // add r0.z, cb0[3].z, cb0[8].y
    r0.z = cb0[3].z + cb0[8].y;
    // mad r0.y, r0.y, cb0[7].w, r0.z
    r0.y = r0.y * cb0[7].w + r0.z;
    // mad r1.x, r0.x, cb0[7].z, cb0[8].x
    r1.x = r0.x * cb0[7].z + cb0[8].x;
    // mov r0.x, l(-1.000000)
    r0.x = -1.0f;
    // add r1.y, r0.y, r0.x
    r1.y = r0.y + r0.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r1.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch3 = t1.SampleBias(s1, r1.xy, 0.0f);
    r0.x = fetch3.x;
    r0.y = fetch3.y;
    r0.z = fetch3.z;
    // mul r0.xyz, r0.xyzx, cb0[8].zzzz
    float stage4_x = r0.x * cb0[8].z;
    float stage4_y = r0.y * cb0[8].z;
    float stage4_z = r0.z * cb0[8].z;
    r0.x = stage4_x;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage5_x = max(abs(r0.x), 1e-06f);
    float stage5_y = max(abs(r0.y), 1e-06f);
    float stage5_z = max(abs(r0.z), 1e-06f);
    r0.x = stage5_x;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // log r0.xyz, r0.xyzx
    float stage6_x = log2(r0.x);
    float stage6_y = log2(r0.y);
    float stage6_z = log2(r0.z);
    r0.x = stage6_x;
    r0.y = stage6_y;
    r0.z = stage6_z;
    // mul r0.xyz, r0.xyzx, cb0[8].wwww
    float stage7_x = r0.x * cb0[8].w;
    float stage7_y = r0.y * cb0[8].w;
    float stage7_z = r0.z * cb0[8].w;
    r0.x = stage7_x;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // exp r0.xyz, r0.xyzx
    float stage8_x = exp2(r0.x);
    float stage8_y = exp2(r0.y);
    float stage8_z = exp2(r0.z);
    r0.x = stage8_x;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.xyzx, r0.wwww
    float stage9_x = -r0.x + r0.w;
    float stage9_y = -r0.y + r0.w;
    float stage9_z = -r0.z + r0.w;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    // mad r0.xyz, cb0[9].xxxx, r1.xyzx, r0.xyzx
    float stage10_x = cb0[9].x * r1.x + r0.x;
    float stage10_y = cb0[9].x * r1.y + r0.y;
    float stage10_z = cb0[9].x * r1.z + r0.z;
    r0.x = stage10_x;
    r0.y = stage10_y;
    r0.z = stage10_z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // max r0.w, |r0.w|, l(0.000001)
    r0.w = max(abs(r0.w), 1e-06f);
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, cb0[9].z
    r0.w = r0.w * cb0[9].z;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // add r1.x, cb0[3].y, cb0[9].w
    r1.x = cb0[3].y + cb0[9].w;
    // add r0.w, r0.w, -r1.x
    r0.w = r0.w + -r1.x;
    // mul_sat r1.xy, r0.wwww, l(10.000000, 8.000000, 0.000000, 0.000000)
    float stage11_x = saturate(r0.w * 10.0f);
    float stage11_y = saturate(r0.w * 8.0f);
    r1.x = stage11_x;
    r1.y = stage11_y;
    // add r0.w, -r1.y, r1.x
    r0.w = -r1.y + r1.x;
    // mul r1.x, r1.x, cb0[1].w
    r1.x = r1.x * cb0[1].w;
    // mul r0.w, r0.w, cb0[10].x
    r0.w = r0.w * cb0[10].x;
    // max r0.w, |r0.w|, l(0.000001)
    r0.w = max(abs(r0.w), 1e-06f);
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, cb0[10].y
    r0.w = r0.w * cb0[10].y;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul r1.yz, v4.xxyx, cb0[5].xxxx
    float stage12_y = v4.x * cb0[5].x;
    float stage12_z = v4.y * cb0[5].x;
    r1.y = stage12_y;
    r1.z = stage12_z;
    // mul r1.yz, r1.yyzy, l(0.000000, 6.283185, 6.283185, 0.000000)
    float stage13_y = r1.y * 6.283185f;
    float stage13_z = r1.z * 6.283185f;
    r1.y = stage13_y;
    r1.z = stage13_z;
    // sincos r1.yz, null, -r1.yyzy
    float stage14_y = -r1.y;
    float stage14_z = -r1.z;
    float stage15_y = sin(stage14_y);
    float stage15_z = sin(stage14_z);
    r1.y = stage15_y;
    r1.z = stage15_z;
    // add r1.yz, r1.yyzy, l(0.000000, 1.000000, 1.000000, 0.000000)
    float stage16_y = r1.y + 1.0f;
    float stage16_z = r1.z + 1.0f;
    r1.y = stage16_y;
    r1.z = stage16_z;
    // log r2.xy, r1.yzyy
    float stage17_x = log2(r1.y);
    float stage17_y = log2(r1.z);
    r2.x = stage17_x;
    r2.y = stage17_y;
    // lt r1.yz, r1.yyzy, l(0.000000, 0.000001, 0.000001, 0.000000)
    float stage18_y = asfloat((r1.y < 1e-06f) ? 0xffffffffu : 0u);
    float stage18_z = asfloat((r1.z < 1e-06f) ? 0xffffffffu : 0u);
    r1.y = stage18_y;
    r1.z = stage18_z;
    // mul r2.xy, r2.xyxx, cb0[3].xxxx
    float stage19_x = r2.x * cb0[3].x;
    float stage19_y = r2.y * cb0[3].x;
    r2.x = stage19_x;
    r2.y = stage19_y;
    // exp r2.xy, r2.xyxx
    float stage20_x = exp2(r2.x);
    float stage20_y = exp2(r2.y);
    r2.x = stage20_x;
    r2.y = stage20_y;
    // movc r1.yz, r1.yyzy, l(0,0,0,0), r2.xxyx
    float stage21_y = asuint(r1.y) != 0u ? asfloat(0u) : r2.x;
    float stage21_z = asuint(r1.z) != 0u ? asfloat(0u) : r2.y;
    r1.y = stage21_y;
    r1.z = stage21_z;
    // add r1.yz, r1.yyzy, -cb0[5].yyyy
    float stage22_y = r1.y + -cb0[5].y;
    float stage22_z = r1.z + -cb0[5].y;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // add_sat r1.y, r1.z, r1.y
    r1.y = saturate(r1.z + r1.y);
    // mul r2.xyzw, r0.xxyz, r1.yyyy
    float stage23_x = r0.x * r1.y;
    float stage23_y = r0.x * r1.y;
    float stage23_z = r0.y * r1.y;
    float stage23_w = r0.z * r1.y;
    r2.x = stage23_x;
    r2.y = stage23_y;
    r2.z = stage23_z;
    r2.w = stage23_w;
    // mul r2.xyzw, r2.xyzw, cb0[9].yyyy
    float stage24_x = r2.x * cb0[9].y;
    float stage24_y = r2.y * cb0[9].y;
    float stage24_z = r2.z * cb0[9].y;
    float stage24_w = r2.w * cb0[9].y;
    r2.x = stage24_x;
    r2.y = stage24_y;
    r2.z = stage24_z;
    r2.w = stage24_w;
    // mul r0.xyz, cb0[1].xyzx, cb0[4].xyzx
    float stage25_x = cb0[1].x * cb0[4].x;
    float stage25_y = cb0[1].y * cb0[4].y;
    float stage25_z = cb0[1].z * cb0[4].z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // mad r0.xyz, r0.wwww, r0.xyzx, r2.yzwy
    float stage26_x = r0.w * r0.x + r2.y;
    float stage26_y = r0.w * r0.y + r2.z;
    float stage26_z = r0.w * r0.z + r2.w;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // mul r0.w, r1.x, r2.x
    r0.w = r1.x * r2.x;
    // mul r0.w, r0.w, cb0[0].x
    r0.w = r0.w * cb0[0].x;
    // mad r0.xyz, cb0[1].xyzx, r0.xyzx, cb0[2].xyzx
    float stage27_x = cb0[1].x * r0.x + cb0[2].x;
    float stage27_y = cb0[1].y * r0.y + cb0[2].y;
    float stage27_z = cb0[1].z * r0.z + cb0[2].z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // mul r0.xyz, r0.xyzx, v5.wwww
    float stage28_x = r0.x * v5.w;
    float stage28_y = r0.y * v5.w;
    float stage28_z = r0.z * v5.w;
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // mul o0.xyz, r0.wwww, r0.xyzx
    float stage29_x = r0.w * r0.x;
    float stage29_y = r0.w * r0.y;
    float stage29_z = r0.w * r0.z;
    o0.x = stage29_x;
    o0.y = stage29_y;
    o0.z = stage29_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_m_me_boxlinelight_colortex_01_01_ts_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
