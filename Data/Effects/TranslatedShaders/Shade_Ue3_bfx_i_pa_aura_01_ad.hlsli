// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_bfx_i_pa_aura_01_ad_Constants0 : register(b0)
{
    float4 cb0[9];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_bfx_i_pa_aura_01_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v5;
};

struct Shade_Ue3_bfx_i_pa_aura_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_bfx_i_pa_aura_01_ad_OUTPUT Shade_Ue3_bfx_i_pa_aura_01_ad(Shade_Ue3_bfx_i_pa_aura_01_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;

    // mul r0.xy, v2.xyxx, cb0[4].xzxx
    float stage1_x = v2.x * cb0[4].x;
    float stage1_y = v2.y * cb0[4].z;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, r0.xyxx, l(3.000000, 1.500000, 0.000000, 0.000000), cb0[4].ywyy
    float stage2_x = r0.x * 3.0f + cb0[4].y;
    float stage2_y = r0.y * 1.5f + cb0[4].w;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t2.xyzw, s0, l(0.000000)
    float4 fetch3 = t2.SampleBias(s0, r0.xy, 0.0f);
    r0.x = fetch3.x;
    r0.y = fetch3.y;
    r0.z = fetch3.z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage4_x = max(abs(r0.x), 1e-06f);
    float stage4_y = max(abs(r0.y), 1e-06f);
    float stage4_z = max(abs(r0.z), 1e-06f);
    r0.x = stage4_x;
    r0.y = stage4_y;
    r0.z = stage4_z;
    // log r0.xyz, r0.xyzx
    float stage5_x = log2(r0.x);
    float stage5_y = log2(r0.y);
    float stage5_z = log2(r0.z);
    r0.x = stage5_x;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // mul r0.xyz, r0.xyzx, cb0[5].xxxx
    float stage6_x = r0.x * cb0[5].x;
    float stage6_y = r0.y * cb0[5].x;
    float stage6_z = r0.z * cb0[5].x;
    r0.x = stage6_x;
    r0.y = stage6_y;
    r0.z = stage6_z;
    // exp r0.xyz, r0.xyzx
    float stage7_x = exp2(r0.x);
    float stage7_y = exp2(r0.y);
    float stage7_z = exp2(r0.z);
    r0.x = stage7_x;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // mul r1.xyz, r0.xyzx, cb0[2].xyzx
    float stage8_x = r0.x * cb0[2].x;
    float stage8_y = r0.y * cb0[2].y;
    float stage8_z = r0.z * cb0[2].z;
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    // dp3 r0.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r0.xyz, -r0.xyzx, cb0[2].xyzx, r0.wwww
    float stage9_x = -r0.x * cb0[2].x + r0.w;
    float stage9_y = -r0.y * cb0[2].y + r0.w;
    float stage9_z = -r0.z * cb0[2].z + r0.w;
    r0.x = stage9_x;
    r0.y = stage9_y;
    r0.z = stage9_z;
    // mad r0.xyz, cb0[5].yyyy, r0.xyzx, r1.xyzx
    float stage10_x = cb0[5].y * r0.x + r1.x;
    float stage10_y = cb0[5].y * r0.y + r1.y;
    float stage10_z = cb0[5].y * r0.z + r1.z;
    r0.x = stage10_x;
    r0.y = stage10_y;
    r0.z = stage10_z;
    // mul r0.xyz, r0.xyzx, l(5.000000, 5.000000, 5.000000, 0.000000)
    float stage11_x = r0.x * 5.0f;
    float stage11_y = r0.y * 5.0f;
    float stage11_z = r0.z * 5.0f;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mul r0.w, v2.x, cb0[5].z
    r0.w = v2.x * cb0[5].z;
    // mad r1.x, r0.w, l(0.800000), cb0[5].w
    r1.x = r0.w * 0.8f + cb0[5].w;
    // mul r0.w, v2.y, cb0[6].x
    r0.w = v2.y * cb0[6].x;
    // mad r1.y, r0.w, l(0.800000), cb0[6].y
    r1.y = r0.w * 0.8f + cb0[6].y;
    // mad r1.zw, v2.xxxy, l(0.000000, 0.000000, 0.400000, 0.400000), cb0[3].xxxy
    float stage12_z = v2.x * 0.4f + cb0[3].x;
    float stage12_w = v2.y * 0.4f + cb0[3].y;
    r1.z = stage12_z;
    r1.w = stage12_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.zwzz, t0.xzwy, s1, l(0.000000)
    float4 fetch13 = t0.SampleBias(s1, r1.zw, 0.0f);
    r0.w = fetch13.y;
    // add r0.w, r0.w, l(0.500000)
    r0.w = r0.w + 0.5f;
    // mad r1.xy, cb0[7].wwww, r0.wwww, r1.xyxx
    float stage14_x = cb0[7].w * r0.w + r1.x;
    float stage14_y = cb0[7].w * r0.w + r1.y;
    r1.x = stage14_x;
    r1.y = stage14_y;
    // mad r1.zw, cb0[7].wwww, r0.wwww, v2.xxxy
    float stage15_z = cb0[7].w * r0.w + v2.x;
    float stage15_w = cb0[7].w * r0.w + v2.y;
    r1.z = stage15_z;
    r1.w = stage15_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r1.zwzz, t1.yzwx, s3, l(0.000000)
    float4 fetch16 = t1.SampleBias(s3, r1.zw, 0.0f);
    r0.w = fetch16.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t3.xyzw, s2, l(0.000000)
    float4 fetch17 = t3.SampleBias(s2, r1.xy, 0.0f);
    r1.x = fetch17.x;
    r1.y = fetch17.y;
    r1.z = fetch17.z;
    // dp3 r1.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // add r2.xyz, -r1.xyzx, r1.wwww
    float stage18_x = -r1.x + r1.w;
    float stage18_y = -r1.y + r1.w;
    float stage18_z = -r1.z + r1.w;
    r2.x = stage18_x;
    r2.y = stage18_y;
    r2.z = stage18_z;
    // mad r1.xyz, cb0[8].xxxx, r2.xyzx, r1.xyzx
    float stage19_x = cb0[8].x * r2.x + r1.x;
    float stage19_y = cb0[8].x * r2.y + r1.y;
    float stage19_z = cb0[8].x * r2.z + r1.z;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage20_x = max(abs(r1.x), 1e-06f);
    float stage20_y = max(abs(r1.y), 1e-06f);
    float stage20_z = max(abs(r1.z), 1e-06f);
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.z = stage20_z;
    // log r1.xyz, r1.xyzx
    float stage21_x = log2(r1.x);
    float stage21_y = log2(r1.y);
    float stage21_z = log2(r1.z);
    r1.x = stage21_x;
    r1.y = stage21_y;
    r1.z = stage21_z;
    // mul r1.xyz, r1.xyzx, cb0[8].yyyy
    float stage22_x = r1.x * cb0[8].y;
    float stage22_y = r1.y * cb0[8].y;
    float stage22_z = r1.z * cb0[8].y;
    r1.x = stage22_x;
    r1.y = stage22_y;
    r1.z = stage22_z;
    // exp r1.xyz, r1.xyzx
    float stage23_x = exp2(r1.x);
    float stage23_y = exp2(r1.y);
    float stage23_z = exp2(r1.z);
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // mul r1.xyz, r1.xyzx, cb0[8].zzzz
    float stage24_x = r1.x * cb0[8].z;
    float stage24_y = r1.y * cb0[8].z;
    float stage24_z = r1.z * cb0[8].z;
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // mul r0.xyz, r0.xyzx, r1.xyzx
    float stage25_x = r0.x * r1.x;
    float stage25_y = r0.y * r1.y;
    float stage25_z = r0.z * r1.z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage26_x = r0.x * v3.x + cb0[1].x;
    float stage26_y = r0.y * v3.y + cb0[1].y;
    float stage26_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // mul r0.xyz, r0.xyzx, v5.wwww
    float stage27_x = r0.x * v5.w;
    float stage27_y = r0.y * v5.w;
    float stage27_z = r0.z * v5.w;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // add r1.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage28_x = v2.x + -0.5f;
    float stage28_y = v2.y + -0.5f;
    r1.x = stage28_x;
    r1.y = stage28_y;
    // dp2 r1.x, r1.xyxx, r1.xyxx
    r1.x = dot(r1.xy, r1.xy);
    // sqrt r1.x, r1.x
    r1.x = sqrt(r1.x);
    // mad r1.x, -r1.x, l(2.000000), l(1.000000)
    r1.x = -r1.x * 2.0f + 1.0f;
    // mul r1.x, r1.x, l(0.500000)
    r1.x = r1.x * 0.5f;
    // max r1.x, r1.x, l(0.000000)
    r1.x = max(r1.x, 0.0f);
    // lt r1.y, r1.x, l(0.000001)
    r1.y = asfloat((r1.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, r1.x
    r1.x = r1.x * r1.x;
    // movc r1.x, r1.y, l(0), r1.x
    r1.x = asuint(r1.y) != 0u ? asfloat(0u) : r1.x;
    // log r1.y, |r0.w|
    r1.y = log2(abs(r0.w));
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.y, r1.y, cb0[8].w
    r1.y = r1.y * cb0[8].w;
    // exp r1.y, r1.y
    r1.y = exp2(r1.y);
    // mul r1.x, r1.x, r1.y
    r1.x = r1.x * r1.y;
    // mul r1.x, r1.x, v3.w
    r1.x = r1.x * v3.w;
    // mul r1.x, r1.x, cb0[0].x
    r1.x = r1.x * cb0[0].x;
    // movc r0.w, r0.w, l(0), r1.x
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.x;
    // mul o0.xyz, r0.wwww, r0.xyzx
    float stage29_x = r0.w * r0.x;
    float stage29_y = r0.w * r0.y;
    float stage29_z = r0.w * r0.z;
    o0.x = stage29_x;
    o0.y = stage29_y;
    o0.z = stage29_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_bfx_i_pa_aura_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
