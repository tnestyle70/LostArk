// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303_Constants0 : register(b0)
{
    float4 cb0[16];
};
cbuffer Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);

struct Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303_OUTPUT Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303(Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.yxyy, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.y + -0.5f;
    float stage1_y = v2.x + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // max r0.z, |r0.y|, |r0.x|
    r0.z = max(abs(r0.y), abs(r0.x));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r0.y|, |r0.x|
    r0.w = min(abs(r0.y), abs(r0.x));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.x, r0.w, l(0.020835), l(-0.085133)
    r1.x = r0.w * 0.020835f + -0.085133f;
    // mad r1.x, r0.w, r1.x, l(0.180141)
    r1.x = r0.w * r1.x + 0.180141f;
    // mad r1.x, r0.w, r1.x, l(-0.330299)
    r1.x = r0.w * r1.x + -0.330299f;
    // mad r0.w, r0.w, r1.x, l(0.999866)
    r0.w = r0.w * r1.x + 0.999866f;
    // mul r1.x, r0.w, r0.z
    r1.x = r0.w * r0.z;
    // mad r1.x, r1.x, l(-2.000000), l(1.570796)
    r1.x = r1.x * -2.0f + 1.570796f;
    // lt r1.y, |r0.y|, |r0.x|
    r1.y = asfloat((abs(r0.y) < abs(r0.x)) ? 0xffffffffu : 0u);
    // and r1.x, r1.y, r1.x
    r1.x = asfloat(asuint(r1.y) & asuint(r1.x));
    // mad r0.z, r0.z, r0.w, r1.x
    r0.z = r0.z * r0.w + r1.x;
    // lt r0.w, r0.y, -r0.y
    r0.w = asfloat((r0.y < -r0.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r0.y, r0.x
    r0.w = min(r0.y, r0.x);
    // lt r0.w, r0.w, -r0.w
    r0.w = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // max r1.x, r0.y, r0.x
    r1.x = max(r0.y, r0.x);
    // dp2 r0.x, -r0.xyxx, -r0.xyxx
    r0.x = dot(-r0.xy, -r0.xy);
    // sqrt r0.y, r0.x
    r0.y = sqrt(r0.x);
    // ge r1.x, r1.x, -r1.x
    r1.x = asfloat((r1.x >= -r1.x) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, r1.x
    r0.w = asfloat(asuint(r0.w) & asuint(r1.x));
    // movc r0.z, r0.w, -r0.z, r0.z
    r0.z = asuint(r0.w) != 0u ? -r0.z : r0.z;
    // mad r0.z, r0.z, l(0.318471), l(1.000000)
    r0.z = r0.z * 0.318471f + 1.0f;
    // mul r0.x, r0.z, l(0.500000)
    r0.x = r0.z * 0.5f;
    // mad r0.zw, r0.xxxy, cb0[3].xxxy, cb0[4].xxxy
    float stage2_z = r0.x * cb0[3].x + cb0[4].x;
    float stage2_w = r0.y * cb0[3].y + cb0[4].y;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.x, r0.zwzz, t1.xyzw, s0, l(0.000000)
    float4 fetch3 = t1.SampleBias(s0, r0.zw, 0.0f);
    r1.x = fetch3.x;
    // mov r2.y, cb0[12].z
    r2.y = cb0[12].z;
    // mov r2.xz, l(0,0,0,0)
    float stage4_x = asfloat(0u);
    float stage4_z = asfloat(0u);
    r2.x = stage4_x;
    r2.z = stage4_z;
    // add r3.xyzw, r0.zwzw, r2.yxxy
    float stage5_x = r0.z + r2.y;
    float stage5_y = r0.w + r2.x;
    float stage5_z = r0.z + r2.x;
    float stage5_w = r0.w + r2.y;
    r3.x = stage5_x;
    r3.y = stage5_y;
    r3.z = stage5_z;
    r3.w = stage5_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.z, r3.xyxx, t1.yzxw, s0, l(0.000000)
    float4 fetch6 = t1.SampleBias(s0, r3.xy, 0.0f);
    r0.z = fetch6.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r3.zwzz, t1.yzwx, s0, l(0.000000)
    float4 fetch7 = t1.SampleBias(s0, r3.zw, 0.0f);
    r0.w = fetch7.x;
    // add r2.xy, -r1.xxxx, r0.zwzz
    float stage8_x = -r1.x + r0.z;
    float stage8_y = -r1.x + r0.w;
    r2.x = stage8_x;
    r2.y = stage8_y;
    // mul r1.xy, r2.xyxx, l(8.000000, 8.000000, 0.000000, 0.000000)
    float stage9_x = r2.x * 8.0f;
    float stage9_y = r2.y * 8.0f;
    r1.x = stage9_x;
    r1.y = stage9_y;
    // mov r1.z, l(0)
    r1.z = asfloat(0u);
    // add r1.xyz, -r1.xyzx, l(0.000000, 0.000000, 1.000000, 0.000000)
    float stage10_x = -r1.x + 0.0f;
    float stage10_y = -r1.y + 0.0f;
    float stage10_z = -r1.z + 1.0f;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // dp3 r0.z, r1.xyzx, r1.xyzx
    r0.z = dot(r1.xyz, r1.xyz);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // div r0.zw, r1.xxxy, r0.zzzz
    float stage11_z = r1.x / r0.z;
    float stage11_w = r1.y / r0.z;
    r0.z = stage11_z;
    r0.w = stage11_w;
    // mad r0.zw, r0.zzzw, l(0.000000, 0.000000, 0.500000, 0.500000), l(0.000000, 0.000000, 0.500000, 0.500000)
    float stage12_z = r0.z * 0.5f + 0.5f;
    float stage12_w = r0.w * 0.5f + 0.5f;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // mul r0.zw, r0.zzzw, v4.xxxx
    float stage13_z = r0.z * v4.x;
    float stage13_w = r0.w * v4.x;
    r0.z = stage13_z;
    r0.w = stage13_w;
    // mad r1.xy, r0.xyxx, cb0[2].xyxx, r0.zwzz
    float stage14_x = r0.x * cb0[2].x + r0.z;
    float stage14_y = r0.y * cb0[2].y + r0.w;
    r1.x = stage14_x;
    r1.y = stage14_y;
    // mov r2.w, v4.w
    r2.w = v4.w;
    // add r1.xy, r1.xyxx, r2.zwzz
    float stage15_x = r1.x + r2.z;
    float stage15_y = r1.y + r2.w;
    r1.x = stage15_x;
    r1.y = stage15_y;
    // add r1.xy, r1.xyxx, cb0[5].xyxx
    float stage16_x = r1.x + cb0[5].x;
    float stage16_y = r1.y + cb0[5].y;
    r1.x = stage16_x;
    r1.y = stage16_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t3.xyzw, s1, l(0.000000)
    float4 fetch17 = t3.SampleBias(s1, r1.xy, 0.0f);
    r1.x = fetch17.x;
    r1.y = fetch17.y;
    r1.z = fetch17.z;
    // mad r2.xy, r0.xyxx, cb0[6].xyxx, r0.zwzz
    float stage18_x = r0.x * cb0[6].x + r0.z;
    float stage18_y = r0.y * cb0[6].y + r0.w;
    r2.x = stage18_x;
    r2.y = stage18_y;
    // mad r0.xy, r0.xyxx, cb0[9].xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage19_x = r0.x * cb0[9].x + -0.5f;
    float stage19_y = r0.y * cb0[9].y + -0.5f;
    r0.x = stage19_x;
    r0.y = stage19_y;
    // add r2.xy, r2.xyxx, cb0[7].xyxx
    float stage20_x = r2.x + cb0[7].x;
    float stage20_y = r2.y + cb0[7].y;
    r2.x = stage20_x;
    r2.y = stage20_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xyz, r2.xyxx, t4.xyzw, s2, l(0.000000)
    float4 fetch21 = t4.SampleBias(s2, r2.xy, 0.0f);
    r2.x = fetch21.x;
    r2.y = fetch21.y;
    r2.z = fetch21.z;
    // mul r3.xyz, r1.xyzx, r2.xyzx
    float stage22_x = r1.x * r2.x;
    float stage22_y = r1.y * r2.y;
    float stage22_z = r1.z * r2.z;
    r3.x = stage22_x;
    r3.y = stage22_y;
    r3.z = stage22_z;
    // dp3 r1.w, r3.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.w = dot(r3.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r1.xyzx, r2.xyzx, r1.wwww
    float stage23_x = -r1.x * r2.x + r1.w;
    float stage23_y = -r1.y * r2.y + r1.w;
    float stage23_z = -r1.z * r2.z + r1.w;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // mad r1.xyz, cb0[13].xxxx, r1.xyzx, r3.xyzx
    float stage24_x = cb0[13].x * r1.x + r3.x;
    float stage24_y = cb0[13].x * r1.y + r3.y;
    float stage24_z = cb0[13].x * r1.z + r3.z;
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // max r1.xyz, |r1.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage25_x = max(abs(r1.x), 1e-06f);
    float stage25_y = max(abs(r1.y), 1e-06f);
    float stage25_z = max(abs(r1.z), 1e-06f);
    r1.x = stage25_x;
    r1.y = stage25_y;
    r1.z = stage25_z;
    // log r1.xyz, r1.xyzx
    float stage26_x = log2(r1.x);
    float stage26_y = log2(r1.y);
    float stage26_z = log2(r1.z);
    r1.x = stage26_x;
    r1.y = stage26_y;
    r1.z = stage26_z;
    // mul r1.xyz, r1.xyzx, cb0[13].yyyy
    float stage27_x = r1.x * cb0[13].y;
    float stage27_y = r1.y * cb0[13].y;
    float stage27_z = r1.z * cb0[13].y;
    r1.x = stage27_x;
    r1.y = stage27_y;
    r1.z = stage27_z;
    // exp r1.xyz, r1.xyzx
    float stage28_x = exp2(r1.x);
    float stage28_y = exp2(r1.y);
    float stage28_z = exp2(r1.z);
    r1.x = stage28_x;
    r1.y = stage28_y;
    r1.z = stage28_z;
    // mad r1.xyz, cb0[13].zzzz, r1.xyzx, cb0[8].xyzx
    float stage29_x = cb0[13].z * r1.x + cb0[8].x;
    float stage29_y = cb0[13].z * r1.y + cb0[8].y;
    float stage29_z = cb0[13].z * r1.z + cb0[8].z;
    r1.x = stage29_x;
    r1.y = stage29_y;
    r1.z = stage29_z;
    // mad r1.xyz, v3.xyzx, r1.xyzx, cb0[1].xyzx
    float stage30_x = v3.x * r1.x + cb0[1].x;
    float stage30_y = v3.y * r1.y + cb0[1].y;
    float stage30_z = v3.z * r1.z + cb0[1].z;
    r1.x = stage30_x;
    r1.y = stage30_y;
    r1.z = stage30_z;
    // mad o0.xyz, r1.xyzx, v5.wwww, v5.xyzx
    float stage31_x = r1.x * v5.w + v5.x;
    float stage31_y = r1.y * v5.w + v5.y;
    float stage31_z = r1.z * v5.w + v5.z;
    o0.x = stage31_x;
    o0.y = stage31_y;
    o0.z = stage31_z;
    // dp2 r1.x, cb0[10].xyxx, r0.xyxx
    r1.x = dot(cb0[10].xy, r0.xy);
    // dp2 r1.y, cb0[11].xyxx, r0.xyxx
    r1.y = dot(cb0[11].xy, r0.xy);
    // add r0.xy, r1.xyxx, v4.zyzz
    float stage32_x = r1.x + v4.z;
    float stage32_y = r1.y + v4.y;
    r0.x = stage32_x;
    r0.y = stage32_y;
    // add r0.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage33_x = r0.x + 0.5f;
    float stage33_y = r0.y + 0.5f;
    r0.x = stage33_x;
    r0.y = stage33_y;
    // mad r0.xy, cb0[14].zzzz, r0.zwzz, r0.xyxx
    float stage34_x = cb0[14].z * r0.z + r0.x;
    float stage34_y = cb0[14].z * r0.w + r0.y;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s4, l(0.000000)
    float4 fetch35 = t2.SampleBias(s4, r0.xy, 0.0f);
    r0.x = fetch35.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, v2.xyxx, t0.yxzw, s3, l(0.000000)
    float4 fetch36 = t0.SampleBias(s3, v2.xy, 0.0f);
    r0.y = fetch36.x;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // max r0.x, |r0.x|, l(0.000001)
    r0.x = max(abs(r0.x), 1e-06f);
    // log r0.x, r0.x
    r0.x = log2(r0.x);
    // mul r0.x, r0.x, cb0[14].w
    r0.x = r0.x * cb0[14].w;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul_sat r0.x, r0.x, cb0[15].x
    r0.x = saturate(r0.x * cb0[15].x);
    // mul r0.x, r0.x, v3.w
    r0.x = r0.x * v3.w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage37_x = r0.x * v1.x;
    float stage37_y = r0.x * v1.y;
    float stage37_z = r0.x * v1.z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage38_x = r0.w * v0.z;
    float stage38_y = r0.w * v0.x;
    float stage38_z = r0.w * v0.y;
    r1.x = stage38_x;
    r1.y = stage38_y;
    r1.z = stage38_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage39_x = r0.x * r1.x;
    float stage39_y = r0.x * r1.y;
    float stage39_z = r0.x * r1.z;
    r0.x = stage39_x;
    r0.y = stage39_y;
    r0.z = stage39_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage40_x = r0.x / r0.w;
    float stage40_y = r0.y / r0.w;
    r0.x = stage40_x;
    r0.y = stage40_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage41_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage41_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage41_x;
    r1.y = stage41_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage42_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage42_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage42_x;
    r1.y = stage42_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage43_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage43_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage43_x;
    r1.y = stage43_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage44_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage44_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage44_x;
    r0.y = stage44_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage45_x = r0.x * 0.5f + 0.5f;
    float stage45_y = r0.y * 0.5f + 0.5f;
    o2.x = stage45_x;
    o2.y = stage45_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage46_z = 1.0f;
    float stage46_w = asfloat(0u);
    o2.z = stage46_z;
    o2.w = stage46_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage47_x = cb2[3].x;
    float stage47_y = cb2[3].y;
    float stage47_z = cb2[3].z;
    o3.x = stage47_x;
    o3.y = stage47_y;
    o3.z = stage47_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage48_x = asfloat(0u);
    float stage48_y = asfloat(0u);
    float stage48_z = asfloat(0u);
    float stage48_w = asfloat(0u);
    o4.x = stage48_x;
    o4.y = stage48_y;
    o4.z = stage48_z;
    o4.w = stage48_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage49_x = asfloat(0u);
    float stage49_y = asfloat(0u);
    float stage49_z = asfloat(0u);
    float stage49_w = asfloat(0u);
    o5.x = stage49_x;
    o5.y = stage49_y;
    o5.z = stage49_z;
    o5.w = stage49_w;

    Shade_Ue3_fx_k_pa_makeflow_01_tr_e1eb2cd1402c8d73457cae6ddb3c0e146263ca07fd1ae9975d50349081021303_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
