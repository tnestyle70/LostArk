// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_me_watertrail_01_tr_Constants0 : register(b0)
{
    float4 cb0[16];
};
cbuffer Shade_Ue3_fx_m_me_watertrail_01_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);

struct Shade_Ue3_fx_m_me_watertrail_01_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
};

struct Shade_Ue3_fx_m_me_watertrail_01_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_m_me_watertrail_01_tr_OUTPUT Shade_Ue3_fx_m_me_watertrail_01_tr(Shade_Ue3_fx_m_me_watertrail_01_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // dp3 r0.x, v6.xyzx, v6.xyzx
    r0.x = dot(v6.xyz, v6.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.x, r0.x, v6.z
    r0.x = r0.x * v6.z;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[15].z
    r0.y = r0.y * cb0[15].z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // add r0.yz, v4.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage1_y = v4.x + -0.5f;
    float stage1_z = v4.y + -0.5f;
    r0.y = stage1_y;
    r0.z = stage1_z;
    // dp2 r1.x, cb0[4].xyxx, r0.yzyy
    r1.x = dot(cb0[4].xy, r0.yz);
    // dp2 r1.y, cb0[5].xyxx, r0.yzyy
    r1.y = dot(cb0[5].xy, r0.yz);
    // add r0.yz, r1.xxyx, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage2_y = r1.x + 0.5f;
    float stage2_z = r1.y + 0.5f;
    r0.y = stage2_y;
    r0.z = stage2_z;
    // mul r0.w, r0.y, cb0[7].w
    r0.w = r0.y * cb0[7].w;
    // mad r1.x, cb0[7].z, cb0[7].y, r0.w
    r1.x = cb0[7].z * cb0[7].y + r0.w;
    // mul r0.w, r0.z, cb0[8].x
    r0.w = r0.z * cb0[8].x;
    // mad r1.y, cb0[7].z, cb0[9].w, r0.w
    r1.y = cb0[7].z * cb0[9].w + r0.w;
    // add r1.xy, r1.xyxx, cb0[6].xyxx
    float stage3_x = r1.x + cb0[6].x;
    float stage3_y = r1.y + cb0[6].y;
    r1.x = stage3_x;
    r1.y = stage3_y;
    // add r0.w, cb0[3].w, cb0[12].x
    r0.w = cb0[3].w + cb0[12].x;
    // add r0.w, r0.w, l(-1.000000)
    r0.w = r0.w + -1.0f;
    // mul r1.zw, r0.yyyz, cb0[11].xxxy
    float stage4_z = r0.y * cb0[11].x;
    float stage4_w = r0.z * cb0[11].y;
    r1.z = stage4_z;
    r1.w = stage4_w;
    // mul r0.yz, r0.yyzy, cb0[14].xxyx
    float stage5_y = r0.y * cb0[14].x;
    float stage5_z = r0.z * cb0[14].y;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // mad r1.z, cb0[7].z, cb0[10].w, r1.z
    r1.z = cb0[7].z * cb0[10].w + r1.z;
    // mad r1.w, cb0[7].z, cb0[11].z, r1.w
    r1.w = cb0[7].z * cb0[11].z + r1.w;
    // mad r2.y, cb0[3].y, cb0[11].w, r1.w
    r2.y = cb0[3].y * cb0[11].w + r1.w;
    // mad r2.x, cb0[3].y, cb0[10].z, r1.z
    r2.x = cb0[3].y * cb0[10].z + r1.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.zw, r2.xyxx, t0.zwxy, s0, l(0.000000)
    float4 fetch6 = t0.SampleBias(s0, r2.xy, 0.0f);
    r1.z = fetch6.x;
    r1.w = fetch6.y;
    // mad r1.xy, r0.wwww, r1.zwzz, r1.xyxx
    float stage7_x = r0.w * r1.z + r1.x;
    float stage7_y = r0.w * r1.w + r1.y;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // add r1.zw, cb0[3].xxxz, l(0.000000, 0.000000, -1.000000, -1.000000)
    float stage8_z = cb0[3].x + -1.0f;
    float stage8_w = cb0[3].z + -1.0f;
    r1.z = stage8_z;
    r1.w = stage8_w;
    // mad r2.x, r1.z, cb0[7].x, r1.x
    r2.x = r1.z * cb0[7].x + r1.x;
    // mad r2.y, r1.z, cb0[12].y, r1.y
    r2.y = r1.z * cb0[12].y + r1.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xyz, r2.xyxx, t2.xyzw, s2, l(0.000000)
    float4 fetch9 = t2.SampleBias(s2, r2.xy, 0.0f);
    r1.x = fetch9.x;
    r1.y = fetch9.y;
    r1.z = fetch9.z;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.xy, r2.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch10 = t1.SampleBias(s1, r2.xy, 0.0f);
    r2.x = fetch10.x;
    r2.y = fetch10.y;
    // mad r2.xy, r2.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage11_x = r2.x * 2.0f + -1.0f;
    float stage11_y = r2.y * 2.0f + -1.0f;
    r2.x = stage11_x;
    r2.y = stage11_y;
    // mul r3.xyzw, r1.xxyz, cb0[1].wxyz
    float stage12_x = r1.x * cb0[1].w;
    float stage12_y = r1.x * cb0[1].x;
    float stage12_z = r1.y * cb0[1].y;
    float stage12_w = r1.z * cb0[1].z;
    r3.x = stage12_x;
    r3.y = stage12_y;
    r3.z = stage12_z;
    r3.w = stage12_w;
    // mad r0.y, cb0[7].z, cb0[13].w, r0.y
    r0.y = cb0[7].z * cb0[13].w + r0.y;
    // mad r0.z, cb0[7].z, cb0[14].z, r0.z
    r0.z = cb0[7].z * cb0[14].z + r0.z;
    // mad r4.y, cb0[3].y, cb0[14].w, r0.z
    r4.y = cb0[3].y * cb0[14].w + r0.z;
    // mad r4.x, cb0[3].y, cb0[13].z, r0.y
    r4.x = cb0[3].y * cb0[13].z + r0.y;
    // add r0.yz, r4.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage13_y = r4.x + -0.5f;
    float stage13_z = r4.y + -0.5f;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // dp2 r4.x, l(0.000796, -1.000000, 0.000000, 0.000000), r0.yzyy
    r4.x = dot(float2(0.000796f, -1.0f), r0.yz);
    // dp2 r4.y, l(1.000000, 0.000796, 0.000000, 0.000000), r0.yzyy
    r4.y = dot(float2(1.0f, 0.000796f), r0.yz);
    // add r0.yz, r4.xxyx, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage14_y = r4.x + 0.5f;
    float stage14_z = r4.y + 0.5f;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t0.yxzw, s0, l(0.000000)
    float4 fetch15 = t0.SampleBias(s0, r0.yz, 0.0f);
    r0.y = fetch15.x;
    // add r0.y, -r1.w, r0.y
    r0.y = -r1.w + r0.y;
    // mul_sat r0.y, r0.y, cb0[15].x
    r0.y = saturate(r0.y * cb0[15].x);
    // mul r0.y, r0.y, r3.x
    r0.y = r0.y * r3.x;
    // add r0.zw, -v4.xxxy, l(0.000000, 0.000000, 1.000000, 1.000000)
    float stage16_z = -v4.x + 1.0f;
    float stage16_w = -v4.y + 1.0f;
    r0.z = stage16_z;
    r0.w = stage16_w;
    // mul r0.zw, r0.zzzw, v4.xxxy
    float stage17_z = r0.z * v4.x;
    float stage17_w = r0.w * v4.y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // mul r0.z, r0.w, r0.z
    r0.z = r0.w * r0.z;
    // mul_sat r0.z, r0.z, cb0[15].y
    r0.z = saturate(r0.z * cb0[15].y);
    // mul r0.y, r0.z, r0.y
    r0.y = r0.z * r0.y;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul_sat r0.x, r0.x, cb0[15].w
    r0.x = saturate(r0.x * cb0[15].w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, r3.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.x = dot(r3.yzw, float3(0.3f, 0.59f, 0.11f));
    // mad r0.xyz, -cb0[1].xyzx, r1.xyzx, r0.xxxx
    float stage18_x = -cb0[1].x * r1.x + r0.x;
    float stage18_y = -cb0[1].y * r1.y + r0.x;
    float stage18_z = -cb0[1].z * r1.z + r0.x;
    r0.x = stage18_x;
    r0.y = stage18_y;
    r0.z = stage18_z;
    // mad r0.xyz, cb0[12].zzzz, r0.xyzx, r3.yzwy
    float stage19_x = cb0[12].z * r0.x + r3.y;
    float stage19_y = cb0[12].z * r0.y + r3.z;
    float stage19_z = cb0[12].z * r0.z + r3.w;
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    // mul r1.xyz, r0.xyzx, cb0[13].yyyy
    float stage20_x = r0.x * cb0[13].y;
    float stage20_y = r0.y * cb0[13].y;
    float stage20_z = r0.z * cb0[13].y;
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.z = stage20_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage21_x = max(abs(r0.x), 1e-06f);
    float stage21_y = max(abs(r0.y), 1e-06f);
    float stage21_z = max(abs(r0.z), 1e-06f);
    r0.x = stage21_x;
    r0.y = stage21_y;
    r0.z = stage21_z;
    // log r0.xyz, r0.xyzx
    float stage22_x = log2(r0.x);
    float stage22_y = log2(r0.y);
    float stage22_z = log2(r0.z);
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    // mul r0.xyz, r0.xyzx, cb0[12].wwww
    float stage23_x = r0.x * cb0[12].w;
    float stage23_y = r0.y * cb0[12].w;
    float stage23_z = r0.z * cb0[12].w;
    r0.x = stage23_x;
    r0.y = stage23_y;
    r0.z = stage23_z;
    // exp r0.xyz, r0.xyzx
    float stage24_x = exp2(r0.x);
    float stage24_y = exp2(r0.y);
    float stage24_z = exp2(r0.z);
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // mad r0.xyz, cb0[13].xxxx, r0.xyzx, r1.xyzx
    float stage25_x = cb0[13].x * r0.x + r1.x;
    float stage25_y = cb0[13].x * r0.y + r1.y;
    float stage25_z = cb0[13].x * r0.z + r1.z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // add r0.xyz, r0.xyzx, cb0[2].xyzx
    float stage26_x = r0.x + cb0[2].x;
    float stage26_y = r0.y + cb0[2].y;
    float stage26_z = r0.z + cb0[2].z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage27_x = r0.x * v5.w + v5.x;
    float stage27_y = r0.y * v5.w + v5.y;
    float stage27_z = r0.z * v5.w + v5.z;
    o0.x = stage27_x;
    o0.y = stage27_y;
    o0.z = stage27_z;
    // dp2 r0.x, r2.xyxx, r2.xyxx
    r0.x = dot(r2.xy, r2.xy);
    // add r0.x, -r0.x, l(1.000000)
    r0.x = -r0.x + 1.0f;
    // max r0.x, r0.x, l(0.000000)
    r0.x = max(r0.x, 0.0f);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // add r2.z, r0.x, l(0.000010)
    r2.z = r0.x + 1e-05f;
    // mul r0.xyz, r2.xyzx, l(0.300000, 0.300000, 1.000000, 0.000000)
    float stage28_x = r2.x * 0.3f;
    float stage28_y = r2.y * 0.3f;
    float stage28_z = r2.z * 1.0f;
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // dp3 r0.w, r0.xyzx, r0.xyzx
    r0.w = dot(r0.xyz, r0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r0.xyz, r0.wwww, r0.xyzx
    float stage29_x = r0.w * r0.x;
    float stage29_y = r0.w * r0.y;
    float stage29_z = r0.w * r0.z;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // dp3 r0.w, v1.xyzx, v1.xyzx
    r0.w = dot(v1.xyz, v1.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v1.xyzx
    float stage30_x = r0.w * v1.x;
    float stage30_y = r0.w * v1.y;
    float stage30_z = r0.w * v1.z;
    r1.x = stage30_x;
    r1.y = stage30_y;
    r1.z = stage30_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r2.xyz, r0.wwww, v0.xyzx
    float stage31_x = r0.w * v0.x;
    float stage31_y = r0.w * v0.y;
    float stage31_z = r0.w * v0.z;
    r2.x = stage31_x;
    r2.y = stage31_y;
    r2.z = stage31_z;
    // mul r3.xyz, r1.zxyz, r2.yzxy
    float stage32_x = r1.z * r2.y;
    float stage32_y = r1.x * r2.z;
    float stage32_z = r1.y * r2.x;
    r3.x = stage32_x;
    r3.y = stage32_y;
    r3.z = stage32_z;
    // mad r3.xyz, r1.yzxy, r2.zxyz, -r3.xyzx
    float stage33_x = r1.y * r2.z + -r3.x;
    float stage33_y = r1.z * r2.x + -r3.y;
    float stage33_z = r1.x * r2.y + -r3.z;
    r3.x = stage33_x;
    r3.y = stage33_y;
    r3.z = stage33_z;
    // dp3 r1.z, r1.xyzx, r0.xyzx
    r1.z = dot(r1.xyz, r0.xyz);
    // dp3 r1.x, r2.xyzx, r0.xyzx
    r1.x = dot(r2.xyz, r0.xyz);
    // mul r2.xyz, r3.xyzx, v1.wwww
    float stage34_x = r3.x * v1.w;
    float stage34_y = r3.y * v1.w;
    float stage34_z = r3.z * v1.w;
    r2.x = stage34_x;
    r2.y = stage34_y;
    r2.z = stage34_z;
    // dp3 r1.y, r2.xyzx, r0.xyzx
    r1.y = dot(r2.xyz, r0.xyz);
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage35_x = r0.x * r1.x;
    float stage35_y = r0.x * r1.y;
    float stage35_z = r0.x * r1.z;
    r0.x = stage35_x;
    r0.y = stage35_y;
    r0.z = stage35_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage36_x = r0.x / r0.w;
    float stage36_y = r0.y / r0.w;
    r0.x = stage36_x;
    r0.y = stage36_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage37_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage37_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage37_x;
    r1.y = stage37_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage38_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage38_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage38_x;
    r1.y = stage38_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage39_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage39_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage39_x;
    r1.y = stage39_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage40_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage40_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage40_x;
    r0.y = stage40_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage41_x = r0.x * 0.5f + 0.5f;
    float stage41_y = r0.y * 0.5f + 0.5f;
    o2.x = stage41_x;
    o2.y = stage41_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage42_z = 1.0f;
    float stage42_w = asfloat(0u);
    o2.z = stage42_z;
    o2.w = stage42_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage43_x = cb2[3].x;
    float stage43_y = cb2[3].y;
    float stage43_z = cb2[3].z;
    o3.x = stage43_x;
    o3.y = stage43_y;
    o3.z = stage43_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage44_x = asfloat(0u);
    float stage44_y = asfloat(0u);
    float stage44_z = asfloat(0u);
    float stage44_w = asfloat(0u);
    o4.x = stage44_x;
    o4.y = stage44_y;
    o4.z = stage44_z;
    o4.w = stage44_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage45_x = asfloat(0u);
    float stage45_y = asfloat(0u);
    float stage45_z = asfloat(0u);
    float stage45_w = asfloat(0u);
    o5.x = stage45_x;
    o5.y = stage45_y;
    o5.z = stage45_z;
    o5.w = stage45_w;

    Shade_Ue3_fx_m_me_watertrail_01_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
