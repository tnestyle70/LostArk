// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_f_pa_ri_01_ad_Constants0 : register(b0)
{
    float4 cb0[3];
};
SamplerState s0 : register(s0);
Texture2D t0 : register(t0);

struct Shade_Ue3_fx_f_pa_ri_01_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_f_pa_ri_01_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_f_pa_ri_01_ad_OUTPUT Shade_Ue3_fx_f_pa_ri_01_ad(Shade_Ue3_fx_f_pa_ri_01_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;

    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, v2.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch1 = t0.SampleBias(s0, v2.xy, 0.0f);
    r0.x = fetch1.x;
    r0.y = fetch1.y;
    r0.z = fetch1.z;
    // max r1.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage2_x = max(abs(r0.x), 1e-06f);
    float stage2_y = max(abs(r0.y), 1e-06f);
    float stage2_z = max(abs(r0.z), 1e-06f);
    r1.x = stage2_x;
    r1.y = stage2_y;
    r1.z = stage2_z;
    // mul r0.xyz, r0.xyzx, cb0[2].yyyy
    float stage3_x = r0.x * cb0[2].y;
    float stage3_y = r0.y * cb0[2].y;
    float stage3_z = r0.z * cb0[2].y;
    r0.x = stage3_x;
    r0.y = stage3_y;
    r0.z = stage3_z;
    // log r1.xyz, r1.xyzx
    float stage4_x = log2(r1.x);
    float stage4_y = log2(r1.y);
    float stage4_z = log2(r1.z);
    r1.x = stage4_x;
    r1.y = stage4_y;
    r1.z = stage4_z;
    // mul r1.xyz, r1.xyzx, l(0.700000, 0.700000, 0.700000, 0.000000)
    float stage5_x = r1.x * 0.7f;
    float stage5_y = r1.y * 0.7f;
    float stage5_z = r1.z * 0.7f;
    r1.x = stage5_x;
    r1.y = stage5_y;
    r1.z = stage5_z;
    // exp r1.xyz, r1.xyzx
    float stage6_x = exp2(r1.x);
    float stage6_y = exp2(r1.y);
    float stage6_z = exp2(r1.z);
    r1.x = stage6_x;
    r1.y = stage6_y;
    r1.z = stage6_z;
    // mul r1.xyz, r1.xyzx, l(50.000000, 50.000000, 50.000000, 0.000000)
    float stage7_x = r1.x * 50.0f;
    float stage7_y = r1.y * 50.0f;
    float stage7_z = r1.z * 50.0f;
    r1.x = stage7_x;
    r1.y = stage7_y;
    r1.z = stage7_z;
    // min r1.xyz, r1.xyzx, l(1.000000, 1.000000, 1.000000, 0.000000)
    float stage8_x = min(r1.x, 1.0f);
    float stage8_y = min(r1.y, 1.0f);
    float stage8_z = min(r1.z, 1.0f);
    r1.x = stage8_x;
    r1.y = stage8_y;
    r1.z = stage8_z;
    // dp3 r0.w, r1.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r1.xyz, float3(0.3f, 0.59f, 0.11f));
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, v4.w
    r0.w = r0.w * v4.w;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul r0.w, r0.w, v3.w
    r0.w = r0.w * v3.w;
    // mul r0.w, r0.w, cb0[0].x
    r0.w = r0.w * cb0[0].x;
    // mul r1.x, v4.x, cb0[2].x
    r1.x = v4.x * cb0[2].x;
    // mul r1.x, r1.x, l(6.283185)
    r1.x = r1.x * 6.283185f;
    // sincos r1.x, null, r1.x
    float stage9_x = r1.x;
    r1.x = sin(stage9_x);
    // add r1.x, r1.x, v4.y
    r1.x = r1.x + v4.y;
    // mul r1.x, r1.x, v4.z
    r1.x = r1.x * v4.z;
    // mul r1.xyz, r0.xyzx, r1.xxxx
    float stage10_x = r0.x * r1.x;
    float stage10_y = r0.y * r1.x;
    float stage10_z = r0.z * r1.x;
    r1.x = stage10_x;
    r1.y = stage10_y;
    r1.z = stage10_z;
    // mad r0.xyz, cb0[2].zzzz, r1.xyzx, r0.xyzx
    float stage11_x = cb0[2].z * r1.x + r0.x;
    float stage11_y = cb0[2].z * r1.y + r0.y;
    float stage11_z = cb0[2].z * r1.z + r0.z;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage12_x = r0.x * v3.x + cb0[1].x;
    float stage12_y = r0.y * v3.y + cb0[1].y;
    float stage12_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mul r0.xyz, r0.xyzx, v5.wwww
    float stage13_x = r0.x * v5.w;
    float stage13_y = r0.y * v5.w;
    float stage13_z = r0.z * v5.w;
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // mul o0.xyz, r0.wwww, r0.xyzx
    float stage14_x = r0.w * r0.x;
    float stage14_y = r0.w * r0.y;
    float stage14_z = r0.w * r0.z;
    o0.x = stage14_x;
    o0.y = stage14_y;
    o0.z = stage14_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_f_pa_ri_01_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
