// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_trail_07_tr_Constants0 : register(b0)
{
    float4 cb0[11];
};
cbuffer Shade_Ue3_fx_d_pa_trail_07_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);

struct Shade_Ue3_fx_d_pa_trail_07_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_d_pa_trail_07_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_d_pa_trail_07_tr_OUTPUT Shade_Ue3_fx_d_pa_trail_07_tr(Shade_Ue3_fx_d_pa_trail_07_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul r0.xy, v2.xyxx, cb0[2].xyxx
    float stage1_x = v2.x * cb0[2].x;
    float stage1_y = v2.y * cb0[2].y;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // frc r0.xy, r0.xyxx
    float stage2_x = frac(r0.x);
    float stage2_y = frac(r0.y);
    r0.x = stage2_x;
    r0.y = stage2_y;
    // add r0.zw, r0.xxxy, l(0.000000, 0.000000, -0.500000, -0.500000)
    float stage3_z = r0.x + -0.5f;
    float stage3_w = r0.y + -0.5f;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // dp2 r0.z, r0.zwzz, r0.zwzz
    r0.z = dot(r0.zw, r0.zw);
    // sqrt r0.z, r0.z
    r0.z = sqrt(r0.z);
    // mad r0.z, -r0.z, l(2.000000), l(1.000000)
    r0.z = -r0.z * 2.0f + 1.0f;
    // max r0.z, r0.z, l(0.000000)
    r0.z = max(r0.z, 0.0f);
    // lt r0.w, r0.z, l(0.000001)
    r0.w = asfloat((r0.z < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.z, r0.z, r0.z
    r0.z = r0.z * r0.z;
    // movc r0.z, r0.w, l(0), r0.z
    r0.z = asuint(r0.w) != 0u ? asfloat(0u) : r0.z;
    // add r0.z, r0.z, r0.z
    r0.z = r0.z + r0.z;
    // add r1.xy, -v4.yzyy, l(1.000000, 1.000000, 0.000000, 0.000000)
    float stage4_x = -v4.y + 1.0f;
    float stage4_y = -v4.z + 1.0f;
    r1.x = stage4_x;
    r1.y = stage4_y;
    // mad r0.z, -r0.z, r1.x, l(1.000000)
    r0.z = -r0.z * r1.x + 1.0f;
    // add r2.y, -r0.x, l(1.000000)
    r2.y = -r0.x + 1.0f;
    // mov r2.x, v2.y
    r2.x = v2.y;
    // add r0.xy, r0.xyxx, -r2.xyxx
    float stage5_x = r0.x + -r2.x;
    float stage5_y = r0.y + -r2.y;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // mad r0.xy, r0.zzzz, r0.xyxx, r2.xyxx
    float stage6_x = r0.z * r0.x + r2.x;
    float stage6_y = r0.z * r0.y + r2.y;
    r0.x = stage6_x;
    r0.y = stage6_y;
    // log r1.x, |r0.y|
    r1.x = log2(abs(r0.y));
    // mul r1.x, r1.x, v4.x
    r1.x = r1.x * v4.x;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.y, r0.y, l(0), r1.x
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r1.x;
    // mul r0.w, r0.y, cb0[8].z
    r0.w = r0.y * cb0[8].z;
    // mul r0.z, r0.y, cb0[7].w
    r0.z = r0.y * cb0[7].w;
    // add r0.yz, r0.xxzx, cb0[3].xxyx
    float stage7_y = r0.x + cb0[3].x;
    float stage7_z = r0.z + cb0[3].y;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // add r0.xw, r0.xxxw, cb0[4].xxxy
    float stage8_x = r0.x + cb0[4].x;
    float stage8_w = r0.w + cb0[4].y;
    r0.x = stage8_x;
    r0.w = stage8_w;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xzw, r0.xwxx, t3.xwyz, s1, cb0[6].x
    float4 fetch9 = t3.SampleLevel(s1, r0.xw, cb0[6].x);
    r1.x = fetch9.x;
    r1.z = fetch9.y;
    r1.w = fetch9.z;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r0.yzyy, t2.xyzw, s0, cb0[6].x
    float4 fetch10 = t2.SampleLevel(s0, r0.yz, cb0[6].x);
    r2.x = fetch10.x;
    r2.y = fetch10.y;
    r2.z = fetch10.z;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.x, r0.yzyy, t0.xyzw, s2, cb0[10].x
    float4 fetch11 = t0.SampleLevel(s2, r0.yz, cb0[10].x);
    r0.x = fetch11.x;
    // mul r0.yzw, r1.xxzw, r2.xxyz
    float stage12_y = r1.x * r2.x;
    float stage12_z = r1.z * r2.y;
    float stage12_w = r1.w * r2.z;
    r0.y = stage12_y;
    r0.z = stage12_z;
    r0.w = stage12_w;
    // dp3 r2.w, r0.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r2.w = dot(r0.yzw, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xzw, -r2.xxyz, r1.xxzw, r2.wwww
    float stage13_x = -r2.x * r1.x + r2.w;
    float stage13_z = -r2.y * r1.z + r2.w;
    float stage13_w = -r2.z * r1.w + r2.w;
    r1.x = stage13_x;
    r1.z = stage13_z;
    r1.w = stage13_w;
    // mad r0.yzw, cb0[9].yyyy, r1.xxzw, r0.yyzw
    float stage14_y = cb0[9].y * r1.x + r0.y;
    float stage14_z = cb0[9].y * r1.z + r0.z;
    float stage14_w = cb0[9].y * r1.w + r0.w;
    r0.y = stage14_y;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // max r0.yzw, |r0.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage15_y = max(abs(r0.y), 1e-06f);
    float stage15_z = max(abs(r0.z), 1e-06f);
    float stage15_w = max(abs(r0.w), 1e-06f);
    r0.y = stage15_y;
    r0.z = stage15_z;
    r0.w = stage15_w;
    // log r0.yzw, r0.yyzw
    float stage16_y = log2(r0.y);
    float stage16_z = log2(r0.z);
    float stage16_w = log2(r0.w);
    r0.y = stage16_y;
    r0.z = stage16_z;
    r0.w = stage16_w;
    // mul r0.yzw, r0.yyzw, cb0[9].zzzz
    float stage17_y = r0.y * cb0[9].z;
    float stage17_z = r0.z * cb0[9].z;
    float stage17_w = r0.w * cb0[9].z;
    r0.y = stage17_y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // exp r0.yzw, r0.yyzw
    float stage18_y = exp2(r0.y);
    float stage18_z = exp2(r0.z);
    float stage18_w = exp2(r0.w);
    r0.y = stage18_y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mul r0.yzw, r0.yyzw, cb0[9].wwww
    float stage19_y = r0.y * cb0[9].w;
    float stage19_z = r0.z * cb0[9].w;
    float stage19_w = r0.w * cb0[9].w;
    r0.y = stage19_y;
    r0.z = stage19_z;
    r0.w = stage19_w;
    // mul r1.xzw, cb0[5].xxyz, cb0[5].wwww
    float stage20_x = cb0[5].x * cb0[5].w;
    float stage20_z = cb0[5].y * cb0[5].w;
    float stage20_w = cb0[5].z * cb0[5].w;
    r1.x = stage20_x;
    r1.z = stage20_z;
    r1.w = stage20_w;
    // mul r0.yzw, r0.yyzw, r1.xxzw
    float stage21_y = r0.y * r1.x;
    float stage21_z = r0.z * r1.z;
    float stage21_w = r0.w * r1.w;
    r0.y = stage21_y;
    r0.z = stage21_z;
    r0.w = stage21_w;
    // mad r0.yzw, r0.yyzw, v3.xxyz, cb0[1].xxyz
    float stage22_y = r0.y * v3.x + cb0[1].x;
    float stage22_z = r0.z * v3.y + cb0[1].y;
    float stage22_w = r0.w * v3.z + cb0[1].z;
    r0.y = stage22_y;
    r0.z = stage22_z;
    r0.w = stage22_w;
    // mad o0.xyz, r0.yzwy, v5.wwww, v5.xyzx
    float stage23_x = r0.y * v5.w + v5.x;
    float stage23_y = r0.z * v5.w + v5.y;
    float stage23_z = r0.w * v5.w + v5.z;
    o0.x = stage23_x;
    o0.y = stage23_y;
    o0.z = stage23_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, v2.xyxx, t1.yxzw, s3, l(0.000000)
    float4 fetch24 = t1.SampleBias(s3, v2.xy, 0.0f);
    r0.y = fetch24.x;
    // mad r0.x, r0.x, r0.y, -r1.y
    r0.x = r0.x * r0.y + -r1.y;
    // mul_sat r0.x, r0.x, cb0[10].y
    r0.x = saturate(r0.x * cb0[10].y);
    // lt r0.y, r0.x, l(0.000001)
    r0.y = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // log r0.x, r0.x
    r0.x = log2(r0.x);
    // mul r0.x, r0.x, cb0[10].z
    r0.x = r0.x * cb0[10].z;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // movc o0.w, r0.y, l(0), r0.x
    o0.w = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage25_x = r0.x * v1.x;
    float stage25_y = r0.x * v1.y;
    float stage25_z = r0.x * v1.z;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage26_x = r0.w * v0.z;
    float stage26_y = r0.w * v0.x;
    float stage26_z = r0.w * v0.y;
    r1.x = stage26_x;
    r1.y = stage26_y;
    r1.z = stage26_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage27_x = r0.x * r1.x;
    float stage27_y = r0.x * r1.y;
    float stage27_z = r0.x * r1.z;
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage28_x = r0.x / r0.w;
    float stage28_y = r0.y / r0.w;
    r0.x = stage28_x;
    r0.y = stage28_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage29_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage29_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage29_x;
    r1.y = stage29_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage30_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage30_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage31_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage31_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage31_x;
    r1.y = stage31_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage32_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage32_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage32_x;
    r0.y = stage32_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage33_x = r0.x * 0.5f + 0.5f;
    float stage33_y = r0.y * 0.5f + 0.5f;
    o2.x = stage33_x;
    o2.y = stage33_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage34_z = 1.0f;
    float stage34_w = asfloat(0u);
    o2.z = stage34_z;
    o2.w = stage34_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage35_x = cb2[3].x;
    float stage35_y = cb2[3].y;
    float stage35_z = cb2[3].z;
    o3.x = stage35_x;
    o3.y = stage35_y;
    o3.z = stage35_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage36_x = asfloat(0u);
    float stage36_y = asfloat(0u);
    float stage36_z = asfloat(0u);
    float stage36_w = asfloat(0u);
    o4.x = stage36_x;
    o4.y = stage36_y;
    o4.z = stage36_z;
    o4.w = stage36_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage37_x = asfloat(0u);
    float stage37_y = asfloat(0u);
    float stage37_z = asfloat(0u);
    float stage37_w = asfloat(0u);
    o5.x = stage37_x;
    o5.y = stage37_y;
    o5.z = stage37_z;
    o5.w = stage37_w;

    Shade_Ue3_fx_d_pa_trail_07_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
