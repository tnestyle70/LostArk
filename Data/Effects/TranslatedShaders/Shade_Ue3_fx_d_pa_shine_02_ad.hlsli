// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_shine_02_ad_Constants0 : register(b0)
{
    float4 cb0[10];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_d_pa_shine_02_ad_INPUT
{
    float4 v2;
    float4 v3;
    float4 v5;
};

struct Shade_Ue3_fx_d_pa_shine_02_ad_OUTPUT
{
    float4 o0;
};

Shade_Ue3_fx_d_pa_shine_02_ad_OUTPUT Shade_Ue3_fx_d_pa_shine_02_ad(Shade_Ue3_fx_d_pa_shine_02_ad_INPUT stage)
{
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 o0 = (float4)0;

    // add r0.xyzw, v2.xyxy, l(-0.500000, -0.500000, -0.500000, -0.500000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    float stage1_z = v2.x + -0.5f;
    float stage1_w = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    r0.z = stage1_z;
    r0.w = stage1_w;
    // dp4 r0.x, cb0[2].xyxy, r0.xyzw
    r0.x = dot(cb0[2].xyxy, r0.xyzw);
    // dp2 r0.y, cb0[3].xyxx, r0.zwzz
    r0.y = dot(cb0[3].xy, r0.zw);
    // add r0.y, r0.y, l(0.500000)
    r0.y = r0.y + 0.5f;
    // add r0.z, -r0.y, l(1.000000)
    r0.z = -r0.y + 1.0f;
    // mad r0.w, r0.z, cb0[4].w, cb0[5].x
    r0.w = r0.z * cb0[4].w + cb0[5].x;
    // mul_sat r0.z, r0.z, cb0[9].x
    r0.z = saturate(r0.z * cb0[9].x);
    // mul r0.w, r0.w, r0.w
    r0.w = r0.w * r0.w;
    // div r0.x, r0.x, r0.w
    r0.x = r0.x / r0.w;
    // mad_sat r0.x, r0.x, l(0.500000), l(0.500000)
    r0.x = saturate(r0.x * 0.5f + 0.5f);
    // add r0.w, -r0.x, l(1.000000)
    r0.w = -r0.x + 1.0f;
    // mul r0.w, r0.x, r0.w
    r0.w = r0.x * r0.w;
    // mul r1.x, r0.w, l(4.000000)
    r1.x = r0.w * 4.0f;
    // lt r0.w, r0.w, l(0.000000)
    r0.w = asfloat((r0.w < 0.0f) ? 0xffffffffu : 0u);
    // log r1.x, r1.x
    r1.x = log2(r1.x);
    // mul r1.x, r1.x, cb0[5].y
    r1.x = r1.x * cb0[5].y;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r1.x, r1.x, cb0[5].z
    r1.x = r1.x * cb0[5].z;
    // movc r0.w, r0.w, l(0), r1.x
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.x;
    // log r1.x, |r0.y|
    r1.x = log2(abs(r0.y));
    // mul r1.x, r1.x, cb0[5].w
    r1.x = r1.x * cb0[5].w;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r1.x, r1.x, cb0[6].x
    r1.x = r1.x * cb0[6].x;
    // lt r1.y, |r0.y|, l(0.000001)
    r1.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r1.x, r1.y, l(0), r1.x
    r1.x = asuint(r1.y) != 0u ? asfloat(0u) : r1.x;
    // mul r0.w, r0.w, r1.x
    r0.w = r0.w * r1.x;
    // mul r1.x, r0.x, cb0[6].w
    r1.x = r0.x * cb0[6].w;
    // mad r1.x, cb0[6].z, cb0[6].y, r1.x
    r1.x = cb0[6].z * cb0[6].y + r1.x;
    // mul r1.zw, cb0[6].zzzz, cb0[7].yyyz
    float stage2_z = cb0[6].z * cb0[7].y;
    float stage2_w = cb0[6].z * cb0[7].z;
    r1.z = stage2_z;
    r1.w = stage2_w;
    // mad r1.y, cb0[7].x, r0.y, r1.z
    r1.y = cb0[7].x * r0.y + r1.z;
    // mul r0.y, r0.y, cb0[8].x
    r0.y = r0.y * cb0[8].x;
    // mad r2.y, cb0[6].z, cb0[8].y, r0.y
    r2.y = cb0[6].z * cb0[8].y + r0.y;
    // mad r2.x, cb0[7].w, r0.x, r1.w
    r2.x = cb0[7].w * r0.x + r1.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r2.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch3 = t1.SampleBias(s1, r2.xy, 0.0f);
    r0.x = fetch3.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r1.xyxx, t0.yxzw, s0, l(0.000000)
    float4 fetch4 = t0.SampleBias(s0, r1.xy, 0.0f);
    r0.y = fetch4.x;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // lt r0.y, |r0.x|, l(0.000001)
    r0.y = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // log r0.x, |r0.x|
    r0.x = log2(abs(r0.x));
    // mul r0.x, r0.x, cb0[8].z
    r0.x = r0.x * cb0[8].z;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mul r0.x, r0.x, cb0[8].w
    r0.x = r0.x * cb0[8].w;
    // movc r0.x, r0.y, l(0), r0.x
    r0.x = asuint(r0.y) != 0u ? asfloat(0u) : r0.x;
    // mul_sat r0.x, r0.x, r0.w
    r0.x = saturate(r0.x * r0.w);
    // log r0.y, r0.z
    r0.y = log2(r0.z);
    // lt r0.z, r0.z, l(0.000001)
    r0.z = asfloat((r0.z < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[9].y
    r0.y = r0.y * cb0[9].y;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul r0.x, r0.x, cb0[0].x
    r0.x = r0.x * cb0[0].x;
    // movc r0.x, r0.z, l(0), r0.x
    r0.x = asuint(r0.z) != 0u ? asfloat(0u) : r0.x;
    // add r0.yzw, v3.xxyz, cb0[1].xxyz
    float stage5_y = v3.x + cb0[1].x;
    float stage5_z = v3.y + cb0[1].y;
    float stage5_w = v3.z + cb0[1].z;
    r0.y = stage5_y;
    r0.z = stage5_z;
    r0.w = stage5_w;
    // mul r0.yzw, r0.yyzw, v5.wwww
    float stage6_y = r0.y * v5.w;
    float stage6_z = r0.z * v5.w;
    float stage6_w = r0.w * v5.w;
    r0.y = stage6_y;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // mul o0.xyz, r0.xxxx, r0.yzwy
    float stage7_x = r0.x * r0.y;
    float stage7_y = r0.x * r0.z;
    float stage7_z = r0.x * r0.w;
    o0.x = stage7_x;
    o0.y = stage7_y;
    o0.z = stage7_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);

    Shade_Ue3_fx_d_pa_shine_02_ad_OUTPUT result;
    result.o0 = o0;
    return result;
}
