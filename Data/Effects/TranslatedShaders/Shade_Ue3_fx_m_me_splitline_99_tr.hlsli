// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_me_splitline_99_tr_Constants0 : register(b0)
{
    float4 cb0[6];
};
cbuffer Shade_Ue3_fx_m_me_splitline_99_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
Texture2D t0 : register(t0);

struct Shade_Ue3_fx_m_me_splitline_99_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v7;
};

struct Shade_Ue3_fx_m_me_splitline_99_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_m_me_splitline_99_tr_OUTPUT Shade_Ue3_fx_m_me_splitline_99_tr(Shade_Ue3_fx_m_me_splitline_99_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 r4 = (float4)0;
    float4 r5 = (float4)0;
    float4 r6 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage1_x = r0.x * v1.x;
    float stage1_y = r0.x * v1.y;
    float stage1_z = r0.x * v1.z;
    r0.x = stage1_x;
    r0.y = stage1_y;
    r0.z = stage1_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.xyzx
    float stage2_x = r0.w * v0.x;
    float stage2_y = r0.w * v0.y;
    float stage2_z = r0.w * v0.z;
    r1.x = stage2_x;
    r1.y = stage2_y;
    r1.z = stage2_z;
    // mul r0.w, r0.y, r1.x
    r0.w = r0.y * r1.x;
    // mad r0.w, r0.x, r1.y, -r0.w
    r0.w = r0.x * r1.y + -r0.w;
    // mul r0.y, r0.w, v1.w
    r0.y = r0.w * v1.w;
    // mul o0.w, cb0[0].x, cb0[1].w
    o0.w = cb0[0].x * cb0[1].w;
    // div r1.xy, v7.xyxx, v7.wwww
    float stage3_x = v7.x / v7.w;
    float stage3_y = v7.y / v7.w;
    r1.x = stage3_x;
    r1.y = stage3_y;
    // mad r1.xy, r1.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage4_x = r1.x * cb2[0].x + cb2[0].w;
    float stage4_y = r1.y * cb2[0].y + cb2[0].z;
    r1.x = stage4_x;
    r1.y = stage4_y;
    // mul r2.xyz, cb0[3].wyzw, cb0[5].xyzx
    float stage5_x = cb0[3].w * cb0[5].x;
    float stage5_y = cb0[3].y * cb0[5].y;
    float stage5_z = cb0[3].z * cb0[5].z;
    r2.x = stage5_x;
    r2.y = stage5_y;
    r2.z = stage5_z;
    // add r0.w, -v4.y, l(1.000000)
    r0.w = -v4.y + 1.0f;
    // lt r1.w, |r0.w|, l(0.000001)
    r1.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // log r0.w, |r0.w|
    r0.w = log2(abs(r0.w));
    // mul r0.w, r0.w, l(15.000000)
    r0.w = r0.w * 15.0f;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // movc r0.w, r1.w, l(0), r0.w
    r0.w = asuint(r1.w) != 0u ? asfloat(0u) : r0.w;
    // lt r1.w, |v4.y|, l(0.000001)
    r1.w = asfloat((abs(v4.y) < 1e-06f) ? 0xffffffffu : 0u);
    // log r2.w, |v4.y|
    r2.w = log2(abs(v4.y));
    // mul r2.w, r2.w, l(15.000000)
    r2.w = r2.w * 15.0f;
    // exp r2.w, r2.w
    r2.w = exp2(r2.w);
    // movc r1.w, r1.w, l(0), r2.w
    r1.w = asuint(r1.w) != 0u ? asfloat(0u) : r2.w;
    // add r0.w, r0.w, r1.w
    r0.w = r0.w + r1.w;
    // add r1.w, v4.x, l(-0.500000)
    r1.w = v4.x + -0.5f;
    // add r2.w, |r1.w|, |r1.w|
    r2.w = abs(r1.w) + abs(r1.w);
    // lt r1.w, |r1.w|, l(0.000000)
    r1.w = asfloat((abs(r1.w) < 0.0f) ? 0xffffffffu : 0u);
    // log r2.w, r2.w
    r2.w = log2(r2.w);
    // mul r2.w, r2.w, l(10.000000)
    r2.w = r2.w * 10.0f;
    // exp r2.w, r2.w
    r2.w = exp2(r2.w);
    // movc r1.w, r1.w, l(0), r2.w
    r1.w = asuint(r1.w) != 0u ? asfloat(0u) : r2.w;
    // add r0.w, r0.w, r1.w
    r0.w = r0.w + r1.w;
    // mul r1.w, r0.w, r2.x
    r1.w = r0.w * r2.x;
    // mul r2.x, r1.w, l(0.100000)
    r2.x = r1.w * 0.1f;
    // mad r3.xy, r1.wwww, l(0.050000, 0.050000, 0.000000, 0.000000), r1.xyxx
    float stage6_x = r1.w * 0.05f + r1.x;
    float stage6_y = r1.w * 0.05f + r1.y;
    r3.x = stage6_x;
    r3.y = stage6_y;
    // mad r1.w, cb0[3].x, l(0.010000), r2.x
    r1.w = cb0[3].x * 0.01f + r2.x;
    // sample_b_indexable(texture2d)(float,float,float,float) r4.xyz, r3.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch7 = t0.SampleBias(s0, r3.xy, 0.0f);
    r4.x = fetch7.x;
    r4.y = fetch7.y;
    r4.z = fetch7.z;
    // add r1.xy, r1.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage8_x = r1.x + -0.5f;
    float stage8_y = r1.y + -0.5f;
    r1.x = stage8_x;
    r1.y = stage8_y;
    // mul r1.xyw, r1.xyxw, l(0.000000, 1.000000, 0.000000, 0.100000)
    float stage9_x = r1.x * 0.0f;
    float stage9_y = r1.y * 1.0f;
    float stage9_w = r1.w * 0.1f;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.w = stage9_w;
    // mov r2.xw, r3.yyyx
    float stage10_x = r3.y;
    float stage10_w = r3.x;
    r2.x = stage10_x;
    r2.w = stage10_w;
    // mov r5.x, r4.x
    r5.x = r4.x;
    // mov r5.y, l(0)
    r5.y = asfloat(0u);
    // loop
    while (true) {
    // ge r3.z, r5.y, l(3.000000)
    r3.z = asfloat((r5.y >= 3.0f) ? 0xffffffffu : 0u);
    // breakc_nz r3.z
    if (asuint(r3.z) != 0u) break;
    // mad r2.xw, -r1.yyyx, r1.wwww, r2.xxxw
    float stage11_x = -r1.y * r1.w + r2.x;
    float stage11_w = -r1.x * r1.w + r2.w;
    r2.x = stage11_x;
    r2.w = stage11_w;
    // sample_b_indexable(texture2d)(float,float,float,float) r3.z, r2.wxww, t0.yzxw, s0, l(0.000000)
    float4 fetch12 = t0.SampleBias(s0, r2.wx, 0.0f);
    r3.z = fetch12.x;
    // add r5.x, r3.z, r5.x
    r5.x = r3.z + r5.x;
    // add r5.y, r5.y, l(1.000000)
    r5.y = r5.y + 1.0f;
    // endloop
    }
    // mov r3.z, r3.x
    r3.z = r3.x;
    // mov r3.y, r2.x
    r3.y = r2.x;
    // mov r6.x, r4.y
    r6.x = r4.y;
    // mov r6.y, l(0)
    r6.y = asfloat(0u);
    // loop
    while (true) {
    // ge r2.w, r6.y, l(3.000000)
    r2.w = asfloat((r6.y >= 3.0f) ? 0xffffffffu : 0u);
    // breakc_nz r2.w
    if (asuint(r2.w) != 0u) break;
    // mad r3.yz, -r1.yyxy, r1.wwww, r3.yyzy
    float stage13_y = -r1.y * r1.w + r3.y;
    float stage13_z = -r1.x * r1.w + r3.z;
    r3.y = stage13_y;
    r3.z = stage13_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.w, r3.zyzz, t0.xzwy, s0, l(0.000000)
    float4 fetch14 = t0.SampleBias(s0, r3.zy, 0.0f);
    r2.w = fetch14.y;
    // add r6.x, r2.w, r6.x
    r6.x = r2.w + r6.x;
    // add r6.y, r6.y, l(1.000000)
    r6.y = r6.y + 1.0f;
    // endloop
    }
    // mov r5.y, r6.x
    r5.y = r6.x;
    // mov r4.xy, r3.xyxx
    float stage15_x = r3.x;
    float stage15_y = r3.y;
    r4.x = stage15_x;
    r4.y = stage15_y;
    // mov r6.x, r4.z
    r6.x = r4.z;
    // mov r6.y, l(0)
    r6.y = asfloat(0u);
    // loop
    while (true) {
    // ge r2.x, r6.y, l(3.000000)
    r2.x = asfloat((r6.y >= 3.0f) ? 0xffffffffu : 0u);
    // breakc_nz r2.x
    if (asuint(r2.x) != 0u) break;
    // mad r4.xy, -r1.xyxx, r1.wwww, r4.xyxx
    float stage16_x = -r1.x * r1.w + r4.x;
    float stage16_y = -r1.y * r1.w + r4.y;
    r4.x = stage16_x;
    r4.y = stage16_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r2.x, r4.xyxx, t0.zxyw, s0, l(0.000000)
    float4 fetch17 = t0.SampleBias(s0, r4.xy, 0.0f);
    r2.x = fetch17.z;
    // add r6.x, r2.x, r6.x
    r6.x = r2.x + r6.x;
    // add r6.y, r6.y, l(1.000000)
    r6.y = r6.y + 1.0f;
    // endloop
    }
    // mov r5.z, r6.x
    r5.z = r6.x;
    // mul r1.xyw, r5.xyxz, l(0.250000, 0.250000, 0.000000, 0.250000)
    float stage18_x = r5.x * 0.25f;
    float stage18_y = r5.y * 0.25f;
    float stage18_w = r5.z * 0.25f;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.w = stage18_w;
    // dp3 r2.x, r1.xywx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r2.x = dot(r1.xyw, float3(0.3f, 0.59f, 0.11f));
    // mad r3.xyz, -r5.xyzx, l(0.250000, 0.250000, 0.250000, 0.000000), r2.xxxx
    float stage19_x = -r5.x * 0.25f + r2.x;
    float stage19_y = -r5.y * 0.25f + r2.x;
    float stage19_z = -r5.z * 0.25f + r2.x;
    r3.x = stage19_x;
    r3.y = stage19_y;
    r3.z = stage19_z;
    // mad r1.xyw, r2.yyyy, r3.xyxz, r1.xyxw
    float stage20_x = r2.y * r3.x + r1.x;
    float stage20_y = r2.y * r3.y + r1.y;
    float stage20_w = r2.y * r3.z + r1.w;
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.w = stage20_w;
    // lt r2.x, r0.w, l(0.000001)
    r2.x = asfloat((r0.w < 1e-06f) ? 0xffffffffu : 0u);
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, r2.z
    r0.w = r0.w * r2.z;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul r2.yzw, r0.wwww, cb0[4].xxyz
    float stage21_y = r0.w * cb0[4].x;
    float stage21_z = r0.w * cb0[4].y;
    float stage21_w = r0.w * cb0[4].z;
    r2.y = stage21_y;
    r2.z = stage21_z;
    r2.w = stage21_w;
    // movc r2.xyz, r2.xxxx, l(0,0,0,0), r2.yzwy
    float stage22_x = asuint(r2.x) != 0u ? asfloat(0u) : r2.y;
    float stage22_y = asuint(r2.x) != 0u ? asfloat(0u) : r2.z;
    float stage22_z = asuint(r2.x) != 0u ? asfloat(0u) : r2.w;
    r2.x = stage22_x;
    r2.y = stage22_y;
    r2.z = stage22_z;
    // add r1.xyw, r1.xyxw, r2.xyxz
    float stage23_x = r1.x + r2.x;
    float stage23_y = r1.y + r2.y;
    float stage23_w = r1.w + r2.z;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.w = stage23_w;
    // mad r1.xyw, cb0[1].xyxz, r1.xyxw, cb0[2].xyxz
    float stage24_x = cb0[1].x * r1.x + cb0[2].x;
    float stage24_y = cb0[1].y * r1.y + cb0[2].y;
    float stage24_w = cb0[1].z * r1.w + cb0[2].z;
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.w = stage24_w;
    // mad o0.xyz, r1.xywx, v5.wwww, v5.xyzx
    float stage25_x = r1.x * v5.w + v5.x;
    float stage25_y = r1.y * v5.w + v5.y;
    float stage25_z = r1.w * v5.w + v5.z;
    o0.x = stage25_x;
    o0.y = stage25_y;
    o0.z = stage25_z;
    // mov r0.x, r1.z
    r0.x = r1.z;
    // dp3 r0.w, r0.xyzx, r0.xyzx
    r0.w = dot(r0.xyz, r0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r0.xyz, r0.wwww, r0.xyzx
    float stage26_x = r0.w * r0.x;
    float stage26_y = r0.w * r0.y;
    float stage26_z = r0.w * r0.z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage27_x = r0.x / r0.w;
    float stage27_y = r0.y / r0.w;
    r0.x = stage27_x;
    r0.y = stage27_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage28_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage28_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage28_x;
    r1.y = stage28_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage29_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage29_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage29_x;
    r1.y = stage29_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage30_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage30_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage30_x;
    r1.y = stage30_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage31_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage31_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage31_x;
    r0.y = stage31_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage32_x = r0.x * 0.5f + 0.5f;
    float stage32_y = r0.y * 0.5f + 0.5f;
    o2.x = stage32_x;
    o2.y = stage32_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage33_z = 1.0f;
    float stage33_w = asfloat(0u);
    o2.z = stage33_z;
    o2.w = stage33_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage34_x = cb2[3].x;
    float stage34_y = cb2[3].y;
    float stage34_z = cb2[3].z;
    o3.x = stage34_x;
    o3.y = stage34_y;
    o3.z = stage34_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage35_x = asfloat(0u);
    float stage35_y = asfloat(0u);
    float stage35_z = asfloat(0u);
    float stage35_w = asfloat(0u);
    o4.x = stage35_x;
    o4.y = stage35_y;
    o4.z = stage35_z;
    o4.w = stage35_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage36_x = asfloat(0u);
    float stage36_y = asfloat(0u);
    float stage36_z = asfloat(0u);
    float stage36_w = asfloat(0u);
    o5.x = stage36_x;
    o5.y = stage36_y;
    o5.z = stage36_z;
    o5.w = stage36_w;

    Shade_Ue3_fx_m_me_splitline_99_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
