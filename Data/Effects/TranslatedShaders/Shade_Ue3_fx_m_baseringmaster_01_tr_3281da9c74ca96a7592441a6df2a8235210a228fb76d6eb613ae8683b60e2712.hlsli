// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712_Constants0 : register(b0)
{
    float4 cb0[24];
};
cbuffer Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
SamplerState s3 : register(s3);
SamplerState s4 : register(s4);
SamplerState s5 : register(s5);
SamplerState s6 : register(s6);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);
Texture2D t3 : register(t3);
Texture2D t4 : register(t4);
Texture2D t5 : register(t5);
Texture2D t6 : register(t6);

struct Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    float4 v7;
};

struct Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712_OUTPUT Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712(Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // max r0.z, |r0.y|, |r0.x|
    r0.z = max(abs(r0.y), abs(r0.x));
    // div r0.z, l(1.000000, 1.000000, 1.000000, 1.000000), r0.z
    r0.z = 1.0f / r0.z;
    // min r0.w, |r0.y|, |r0.x|
    r0.w = min(abs(r0.y), abs(r0.x));
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r0.z, r0.z
    r0.w = r0.z * r0.z;
    // mad r1.x, r0.w, l(0.020835), l(-0.085133)
    r1.x = r0.w * 0.020835f + -0.085133f;
    // mad r1.x, r0.w, r1.x, l(0.180141)
    r1.x = r0.w * r1.x + 0.180141f;
    // mad r1.x, r0.w, r1.x, l(-0.330299)
    r1.x = r0.w * r1.x + -0.330299f;
    // mad r0.w, r0.w, r1.x, l(0.999866)
    r0.w = r0.w * r1.x + 0.999866f;
    // mul r1.x, r0.w, r0.z
    r1.x = r0.w * r0.z;
    // mad r1.x, r1.x, l(-2.000000), l(1.570796)
    r1.x = r1.x * -2.0f + 1.570796f;
    // lt r1.y, |r0.y|, |r0.x|
    r1.y = asfloat((abs(r0.y) < abs(r0.x)) ? 0xffffffffu : 0u);
    // and r1.x, r1.y, r1.x
    r1.x = asfloat(asuint(r1.y) & asuint(r1.x));
    // mad r0.z, r0.z, r0.w, r1.x
    r0.z = r0.z * r0.w + r1.x;
    // lt r0.w, r0.y, -r0.y
    r0.w = asfloat((r0.y < -r0.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, l(0xc0490fdb)
    r0.w = asfloat(asuint(r0.w) & asuint(-3.1415927410125732f));
    // add r0.z, r0.w, r0.z
    r0.z = r0.w + r0.z;
    // min r0.w, r0.y, r0.x
    r0.w = min(r0.y, r0.x);
    // lt r0.w, r0.w, -r0.w
    r0.w = asfloat((r0.w < -r0.w) ? 0xffffffffu : 0u);
    // max r1.x, r0.y, r0.x
    r1.x = max(r0.y, r0.x);
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // ge r0.y, r1.x, -r1.x
    r0.y = asfloat((r1.x >= -r1.x) ? 0xffffffffu : 0u);
    // and r0.y, r0.y, r0.w
    r0.y = asfloat(asuint(r0.y) & asuint(r0.w));
    // movc r0.y, r0.y, -r0.z, r0.z
    r0.y = asuint(r0.y) != 0u ? -r0.z : r0.z;
    // mad r0.y, r0.y, l(0.159155), l(0.500000)
    r0.y = r0.y * 0.159155f + 0.5f;
    // mad r0.z, v4.y, cb0[10].y, cb0[10].z
    r0.z = v4.y * cb0[10].y + cb0[10].z;
    // add r0.z, r0.z, cb0[11].x
    r0.z = r0.z + cb0[11].x;
    // mad r0.z, cb0[11].y, r0.y, r0.z
    r0.z = cb0[11].y * r0.y + r0.z;
    // mad r0.w, v4.y, cb0[9].x, cb0[9].y
    r0.w = v4.y * cb0[9].x + cb0[9].y;
    // add r0.w, r0.w, cb0[9].w
    r0.w = r0.w + cb0[9].w;
    // mad r0.w, r0.x, cb0[8].w, r0.w
    r0.w = r0.x * cb0[8].w + r0.w;
    // mad r1.x, cb0[10].x, r0.w, r0.z
    r1.x = cb0[10].x * r0.w + r0.z;
    // mad r1.y, r0.z, cb0[11].z, r0.w
    r1.y = r0.z * cb0[11].z + r0.w;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.zw, r1.xyxx, t1.zwxy, s3, cb0[2].x
    float4 fetch2 = t1.SampleLevel(s3, r1.xy, cb0[2].x);
    r0.z = fetch2.x;
    r0.w = fetch2.y;
    // mul r0.zw, r0.zzzw, cb0[11].wwww
    float stage3_z = r0.z * cb0[11].w;
    float stage3_w = r0.w * cb0[11].w;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // mad r1.x, v4.y, cb0[7].x, cb0[7].y
    r1.x = v4.y * cb0[7].x + cb0[7].y;
    // add r1.x, r1.x, cb0[7].w
    r1.x = r1.x + cb0[7].w;
    // mad r1.x, cb0[8].x, r0.y, r1.x
    r1.x = cb0[8].x * r0.y + r1.x;
    // mad r1.y, v4.y, cb0[5].w, cb0[6].x
    r1.y = v4.y * cb0[5].w + cb0[6].x;
    // add r1.y, r1.y, cb0[6].z
    r1.y = r1.y + cb0[6].z;
    // mad r1.y, r0.x, cb0[5].z, r1.y
    r1.y = r0.x * cb0[5].z + r1.y;
    // mad r2.x, cb0[6].w, r1.y, r1.x
    r2.x = cb0[6].w * r1.y + r1.x;
    // mad r2.y, r1.x, cb0[8].y, r1.y
    r2.y = r1.x * cb0[8].y + r1.y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xy, r2.xyxx, t0.xyzw, s2, cb0[2].x
    float4 fetch4 = t0.SampleLevel(s2, r2.xy, cb0[2].x);
    r1.x = fetch4.x;
    r1.y = fetch4.y;
    // mad r0.zw, cb0[8].zzzz, r1.xxxy, r0.zzzw
    float stage5_z = cb0[8].z * r1.x + r0.z;
    float stage5_w = cb0[8].z * r1.y + r0.w;
    r0.z = stage5_z;
    r0.w = stage5_w;
    // mul r0.zw, r0.zzzw, v4.zzzz
    float stage6_z = r0.z * v4.z;
    float stage6_w = r0.w * v4.z;
    r0.z = stage6_z;
    r0.w = stage6_w;
    // mad r1.x, v4.x, cb0[14].x, cb0[14].y
    r1.x = v4.x * cb0[14].x + cb0[14].y;
    // mad r1.x, cb0[14].z, r0.y, r1.x
    r1.x = cb0[14].z * r0.y + r1.x;
    // add r1.x, r1.x, cb0[14].w
    r1.x = r1.x + cb0[14].w;
    // mad r1.y, v4.x, cb0[12].z, cb0[12].w
    r1.y = v4.x * cb0[12].z + cb0[12].w;
    // mad r1.y, r0.x, cb0[12].y, r1.y
    r1.y = r0.x * cb0[12].y + r1.y;
    // add r1.y, r1.y, cb0[13].y
    r1.y = r1.y + cb0[13].y;
    // mad r2.x, cb0[13].z, r1.y, r1.x
    r2.x = cb0[13].z * r1.y + r1.x;
    // mad r2.y, r1.x, cb0[15].x, r1.y
    r2.y = r1.x * cb0[15].x + r1.y;
    // mad r1.xy, cb0[15].yyyy, r0.zwzz, r2.xyxx
    float stage7_x = cb0[15].y * r0.z + r2.x;
    float stage7_y = cb0[15].y * r0.w + r2.y;
    r1.x = stage7_x;
    r1.y = stage7_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xyz, r1.xyxx, t2.xyzw, s4, cb0[2].x
    float4 fetch8 = t2.SampleLevel(s4, r1.xy, cb0[2].x);
    r1.x = fetch8.x;
    r1.y = fetch8.y;
    r1.z = fetch8.z;
    // mul r1.xyzw, r1.xxyz, cb0[15].zzzz
    float stage9_x = r1.x * cb0[15].z;
    float stage9_y = r1.x * cb0[15].z;
    float stage9_z = r1.y * cb0[15].z;
    float stage9_w = r1.z * cb0[15].z;
    r1.x = stage9_x;
    r1.y = stage9_y;
    r1.z = stage9_z;
    r1.w = stage9_w;
    // mad r2.x, v4.x, cb0[17].z, cb0[17].w
    r2.x = v4.x * cb0[17].z + cb0[17].w;
    // add r2.x, r2.x, cb0[18].x
    r2.x = r2.x + cb0[18].x;
    // mad r2.x, cb0[18].y, r0.y, r2.x
    r2.x = cb0[18].y * r0.y + r2.x;
    // mad r2.y, v4.x, cb0[16].x, cb0[16].y
    r2.y = v4.x * cb0[16].x + cb0[16].y;
    // add r2.y, r2.y, cb0[16].w
    r2.y = r2.y + cb0[16].w;
    // mad r2.y, r0.x, cb0[15].w, r2.y
    r2.y = r0.x * cb0[15].w + r2.y;
    // mad r3.x, cb0[17].x, r2.y, r2.x
    r3.x = cb0[17].x * r2.y + r2.x;
    // mad r3.y, r2.x, cb0[18].z, r2.y
    r3.y = r2.x * cb0[18].z + r2.y;
    // mad r2.xy, cb0[18].wwww, r0.zwzz, r3.xyxx
    float stage10_x = cb0[18].w * r0.z + r3.x;
    float stage10_y = cb0[18].w * r0.w + r3.y;
    r2.x = stage10_x;
    r2.y = stage10_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r2.xyxx, t3.xyzw, s5, cb0[2].x
    float4 fetch11 = t3.SampleLevel(s5, r2.xy, cb0[2].x);
    r2.x = fetch11.x;
    r2.y = fetch11.y;
    r2.z = fetch11.z;
    // mul r2.xyzw, r2.xxyz, cb0[19].xxxx
    float stage12_x = r2.x * cb0[19].x;
    float stage12_y = r2.x * cb0[19].x;
    float stage12_z = r2.y * cb0[19].x;
    float stage12_w = r2.z * cb0[19].x;
    r2.x = stage12_x;
    r2.y = stage12_y;
    r2.z = stage12_z;
    r2.w = stage12_w;
    // mul r1.xyzw, r1.xyzw, r2.xyzw
    float stage13_x = r1.x * r2.x;
    float stage13_y = r1.y * r2.y;
    float stage13_z = r1.z * r2.z;
    float stage13_w = r1.w * r2.w;
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    r1.w = stage13_w;
    // mad r2.x, v4.x, cb0[21].x, cb0[21].y
    r2.x = v4.x * cb0[21].x + cb0[21].y;
    // mad r2.x, cb0[21].z, r0.y, r2.x
    r2.x = cb0[21].z * r0.y + r2.x;
    // add r2.x, r2.x, cb0[21].w
    r2.x = r2.x + cb0[21].w;
    // mad r2.y, v4.x, cb0[19].z, cb0[19].w
    r2.y = v4.x * cb0[19].z + cb0[19].w;
    // mad r2.y, r0.x, cb0[19].y, r2.y
    r2.y = r0.x * cb0[19].y + r2.y;
    // add r2.y, r2.y, cb0[20].y
    r2.y = r2.y + cb0[20].y;
    // mad r3.x, cb0[20].z, r2.y, r2.x
    r3.x = cb0[20].z * r2.y + r2.x;
    // mad r3.y, r2.x, cb0[22].x, r2.y
    r3.y = r2.x * cb0[22].x + r2.y;
    // mad r2.xy, cb0[22].yyyy, r0.zwzz, r3.xyxx
    float stage14_x = cb0[22].y * r0.z + r3.x;
    float stage14_y = cb0[22].y * r0.w + r3.y;
    r2.x = stage14_x;
    r2.y = stage14_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r2.xyxx, t4.xyzw, s6, cb0[2].x
    float4 fetch15 = t4.SampleLevel(s6, r2.xy, cb0[2].x);
    r2.x = fetch15.x;
    r2.y = fetch15.y;
    r2.z = fetch15.z;
    // mul r1.xyzw, r1.xyzw, r2.xxyz
    float stage16_x = r1.x * r2.x;
    float stage16_y = r1.y * r2.x;
    float stage16_z = r1.z * r2.y;
    float stage16_w = r1.w * r2.z;
    r1.x = stage16_x;
    r1.y = stage16_y;
    r1.z = stage16_z;
    r1.w = stage16_w;
    // max r1.xyzw, |r1.xyzw|, l(0.000001, 0.000001, 0.000001, 0.000001)
    float stage17_x = max(abs(r1.x), 1e-06f);
    float stage17_y = max(abs(r1.y), 1e-06f);
    float stage17_z = max(abs(r1.z), 1e-06f);
    float stage17_w = max(abs(r1.w), 1e-06f);
    r1.x = stage17_x;
    r1.y = stage17_y;
    r1.z = stage17_z;
    r1.w = stage17_w;
    // log r1.xyzw, r1.xyzw
    float stage18_x = log2(r1.x);
    float stage18_y = log2(r1.y);
    float stage18_z = log2(r1.z);
    float stage18_w = log2(r1.w);
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    r1.w = stage18_w;
    // mul r2.x, v4.w, cb0[22].z
    r2.x = v4.w * cb0[22].z;
    // mul r1.xyzw, r1.xyzw, r2.xxxx
    float stage19_x = r1.x * r2.x;
    float stage19_y = r1.y * r2.x;
    float stage19_z = r1.z * r2.x;
    float stage19_w = r1.w * r2.x;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    r1.w = stage19_w;
    // exp r1.xyzw, r1.xyzw
    float stage20_x = exp2(r1.x);
    float stage20_y = exp2(r1.y);
    float stage20_z = exp2(r1.z);
    float stage20_w = exp2(r1.w);
    r1.x = stage20_x;
    r1.y = stage20_y;
    r1.z = stage20_z;
    r1.w = stage20_w;
    // mul_sat r1.xyzw, r1.xyzw, cb0[22].wwww
    float stage21_x = saturate(r1.x * cb0[22].w);
    float stage21_y = saturate(r1.y * cb0[22].w);
    float stage21_z = saturate(r1.z * cb0[22].w);
    float stage21_w = saturate(r1.w * cb0[22].w);
    r1.x = stage21_x;
    r1.y = stage21_y;
    r1.z = stage21_z;
    r1.w = stage21_w;
    // mad r2.x, v4.x, cb0[4].y, cb0[4].z
    r2.x = v4.x * cb0[4].y + cb0[4].z;
    // mad r0.y, cb0[4].w, r0.y, r2.x
    r0.y = cb0[4].w * r0.y + r2.x;
    // add r0.y, r0.y, cb0[5].x
    r0.y = r0.y + cb0[5].x;
    // mad r2.x, v4.x, cb0[2].z, cb0[2].w
    r2.x = v4.x * cb0[2].z + cb0[2].w;
    // mad r0.x, r0.x, cb0[2].y, r2.x
    r0.x = r0.x * cb0[2].y + r2.x;
    // add r0.x, r0.x, cb0[3].z
    r0.x = r0.x + cb0[3].z;
    // mad r2.x, cb0[3].w, r0.x, r0.y
    r2.x = cb0[3].w * r0.x + r0.y;
    // mad r2.y, r0.y, cb0[5].y, r0.x
    r2.y = r0.y * cb0[5].y + r0.x;
    // mad r0.xy, cb0[12].xxxx, r0.zwzz, r2.xyxx
    float stage22_x = cb0[12].x * r0.z + r2.x;
    float stage22_y = cb0[12].x * r0.w + r2.y;
    r0.x = stage22_x;
    r0.y = stage22_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t6.xyzw, s1, cb0[2].x
    float4 fetch23 = t6.SampleLevel(s1, r0.xy, cb0[2].x);
    r0.x = fetch23.x;
    r0.y = fetch23.y;
    r0.z = fetch23.z;
    // mul r2.xyz, r1.yzwy, r0.xyzx
    float stage24_x = r1.y * r0.x;
    float stage24_y = r1.z * r0.y;
    float stage24_z = r1.w * r0.z;
    r2.x = stage24_x;
    r2.y = stage24_y;
    r2.z = stage24_z;
    // dp3 r0.w, r2.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r2.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r0.xyz, -r0.xyzx, r1.yzwy, r0.wwww
    float stage25_x = -r0.x * r1.y + r0.w;
    float stage25_y = -r0.y * r1.z + r0.w;
    float stage25_z = -r0.z * r1.w + r0.w;
    r0.x = stage25_x;
    r0.y = stage25_y;
    r0.z = stage25_z;
    // mad r0.xyz, cb0[23].xxxx, r0.xyzx, r2.xyzx
    float stage26_x = cb0[23].x * r0.x + r2.x;
    float stage26_y = cb0[23].x * r0.y + r2.y;
    float stage26_z = cb0[23].x * r0.z + r2.z;
    r0.x = stage26_x;
    r0.y = stage26_y;
    r0.z = stage26_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage27_x = max(abs(r0.x), 1e-06f);
    float stage27_y = max(abs(r0.y), 1e-06f);
    float stage27_z = max(abs(r0.z), 1e-06f);
    r0.x = stage27_x;
    r0.y = stage27_y;
    r0.z = stage27_z;
    // log r0.xyz, r0.xyzx
    float stage28_x = log2(r0.x);
    float stage28_y = log2(r0.y);
    float stage28_z = log2(r0.z);
    r0.x = stage28_x;
    r0.y = stage28_y;
    r0.z = stage28_z;
    // mul r0.xyz, r0.xyzx, cb0[23].yyyy
    float stage29_x = r0.x * cb0[23].y;
    float stage29_y = r0.y * cb0[23].y;
    float stage29_z = r0.z * cb0[23].y;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // exp r0.xyz, r0.xyzx
    float stage30_x = exp2(r0.x);
    float stage30_y = exp2(r0.y);
    float stage30_z = exp2(r0.z);
    r0.x = stage30_x;
    r0.y = stage30_y;
    r0.z = stage30_z;
    // mul r0.xyz, r0.xyzx, cb0[23].zzzz
    float stage31_x = r0.x * cb0[23].z;
    float stage31_y = r0.y * cb0[23].z;
    float stage31_z = r0.z * cb0[23].z;
    r0.x = stage31_x;
    r0.y = stage31_y;
    r0.z = stage31_z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage32_x = r0.x * v3.x + cb0[1].x;
    float stage32_y = r0.y * v3.y + cb0[1].y;
    float stage32_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage32_x;
    r0.y = stage32_y;
    r0.z = stage32_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage33_x = r0.x * v5.w + v5.x;
    float stage33_y = r0.y * v5.w + v5.y;
    float stage33_z = r0.z * v5.w + v5.z;
    o0.x = stage33_x;
    o0.y = stage33_y;
    o0.z = stage33_z;
    // div r0.xy, v7.xyxx, v7.wwww
    float stage34_x = v7.x / v7.w;
    float stage34_y = v7.y / v7.w;
    r0.x = stage34_x;
    r0.y = stage34_y;
    // mad r0.xy, r0.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage35_x = r0.x * cb2[0].x + cb2[0].w;
    float stage35_y = r0.y * cb2[0].y + cb2[0].z;
    r0.x = stage35_x;
    r0.y = stage35_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t5.xyzw, s0, l(0.000000)
    float4 fetch36 = t5.SampleLevel(s0, r0.xy, 0.0f);
    r0.x = fetch36.x;
    // min r0.x, r0.x, l(0.999000)
    r0.x = min(r0.x, 0.999f);
    // mad r0.y, r0.x, cb2[1].z, -cb2[1].w
    r0.y = r0.x * cb2[1].z + -cb2[1].w;
    // mad r0.x, r0.x, cb2[1].x, cb2[1].y
    r0.x = r0.x * cb2[1].x + cb2[1].y;
    // div r0.y, l(1.000000, 1.000000, 1.000000, 1.000000), r0.y
    r0.y = 1.0f / r0.y;
    // add r0.x, r0.y, r0.x
    r0.x = r0.y + r0.x;
    // add r0.x, r0.x, -v7.w
    r0.x = r0.x + -v7.w;
    // add r0.y, -cb0[23].w, l(1.000000)
    r0.y = -cb0[23].w + 1.0f;
    // max r0.y, r0.y, l(0.001000)
    r0.y = max(r0.y, 0.001f);
    // div_sat r0.x, r0.x, r0.y
    r0.x = saturate(r0.x / r0.y);
    // mul r0.x, r0.x, r1.x
    r0.x = r0.x * r1.x;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage37_x = r0.x * v1.x;
    float stage37_y = r0.x * v1.y;
    float stage37_z = r0.x * v1.z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    r0.z = stage37_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage38_x = r0.w * v0.z;
    float stage38_y = r0.w * v0.x;
    float stage38_z = r0.w * v0.y;
    r1.x = stage38_x;
    r1.y = stage38_y;
    r1.z = stage38_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage39_x = r0.x * r1.x;
    float stage39_y = r0.x * r1.y;
    float stage39_z = r0.x * r1.z;
    r0.x = stage39_x;
    r0.y = stage39_y;
    r0.z = stage39_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage40_x = r0.x / r0.w;
    float stage40_y = r0.y / r0.w;
    r0.x = stage40_x;
    r0.y = stage40_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage41_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage41_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage41_x;
    r1.y = stage41_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage42_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage42_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage42_x;
    r1.y = stage42_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage43_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage43_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage43_x;
    r1.y = stage43_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage44_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage44_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage44_x;
    r0.y = stage44_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage45_x = r0.x * 0.5f + 0.5f;
    float stage45_y = r0.y * 0.5f + 0.5f;
    o2.x = stage45_x;
    o2.y = stage45_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage46_z = 1.0f;
    float stage46_w = asfloat(0u);
    o2.z = stage46_z;
    o2.w = stage46_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage47_x = cb2[3].x;
    float stage47_y = cb2[3].y;
    float stage47_z = cb2[3].z;
    o3.x = stage47_x;
    o3.y = stage47_y;
    o3.z = stage47_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage48_x = asfloat(0u);
    float stage48_y = asfloat(0u);
    float stage48_z = asfloat(0u);
    float stage48_w = asfloat(0u);
    o4.x = stage48_x;
    o4.y = stage48_y;
    o4.z = stage48_z;
    o4.w = stage48_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage49_x = asfloat(0u);
    float stage49_y = asfloat(0u);
    float stage49_z = asfloat(0u);
    float stage49_w = asfloat(0u);
    o5.x = stage49_x;
    o5.y = stage49_y;
    o5.z = stage49_z;
    o5.w = stage49_w;

    Shade_Ue3_fx_m_baseringmaster_01_tr_3281da9c74ca96a7592441a6df2a8235210a228fb76d6eb613ae8683b60e2712_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
