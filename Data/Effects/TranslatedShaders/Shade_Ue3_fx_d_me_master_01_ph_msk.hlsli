// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_me_master_01_ph_msk_Constants0 : register(b0)
{
    float4 cb0[12];
};
cbuffer Shade_Ue3_fx_d_me_master_01_ph_msk_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
SamplerState s2 : register(s2);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);
Texture2D t2 : register(t2);

struct Shade_Ue3_fx_d_me_master_01_ph_msk_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
    float4 v6;
    uint4 vCoverage;
};

struct Shade_Ue3_fx_d_me_master_01_ph_msk_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
    uint oMask;
};

Shade_Ue3_fx_d_me_master_01_ph_msk_OUTPUT Shade_Ue3_fx_d_me_master_01_ph_msk(Shade_Ue3_fx_d_me_master_01_ph_msk_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    uint4 vCoverage = stage.vCoverage;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;
    uint oMask = 0xffffffffu;

    // add r0.xy, v6.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage1_x = v6.x + 0.5f;
    float stage1_y = v6.y + 0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // frc r0.xy, r0.xyxx
    float stage2_x = frac(r0.x);
    float stage2_y = frac(r0.y);
    r0.x = stage2_x;
    r0.y = stage2_y;
    // mov_sat r0.z, cb0[0].w
    r0.z = saturate(cb0[0].w);
    // add r0.xy, r0.zzzz, r0.xyxx
    float stage3_x = r0.z + r0.x;
    float stage3_y = r0.z + r0.y;
    r0.x = stage3_x;
    r0.y = stage3_y;
    // round_ni r0.xy, r0.xyxx
    float stage4_x = floor(r0.x);
    float stage4_y = floor(r0.y);
    r0.x = stage4_x;
    r0.y = stage4_y;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // frc r0.yw, v6.xxxy
    float stage5_y = frac(v6.x);
    float stage5_w = frac(v6.y);
    r0.y = stage5_y;
    r0.w = stage5_w;
    // add r0.yz, r0.zzzz, r0.yywy
    float stage6_y = r0.z + r0.y;
    float stage6_z = r0.z + r0.w;
    r0.y = stage6_y;
    r0.z = stage6_z;
    // round_ni r0.yz, r0.yyzy
    float stage7_y = floor(r0.y);
    float stage7_z = floor(r0.z);
    r0.y = stage7_y;
    r0.z = stage7_z;
    // mad r0.x, r0.y, r0.z, r0.x
    r0.x = r0.y * r0.z + r0.x;
    // min r0.x, r0.x, l(1.000000)
    r0.x = min(r0.x, 1.0f);
    // add r0.x, r0.x, l(-0.166000)
    r0.x = r0.x + -0.166f;
    // lt r0.x, r0.x, l(0.000000)
    r0.x = asfloat((r0.x < 0.0f) ? 0xffffffffu : 0u);
    // discard_nz r0.x
    if (asuint(r0.x) != 0u) discard;
    // mov oMask, vCoverage.x
    oMask = asuint(vCoverage.x);
    // dp3 r0.x, v5.xyzx, v5.xyzx
    r0.x = dot(v5.xyz, v5.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.x, r0.x, v5.z
    r0.x = r0.x * v5.z;
    // add r0.x, -|r0.x|, l(1.000000)
    r0.x = -(abs(r0.x)) + 1.0f;
    // add r0.yz, v4.xxyx, l(0.000000, -0.500000, -0.500000, 0.000000)
    float stage8_y = v4.x + -0.5f;
    float stage8_z = v4.y + -0.5f;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // mad r0.yz, r0.yyzy, cb0[3].xxyx, l(0.000000, 0.500000, 0.500000, 0.000000)
    float stage9_y = r0.y * cb0[3].x + 0.5f;
    float stage9_z = r0.z * cb0[3].y + 0.5f;
    r0.y = stage9_y;
    r0.z = stage9_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t1.xyzw, s1, l(0.000000)
    float4 fetch10 = t1.SampleBias(s1, r0.yz, 0.0f);
    r0.y = fetch10.y;
    // mul r0.x, r0.y, r0.x
    r0.x = r0.y * r0.x;
    // log r0.y, |r0.x|
    r0.y = log2(abs(r0.x));
    // lt r0.x, |r0.x|, l(0.000001)
    r0.x = asfloat((abs(r0.x) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[10].x
    r0.y = r0.y * cb0[10].x;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mul r0.yz, v4.xxyx, cb0[6].xxyx
    float stage11_y = v4.x * cb0[6].x;
    float stage11_z = v4.y * cb0[6].y;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.yzw, r0.yzyy, t2.wxyz, s2, l(0.000000)
    float4 fetch12 = t2.SampleBias(s2, r0.yz, 0.0f);
    r0.y = fetch12.x;
    r0.z = fetch12.y;
    r0.w = fetch12.z;
    // dp3 r1.x, r0.yzwy, l(0.300000, 0.590000, 0.110000, 0.000000)
    r1.x = dot(r0.yzw, float3(0.3f, 0.59f, 0.11f));
    // add r1.xyz, -r0.yzwy, r1.xxxx
    float stage13_x = -r0.y + r1.x;
    float stage13_y = -r0.z + r1.x;
    float stage13_z = -r0.w + r1.x;
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    // mad r0.yzw, cb0[10].wwww, r1.xxyz, r0.yyzw
    float stage14_y = cb0[10].w * r1.x + r0.y;
    float stage14_z = cb0[10].w * r1.y + r0.z;
    float stage14_w = cb0[10].w * r1.z + r0.w;
    r0.y = stage14_y;
    r0.z = stage14_z;
    r0.w = stage14_w;
    // max r0.yzw, |r0.yyzw|, l(0.000000, 0.000001, 0.000001, 0.000001)
    float stage15_y = max(abs(r0.y), 1e-06f);
    float stage15_z = max(abs(r0.z), 1e-06f);
    float stage15_w = max(abs(r0.w), 1e-06f);
    r0.y = stage15_y;
    r0.z = stage15_z;
    r0.w = stage15_w;
    // log r0.yzw, r0.yyzw
    float stage16_y = log2(r0.y);
    float stage16_z = log2(r0.z);
    float stage16_w = log2(r0.w);
    r0.y = stage16_y;
    r0.z = stage16_z;
    r0.w = stage16_w;
    // mul r0.yzw, r0.yyzw, cb0[11].xxxx
    float stage17_y = r0.y * cb0[11].x;
    float stage17_z = r0.z * cb0[11].x;
    float stage17_w = r0.w * cb0[11].x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    r0.w = stage17_w;
    // exp r0.yzw, r0.yyzw
    float stage18_y = exp2(r0.y);
    float stage18_z = exp2(r0.z);
    float stage18_w = exp2(r0.w);
    r0.y = stage18_y;
    r0.z = stage18_z;
    r0.w = stage18_w;
    // mul r1.xyz, cb0[7].xyzx, cb0[7].wwww
    float stage19_x = cb0[7].x * cb0[7].w;
    float stage19_y = cb0[7].y * cb0[7].w;
    float stage19_z = cb0[7].z * cb0[7].w;
    r1.x = stage19_x;
    r1.y = stage19_y;
    r1.z = stage19_z;
    // mul r0.yzw, r0.yyzw, r1.xxyz
    float stage20_y = r0.y * r1.x;
    float stage20_z = r0.z * r1.y;
    float stage20_w = r0.w * r1.z;
    r0.y = stage20_y;
    r0.z = stage20_z;
    r0.w = stage20_w;
    // mul r1.xy, v4.xyxx, cb0[8].zwzz
    float stage21_x = v4.x * cb0[8].z;
    float stage21_y = v4.y * cb0[8].w;
    r1.x = stage21_x;
    r1.y = stage21_y;
    // mad r2.x, cb0[8].y, cb0[8].x, r1.x
    r2.x = cb0[8].y * cb0[8].x + r1.x;
    // mad r2.y, cb0[8].y, cb0[9].x, r1.y
    r2.y = cb0[8].y * cb0[9].x + r1.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r1.xy, r2.xyxx, t0.xyzw, s0, l(0.000000)
    float4 fetch22 = t0.SampleBias(s0, r2.xy, 0.0f);
    r1.x = fetch22.x;
    r1.y = fetch22.y;
    // mad r1.xy, r1.xyxx, l(2.000000, 2.000000, 0.000000, 0.000000), l(-1.000000, -1.000000, 0.000000, 0.000000)
    float stage23_x = r1.x * 2.0f + -1.0f;
    float stage23_y = r1.y * 2.0f + -1.0f;
    r1.x = stage23_x;
    r1.y = stage23_y;
    // dp2 r1.w, r1.xyxx, r1.xyxx
    r1.w = dot(r1.xy, r1.xy);
    // add r1.w, -r1.w, l(1.000000)
    r1.w = -r1.w + 1.0f;
    // max r1.w, r1.w, l(0.000000)
    r1.w = max(r1.w, 0.0f);
    // sqrt r1.w, r1.w
    r1.w = sqrt(r1.w);
    // add r1.z, r1.w, l(0.000010)
    r1.z = r1.w + 1e-05f;
    // mul r1.xyz, r1.xyzx, cb0[2].xyzx
    float stage24_x = r1.x * cb0[2].x;
    float stage24_y = r1.y * cb0[2].y;
    float stage24_z = r1.z * cb0[2].z;
    r1.x = stage24_x;
    r1.y = stage24_y;
    r1.z = stage24_z;
    // dp3 r1.w, v1.xyzx, v1.xyzx
    r1.w = dot(v1.xyz, v1.xyz);
    // rsq r1.w, r1.w
    r1.w = rsqrt(r1.w);
    // mad r2.x, v1.z, r1.w, l(1.000000)
    r2.x = v1.z * r1.w + 1.0f;
    // mul r2.yzw, r1.wwww, v1.xxyz
    float stage25_y = r1.w * v1.x;
    float stage25_z = r1.w * v1.y;
    float stage25_w = r1.w * v1.z;
    r2.y = stage25_y;
    r2.z = stage25_z;
    r2.w = stage25_w;
    // mul_sat r1.w, r2.x, l(0.500000)
    r1.w = saturate(r2.x * 0.5f);
    // mad r1.w, r1.w, l(0.950000), l(0.050000)
    r1.w = r1.w * 0.95f + 0.05f;
    // dp3 r1.w, r1.xyzx, r1.wwww
    r1.w = dot(r1.xyz, r1.www);
    // log r2.x, |r1.w|
    r2.x = log2(abs(r1.w));
    // lt r1.w, |r1.w|, l(0.000001)
    r1.w = asfloat((abs(r1.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r2.x, r2.x, cb0[11].y
    r2.x = r2.x * cb0[11].y;
    // exp r2.x, r2.x
    r2.x = exp2(r2.x);
    // mul r2.x, r2.x, cb0[11].z
    r2.x = r2.x * cb0[11].z;
    // movc r1.w, r1.w, l(0), r2.x
    r1.w = asuint(r1.w) != 0u ? asfloat(0u) : r2.x;
    // mul r0.yzw, r0.yyzw, r1.wwww
    float stage26_y = r0.y * r1.w;
    float stage26_z = r0.z * r1.w;
    float stage26_w = r0.w * r1.w;
    r0.y = stage26_y;
    r0.z = stage26_z;
    r0.w = stage26_w;
    // mul r3.xyz, cb0[4].xyzx, cb0[4].wwww
    float stage27_x = cb0[4].x * cb0[4].w;
    float stage27_y = cb0[4].y * cb0[4].w;
    float stage27_z = cb0[4].z * cb0[4].w;
    r3.x = stage27_x;
    r3.y = stage27_y;
    r3.z = stage27_z;
    // mul r3.xyz, r3.xyzx, cb0[5].xxxx
    float stage28_x = r3.x * cb0[5].x;
    float stage28_y = r3.y * cb0[5].x;
    float stage28_z = r3.z * cb0[5].x;
    r3.x = stage28_x;
    r3.y = stage28_y;
    r3.z = stage28_z;
    // mad r0.xyz, r0.xxxx, r3.xyzx, r0.yzwy
    float stage29_x = r0.x * r3.x + r0.y;
    float stage29_y = r0.x * r3.y + r0.z;
    float stage29_z = r0.x * r3.z + r0.w;
    r0.x = stage29_x;
    r0.y = stage29_y;
    r0.z = stage29_z;
    // mad o0.xyz, r0.xyzx, cb0[0].xyzx, cb0[1].xyzx
    float stage30_x = r0.x * cb0[0].x + cb0[1].x;
    float stage30_y = r0.y * cb0[0].y + cb0[1].y;
    float stage30_z = r0.z * cb0[0].z + cb0[1].z;
    o0.x = stage30_x;
    o0.y = stage30_y;
    o0.z = stage30_z;
    // mov o0.w, l(0)
    o0.w = asfloat(0u);
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage31_x = r0.x * r1.x;
    float stage31_y = r0.x * r1.y;
    float stage31_z = r0.x * r1.z;
    r0.x = stage31_x;
    r0.y = stage31_y;
    r0.z = stage31_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.xyzx
    float stage32_x = r0.w * v0.x;
    float stage32_y = r0.w * v0.y;
    float stage32_z = r0.w * v0.z;
    r1.x = stage32_x;
    r1.y = stage32_y;
    r1.z = stage32_z;
    // mul r3.xyz, r1.yzxy, r2.wyzw
    float stage33_x = r1.y * r2.w;
    float stage33_y = r1.z * r2.y;
    float stage33_z = r1.x * r2.z;
    r3.x = stage33_x;
    r3.y = stage33_y;
    r3.z = stage33_z;
    // mad r3.xyz, r2.zwyz, r1.zxyz, -r3.xyzx
    float stage34_x = r2.z * r1.z + -r3.x;
    float stage34_y = r2.w * r1.x + -r3.y;
    float stage34_z = r2.y * r1.y + -r3.z;
    r3.x = stage34_x;
    r3.y = stage34_y;
    r3.z = stage34_z;
    // dp3 r2.z, r2.yzwy, r0.xyzx
    r2.z = dot(r2.yzw, r0.xyz);
    // dp3 r2.x, r1.xyzx, r0.xyzx
    r2.x = dot(r1.xyz, r0.xyz);
    // mul r1.xyz, r3.xyzx, v1.wwww
    float stage35_x = r3.x * v1.w;
    float stage35_y = r3.y * v1.w;
    float stage35_z = r3.z * v1.w;
    r1.x = stage35_x;
    r1.y = stage35_y;
    r1.z = stage35_z;
    // dp3 r2.y, r1.xyzx, r0.xyzx
    r2.y = dot(r1.xyz, r0.xyz);
    // dp3 r0.x, r2.xyzx, r2.xyzx
    r0.x = dot(r2.xyz, r2.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r2.xyzx
    float stage36_x = r0.x * r2.x;
    float stage36_y = r0.x * r2.y;
    float stage36_z = r0.x * r2.z;
    r0.x = stage36_x;
    r0.y = stage36_y;
    r0.z = stage36_z;
    // ge r0.w, l(0.000000), r0.z
    r0.w = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // dp3 r0.z, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.z = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.zzzz
    float stage37_x = r0.x / r0.z;
    float stage37_y = r0.y / r0.z;
    r0.x = stage37_x;
    r0.y = stage37_y;
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage38_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage38_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage38_x;
    r1.y = stage38_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage39_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage39_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage39_x;
    r1.y = stage39_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage40_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage40_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage40_x;
    r1.y = stage40_y;
    // movc r0.xy, r0.wwww, r1.xyxx, r0.xyxx
    float stage41_x = asuint(r0.w) != 0u ? r1.x : r0.x;
    float stage41_y = asuint(r0.w) != 0u ? r1.y : r0.y;
    r0.x = stage41_x;
    r0.y = stage41_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage42_x = r0.x * 0.5f + 0.5f;
    float stage42_y = r0.y * 0.5f + 0.5f;
    o2.x = stage42_x;
    o2.y = stage42_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage43_z = 1.0f;
    float stage43_w = asfloat(0u);
    o2.z = stage43_z;
    o2.w = stage43_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage44_x = cb2[3].x;
    float stage44_y = cb2[3].y;
    float stage44_z = cb2[3].z;
    o3.x = stage44_x;
    o3.y = stage44_y;
    o3.z = stage44_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage45_x = asfloat(0u);
    float stage45_y = asfloat(0u);
    float stage45_z = asfloat(0u);
    float stage45_w = asfloat(0u);
    o4.x = stage45_x;
    o4.y = stage45_y;
    o4.z = stage45_z;
    o4.w = stage45_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage46_x = asfloat(0u);
    float stage46_y = asfloat(0u);
    float stage46_z = asfloat(0u);
    float stage46_w = asfloat(0u);
    o5.x = stage46_x;
    o5.y = stage46_y;
    o5.z = stage46_z;
    o5.w = stage46_w;

    Shade_Ue3_fx_d_me_master_01_ph_msk_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    result.oMask = oMask;
    return result;
}
