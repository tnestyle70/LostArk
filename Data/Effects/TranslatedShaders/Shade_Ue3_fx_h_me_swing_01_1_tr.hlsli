// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_h_me_swing_01_1_tr_Constants0 : register(b0)
{
    float4 cb0[5];
};
cbuffer Shade_Ue3_fx_h_me_swing_01_1_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_h_me_swing_01_1_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_h_me_swing_01_1_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_h_me_swing_01_1_tr_OUTPUT Shade_Ue3_fx_h_me_swing_01_1_tr(Shade_Ue3_fx_h_me_swing_01_1_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xyzw, v4.xyyx, l(-0.020000, -0.100000, -1.000000, -0.050000)
    float stage1_x = v4.x + -0.02f;
    float stage1_y = v4.y + -0.1f;
    float stage1_z = v4.y + -1.0f;
    float stage1_w = v4.x + -0.05f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    r0.z = stage1_z;
    r0.w = stage1_w;
    // log r1.x, |r0.y|
    r1.x = log2(abs(r0.y));
    // mul r1.x, r1.x, cb0[4].x
    r1.x = r1.x * cb0[4].x;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mad_sat r1.y, -r0.z, l(20.000000), l(1.000000)
    r1.y = saturate(-r0.z * 20.0f + 1.0f);
    // mul r1.x, r1.y, r1.x
    r1.x = r1.y * r1.x;
    // mul r1.x, r1.x, l(3.000000)
    r1.x = r1.x * 3.0f;
    // min r1.x, r1.x, l(1.000000)
    r1.x = min(r1.x, 1.0f);
    // lt r0.y, |r0.y|, l(0.000001)
    r0.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // movc r0.y, r0.y, l(0), r1.x
    r0.y = asuint(r0.y) != 0u ? asfloat(0u) : r1.x;
    // mul r0.xz, r0.xxzx, l(10.000000, 0.000000, 10.000000, 0.000000)
    float stage2_x = r0.x * 10.0f;
    float stage2_z = r0.z * 10.0f;
    r0.x = stage2_x;
    r0.z = stage2_z;
    // mul r1.x, |r0.z|, |r0.z|
    r1.x = abs(r0.z) * abs(r0.z);
    // min r1.x, r1.x, l(1.000000)
    r1.x = min(r1.x, 1.0f);
    // lt r0.z, |r0.z|, l(0.000001)
    r0.z = asfloat((abs(r0.z) < 1e-06f) ? 0xffffffffu : 0u);
    // add r0.xw, -|r0.xxxw|, l(1.000000, 0.000000, 0.000000, 1.000000)
    float stage3_x = -(abs(r0.x)) + 1.0f;
    float stage3_w = -(abs(r0.w)) + 1.0f;
    r0.x = stage3_x;
    r0.w = stage3_w;
    // max r0.x, r0.x, l(0.000000)
    r0.x = max(r0.x, 0.0f);
    // movc r0.z, r0.z, l(0), r1.x
    r0.z = asuint(r0.z) != 0u ? asfloat(0u) : r1.x;
    // mul r1.x, |r0.w|, |r0.w|
    r1.x = abs(r0.w) * abs(r0.w);
    // mul r1.x, |r0.w|, r1.x
    r1.x = abs(r0.w) * r1.x;
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.yzw, v4.xxxy, l(0.000000, 10.000000, 1.500000, 1.000000)
    float stage4_y = v4.x * 10.0f;
    float stage4_z = v4.x * 1.5f;
    float stage4_w = v4.y * 1.0f;
    r1.y = stage4_y;
    r1.z = stage4_z;
    r1.w = stage4_w;
    // mov_sat r1.y, r1.y
    r1.y = saturate(r1.y);
    // mul r1.x, r1.y, r1.x
    r1.x = r1.y * r1.x;
    // mul r1.x, r1.x, l(3.000000)
    r1.x = r1.x * 3.0f;
    // mul r0.z, r0.z, r1.x
    r0.z = r0.z * r1.x;
    // movc r0.z, r0.w, l(0), r0.z
    r0.z = asuint(r0.w) != 0u ? asfloat(0u) : r0.z;
    // mul r0.w, r0.y, r0.z
    r0.w = r0.y * r0.z;
    // min r0.w, r0.w, l(1.000000)
    r0.w = min(r0.w, 1.0f);
    // log r1.x, r0.x
    r1.x = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, l(15.000000)
    r1.x = r1.x * 15.0f;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r1.x, r1.x, cb0[4].y
    r1.x = r1.x * cb0[4].y;
    // mul r0.y, r0.y, r1.x
    r0.y = r0.y * r1.x;
    // movc r0.x, r0.x, l(0), r0.y
    r0.x = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mad r0.x, r0.x, r0.z, r0.w
    r0.x = r0.x * r0.z + r0.w;
    // mad r0.yz, v4.xxyx, l(0.000000, 0.500000, 0.300000, 0.000000), cb0[3].xxyx
    float stage5_y = v4.x * 0.5f + cb0[3].x;
    float stage5_z = v4.y * 0.3f + cb0[3].y;
    r0.y = stage5_y;
    r0.z = stage5_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t0.yxzw, s0, l(0.000000)
    float4 fetch6 = t0.SampleBias(s0, r0.yz, 0.0f);
    r0.y = fetch6.x;
    // mad r0.yz, r0.yyyy, l(0.000000, 0.100000, 0.100000, 0.000000), r1.zzwz
    float stage7_y = r0.y * 0.1f + r1.z;
    float stage7_z = r0.y * 0.1f + r1.w;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // add r0.yz, r0.yyzy, l(0.000000, 0.000000, 0.050000, 0.000000)
    float stage8_y = r0.y + 0.0f;
    float stage8_z = r0.z + 0.05f;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t1.yxzw, s1, l(0.000000)
    float4 fetch9 = t1.SampleBias(s1, r0.yz, 0.0f);
    r0.y = fetch9.x;
    // mul r0.y, r0.w, r0.y
    r0.y = r0.w * r0.y;
    // mad r0.y, r0.y, l(100.000000), r0.x
    r0.y = r0.y * 100.0f + r0.x;
    // mul r0.x, r0.x, cb0[1].w
    r0.x = r0.x * cb0[1].w;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // mad r0.xyz, r0.yyyy, cb0[1].xyzx, cb0[2].xyzx
    float stage10_x = r0.y * cb0[1].x + cb0[2].x;
    float stage10_y = r0.y * cb0[1].y + cb0[2].y;
    float stage10_z = r0.y * cb0[1].z + cb0[2].z;
    r0.x = stage10_x;
    r0.y = stage10_y;
    r0.z = stage10_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage11_x = r0.x * v5.w + v5.x;
    float stage11_y = r0.y * v5.w + v5.y;
    float stage11_z = r0.z * v5.w + v5.z;
    o0.x = stage11_x;
    o0.y = stage11_y;
    o0.z = stage11_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage12_x = r0.x * v1.x;
    float stage12_y = r0.x * v1.y;
    float stage12_z = r0.x * v1.z;
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage13_x = r0.w * v0.z;
    float stage13_y = r0.w * v0.x;
    float stage13_z = r0.w * v0.y;
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage14_x = r0.x * r1.x;
    float stage14_y = r0.x * r1.y;
    float stage14_z = r0.x * r1.z;
    r0.x = stage14_x;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage15_x = r0.x / r0.w;
    float stage15_y = r0.y / r0.w;
    r0.x = stage15_x;
    r0.y = stage15_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage16_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage16_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage16_x;
    r1.y = stage16_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage17_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage17_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage17_x;
    r1.y = stage17_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage18_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage18_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage18_x;
    r1.y = stage18_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage19_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage19_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage19_x;
    r0.y = stage19_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage20_x = r0.x * 0.5f + 0.5f;
    float stage20_y = r0.y * 0.5f + 0.5f;
    o2.x = stage20_x;
    o2.y = stage20_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage21_z = 1.0f;
    float stage21_w = asfloat(0u);
    o2.z = stage21_z;
    o2.w = stage21_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage22_x = cb2[3].x;
    float stage22_y = cb2[3].y;
    float stage22_z = cb2[3].z;
    o3.x = stage22_x;
    o3.y = stage22_y;
    o3.z = stage22_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage23_x = asfloat(0u);
    float stage23_y = asfloat(0u);
    float stage23_z = asfloat(0u);
    float stage23_w = asfloat(0u);
    o4.x = stage23_x;
    o4.y = stage23_y;
    o4.z = stage23_z;
    o4.w = stage23_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage24_x = asfloat(0u);
    float stage24_y = asfloat(0u);
    float stage24_z = asfloat(0u);
    float stage24_w = asfloat(0u);
    o5.x = stage24_x;
    o5.y = stage24_y;
    o5.z = stage24_z;
    o5.w = stage24_w;

    Shade_Ue3_fx_h_me_swing_01_1_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
