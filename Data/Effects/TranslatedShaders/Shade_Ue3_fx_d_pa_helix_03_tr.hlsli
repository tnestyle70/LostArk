// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_d_pa_helix_03_tr_Constants0 : register(b0)
{
    float4 cb0[9];
};
cbuffer Shade_Ue3_fx_d_pa_helix_03_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_d_pa_helix_03_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
    float4 v6;
};

struct Shade_Ue3_fx_d_pa_helix_03_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_d_pa_helix_03_tr_OUTPUT Shade_Ue3_fx_d_pa_helix_03_tr(Shade_Ue3_fx_d_pa_helix_03_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 v6 = stage.v6;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // mul_sat r0.x, v4.x, l(0.660000)
    r0.x = saturate(v4.x * 0.66f);
    // add r1.xyzw, v2.xyxy, l(-0.500000, -0.500000, -0.500000, -0.500000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    float stage1_z = v2.x + -0.5f;
    float stage1_w = v2.y + -0.5f;
    r1.x = stage1_x;
    r1.y = stage1_y;
    r1.z = stage1_z;
    r1.w = stage1_w;
    // dp2 r0.y, cb0[3].xyxx, r1.zwzz
    r0.y = dot(cb0[3].xy, r1.zw);
    // dp4 r0.z, cb0[2].xyxy, r1.xyzw
    r0.z = dot(cb0[2].xyxy, r1.xyzw);
    // add r0.y, r0.y, l(0.500000)
    r0.y = r0.y + 0.5f;
    // add r0.w, r0.y, v4.y
    r0.w = r0.y + v4.y;
    // mul r0.w, r0.w, cb0[5].z
    r0.w = r0.w * cb0[5].z;
    // mul r0.w, r0.w, l(6.283185)
    r0.w = r0.w * 6.283185f;
    // sincos r0.w, null, r0.w
    float stage2_w = r0.w;
    r0.w = sin(stage2_w);
    // mul r0.x, r0.w, r0.x
    r0.x = r0.w * r0.x;
    // mul r0.x, r0.x, l(0.250000)
    r0.x = r0.x * 0.25f;
    // add r0.w, -r0.y, l(1.000000)
    r0.w = -r0.y + 1.0f;
    // mul r1.x, r0.w, r0.y
    r1.x = r0.w * r0.y;
    // mul r0.x, r0.x, r1.x
    r0.x = r0.x * r1.x;
    // mad r0.x, r0.x, l(4.000000), r0.z
    r0.x = r0.x * 4.0f + r0.z;
    // mul r0.zw, r0.xxxw, r0.xxxw
    float stage3_z = r0.x * r0.x;
    float stage3_w = r0.w * r0.w;
    r0.z = stage3_z;
    r0.w = stage3_w;
    // log r1.x, |r0.y|
    r1.x = log2(abs(r0.y));
    // mul r1.x, r1.x, v4.w
    r1.x = r1.x * v4.w;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // lt r1.y, |r0.y|, l(0.000001)
    r1.y = asfloat((abs(r0.y) < 1e-06f) ? 0xffffffffu : 0u);
    // mad r0.y, r0.y, v4.z, l(-0.500000)
    r0.y = r0.y * v4.z + -0.5f;
    // movc r1.x, r1.y, l(0), r1.x
    r1.x = asuint(r1.y) != 0u ? asfloat(0u) : r1.x;
    // mul r1.y, r1.x, r1.x
    r1.y = r1.x * r1.x;
    // div r0.xz, r0.xxzx, r1.xxyx
    float stage4_x = r0.x / r1.x;
    float stage4_z = r0.z / r1.y;
    r0.x = stage4_x;
    r0.z = stage4_z;
    // min r0.z, r0.z, l(1.000000)
    r0.z = min(r0.z, 1.0f);
    // add r0.z, -r0.z, l(1.000000)
    r0.z = -r0.z + 1.0f;
    // mul r0.z, r1.x, r0.z
    r0.z = r1.x * r0.z;
    // mad_sat r0.x, r0.x, l(0.500000), l(0.500000)
    r0.x = saturate(r0.x * 0.5f + 0.5f);
    // sqrt r1.x, r0.z
    r1.x = sqrt(r0.z);
    // mul r0.z, r0.z, r0.w
    r0.z = r0.z * r0.w;
    // mul r0.w, r1.x, cb0[5].w
    r0.w = r1.x * cb0[5].w;
    // mad r1.xyzw, r0.wwww, l(0.050000, 0.050000, -0.050000, -0.050000), l(-0.025000, -0.025000, -0.025000, -0.025000)
    float stage5_x = r0.w * 0.05f + -0.025f;
    float stage5_y = r0.w * 0.05f + -0.025f;
    float stage5_z = r0.w * -0.05f + -0.025f;
    float stage5_w = r0.w * -0.05f + -0.025f;
    r1.x = stage5_x;
    r1.y = stage5_y;
    r1.z = stage5_z;
    r1.w = stage5_w;
    // mul r2.xyz, r0.xxxx, l(0.500000, 3.141593, -0.500000, 0.000000)
    float stage6_x = r0.x * 0.5f;
    float stage6_y = r0.x * 3.141593f;
    float stage6_z = r0.x * -0.5f;
    r2.x = stage6_x;
    r2.y = stage6_y;
    r2.z = stage6_z;
    // sincos null, r0.w, r2.y
    float stage7_w = r2.y;
    r0.w = cos(stage7_w);
    // mad r2.yz, r0.yyyy, l(0.000000, 2.000000, 2.000000, 0.000000), r2.xxzx
    float stage8_y = r0.y * 2.0f + r2.x;
    float stage8_z = r0.y * 2.0f + r2.z;
    r2.y = stage8_y;
    r2.z = stage8_z;
    // add r0.y, r0.w, l(1.000000)
    r0.y = r0.w + 1.0f;
    // mad r0.y, -r0.y, l(0.500000), l(1.000000)
    r0.y = -r0.y * 0.5f + 1.0f;
    // add r0.y, -r0.y, r0.x
    r0.y = -r0.y + r0.x;
    // mad r2.x, r0.y, l(0.050000), r0.x
    r2.x = r0.y * 0.05f + r0.x;
    // add r2.w, -r2.x, l(1.000000)
    r2.w = -r2.x + 1.0f;
    // dp3 r0.x, v6.xyzx, v6.xyzx
    r0.x = dot(v6.xyz, v6.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r3.xyzw, r0.xxxx, v6.xyxy
    float stage9_x = r0.x * v6.x;
    float stage9_y = r0.x * v6.y;
    float stage9_z = r0.x * v6.x;
    float stage9_w = r0.x * v6.y;
    r3.x = stage9_x;
    r3.y = stage9_y;
    r3.z = stage9_z;
    r3.w = stage9_w;
    // mad r0.xy, r1.zwzz, r3.zwzz, r2.wzww
    float stage10_x = r1.z * r3.z + r2.w;
    float stage10_y = r1.w * r3.w + r2.z;
    r0.x = stage10_x;
    r0.y = stage10_y;
    // mad r1.xy, r1.xyxx, r3.xyxx, r2.xyxx
    float stage11_x = r1.x * r3.x + r2.x;
    float stage11_y = r1.y * r3.y + r2.y;
    r1.x = stage11_x;
    r1.y = stage11_y;
    // mul r1.zw, r0.xxxy, cb0[4].zzzw
    float stage12_z = r0.x * cb0[4].z;
    float stage12_w = r0.y * cb0[4].w;
    r1.z = stage12_z;
    r1.w = stage12_w;
    // mul r0.xy, r0.xyxx, cb0[6].zwzz
    float stage13_x = r0.x * cb0[6].z;
    float stage13_y = r0.y * cb0[6].w;
    r0.x = stage13_x;
    r0.y = stage13_y;
    // mad r2.x, cb0[4].y, cb0[4].x, r1.z
    r2.x = cb0[4].y * cb0[4].x + r1.z;
    // mad r2.y, cb0[4].y, cb0[6].x, r1.w
    r2.y = cb0[4].y * cb0[6].x + r1.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r2.xyxx, t0.yzwx, s0, l(0.000000)
    float4 fetch14 = t0.SampleBias(s0, r2.xy, 0.0f);
    r0.w = fetch14.x;
    // mad r2.x, cb0[4].y, cb0[6].y, r0.x
    r2.x = cb0[4].y * cb0[6].y + r0.x;
    // mad r2.y, cb0[4].y, cb0[7].x, r0.y
    r2.y = cb0[4].y * cb0[7].x + r0.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r2.xyxx, t1.xyzw, s1, l(0.000000)
    float4 fetch15 = t1.SampleBias(s1, r2.xy, 0.0f);
    r0.x = fetch15.x;
    // mul r0.x, r0.x, r0.w
    r0.x = r0.x * r0.w;
    // mul r0.x, r0.x, cb0[7].y
    r0.x = r0.x * cb0[7].y;
    // mul r0.yw, r1.xxxy, cb0[4].zzzw
    float stage16_y = r1.x * cb0[4].z;
    float stage16_w = r1.y * cb0[4].w;
    r0.y = stage16_y;
    r0.w = stage16_w;
    // mul r1.xy, r1.xyxx, cb0[6].zwzz
    float stage17_x = r1.x * cb0[6].z;
    float stage17_y = r1.y * cb0[6].w;
    r1.x = stage17_x;
    r1.y = stage17_y;
    // mad r2.x, cb0[4].y, cb0[4].x, r0.y
    r2.x = cb0[4].y * cb0[4].x + r0.y;
    // mad r2.y, cb0[4].y, cb0[6].x, r0.w
    r2.y = cb0[4].y * cb0[6].x + r0.w;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r2.xyxx, t0.yxzw, s0, l(0.000000)
    float4 fetch18 = t0.SampleBias(s0, r2.xy, 0.0f);
    r0.y = fetch18.x;
    // mad r2.x, cb0[4].y, cb0[6].y, r1.x
    r2.x = cb0[4].y * cb0[6].y + r1.x;
    // mad r2.y, cb0[4].y, cb0[7].x, r1.y
    r2.y = cb0[4].y * cb0[7].x + r1.y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.w, r2.xyxx, t1.yzwx, s1, l(0.000000)
    float4 fetch19 = t1.SampleBias(s1, r2.xy, 0.0f);
    r0.w = fetch19.x;
    // mad r0.x, r0.y, r0.w, r0.x
    r0.x = r0.y * r0.w + r0.x;
    // mul_sat r0.x, r0.x, r0.z
    r0.x = saturate(r0.x * r0.z);
    // max r0.x, r0.x, l(0.000001)
    r0.x = max(r0.x, 1e-06f);
    // log r0.x, r0.x
    r0.x = log2(r0.x);
    // mul r0.x, r0.x, cb0[7].z
    r0.x = r0.x * cb0[7].z;
    // exp r0.x, r0.x
    r0.x = exp2(r0.x);
    // mad r0.x, cb0[7].w, r0.x, cb0[8].y
    r0.x = cb0[7].w * r0.x + cb0[8].y;
    // mul_sat r0.x, r0.x, v3.w
    r0.x = saturate(r0.x * v3.w);
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // add r0.xyz, v3.xyzx, cb0[1].xyzx
    float stage20_x = v3.x + cb0[1].x;
    float stage20_y = v3.y + cb0[1].y;
    float stage20_z = v3.z + cb0[1].z;
    r0.x = stage20_x;
    r0.y = stage20_y;
    r0.z = stage20_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage21_x = r0.x * v5.w + v5.x;
    float stage21_y = r0.y * v5.w + v5.y;
    float stage21_z = r0.z * v5.w + v5.z;
    o0.x = stage21_x;
    o0.y = stage21_y;
    o0.z = stage21_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage22_x = r0.x * v1.x;
    float stage22_y = r0.x * v1.y;
    float stage22_z = r0.x * v1.z;
    r0.x = stage22_x;
    r0.y = stage22_y;
    r0.z = stage22_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage23_x = r0.w * v0.z;
    float stage23_y = r0.w * v0.x;
    float stage23_z = r0.w * v0.y;
    r1.x = stage23_x;
    r1.y = stage23_y;
    r1.z = stage23_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage24_x = r0.x * r1.x;
    float stage24_y = r0.x * r1.y;
    float stage24_z = r0.x * r1.z;
    r0.x = stage24_x;
    r0.y = stage24_y;
    r0.z = stage24_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage25_x = r0.x / r0.w;
    float stage25_y = r0.y / r0.w;
    r0.x = stage25_x;
    r0.y = stage25_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage26_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage26_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage26_x;
    r1.y = stage26_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage27_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage27_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage27_x;
    r1.y = stage27_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage28_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage28_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage28_x;
    r1.y = stage28_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage29_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage29_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage29_x;
    r0.y = stage29_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage30_x = r0.x * 0.5f + 0.5f;
    float stage30_y = r0.y * 0.5f + 0.5f;
    o2.x = stage30_x;
    o2.y = stage30_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage31_z = 1.0f;
    float stage31_w = asfloat(0u);
    o2.z = stage31_z;
    o2.w = stage31_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage32_x = cb2[3].x;
    float stage32_y = cb2[3].y;
    float stage32_z = cb2[3].z;
    o3.x = stage32_x;
    o3.y = stage32_y;
    o3.z = stage32_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage33_x = asfloat(0u);
    float stage33_y = asfloat(0u);
    float stage33_z = asfloat(0u);
    float stage33_w = asfloat(0u);
    o4.x = stage33_x;
    o4.y = stage33_y;
    o4.z = stage33_z;
    o4.w = stage33_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage34_x = asfloat(0u);
    float stage34_y = asfloat(0u);
    float stage34_z = asfloat(0u);
    float stage34_w = asfloat(0u);
    o5.x = stage34_x;
    o5.y = stage34_y;
    o5.z = stage34_z;
    o5.w = stage34_w;

    Shade_Ue3_fx_d_pa_helix_03_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
