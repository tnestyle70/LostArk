// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

cbuffer Shade_Ue3_fx_mm_twirl_05_tr_Constants0 : register(b0)
{
    float4 cb0[10];
};
cbuffer Shade_Ue3_fx_mm_twirl_05_tr_Constants2 : register(b2)
{
    float4 cb2[4];
};
SamplerState s0 : register(s0);
SamplerState s1 : register(s1);
Texture2D t0 : register(t0);
Texture2D t1 : register(t1);

struct Shade_Ue3_fx_mm_twirl_05_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v4;
    float4 v5;
};

struct Shade_Ue3_fx_mm_twirl_05_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_mm_twirl_05_tr_OUTPUT Shade_Ue3_fx_mm_twirl_05_tr(Shade_Ue3_fx_mm_twirl_05_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v4 = stage.v4;
    float4 v5 = stage.v5;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 r2 = (float4)0;
    float4 r3 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // add r0.xy, v2.xyxx, l(-0.500000, -0.500000, 0.000000, 0.000000)
    float stage1_x = v2.x + -0.5f;
    float stage1_y = v2.y + -0.5f;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // add r0.zw, r0.xxxy, r0.xxxy
    float stage2_z = r0.x + r0.x;
    float stage2_w = r0.y + r0.y;
    r0.z = stage2_z;
    r0.w = stage2_w;
    // dp2 r0.x, r0.xyxx, r0.xyxx
    r0.x = dot(r0.xy, r0.xy);
    // sqrt r0.x, r0.x
    r0.x = sqrt(r0.x);
    // max r0.y, |r0.z|, |r0.w|
    r0.y = max(abs(r0.z), abs(r0.w));
    // div r0.y, l(1.000000, 1.000000, 1.000000, 1.000000), r0.y
    r0.y = 1.0f / r0.y;
    // min r1.x, |r0.z|, |r0.w|
    r1.x = min(abs(r0.z), abs(r0.w));
    // mul r0.y, r0.y, r1.x
    r0.y = r0.y * r1.x;
    // mul r1.x, r0.y, r0.y
    r1.x = r0.y * r0.y;
    // mad r1.y, r1.x, l(0.020835), l(-0.085133)
    r1.y = r1.x * 0.020835f + -0.085133f;
    // mad r1.y, r1.x, r1.y, l(0.180141)
    r1.y = r1.x * r1.y + 0.180141f;
    // mad r1.y, r1.x, r1.y, l(-0.330299)
    r1.y = r1.x * r1.y + -0.330299f;
    // mad r1.x, r1.x, r1.y, l(0.999866)
    r1.x = r1.x * r1.y + 0.999866f;
    // mul r1.y, r0.y, r1.x
    r1.y = r0.y * r1.x;
    // mad r1.y, r1.y, l(-2.000000), l(1.570796)
    r1.y = r1.y * -2.0f + 1.570796f;
    // lt r1.z, |r0.z|, |r0.w|
    r1.z = asfloat((abs(r0.z) < abs(r0.w)) ? 0xffffffffu : 0u);
    // and r1.y, r1.z, r1.y
    r1.y = asfloat(asuint(r1.z) & asuint(r1.y));
    // mad r0.y, r0.y, r1.x, r1.y
    r0.y = r0.y * r1.x + r1.y;
    // lt r1.x, r0.z, -r0.z
    r1.x = asfloat((r0.z < -r0.z) ? 0xffffffffu : 0u);
    // and r1.x, r1.x, l(0xc0490fdb)
    r1.x = asfloat(asuint(r1.x) & asuint(-3.1415927410125732f));
    // add r0.y, r0.y, r1.x
    r0.y = r0.y + r1.x;
    // min r1.x, r0.z, r0.w
    r1.x = min(r0.z, r0.w);
    // lt r1.x, r1.x, -r1.x
    r1.x = asfloat((r1.x < -r1.x) ? 0xffffffffu : 0u);
    // max r1.y, r0.z, r0.w
    r1.y = max(r0.z, r0.w);
    // dp2 r0.z, r0.zwzz, r0.zwzz
    r0.z = dot(r0.zw, r0.zw);
    // min r0.z, r0.z, l(1.000000)
    r0.z = min(r0.z, 1.0f);
    // ge r0.w, r1.y, -r1.y
    r0.w = asfloat((r1.y >= -r1.y) ? 0xffffffffu : 0u);
    // and r0.w, r0.w, r1.x
    r0.w = asfloat(asuint(r0.w) & asuint(r1.x));
    // movc r0.y, r0.w, -r0.y, r0.y
    r0.y = asuint(r0.w) != 0u ? -r0.y : r0.y;
    // add r0.w, -r0.x, l(0.500000)
    r0.w = -r0.x + 0.5f;
    // add r0.w, r0.w, r0.w
    r0.w = r0.w + r0.w;
    // mul r1.x, v4.w, cb0[4].z
    r1.x = v4.w * cb0[4].z;
    // mul r0.w, r0.w, r1.x
    r0.w = r0.w * r1.x;
    // log r1.x, |r0.w|
    r1.x = log2(abs(r0.w));
    // lt r0.w, |r0.w|, l(0.000001)
    r0.w = asfloat((abs(r0.w) < 1e-06f) ? 0xffffffffu : 0u);
    // mul r1.x, r1.x, cb0[4].w
    r1.x = r1.x * cb0[4].w;
    // exp r1.x, r1.x
    r1.x = exp2(r1.x);
    // mul r1.x, r1.x, cb0[5].x
    r1.x = r1.x * cb0[5].x;
    // movc r0.w, r0.w, l(0), r1.x
    r0.w = asuint(r0.w) != 0u ? asfloat(0u) : r1.x;
    // mad r1.x, r0.y, l(0.318310), r0.w
    r1.x = r0.y * 0.31831f + r0.w;
    // mul r0.y, cb0[3].y, cb0[3].z
    r0.y = cb0[3].y * cb0[3].z;
    // mul r0.w, r0.y, cb0[5].z
    r0.w = r0.y * cb0[5].z;
    // mad r2.x, cb0[5].w, r1.x, r0.w
    r2.x = cb0[5].w * r1.x + r0.w;
    // add r0.w, r0.x, r0.x
    r0.w = r0.x + r0.x;
    // log r0.w, r0.w
    r0.w = log2(r0.w);
    // mul r0.w, r0.w, v4.z
    r0.w = r0.w * v4.z;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // lt r1.z, r0.x, l(0.000000)
    r1.z = asfloat((r0.x < 0.0f) ? 0xffffffffu : 0u);
    // mad r0.x, -r0.x, cb0[8].y, l(1.000000)
    r0.x = -r0.x * cb0[8].y + 1.0f;
    // mul_sat r0.x, r0.x, cb0[9].y
    r0.x = saturate(r0.x * cb0[9].y);
    // movc r1.y, r1.z, l(0), r0.w
    r1.y = asuint(r1.z) != 0u ? asfloat(0u) : r0.w;
    // mul r0.w, r1.y, cb0[6].x
    r0.w = r1.y * cb0[6].x;
    // mul r1.xy, r1.xyxx, cb0[4].xyxx
    float stage3_x = r1.x * cb0[4].x;
    float stage3_y = r1.y * cb0[4].y;
    r1.x = stage3_x;
    r1.y = stage3_y;
    // mad r2.y, r0.y, cb0[6].y, r0.w
    r2.y = r0.y * cb0[6].y + r0.w;
    // sample_l_indexable(texture2d)(float,float,float,float) r2.xyz, r2.xyxx, t1.xyzw, s1, cb0[3].x
    float4 fetch4 = t1.SampleLevel(s1, r2.xy, cb0[3].x);
    r2.x = fetch4.x;
    r2.y = fetch4.y;
    r2.z = fetch4.z;
    // mad r3.x, r0.y, cb0[3].w, r1.x
    r3.x = r0.y * cb0[3].w + r1.x;
    // mad r3.y, r0.y, cb0[5].y, r1.y
    r3.y = r0.y * cb0[5].y + r1.y;
    // sample_l_indexable(texture2d)(float,float,float,float) r1.xyz, r3.xyxx, t0.xyzw, s0, cb0[3].x
    float4 fetch5 = t0.SampleLevel(s0, r3.xy, cb0[3].x);
    r1.x = fetch5.x;
    r1.y = fetch5.y;
    r1.z = fetch5.z;
    // add r0.y, -r1.x, r2.x
    r0.y = -r1.x + r2.x;
    // mad r0.y, r0.y, l(0.500000), r1.x
    r0.y = r0.y * 0.5f + r1.x;
    // add r0.w, -r0.z, l(1.000000)
    r0.w = -r0.z + 1.0f;
    // mul r0.w, r0.z, r0.w
    r0.w = r0.z * r0.w;
    // mul_sat r0.z, r0.z, cb0[7].y
    r0.z = saturate(r0.z * cb0[7].y);
    // mul r0.y, r0.w, r0.y
    r0.y = r0.w * r0.y;
    // log r0.w, r0.z
    r0.w = log2(r0.z);
    // lt r0.z, r0.z, l(0.000001)
    r0.z = asfloat((r0.z < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.w, r0.w, cb0[7].z
    r0.w = r0.w * cb0[7].z;
    // exp r0.w, r0.w
    r0.w = exp2(r0.w);
    // mul r0.x, r0.x, r0.w
    r0.x = r0.x * r0.w;
    // movc r0.x, r0.z, l(0), r0.x
    r0.x = asuint(r0.z) != 0u ? asfloat(0u) : r0.x;
    // mad_sat r0.x, r0.y, cb0[7].x, r0.x
    r0.x = saturate(r0.y * cb0[7].x + r0.x);
    // log r0.y, r0.x
    r0.y = log2(r0.x);
    // lt r0.x, r0.x, l(0.000001)
    r0.x = asfloat((r0.x < 1e-06f) ? 0xffffffffu : 0u);
    // mul r0.y, r0.y, cb0[9].z
    r0.y = r0.y * cb0[9].z;
    // exp r0.y, r0.y
    r0.y = exp2(r0.y);
    // min r0.y, r0.y, l(1.000000)
    r0.y = min(r0.y, 1.0f);
    // mul_sat r0.y, r0.y, v3.w
    r0.y = saturate(r0.y * v3.w);
    // mul r0.y, r0.y, cb0[0].x
    r0.y = r0.y * cb0[0].x;
    // movc o0.w, r0.x, l(0), r0.y
    o0.w = asuint(r0.x) != 0u ? asfloat(0u) : r0.y;
    // mul r0.xyz, r2.xyzx, r1.xyzx
    float stage6_x = r2.x * r1.x;
    float stage6_y = r2.y * r1.y;
    float stage6_z = r2.z * r1.z;
    r0.x = stage6_x;
    r0.y = stage6_y;
    r0.z = stage6_z;
    // dp3 r0.w, r0.xyzx, l(0.300000, 0.590000, 0.110000, 0.000000)
    r0.w = dot(r0.xyz, float3(0.3f, 0.59f, 0.11f));
    // mad r1.xyz, -r1.xyzx, r2.xyzx, r0.wwww
    float stage7_x = -r1.x * r2.x + r0.w;
    float stage7_y = -r1.y * r2.y + r0.w;
    float stage7_z = -r1.z * r2.z + r0.w;
    r1.x = stage7_x;
    r1.y = stage7_y;
    r1.z = stage7_z;
    // mad r0.xyz, cb0[6].zzzz, r1.xyzx, r0.xyzx
    float stage8_x = cb0[6].z * r1.x + r0.x;
    float stage8_y = cb0[6].z * r1.y + r0.y;
    float stage8_z = cb0[6].z * r1.z + r0.z;
    r0.x = stage8_x;
    r0.y = stage8_y;
    r0.z = stage8_z;
    // max r0.xyz, |r0.xyzx|, l(0.000001, 0.000001, 0.000001, 0.000000)
    float stage9_x = max(abs(r0.x), 1e-06f);
    float stage9_y = max(abs(r0.y), 1e-06f);
    float stage9_z = max(abs(r0.z), 1e-06f);
    r0.x = stage9_x;
    r0.y = stage9_y;
    r0.z = stage9_z;
    // log r0.xyz, r0.xyzx
    float stage10_x = log2(r0.x);
    float stage10_y = log2(r0.y);
    float stage10_z = log2(r0.z);
    r0.x = stage10_x;
    r0.y = stage10_y;
    r0.z = stage10_z;
    // mul r0.xyz, r0.xyzx, cb0[6].wwww
    float stage11_x = r0.x * cb0[6].w;
    float stage11_y = r0.y * cb0[6].w;
    float stage11_z = r0.z * cb0[6].w;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // exp r0.xyz, r0.xyzx
    float stage12_x = exp2(r0.x);
    float stage12_y = exp2(r0.y);
    float stage12_z = exp2(r0.z);
    r0.x = stage12_x;
    r0.y = stage12_y;
    r0.z = stage12_z;
    // mul r1.xyz, cb0[2].xyzx, cb0[2].wwww
    float stage13_x = cb0[2].x * cb0[2].w;
    float stage13_y = cb0[2].y * cb0[2].w;
    float stage13_z = cb0[2].z * cb0[2].w;
    r1.x = stage13_x;
    r1.y = stage13_y;
    r1.z = stage13_z;
    // mul r0.xyz, r0.xyzx, r1.xyzx
    float stage14_x = r0.x * r1.x;
    float stage14_y = r0.y * r1.y;
    float stage14_z = r0.z * r1.z;
    r0.x = stage14_x;
    r0.y = stage14_y;
    r0.z = stage14_z;
    // mad r0.xyz, v3.xyzx, r0.xyzx, cb0[1].xyzx
    float stage15_x = v3.x * r0.x + cb0[1].x;
    float stage15_y = v3.y * r0.y + cb0[1].y;
    float stage15_z = v3.z * r0.z + cb0[1].z;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage16_x = r0.x * v5.w + v5.x;
    float stage16_y = r0.y * v5.w + v5.y;
    float stage16_z = r0.z * v5.w + v5.z;
    o0.x = stage16_x;
    o0.y = stage16_y;
    o0.z = stage16_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage17_x = r0.x * v1.x;
    float stage17_y = r0.x * v1.y;
    float stage17_z = r0.x * v1.z;
    r0.x = stage17_x;
    r0.y = stage17_y;
    r0.z = stage17_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage18_x = r0.w * v0.z;
    float stage18_y = r0.w * v0.x;
    float stage18_z = r0.w * v0.y;
    r1.x = stage18_x;
    r1.y = stage18_y;
    r1.z = stage18_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage19_x = r0.x * r1.x;
    float stage19_y = r0.x * r1.y;
    float stage19_z = r0.x * r1.z;
    r0.x = stage19_x;
    r0.y = stage19_y;
    r0.z = stage19_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage20_x = r0.x / r0.w;
    float stage20_y = r0.y / r0.w;
    r0.x = stage20_x;
    r0.y = stage20_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage21_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage21_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage21_x;
    r1.y = stage21_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage22_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage22_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage22_x;
    r1.y = stage22_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage23_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage23_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage23_x;
    r1.y = stage23_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage24_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage24_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage24_x;
    r0.y = stage24_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage25_x = r0.x * 0.5f + 0.5f;
    float stage25_y = r0.y * 0.5f + 0.5f;
    o2.x = stage25_x;
    o2.y = stage25_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage26_z = 1.0f;
    float stage26_w = asfloat(0u);
    o2.z = stage26_z;
    o2.w = stage26_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage27_x = cb2[3].x;
    float stage27_y = cb2[3].y;
    float stage27_z = cb2[3].z;
    o3.x = stage27_x;
    o3.y = stage27_y;
    o3.z = stage27_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage28_x = asfloat(0u);
    float stage28_y = asfloat(0u);
    float stage28_z = asfloat(0u);
    float stage28_w = asfloat(0u);
    o4.x = stage28_x;
    o4.y = stage28_y;
    o4.z = stage28_z;
    o4.w = stage28_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage29_x = asfloat(0u);
    float stage29_y = asfloat(0u);
    float stage29_z = asfloat(0u);
    float stage29_w = asfloat(0u);
    o5.x = stage29_x;
    o5.y = stage29_y;
    o5.z = stage29_z;
    o5.w = stage29_w;

    Shade_Ue3_fx_mm_twirl_05_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
