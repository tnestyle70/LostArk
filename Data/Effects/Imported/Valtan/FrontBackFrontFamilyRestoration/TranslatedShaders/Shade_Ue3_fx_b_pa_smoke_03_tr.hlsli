// Generated by Tools/EffectPipeline/translate_ue3_dxbc_to_hlsl.py.
// One statement per cooked DXBC instruction.  Do not hand-edit: regenerate.

float4 cb0[4];
float4 cb2[4];
SamplerState s0;
SamplerState s1;
SamplerState s2;
SamplerState s3;
Texture2D t0;
Texture2D t1;
Texture2D t2;
Texture2D t3;

struct Shade_Ue3_fx_b_pa_smoke_03_tr_INPUT
{
    float4 v0;
    float4 v1;
    float4 v2;
    float4 v3;
    float4 v5;
    float4 v7;
};

struct Shade_Ue3_fx_b_pa_smoke_03_tr_OUTPUT
{
    float4 o0;
    float4 o2;
    float4 o3;
    float4 o4;
    float4 o5;
};

Shade_Ue3_fx_b_pa_smoke_03_tr_OUTPUT Shade_Ue3_fx_b_pa_smoke_03_tr(Shade_Ue3_fx_b_pa_smoke_03_tr_INPUT stage)
{
    float4 v0 = stage.v0;
    float4 v1 = stage.v1;
    float4 v2 = stage.v2;
    float4 v3 = stage.v3;
    float4 v5 = stage.v5;
    float4 v7 = stage.v7;
    float4 r0 = (float4)0;
    float4 r1 = (float4)0;
    float4 o0 = (float4)0;
    float4 o2 = (float4)0;
    float4 o3 = (float4)0;
    float4 o4 = (float4)0;
    float4 o5 = (float4)0;

    // div r0.xy, v7.xyxx, v7.wwww
    float stage1_x = v7.x / v7.w;
    float stage1_y = v7.y / v7.w;
    r0.x = stage1_x;
    r0.y = stage1_y;
    // mad r0.xy, r0.xyxx, cb2[0].xyxx, cb2[0].wzww
    float stage2_x = r0.x * cb2[0].x + cb2[0].w;
    float stage2_y = r0.y * cb2[0].y + cb2[0].z;
    r0.x = stage2_x;
    r0.y = stage2_y;
    // sample_l_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t1.xyzw, s0, l(0.000000)
    float4 fetch3 = t1.SampleLevel(s0, r0.xy, 0.0f);
    r0.x = fetch3.x;
    // min r0.x, r0.x, l(0.999000)
    r0.x = min(r0.x, 0.999f);
    // mad r0.y, r0.x, cb2[1].z, -cb2[1].w
    r0.y = r0.x * cb2[1].z + -cb2[1].w;
    // mad r0.x, r0.x, cb2[1].x, cb2[1].y
    r0.x = r0.x * cb2[1].x + cb2[1].y;
    // div r0.y, l(1.000000, 1.000000, 1.000000, 1.000000), r0.y
    r0.y = 1.0f / r0.y;
    // add r0.x, r0.y, r0.x
    r0.x = r0.y + r0.x;
    // add r0.x, r0.x, -v7.w
    r0.x = r0.x + -v7.w;
    // mul_sat r0.x, r0.x, l(0.010000)
    r0.x = saturate(r0.x * 0.01f);
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, v2.xyxx, t0.yxzw, s3, l(0.000000)
    float4 fetch4 = t0.SampleBias(s3, v2.xy, 0.0f);
    r0.y = fetch4.x;
    // mul_sat r0.y, r0.y, l(2.500000)
    r0.y = saturate(r0.y * 2.5f);
    // mul r0.y, r0.y, v3.w
    r0.y = r0.y * v3.w;
    // mul r0.x, r0.x, r0.y
    r0.x = r0.x * r0.y;
    // mul o0.w, r0.x, cb0[0].x
    o0.w = r0.x * cb0[0].x;
    // add r0.xy, v2.xyxx, cb0[2].xyxx
    float stage5_x = v2.x + cb0[2].x;
    float stage5_y = v2.y + cb0[2].y;
    r0.x = stage5_x;
    r0.y = stage5_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.x, r0.xyxx, t2.xyzw, s1, l(0.000000)
    float4 fetch6 = t2.SampleBias(s1, r0.xy, 0.0f);
    r0.x = fetch6.x;
    // add r0.yz, v2.xxyx, cb0[3].xxyx
    float stage7_y = v2.x + cb0[3].x;
    float stage7_z = v2.y + cb0[3].y;
    r0.y = stage7_y;
    r0.z = stage7_z;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.y, r0.yzyy, t2.xyzw, s1, l(0.000000)
    float4 fetch8 = t2.SampleBias(s1, r0.yz, 0.0f);
    r0.y = fetch8.y;
    // add r0.x, r0.y, r0.x
    r0.x = r0.y + r0.x;
    // mad r0.xy, r0.xxxx, l(0.100000, 0.100000, 0.000000, 0.000000), v2.xyxx
    float stage9_x = r0.x * 0.1f + v2.x;
    float stage9_y = r0.x * 0.1f + v2.y;
    r0.x = stage9_x;
    r0.y = stage9_y;
    // sample_b_indexable(texture2d)(float,float,float,float) r0.xyz, r0.xyxx, t3.xyzw, s2, l(0.000000)
    float4 fetch10 = t3.SampleBias(s2, r0.xy, 0.0f);
    r0.x = fetch10.x;
    r0.y = fetch10.y;
    r0.z = fetch10.z;
    // mad r0.xyz, r0.xyzx, v3.xyzx, cb0[1].xyzx
    float stage11_x = r0.x * v3.x + cb0[1].x;
    float stage11_y = r0.y * v3.y + cb0[1].y;
    float stage11_z = r0.z * v3.z + cb0[1].z;
    r0.x = stage11_x;
    r0.y = stage11_y;
    r0.z = stage11_z;
    // mad o0.xyz, r0.xyzx, v5.wwww, v5.xyzx
    float stage12_x = r0.x * v5.w + v5.x;
    float stage12_y = r0.y * v5.w + v5.y;
    float stage12_z = r0.z * v5.w + v5.z;
    o0.x = stage12_x;
    o0.y = stage12_y;
    o0.z = stage12_z;
    // dp3 r0.x, v1.xyzx, v1.xyzx
    r0.x = dot(v1.xyz, v1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, v1.xyzx
    float stage13_x = r0.x * v1.x;
    float stage13_y = r0.x * v1.y;
    float stage13_z = r0.x * v1.z;
    r0.x = stage13_x;
    r0.y = stage13_y;
    r0.z = stage13_z;
    // dp3 r0.w, v0.xyzx, v0.xyzx
    r0.w = dot(v0.xyz, v0.xyz);
    // rsq r0.w, r0.w
    r0.w = rsqrt(r0.w);
    // mul r1.xyz, r0.wwww, v0.zxyz
    float stage14_x = r0.w * v0.z;
    float stage14_y = r0.w * v0.x;
    float stage14_z = r0.w * v0.y;
    r1.x = stage14_x;
    r1.y = stage14_y;
    r1.z = stage14_z;
    // mul r0.y, r0.y, r1.y
    r0.y = r0.y * r1.y;
    // mad r0.x, r0.x, r1.z, -r0.y
    r0.x = r0.x * r1.z + -r0.y;
    // mov r1.z, r0.z
    r1.z = r0.z;
    // mul r1.y, r0.x, v1.w
    r1.y = r0.x * v1.w;
    // dp3 r0.x, r1.xyzx, r1.xyzx
    r0.x = dot(r1.xyz, r1.xyz);
    // rsq r0.x, r0.x
    r0.x = rsqrt(r0.x);
    // mul r0.xyz, r0.xxxx, r1.xyzx
    float stage15_x = r0.x * r1.x;
    float stage15_y = r0.x * r1.y;
    float stage15_z = r0.x * r1.z;
    r0.x = stage15_x;
    r0.y = stage15_y;
    r0.z = stage15_z;
    // dp3 r0.w, l(1.000000, 1.000000, 1.000000, 0.000000), |r0.xyzx|
    r0.w = dot(float3(1.0f, 1.0f, 1.0f), abs(r0.xyz));
    // div r0.xy, r0.xyxx, r0.wwww
    float stage16_x = r0.x / r0.w;
    float stage16_y = r0.y / r0.w;
    r0.x = stage16_x;
    r0.y = stage16_y;
    // ge r0.z, l(0.000000), r0.z
    r0.z = asfloat((0.0f >= r0.z) ? 0xffffffffu : 0u);
    // ge r1.xy, r0.xyxx, l(0.000000, 0.000000, 0.000000, 0.000000)
    float stage17_x = asfloat((r0.x >= 0.0f) ? 0xffffffffu : 0u);
    float stage17_y = asfloat((r0.y >= 0.0f) ? 0xffffffffu : 0u);
    r1.x = stage17_x;
    r1.y = stage17_y;
    // movc r1.xy, r1.xyxx, l(1.000000,1.000000,0,0), l(-1.000000,-1.000000,0,0)
    float stage18_x = asuint(r1.x) != 0u ? 1.0f : -1.0f;
    float stage18_y = asuint(r1.y) != 0u ? 1.0f : -1.0f;
    r1.x = stage18_x;
    r1.y = stage18_y;
    // mad r1.xy, -|r0.yxyy|, r1.xyxx, r1.xyxx
    float stage19_x = -(abs(r0.y)) * r1.x + r1.x;
    float stage19_y = -(abs(r0.x)) * r1.y + r1.y;
    r1.x = stage19_x;
    r1.y = stage19_y;
    // movc r0.xy, r0.zzzz, r1.xyxx, r0.xyxx
    float stage20_x = asuint(r0.z) != 0u ? r1.x : r0.x;
    float stage20_y = asuint(r0.z) != 0u ? r1.y : r0.y;
    r0.x = stage20_x;
    r0.y = stage20_y;
    // mad o2.xy, r0.xyxx, l(0.500000, 0.500000, 0.000000, 0.000000), l(0.500000, 0.500000, 0.000000, 0.000000)
    float stage21_x = r0.x * 0.5f + 0.5f;
    float stage21_y = r0.y * 0.5f + 0.5f;
    o2.x = stage21_x;
    o2.y = stage21_y;
    // mov o2.zw, l(0,0,1.000000,0)
    float stage22_z = 1.0f;
    float stage22_w = asfloat(0u);
    o2.z = stage22_z;
    o2.w = stage22_w;
    // mov o3.xyz, cb2[3].xyzx
    float stage23_x = cb2[3].x;
    float stage23_y = cb2[3].y;
    float stage23_z = cb2[3].z;
    o3.x = stage23_x;
    o3.y = stage23_y;
    o3.z = stage23_z;
    // mov o3.w, l(0)
    o3.w = asfloat(0u);
    // mov o4.xyzw, l(0,0,0,0)
    float stage24_x = asfloat(0u);
    float stage24_y = asfloat(0u);
    float stage24_z = asfloat(0u);
    float stage24_w = asfloat(0u);
    o4.x = stage24_x;
    o4.y = stage24_y;
    o4.z = stage24_z;
    o4.w = stage24_w;
    // mov o5.xyzw, l(0,0,0,0)
    float stage25_x = asfloat(0u);
    float stage25_y = asfloat(0u);
    float stage25_z = asfloat(0u);
    float stage25_w = asfloat(0u);
    o5.x = stage25_x;
    o5.y = stage25_y;
    o5.z = stage25_z;
    o5.w = stage25_w;

    Shade_Ue3_fx_b_pa_smoke_03_tr_OUTPUT result;
    result.o0 = o0;
    result.o2 = o2;
    result.o3 = o3;
    result.o4 = o4;
    result.o5 = o5;
    return result;
}
